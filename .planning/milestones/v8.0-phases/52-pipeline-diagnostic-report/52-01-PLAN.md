---
phase: 52-pipeline-diagnostic-report
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/_pipeline.py
  - python/fdars/advisor/__init__.py
  - tests/test_pipeline_report.py
autonomous: true
requirements: [PIPE-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "build_pipeline_report() aggregates >=2 stages into a LIST of per-stage {stage, aspect, diagnostics} blocks (PIPE-01)"
    - "Aggregation preserves caller-declared stage order exactly"
    - "Aggregation is NEVER a flat {**a,**b} merge — same-keyed diagnostics across stages are never collided/dropped (PIPE-01)"
    - "The union-grounding payload wrapper collects every stage's numbers via _flatten_diagnostics_numbers (mirrors Phase-51 {\"_candidates\":[...]} → {\"_stages\":[...]})"
    - "_pipeline.py imports no anthropic/provider package at module load (offline-deterministic core)"
  artifacts:
    - python/fdars/advisor/_pipeline.py
    - tests/test_pipeline_report.py
  key_links:
    - "build_pipeline_report is exported from fdars.advisor.__init__ __all__"
    - "each stage entry runs build_diagnostics per stage OR accepts a precomputed diagnostics dict (has 'method' key)"
    - "the {\"_stages\":[...]} union wrapper feeds _flatten_diagnostics_numbers so a fabricated number is catchable and every real cross-stage number is present"
---

<objective>
TRACER: deliver the end-to-end offline aggregation core of the pipeline diagnostic report — `build_pipeline_report()` that takes an ordered list of stage entries, runs `build_diagnostics` per stage (or accepts precomputed dicts), and aggregates them as a LIST of per-stage labeled blocks with correct per-stage provenance, plus the union-grounding payload wrapper (`{"_stages":[...]}`) that later plans' grounding check relies on. This is the thinnest path that touches every layer the phase modifies (stage input → per-stage build_diagnostics → list-of-blocks aggregation → union payload) and proves per-stage isolation before narration, caveats, and MCP are built on top.

Purpose: Per-stage isolation is the load-bearing property the auto-tuning capstone (Phase 53) reuses; provenance must never collapse. Proving the 2-stage aggregation + union wrapper end-to-end first catches an architectural dead-end after one commit instead of after all layers ship.
Output: `python/fdars/advisor/_pipeline.py` (offline core), export in `__init__.py`, offline test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-pipeline-diagnostic-report/52-CONTEXT.md

# The direct Phase-51 analog this plan mirrors (union-grounding + per-block provenance):
@python/fdars/advisor/_compare_methods.py
@.planning/phases/51-comparative-method-selection/51-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Cumulative across Phase 52 (this plan delivers the first three items):
- `build_pipeline_report()` — offline aggregation core (this plan, PIPE-01)
- Per-stage list-of-blocks structure `[{stage, aspect, diagnostics}]` (this plan, PIPE-01)
- `{"_stages":[...]}` union-grounding payload wrapper (this plan, PIPE-01)
- `pipeline_report()` — narrative path (Plan 02, PIPE-02)
- "pipeline" advise task family key + system prompt (Plan 02, PIPE-02)
- `PipelineReport` schema (Plan 02, PIPE-02)
- cross-stage caveat rule constants + rule table (Plan 02, PIPE-03)
- `fdars_build_pipeline_report` MCP tool + `mcp/_pipeline.py` helper (Plan 03, PIPE-04)
- test files: `tests/test_pipeline_report.py` (this plan), `tests/test_pipeline_report_advise.py` (Plan 02), `tests/test_mcp_pipeline_report.py` (Plan 03)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: build_pipeline_report() offline aggregation core (end-to-end tracer)</name>
  <files>python/fdars/advisor/_pipeline.py</files>
  <read_first>
    - python/fdars/advisor/_compare_methods.py — the direct analog: `_normalize_candidates` (dual-input: precomputed diagnostics dict identified by a "method" key vs raw result dict → build_diagnostics), the `{"_candidates":[...]}` union payload construction, and the deferred `from fdars.advisor import build_diagnostics` local import that keeps the module LLM-free
    - python/fdars/advisor/__init__.py — `build_diagnostics(result, method, *, argvals=None, ...)` signature and the list of supported aspect strings (_supported set)
  </read_first>
  <behavior>
    - build_pipeline_report on 2 stages returns a dict whose "stages" value is a LIST of exactly 2 blocks, each {stage, aspect, diagnostics}, in caller-declared order
    - a stage entry carrying a precomputed diagnostics dict (has "method" key) is passed through without re-running build_diagnostics
    - a stage entry carrying a raw result dict is run through build_diagnostics(result, aspect, argvals=...) once
    - two stages that share a same-named diagnostic key (e.g. both have "n_components") both survive — neither value is dropped (proves no flat-merge)
    - the union payload built from the stages is a dict {"_stages": [<diag>, <diag>]}; running it through _flatten_diagnostics_numbers yields numbers from BOTH stages
    - build_pipeline_report(run_llm=False) returns the aggregate offline with no anthropic import
  </behavior>
  <action>Create `python/fdars/advisor/_pipeline.py`. Define `build_pipeline_report(stages, *, argvals=None, run_llm=True, domain_context="", model="claude-opus-4-8", provider=None, **kwargs)` where `stages` is an ordered list of stage entries — each entry a dict `{"stage_name": str, "aspect": str, "result": <raw result or precomputed diagnostics dict>}` (accept the diagnostics under key "diagnostics", "result", or "value" for caller convenience, mirroring `_normalize_candidates`). Implement a `_normalize_stages(stages, argvals, **kwargs)` helper that, per stage IN CALLER ORDER, detects a precomputed diagnostics dict by the presence of a "method" key and passes it through unchanged, otherwise calls `build_diagnostics(result, aspect, argvals=argvals, **kwargs)` via a DEFERRED local import (`from fdars.advisor import build_diagnostics`) so the module stays LLM-free at load. Return per-stage blocks as a LIST `[{"stage": stage_name, "aspect": aspect, "diagnostics": diag}, ...]` — NEVER `{**a, **b}`. Define `_build_stages_union(blocks)` returning `{"_stages": [b["diagnostics"] for b in blocks]}` (the exact analog of the Phase-51 `{"_candidates":[...]}` wrapper) so `_flatten_diagnostics_numbers` recurses into the list and collects every stage's numbers without key-collision loss. `build_pipeline_report` assembles `result = {"stages": blocks}` and, when `run_llm=False`, returns `result` immediately (narrative path is Plan 02's hook — raise `NotImplementedError` for `run_llm=True` here, exactly as Phase-51 Plan 01 deferred its LLM path). Raise `ValueError` on an empty stages list and on a stage entry missing "stage_name" or "aspect". Do NOT flat-merge diagnostics anywhere. Do NOT add any new `build_diagnostics` method/aspect key.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_report.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pipeline_report.py` exits 0
    - the aggregated report's "stages" value is a list of per-stage blocks, not a flat dict (assert type is list; assert two same-keyed stage diagnostics both retrievable)
    - the `{"_stages":[...]}` union payload run through `_flatten_diagnostics_numbers` contains numbers from every stage
    - `build_pipeline_report(run_llm=False)` returns offline with no anthropic import required
    - stage order in the output equals caller-declared order
  </acceptance_criteria>
  <done>build_pipeline_report() aggregates >=2 stages into an ordered list of {stage, aspect, diagnostics} blocks with no flat-merge, and the union wrapper collects all stages' numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Export build_pipeline_report from advisor package</name>
  <files>python/fdars/advisor/__init__.py</files>
  <read_first>
    - python/fdars/advisor/__init__.py — the existing `__all__` list and the `from fdars.advisor._compare_methods import compare_methods  # noqa: E402` re-export line (mirror this exact style; the import must stay side-effect-free / LLM-free)
  </read_first>
  <action>Add `"build_pipeline_report"` to `__all__` in `python/fdars/advisor/__init__.py` and add `from fdars.advisor._pipeline import build_pipeline_report  # noqa: E402` next to the existing `compare_methods` re-export. Do NOT touch `build_diagnostics._supported` or any `_DIAGNOSTICS_METHODS` — guard-sync stays a no-op (no new method slot).</action>
  <verify>
    <automated>.venv/bin/python -c "from fdars.advisor import build_pipeline_report; import fdars.advisor as a; assert 'build_pipeline_report' in a.__all__"</automated>
  </verify>
  <acceptance_criteria>
    - `from fdars.advisor import build_pipeline_report` succeeds
    - `build_pipeline_report` is in `fdars.advisor.__all__`
    - importing `fdars.advisor` does not import anthropic (module load stays LLM-free)
  </acceptance_criteria>
  <done>build_pipeline_report is importable via the public `fdars.advisor` path and listed in __all__.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline test suite for aggregation + provenance + union payload</name>
  <files>tests/test_pipeline_report.py</files>
  <read_first>
    - tests/test_compare_methods.py — the analog offline test suite: determinism assertions, dual-input (precomputed vs raw) tests, `test_core_is_llm_free` checking column-0 (module-level) imports only, `test_full_suite_offline_no_api_key`
    - python/fdars/advisor/providers/_validate.py — `_flatten_diagnostics_numbers` (what the union payload must feed) and `GroundingViolationError`
  </read_first>
  <behavior>
    - test_stages_is_list_of_blocks — output "stages" is a list; each element has stage, aspect, diagnostics
    - test_stage_order_preserved — caller order is preserved in output
    - test_no_flat_merge_same_key_survives — two stages both carrying "n_components" both retrievable (neither dropped)
    - test_precomputed_diagnostics_passthrough — a stage with a dict having a "method" key is not re-run
    - test_raw_result_runs_build_diagnostics — a raw result stage produces diagnostics with a "method" key
    - test_union_payload_collects_all_stage_numbers — `_flatten_diagnostics_numbers({"_stages":[...]})` contains numbers from every stage
    - test_empty_stages_raises — ValueError on empty stages
    - test_core_is_llm_free — no module-level anthropic/providers import in `_pipeline.py`
    - test_full_suite_offline_no_api_key — the whole file runs with no ANTHROPIC_API_KEY
  </behavior>
  <action>Create `tests/test_pipeline_report.py` with offline tests mirroring `tests/test_compare_methods.py`. Build small synthetic stage fixtures at Claude's discretion — a represent stage (raw data dict `{"data": ..., "argvals": ...}` for the "represent" aspect) followed by an fpca stage (a precomputed fpca diagnostics dict with a "method" key, or a raw fpca result). Assert: the output "stages" is a LIST of `{stage, aspect, diagnostics}` blocks; caller order preserved; two stages sharing a diagnostic key both survive (construct two precomputed diagnostics dicts that both contain the same key with different values and assert both values are present in the two blocks — proving no flat-merge); a precomputed diagnostics dict (identified by "method") is passed through unchanged; the `{"_stages":[...]}` union payload fed to `_flatten_diagnostics_numbers` (import from `fdars.advisor.providers._validate`) contains values from every stage; empty stages raises ValueError. Add `test_core_is_llm_free` checking only column-0 imports of `_pipeline.py` (mirror the Phase-51 fix: use `line.startswith(...)` NOT `line.strip().startswith(...)` so deferred local imports in the run_llm branch are not flagged). Add `test_full_suite_offline_no_api_key`.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_report.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pipeline_report.py` exits 0
    - the full suite (`.venv/bin/python -m pytest -q`) shows no regressions
    - `test_core_is_llm_free` passes (no module-level LLM import in `_pipeline.py`)
    - `test_no_flat_merge_same_key_survives` asserts both same-keyed stage values survive
  </acceptance_criteria>
  <done>Offline test suite proves the aggregation is a per-stage list of blocks (no flat-merge), preserves order, feeds the union payload correctly, and the core is LLM-free; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage input → build_diagnostics | caller-supplied result/diagnostics dicts cross into the aggregator; unvalidated shapes reach fdars builders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Information Disclosure | provenance collapse via flat-merge in `_pipeline.py` aggregation | high | mitigate | aggregate as a LIST of per-stage blocks; test_no_flat_merge_same_key_survives asserts same-keyed values are never dropped (PIPE-01) |
| T-52-02 | Tampering | fabricated cross-stage number ungroundable due to key-collision in the union payload | high | mitigate | build `{"_stages":[...]}` wrapper so `_flatten_diagnostics_numbers` recurses the list and collects every stage's numbers (mirrors Phase-51 GROUND-03) |
| T-52-03 | Elevation of Privilege | LLM SDK imported at module load in the offline core | medium | mitigate | defer `build_diagnostics` import; test_core_is_llm_free asserts no module-level anthropic/providers import |
| T-52-SC | Tampering | npm/pip/cargo installs | high | accept | no new packages installed this plan (zero new runtime deps per RESEARCH.md); no install tasks present |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_pipeline_report.py -q` passes
- `.venv/bin/python -m pytest -q` (full suite) shows no regressions
- `from fdars.advisor import build_pipeline_report` succeeds and is in `__all__`
- aggregate is a list of per-stage blocks; the `{"_stages":[...]}` union payload collects every stage's numbers
- no new `_DIAGNOSTICS_METHODS` / `build_diagnostics._supported` key (guard-sync no-op — confirm empty diff on those tokens)
</verification>

<success_criteria>
build_pipeline_report() aggregates >=2 stages into an ordered per-stage list of {stage, aspect, diagnostics} blocks with no flat-merge, exposes a `{"_stages":[...]}` union payload that collects every stage's numbers, is exported LLM-free from `fdars.advisor`, and the full test suite stays green.
</success_criteria>

<output>
Create `.planning/phases/52-pipeline-diagnostic-report/52-01-SUMMARY.md` when done.
</output>

---
phase: 52-pipeline-diagnostic-report
plan: "02"
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - python/fdars/advisor/_pipeline.py
  - python/fdars/advisor/_prompts.py
  - python/fdars/advisor/_schema.py
  - tests/test_pipeline_report_advise.py
autonomous: true
requirements: [PIPE-02, PIPE-03]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A deterministic Python caveat rule table computes cross-stage caveats from real diagnostics — the LLM never invents them (PIPE-03)"
    - "Caveat rules: high represent imputed_fraction → FPCA/clustering reliability caveat; high outlier count/fraction → downstream caveat; low cumulative variance explained → clustering caveat"
    - "Caveat thresholds are documented module constants, overridable via params, with conservative defaults (PIPE-03)"
    - "pipeline_report() produces a grounded narrative via a new 'pipeline' advise task family with a PipelineReport schema (PIPE-02)"
    - "Grounding is a UNION check across all stages ({\"_stages\":[...]}) — fabrication is caught; real cross-stage numbers pass — with per-stage attribution preserved in the PROMPT (not flat-merged, no per-stage-strict over-rejection)"
  artifacts:
    - python/fdars/advisor/_schema.py
    - tests/test_pipeline_report_advise.py
  key_links:
    - "'pipeline' added to _supported_tasks in _system_prompt() with a narration-only, per-stage-attribution task clause"
    - "PipelineReport schema (stages + narrative + caveats) validated like Advice, with pydantic-absent fallback stand-in"
    - "caveats are Python-computed structured items surfaced in the result and included in the LLM prompt for narration; the LLM narrates but does not generate them"
---

<objective>
Build the narrative + caveats layer on top of the proven aggregation tracer: (1) a DETERMINISTIC Python cross-stage caveat rule table (PIPE-03) computed from the real per-stage diagnostics, (2) a `PipelineReport` schema (PIPE-02), and (3) the `pipeline_report()` LLM path using a new "pipeline" advise task family that narrates the per-stage report and Python-computed caveats under UNION grounding across all stages.

Purpose: Cross-stage caveats MUST be deterministic Python functions of real fdars numbers (grounded) — the LLM narrates them, never generates them. Union grounding (mirroring Phase-51 WR-03) catches fabrication while preserving per-stage prompt provenance and avoiding the per-stage-strict over-rejection lesson.
Output: caveat rule table + constants in `_pipeline.py`, `PipelineReport` in `_schema.py`, "pipeline" clause in `_prompts.py`, offline + env-gated live tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-pipeline-diagnostic-report/52-CONTEXT.md

@python/fdars/advisor/_compare_methods.py
@.planning/phases/51-comparative-method-selection/51-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds: `pipeline_report()`, the "pipeline" task-family key + system prompt clause, the `PipelineReport` schema (fields: stages, narrative, caveats), the caveat rule table + threshold constants. See Plan 01 SUMMARY for the aggregation core and 01/03 for the MCP tool.
Caveat constant names (Claude's discretion, documented, conservative defaults): e.g. `_IMPUTED_FRACTION_CAVEAT_THRESHOLD`, `_OUTLIER_FRACTION_CAVEAT_THRESHOLD`, `_LOW_CUMULATIVE_VARIANCE_THRESHOLD`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic cross-stage caveat rule table (PIPE-03)</name>
  <files>python/fdars/advisor/_pipeline.py</files>
  <read_first>
    - python/fdars/advisor/_pipeline.py — the aggregation core from Plan 01 (the list-of-blocks structure the rules read from)
    - python/fdars/advisor/aspects/represent.py — the real key names `imputed_fraction`, `imputation_mae` (represent aspect)
    - python/fdars/advisor/aspects/outliers.py — the real key names `n_outliers`, `outlier_fraction`, `n_union_outliers` (outliers aspect)
    - python/fdars/advisor/aspects/fpca.py — the real key names `n_components`, `cumulative_variance_explained` (fpca aspect; cumulative variance is a list — use the last element)
  </read_first>
  <behavior>
    - a stages list where the represent block has imputed_fraction above the threshold yields exactly one caveat mentioning FPCA/clustering reliability, carrying the real imputed_fraction value and the source stage name
    - a stages list where an outliers block has outlier_fraction (or n_outliers) above threshold yields a downstream caveat carrying the real value
    - a stages list where an fpca block has last cumulative_variance_explained below threshold yields a clustering caveat carrying the real value
    - a stages list with all values below thresholds yields zero caveats
    - overriding a threshold via a param changes which caveats fire
    - every caveat's cited numeric value equals a real diagnostic value present in some stage (grounded)
  </behavior>
  <action>In `python/fdars/advisor/_pipeline.py` add documented module-level threshold constants with conservative defaults (names at Claude's discretion, e.g. `_IMPUTED_FRACTION_CAVEAT_THRESHOLD = 0.2`, `_OUTLIER_FRACTION_CAVEAT_THRESHOLD`, `_LOW_CUMULATIVE_VARIANCE_THRESHOLD`) and a `_compute_cross_stage_caveats(blocks, *, thresholds=None)` function. The function iterates the per-stage blocks (never a flat-merged dict), applies a small DETERMINISTIC rule table on the REAL diagnostics: (rule 1) a represent-aspect block with `imputed_fraction` above the imputed-fraction threshold emits a structured caveat about FPCA/clustering reliability; (rule 2) an outliers-aspect block with `outlier_fraction` (fall back to `n_outliers`/`n_union_outliers` when fraction absent) above the outlier threshold emits a downstream caveat; (rule 3) an fpca-aspect block whose LAST `cumulative_variance_explained` element is below the low-variance threshold emits a clustering caveat. Each caveat is a structured dict `{"stage": <source stage>, "aspect": <aspect>, "rule": <rule id str>, "value": <real diagnostic float/int>, "message": <plain-language caveat text>}` — the numeric `value` must be the real fdars-computed scalar so it grounds. Accept a `thresholds` dict param to override any default (conservative defaults documented in the constant docstrings). The LLM is NOT involved — this is pure Python on real numbers. Emit native `float`/`int` only (no numpy scalars).</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_report_advise.py -x -q -k caveat</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pipeline_report_advise.py -k caveat` exits 0
    - a high imputed_fraction represent stage produces an FPCA/clustering caveat carrying the real value
    - all-below-threshold stages produce zero caveats
    - a threshold override via param changes which caveats fire
    - caveat numeric values are native float/int and equal real per-stage diagnostics (grounded)
  </acceptance_criteria>
  <done>A deterministic Python rule table computes structured cross-stage caveats from real per-stage diagnostics with documented, overridable, conservative thresholds; the LLM is never consulted to produce them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PipelineReport schema (PIPE-02)</name>
  <files>python/fdars/advisor/_schema.py</files>
  <read_first>
    - python/fdars/advisor/_schema.py — the `Advice`/`Recommendation` pattern: pydantic `BaseModel` path AND the `except ImportError` plain-class fallback stand-in (mirror BOTH for PipelineReport so importing the module offline without the [advisor] extra still succeeds)
  </read_first>
  <behavior>
    - PipelineReport validates with stages (per-stage summary list), narrative (str), and caveats (list) fields
    - when pydantic is absent, a PipelineReport stand-in class with the same fields exists and is constructible
    - PipelineReport is importable from fdars.advisor._schema
  </behavior>
  <action>In `python/fdars/advisor/_schema.py` add a `PipelineReport` model INSIDE both the pydantic `try` branch (as a `_PydanticBaseModel` subclass) and the `except ImportError` fallback branch (as a plain class with `__init__`, `__repr__`, `__eq__` mirroring the `Advice` stand-in). Fields (names at Claude's discretion, documented): a per-stage summary list `stages` (list of per-stage narrative sections — model each as a nested model or a list of str), an overall `narrative` str, and a DISTINCT structured `caveats` list (the cross-stage caveats field, separate from any per-stage text). Keep it schema-validated like `Advice` so `complete_structured(PipelineReport, ...)` can use it as the output_format. Do not change `Advice` or `Recommendation`.</action>
  <verify>
    <automated>.venv/bin/python -c "from fdars.advisor._schema import PipelineReport; print(PipelineReport)"</automated>
  </verify>
  <acceptance_criteria>
    - `from fdars.advisor._schema import PipelineReport` succeeds
    - PipelineReport has stages, narrative, and caveats fields
    - the module still imports with pydantic absent (fallback stand-in exists)
    - `Advice` and `Recommendation` are unchanged (equality-assert or diff-check)
  </acceptance_criteria>
  <done>A PipelineReport schema (stages + narrative + caveats) exists in both the pydantic and pydantic-absent paths, validated like Advice, without altering existing schemas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 'pipeline' task family + pipeline_report() narrative path with union grounding (PIPE-02)</name>
  <files>python/fdars/advisor/_prompts.py, python/fdars/advisor/_pipeline.py, tests/test_pipeline_report_advise.py</files>
  <read_first>
    - python/fdars/advisor/_prompts.py — the `_supported_tasks` set and the "comparison" task clause added in Phase 51 (mirror its narration-only, per-block-attribution wording for the new "pipeline" clause); the `_GROUNDING_INVARIANT` embedding
    - python/fdars/advisor/_compare_methods.py — the `run_llm=True` branch: deferred provider/schema/_check_grounding imports, per-block labeled provenance payload sent to the LLM, and the `{"_candidates":[...]}` UNION grounding check (mirror exactly with `{"_stages":[...]}`)
    - python/fdars/advisor/providers/_validate.py — `_check_grounding(advice, diagnostics)` and how the union payload is consumed
    - tests/test_compare_methods_advise.py — the offline mock-narration tests + the env-gated `test_live_comparison_narration` skip pattern
  </read_first>
  <behavior>
    - _system_prompt("pipeline", aspect=...) returns a prompt containing the grounding invariant and a narration-only, per-stage-attribution clause; the four pre-existing task prompts (interpretation/parameter/method/comparison) are unchanged
    - pipeline_report(run_llm=True) computes stages + Python caveats BEFORE the LLM call, sends per-stage labeled blocks (never flat-merged) plus the structured caveats to the LLM, and runs _check_grounding ONCE against the {"_stages":[...]} union
    - a mock narration citing a number present in stage B's real diagnostics passes even when narrating stage A context (union grounding — no per-stage-strict over-rejection)
    - a mock narration citing a fabricated number (in no stage) raises GroundingViolationError
    - the returned result carries the Python-computed caveats (LLM did not invent them)
    - test_live_pipeline_narration is collected and SKIPPED without ANTHROPIC_API_KEY
  </behavior>
  <action>In `_prompts.py`: add `"pipeline"` to `_supported_tasks` and a "pipeline" task clause instructing the model to narrate the per-stage report and the SUPPLIED (Python-computed) cross-stage caveats, referencing each stage by its explicit stage name, citing only values present in the supplied diagnostics, and never inventing caveats — the caveats are already decided by fdars-side computation (mirror the "comparison" clause's narration-only, attribution-preserving wording). Leave the other four clauses byte-identical. In `_pipeline.py`: implement the `run_llm=True` branch of `build_pipeline_report` OR add a dedicated `pipeline_report(stages, *, argvals=None, domain_context="", model="claude-opus-4-8", provider=None, thresholds=None, **kwargs)` entry point (Claude's discretion — keep the offline core reusable). The narrative path: (1) normalize stages to blocks (Plan 01 core), (2) compute caveats via `_compute_cross_stage_caveats` BEFORE any LLM call, (3) deferred-import provider/schema/_check_grounding, (4) build a user message carrying per-stage LABELED blocks `[{stage, aspect, diagnostics}]` (NEVER flat-merged) plus the structured caveats for narration, (5) call `p.complete_structured(PipelineReport, messages, system)` with `system = _system_prompt("pipeline")`, (6) run `_check_grounding(report, {"_stages": [b["diagnostics"] for b in blocks]})` ONCE against the union — do NOT run per-stage-strict checks (the Phase-51 WR-03 over-rejection lesson), (7) attach the Python-computed caveats to the returned result so the caveats are authoritative regardless of narration. Export `pipeline_report` from `__init__.py` if a separate entry point is chosen (add to `__all__` + re-export, same LLM-free style). Add offline + env-gated live tests to `tests/test_pipeline_report_advise.py`.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_report_advise.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pipeline_report_advise.py` exits 0
    - the four pre-existing task prompts are byte-unchanged (equality-asserted)
    - a fabricated number (in no stage) raises GroundingViolationError; a real cross-stage number (present in some stage) passes under the union check
    - the returned result's caveats are the Python-computed structured items (not LLM-generated)
    - `test_live_pipeline_narration` is skipped cleanly without ANTHROPIC_API_KEY
    - full suite (`.venv/bin/python -m pytest -q`) shows no regressions
  </acceptance_criteria>
  <done>pipeline_report() narrates the per-stage report + Python-computed caveats via the 'pipeline' task family under union grounding; fabrication is caught, real cross-stage numbers pass, caveats stay Python-authoritative, and the four existing prompts are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caveats → LLM prompt | Python-computed caveats cross into LLM narration; the LLM must narrate but never author caveats |
| LLM narration → grounding check | model output crosses back; fabricated numbers must be rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-04 | Tampering | LLM inventing a caveat not derived from real diagnostics | high | mitigate | caveats computed by `_compute_cross_stage_caveats` (deterministic Python) BEFORE the LLM call and attached authoritatively to the result; the "pipeline" prompt clause forbids inventing caveats (PIPE-03) |
| T-52-05 | Tampering | fabricated numeric citation in narration | high | mitigate | `_check_grounding` against the `{"_stages":[...]}` union catches any number absent from every stage (PIPE-02) |
| T-52-06 | Denial of Service | per-stage-strict grounding over-rejects legitimate cross-stage narration | medium | mitigate | use ONE union check, not per-stage-strict (Phase-51 WR-03 lesson); test asserts a real stage-B number passes while narrating stage-A |
| T-52-07 | Elevation of Privilege | LLM SDK imported at module load | medium | mitigate | provider/schema imports deferred into the run_llm branch; module load stays LLM-free |
| T-52-SC | Tampering | npm/pip/cargo installs | high | accept | no new packages installed this plan (zero new runtime deps per RESEARCH.md); no install tasks present |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_pipeline_report_advise.py -q` passes
- `.venv/bin/python -m pytest -q` (full suite) shows no regressions
- the four pre-existing task prompts are byte-unchanged
- caveats are Python-computed structured items with documented, overridable, conservative thresholds
- union grounding: fabrication raises GroundingViolationError; real cross-stage numbers pass
- no new `_DIAGNOSTICS_METHODS` / `build_diagnostics._supported` key (guard-sync no-op)
</verification>

<success_criteria>
Cross-stage caveats are deterministic Python functions of real per-stage diagnostics (LLM narrates, never invents); `pipeline_report()` produces a grounded PipelineReport narrative via the new "pipeline" task family under union grounding that catches fabrication and passes real cross-stage numbers; existing prompts and schemas are untouched; full suite green.
</success_criteria>

<output>
Create `.planning/phases/52-pipeline-diagnostic-report/52-02-SUMMARY.md` when done.
</output>

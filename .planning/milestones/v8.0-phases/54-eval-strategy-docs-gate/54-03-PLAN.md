---
phase: 54-eval-strategy-docs-gate
plan: "03"
type: execute
wave: 2
depends_on: ["54-02"]
files_modified:
  - docs/advisor/comparative-selection.md
  - docs/advisor/pipeline-report.md
  - docs/advisor/auto-tuning.md
  - docs/advisor/aspects.md
  - mkdocs.yml
autonomous: true
requirements: [DOCS-01, DOCS-02]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Three new mature-structure pages exist under docs/advisor/ (comparative selection, pipeline report, auto-tuning), each embedding its Plan-02 SVG and carrying a runnable offline FDARS_FENCE_OK worked example on small synthetic data."
    - "The auto-tuning page worked example uses the offline/injectable path (FakeProvider + seams / heuristic) — no network and no API key in the docs build."
    - "docs/advisor/aspects.md is updated to document the three deferred-aspect scalars shipped in Phase 50 (PACE-FPCA noise/signal + truncated-rank + band-width; ITP detection+localisation; elastic-multinomial overfitting_gap + n_classes_flag)."
    - "The three new pages are wired into the AI Advisor nav in mkdocs.yml; the site nav resolves them."
  artifacts:
    - docs/advisor/comparative-selection.md
    - docs/advisor/pipeline-report.md
    - docs/advisor/auto-tuning.md
    - docs/advisor/aspects.md
    - mkdocs.yml
  key_links:
    - "each new page embeds ../assets/diagrams/advisor-{comparative-selection,pipeline-report,auto-tuning}.svg produced by Plan 02"
    - "each new page's worked-example fence prints FDARS_FENCE_OK and runs offline (auto-tune uses injectable propose_fn / FakeProvider, no key)"
    - "mkdocs.yml AI Advisor nav lists the three new pages"
---

<objective>
Author the three new mature-structure documentation pages for the v8.0 capabilities (comparative method-selection, pipeline diagnostic report, closed-loop auto-tuning), each embedding its Plan-02 SVG and carrying a runnable offline `FDARS_FENCE_OK` worked example on small synthetic data; update `docs/advisor/aspects.md` for the three deferred-aspect scalars shipped in Phase 50; and wire the three new pages into the AI Advisor nav in `mkdocs.yml`. This plan depends on Plan 02 (the SVGs) and runs sequentially on main (fences build against the real .venv tree).

Purpose: DOCS-01 (pages + embedded diagrams + aspects update) and DOCS-02 (offline worked examples). Each page follows the mature-page template — intro / method / worked example / parameters / caveats — and is method-accurate against the shipped 50–53 code.
Output: three new pages, the aspects.md update, and the nav wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Mature-page template + fence + SVG-embed conventions to mirror
@docs/advisor/python-api.md
@docs/represent/pace-fpca.md
@scripts/docs_fig.py

# Method-accuracy ground truth (shipped API + semantics)
@python/fdars/advisor/_compare_methods.py
@python/fdars/advisor/_pipeline.py
@python/fdars/advisor/__init__.py
@.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-02-SUMMARY.md
@docs/advisor/aspects.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: comparative-selection.md + pipeline-report.md pages (embed SVGs, offline fences)</name>
  <files>docs/advisor/comparative-selection.md, docs/advisor/pipeline-report.md</files>
  <read_first>
    - docs/advisor/python-api.md — the mature-page structure to mirror (worked example, functions, schema sections) and the SVG-embed markdown (`![alt](../assets/diagrams/NAME.svg){ .fdars-diagram }`).
    - docs/represent/pace-fpca.md — the exact executed-fence header (```python exec="1" html="1" source="above"), the `fast()` helper import from `docs_fig`, and the trailing `print("FDARS_FENCE_OK")` sentinel.
    - python/fdars/advisor/_compare_methods.py — `compare_methods()` signature + accepted candidate forms + families + `run_llm` semantics (fdars-computed winner).
    - python/fdars/advisor/_pipeline.py — `build_pipeline_report()` / `pipeline_report()` signatures, per-stage blocks, `_compute_cross_stage_caveats` rules; and .planning/phases/52-pipeline-diagnostic-report/52-02-SUMMARY.md for caveat semantics.
  </read_first>
  <action>Create `docs/advisor/comparative-selection.md` and `docs/advisor/pipeline-report.md`, each following the mature-page template (intro / method / worked example / parameters / caveats). Embed the matching Plan-02 SVG near the top via `![descriptive alt](../assets/diagrams/advisor-comparative-selection.svg){ .fdars-diagram }` and `![...](../assets/diagrams/advisor-pipeline-report.svg){ .fdars-diagram }`. Each page carries ONE runnable offline worked example in a ```python exec="1" html="1" source="above" fence that: builds small synthetic data (n <= 20), calls the offline path (`compare_methods(..., run_llm=False)` for the comparison page; `build_pipeline_report(..., run_llm=False)` for the pipeline page), prints a concise grounded result, and ends with `print("FDARS_FENCE_OK")`. Use the `fast()` helper from `docs_fig` for any sizes so DOCS_FAST keeps the fence cheap. Document the fdars-authoritative-winner invariant (comparison) and per-stage provenance + Python-computed caveats (pipeline) accurately. Do NOT wire nav here (Task 3 owns mkdocs.yml).</action>
  <verify>
    <automated>test -f docs/advisor/comparative-selection.md && test -f docs/advisor/pipeline-report.md && grep -q 'FDARS_FENCE_OK' docs/advisor/comparative-selection.md && grep -q 'FDARS_FENCE_OK' docs/advisor/pipeline-report.md && grep -q 'advisor-comparative-selection.svg' docs/advisor/comparative-selection.md && grep -q 'advisor-pipeline-report.svg' docs/advisor/pipeline-report.md && grep -q 'run_llm=False' docs/advisor/comparative-selection.md</automated>
  </verify>
  <acceptance_criteria>
    - Both pages exist, follow the intro/method/worked-example/parameters/caveats structure, and embed their Plan-02 SVG with `{ .fdars-diagram }`.
    - Each page has exactly one executed fence ending in `print("FDARS_FENCE_OK")` using the offline path (`run_llm=False`) on small synthetic data.
    - The comparison page documents the fdars-computed winner (LLM narrates only); the pipeline page documents per-stage provenance + Python-computed caveats.
  </acceptance_criteria>
  <done>The comparative-selection and pipeline-report pages exist, embed their SVGs, and carry offline FDARS_FENCE_OK worked examples on the offline path.</done>
</task>

<task type="auto">
  <name>Task 2: auto-tuning.md page + aspects.md deferred-scalar update</name>
  <files>docs/advisor/auto-tuning.md, docs/advisor/aspects.md</files>
  <read_first>
    - docs/advisor/python-api.md + docs/represent/pace-fpca.md — mature-page template, SVG embed, executed-fence header + `FDARS_FENCE_OK` sentinel + `fast()` helper.
    - python/fdars/advisor/__init__.py — `auto_tune()` signature; the `provider`, `_run_method`, `_build_diagnostics` seams and the grounding-invariant numeric boundary (parameter_delta only); and .planning/phases/53-closed-loop-auto-tuning-capstone/53-02-SUMMARY.md for the offline FakeProvider path.
    - python/fdars/mcp/_tuning.py — the heuristic (LLM-free) propose_fn as an alternative offline injectable path for the doc example.
    - .planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-02-SUMMARY.md — the exact new deferred-aspect scalars to document: PACE-FPCA (`pace_noise_signal_ratio`, `pace_truncated_rank_flagged`, `pace_mean_prediction_band_width`); ITP (`itp_min_adjusted_pvalue`, `itp_detected_at_0.05`, `itp_n_significant_0.05`, `itp_fraction_significant_0.05`, `itp_first_significant_basis` — detection AND localisation together); elastic-multinomial (`overfitting_gap`, `n_classes_flagged`).
    - docs/advisor/aspects.md — the existing fpca / classification / inference sections and the table format to extend (do NOT rewrite existing rows; append the new scalars).
  </read_first>
  <action>Create `docs/advisor/auto-tuning.md` following the mature-page template, embedding `![...](../assets/diagrams/advisor-auto-tuning.svg){ .fdars-diagram }`. Its worked example MUST use the OFFLINE/injectable path — either `auto_tune(...)` with an injected offline `provider=FakeProvider` + `_run_method`/`_build_diagnostics` seams, OR the heuristic (LLM-free) propose_fn from `mcp/_tuning.py` — so the docs build needs NO network and NO API key. Build small synthetic data (n <= 20), run a bounded loop (small `max_steps`), print the resulting TuneResult stop_reason + improved flag, and end with `print("FDARS_FENCE_OK")`. Document bounded termination, the schema-validated numeric parameter_delta boundary, and the Goodhart guard accurately. Separately, update `docs/advisor/aspects.md` to document the three deferred-aspect scalars shipped in Phase 50: extend the fpca section with the three PACE-FPCA scalars, the inference/ITP section with the detection+localisation scalars (emphasising they are cited together — whether vs where), and the classification section with `overfitting_gap` + `n_classes_flagged` (noting `overfitting_gap` is None unless a holdout_accuracy is supplied). Append rows to the existing tables; keep existing content intact.</action>
  <verify>
    <automated>test -f docs/advisor/auto-tuning.md && grep -q 'FDARS_FENCE_OK' docs/advisor/auto-tuning.md && grep -q 'advisor-auto-tuning.svg' docs/advisor/auto-tuning.md && ! grep -q 'ANTHROPIC_API_KEY' docs/advisor/auto-tuning.md && grep -q 'pace_noise_signal_ratio' docs/advisor/aspects.md && grep -q 'itp_min_adjusted_pvalue' docs/advisor/aspects.md && grep -q 'overfitting_gap' docs/advisor/aspects.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/advisor/auto-tuning.md` exists, embeds `advisor-auto-tuning.svg`, and its fence ends in `print("FDARS_FENCE_OK")` using the offline/injectable path (no ANTHROPIC_API_KEY, no network).
    - `docs/advisor/aspects.md` now documents `pace_noise_signal_ratio` / `pace_truncated_rank_flagged` / `pace_mean_prediction_band_width`, the ITP detection+localisation scalars, and `overfitting_gap` / `n_classes_flagged` — with existing rows preserved.
    - The auto-tune page documents bounded termination + the parameter_delta grounding boundary.
  </acceptance_criteria>
  <done>The auto-tuning page exists with an offline worked example, and aspects.md documents the three Phase-50 deferred-aspect scalar families.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the three new pages into the AI Advisor nav (mkdocs.yml)</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (lines around the `AI Advisor:` nav section, ~151–157) — the existing entries (index, Python API, Provider Setup, Per-Aspect Coverage, MCP Server, Agent Skill) and the `Label: advisor/page.md` nav-entry format + indentation.
  </read_first>
  <action>Edit the `AI Advisor:` nav section in `mkdocs.yml` to add the three new pages with descriptive labels: `Comparative Selection: advisor/comparative-selection.md`, `Pipeline Report: advisor/pipeline-report.md`, and `Auto-Tuning: advisor/auto-tuning.md`. Place them in a sensible order among the existing advisor entries (e.g. after Python API, before/around Per-Aspect Coverage), matching the existing two-space-nested list indentation exactly. Do not touch any other nav section.</action>
  <verify>
    <automated>grep -q 'advisor/comparative-selection.md' mkdocs.yml && grep -q 'advisor/pipeline-report.md' mkdocs.yml && grep -q 'advisor/auto-tuning.md' mkdocs.yml && .venv/bin/python -c "import yaml,io; yaml.safe_load(open('mkdocs.yml').read().replace('!!python/name:','')) if False else None; print('nav-entries-present')"</automated>
  </verify>
  <acceptance_criteria>
    - `mkdocs.yml` AI Advisor nav lists `advisor/comparative-selection.md`, `advisor/pipeline-report.md`, and `advisor/auto-tuning.md`.
    - Indentation matches the existing nav entries; no other nav section is modified.
  </acceptance_criteria>
  <done>The three new pages are wired into the AI Advisor nav in mkdocs.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs fence → network | An executed fence that reaches the network (e.g. a real LLM call) would break the offline build and require an API key. |
| page prose/diagram → reader | Method-inaccurate prose or a wrong embedded diagram misleads readers; caught by the DOCS-03 human review + strict build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54C-01 | Denial of Service | auto-tune fence | high | mitigate | Auto-tune example uses the offline/injectable path (FakeProvider + seams or heuristic propose_fn); `! grep ANTHROPIC_API_KEY` asserted; no network in the docs build. |
| T-54C-02 | Denial of Service | executed fences | medium | mitigate | All fence data small (n <= 20) and use `fast()` so DOCS_FAST keeps the ~19–25 min whole-site build tractable. |
| T-54C-03 | Tampering | aspects.md edit | medium | mitigate | Append-only to existing tables; existing rows preserved; new scalars taken verbatim from the 50-02 SUMMARY. |
| T-54C-SC | Tampering | pip/npm installs | low | accept | No package installs (docs authoring against existing .venv); no legitimacy checkpoint required. |
</threat_model>

<verification>
- Three new pages exist under `docs/advisor/`, each embedding its Plan-02 SVG and ending its fence in `print("FDARS_FENCE_OK")`.
- The auto-tune fence uses the offline/injectable path (no `ANTHROPIC_API_KEY`, no network).
- `docs/advisor/aspects.md` documents the three Phase-50 deferred-aspect scalar families with existing content preserved.
- `mkdocs.yml` AI Advisor nav lists the three new pages.
- (Whole-site `mkdocs build --strict` green is verified in Plan 04, not here.)
</verification>

<success_criteria>
Three method-accurate new pages with embedded SVGs and offline `FDARS_FENCE_OK` worked examples, the aspects.md deferred-scalar update, and nav wiring — DOCS-01 and DOCS-02 satisfied (the whole-site strict build + human review is Plan 04).
</success_criteria>

<output>
Create `.planning/phases/54-eval-strategy-docs-gate/54-03-SUMMARY.md` when done.
</output>

---
phase: 54-eval-strategy-docs-gate
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/advisor-comparative-selection.svg
  - docs/assets/diagrams/advisor-pipeline-report.svg
  - docs/assets/diagrams/advisor-auto-tuning.svg
autonomous: true
requirements: [DOCS-01]
estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Three new hand-authored inline SVGs exist, each method-accurate against the shipped 50–53 code (compare_methods, build_pipeline_report/pipeline_report, auto_tune)."
    - "Each SVG conforms to STYLE_SPEC: viewBox width 720, canonical five-class <style> block, role=img + aria-label matching the title, fill=none root."
    - "Each SVG passes the SVGO idempotence gate under svgo.config.mjs (svgo(svgo(svg)) == svgo(svg))."
  artifacts:
    - docs/assets/diagrams/advisor-comparative-selection.svg
    - docs/assets/diagrams/advisor-pipeline-report.svg
    - docs/assets/diagrams/advisor-auto-tuning.svg
  key_links:
    - "comparison diagram depicts N candidates through build_diagnostics then a fdars-computed deterministic sort picking the winner while the LLM narrates only (winner NOT LLM-chosen)"
    - "pipeline diagram depicts represent to smooth to cluster/regress to monitor per-stage blocks (never flat-merged), Python-computed cross-stage caveats, grounded narrative"
    - "auto-tune diagram depicts propose (parameter_delta) to apply/clamp to re-run fdars to compare target vs budget to iterate, with bounded termination and a Goodhart guard"
---

<objective>
Author the three new method-accurate hand-authored inline SVG diagrams for the v8.0 agentic capabilities, STYLE_SPEC-conformant and SVGO-idempotent. These are the DOCS-01 diagrams the new pages (Plan 03) embed. This plan produces ONLY the SVG files — no page prose, no nav — so its files stay disjoint from Plans 01/03/04 (sequential-on-main, same-wave file disjointness).

Purpose: Method-accuracy is validated by human review on the built site (DOCS-03), so each diagram must faithfully depict what the shipped code actually does — the grounding invariant (fdars computes; LLM interprets), the fdars-authoritative winner, per-stage provenance, and the bounded auto-tune loop.
Output: `advisor-comparative-selection.svg`, `advisor-pipeline-report.svg`, `advisor-auto-tuning.svg` under `docs/assets/diagrams/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The style contract every new SVG must follow verbatim
@docs/assets/diagrams/STYLE_SPEC.md

# Conforming analog to mirror structure/classes/aria exactly
@docs/assets/diagrams/advisor-loop.svg

# Method-accuracy ground truth (what each diagram must depict)
@.planning/phases/51-comparative-method-selection/51-02-SUMMARY.md
@.planning/phases/52-pipeline-diagnostic-report/52-02-SUMMARY.md
@.planning/phases/53-closed-loop-auto-tuning-capstone/53-01-SUMMARY.md
@.planning/phases/53-closed-loop-auto-tuning-capstone/53-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: advisor-comparative-selection.svg — fdars-authoritative ranking flow</name>
  <files>docs/assets/diagrams/advisor-comparative-selection.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — viewBox (720 width; height 300/480/520), canonical five-class `<style>` block copied verbatim, colour palette, stroke weights, `role="img"` + `aria-label` matching the title, method/process orange-accent panel pattern.
    - docs/assets/diagrams/advisor-loop.svg — a conforming advisor diagram to mirror for structure, class usage, title/subtitle positions (y≈26 / y≈44), and aria-label wording.
    - .planning/phases/51-comparative-method-selection/51-02-SUMMARY.md — the exact semantics: N labeled candidates through build_diagnostics each, then a deterministic fdars sort fixes the winner BEFORE the LLM call and the LLM narrates only; per-candidate labeled provenance blocks (never flat-merged); the LLM cannot override the winner (COMPARE-01/02).
  </read_first>
  <action>Create `docs/assets/diagrams/advisor-comparative-selection.svg` depicting the comparison ranking flow method-accurately: several labeled candidate methods each entering build_diagnostics (per-candidate labeled blocks, shown as distinct blocks, never merged), converging into a deterministic fdars-computed sort that selects the winner, with the LLM shown in a separate orange-accent process panel labelled as narration-only that does not choose the winner. Make the fdars-authoritative-winner point visually unambiguous. Copy the canonical five-class `<style>` block verbatim, use viewBox width 720 (height 300 unless a two-row layout is needed, then 480), set `role="img"` and an `aria-label` that matches the diagram title text. Use the structural and data-curve palette from STYLE_SPEC. Hand-author the XML directly.</action>
  <verify>
    <automated>test -f docs/assets/diagrams/advisor-comparative-selection.svg && P1=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/advisor-comparative-selection.svg --output -) && P2=$(printf '%s' "$P1" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && [ "$P1" = "$P2" ] && printf '%s' "$P1" | grep -q 'role="img"' && printf '%s' "$P1" | grep -q 'viewBox="0 0 720'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/assets/diagrams/advisor-comparative-selection.svg` exists and passes the SVGO idempotence check (pass1 == pass2) under svgo.config.mjs.
    - Root `<svg>` has `viewBox="0 0 720 {300|480|520}"`, `fill="none"`, `role="img"`, and an `aria-label` matching the title text.
    - The canonical five-class `<style>` block (`.ttl .sub .lab .sm .mono`) is present verbatim.
    - The diagram shows the winner selected by the fdars-computed sort with the LLM in a narration-only role (method-accurate to COMPARE-01).
  </acceptance_criteria>
  <done>A STYLE_SPEC-conformant, SVGO-idempotent comparison-ranking SVG exists depicting fdars-authoritative winner selection and LLM narration-only.</done>
</task>

<task type="auto">
  <name>Task 2: advisor-pipeline-report.svg — per-stage aggregation with provenance + caveats</name>
  <files>docs/assets/diagrams/advisor-pipeline-report.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — same conformance contract (viewBox 720, five-class `<style>`, role/aria, palette, panels); a taller viewBox (480) is allowed for a multi-stage two-row layout.
    - docs/assets/diagrams/advisor-loop.svg — conforming structure/class analog.
    - .planning/phases/52-pipeline-diagnostic-report/52-02-SUMMARY.md — the exact semantics: per-stage diagnostic blocks (represent → smooth → cluster/regress → monitor) kept as a LIST of labeled blocks (NEVER flat-merged); `_compute_cross_stage_caveats` computes caveats in Python BEFORE the LLM call (R1 imputed_fraction, R2 outlier fraction, R3 low cumulative_variance); union grounding once against `{"_stages":[...]}`; the LLM narrates but cannot invent caveats.
  </read_first>
  <action>Create `docs/assets/diagrams/advisor-pipeline-report.svg` depicting the pipeline stage-aggregation method-accurately: the four ordered stages (represent → smooth → cluster/regress → monitor) each as a distinct labeled per-stage diagnostics block (visually per-stage, never merged into one dict), an arrow showing Python-computed cross-stage caveats derived from earlier stages flowing to downstream stages (e.g. high imputed fraction flags an FPCA caveat), and a final grounded narrative panel with the LLM in an orange-accent narration-only role that cannot invent caveats. Copy the five-class `<style>` block verbatim; use viewBox width 720 (height 480 if a two-row layout best fits the four stages + caveats); set `role="img"` + matching `aria-label`. Hand-author the XML directly.</action>
  <verify>
    <automated>test -f docs/assets/diagrams/advisor-pipeline-report.svg && P1=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/advisor-pipeline-report.svg --output -) && P2=$(printf '%s' "$P1" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && [ "$P1" = "$P2" ] && printf '%s' "$P1" | grep -q 'role="img"' && printf '%s' "$P1" | grep -q 'viewBox="0 0 720'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/assets/diagrams/advisor-pipeline-report.svg` exists and passes the SVGO idempotence check under svgo.config.mjs.
    - Root `<svg>` conforms (viewBox 720 width, `fill="none"`, `role="img"`, matching `aria-label`); five-class `<style>` block present verbatim.
    - The diagram shows per-stage blocks (not flat-merged), Python-computed cross-stage caveats, and LLM narration-only (method-accurate to PIPE-01/02/03).
  </acceptance_criteria>
  <done>A STYLE_SPEC-conformant, SVGO-idempotent pipeline-aggregation SVG exists depicting per-stage provenance, cross-stage caveats, and grounded narration.</done>
</task>

<task type="auto">
  <name>Task 3: advisor-auto-tuning.svg — closed-loop propose→apply→re-run→compare</name>
  <files>docs/assets/diagrams/advisor-auto-tuning.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — conformance contract; the existing advisor-loop.svg is the closest cyclic-diagram analog for a loop layout.
    - docs/assets/diagrams/advisor-loop.svg — mirror the cyclic arrow layout, class usage, and aria-label wording.
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-01-SUMMARY.md — loop core semantics: budget-first termination, 5 bounded stop reasons (budget / converged / oscillation / guard_stop / parse_failure), Goodhart guard runs AFTER the fdars re-run.
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-02-SUMMARY.md — the LLM proposes a SINGLE change via schema-validated numeric `parameter_delta` (never prose); new_value is CLAMPED to range; the LLM never sets a number directly in the numeric path.
  </read_first>
  <action>Create `docs/assets/diagrams/advisor-auto-tuning.svg` depicting the closed-loop tuning cycle method-accurately: propose (LLM emits a schema-validated numeric parameter_delta, shown as the ONLY LLM numeric contribution) → apply/clamp the new_value to the declared range → re-run fdars → compare target metric vs budget/convergence → iterate; annotate the bounded termination conditions (budget, converged, oscillation, guard_stop, parse_failure) and show the Goodhart guard running after the fdars re-run. Keep the LLM proposal in an orange-accent process panel and make clear fdars computes the numbers. Copy the five-class `<style>` block verbatim; viewBox width 720 (height 300 or 480 as layout needs); `role="img"` + matching `aria-label`. Hand-author the XML directly.</action>
  <verify>
    <automated>test -f docs/assets/diagrams/advisor-auto-tuning.svg && P1=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/advisor-auto-tuning.svg --output -) && P2=$(printf '%s' "$P1" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && [ "$P1" = "$P2" ] && printf '%s' "$P1" | grep -q 'role="img"' && printf '%s' "$P1" | grep -q 'viewBox="0 0 720'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/assets/diagrams/advisor-auto-tuning.svg` exists and passes the SVGO idempotence check under svgo.config.mjs.
    - Root `<svg>` conforms (viewBox 720 width, `fill="none"`, `role="img"`, matching `aria-label`); five-class `<style>` block present verbatim.
    - The diagram shows propose (parameter_delta) → apply/clamp → re-run fdars → compare → iterate, with bounded termination and Goodhart guard (method-accurate to TUNE-02/03/05).
  </acceptance_criteria>
  <done>A STYLE_SPEC-conformant, SVGO-idempotent auto-tune-loop SVG exists depicting the bounded propose→apply→re-run→compare cycle with schema-validated numeric proposals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored SVG → built site | A method-inaccurate diagram is the primary risk — it misleads readers and is only caught by the DOCS-03 human review. |
| SVG → SVGO gate | Non-idempotent SVG structure would fail the build-determinism gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54B-01 | Tampering | diagram method-accuracy | high | mitigate | Each SVG authored against the shipped 50–53 SUMMARY semantics; final blocking human diagram review in Plan 04 (DOCS-03) is the last line of defense. |
| T-54B-02 | Denial of Service | SVGO idempotence | medium | mitigate | Per-file idempotence check in each task `<verify>`; copy the canonical `<style>` block verbatim to avoid inlineStyles mangling. |
| T-54B-SC | Tampering | npm svgo invocation | low | accept | `npx svgo@3.3.4` is the pinned, already-in-use gate tool (STYLE_SPEC); no new package added to the project. |
</threat_model>

<verification>
- All three SVGs exist under `docs/assets/diagrams/` with the expected filenames.
- Each passes the SVGO idempotence gate under `svgo.config.mjs` (`svgo(svgo(svg)) == svgo(svg)`).
- Each has viewBox width 720, `role="img"`, an `aria-label` matching its title, and the canonical five-class `<style>` block.
- Each depicts its capability method-accurately per the shipped 50–53 semantics (to be confirmed by the Plan 04 human review).
</verification>

<success_criteria>
Three method-accurate, STYLE_SPEC-conformant, SVGO-idempotent SVGs exist for comparative selection, pipeline report, and auto-tuning; DOCS-01 diagram deliverable satisfied (pages that embed them come in Plan 03; human accuracy review in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/54-eval-strategy-docs-gate/54-02-SUMMARY.md` when done.
</output>

---
phase: 53-closed-loop-auto-tuning-capstone
plan: "03"
type: execute
wave: 3
depends_on: ["53-01"]
files_modified:
  - python/fdars/mcp/_tuning.py
  - python/fdars/mcp/server.py
  - tests/test_mcp_tuning.py
autonomous: true
requirements: [TUNE-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "fdars_auto_tune MCP tool drives the wave-1 loop core with a DETERMINISTIC heuristic propose_fn (gradient-sign line search + bisection); it never calls advise() and never imports the advisor LLM path (TUNE-04)"
    - "A file-scan test proves mcp/_tuning.py and the fdars_auto_tune handler never reference advise (LLM-free invariant) (TUNE-04)"
    - "The heuristic rounds n_basis/n_comp/k to integers and steps lambda_ on a log scale; the same dataset + same initial params gives a byte-identical result (determinism) (TUNE-04)"
    - "fdars_auto_tune enforces max_steps default 10 and a hard cap of 20 (ValueError above 20) and validates method against _RUNNABLE_METHODS at the tool boundary (TUNE-04)"
    - "fdars_auto_tune returns by-reference: only a trace handle + compact scalar summary cross the MCP boundary (no arrays) (TUNE-04)"
    - "Adding fdars_auto_tune is a guard-sync no-op: _RUNNABLE_METHODS (6) and _DIAGNOSTICS_METHODS (14) are unchanged (guard-sync no-op)"
  artifacts:
    - python/fdars/mcp/_tuning.py
    - python/fdars/mcp/server.py
    - tests/test_mcp_tuning.py
  key_links:
    - "fdars_auto_tune delegates entirely to run_tuning_loop_mcp; no loop logic inlined in server.py (Single Responsibility)"
    - "run_tuning_loop_mcp builds a heuristic propose_fn and passes it to advisor._tuning.run_tuning_loop (shared core, one loop)"
    - "mcp/_tuning.py imports nothing from fdars.advisor._prompts / providers / advise — heuristic is self-contained"
    - "max_steps hard cap 20 enforced at the tool boundary with ValueError"
---

<objective>
MCP tool + heuristic: expose fdars_auto_tune as the agentic surface, driving the SAME wave-1 loop core (advisor._tuning.run_tuning_loop) with a deterministic, LLM-free heuristic propose_fn (gradient-sign line search + bisection; integer rounding for n_basis/n_comp/k; log-scale factor for lambda_). The tool is provably LLM-free — it never imports or calls advise() — validated by a file-scan test, and returns by-reference (per D: TUNE-04).

Purpose: The MCP boundary must stay provably LLM-free (a hard milestone constraint). This plan mirrors the Phase-51/52 LLM-free MCP tool pattern (fdars_compare_methods / fdars_build_pipeline_report) exactly: thin @mcp.tool handler validates input and delegates to a standalone helper that never touches advise().
Output: mcp/_tuning.py (heuristic propose_fn + run_tuning_loop_mcp), fdars_auto_tune @mcp.tool in server.py, and tests/test_mcp_tuning.py covering heuristic determinism, LLM-free scan, max_steps cap, by-reference, and guard-sync no-op.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md

@python/fdars/mcp/_compare_methods.py
@python/fdars/mcp/server.py
@.planning/phases/51-comparative-method-selection/51-03-SUMMARY.md
@.planning/phases/53-closed-loop-auto-tuning-capstone/53-01-PLAN.md
</context>

<artifacts>
## Artifacts this phase produces (cumulative — this plan's slice)

- _heuristic_step(current_params, history, param_spec) + _make_heuristic_propose_fn(param_spec) in python/fdars/mcp/_tuning.py (gradient-sign + bisection; int rounding; log-scale lambda_)
- run_tuning_loop_mcp(dataset_id, method, initial_params, *, target_metric=None, max_steps=10) -> dict (by-reference) in python/fdars/mcp/_tuning.py
- fdars_auto_tune @mcp.tool (flat scalar schema lambda_/n_basis/n_comp/k/seed; max_steps default 10 / hard cap 20; method validated against _RUNNABLE_METHODS) in python/fdars/mcp/server.py
- Tests: tests/test_mcp_tuning.py (heuristic determinism, LLM-free scan, max_steps cap, by-reference, guard-sync no-op)
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Heuristic propose_fn + run_tuning_loop_mcp helper (LLM-free)</name>
  <files>python/fdars/mcp/_tuning.py</files>
  <read_first>
    - python/fdars/mcp/_compare_methods.py (analog — the LLM-free MCP helper pattern: Python 3.10 guard, deferred imports, allowlist, by-reference return via registry.store_result, and the deferred import of run_method/build_diagnostics)
    - python/fdars/advisor/_tuning.py (run_tuning_loop signature + _PARAM_REGISTRY — from Plan 01; this helper builds a propose_fn and calls the shared core)
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md Pattern 3 (heuristic gradient-sign + bisection step rule, log-scale lambda_, integer rounding) and Common Pitfall 6 (never import advise)
  </read_first>
  <behavior>
    - _heuristic_step with empty history takes a coarse step in the improving direction (log-scale factor for lambda_, (hi-lo)/10 additive step for integer params)
    - with history, direction continues on improvement and reverses otherwise; step size halves (bisection) on each direction reversal; minimum step 1 for int params
    - lambda_ is stepped by a multiplicative factor (log scale), not additively; the value is clamped to range; integer params are round-then-int
    - run_tuning_loop_mcp resolves the param_spec from advisor._tuning._PARAM_REGISTRY, builds the heuristic propose_fn, calls advisor._tuning.run_tuning_loop, stores the full trace via registry.store_result, and returns a compact by-reference dict (trace handle + scalar summary, no arrays)
    - the module imports nothing from fdars.advisor._prompts, providers, or advise
  </behavior>
  <action>Create python/fdars/mcp/_tuning.py mirroring _compare_methods.py structure: the Python 3.10 sys.version_info guard raising ImportError, __all__ listing run_tuning_loop_mcp, and deferred imports inside the function. Implement _heuristic_step(current_params, history, param_spec) exactly per RESEARCH Pattern 3: empty-history coarse step in the improving direction; with-history direction continue/reverse with bisection-style halving keyed off the count of accepted/rejected reversals; multiplicative log-scale factor for log_scale params (lambda_) with a minimum factor floor; additive step for linear params with a minimum step (1 for int); clamp to param_spec range; round-then-int for integer params. Implement _make_heuristic_propose_fn(param_spec) returning a closure over _heuristic_step. Implement run_tuning_loop_mcp(dataset_id, method, initial_params, *, target_metric=None, max_steps=10): deferred-import advisor._tuning (_PARAM_REGISTRY, run_tuning_loop), resolve spec and raise ValueError for tuneable=False methods, resolve target_metric default from spec, resolve guard_thresholds from spec['guard_metrics'], build the heuristic propose_fn, call run_tuning_loop, then store the full trace via registry.store_result and return {trace_id, method, param, target_metric, stop_reason, n_steps, steps_used, budget_remaining, initial_target_value, final_target_value, improved} — scalars + a handle only, NO arrays (by-reference invariant). Keep the module LLM-free: import run_method/build_diagnostics/registry lazily and never import anything from the advisor LLM path.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -c "import sys; sys.version_info[:2] >= (3,10) or sys.exit(0); from fdars.mcp._tuning import run_tuning_loop_mcp, _heuristic_step, _make_heuristic_propose_fn; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - the import one-liner exits 0 (helper + heuristic importable on 3.10+; no-op on 3.9)
    - grep -v '^#' python/fdars/mcp/_tuning.py | grep -c "advise" returns 0 (LLM-free helper; construct any needed test token at runtime elsewhere)
    - _heuristic_step rounds integer params to int and steps lambda_ multiplicatively
    - run_tuning_loop_mcp returns a dict of scalars + a trace handle (no arrays)
  </acceptance_criteria>
  <done>mcp/_tuning.py provides a deterministic LLM-free heuristic propose_fn and a run_tuning_loop_mcp helper that delegates to the shared advisor._tuning loop core and returns by-reference; the module references nothing from the advisor LLM path.</done>
</task>

<task type="auto">
  <name>Task 2: fdars_auto_tune @mcp.tool (validation + max_steps cap + delegation)</name>
  <files>python/fdars/mcp/server.py</files>
  <read_first>
    - python/fdars/mcp/server.py (the file being modified — study fdars_compare_methods and fdars_build_pipeline_report @mcp.tool handlers: flat scalar schema, method validated against _RUNNABLE_METHODS at the boundary, 3-line delegation to a helper, guard-sync no-op docstring note)
    - python/fdars/mcp/_tuning.py (run_tuning_loop_mcp signature — from Task 1)
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md MCP tool skeleton (fdars_auto_tune flat scalar params, max_steps hard cap 20)
  </read_first>
  <action>Add fdars_auto_tune as a synchronous @mcp.tool() handler in server.py mirroring fdars_compare_methods: flat scalar parameters dataset_id, method, target_metric (optional), max_steps (default 10), lambda_, n_basis, n_comp, k, seed (all optional). Lowercase method; validate method_lc not in _RUNNABLE_METHODS raising ValueError naming sorted(_RUNNABLE_METHODS); enforce max_steps > 20 raising ValueError with the hard-cap message. Assemble initial_params from the non-None flat scalars (lambda_/n_basis/n_comp/k/seed). Import run_tuning_loop_mcp from fdars.mcp._tuning inside the handler (no advise import at module top or in the handler) and delegate entirely — no loop or heuristic logic inlined in server.py (Single Responsibility). Update the module tool-list docstring to include fdars_auto_tune. Add the guard-sync no-op note to the docstring (adding this tool expands neither _RUNNABLE_METHODS nor _DIAGNOSTICS_METHODS). Do NOT modify _RUNNABLE_METHODS or _DIAGNOSTICS_METHODS.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -c "import sys; sys.version_info[:2] >= (3,10) or sys.exit(0); from fdars.mcp import server; assert hasattr(server, 'fdars_auto_tune'); assert len(server._RUNNABLE_METHODS)==6 and len(server._DIAGNOSTICS_METHODS)==14; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - fdars_auto_tune is registered in server.py and importable on 3.10+
    - _RUNNABLE_METHODS stays 6 and _DIAGNOSTICS_METHODS stays 14 (guard-sync no-op)
    - method not in _RUNNABLE_METHODS raises ValueError at the tool boundary
    - max_steps > 20 raises ValueError (hard cap)
    - the handler delegates to run_tuning_loop_mcp with no inlined loop logic
  </acceptance_criteria>
  <done>fdars_auto_tune is a thin, boundary-validating @mcp.tool that delegates to the LLM-free run_tuning_loop_mcp helper, enforces the max_steps hard cap, and leaves the guard-sync sets untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MCP tuning tests — LLM-free scan, determinism, cap, by-reference, guard-sync</name>
  <files>tests/test_mcp_tuning.py</files>
  <read_first>
    - tests/test_mcp_compare_methods.py (analog — test_tool_never_imports_advise runtime-token file-scan, guard-sync-no-op set-size assertions, by-reference no-arrays assertion, the dataset-registration fixture pattern)
    - python/fdars/mcp/_tuning.py and python/fdars/mcp/server.py (the surfaces under test)
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md Phase-Requirements-to-Test map (TUNE-04 row) and Common Pitfall 6
  </read_first>
  <behavior>
    - test_auto_tune_does_not_import_advise: file-scan of mcp/_tuning.py (and the fdars_auto_tune handler region of server.py) contains no advise token (token built at runtime to avoid self-flagging)
    - test_heuristic_deterministic: two run_tuning_loop_mcp calls with the same registered dataset + same initial params produce equal compact result dicts
    - test_max_steps_hard_cap: fdars_auto_tune with max_steps=21 raises ValueError
    - test_rejects_method_not_runnable: fdars_auto_tune with a non-runnable method raises ValueError naming the supported set
    - test_returns_by_reference_no_arrays: the returned dict values are scalars/handles only (no list/ndarray payloads)
    - test_guard_sync_still_no_op: _RUNNABLE_METHODS has 6 and _DIAGNOSTICS_METHODS has 14
  </behavior>
  <action>Create tests/test_mcp_tuning.py mirroring test_mcp_compare_methods.py. Skip the whole module on Python < 3.10 (importorskip mcp or a version guard). Register a small synthetic dataset via registry.store_dataset for the determinism/by-reference tests (use a tuneable method such as fpca or clustering with a fixed seed so the heuristic is fully reproducible). Write test_auto_tune_does_not_import_advise building the search token at runtime (advise concatenation) and asserting it is absent from mcp/_tuning.py; extend the scan to the fdars_auto_tune handler by reading server.py and asserting the token does not appear within its handler body. Write test_heuristic_deterministic (two calls, equal result dicts), test_max_steps_hard_cap (max_steps=21 raises ValueError), test_rejects_method_not_runnable, test_returns_by_reference_no_arrays (assert every value in the returned dict is a str/int/float/bool/None, never a list or ndarray), and test_guard_sync_still_no_op (set sizes 6 and 14). No anthropic import and no network in any test.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -m pytest tests/test_mcp_tuning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_mcp_tuning.py exits 0 (or skips cleanly on 3.9)
    - fdars_auto_tune module does not import advise (file-scan test passes)
    - two identical heuristic runs produce equal result dicts (determinism)
    - max_steps=21 raises ValueError; non-runnable method raises ValueError
    - returned dict carries only scalars + handles (no arrays)
    - guard-sync no-op confirmed (6 runnable, 14 diagnostics)
  </acceptance_criteria>
  <done>The MCP tuning test suite proves the LLM-free invariant (extended to the new file + handler), heuristic determinism, the max_steps hard cap, by-reference return, and the guard-sync no-op — all offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> fdars_auto_tune | untrusted method/params/max_steps cross the tool boundary; validated + capped before any run |
| heuristic -> fdars (run_method) | deterministic Python stepper; no LLM anywhere on this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53C-01 | Elevation of Privilege | LLM leaking into MCP path | critical | mitigate | mcp/_tuning.py + the fdars_auto_tune handler never import/call advise; file-scan test enforces it (extends the Phase-51/52 invariant) |
| T-53C-02 | Denial of Service | max_steps abuse | high | mitigate | hard cap 20 enforced at the tool boundary with ValueError; loop is bounded regardless |
| T-53C-03 | Tampering | unsupported method / guard-sync drift | medium | mitigate | method validated against _RUNNABLE_METHODS at the boundary; guard-sync set sizes asserted unchanged (6/14) |
| T-53C-04 | Information Disclosure | arrays crossing the MCP boundary | medium | mitigate | by-reference return — only scalars + a trace handle cross; test_returns_by_reference_no_arrays enforces it |
</threat_model>

<verification>
- MCP tuning suite green (or clean-skip on 3.9): `cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -m pytest tests/test_mcp_tuning.py -q`
- LLM-free helper at load: `grep -v '^#' python/fdars/mcp/_tuning.py | grep -c anthropic` returns 0
- No MCP regression: `PYTHONPATH=python python -m pytest tests/test_mcp_server.py tests/test_mcp_compare_methods.py -q`
</verification>

<success_criteria>
- fdars_auto_tune drives the shared loop core with a deterministic, LLM-free heuristic
- File-scan proves no advise reference in the new file + handler
- max_steps hard cap and method validation enforced at the tool boundary
- by-reference return (no arrays); guard-sync no-op (6/14)
- Everything proven offline
</success_criteria>

<output>
Create `.planning/phases/53-closed-loop-auto-tuning-capstone/53-03-SUMMARY.md` when done
</output>

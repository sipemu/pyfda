---
phase: 51-comparative-method-selection
plan: 03
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - python/fdars/mcp/_compare_methods.py
  - python/fdars/mcp/server.py
  - tests/test_mcp_compare_methods.py
autonomous: true
requirements: [COMPARE-04]
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "fdars_compare_methods re-runs each candidate via the existing _RUNNABLE_METHODS + build_diagnostics and returns the deterministic ranking by-reference (handle), never full arrays (COMPARE-04)."
    - "The MCP tool never imports or calls advise() — the provably-LLM-free boundary holds and test_mcp_does_not_import_advise stays green (COMPARE-04)."
    - "A requested candidate method not in the 6 _RUNNABLE_METHODS is rejected with a clear error (COMPARE-04)."
    - "The MCP ranking equals the Plan-01 deterministic ranking for the same candidates — the tool reuses the ranking core, it does not reimplement it."
  artifacts:
    - "MCP tool fdars_compare_methods in python/fdars/mcp/server.py (thin @mcp.tool wrapper)"
    - "python/fdars/mcp/_compare_methods.py — helper that re-runs candidates and calls the deterministic ranking core (compare_methods run_llm=False)"
    - "tests/test_mcp_compare_methods.py — ranking-by-reference, unsupported-method rejection, LLM-free guard"
  key_links:
    - "fdars_compare_methods -> _compare_methods.compare_methods_mcp -> run_method (per candidate) -> build_diagnostics -> advisor._compare_methods.compare_methods(run_llm=False)"
    - "unsupported method -> ValueError (validated against _RUNNABLE_METHODS before any run)"
    - "return value -> ranking handle + scalar summaries by-reference (no arrays cross the boundary)"
---

<objective>
Expose the deterministic comparison over MCP as `fdars_compare_methods`: it
re-runs each candidate via the existing six `_RUNNABLE_METHODS` + `build_diagnostics`,
delegates ranking to the Plan-01 deterministic core (`compare_methods(run_llm=False)`),
and returns the ranking by-reference. It is provably LLM-free — it never imports
or calls `advise()`.

Purpose: COMPARE-04 (MCP tool exposes the comparison and stays LLM-free, re-running
via existing runnable methods). Reuse the single ranking implementation (Anti-Pattern
5: do not duplicate comparison logic in the MCP tool).

Output: `python/fdars/mcp/_compare_methods.py` helper, `fdars_compare_methods`
tool in `mcp/server.py`, LLM-free + by-reference + unsupported-method tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-comparative-method-selection/51-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/51-comparative-method-selection/51-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 03 subset)

- MCP tool name: `fdars_compare_methods` (thin `@mcp.tool()` handler in python/fdars/mcp/server.py, synchronous `def`).
- Helper: `compare_methods_mcp(dataset_id, method, candidate_params, metric=None)` in `python/fdars/mcp/_compare_methods.py` — re-runs each candidate, builds diagnostics, and calls `fdars.advisor._compare_methods.compare_methods(..., run_llm=False)`.
- Return shape: `{"ranking_id": <handle>, "method", "metric", "winner", "ranking": [{"label","result_id","metric_value"}...]}` — by-reference handles + scalar metric summaries only; no arrays.
- Test names: `tests/test_mcp_compare_methods.py::test_ranking_matches_offline_core`, `::test_returns_by_reference_no_arrays`, `::test_rejects_method_not_in_runnable`, `::test_rejects_candidate_method_outside_runnable`, `::test_tool_never_imports_advise`, `::test_guard_sync_still_no_op`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP re-run helper delegating to the deterministic ranking core (COMPARE-04)</name>
  <read_first>
    - python/fdars/mcp/_compare.py (compare_run: analog — re-runs via run_method, builds diagnostics, returns by-reference; _ALLOWED_PARAMS allowlist pattern)
    - python/fdars/mcp/_runner.py (run_method + _RUNNABLE_METHODS — the six re-runnable methods; scalar-only params)
    - python/fdars/mcp/_registry.py (store_dataset/store_result/get_result handle API for by-reference returns)
    - python/fdars/advisor/_compare_methods.py (compare_methods run_llm=False — the ranking core to delegate to)
  </read_first>
  <files>python/fdars/mcp/_compare_methods.py, tests/test_mcp_compare_methods.py</files>
  <behavior>
    - Given a dataset handle and a list of per-candidate param dicts for a single runnable method, the helper runs each candidate, builds diagnostics, and returns a ranking whose order/winner equals compare_methods(run_llm=False) on those diagnostics.
    - Each candidate's raw result is stored in the registry and referenced by result_id in the return; no arrays appear in the return dict.
    - A candidate params entry with a key outside the run_method allowlist raises ValueError.
  </behavior>
  <action>
    Create `python/fdars/mcp/_compare_methods.py` with `compare_methods_mcp(dataset_id, method, candidate_params, metric=None)`. For each entry in `candidate_params` (a list of flat scalar-param dicts, allowlist `{"lambda_","n_basis","n_comp","k","seed"}` mirroring `_compare._ALLOWED_PARAMS` — validate and raise ValueError on unknown keys before any run), call `run_method(dataset_id, method, **params)`, store the raw result via `registry.store_result`, and build diagnostics via `advisor.build_diagnostics(result, method, argvals=argvals)`. Derive a stable label per candidate from the method + its params (e.g. `"clustering(k=3)"`); ensure labels are unique (append an index if two candidates share params). Assemble a labeled candidates mapping and call `fdars.advisor._compare_methods.compare_methods(candidates, method=method, metric=metric, run_llm=False)` to get the deterministic ranking + winner. Do NOT reimplement the sort here (Anti-Pattern 5). Build the return as `{"ranking_id": <stored handle for the full ranking dict>, "method", "metric", "winner", "ranking": [{"label","result_id","metric_value"}...]}` — only handles + scalar metric values cross the boundary (Anti-Pattern 4: no arrays).

    This module must NOT import `advise` or any provider; it imports only `run_method`, `registry`, and `advisor.build_diagnostics` + `advisor._compare_methods.compare_methods` (the run_llm=False path, which is itself LLM-free per Plan 01). Guard the Python-version import (mirror `_compare.py`'s `sys.version_info < (3,10)` check).

    Add tests in `tests/test_mcp_compare_methods.py` (module-level pytestmark skip if mcp extra unavailable, mirror `tests/test_mcp_server.py`): `test_ranking_matches_offline_core` (run two clustering candidates via the helper; independently build the same diagnostics and call the advisor core; assert same winner + same label order), `test_returns_by_reference_no_arrays` (json.dumps the return succeeds and contains result_id handles, not arrays), `test_rejects_candidate_method_outside_runnable` (params with a bad allowlist key -> ValueError).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_compare_methods.py -q -k "matches_offline or by_reference or outside_runnable"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_mcp_compare_methods.py -k "matches_offline or by_reference or outside_runnable"` exits 0.
    - The helper's ranking winner + order equals `compare_methods(run_llm=False)` on the same diagnostics.
    - The return dict is JSON-serialisable and carries only handles + scalar metric values (no arrays).
    - `fdars/mcp/_compare_methods.py` does not import `advise` (grep at import lines returns nothing).
  </acceptance_criteria>
  <done>The MCP re-run helper produces the deterministic ranking by-reference by delegating to the single ranking core.</done>
  <reversibility rating="reversible">Internal helper; the tool return shape is defined here but consumed only by agentic callers, not by other phase plans.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fdars_compare_methods @mcp.tool wrapper + unsupported-method rejection (COMPARE-04)</name>
  <read_first>
    - python/fdars/mcp/server.py (fdars_compare_run: analog @mcp.tool wrapper; _RUNNABLE_METHODS validation at the tool boundary; flat scalar schema; synchronous def)
    - python/fdars/mcp/_compare_methods.py (compare_methods_mcp from Task 1)
    - python/fdars/mcp/_runner.py (_RUNNABLE_METHODS — the 6 methods)
  </read_first>
  <files>python/fdars/mcp/server.py, tests/test_mcp_compare_methods.py</files>
  <behavior>
    - fdars_compare_methods validates the method against _RUNNABLE_METHODS at the tool boundary and raises ValueError for a method not among the 6.
    - The tool returns the by-reference ranking from the helper.
    - The tool schema is flat (no nested provider/model args) and the handler is synchronous.
  </behavior>
  <action>
    Add `fdars_compare_methods` as a `@mcp.tool()` synchronous handler in `python/fdars/mcp/server.py`, mirroring `fdars_compare_run`'s structure. Signature: `fdars_compare_methods(dataset_id: str, method: str, candidate_params: list[dict], metric: str | None = None) -> dict`. Validate `method.lower()` against `_RUNNABLE_METHODS` at the tool boundary BEFORE any run and raise `ValueError` naming the supported set if absent (51-CONTEXT "Runnable coverage": a candidate method outside the 6 is rejected with a clear error). Delegate to `_compare_methods.compare_methods_mcp(dataset_id, method_lc, candidate_params, metric=metric)` — do not inline the re-run/ranking logic. Do NOT add a provider or model argument (the MCP layer is LLM-free, Anti-Pattern 3). Import the helper lazily inside the handler (mirror the existing tools' lazy imports). Add no new entry to `_DIAGNOSTICS_METHODS` or `_RUNNABLE_METHODS` (guard-sync no-op — 51-CONTEXT hard constraint).

    Update the module docstring tool list to include `fdars_compare_methods`.

    Add tests: `test_rejects_method_not_in_runnable` (call the tool with method="regression" -> ValueError naming _RUNNABLE_METHODS), and a happy-path in-process tool call (via the MCP Client pattern in `tests/test_mcp_server.py`, or by calling the underlying handler function) asserting the returned winner matches the offline core.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_compare_methods.py -q -k "not_in_runnable or tool"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_mcp_compare_methods.py -k "not_in_runnable or tool"` exits 0.
    - Calling the tool with a method outside the 6 `_RUNNABLE_METHODS` raises ValueError naming the supported set.
    - The tool delegates to `compare_methods_mcp` and returns the by-reference ranking; no provider/model argument exists on the tool.
    - `_DIAGNOSTICS_METHODS` and `_RUNNABLE_METHODS` are unchanged (guard-sync no-op).
  </acceptance_criteria>
  <done>fdars_compare_methods is registered, validates runnable coverage, and delegates to the single ranking core with a flat LLM-free schema.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the MCP tool stays LLM-free and guard-sync stays a no-op (COMPARE-04)</name>
  <read_first>
    - tests/test_mcp_server.py (test_mcp_does_not_import_advise — the file-scan LLM-free invariant; test_diagnostics_methods_match_advisor_supported — the guard-sync drift lock)
    - python/fdars/mcp/_compare_methods.py (must contain no advise import)
    - python/fdars/mcp/server.py (fdars_compare_methods must not import advise)
  </read_first>
  <files>tests/test_mcp_compare_methods.py</files>
  <action>
    Add `test_tool_never_imports_advise`: scan `python/fdars/mcp/_compare_methods.py` (and re-affirm `server.py`) with pathlib for the advise token constructed at runtime (mirror `tests/test_mcp_server.py::test_mcp_does_not_import_advise` so the test file does not self-flag), asserting the new MCP comparison module does not reference `advise`. Add `test_guard_sync_still_no_op`: assert `fdars.mcp.server._DIAGNOSTICS_METHODS` and `_RUNNABLE_METHODS` have the same membership as before this phase (14 diagnostics methods, 6 runnable) — i.e. adding the comparison tool added no method slot. Confirm the repo-wide `tests/test_mcp_server.py::test_mcp_does_not_import_advise` still passes with the new module present.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_compare_methods.py tests/test_mcp_server.py -q -k "import_advise or guard_sync or does_not_import"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_mcp_compare_methods.py tests/test_mcp_server.py -k "import_advise or guard_sync or does_not_import"` exits 0.
    - `fdars/mcp/_compare_methods.py` does not import or call advise (file scan clean).
    - `_DIAGNOSTICS_METHODS` (14) and `_RUNNABLE_METHODS` (6) membership is unchanged by this phase (guard-sync no-op).
    - The pre-existing `test_mcp_does_not_import_advise` remains green with the new tool present.
  </acceptance_criteria>
  <done>The MCP comparison tool is provably LLM-free and the guard-sync remains a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (LLM) -> fdars_compare_methods | Untrusted dataset_id, method, candidate_params, metric cross the MCP stdio boundary |
| fdars_compare_methods -> fdars | Via run_method + build_diagnostics (existing validated path); arrays stay in the registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-09 | Elevation | LLM inside MCP compute path | critical | mitigate | Tool imports only run_method/registry/build_diagnostics + the LLM-free ranking core; no advise import; test_tool_never_imports_advise + test_mcp_does_not_import_advise |
| T-51-10 | Tampering | unsupported candidate method | high | mitigate | method validated against _RUNNABLE_METHODS at the tool boundary before any run; candidate_params keys allowlisted (mirror _ALLOWED_PARAMS) |
| T-51-11 | Information disclosure | arrays crossing MCP boundary | medium | mitigate | Return only handles + scalar metric values; test_returns_by_reference_no_arrays |
| T-51-12 | Tampering | guard-sync drift | high | mitigate | No new _DIAGNOSTICS_METHODS/_RUNNABLE_METHODS entry; test_guard_sync_still_no_op |
| T-51-SC | Tampering | npm/pip/cargo installs | high | accept | No new package installs (uses existing fdars[mcp] extra) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_mcp_compare_methods.py -q` passes (skips cleanly if mcp extra unavailable).
- `tests/test_mcp_server.py::test_mcp_does_not_import_advise` and `::test_diagnostics_methods_match_advisor_supported` stay green.
- Ranking-by-reference, unsupported-method rejection, and LLM-free boundary all asserted.
- Guard-sync no-op confirmed.
</verification>

<success_criteria>
- COMPARE-04 (fdars_compare_methods MCP tool, LLM-free, re-runs via runnable methods, rejects non-runnable candidate methods) implemented and tested.
- The MCP tool reuses the single ranking core; no duplicated comparison logic.
- MCP-LLM-free boundary and guard-sync no-op both preserved.
</success_criteria>

<output>
Create `.planning/phases/51-comparative-method-selection/51-03-SUMMARY.md` when done
</output>

---
phase: 14-advisor-concept-diagrams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/advisor/index.md
  - docs/assets/diagrams/advisor-grounding-invariant.svg
  - docs/assets/diagrams/advisor-loop.svg
autonomous: false
requirements: [CONCEPT-01, CONCEPT-02, CONCEPT-03, ADVDIA-01, ADVDIA-02]
estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "A reader opening docs/advisor/index.md learns what the advisor is, its three surfaces (Python API / MCP / Agent Skill), and when to use it (CONCEPT-01)"
    - "The page states the grounding invariant in plain terms: fdars computes every number; the LLM only interprets and cites diagnostic values, never fabricating numbers (CONCEPT-02)"
    - "The page documents the [advisor] and [mcp] install extras and the offline-core vs env-gated LLM (ANTHROPIC_API_KEY) boundary (CONCEPT-03)"
    - "The grounding-invariant SVG renders on the page and is stable under the SVGO idempotence gate (ADVDIA-01)"
    - "The advisor-loop SVG renders on the page and is stable under the SVGO idempotence gate (ADVDIA-02)"
  artifacts:
    - docs/advisor/index.md
    - docs/assets/diagrams/advisor-grounding-invariant.svg
    - docs/assets/diagrams/advisor-loop.svg
  key_links:
    - "docs/advisor/index.md image includes resolve to ../assets/diagrams/advisor-grounding-invariant.svg and ../assets/diagrams/advisor-loop.svg"
    - "Both new SVGs pass svgo@3.3.4 --config svgo.config.mjs pass2 == pass1 (the CI gate in .github/workflows/docs.yml)"
    - "Overview content is method-accurate against python/fdars/advisor.py, python/fdars/mcp/server.py, and .claude/skills/fdars-advisor/SKILL.md"
---

<objective>
Author the new top-level "AI Advisor" overview page (`docs/advisor/index.md`) plus two new hand-authored inline SVG diagrams (grounding invariant; advisor loop) that pass the existing SVGO idempotence + determinism CI gate to STYLE_SPEC standard.

Purpose: Give the published MkDocs site its first conceptual entry point for the shipped v2.0 grounded AI advisor — a reader understands what the advisor is, its three surfaces, when to use it, and the grounding invariant, reinforced by two accurate diagrams. This is the concept anchor that Phases 15–17 (per-surface pages) link back to.

Output: `docs/advisor/index.md`, `docs/assets/diagrams/advisor-grounding-invariant.svg`, `docs/assets/diagrams/advisor-loop.svg`.

Covers CONCEPT-01, CONCEPT-02, CONCEPT-03, ADVDIA-01, ADVDIA-02.

Scope boundary: this phase authors the page + diagrams. It does NOT wire the top-level `mkdocs.yml` nav entry or run the full-site build gate — that is Phase 18 (NAVDOC-01/02). The page may reference the diagrams via the standard `{ .fdars-diagram }` include so they render. Documentation-only: no advisor code changes (per PROJECT constraint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-advisor-concept-diagrams/14-CONTEXT.md

# Style contract for all new SVGs (viewBox, five CSS classes, palette, accessibility)
@docs/assets/diagrams/STYLE_SPEC.md
# Idempotence-gate config the new SVGs must be stable under
@svgo.config.mjs
# Shipped code being documented — content MUST be method-accurate against these
@python/fdars/advisor.py
@python/fdars/mcp/server.py
@.claude/skills/fdars-advisor/SKILL.md
# Existing section landing page — match its hero/admonition conventions and relative-path idiom
@docs/represent/index.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Grounding-invariant SVG wired into a minimal page, gate-verified end-to-end</name>
  <files>docs/assets/diagrams/advisor-grounding-invariant.svg, docs/advisor/index.md</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (viewBox 0 0 720 300, canonical `<style>` block with `.ttl/.sub/.lab/.sm/.mono`, muted palette, `role="img"` + `aria-label`, panel patterns, stroke weights)
    - docs/assets/diagrams/clustering.svg (a real conforming diagram to model SVG structure/idiom on)
    - svgo.config.mjs (the exact plugin overrides the SVG must remain stable under — no `convertPathData`, no `mergePaths`, no `collapseGroups`, no `inlineStyles`)
    - python/fdars/advisor.py (grounding invariant in the module docstring + `_system_prompt`; `build_diagnostics` produces the numbers, `advise` returns `Advice` whose `Recommendation.evidence` cites diagnostic values)
    - docs/represent/index.md (relative image-include idiom and page-header conventions to match)
  </read_first>
  <action>
This is the end-to-end tracer: prove one diagram renders on the new page AND is stable under the SVGO gate before authoring the second diagram or the full page prose.

Create `docs/assets/diagrams/advisor-grounding-invariant.svg` as a hand-authored inline SVG conforming to STYLE_SPEC: root `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 300" fill="none" role="img" aria-label="...">` where the aria-label matches the diagram title text; copy the canonical five-class `<style>` block verbatim; use only the muted structural palette (`#1a1a2e`, `#6c757d`, `#495057`, `#ced4da`, `#f8f9fa`, and the orange accent `#fd7e14`/`#fff4ea` for the method/process panel). Depict a two-lane metaphor: a left lane "fdars computes numbers" (data to `build_diagnostics` to numeric diagnostics) and a right lane "LLM interprets and cites" (`advise` to `Advice` with evidence/rationale). Draw an explicit boundary/divider between the two lanes labelled to convey that the LLM never fabricates numbers. Draw a literal "cites" arrow from the computed diagnostic values into the evidence/rationale of `Advice`. Use `.mono` for the `build_diagnostics` / `advise` / `Advice` / `evidence` code labels. Keep the invariant faithful to the shipped wording: fdars computes every number; the LLM only interprets and cites diagnostic values — it never fabricates numbers.

Create `docs/advisor/index.md` as a minimal placeholder in this task (a top-level `# AI Advisor` H1 plus a single grounding-invariant section that embeds the diagram via `![Grounding invariant: fdars computes numbers, the LLM only cites them](../assets/diagrams/advisor-grounding-invariant.svg){ .fdars-diagram }`). Full prose and the surfaces/when/install sections are added in Task 3 and Task 4 — keep this file editable, do not finalize it here.

Do NOT run svgo with `-o <file>` (never rewrite the hand-authored source); always invoke in stdout mode. Do NOT introduce new colors or a non-720 viewBox width.
  </action>
  <verify>
    <automated>test -f docs/assets/diagrams/advisor-grounding-invariant.svg && first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/advisor-grounding-invariant.svg --output -) && second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && diff <(printf '%s' "$first") <(printf '%s' "$second") && grep -q 'viewBox="0 0 720 300"' docs/assets/diagrams/advisor-grounding-invariant.svg && grep -q 'role="img"' docs/assets/diagrams/advisor-grounding-invariant.svg && grep -Fq 'advisor-grounding-invariant.svg' docs/advisor/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/assets/diagrams/advisor-grounding-invariant.svg` exists.
    - `svgo@3.3.4 --config svgo.config.mjs` pass2 equals pass1 (empty diff) for the new SVG.
    - The SVG root contains `viewBox="0 0 720 300"`, `role="img"`, an `aria-label`, and the canonical five-class `<style>` block (`grep -q '.ttl{'`).
    - The SVG contains `.mono` labels naming `build_diagnostics`, `advise`, and `Advice`.
    - `docs/advisor/index.md` exists and its grep for `advisor-grounding-invariant.svg` matches (the diagram include resolves).
  </acceptance_criteria>
  <done>The grounding-invariant SVG exists, is stable under the SVGO idempotence gate to STYLE_SPEC standard, and is embedded in a minimal `docs/advisor/index.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Advisor-loop SVG (interpret to recommend to re-run to compare) gate-verified</name>
  <files>docs/assets/diagrams/advisor-loop.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (same conformance contract as Task 1)
    - docs/assets/diagrams/advisor-grounding-invariant.svg (the diagram authored in Task 1 — reuse its class usage and palette for visual consistency)
    - svgo.config.mjs (idempotence-gate plugin overrides)
    - python/fdars/mcp/server.py (`fdars_run_method` = re-run, `fdars_compare_run` returns before/after `delta` = compare; the agentic loop lives on the MCP/Skill surfaces)
    - python/fdars/advisor.py (`advise` is the recommend step; the Python API surface is recommend-only — it returns `Advice` and stops, it does not re-run or compare)
    - .claude/skills/fdars-advisor/SKILL.md (the interpret→recommend→re-run→compare workflow wording and the before/after delta block)
  </read_first>
  <action>
Create `docs/assets/diagrams/advisor-loop.svg` as a hand-authored inline SVG conforming to STYLE_SPEC (same root pattern, `<style>` block, palette, and stroke weights as Task 1). Depict a cyclic four-node flow: interpret to recommend to re-run to compare, with an arrow from compare feeding back to interpret (close the loop). Annotate the compare node with the before/after diagnostics delta. Show the Python API as a recommend-only exit branch: a labelled arrow leaving the recommend node to an end/exit marker, making visible that the Python API stops after recommend while MCP and the Agent Skill continue through re-run and compare. Use `.mono` for any code-ish labels (e.g. the compare-node delta label). Keep the aria-label matching the diagram title (the advisor loop). Reuse only the existing muted palette; the orange accent panel may mark the agentic (re-run/compare) portion. Do NOT run svgo with `-o <file>`; do NOT introduce new colors or a non-720 viewBox width.
  </action>
  <verify>
    <automated>test -f docs/assets/diagrams/advisor-loop.svg && first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/advisor-loop.svg --output -) && second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && diff <(printf '%s' "$first") <(printf '%s' "$second") && grep -q 'viewBox="0 0 720 30' docs/assets/diagrams/advisor-loop.svg && grep -q 'role="img"' docs/assets/diagrams/advisor-loop.svg && grep -q '.ttl{' docs/assets/diagrams/advisor-loop.svg</automated>
  </verify>
  <acceptance_criteria>
    - `docs/assets/diagrams/advisor-loop.svg` exists.
    - `svgo@3.3.4 --config svgo.config.mjs` pass2 equals pass1 (empty diff) for the new SVG.
    - The SVG root contains a 720-width viewBox (one of the STYLE_SPEC-allowed heights), `role="img"`, an `aria-label`, and the canonical five-class `<style>` block.
    - The SVG text content names all four loop stages (interpret, recommend, re-run, compare) and marks a recommend-only Python-API exit distinct from the MCP/Skill continuation.
  </acceptance_criteria>
  <done>The advisor-loop SVG exists, is stable under the SVGO idempotence gate to STYLE_SPEC standard, shows the cyclic four-node loop, and makes the Python-API recommend-only exit visible.</done>
</task>

<task type="auto">
  <name>Task 3: Author the full overview prose — what / three surfaces / when to use</name>
  <files>docs/advisor/index.md</files>
  <read_first>
    - docs/advisor/index.md (the minimal page from Task 1 — extend it, do not overwrite the grounding-invariant include)
    - docs/represent/index.md (hero/admonition/relative-path conventions to match; `!!! info` admonition idiom)
    - python/fdars/advisor.py (`build_diagnostics`, `advise`, `describe_cluster_differences`, `Advice` schema — the Python API surface being teased)
    - python/fdars/mcp/server.py (the three tools + `run_stdio` — the MCP surface being teased)
    - .claude/skills/fdars-advisor/SKILL.md (the Agent Skill surface + its "when to use" trigger list: parameter tuning, method choice, interpreting diagnostics, before/after comparison)
    - docs/assets/diagrams/advisor-loop.svg (embed this diagram in the "how it works" section)
  </read_first>
  <action>
Extend `docs/advisor/index.md` (keep the Task 1 grounding-invariant section and its diagram include near the top). Add: (1) a short "what the advisor is" opening — a grounded AI advisor over fdars results that interprets computed diagnostics and recommends parameter/method changes, method-accurate against `python/fdars/advisor.py`. (2) A three-surfaces section with one one-paragraph teaser each — Python API (recommend-only: `build_diagnostics` / `advise` / `describe_cluster_differences`), MCP server (the three tools `fdars_build_diagnostics` / `fdars_run_method` / `fdars_compare_run` over stdio, by-reference handles), and Agent Skill (git-URL install, full interpret→recommend→re-run→compare walkthrough) — each teaser ending with a link placeholder to its dedicated page (the per-surface pages ship in Phases 15–17; use relative links `python-api.md`, `mcp.md`, `agent-skill.md` or a clearly-marked "(coming soon)" so the page stays self-consistent without asserting a built target). (3) A "when to use it" short bulleted list drawn from the SKILL.md trigger wording: parameter tuning, method choice, interpreting diagnostics, before/after comparison. (4) A "how it works" section that embeds the advisor-loop diagram via `![Advisor loop: interpret, recommend, re-run, compare](../assets/diagrams/advisor-loop.svg){ .fdars-diagram }` and notes in prose that the Python API is recommend-only while MCP and the Agent Skill continue through re-run and compare.

Do NOT add any runnable code fence on this page (the overview stays conceptual and diagram-led; the first runnable worked example lives on the Python API page in Phase 15). Keep all three surface names present verbatim so the accuracy check can find them.
  </action>
  <verify>
    <automated>grep -q "Python API" docs/advisor/index.md && grep -q "MCP" docs/advisor/index.md && grep -q "Agent Skill" docs/advisor/index.md && grep -Fq "advisor-loop.svg" docs/advisor/index.md && grep -Eq "build_diagnostics|advise" docs/advisor/index.md && grep -Eq "fdars_run_method|fdars_compare_run" docs/advisor/index.md && ! grep -qE '^```(python|py|pycon)' docs/advisor/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/advisor/index.md` names all three surfaces: "Python API", "MCP", and "Agent Skill".
    - The page references the Python API primitives (`build_diagnostics` and/or `advise`) and MCP tools (`fdars_run_method` / `fdars_compare_run`), method-accurate against the shipped source.
    - The page embeds the advisor-loop diagram (grep for `advisor-loop.svg` matches).
    - The page contains a "when to use" list covering parameter tuning, method choice, interpreting diagnostics, and before/after comparison.
    - The page contains NO runnable python/py/pycon code fence (the negative grep passes).
  </acceptance_criteria>
  <done>The overview page explains what the advisor is, names and teases all three surfaces with links, lists when to use it, and embeds the advisor-loop diagram — all method-accurate against the shipped code, with no runnable code fence.</done>
</task>

<task type="auto">
  <name>Task 4: Author the grounding-invariant + install-extras prose</name>
  <files>docs/advisor/index.md</files>
  <read_first>
    - docs/advisor/index.md (the page from Tasks 1 and 3 — extend the grounding-invariant section and add the install-extras section)
    - python/fdars/advisor.py (`_require_anthropic` install hint `pip install fdars[advisor]`, `ADVISOR_ANTHROPIC_MIN_VERSION = "0.72.0"`; the offline `build_diagnostics` path vs the env-gated `advise` path)
    - python/fdars/mcp/server.py (the `[mcp]` Python 3.10+ guard and `mcp>=2.0.0` requirement)
    - .claude/skills/fdars-advisor/SKILL.md (install/extras wording: `[advisor]` = anthropic>=0.72.0 + pydantic>=2.0; `[mcp]` = mcp>=2.0.0, Python 3.10+; ANTHROPIC_API_KEY gates the LLM step)
    - docs/represent/index.md (`!!! info` admonition idiom for the offline-vs-env-gated boundary callout)
  </read_first>
  <action>
Complete `docs/advisor/index.md`. (1) In the grounding-invariant section (which already holds the diagram from Task 1), add plain-language prose stating the invariant faithfully: fdars computes every number via `build_diagnostics` (offline, deterministic); the LLM in `advise` only interprets and cites those diagnostic values in each `Recommendation`'s `evidence`, and never fabricates numbers — noting the schema (`evidence` required) and system prompt enforce it. (2) Add an "Installation" section documenting the two extras accurately: `[advisor]` installs `anthropic>=0.72.0` and `pydantic>=2.0`; `[mcp]` installs `mcp>=2.0.0` and requires Python 3.10+. State the offline-core vs env-gated LLM boundary: `build_diagnostics` and the `run_llm=False` paths work fully offline with no API key; the `advise` / grounded-LLM step requires `ANTHROPIC_API_KEY`. Present the boundary as a `!!! info` admonition matching the site idiom. You may show install commands as shell (` ```bash `) fences — those are not executed by the docs doctest harness — but do NOT add any python/py/pycon runnable fence.

Keep the invariant wording faithful to the shipped source; do not overstate (e.g. do not claim the LLM validates numbers — it only cites them).
  </action>
  <verify>
    <automated>grep -qF "[advisor]" docs/advisor/index.md && grep -qF "[mcp]" docs/advisor/index.md && grep -q "ANTHROPIC_API_KEY" docs/advisor/index.md && grep -Eq "0\.72\.0" docs/advisor/index.md && grep -Eq "3\.10" docs/advisor/index.md && grep -Eiq "never fabricat|only interpret|cites" docs/advisor/index.md && ! grep -qE '^```(python|py|pycon)' docs/advisor/index.md</automated>
  </verify>
  <acceptance_criteria>
    - The page documents both `[advisor]` and `[mcp]` extras with their pinned floors (`anthropic>=0.72.0`, `pydantic>=2.0`, `mcp>=2.0.0`, Python 3.10+).
    - The page names `ANTHROPIC_API_KEY` and states the offline-core vs env-gated LLM boundary.
    - The grounding-invariant prose faithfully states that the LLM only interprets and cites diagnostic values and never fabricates numbers.
    - The page still contains NO runnable python/py/pycon code fence.
  </acceptance_criteria>
  <done>The overview page fully documents the grounding invariant in plain terms and the `[advisor]`/`[mcp]` install extras plus the offline vs env-gated boundary — method-accurate, no runnable code fence.</done>
</task>

</tasks>

<checkpoint>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Section review gate — built-site review of overview page + both diagrams</name>
  <what-built>The new `docs/advisor/index.md` overview page and both new inline SVG diagrams (`advisor-grounding-invariant.svg`, `advisor-loop.svg`). This is the per-section review gate mandated by the project process before moving to the next section (Phase 15).</what-built>
  <how-to-verify>
    Build/serve the docs (per the MEMORY docs-diagram-verify recipe: venv + PYTHONPATH + DOCS_FAST, `/pyfda serve` base) and open the AI Advisor overview page. Confirm: (1) both diagrams render and are visually STYLE_SPEC-consistent (muted palette, no clipped labels, 720-wide canvas); (2) the grounding-invariant diagram shows the two lanes + the "cites" arrow + the no-fabrication boundary; (3) the advisor-loop diagram shows the cyclic interpret→recommend→re-run→compare flow with the Python-API recommend-only exit; (4) the prose names all three surfaces, states the grounding invariant faithfully, and documents the `[advisor]`/`[mcp]` extras + the ANTHROPIC_API_KEY boundary; (5) nothing on the page misstates the shipped behavior. Optionally render the SVGs standalone with `rsvg-convert` to eyeball them.
  </how-to-verify>
  <resume-signal>Type "approved" to close the Phase 14 review gate, or describe any diagram/prose corrections needed.</resume-signal>
</task>
</checkpoint>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SVG → SVGO CI gate | Hand-authored markup must remain structurally stable under the pinned svgo config; malformed markup fails the build |
| docs prose → shipped code | Prose claims about advisor behavior are a trust surface — a misstatement ships an inaccurate doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | new inline SVGs | medium | mitigate | Every SVG task verifies svgo@3.3.4 pass2 == pass1 (idempotence gate) before done; never invoke svgo with `-o` to rewrite the source |
| T-14-02 | Information disclosure | overview prose | high | mitigate | Content authored method-accurately against `advisor.py`/`mcp/server.py`/`SKILL.md`; human-verify review gate confirms no behavioral misstatement (grounding invariant not overstated) |
| T-14-03 | Denial of service | docs build | low | accept | No `mkdocs.yml` nav wiring or full-build in this phase (deferred to Phase 18); page is authored but not yet in the build graph, so a page error cannot break the published build this phase |

No package-manager install tasks in this phase (documentation + SVG authoring only); no package-legitimacy gate required.
</threat_model>

<verification>
- Both new SVGs pass the SVGO idempotence gate: for each, `npx svgo@3.3.4 --config svgo.config.mjs` pass2 == pass1 (empty diff), matching the CI gate in `.github/workflows/docs.yml`.
- Both SVGs conform to STYLE_SPEC: 720-width viewBox, `role="img"`, `aria-label`, canonical five-class `<style>` block, muted palette only.
- `docs/advisor/index.md` embeds both diagrams via `{ .fdars-diagram }` includes that resolve to the new SVG files.
- The page names all three surfaces, states the grounding invariant faithfully, documents `[advisor]`/`[mcp]` extras + the ANTHROPIC_API_KEY boundary, and contains no runnable python code fence.
- Human review gate on the built/served site confirms visual + method accuracy.
</verification>

<success_criteria>
- CONCEPT-01: overview page explains what the advisor is, names the three surfaces, and states when to use it (Task 3).
- CONCEPT-02: page explains the grounding invariant in plain terms (Task 1 diagram + Task 4 prose).
- CONCEPT-03: page documents the `[advisor]`/`[mcp]` extras and the offline-core vs env-gated LLM boundary (Task 4).
- ADVDIA-01: grounding-invariant SVG renders and passes the SVGO/determinism gate (Task 1).
- ADVDIA-02: advisor-loop SVG renders and passes the same gate (Task 2).
</success_criteria>

<artifacts_this_phase_produces>
New files created by this phase:
- `docs/advisor/index.md` — the "AI Advisor" overview page.
- `docs/assets/diagrams/advisor-grounding-invariant.svg` — grounding-invariant diagram (ADVDIA-01).
- `docs/assets/diagrams/advisor-loop.svg` — advisor-loop diagram (ADVDIA-02).

NOT produced this phase (deferred to Phase 18): `mkdocs.yml` nav wiring for the "AI Advisor" section and the full-site build gate.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-advisor-concept-diagrams/14-01-SUMMARY.md` when done.
</output>

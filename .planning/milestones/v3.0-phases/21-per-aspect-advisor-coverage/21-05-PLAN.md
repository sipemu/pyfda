---
phase: 21-per-aspect-advisor-coverage
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
files_modified:
  - python/fdars/advisor/aspects/spm.py
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor.py
autonomous: true
requirements: [ASPECT-05, ASPECT-06]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "build_diagnostics(spm_result, method='spm') computes T2/SPE exceedance rates and cumulative variance via the shared eigenvalue helper (ASPECT-05)"
    - "The spm builder makes exactly ONE live fdars call (spe_moment_match_diagnostic) with graceful None fallback when fdars is unavailable; stochastic arl0_t2 is excluded (ASPECT-05)"
    - "_system_prompt('interpretation','spm') carries an spm-specific clause (ASPECT-06)"
    - "The spm diagnostics dict is deterministic and JSON-serialisable (success criterion 4)"
  artifacts:
    - python/fdars/advisor/aspects/spm.py
  key_links:
    - "spm.py -> _utils._eigenvalues_to_variance_cumulative (shared helper, not copied)"
    - "spm.py -> fdars.spm.spe_moment_match_diagnostic (the one live, deterministic, offline-safe call)"
    - "dispatcher _supported gains 'spm' -> lazy builder import"
---

<objective>
Add the final, HIGH-complexity aspect: monitoring/SPM (ASPECT-05). This is the only builder that makes a live fdars call — `spe_moment_match_diagnostic(spe_values)` — which is deterministic (pure moment computation, no RNG) and therefore offline-safe. It reuses the shared `_eigenvalues_to_variance_cumulative` helper from `_utils.py` (landed in plan 03) rather than copying the eigenvalue-to-variance code. It applies RESEARCH correction #8: the live call returns key `excess_kurtosis`, which the builder renames to `spe_kurtosis_excess` in the diagnostics dict. Stochastic `arl0_t2` is explicitly EXCLUDED (breaks determinism — FUT-02).

Verified at plan time: `fdars.spm.spe_moment_match_diagnostic([...])` returns keys `excess_kurtosis`, `theoretical_kurtosis`, `is_adequate` — matches RESEARCH §3.

Purpose: complete full-library aspect coverage; close ASPECT-05 and finalize ASPECT-06 (all seven new aspects have primer clauses).
Output: spm builder, spm prompt clause, an fdars-guarded determinism test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-aspect-advisor-coverage/21-RESEARCH.md

@python/fdars/advisor/aspects/fpca.py
@python/fdars/advisor/aspects/depth.py
@python/fdars/advisor/__init__.py
@python/fdars/advisor/_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spm builder — exceedance rates + shared eigenvalue helper + one live call</name>
  <files>python/fdars/advisor/aspects/spm.py, python/fdars/advisor/__init__.py</files>
  <precondition>fdars is importable in the execution venv (.venv/bin/python -c "import fdars.spm") for the live spe_moment_match_diagnostic path; the builder still degrades gracefully to None when it is not.</precondition>
  <behavior>
    - build_diagnostics(spm_result, "spm") -> n_obs, ncomp=len(eigenvalues), t2_limit/spe_limit floats, t2_max/t2_mean/spe_max/spe_mean floats.
    - t2_exceedance_rate = float(mean(t2 > t2_limit)); spe_exceedance_rate likewise — both native floats in [0,1].
    - variance_explained_cumulative is a list of native floats from the shared helper, last≈1.0.
    - spe_kurtosis_excess + spe_moment_match_adequate are floats/bool when fdars present, None when the live call raises/ImportErrors.
    - json.dumps(sort_keys=True) succeeds; no numpy scalars.
  </behavior>
  <read_first>python/fdars/advisor/aspects/_utils.py (shared helper from plan 03), 21-RESEARCH.md §1g and §3 (spm field list + eigenvalue reuse + correction #8)</read_first>
  <action>Create `python/fdars/advisor/aspects/spm.py` with `_build_spm_diagnostics(raw, **kwargs) -> dict` per 21-RESEARCH §1g. Import the shared helper: `from fdars.advisor.aspects._utils import _eigenvalues_to_variance_cumulative`. Emit: n_obs=int(len(raw["t2"])), ncomp=int(len(raw["eigenvalues"])), t2_limit/spe_limit as direct floats (they are scalar ucl values, NOT dicts — RESEARCH §3), t2_max/t2_mean/spe_max/spe_mean, t2_exceedance_rate=float(np.mean(np.asarray(raw["t2"],dtype=float) > float(raw["t2_limit"]))) and the spe analogue, eigenvalues=[float(v) for v in raw["eigenvalues"]], variance_explained_cumulative=_eigenvalues_to_variance_cumulative(raw["eigenvalues"]) (SPM returns eigenvalues DIRECTLY — no sv-squared/(n-1) step). For the moment-match fields: guard that "spe" is present, then wrap the ONE live call in try/except (per RESEARCH pitfall #5): `from fdars import spm as _spm; mmd = _spm.spe_moment_match_diagnostic(np.asarray(raw["spe"],dtype=float))`; on any ImportError/Exception set spe_kurtosis_excess=None and spe_moment_match_adequate=None. Apply correction #8: read mmd["excess_kurtosis"] and emit it as spe_kurtosis_excess (float); emit mmd["is_adequate"] as spe_moment_match_adequate (bool). Do NOT call or reference arl0_t2 (excluded — stochastic). Guard every key access. Add "spm" to `_supported` + dispatch branch in `__init__.py`.</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json; from fdars.advisor import build_diagnostics as b; d=b({'t2':np.array([1.2,3.4,.8,5.1,2.3,4.,1.5,2.7,.9,3.1]),'spe':np.array([.05,.12,.03,.25,.08,.18,.04,.09,.02,.11]),'t2_limit':4.5,'spe_limit':.20,'eigenvalues':np.array([2.1,.8,.3])},'spm'); assert d['n_obs']==10 and d['ncomp']==3 and abs(d['t2_exceedance_rate']-0.1)<1e-9 and abs(d['spe_exceedance_rate']-0.1)<1e-9 and abs(d['variance_explained_cumulative'][-1]-1.0)<1e-9 and d['spe_kurtosis_excess'] is not None; json.dumps(d,sort_keys=True); print('OK')"</automated>
  </verify>
  <done>spm builder computes exceedance rates + cumulative variance via the shared helper, makes exactly one deterministic live call with graceful fallback, and excludes arl0_t2; dispatch wired.</done>
</task>

<task type="auto">
  <name>Task 2: spm prompt clause (finalizes ASPECT-06 coverage)</name>
  <files>python/fdars/advisor/_prompts.py</files>
  <action>Add an "spm" entry to `_ASPECT_PRIMERS` in `_prompts.py` per 21-RESEARCH §5: explain that t2_exceedance_rate is the fraction of in-control observations exceeding the T2 limit and should approximately equal the design alpha for a well-calibrated Phase-I chart; that spe_kurtosis_excess (from spe_moment_match_diagnostic) measures departure of SPE from the moment-matched chi-squared approximation (high = approximation inadequate); and that variance_explained_cumulative shows how much variation the chosen ncomp components capture. The clause MUST contain the token t2_exceedance_rate for the test. This is the seventh and last new aspect primer, completing ASPECT-06 (every aspect old + new now has an FDA-primer clause). No new schema, no new prompt function.</action>
  <verify>
    <automated>.venv/bin/python -c "from fdars.advisor._prompts import _system_prompt, _ASPECT_PRIMERS; assert 't2_exceedance_rate' in _system_prompt('interpretation','spm'); assert set(_ASPECT_PRIMERS) >= {'depth','outliers','classification','represent','regression','regression_cv','spm'}; assert _system_prompt('interpretation')==_system_prompt('interpretation',''); print('OK')"</automated>
  </verify>
  <done>spm primer present; all seven new aspects have primer clauses; aspect='' still reproduces base prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: spm determinism test (fdars-guarded) + full-suite regression gate</name>
  <files>tests/test_advisor.py</files>
  <behavior>
    - test_spm_deterministic: uses pytest.importorskip("fdars.spm"); the synthetic Phase-I fixture -> equal dicts, byte-identical json.dumps(sort_keys=True), no numpy scalars, spe_kurtosis_excess is a native float.
    - TestPrompts: spm clause present only for aspect='spm'; a coverage assertion that all seven aspect primers exist.
  </behavior>
  <read_first>tests/test_advisor.py (TestBuildDiagnosticsOffline + check_no_numpy walker), 21-RESEARCH.md §8 (spm fixture + importorskip guard)</read_first>
  <action>Add `test_spm_deterministic` to `TestBuildDiagnosticsOffline` using `pytest.importorskip("fdars.spm")` (the live spe_moment_match_diagnostic call needs the compiled extension — per RESEARCH §8) and the synthetic Phase-I fixture from 21-RESEARCH §8. Reuse check_no_numpy: two calls -> equal, byte-identical json.dumps(sort_keys=True), no numpy scalars; assert spe_kurtosis_excess is a native float. Extend TestPrompts with an spm clause assertion and a final coverage assertion that _ASPECT_PRIMERS contains all seven new aspects. This is the final Nyquist gate for the phase's determinism success criterion.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_spm_deterministic -x -q && .venv/bin/python -m pytest tests/test_advisor.py -q</automated>
  </verify>
  <done>spm determinism test passes; the ENTIRE advisor test suite (all aspects + prompts + existing) is green — phase complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller spm result dict -> builder | Untrusted Phase-I result dict crosses into the offline builder |
| builder -> fdars.spm native call | The one crossing into compiled Rust (spe_moment_match_diagnostic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Tampering | spm builder | low | mitigate | Validate "t2"/"spe" present before ops; guard every key; try/except around the one live call with None fallback (ASVS V5) |
| T-21-08 | Denial of Service | spe_moment_match_diagnostic call | low | mitigate | Deterministic pure-moment computation, no RNG; graceful degradation when fdars unavailable prevents hard failure; arl0_t2 excluded to preserve determinism |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor.py -q` passes (all aspects + prompts + existing suite).
- No new package installs; the one live call is to already-installed fdars.
</verification>

<success_criteria>
- ASPECT-05 covered by spm; ASPECT-06 finalized (all seven primers present).
- SPM makes exactly one deterministic live call, excludes arl0_t2, reuses the shared eigenvalue helper, and passes an fdars-guarded determinism test.
</success_criteria>

<output>
Create `.planning/phases/21-per-aspect-advisor-coverage/21-05-SUMMARY.md` when done.
</output>

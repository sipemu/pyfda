---
phase: 20-additional-provider-adapters
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - python/fdars/advisor/providers/ollama.py
  - tests/test_advisor_ollama.py
autonomous: true
requirements: [PROV-04]
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "advise(provider='ollama') produces grounded advice fully locally with no API key (D: PROV-04)."
    - "OllamaProvider returns a raw dict (supports_native_structured_output=False), exercising ValidateAndRetry._fallback_with_retry."
    - "OllamaProvider never passes think= alongside format= (Ollama constraint that silently disables constrained decoding)."
    - "Empty content raises ValueError (GROUND-04 parity)."
  artifacts:
    - python/fdars/advisor/providers/ollama.py
    - tests/test_advisor_ollama.py
  key_links:
    - "resolve_provider('ollama') -> OllamaProvider(host=resolved_base_url) -> ValidateAndRetry._fallback_with_retry (=False path) -> _check_grounding"
    - "OllamaProvider.__init__ -> _require_ollama() (deferred guard landed in plan 20-01)"
---

<objective>
Expansion slice: the Ollama adapter — the first backend to exercise the `supports_native_structured_output=False` path through `ValidateAndRetry._fallback_with_retry`. All shared plumbing (extras, `_require_ollama` guard, the ollama branch in `resolve_provider`) already landed in plan 20-01; this plan adds only the new adapter file and its offline tests.

Purpose: Deliver fully-local, key-free grounded advice and validate the non-native validate-and-retry path end-to-end (PROV-04).
Output: `providers/ollama.py`, `tests/test_advisor_ollama.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-additional-provider-adapters/20-RESEARCH.md
@.planning/phases/20-additional-provider-adapters/20-01-SUMMARY.md

# Read before editing — the non-native path and the adapter pattern:
@python/fdars/advisor/providers/_validate.py
@python/fdars/advisor/providers/anthropic.py
@python/fdars/advisor/_schema.py
@tests/test_advisor_openai.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OllamaProvider adapter (format= constrained decoding, no think=, raw dict return)</name>
  <files>python/fdars/advisor/providers/ollama.py</files>
  <precondition>Plan 20-01 landed `_require_ollama()` in advisor/__init__.py and the `ollama` branch in _factory.py; the ollama SDK is NOT installed in the venv (offline tests mock via sys.modules).</precondition>
  <behavior>
    - Given a mock ollama.chat returning a message with valid Advice JSON content, complete_structured returns a plain dict (NOT an Advice instance) so ValidateAndRetry validates it.
    - The ollama.chat call kwargs include model, messages, format (the schema), and options temperature 0; they do NOT include a think key.
    - Given empty message content, complete_structured raises ValueError.
    - Wrapped in ValidateAndRetry, a valid-JSON dict is validated up to a validated Advice; an invalid dict retries then raises ValueError after MAX_RETRIES.
  </behavior>
  <action>
Create `python/fdars/advisor/providers/ollama.py` mirroring anthropic.py structure (per 20-RESEARCH.md §Ollama Adapter; D: PROV-04). Only top-level import is `from __future__ import annotations`; the ollama SDK import is deferred into method bodies (Pitfall 4).

Class `OllamaProvider`: class attrs `name = "ollama"`, `supports_native_structured_output = False` (grammar-constrained decoding is structurally reliable but field-type coercion can still fail, so route through ValidateAndRetry._fallback_with_retry). `__init__(self, model="llama3.2", host=None)`: deferred-import `_require_ollama` and `_require_pydantic` from `fdars.advisor`, call them, store `self.model` and `self._host`. No API key.

`complete_structured(self, schema, messages, system)`: deferred-import ollama and json. Prepend a `{"role":"system","content":system}` message (Ollama has no top-level system param). Optionally augment the last user message with a JSON-schema hint string built from `schema.model_json_schema()` to improve small-model adherence (per research). Build the call kwargs: `model=self.model`, `messages=<wire messages>`, `format=schema.model_json_schema()` (constrained decoding), `options={"temperature": 0}`; add `host=self._host` only when non-None. Call `ollama.chat(**kwargs)`. Read `response.message.content`; if falsy, raise ValueError for empty content. Return `json.loads(content)` — a raw dict, NOT a validated instance, so ValidateAndRetry runs schema.model_validate + repair retry.

The adapter MUST NOT construct or pass a `think` argument anywhere — passing think= alongside format= silently disables the format constraint on some models (Ollama issue #10929). Add a docstring note that repeated validation failure points at schema serialization, not model reliability (MAX_RETRIES is fixed at 2 in ValidateAndRetry).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "from fdars.advisor.providers.ollama import OllamaProvider; assert OllamaProvider.supports_native_structured_output is False; assert OllamaProvider.name=='ollama'; print('ollama-adapter-ok')"</automated>
  </verify>
  <done>OllamaProvider importable with no ollama SDK installed (deferred import); native flag False; name 'ollama'; no `think` token in the source outside comments.</done>
</task>

<task type="auto">
  <name>Task 2: Offline Ollama tests (dict return, no think=, retry path, ImportError, grounding, resolve_provider)</name>
  <files>tests/test_advisor_ollama.py</files>
  <action>
Create `tests/test_advisor_ollama.py` — offline, network-free, no-key (D: PROV-04). Mock via sys.modules injection: the real `ollama` SDK is absent, so build a fake `ollama` module whose `chat` attribute is a MagicMock and install it with `monkeypatch.setitem(sys.modules, "ollama", fake_module)`, restoring afterward. Reuse the GOOD_ADVICE construction pattern from tests/test_advisor_openai.py.

Tests:
1. Returns raw dict: fake ollama.chat returns a message object with `.message.content = json.dumps(GOOD_ADVICE.model_dump())`; assert complete_structured returns a `dict` (isinstance dict), NOT an Advice instance.
2. Never passes think=: after a complete_structured call, inspect fake `chat.call_args.kwargs` and assert `"think" not in kwargs` and `"format" in kwargs`.
3. ValidateAndRetry validates the dict: wrap OllamaProvider in `ValidateAndRetry` (from _validate.py) with a fake chat returning valid JSON; assert the wrapper returns a validated Advice (isinstance Advice) via the non-native fallback path.
4. Retry then deterministic raise: fake chat returns invalid JSON (missing required fields) on every call; assert the ValidateAndRetry wrapper raises ValueError after MAX_RETRIES (no fabrication).
5. Empty content raises ValueError.
6. Missing-extra ImportError: set sys.modules["ollama"]=None, reload the adapter and call OllamaProvider(); assert pytest.raises(ImportError) matching `pip install fdars\[ollama\]`; restore.
7. Grounding rejection: build an Advice citing an ungrounded number; assert `_check_grounding` raises GroundingViolationError (Ollama path inherits the centralized check).
8. resolve_provider('ollama'): with the fake ollama module installed, assert `resolve_provider(provider="ollama").name == "ollama"` and `.supports_native_structured_output is False`, and that host threads from base_url (call resolve_provider with base_url and assert the OllamaProvider received it — inspect via the constructed adapter or a spy).

Do NOT modify existing test files. Any negative-assertion literal (e.g. "think") must originate from the code under test, not be planted in a production source action.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_ollama.py -q</automated>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor.py tests/test_advisor_providers.py tests/test_advisor_openai.py -q</automated>
  </verify>
  <done>All Ollama tests pass; prior suites (advisor, providers, openai) stay green; runs with no SDK installed and no key/daemon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter -> local Ollama daemon | prompt + schema sent to localhost daemon; response untrusted until validated + grounding-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Tampering | Ollama JSON output | high | mitigate | format= constrained decoding + ValidateAndRetry (≤2 repair retries, deterministic raise) + centralized _check_grounding |
| T-20-05 | Denial of service | think= disabling format= | medium | mitigate | adapter never passes think=; test asserts its absence |
| T-20-06 | Repudiation | field-type coercion (evidence as str vs list) | low | mitigate | non-native path re-validates every dict; failure retries then raises, never fabricates |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor_ollama.py -q` green.
- `grep -nv '^\s*#' python/fdars/advisor/providers/ollama.py | grep -c 'think'` is 0 (no think= in executable lines).
- Prior suites unchanged and green.
</verification>

<success_criteria>
- advise(provider="ollama") produces grounded advice locally with no API key (PROV-04, success-criterion 2).
- The non-native validate-and-retry path is exercised and deterministically raises after the cap.
- No think= is ever passed alongside format=.
</success_criteria>

<output>
Create `.planning/phases/20-additional-provider-adapters/20-02-SUMMARY.md` when done
</output>

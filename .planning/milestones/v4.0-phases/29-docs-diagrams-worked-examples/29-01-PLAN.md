---
phase: 29-docs-diagrams-worked-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/represent/interpolation.md
  - docs/represent/imputation.md
  - docs/assets/diagrams/interpolation-policy.svg
  - docs/assets/diagrams/imputation.svg
  - mkdocs.yml
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader can open represent/interpolation.md and represent/imputation.md on the built site via nav (DOCS-03)"
    - "The interpolation page shows a method-accurate SVG where ExtrapolationPolicy::Exception RAISES (red stop marker), Fill drops to the constant, Periodic wraps — never silent extrapolation (DOCS-01)"
    - "The imputation page shows a method-accurate SVG where Linear ramps across interior NaN gaps and extends flat at boundary gaps; Mean/Constant are horizontal fills (DOCS-01)"
    - "Both pages run a real offline worked example against docs/data emitting FDARS_FENCE_OK (DOCS-02)"
    - "mkdocs build --strict exits 0 offline with the two new pages wired into nav (DOCS-03)"
  artifacts:
    - docs/represent/interpolation.md
    - docs/represent/imputation.md
    - docs/assets/diagrams/interpolation-policy.svg
    - docs/assets/diagrams/imputation.svg
  key_links:
    - "mkdocs.yml Represent nav → the two new page paths"
    - "each page's ![](../assets/diagrams/NAME.svg){ .fdars-diagram } → the committed SVG"
    - "worked-example fence → fdars.represent.spline_interpolate_with_policy / impute_missing_values (real shipped bindings)"
---

<objective>
Stand up the FULL docs toolchain end-to-end on the represent section (the tracer), then expand it to the second represent page. Author docs/represent/interpolation.md (spline interpolation + the four ExtrapolationPolicy variants) and docs/represent/imputation.md (impute_missing_values + the three ImputationMethod strategies), each with a new hand-authored inline SVG and an executed offline FDARS_FENCE_OK worked example, wire both into the mkdocs.yml Represent nav, and prove `mkdocs build --strict` exits 0 offline plus the SVGO idempotence gate passes on both new SVGs.

Purpose: This is the tracer plan for Phase 29 — it proves the whole authoring→build→gate pipeline (new page + new inline SVG + executed fence + nav wiring + strict build + SVGO idempotence) on the represent section BEFORE the analyze/align plans author the rest. If any part of the toolchain is broken, it surfaces here after one commit.
Output: 2 new represent pages, 2 new SVGs, 2 nav entries, a green strict build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Method definitions (authoritative for method-accuracy) — Group 1 rows:
@.planning/research/FEATURES.md

# The SVG style contract — ALL new diagrams must conform:
@docs/assets/diagrams/STYLE_SPEC.md

# Style/prose exemplar for a represent section page (match its tone + fence style):
@docs/represent/basis-representation.md

# The exact executed-fence pattern to reuse (exec="1" html="1" source="above" + FDARS_FENCE_OK sentinel):
@docs/advisor/python-api.md

# A conforming SVG to match structurally (viewBox 0 0 720 300, canonical <style>, role/aria-label):
@docs/assets/diagrams/landmark-registration.svg

# Nav file — new pages go under the Represent section:
@mkdocs.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — interpolation.md page + interpolation-policy.svg + executed fence + nav + strict build</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (canonical &lt;style&gt; block, viewBox 0 0 720 300, palette, stroke weights, role="img"+aria-label)
    - docs/assets/diagrams/landmark-registration.svg (a conforming multi-panel SVG to match structurally)
    - docs/represent/basis-representation.md (page prose + fence style exemplar)
    - docs/advisor/python-api.md lines 17-30 (the exact `exec="1" html="1" source="above"` fence + FDARS_FENCE_OK sentinel print pattern)
    - .planning/research/FEATURES.md (spline_interpolate + spline_interpolate_with_policy + ExtrapolationPolicy rows — the four policy behaviors)
    - mkdocs.yml lines 92-100 (the Represent nav block)
  </read_first>
  <files>docs/represent/interpolation.md, docs/assets/diagrams/interpolation-policy.svg, mkdocs.yml</files>
  <action>
    Author the FIRST new page end-to-end through every layer of the docs toolchain — this is the tracer, so keep it to ONE page and prove the whole pipeline before expanding.

    (a) Write docs/assets/diagrams/interpolation-policy.svg as hand-authored inline SVG conforming to STYLE_SPEC.md: root `&lt;svg xmlns viewBox="0 0 720 300" fill="none" role="img" aria-label="..."&gt;` with an aria-label matching the title text; the canonical five-class `&lt;style&gt;` block copied verbatim; `.ttl` title at y=26 and `.sub` subtitle at y=46. Depict spline interpolation of off-grid query points AND the four ExtrapolationPolicy variants (Boundary/Exception/Fill/Periodic) as distinct panels or annotations. METHOD-ACCURACY (bake into the visual): the Exception panel must show a red stop/error marker at the domain boundary indicating it RAISES rather than extrapolating (do NOT draw an extrapolated curve for Exception); Fill must show the curve dropping to a constant fill line (dashed) beyond the domain; Periodic must show the curve wrapping and repeating its start; Boundary must show a flat horizontal clamp at the boundary value. Use the FDARS data-curve palette (#3f51b5 indigo primary) and structural greys per the spec.

    (b) Write docs/represent/interpolation.md: title + intro prose explaining spline_interpolate (per-curve B-spline fit-then-evaluate at arbitrary query points, order 1/2/4) and the ExtrapolationPolicy contract, embed the SVG with `![Interpolation — concept diagram](../assets/diagrams/interpolation-policy.svg){ .fdars-diagram }`, and add ONE executed worked-example fence using ```python exec="1" html="1" source="above"```. The fence MUST: import `from docs_data import load_growth` (or load_canadian_weather), call the REAL shipped binding `fdars.represent.spline_interpolate_with_policy(X, argvals, query_points, policy=..., ...)` (data-only signature: data, argvals, query_points, policy, fill_value, order) to upsample onto a finer grid, be network-free and deterministic (no RNG or a fixed seed only), and end with a print line containing the literal sentinel FDARS_FENCE_OK. Note in prose the exact policy string values ("boundary"/"exception"/"fill"/"periodic") and that "exception" raises a ValueError on out-of-domain points. Optionally use `from docs_fig import fig, render` for a rendered figure as in basis-representation.md.

    (c) Edit mkdocs.yml: add `- Interpolation: represent/interpolation.md` to the Represent nav block (after Basis Representation is a natural slot). Use a scoped Edit, not a rewrite.

    Do NOT author imputation.md yet — that is Task 2 (expansion). Prove this one page end-to-end first.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict 2>&amp;1 | tail -5; test -f site/represent/interpolation/index.html &amp;&amp; grep -rq "FDARS_FENCE_OK" site/represent/interpolation/index.html &amp;&amp; echo TRACER_FENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict` exits 0 (see verify command; exit status 0).
    - The built page site/represent/interpolation/index.html contains the sentinel FDARS_FENCE_OK (executed fence ran offline).
    - SVGO idempotence holds on the new SVG: `first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/interpolation-policy.svg --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$first") <(printf '%s' "$second")` produces empty output (exit 0).
    - `grep -c 'role="img"' docs/assets/diagrams/interpolation-policy.svg` returns 1 and `grep -c 'viewBox="0 0 720 300"' docs/assets/diagrams/interpolation-policy.svg` returns 1.
    - `grep -c 'represent/interpolation.md' mkdocs.yml` returns 1 (page wired into nav).
  </acceptance_criteria>
  <done>interpolation.md renders on the strict build with an executed FDARS_FENCE_OK fence, its SVG passes SVGO idempotence and conforms to STYLE_SPEC, and it is nav-wired — the full toolchain is proven end-to-end and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Expansion — imputation.md page + imputation.svg + executed fence + nav</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (same conformance contract)
    - docs/represent/interpolation.md (the page just authored in Task 1 — match its structure)
    - .planning/research/FEATURES.md (impute_missing_values + ImputationMethod row — Linear/Mean/Constant behaviors)
    - docs/advisor/python-api.md lines 17-30 (fence + FDARS_FENCE_OK pattern)
  </read_first>
  <files>docs/represent/imputation.md, docs/assets/diagrams/imputation.svg, mkdocs.yml</files>
  <action>
    Reuse the toolchain proven in Task 1 for the second represent page.

    (a) Write docs/assets/diagrams/imputation.svg (hand-authored inline SVG, STYLE_SPEC-conforming: viewBox 0 0 720 300, canonical &lt;style&gt;, role/aria-label). Depict a single curve with NaN gap segments shown as dotted breaks. METHOD-ACCURACY: the Linear method must ramp linearly across an INTERIOR gap between the nearest valid neighbours, and at a BOUNDARY gap must extend FLAT (horizontal) from the last valid value (not ramp to zero). Show Mean and Constant as horizontal fill lines at different levels in a small legend. Make clear all non-NaN values are preserved (only gaps change).

    (b) Write docs/represent/imputation.md mirroring interpolation.md's structure: intro prose on impute_missing_values + the three ImputationMethod strategies (string values "linear"/"mean"/"constant"), the SVG embed, and ONE executed fence ```python exec="1" html="1" source="above"``` that: loads a docs/data dataset (growth or canadian_weather), synthetically injects NaN with a FIXED seed (mask ~8% of cells), calls the REAL binding `fdars.represent.impute_missing_values(X_with_nan, argvals, method="linear")` (signature: data, argvals, method, constant_value), asserts no NaN remain, and prints a line containing FDARS_FENCE_OK. Deterministic + offline. In prose, note the boundary-gap flat-extension behavior and that a curve that is entirely NaN raises ValueError.

    (c) Edit mkdocs.yml: add `- Imputation: represent/imputation.md` immediately after the Interpolation entry. Scoped Edit only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict 2>&amp;1 | tail -5; grep -rq "FDARS_FENCE_OK" site/represent/imputation/index.html &amp;&amp; echo IMPUTE_FENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict` exits 0 with both represent pages present.
    - site/represent/imputation/index.html contains FDARS_FENCE_OK.
    - SVGO idempotence holds on docs/assets/diagrams/imputation.svg (twice-run diff is empty, per the Task-1 svgo command applied to this file).
    - `grep -c 'role="img"' docs/assets/diagrams/imputation.svg` returns 1; `grep -c 'viewBox="0 0 720 300"' docs/assets/diagrams/imputation.svg` returns 1.
    - `grep -c 'represent/imputation.md' mkdocs.yml` returns 1.
  </acceptance_criteria>
  <done>imputation.md renders with an executed FDARS_FENCE_OK fence, its SVG passes SVGO idempotence and STYLE_SPEC, and it is nav-wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human spot-check of the two represent SVGs for method-accuracy</name>
  <what-built>Two new represent docs pages (interpolation, imputation) each with a hand-authored inline SVG and an executed offline worked example, wired into the Represent nav, on a green strict build.</what-built>
  <how-to-verify>
    Build and visually check the two new SVGs for method-accuracy (this is a lightweight per-plan spot-check; the full ~6-diagram review is the Phase-final checkpoint in Plan 04):
    1. Build: `cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate; PYTHONPATH=scripts mkdocs build` (full build, no DOCS_FAST, source of truth).
    2. Render the SVGs to PNG for inspection: `rsvg-convert docs/assets/diagrams/interpolation-policy.svg -o /tmp/interp.png; rsvg-convert docs/assets/diagrams/imputation.svg -o /tmp/impute.png` and open both.
    3. Confirm on interpolation-policy.svg: the Exception panel shows a STOP/error marker at the boundary (does NOT draw an extrapolated curve); Fill drops to a constant; Periodic wraps; Boundary clamps flat.
    4. Confirm on imputation.svg: Linear ramps across the interior gap and extends FLAT at the boundary gap; Mean/Constant are horizontal fills.
    5. Optionally serve and read both pages at http://127.0.0.1:8000 (`mkdocs serve`).
  </how-to-verify>
  <resume-signal>Type "approved" or describe diagram/prose corrections needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → executed fence | markdown-exec runs arbitrary Python at build time; must stay offline/deterministic |
| hand-authored SVG → SVGO gate | committed markup is source of truth; gate is check-only, never rewrites |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Information Disclosure | executed markdown-exec fence | medium | mitigate | fences are network-free, base extras only, fixed seeds; no API key referenced (advisor advise() stays illustrative) |
| T-29-02 | Tampering | committed hand-authored SVG | low | mitigate | SVGO gate runs in stdout mode (--output -), idempotence check only, never rewrites the committed file |
| T-29-03 | Denial of Service | non-deterministic fence output | medium | mitigate | build-determinism gate (svg.hashsalt=fdars-docs, DOCS_FAST parity) + fixed seeds keep the strict build reproducible |
</threat_model>

<verification>
- `PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict` exits 0 with interpolation.md and imputation.md present.
- Both built pages contain FDARS_FENCE_OK.
- Both new SVGs pass the SVGO idempotence gate (twice-run diff empty) and carry role="img" + viewBox="0 0 720 300".
- Both pages appear in the mkdocs.yml Represent nav.
</verification>

<success_criteria>
Two nav-wired represent pages render on a green strict build, each with a method-accurate STYLE_SPEC-conforming SVG (SVGO-idempotent) and an executed offline FDARS_FENCE_OK worked example against the real shipped fdars.represent bindings. The full docs toolchain is proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/29-docs-diagrams-worked-examples/29-01-SUMMARY.md` when done.
</output>

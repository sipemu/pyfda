---
phase: 27-scoring-metrics-alignment-registration-bindings
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scoring_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_scoring.py
autonomous: true
requirements: [STAT-03]
tags: [rust, pyo3, fdars-core, scoring, functional-data]

estimate:
  tokens: 42000
  raw_tokens: 24000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "fdars.scoring.functional_mse(X, X, argvals) == 0.0 for identical curves (STAT-03)"
    - "All 5 metrics (functional_mae, functional_mse, functional_mape, functional_msle, functional_explained_variance) reachable via both `from fdars.scoring import ...` and `fdars.scoring.<fn>(...)`"
    - "MAPE on near-zero true values raises ValueError; MSLE on values <= -1 raises ValueError (STAT-03)"
    - "No .unwrap() in src/scoring_mod.rs; every fallible call routes through to_pyresult()"
  artifacts:
    - "src/scoring_mod.rs (NEW module with 5 pyfunctions + register fn)"
    - "tests/test_scoring.py (NEW: namespace, hand-checked MSE, ValueError guards)"
  key_links:
    - "src/lib.rs register_submodule!(m, \"scoring\", scoring_mod::register)"
    - "python/fdars/__init__.py _submodule_names includes \"scoring\""
---

<objective>
Stand up the NEW `fdars.scoring` submodule and bind the 5 fdars-core 0.17.0 functional
prediction-scoring metrics (`functional_mae`, `functional_mse`, `functional_mape`,
`functional_msle`, `functional_explained_variance`), each a Simpson-integrated scalar
scoring predicted-vs-true curves. Fallible inputs (MAPE near-zero truths, MSLE values ≤ −1)
surface as clean Python `ValueError` via `to_pyresult()`, never a Rust panic. Implements
STAT-03 and CONTEXT.md D: "NEW fdars.scoring submodule holds the 5 metrics".

Purpose: A clean namespace for error-scoring predicted-vs-true curves, conceptually distinct
from `fdars.metric`'s geometric distances — mirroring the Phase 26 `fdars.represent` split.
Output: `src/scoring_mod.rs`, `fdars.scoring` namespace, 5 pyfunctions, `tests/test_scoring.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scoring-metrics-alignment-registration-bindings/27-CONTEXT.md
@.planning/phases/26-interpolation-imputation-functional-statistics-bindings/26-01-SUMMARY.md
@src/represent_mod.rs
@src/lib.rs
@src/convert.rs
@python/fdars/__init__.py
</context>

<artifacts_this_phase_produces>
- NEW module `src/scoring_mod.rs` with 5 `#[pyfunction]`s + `register()` fn
- NEW `fdars.scoring` native namespace (registered in `src/lib.rs` + `_submodule_names`)
- pyfunctions: `functional_mae`, `functional_mse`, `functional_mape`, `functional_msle`, `functional_explained_variance`
- NEW `tests/test_scoring.py`
</artifacts_this_phase_produces>

<confirmed_signatures>
Confirmed against the installed fdars-core 0.17.0 source
(`~/.cargo/registry/src/*/fdars-core-0.17.0/src/scoring.rs` + `src/lib.rs` re-exports).
All 5 share the identical signature and are re-exported at crate top level:

- `fdars_core::functional_mae(y_true: &FdMatrix, y_pred: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>`
- `fdars_core::functional_mse(...)  -> Result<f64, FdarError>`
- `fdars_core::functional_mape(...) -> Result<f64, FdarError>`  (rejects |y_true| below NUMERICAL_EPS → InvalidParameter)
- `fdars_core::functional_msle(...) -> Result<f64, FdarError>`  (rejects any value ≤ −1 → InvalidParameter)
- `fdars_core::functional_explained_variance(...) -> Result<f64, FdarError>`

Argument order is `(y_true, y_pred, argvals)` — assert with asymmetric inputs so a
swapped-order binding is caught. `fdars_core::spline_interpolate` is called the same
top-level way in `src/represent_mod.rs` — mirror that call style.
</confirmed_signatures>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — stand up fdars.scoring end-to-end with functional_mse + hand-checked assertion</name>
  <files>src/scoring_mod.rs, src/lib.rs, python/fdars/__init__.py, tests/test_scoring.py</files>
  <read_first>
    - src/represent_mod.rs — the Phase-26 NEW-submodule reference: module doc header, `use crate::convert::*;`, `#[pyfunction]` shape, `numpy2d_to_fdmatrix`/`numpy1d_to_vec`/`to_pyresult` usage, and the trailing `pub fn register(m: &Bound<'_, PyModule>) -> PyResult<()>` that calls `m.add_function(wrap_pyfunction!(...))`.
    - src/lib.rs — the `mod represent_mod;` declaration (line ~25) and `register_submodule!(m, "represent", represent_mod::register)` (line ~56); add the `scoring` analogs.
    - src/convert.rs — `numpy2d_to_fdmatrix`, `numpy1d_to_vec`, `to_pyresult` (the only helpers this module needs; scalar returns need no `fdmatrix_to_numpy2d`).
    - python/fdars/__init__.py lines 34-53 — the `_submodule_names` tuple (add `"scoring"`); the registration loop and `__all__` pick up the new name automatically.
    - The `<confirmed_signatures>` block above for the exact `functional_mse` signature and argument order.
  </read_first>
  <action>
Create `src/scoring_mod.rs` mirroring `src/represent_mod.rs`: a `//!` module doc header noting
this is the `fdars.scoring` submodule for prediction-scoring metrics, `use crate::convert::*;`,
`use numpy::{PyReadonlyArray1, PyReadonlyArray2};`, `use pyo3::prelude::*;`. In the tracer, write
ONLY `functional_mse` as a `#[pyfunction]` taking `(py, y_true: PyReadonlyArray2<f64>,
y_pred: PyReadonlyArray2<f64>, argvals: PyReadonlyArray1<f64>) -> PyResult<f64>`: convert both
matrices via `numpy2d_to_fdmatrix(...)?`, `argvals` via `numpy1d_to_vec`, call
`to_pyresult(fdars_core::functional_mse(&yt, &yp, &av))?`, return the f64. Add the trailing
`pub fn register(...)` registering just `functional_mse` (siblings added in Task 2). Every fallible
core call must propagate through `to_pyresult()` — never panic-unwrap a Result. Wire the module
end-to-end: in `src/lib.rs` add `mod scoring_mod;` beside the other mod
declarations and `register_submodule!(m, "scoring", scoring_mod::register);` beside the represent
registration. In `python/fdars/__init__.py` add `"scoring"` to the `_submodule_names` tuple.
Rebuild with `.venv/bin/maturin develop`. Create `tests/test_scoring.py` with a namespace test
(both `from fdars.scoring import functional_mse` and `fdars.scoring.functional_mse` resolve) and a
hand-checked assertion: build a small literal `y_true` (e.g. 2 curves on a linspace grid),
`functional_mse(y_true, y_true, argvals)` must be `pytest.approx(0.0, abs=1e-12)`; and a
non-identical pair whose integrated squared error is analytically known (e.g. constant offset c
over domain length L → per-curve MSE = c²·L, averaged) asserted with `pytest.approx`.
Verify green before Task 2 expands.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_scoring.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from fdars.scoring import functional_mse; import fdars; print(fdars.scoring.functional_mse)"` succeeds (both import paths resolve).
    - `tests/test_scoring.py` passes: identical-curve MSE == approx(0.0); known-offset MSE matches the analytic value.
    - `grep -v '^\s*//' src/scoring_mod.rs | grep -c 'unwrap()'` returns `0`.
  </acceptance_criteria>
  <done>fdars.scoring exists end-to-end; functional_mse bound, registered, rebuilt, and its hand-checked assertions pass; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand — bind the 4 remaining metrics with MAPE/MSLE ValueError guards</name>
  <files>src/scoring_mod.rs, tests/test_scoring.py</files>
  <read_first>
    - src/scoring_mod.rs — the Task-1 `functional_mse` wrapper and `register()` fn (add the 4 siblings following the identical shape).
    - src/represent_mod.rs — confirm the `PyResult<f64>`-vs-matrix return conventions and the `to_pyresult` propagation used for fallible core calls.
    - The `<confirmed_signatures>` block for the MAPE (near-zero rejection) and MSLE (≤ −1 rejection) failure conditions.
    - tests/test_scoring.py from Task 1 (extend, do not rewrite).
  </read_first>
  <behavior>
    - functional_mae(y_true, y_pred, argvals) returns a finite non-negative float; == approx(0.0) for identical curves.
    - functional_mape rejects near-zero true values: y_true containing an entry at/near 0.0 raises ValueError (no epsilon guard upstream).
    - functional_msle rejects values ≤ −1: a y_true or y_pred entry of -1.5 raises ValueError.
    - functional_explained_variance(y_true, y_pred, argvals) returns a float ≤ 1.0; == approx(1.0) for identical curves.
    - Argument order is (y_true, y_pred): an asymmetric MAE case (distinct y_true, y_pred) is invariant under order for MAE/MSE but the test documents the (y_true, y_pred) contract; MAPE (asymmetric in y_true) confirms order via its zero-guard triggering on y_true, not y_pred.
  </behavior>
  <action>
In `src/scoring_mod.rs`, add `functional_mae`, `functional_mape`, `functional_msle`, and
`functional_explained_variance` as `#[pyfunction]`s, each identical in shape to the Task-1
`functional_mse` wrapper — `(py, y_true, y_pred, argvals) -> PyResult<f64>`, both matrices through
`numpy2d_to_fdmatrix(...)?`, `argvals` through `numpy1d_to_vec`, call the matching
`fdars_core::functional_*` through `to_pyresult(...)?`, return the f64. Give each a NumPy-style
docstring naming the metric, the `(y_true, y_pred, argvals)` order, and the ValueError conditions
(MAPE: near-zero true values; MSLE: values ≤ −1). Register all four in `register()`. Every fallible
core call must propagate through `to_pyresult()` — never panic-unwrap a Result.
Rebuild with `.venv/bin/maturin develop`. Extend `tests/test_scoring.py`: identical-curve MAE ==
approx(0.0) and explained_variance == approx(1.0); `pytest.raises(ValueError)` for MAPE with a
y_true entry of 0.0 and for MSLE with a value of -1.5; a MAPE zero-guard-on-y_true case
documenting that the first positional arg is y_true. Then run the FULL suite and confirm no
regressions against the 328-passed baseline.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_scoring.py -q 2>&1 | tail -15 && .venv/bin/pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_scoring.py -q` passes, including `pytest.raises(ValueError)` for MAPE near-zero and MSLE ≤ −1.
    - `.venv/bin/python -c "import fdars; [getattr(fdars.scoring, f) for f in ('functional_mae','functional_mse','functional_mape','functional_msle','functional_explained_variance')]; print('ok')"` prints `ok`.
    - `grep -v '^\s*//' src/scoring_mod.rs | grep -c 'unwrap()'` returns `0`.
    - Full suite: `.venv/bin/pytest tests/ -q` shows no regressions vs the 328-passed / 4-skipped baseline (328 + new scoring tests all pass).
  </acceptance_criteria>
  <done>All 5 scoring metrics bound and registered; MAPE/MSLE ValueError guards tested; full suite green with no regressions; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 wrapper | Untrusted numpy arrays (shape/NaN/near-zero/negative values) cross into Rust here |
| PyO3 wrapper → fdars-core | Converted FdMatrix/argvals cross into the compute engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01-01 | Denial of Service | `.unwrap()` panic on fallible fdars-core Result (MAPE near-zero, MSLE ≤ −1) | high | mitigate | Every call routes through `to_pyresult()`; `grep -c unwrap()` gate == 0; `pytest.raises(ValueError)` tests for MAPE and MSLE |
| T-27-01-02 | Tampering | Swapped `(y_true, y_pred)` argument order silently mis-scores | medium | mitigate | Test documents order; MAPE zero-guard fires on the first positional arg (y_true), pinning the contract |
| T-27-01-03 | Information Disclosure | Wrong numeric result from an incorrect Simpson/integration wiring | medium | mitigate | Hand-checked `functional_mse(X,X)==0` and known-offset analytic value assertions |

No npm/pip/cargo installs introduced in this plan (fdars-core 0.17.0 already pinned in Phase 25); package-legitimacy gate not applicable.
</threat_model>

<verification>
- `fdars.scoring` reachable via both import styles; all 5 metrics present.
- Hand-checked: `functional_mse(X, X)` == approx(0.0); known-offset MSE matches analytic value; `functional_explained_variance(X, X)` == approx(1.0).
- `pytest.raises(ValueError)` for MAPE near-zero truths and MSLE values ≤ −1.
- `grep -v '^\s*//' src/scoring_mod.rs | grep -c 'unwrap()'` == 0.
- Full suite green, no regressions vs 328-passed baseline.
</verification>

<success_criteria>
STAT-03 satisfied: user can score functional predictions with all 5 metrics; fallible inputs
(MAPE near-zero truths, MSLE values ≤ −1) surface as `ValueError` via `to_pyresult()` with no
`.unwrap()` panics; `functional_mse` of identical curves is hand-verified == 0.
</success_criteria>

<output>
Create `.planning/phases/27-scoring-metrics-alignment-registration-bindings/27-01-SUMMARY.md` when done.
</output>

---
phase: 56-transformers
plan: 03
type: execute
wave: 3
depends_on: [56-02]
files_modified:
  - tests/sklearn/test_transformer_pipeline.py
autonomous: true
requirements: [XFORM-01, XFORM-06]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Pipeline([smoother, fpca]) fit->transform round-trips: the grid-changing chain produces finite (n_obs, n_components) scores (XFORM-06)"
    - "FPCATransformer fit is idempotent under SVD sign canonicalization — repeated fits produce identical components_ (XFORM-01)"
    - "Every transformer's source calls fdars._native.* and never constructs an Fdata (contract assertion across all 8)"
  artifacts:
    - tests/sklearn/test_transformer_pipeline.py
  key_links:
    - "sklearn.pipeline.Pipeline([BSplineSmoother, FPCATransformer]) -> fit -> transform -> (n_obs, n_components) scores"
    - "FPCATransformer.fit x2 -> identical components_ (sign-canonicalized)"
---

<objective>
Close the transformer family with the cross-transformer guarantees the earlier plans could not assert in isolation: the grid-changing `Pipeline([smoother, fpca])` chain (XFORM-06) and the FPCA idempotence regression guard (XFORM-01), plus a static contract check that NO transformer constructs an Fdata.

Purpose: XFORM-06 is an explicit success criterion that only becomes testable once smoothers and FPCA are both green (Waves 1-2). The Fdata-free contract assertion protects the milestone-wide "call fdars._native.* directly, never construct an Fdata" rule against future edits.
Output: A new `tests/sklearn/test_transformer_pipeline.py` proving the chain round-trips, FPCA fit is idempotent, and all 8 transformers are Fdata-free.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-transformers/56-CONTEXT.md

@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_base.py
@tests/sklearn/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline([smoother, fpca]) round-trip test + FPCA idempotence guard (XFORM-06, XFORM-01)</name>
  <files>tests/sklearn/test_transformer_pipeline.py</files>
  <read_first>
python/fdars/sklearn/_skeletons.py (FPCATransformer.fit — SVD sign canonicalization via _sign_canonicalize, components_ shape (n_comp, n_pts); BSplineSmoother.transform — shape-preserving (n_obs, n_points) output that feeds FPCA);
python/fdars/sklearn/_base.py (_sign_canonicalize — why repeated fits produce identical components_, the basis for check_fit_idempotent);
tests/sklearn/conftest.py (sklearn importorskip already gates this directory).
  </read_first>
  <action>
Create tests/sklearn/test_transformer_pipeline.py. Build a small deterministic (n_obs, n_points) float64 dataset with a fixed numpy seed (keep it small — no network, fast). Write test_smoother_fpca_pipeline_roundtrip: construct sklearn.pipeline.Pipeline([("smoother", BSplineSmoother()), ("fpca", FPCATransformer(n_components=2))]), call fit(X) then transform(X), and assert the output shape is (n_obs, 2), the output is all-finite, and fit_transform(X) matches the fit-then-transform result. Write test_fpca_fit_idempotent: fit an FPCATransformer twice on the same X and assert np.allclose on components_ across the two fits (regression guard for the SVD sign canonicalization behind XFORM-01). Import estimators from fdars.sklearn._skeletons.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_transformer_pipeline.py -q -k "pipeline_roundtrip or fpca_fit_idempotent"</automated>
  </verify>
  <done>The smoother->FPCA pipeline fits and transforms to a finite (n_obs, 2) score matrix and fit_transform is consistent with fit+transform (XFORM-06); two FPCA fits on the same data yield identical components_ (XFORM-01).</done>
</task>

<task type="auto">
  <name>Task 2: Static contract test — no transformer constructs an Fdata</name>
  <files>tests/sklearn/test_transformer_pipeline.py</files>
  <read_first>
python/fdars/sklearn/_skeletons.py (the 8 transformer classes: FPCATransformer, BSplineSmoother, LocalPolynomialSmoother, BasisRepresentation, Imputer, SplineInterpolator, DepthTransformer, NormTransformer — confirm each calls _native.* and none references Fdata);
.planning/phases/56-transformers/56-CONTEXT.md (contract: estimators call fdars._native.* directly and NEVER construct an Fdata — dtype side-effects break check_estimator).
  </read_first>
  <action>
Add test_transformers_never_construct_fdata to tests/sklearn/test_transformer_pipeline.py. For each of the 8 transformer classes, get inspect.getsource(cls) and assert the source does not construct an Fdata (no "Fdata(" call) and that it references "_native" (i.e. routes compute through the native layer). Parametrize over the 8 classes so each is an independently-named case. This is a source-level guard — it must NOT import fdars.fdata_class or instantiate Fdata itself.
  </action>
  <verify>
    <automated>.venv/bin/pytest "tests/sklearn/test_transformer_pipeline.py::test_transformers_never_construct_fdata" -q</automated>
  </verify>
  <done>All 8 transformer classes are asserted Fdata-free and native-routed at the source level; the test parametrizes one case per class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chained transformers (smoother -> fpca) | Intermediate grid-changing output crosses from one estimator to the next inside a Pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-05 | Tampering | transformer source contract drift | low | mitigate | static test asserts no transformer constructs an Fdata, catching a regression that would reintroduce dtype side-effects breaking check_estimator |
| T-56-06 | Information Disclosure | pipeline test dataset | low | accept | test data is a small deterministic in-memory synthetic array; no external data, no network |
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_transformer_pipeline.py -q` — pipeline round-trip, FPCA idempotence, and the Fdata-free contract all green.
- `.venv/bin/pytest tests/sklearn/ -q` — the full sklearn test tree stays green (all 8 transformers + registry + pipeline).
</verification>

<success_criteria>
XFORM-06 pipeline round-trip test passes; XFORM-01 FPCA idempotence regression guard passes; all 8 transformers proven Fdata-free at the source level; whole tests/sklearn/ tree green.
</success_criteria>

<output>
Create `.planning/phases/56-transformers/56-03-SUMMARY.md` when done.
</output>

---
phase: 57-regressors-classifiers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_regressors_compliance.py
autonomous: true
requirements: [REG-01]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "REG-01: FPCRegressor passes the full per-estimator parametrize_with_checks battery — check_regressors_train (R2>0.5) + check_methods_subset_invariance + check_requires_y_none all green"
    - "REG-01: FPCRegressor.score() works via RegressorMixin (r2_score on stored-model predict) — score() NOT overridden"
    - "REG-01: PLSRegressor passes the full per-estimator parametrize_with_checks battery with the shared _require_y guard"
    - "predict() is stored-model (fit-on-train-only via native predict_fregre_lm / predict_fregre_pls) — predict(X[mask]) == predict(X)[mask], no vstack of train+test"
    - "Shared helper _require_y(estimator, y) exists at module scope and raises ValueError with the substring 'requires y to be passed' — reused by every regressor/classifier in Plans 02"
  artifacts:
    - "python/fdars/sklearn/_skeletons.py module-level _require_y helper"
    - "tests/sklearn/test_regressors_compliance.py (per-estimator parametrize_with_checks for FPCRegressor + PLSRegressor)"
    - "python/fdars/sklearn/_coverage.py TRIAGE_VERDICTS['FPCRegressor'] + ['PLSRegressor'] flipped to PASS"
  key_links:
    - "FPCRegressor.predict -> _native.regression.predict_fregre_lm(X_fit_, y_fit_, X_new, n_comp) — native re-fits on STORED train only, predicts X_new, inherently subset-invariant"
    - "PLSRegressor.predict -> _native.regression.predict_fregre_pls(X_fit_, argvals_, y_fit_, X_new, n_comp)"
    - "_require_y(self, y) raising ValueError('...requires y to be passed...') called at top of every regressor/classifier fit"
---

<objective>
Tracer for Phase 57: promote FPCRegressor to a fully `check_estimator`-green `RegressorMixin` end-to-end via the stored-model-predict pattern, proving the exact recipe every remaining predictor in Plan 02 reuses. Bring PLSRegressor along (it is one guard away) so REG-01 is fully satisfied in this plan. Establish the shared `_require_y` guard once so Plan 02 references it by name (no forward-reference to undefined code).

Purpose: prove the stored-model-predict architecture — and the shared y=None guard — on the agent's best early-context tokens before expanding to the differentiator regressors and the classifier family in Plan 02. REG-01 requires FPC-based + PLS regression to be `RegressorMixin` estimators exposing a working `score()`.
Output: `_require_y` module helper; FPCRegressor + PLSRegressor green under per-estimator `parametrize_with_checks`; their `_coverage.py` verdicts flipped to PASS; a fast compliance test file mirroring the Phase-56 transformer harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_base.py
@python/fdars/sklearn/_coverage.py
@tests/sklearn/test_transformers_compliance.py
</context>

<artifacts_produced>
New symbols this plan introduces (Plan 02 depends on these existing by name):
- `python/fdars/sklearn/_skeletons.py::_require_y(estimator, y)` — module-level helper raising `ValueError` with the substring `requires y to be passed` when `y is None`.
- `FPCRegressor.X_fit_`, `.y_fit_`, `.n_components_` — stored fit state consumed by `predict`.
- `PLSRegressor.X_fit_`, `.argvals_`, `.y_fit_`, `.n_components_` — stored fit state consumed by `predict`.
- `tests/sklearn/test_regressors_compliance.py::test_fpc_regressor_compliance`, `::test_pls_regressor_compliance`.
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Shared _require_y helper + FPCRegressor stored-model predict — full parametrize_with_checks green (REG-01)</name>
  <files>python/fdars/sklearn/_skeletons.py, tests/sklearn/test_regressors_compliance.py</files>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 64-110 (module-level helpers _validate_allow_nan, _pairwise_l2 — add _require_y beside them) and lines 918-989 (current FPCRegressor: already stores X_fit_/y_fit_/n_components_ and predicts via predict_fregre_lm; the vstack anti-pattern is NOT present here — the failures are R2 and y=None only);
    src/regression_mod.rs lines 476-493 (predict_fregre_lm(data_fit, response, new_data, n_comp): re-fits fregre_lm on data_fit+response ONLY, then predicts new_data — subset-invariant by construction);
    src/regression_mod.rs lines 110-132 (fregre_lm returns dict: fitted_values, residuals, beta_t, r_squared, coefficients, intercept);
    python/fdars/sklearn/_base.py lines 122-188 (_BaseFdarsEstimator contract: verbatim arg storage, _resolve_argvals, float64 upcast);
    _coverage.py lines 194-204 (FPCRegressor 4 failures: check_regressors_train x3 R2<=0.5 + check_requires_y_none).
  </read_first>
  <behavior>
    - Module scope gains `_require_y(estimator, y)`: raises `ValueError` whose message contains the substring `requires y to be passed` when `y is None`; returns nothing otherwise. Message format: f-string beginning with the estimator class name, e.g. `f"{type(estimator).__name__} requires y to be passed; got y=None."`
    - parametrize_with_checks([FPCRegressor()]) runs every check green (no exemptions, no xfail).
    - check_regressors_train: R2 > 0.5 on the sklearn battery data (~100 obs, ~20 features). score() is inherited from RegressorMixin (r2_score(y, predict(X))) — NOT overridden.
    - check_methods_subset_invariance: predict(X[mask]) equals predict(X)[mask] — predict passes stored X_fit_/y_fit_ to predict_fregre_lm so each test row is independent of siblings.
    - check_requires_y_none: fit(X, y=None) raises the _require_y ValueError.
  </behavior>
  <action>
    Add a module-level helper `_require_y(estimator, y)` next to `_pairwise_l2` (around line 111). It raises `ValueError(f"{type(estimator).__name__} requires y to be passed; got y=None.")` when `y is None`. This is the single shared y=None guard the whole phase reuses; Plan 02 calls it by this exact name. Do NOT place the literal acceptance substring `requires y to be passed` in any comment — it appears only inside the raised message string.

    In FPCRegressor.fit, call `_require_y(self, y)` as the FIRST statement (before `_validate`), so fit(X, y=None) raises before any array work. Keep the rest of fit as-is: `_validate(self, X, y, ...)`, float64 upcast, the `n_obs < self._min_samples` guard, compute `result = _native.regression.fregre_lm(X, y, n_comp)`, and store `X_fit_`, `y_fit_`, `n_components_`, `r_squared_`, `fitted_values_`.

    Fix the R2<=0.5 failure (root cause: default `n_components=3` capped by `min(n_components, n_obs-1, n_pts)` still under-fits the battery's random-Gaussian design). Choose an effective component count that gives a real fit on battery-sized data (~100x~20): compute `n_comp = min(self.n_components, n_obs - 1, n_pts)` but raise the DEFAULT: change the constructor default from `n_components=3` to a higher value that the battery needs (e.g. `n_components=10`) so the stored fit projects onto enough FPCs to clear R2>0.5, while the `min(...)` cap keeps small-battery cases (1-feature, few-sample) valid. Store the effective `n_components_` used. Do not clip in `__init__` (verbatim storage; cap only in fit). Confirm `r_squared_` reflects a genuine in-sample fit.

    predict is already correct: `_native.regression.predict_fregre_lm(self.X_fit_, self.y_fit_, X, self.n_components_)`. Leave it — it re-fits on stored train ONLY and predicts X (subset-invariant). Do NOT introduce any `np.vstack([X_fit_, X])`.

    Keep the contract: subclass _BaseFdarsEstimator, store constructor args verbatim, resolve argvals_ in fit, n_features_in_ via _validate then astype float64, call _native directly, never construct Fdata.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py::test_fpc_regressor_compliance -q</automated>
  </verify>
  <acceptance_criteria>
    - `_require_y` exists at module scope; `FPCRegressor().fit(np.random.rand(5,4), None)` raises ValueError containing "requires y to be passed".
    - test_fpc_regressor_compliance passes every parametrize_with_checks check (0 failures).
    - `FPCRegressor.predict` source contains no `vstack`; predict(X[mask]) == predict(X)[mask].
    - `score()` is not defined on FPCRegressor (inherited from RegressorMixin).
  </acceptance_criteria>
  <done>_require_y helper added; FPCRegressor passes every parametrize_with_checks check; predict is stored-model (no vstack); fit(X, y=None) raises the shared ValueError.</done>
  <reversibility rating="reversible">Constructor default n_components change and helper add are local to _skeletons.py; revert is a one-line edit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: PLSRegressor _require_y guard + full compliance green (REG-01)</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 992-1059 (PLSRegressor — already stores X_fit_/argvals_/y_fit_/n_components_ and predicts via predict_fregre_pls, no vstack; sole failure is check_requires_y_none);
    src/regression_mod.rs lines 514-535 (predict_fregre_pls(data, argvals, response, new_data, n_comp): re-fits on stored train only);
    the _require_y helper added in Task 1;
    _coverage.py line 204 (PLSRegressor 1 failure: check_requires_y_none).
  </read_first>
  <action>
    In PLSRegressor.fit, call `_require_y(self, y)` as the FIRST statement (before `_validate`). PLSRegressor already predicts via `predict_fregre_pls(X_fit_, argvals_, y_fit_, X_new, n_comp)` (stored-model, subset-invariant) — do not change predict wiring and do NOT add any vstack.

    Confirm check_regressors_train R2>0.5 holds on battery data. If PLS under-fits at the current default, apply the same fix as FPCRegressor: raise the constructor default `n_components` (e.g. to 10) and keep the `n_comp = min(self.n_components, n_obs-1, n_pts)` cap in fit. Do not override score() (RegressorMixin provides it).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py::test_pls_regressor_compliance -q</automated>
  </verify>
  <acceptance_criteria>
    - `PLSRegressor().fit(np.random.rand(5,4), None)` raises ValueError containing "requires y to be passed".
    - test_pls_regressor_compliance passes every parametrize_with_checks check including check_requires_y_none and check_regressors_train.
    - PLSRegressor.predict source contains no `vstack`.
  </acceptance_criteria>
  <done>PLSRegressor passes every parametrize_with_checks check; y=None guard uses the shared _require_y.</done>
</task>

<task type="auto">
  <name>Task 3: Per-estimator compliance harness + flip FPC/PLS verdicts to PASS (REG-01)</name>
  <files>tests/sklearn/test_regressors_compliance.py, python/fdars/sklearn/_coverage.py</files>
  <read_first>
    tests/sklearn/test_transformers_compliance.py (mirror this: one @parametrize_with_checks([Estimator()])-decorated function per estimator with a check(estimator) body);
    _coverage.py lines 194-204 (FPCRegressor + PLSRegressor TRIAGE_VERDICTS entries and their explanatory comments).
  </read_first>
  <action>
    Create tests/sklearn/test_regressors_compliance.py mirroring the transformer harness: import FPCRegressor + PLSRegressor from `fdars.sklearn._skeletons`; define `test_fpc_regressor_compliance` and `test_pls_regressor_compliance`, each decorated with `@parametrize_with_checks([Estimator()])` and a `check(estimator)` body. Instantiate each estimator with its promoted defaults (bare `FPCRegressor()` / `PLSRegressor()`). Keep it a per-estimator battery (NOT the 28-estimator triage) so it stays fast.

    In _coverage.py, flip `TRIAGE_VERDICTS["FPCRegressor"]` and `TRIAGE_VERDICTS["PLSRegressor"]` to `"PASS"` and update their explanatory comments to record the Phase-57 fix: stored-model predict via predict_fregre_lm/pls (no vstack) + raised default n_components + shared _require_y guard. Leave RobustFPCRegressor/GLMRegressor/NonparametricRegressor and all classifiers untouched (Plan 02).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py -q && .venv/bin/python -c "from fdars.sklearn._coverage import TRIAGE_VERDICTS as v; assert v['FPCRegressor']=='PASS' and v['PLSRegressor']=='PASS', v"</automated>
  </verify>
  <acceptance_criteria>
    - test_regressors_compliance.py green for both estimators.
    - `TRIAGE_VERDICTS['FPCRegressor']` and `['PLSRegressor']` both equal exactly `"PASS"`.
    - tests/sklearn/test_coverage.py still green (verdict-domain + registry integrity unaffected).
  </acceptance_criteria>
  <done>test_regressors_compliance.py green for both estimators; _coverage.py verdicts for FPCRegressor + PLSRegressor read PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/battery -> estimator | sklearn's check_estimator supplies arbitrary numeric arrays; estimator must validate and never construct Fdata |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01 | Tampering | float32/dtype inputs to native predict | low | mitigate | _validate then explicit astype(float64) before every _native call (base-class contract) |
| T-57-02 | Denial of Service | tiny-sample battery data (1 sample/1 feature) | low | mitigate | _min_samples guard raising ValueError with "1 sample" substring before native call |

No package installs in this plan (pure-Python over existing bindings) — no supply-chain threat.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_regressors_compliance.py -q` green.
- FPCRegressor + PLSRegressor verdicts PASS in _coverage.py; test_coverage.py green.
- No `vstack` in FPCRegressor/PLSRegressor predict.
</verification>

<success_criteria>
FPC-based + PLS regression are `check_estimator`-green `RegressorMixin` estimators with a working `score()` (REG-01, success criterion 1). Stored-model-predict pattern + shared `_require_y` guard proven for the rest of the phase.
</success_criteria>

<output>
Create `.planning/phases/57-regressors-classifiers/57-01-SUMMARY.md` when done.
</output>

---
phase: 59-documentation-docs-gate
plan: 03
type: execute
wave: 3
depends_on: [59-01, 59-02]
files_modified:
  - docs/sklearn/gridsearch-example.md
  - docs/assets/diagrams/sklearn-pipeline-dataflow.svg
  - docs/sklearn/index.md
  - mkdocs.yml
autonomous: true
requirements: [DOCS-02, DOCS-03]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "An offline FDARS_FENCE_OK GridSearchCV worked example exists and renders (DOCS-02)."
    - "A method-accurate hand-authored inline SVG (ndarray → transformer → FPC scores → predictor in a Pipeline) exists, meets the v7.0 STYLE_SPEC, and passes the SVGO idempotence check (DOCS-03)."
  artifacts:
    - "docs/sklearn/gridsearch-example.md"
    - "docs/assets/diagrams/sklearn-pipeline-dataflow.svg"
  key_links:
    - "docs/sklearn/gridsearch-example.md fence → sklearn.model_selection.GridSearchCV over a fdars Pipeline (executes offline)"
    - "docs/sklearn/index.md → embeds sklearn-pipeline-dataflow.svg via ![...](../assets/diagrams/…svg){ .fdars-diagram }"
    - "docs/assets/diagrams/sklearn-pipeline-dataflow.svg ↔ STYLE_SPEC (viewBox 0 0 720 {300|480}, five canonical CSS classes, role='img'+aria-label)"
---

<objective>
Author the GridSearchCV offline worked example (DOCS-02) and the method-accurate hand-authored inline SVG data-flow diagram (DOCS-03), and embed the diagram on the concept page.

Purpose: Complete the worked-example surface (Pipeline example landed in Plan 01; GridSearchCV example here) and deliver the required method-accurate diagram meeting the v7.0 STYLE_SPEC + SVGO-idempotence bar. The blocking human diagram review and the whole-site strict gate happen in Plan 04.
Output: `docs/sklearn/gridsearch-example.md`, `docs/assets/diagrams/sklearn-pipeline-dataflow.svg`, diagram embed on `index.md`, extended nav.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

DOCS phase, SEQUENTIAL on `main`, no worktrees. Build from repo root against the main-tree `.venv`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
@docs/represent/interpolation.md            # FDARS_FENCE_OK fence idiom (exec/html/source, docs_fig fig+render, sentinel print)
@tests/sklearn/test_predictive_pipeline.py  # GridSearchCV over Pipeline([Imputer,BSplineSmoother,FPCATransformer,FPCLDAClassifier]) with param_grid fpca__n_components/clf__ncomp — mirror this small grid
@docs/assets/diagrams/STYLE_SPEC.md         # SVG contract: SVGO invocation, canonical <style> block (5 classes), viewBox 720×{300|480}, role/aria, palette, panel patterns
@docs/assets/diagrams/advisor-grounding-invariant.svg  # concrete conforming SVG to copy structure from (two-lane data-flow with arrows)
@python/fdars/sklearn/_coverage.py          # confirm estimator names used in the fence + diagram labels
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GridSearchCV offline worked example page</name>
  <files>docs/sklearn/gridsearch-example.md, mkdocs.yml</files>
  <precondition>The main-tree `.venv` has scikit-learn importable (`.venv/bin/python -c "from sklearn.model_selection import GridSearchCV"`) so the fence executes offline at build time.</precondition>
  <action>
Author `docs/sklearn/gridsearch-example.md`: a short intro on tuning a functional Pipeline with sklearn's `GridSearchCV`, then ONE offline live fence (`exec="1" html="1" source="above"`) that mirrors `test_gridsearch_predictive_pipeline`:
  - Build a SMALL synthetic two-class dataset inline (~40 obs × 20 pts, `np.random.default_rng(7)`, mean-shift 3.0, sparse NaN via `X[::5, 2::7] = np.nan` so the Imputer does real work).
  - `train_test_split(..., stratify=y, random_state=7)`.
  - `pipe = Pipeline([("imputer", Imputer()), ("smoother", BSplineSmoother()), ("fpca", FPCATransformer()), ("clf", FPCLDAClassifier())])` from `fdars.sklearn._skeletons`.
  - `param_grid = {"fpca__n_components": [2, 3], "clf__ncomp": [1, 2]}`, `GridSearchCV(pipe, param_grid, cv=3, refit=True)`, `.fit(X_train, y_train)`.
  - Plot the CV mean score per candidate (bar/line from `grid.cv_results_["mean_test_score"]`) via `from docs_fig import fig, render`; render with `print(render(f))`.
  - End the fence body printing the sentinel: `print("best_params:", grid.best_params_, " FDARS_FENCE_OK")`.
  Keep it OFFLINE, small, fast (2×2 grid, cv=3 = 12 fits, well under a minute). Then add a nav entry `- GridSearchCV Example: sklearn/gridsearch-example.md` to the `scikit-learn API` section (scoped Edit).
  </action>
  <verify>
    <automated>PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict --site-dir /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-03 2>&1 | tee /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/build59-03.log; grep -q "FDARS_FENCE_OK" /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-03/sklearn/gridsearch-example/index.html && echo GRIDSEARCH_OK</automated>
  </verify>
  <done>docs/sklearn/gridsearch-example.md renders the GridSearchCV fence to HTML containing FDARS_FENCE_OK; the scoped strict build exits 0; the page is in nav.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-authored inline SVG data-flow diagram + embed on index.md</name>
  <files>docs/assets/diagrams/sklearn-pipeline-dataflow.svg, docs/sklearn/index.md</files>
  <behavior>
    - The SVG root has `viewBox="0 0 720 300"` (or `0 0 720 480` if a two-row layout is needed), `fill="none"`, `role="img"`, and an `aria-label` matching the title text.
    - The SVG contains the canonical `<style>` block with exactly the five classes `.ttl .sub .lab .sm .mono` (verbatim from STYLE_SPEC).
    - `svgo(svgo(svg)) == svgo(svg)` — the idempotence gate passes under `svgo.config.mjs` pinned `svgo@3.3.4`.
    - index.md references the SVG via `![...](../assets/diagrams/sklearn-pipeline-dataflow.svg){ .fdars-diagram }`.
  </behavior>
  <action>
Hand-author `docs/assets/diagrams/sklearn-pipeline-dataflow.svg` depicting the METHOD-ACCURATE data flow of a functional sklearn Pipeline, copying the structural conventions of `advisor-grounding-invariant.svg` and obeying STYLE_SPEC exactly. Depict the left-to-right flow: `(n_obs, n_points)` ndarray input → transformer stage(s) (e.g. Imputer → BSplineSmoother) → FPCATransformer producing `(n_obs, n_components)` FPC scores → a predictor (classifier/regressor) → prediction, all bounded inside a "sklearn Pipeline" frame. Use the canonical `<style>` block verbatim, `viewBox="0 0 720 300"` (upgrade to `0 0 720 480` only if the flow needs two rows), `fill="none"`, `role="img"`, and an `aria-label` matching the `.ttl` title. Use `.ttl` for the title (~y=28), `.sub` for a one-line subtitle, `.lab`/`.mono`/`.sm` for stage labels (mono for class names like `FPCATransformer`), neutral panels `fill="#f8f9fa" stroke="#ced4da"` for data stages and the orange method panel `fill="#fff4ea" stroke="#fd7e14"` for the FPCA/predictor computation, and arrows `stroke-width="2"` between stages. Accuracy requirement: labels must reflect what the layer actually does — FPCATransformer maps `(n_obs, n_points) → (n_obs, n_components)` scores; the predictor consumes the SCORE matrix, not raw curves. Do NOT invent method behavior. After authoring, run the SVGO idempotence check locally and fix any non-idempotent construct. Then embed the diagram on `docs/sklearn/index.md` with `![functional sklearn Pipeline data flow: ndarray → transformer → FPC scores → predictor](../assets/diagrams/sklearn-pipeline-dataflow.svg){ .fdars-diagram }` near the "families at a glance" prose.
  </action>
  <verify>
    <automated>node -e "const {execFileSync}=require('child_process'); const f='docs/assets/diagrams/sklearn-pipeline-dataflow.svg'; const one=execFileSync('npx',['svgo@3.3.4','--config','svgo.config.mjs','--quiet','--input',f,'--output','-']).toString(); const fs=require('fs'); const tmp=require('os').tmpdir()+'/p1.svg'; fs.writeFileSync(tmp,one); const two=execFileSync('npx',['svgo@3.3.4','--config','svgo.config.mjs','--quiet','--input',tmp,'--output','-']).toString(); process.exit(one===two && /role=\"img\"/.test(one) && /viewBox=\"0 0 720 (300|480)\"/.test(one) && /\.ttl\{/.test(one) && /\.mono\{/.test(one) ? 0 : 1)"</automated>
  </verify>
  <done>sklearn-pipeline-dataflow.svg passes the SVGO idempotence check with role="img", a 720×{300|480} viewBox, and the five canonical CSS classes; index.md embeds it with the .fdars-diagram class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → GridSearchCV fence | build-time execution of first-party fence Python against local `.venv` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-03 | Tampering | live-fence + SVGO invocation | low | accept | Fence is offline first-party code; SVG is hand-authored static asset with no script content; no runtime attack surface. |
| T-59-SC | Tampering | npx svgo@3.3.4 | low | accept | SVGO is pinned to the exact version already used by the diagram gate across v7.0/v8.0; check-only idempotence run, no install into the package. |
</threat_model>

<verification>
- GridSearchCV fence renders offline to HTML containing FDARS_FENCE_OK under a scoped strict build.
- The new SVG passes SVGO idempotence and carries role="img" + 720×{300|480} viewBox + five canonical classes.
- index.md embeds the diagram.
</verification>

<success_criteria>
An offline GridSearchCV worked example emits FDARS_FENCE_OK and a method-accurate hand-authored SVG (ndarray → transformer → FPC scores → predictor) passes the SVGO idempotence gate and is embedded on the concept page.
</success_criteria>

<output>
Create `.planning/phases/59-documentation-docs-gate/59-03-SUMMARY.md` when done.
</output>

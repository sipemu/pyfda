---
phase: 55-compliance-triage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - python/fdars/sklearn/__init__.py
  - python/fdars/sklearn/_base.py
  - python/fdars/sklearn/_coverage.py
  - python/fdars/sklearn/_skeletons.py
  - tests/sklearn/conftest.py
  - tests/sklearn/test_triage.py
  - tests/sklearn/test_foundation.py
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04, TRIAGE-01, TRIAGE-03]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "import fdars succeeds with zero scikit-learn installed (FND-01); python/fdars/__init__.py git-diff is empty (FND-02)"
    - "import fdars.sklearn without the extra raises an actionable ImportError naming pip install fdars[sklearn] (FND-01, FND-02)"
    - "_BaseFdarsEstimator stores argvals verbatim in __init__, resolves self.argvals_ (default np.arange(n_features)) in fit, sets n_features_in_ via validate_data, casts float32->float64, and passes clone/get_params/set_params (FND-03)"
    - "The validate_data + tags-API shim works on both the sklearn 1.6+ public-function path and the 1.3-1.5 private-method path (FND-03)"
    - "FPCATransformer calls fdars._native.regression.fpca directly (never builds an Fdata) and PASSES parametrize_with_checks end-to-end with SVD sign canonicalization (FND-04, TRIAGE-01, TRIAGE-03)"
  artifacts:
    - "pyproject.toml with [sklearn] extra (scikit-learn>=1.3,<1.7) and scikit-learn added to [dev]"
    - "python/fdars/sklearn/__init__.py (gated import + public exports)"
    - "python/fdars/sklearn/_base.py (_BaseFdarsEstimator, _validate shim, _sign_canonicalize)"
    - "python/fdars/sklearn/_skeletons.py (FPCATransformer + import surface for later skeletons)"
    - "python/fdars/sklearn/_coverage.py (EXCLUDED_METHODS, TRIAGE_VERDICTS scaffolds)"
    - "tests/sklearn/conftest.py (importorskip guard)"
    - "tests/sklearn/test_triage.py (parametrize_with_checks harness seeded with FPCATransformer)"
    - "tests/sklearn/test_foundation.py (gating + base-class contract tests)"
  key_links:
    - "sklearn/__init__.py try/except ImportError gate -> actionable message (mirrors mcp/__init__.py)"
    - "_validate shim -> validate_data (1.6+) OR estimator._validate_data (1.3-1.5)"
    - "FPCATransformer.fit -> _native.regression.fpca(X, argvals_, n_comp) -> sign-canonicalized components_"
    - "test_triage.py -> parametrize_with_checks([FPCATransformer(...)]) all-green"
---

<objective>
TRACER: Establish the v9.0 sklearn foundation end-to-end by wiring ONE path through every layer the phase touches — the `[sklearn]` optional extra, the gated `python/fdars/sklearn/` subpackage, the shared `_BaseFdarsEstimator` base class (with the hand-rolled validate_data + tags-API compat shim), and ONE real production-quality estimator (`FPCATransformer`) that PASSES `parametrize_with_checks`. This proves the gating pattern, the base-class contract, the compat shim, and the triage harness all work together before Plan 02 expands to skeleton the remaining ~30 candidates.

Purpose: Prove the whole foundation architecture on the executor's best early-context tokens. If the base-class contract or the tags shim has an architectural dead-end, catch it after one commit — not after all 31 skeletons are written.

Output: `pyproject.toml` `[sklearn]` extra; `python/fdars/sklearn/{__init__,_base,_skeletons,_coverage}.py`; `tests/sklearn/{conftest,test_triage,test_foundation}.py`. FPCATransformer is a real, keep-forever estimator (not a prototype) that the Phase 56 transformer family builds on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-compliance-triage-foundation/55-CONTEXT.md
@.planning/phases/55-compliance-triage-foundation/55-RESEARCH.md

# Gating pattern to mirror exactly (deferred import + actionable ImportError):
@python/fdars/mcp/__init__.py
@python/fdars/advisor/__init__.py
# Existing extras stanza to extend (do NOT rewrite the whole file):
@pyproject.toml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: [sklearn] extra + gated subpackage skeleton + base class + compat shim</name>
  <files>pyproject.toml, python/fdars/sklearn/__init__.py, python/fdars/sklearn/_base.py, python/fdars/sklearn/_coverage.py, python/fdars/sklearn/_skeletons.py</files>
  <read_first>
    - python/fdars/mcp/__init__.py — the try/except ImportError gate shape to copy (FND-02)
    - 55-RESEARCH.md "Complete `_base.py` Skeleton", "`sklearn/__init__.py` Gating", "Pattern 5: `_coverage.py` Format" — verbatim code sketches
    - pyproject.toml lines 39-63 — existing [project.optional-dependencies] format
  </read_first>
  <action>
    Extend `pyproject.toml` `[project.optional-dependencies]` (per FND-01) by ADDING a `sklearn = ["scikit-learn>=1.3,<1.7"]` extra alongside the existing `advisor`/`mcp` extras, and append `"scikit-learn>=1.3,<1.7"` to the existing `dev` extra list so the compliance tests run in dev. Add a comment noting sklearn 1.7 drops Python 3.9. Use Edit for scoped changes — do NOT touch `python/fdars/__init__.py` (FND-02: its git diff MUST stay empty this phase) and do NOT touch the maturin stanza.

    Create `python/fdars/sklearn/__init__.py` mirroring the `mcp/__init__.py` deferred-import gate (FND-02): a `try: from sklearn.base import BaseEstimator` guard that on ImportError re-raises `ImportError` whose message contains the literal install hint `pip install fdars[sklearn]` (FND-01). After the gate, import `_BaseFdarsEstimator` from `._base` and `EXCLUDED_METHODS, TRIAGE_VERDICTS` from `._coverage`, and export them via `__all__`. Add a module docstring noting it is NOT registered in `fdars.__init__` and never imported by plain `import fdars`.

    Create `python/fdars/sklearn/_base.py` with the hand-rolled compat shim (FND-03) — use the hand-rolled try/import approach, NOT the `sklearn-compat` PyPI package (it is SUS-rated per RESEARCH Package Legitimacy Audit): (1) a module-level `_validate(estimator, X, y=None, *, reset=True, dtype="numeric", **kw)` function that tries `sklearn.utils.validation.validate_data` (1.6+) and falls back to `estimator._validate_data` (1.3-1.5); (2) a `_HAS_TAGS_DATACLASS` boolean detecting `sklearn.utils.Tags`; (3) `_BaseFdarsEstimator(BaseEstimator)` storing `argvals` VERBATIM in `__init__` (no conversion — required for clone round-trip, FND-03), a `_resolve_argvals(n_features)` returning `np.arange(n_features, dtype=np.float64)` when `argvals is None` else `np.asarray(self.argvals, dtype=np.float64)`, and a static `_sign_canonicalize(components, scores)` that flips each component so its largest-abs element is positive (FND-03, for FPCA idempotence). Do NOT convert argvals in __init__ — resolve only in fit.

    Create `python/fdars/sklearn/_coverage.py` scaffolds (TRIAGE-02 groundwork): a module docstring, an `EXCLUDED_METHODS` dict pre-seeded with the structural pre-excludes from RESEARCH "Pre-Excluded" table (each entry `{"reason": str, "failing_check": str | None, "functional_api": str}`), and an empty `TRIAGE_VERDICTS` dict with a comment that Plan 03 populates it after the triage run.

    Create `python/fdars/sklearn/_skeletons.py` containing ONLY the real `FPCATransformer(TransformerMixin, _BaseFdarsEstimator)` for now (per RESEARCH "Minimal Skeleton Pattern"): `__init__(argvals=None, n_components=3)` storing `n_components` verbatim; `_min_samples = 2` class attr; `fit` validates via `_validate(self, X, reset=True, dtype="numeric", ensure_2d=True)` then `X.astype(np.float64)`, guards `n_obs < self._min_samples` raising `ValueError` whose message contains the substring `1 sample` when applicable, computes `n_comp = min(self.n_components, n_obs - 1, n_pts)`, sets `self.argvals_`, calls `_native.regression.fpca(X, self.argvals_, n_comp)`, transposes rotation to `(n_comp, n_pts)`, applies `_sign_canonicalize`, stores `components_`/`mean_`/`n_components_`; `transform` runs `check_is_fitted`, validates with `reset=False`, centers and projects; `get_feature_names_out` returns `fpca{i}` names. Import `_native` via `from fdars import _native`. NEVER construct an Fdata (FND-04). This is a keep-forever estimator — Phase 56 imports it.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('python/fdars/sklearn/_base.py').read()); ast.parse(open('python/fdars/sklearn/_skeletons.py').read()); ast.parse(open('python/fdars/sklearn/__init__.py').read()); ast.parse(open('python/fdars/sklearn/_coverage.py').read()); print('parse-ok')" && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); ex=d['project']['optional-dependencies']; assert any('scikit-learn' in p for p in ex['sklearn']), 'sklearn extra missing'; assert any('scikit-learn' in p for p in ex['dev']), 'scikit-learn not in dev'; print('pyproject-ok')"</automated>
  </verify>
  <done>pyproject.toml has a `[sklearn]` extra pinned `scikit-learn>=1.3,<1.7` and `scikit-learn` added to `[dev]`; the four `python/fdars/sklearn/*.py` files parse; `_BaseFdarsEstimator`, `_validate`, `_sign_canonicalize`, `EXCLUDED_METHODS`, `TRIAGE_VERDICTS`, and `FPCATransformer` all exist.</done>
</task>

<task type="auto">
  <name>Task 2: Foundation contract tests — gating + base-class round-trips (both sklearn API paths)</name>
  <files>tests/sklearn/conftest.py, tests/sklearn/test_foundation.py</files>
  <read_first>
    - python/fdars/sklearn/_base.py (from Task 1) — the shim and base-class surface under test
    - 55-RESEARCH.md "Pattern 4" conftest importorskip snippet
    - Success criteria 1 and 2 in ROADMAP Phase 55 — the exact facts to assert
  </read_first>
  <action>
    Create `tests/sklearn/conftest.py` with `import pytest; pytest.importorskip("sklearn", reason="[sklearn] extra not installed")` so the whole sklearn test tree skips cleanly when the extra is absent (FND-01 base-package-stays-sklearn-free guarantee).

    Create `tests/sklearn/test_foundation.py` asserting the FND success facts. (a) Base package purity: a subprocess `python -c "import fdars"` (via `sys.executable`) exits 0 — assert `import fdars` needs zero sklearn (FND-01). (b) Actionable ImportError: skip if sklearn IS installed; otherwise assert `import fdars.sklearn` raises ImportError whose message contains `pip install fdars[sklearn]` (FND-01, FND-02). (c) `python/fdars/__init__.py` unchanged: assert `git diff --quiet -- python/fdars/__init__.py` returns 0 (FND-02) via subprocess. (d) Verbatim storage + clone: build `FPCATransformer(n_components=2)`, assert `get_params()["argvals"] is None` and `get_params()["n_components"] == 2`, then `clone(est)` round-trips params (FND-03). (e) set_params: `est.set_params(n_components=3)` then `get_params()["n_components"] == 3`. (f) fit contract: fit on a `(6, 10)` float64 array, assert `n_features_in_ == 10`, `hasattr(est, "argvals_")` and `est.argvals_.shape == (10,)` defaulting to `np.arange(10)` (FND-03). (g) float32 upcast: fit on a float32 array does NOT raise and produces float64 `components_` (FND-03, FND-04). (h) Shim coverage: assert the module-level `_validate` from `_base` is callable and that exactly one of the two shim branches is active by importing `_HAS_TAGS_DATACLASS` and asserting it is a bool — do not hard-code which sklearn version is present so the test passes on both the 1.3-1.5 and 1.6 paths (FND-03).
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && python -m pytest tests/sklearn/test_foundation.py -x -q || python -m pytest tests/sklearn/test_foundation.py -q -rs 2>&1 | grep -qiE "skipped|passed"</automated>
  </verify>
  <done>With sklearn installed, `pytest tests/sklearn/test_foundation.py` passes all gating + base-class contract assertions on whichever sklearn API path is present; without sklearn the module skips cleanly via importorskip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Triage harness seeded with FPCATransformer — one PASS end-to-end</name>
  <files>tests/sklearn/test_triage.py</files>
  <behavior>
    - parametrize_with_checks([FPCATransformer(n_components=1)]) yields per-check named tests
    - FPCATransformer passes check_fit_idempotent (SVD sign canonicalization holds)
    - FPCATransformer passes check_estimators_dtypes (float32 accepted + upcast)
    - FPCATransformer passes clone/get_params/set_params checks
    - No check errors from an internally-constructed Fdata (FND-04)
  </behavior>
  <read_first>
    - python/fdars/sklearn/_skeletons.py (from Task 1) — FPCATransformer under test
    - 55-RESEARCH.md "Pattern 4: Triage Harness" — the parametrize_with_checks recipe and verdict rule
  </read_first>
  <action>
    Create `tests/sklearn/test_triage.py` per RESEARCH Pattern 4 but seeded with ONLY `FPCATransformer(n_components=1)` for this tracer plan (Plan 02 expands `_ALL_SKELETONS` to all candidates). Import `parametrize_with_checks` from `sklearn.utils.estimator_checks`; define `_ALL_SKELETONS = [FPCATransformer(n_components=1)]`; write `@parametrize_with_checks(_ALL_SKELETONS)` decorating `def test_sklearn_triage(estimator, check): check(estimator)`. Add a module docstring documenting the run command and the PASS / PASS-WITH-FIXES / EXCLUDE verdict rule (all-pass=PASS; only-fixable-guard-fails=PASS-WITH-FIXES; structural-fails=EXCLUDE) so Plan 02/03 executors reuse it. This proves the go/no-go viable-core mechanism works on the FPCA member of the required core (TRIAGE-03) and that the harness surfaces per-check results (TRIAGE-01 mechanism).
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && python -m pytest tests/sklearn/test_triage.py -q && python -m pytest "tests/sklearn/test_triage.py" -q -k "FPCATransformer and (idempotent or dtypes)" || python -m pytest tests/sklearn/test_triage.py -q -rs 2>&1 | grep -qiE "skipped|passed"</automated>
  </verify>
  <done>With sklearn installed, every `parametrize_with_checks` case for `FPCATransformer` PASSES (green end-to-end, proving one viable-core member); without sklearn the harness skips cleanly. The FPCA go/no-go member is confirmed viable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user code -> fdars.sklearn estimators | Untrusted numpy arrays enter fit/transform; validated by sklearn `validate_data` (NaN/inf rejection default) before any native call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Tampering | numpy array input to fit/transform | low | mitigate | `_validate(dtype="numeric")` rejects non-finite/non-numeric input before `_native.*`; explicit `.astype(np.float64)` prevents dtype-confusion into the Rust FFI |
| T-55-02 | Denial of Service | per-curve/large-array native calls | low | accept | Pure in-process compute over caller-supplied arrays; no network, no unbounded external input; caller controls array size |
| T-55-SC | Tampering | `pip install fdars[sklearn]` (scikit-learn) | low | mitigate | Only new dependency is `scikit-learn` — definitively legitimate (top-5 PyPI package); its SUS registry verdict is a metadata artifact per RESEARCH Package Legitimacy Audit; `sklearn-compat` (SUS) explicitly NOT adopted — hand-rolled shim used instead |

Note: this is a pure-Python estimator layer over existing in-process compute — no network, no auth, no new external input surface beyond numpy arrays already validated by sklearn. Surface is minimal; no ASVS categories apply (per RESEARCH Security Domain). ai-integration / schema-gate / assumption-delta capability notes are N/A (sklearn is a conformance framework, not a remote API/ORM/identity transition).
</threat_model>

<verification>
- `import fdars` works with zero sklearn (subprocess exit 0)
- `import fdars.sklearn` without the extra raises ImportError containing `pip install fdars[sklearn]`
- `git diff --quiet -- python/fdars/__init__.py` returns 0
- With sklearn installed: `pytest tests/sklearn/ -q` green (foundation + FPCATransformer triage all-PASS)
</verification>

<success_criteria>
- pyproject.toml `[sklearn]` extra present and pinned `>=1.3,<1.7`; scikit-learn in `[dev]`
- `python/fdars/sklearn/` subpackage gated exactly like `mcp`; `python/fdars/__init__.py` unchanged
- `_BaseFdarsEstimator` passes clone/get_params/set_params, sets n_features_in_ via validate_data, casts float32->float64, resolves argvals_ in fit only
- Compat shim exercised on whichever of the 1.3-1.5 / 1.6 sklearn paths is present
- FPCATransformer PASSES `parametrize_with_checks` end-to-end (viable-core FPCA member confirmed)
</success_criteria>

<output>
Create `.planning/phases/55-compliance-triage-foundation/55-01-SUMMARY.md` when done. In it, record the exact sklearn version present in the dev env, which shim branch (1.6+ public vs 1.3-1.5 private) was active, and confirm FPCATransformer is all-green — Plan 02 needs these facts.
</output>

---
phase: 58-clusterers-outlier-detectors-compliance-gate
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_clusterers_compliance.py
autonomous: true
requirements: [CLUS-01, CLUS-02]
estimate:
  tokens: 42000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "FunctionalKMeans produces identical labels_ across two fits with the same fixed random_state (deterministic; no non_deterministic tag needed)"
    - "FuzzyFunctionalCMeans.fit and FunctionalGMM.fit set a self.n_iter_ integer attribute so check_non_transformer_estimators_n_iter passes — resolves deferred review WR-03"
    - "FunctionalKMeans, FuzzyFunctionalCMeans, and FunctionalGMM each pass the full parametrize_with_checks battery with zero exemptions"
  artifacts:
    - "python/fdars/sklearn/_skeletons.py — n_iter_ added to FuzzyFunctionalCMeans.fit and FunctionalGMM.fit"
    - "tests/sklearn/test_clusterers_compliance.py — three per-clusterer compliance tests + a FunctionalKMeans determinism test"
  key_links:
    - "random_state → u64 seed via check_random_state; the same seed yields the same native kmeans_fd cluster assignment (empirically confirmed deterministic)"
    - "fuzzy_cmeans_fd / gmm_cluster native results expose NO iteration count key, so n_iter_ = max_iter with a documenting comment (same resolution LogisticFPCClassifier used)"
---

<objective>
Complete the clusterer family: add the missing `n_iter_` fitted attribute to FuzzyFunctionalCMeans and FunctionalGMM (the sole `check_non_transformer_estimators_n_iter` failure, resolving deferred Phase-57 review WR-03), and lock in FunctionalKMeans with a determinism regression test under a fixed `random_state`.

Purpose: Deliver CLUS-01 (FunctionalKMeans green + deterministic) and CLUS-02 (fuzzy c-means, functional GMM green). The rayon-parallel kmeans path is empirically deterministic under a fixed seed (confirmed: two `kmeans_fd(..., seed=42)` calls give identical cluster assignments), so NO `non_deterministic` sklearn tag is needed — a plain determinism assertion suffices.

Output: Two clusterers with n_iter_; three green clusterer batteries; a FunctionalKMeans determinism test; `_coverage.py` shows all three clusterers PASS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-clusterers-outlier-detectors-compliance-gate/58-CONTEXT.md
@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_coverage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add n_iter_ to FuzzyFunctionalCMeans and FunctionalGMM (WR-03)</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <read_first>
    - python/fdars/sklearn/_skeletons.py lines 2200-2267 (FuzzyFunctionalCMeans.fit — result keys are `cluster`/`membership`/`centers`, NO iteration count) and 2288-2359 (FunctionalGMM.fit — result keys `cluster`/`membership`/`bic_values`/`icl_values`, NO iteration count).
    - python/fdars/sklearn/_skeletons.py lines 2065-2067 (LogisticFPCClassifier: `self.n_iter_ = self.max_iter` with the documenting comment "native does not expose iterations") — the established WR-03-style resolution to mirror exactly.
    - python/fdars/sklearn/_skeletons.py lines 2178 (FunctionalKMeans already sets `self.n_iter_ = int(result["iter"])` — kmeans_fd DOES expose it; do not touch).
  </read_first>
  <behavior>
    - After `FuzzyFunctionalCMeans().fit(X)`, `est.n_iter_` is an int and equals `est.max_iter` (native exposes no iteration count).
    - After `FunctionalGMM().fit(X)`, `est.n_iter_` is an int and equals `est.max_iter`.
    - Both are set inside fit, after the native call, before `return self`.
  </behavior>
  <action>
    In FuzzyFunctionalCMeans.fit, after `self.cluster_centers_ = np.array(result["centers"])` and before `return self`, add `self.n_iter_ = self.max_iter  # fuzzy_cmeans_fd exposes no iteration count; use max_iter (same convention as LogisticFPCClassifier, resolves WR-03)`.
    In FunctionalGMM.fit, after `self.X_fit_ = X` and before `return self`, add `self.n_iter_ = self.max_iter  # gmm_cluster result exposes bic/icl but no iteration count; use max_iter (resolves WR-03)`.
    Do NOT invent a fake iteration count from bic_values/icl_values length — those are the k-range sweep values, not EM iterations; `max_iter` is the honest upper bound and matches the LogisticFPCClassifier precedent.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np; from fdars.sklearn._skeletons import FuzzyFunctionalCMeans, FunctionalGMM; X=np.random.RandomState(0).rand(30,12); import sys; f=FuzzyFunctionalCMeans().fit(X); g=FunctionalGMM().fit(X); sys.exit(0 if (isinstance(f.n_iter_,int) and f.n_iter_==f.max_iter and isinstance(g.n_iter_,int) and g.n_iter_==g.max_iter) else 1)"</automated>
  </verify>
  <done>Both FuzzyFunctionalCMeans and FunctionalGMM set an integer `self.n_iter_` in fit equal to their `max_iter`.</done>
  <reversibility rating="reversible">Two-line attribute additions; git-checkout revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Clusterer compliance tests + determinism test + flip _coverage verdicts</name>
  <files>tests/sklearn/test_clusterers_compliance.py, python/fdars/sklearn/_coverage.py</files>
  <read_first>
    - tests/sklearn/test_classifiers_compliance.py lines 40-108 (the per-estimator `@parametrize_with_checks` structure + docstring style to mirror).
    - python/fdars/sklearn/_skeletons.py lines 2142-2197 (FunctionalKMeans.fit/predict — labels_ + fixed random_state=42 default) for the determinism test.
    - python/fdars/sklearn/_coverage.py lines 272-279 (FuzzyFunctionalCMeans + FunctionalGMM PASS-WITH-FIXES entries to flip; FunctionalKMeans is already PASS).
  </read_first>
  <action>
    Create tests/sklearn/test_clusterers_compliance.py mirroring test_classifiers_compliance.py. Include:
    - `@parametrize_with_checks([FunctionalKMeans(n_clusters=2)])` → `test_functional_kmeans_compliance` (regression guard, CLUS-01).
    - `@parametrize_with_checks([FuzzyFunctionalCMeans(n_clusters=2)])` → `test_fuzzy_cmeans_compliance` (docstring: `check_non_transformer_estimators_n_iter` now green via n_iter_, CLUS-02).
    - `@parametrize_with_checks([FunctionalGMM(n_clusters=2)])` → `test_functional_gmm_compliance` (same, CLUS-02).
    - A plain `def test_functional_kmeans_deterministic()`: with X=RandomState(0).rand(40,15), assert `np.array_equal(FunctionalKMeans(n_clusters=3, random_state=7).fit(X).labels_, FunctionalKMeans(n_clusters=3, random_state=7).fit(X).labels_)` — proves fixed-random_state reproducibility (CLUS-01) and documents in a comment that the rayon-parallel path is deterministic under a fixed seed, so no `non_deterministic` tag is required.
    Import all three clusterers from `fdars.sklearn._skeletons`.
    In _coverage.py, flip `"FuzzyFunctionalCMeans"` and `"FunctionalGMM"` from PASS-WITH-FIXES to `"PASS"`, updating each preceding comment to state the Phase-58-Plan-03 fix (n_iter_ = max_iter, WR-03 resolved).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_clusterers_compliance.py -q</automated>
  </verify>
  <done>All three clusterer batteries green (0 failed), the determinism test passes, and `_coverage.TRIAGE_VERDICTS` shows FunctionalKMeans/FuzzyFunctionalCMeans/FunctionalGMM all "PASS".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ndarray → native clustering | `check_estimator` supplies arbitrary arrays; skeletons validate + upcast to float64 and map random_state→u64 seed before native calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-03 | Repudiation | FunctionalKMeans reproducibility | low | mitigate | Determinism test pins random_state→seed reproducibility so results are auditable across runs |

No package installs; no supply-chain gate required.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_clusterers_compliance.py -q` green.
- n_iter_ automated check (Task 1) passes.
- No regression: `.venv/bin/pytest tests/sklearn/test_coverage.py -q` still green.
</verification>

<success_criteria>
FunctionalKMeans, FuzzyFunctionalCMeans, and FunctionalGMM pass the full parametrize_with_checks battery with zero exemptions; FunctionalKMeans is deterministic under a fixed random_state; both fuzzy/GMM expose n_iter_ (WR-03 resolved); all three recorded PASS in `_coverage.py` (CLUS-01 + CLUS-02 complete).
</success_criteria>

<output>
Create `.planning/phases/58-clusterers-outlier-detectors-compliance-gate/58-03-SUMMARY.md` when done.
</output>

---
phase: 58-clusterers-outlier-detectors-compliance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_outliers_compliance.py
autonomous: true
requirements: [OUT-01]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "MagnitudeShapeDetector.score_samples(X[mask]) equals score_samples(X)[mask] (subset-invariant) — resolves deferred review CR-03"
    - "MagnitudeShapeDetector.predict(X) yields both -1 and +1 on the sklearn battery via a contamination-derived offset_"
    - "MagnitudeShapeDetector passes the full parametrize_with_checks battery with zero exemptions"
  artifacts:
    - "python/fdars/sklearn/_skeletons.py — _BaseFdarsOutlierDetector carrying contamination/offset_/decision_function; MagnitudeShapeDetector using stored-reference depth scoring"
    - "tests/sklearn/test_outliers_compliance.py — per-detector compliance test for MagnitudeShapeDetector"
  key_links:
    - "_native.depth.modified_band_1d(X_new, self.X_fit_) is the subset-invariant scoring call (depth of each new row vs STORED training set)"
    - "offset_ = percentile(training decision scores, 100*contamination) so predict = sign(decision_function - offset_) yields both classes"
---

<objective>
TRACER: Promote MagnitudeShapeDetector to a fully `check_estimator`-compliant `OutlierMixin` estimator end-to-end, proving the stored-reference subset-invariant scoring pattern that all six outlier detectors will use. This is the thinnest complete path through the outlier machinery: shared base (`decision_function` + `contamination`→`offset_` + `predict`), one detector's `fit`/`score_samples` rewritten to score against a STORED training reference, and a real per-detector `parametrize_with_checks` battery.

Purpose: Resolve deferred Phase-57 review CR-03 (MagnitudeShapeDetector scored outlyingness relative to BATCH statistics, violating `check_methods_subset_invariance`) and establish the pattern before expanding to the other five detectors in Plan 02. The stored-reference depth call is subset-invariant by construction (empirically confirmed: `depth.modified_band_1d(X[mask], X_train) == depth.modified_band_1d(X, X_train)[mask]`).

Output: A subset-invariant `_BaseFdarsOutlierDetector` (contamination/offset_/decision_function machinery) + a green MagnitudeShapeDetector + its compliance test. `_coverage.py` verdict flipped to PASS for MagnitudeShapeDetector (per OUT-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-clusterers-outlier-detectors-compliance-gate/58-CONTEXT.md
@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_coverage.py
@src/depth_mod.rs
@src/outliers_mod.rs
@tests/sklearn/test_classifiers_compliance.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Subset-invariant OutlierMixin base + MagnitudeShapeDetector stored-reference scoring</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <read_first>
    - python/fdars/sklearn/_skeletons.py lines 2393-2434 (`_BaseFdarsOutlierDetector` — current `predict`/`score_samples` contract) and lines 2606-2671 (current `MagnitudeShapeDetector`, the batch-relative CR-03 bug).
    - python/fdars/sklearn/_skeletons.py lines 107-118 (`_pairwise_l2`), 204-233 (`_require_y` — NOT used here; outlier detectors take y=None), and the `_validate`/`check_is_fitted`/`check_random_state` imports at lines 62-67.
    - src/depth_mod.rs lines 211-234 (`modified_band_1d(data, ref_data)` — subset-invariant depth of each `data` row vs `ref_data`) and 518-532 (`functional_depth(data, method)` — self-depth, used at fit for the training fence).
    - tests/sklearn/test_classifiers_compliance.py lines 59-70 (established per-estimator compliance pattern) for the offset_/subset-invariance idioms proven in Phase 57.
  </read_first>
  <behavior>
    - Test (subset-invariance): with X = RandomState(0).rand(30,12), a fitted MagnitudeShapeDetector satisfies `np.allclose(det.score_samples(X[mask]), det.score_samples(X)[mask])` for mask=[0,2,5,7,9]. (Currently FALSE — this is the CR-03 fix.)
    - Test (both predict classes): `set(np.unique(det.predict(X)).tolist())` is a subset of {-1,1} and, on data with injected outliers, contains both -1 and +1 when contamination is a float like 0.1.
    - Test (decision_function alias): `det.decision_function(X)` is finite, shape (30,), and equals `det.score_samples(X) - det.offset_`.
    - Test (higher score = more inlier): the deepest curves (highest training depth) have the highest score_samples.
  </behavior>
  <action>
    Rewrite `_BaseFdarsOutlierDetector` (lines ~2393-2434) to own the sklearn OutlierMixin contract shared by all six detectors:
    - Add a `contamination` constructor param convention: subclasses store `self.contamination` (default float 0.1 for MagnitudeShapeDetector — NOT "auto", because "auto" thresholds are unstable on the tiny battery datasets; a fixed float guarantees `check_outliers_train` sees both classes). Document that 0.1 is the sklearn OutlierMixin default convention.
    - In fit, after the subclass computes per-training-sample decision scores, the base helper `self._set_offset(train_scores)` sets `self.offset_ = float(np.percentile(train_scores, 100.0 * self.contamination))`. Higher score = more inlier, so the lowest `contamination` fraction fall below offset_ and predict as -1.
    - `decision_function(self, X)` returns `self.score_samples(X) - self.offset_` (continuous; this IS the required `decision_function`).
    - `predict(self, X)` returns `np.where(self.decision_function(X) >= 0, 1, -1).astype(np.int64)`.
    - Keep `score_samples` abstract (subclass raises NotImplementedError in base).
    Rewrite MagnitudeShapeDetector (lines ~2606-2671) to the stored-reference depth pattern (this is the CR-03 fix — score each curve's centrality against the STORED training population, never batch stats):
    - `__init__(self, argvals=None, contamination=0.1, depth_method="modified_band")`; store all three verbatim.
    - fit: validate + float64 cast + 1-sample guard (`"1 sample"` substring already in base pattern via `_min_samples`); resolve argvals_; store `self.X_fit_ = X`; compute training decision scores as `train_scores = np.asarray(_native.depth.modified_band_1d(X, X))` (self-depth of the training population; higher = more central = more inlier); call `self._set_offset(train_scores)`; set `self.n_features_in_` via `_validate` reset=True. Return self.
    - score_samples: check_is_fitted; validate reset=False + float64 cast; return `np.asarray(_native.depth.modified_band_1d(X, self.X_fit_))` — the depth of EACH new row against the STORED training set. This depends only on (that row, X_fit_) so it is subset-invariant by construction. Do NOT re-run `magnitude_shape` (batch-relative). Higher depth = more inlier (matches the offset_ convention).
    Do NOT inline this depth call anywhere else in the file; the base owns offset_/decision_function/predict so Plan 02 detectors reuse it verbatim.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np; from fdars.sklearn._skeletons import MagnitudeShapeDetector; X=np.random.RandomState(0).rand(30,12); m=[0,2,5,7,9]; d=MagnitudeShapeDetector().fit(X); import sys; sys.exit(0 if (np.allclose(d.score_samples(X[m]), d.score_samples(X)[m]) and set(np.unique(d.predict(X)).tolist())<= {-1,1} and np.allclose(d.decision_function(X), d.score_samples(X)-d.offset_)) else 1)"</automated>
  </verify>
  <done>MagnitudeShapeDetector.score_samples is subset-invariant (scores each row vs stored X_fit_ depth), decision_function = score_samples - offset_, predict returns int64 in {-1,+1}; the empirical subset-invariance assertion that was FALSE before now passes.</done>
  <reversibility rating="reversible">Pure-Python scoring-strategy change in a single skeleton + its shared base; revert is a git checkout of one file.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Per-detector compliance test for MagnitudeShapeDetector + flip _coverage verdict</name>
  <files>tests/sklearn/test_outliers_compliance.py, python/fdars/sklearn/_coverage.py</files>
  <read_first>
    - tests/sklearn/test_classifiers_compliance.py lines 40-70 (the exact `@parametrize_with_checks([Estimator(...)])` per-estimator pattern + module docstring style to mirror).
    - tests/sklearn/conftest.py (the `pytest.importorskip("sklearn")` gate — inherited, no per-file skip needed).
    - python/fdars/sklearn/_coverage.py lines 282-310 (the outlier-detector `TRIAGE_VERDICTS` block; flip only MagnitudeShapeDetector here, the other five stay PASS-WITH-FIXES until Plan 02).
  </read_first>
  <action>
    Create tests/sklearn/test_outliers_compliance.py mirroring test_classifiers_compliance.py: a module docstring naming Phase 58 scope, `from __future__ import annotations`, `from sklearn.utils.estimator_checks import parametrize_with_checks`, and a single `@parametrize_with_checks([MagnitudeShapeDetector(contamination=0.1)])` test function `test_magnitude_shape_compliance(estimator, check)` whose docstring lists the checks it proves green: `check_outliers_train` (both classes via contamination), `check_methods_subset_invariance` (stored-reference depth — CR-03 fix), `check_outliers_fit_predict`, `check_estimators_dtypes` (float32→float64 cast). Body is `check(estimator)`. Import MagnitudeShapeDetector from `fdars.sklearn._skeletons`.
    In _coverage.py, change the `"MagnitudeShapeDetector"` value from the PASS-WITH-FIXES string to `"PASS"`, and update the preceding comment block to state the CR-03 subset-invariance fix landed in Phase 58 Plan 01 via stored-reference `modified_band_1d(X, X_fit_)` depth + contamination-derived offset_.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_outliers_compliance.py::test_magnitude_shape_compliance -q</automated>
  </verify>
  <done>The MagnitudeShapeDetector battery is green (0 failed) and `_coverage.TRIAGE_VERDICTS["MagnitudeShapeDetector"] == "PASS"`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ndarray → native | `check_estimator` supplies arbitrary float32/float64 arrays; the skeleton upcasts to float64 before the native depth call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-01 | Tampering | MagnitudeShapeDetector.score_samples | low | mitigate | Stored X_fit_ is validated at fit; score_samples re-validates with reset=False so a shape/dtype-mismatched batch raises before the native call |

No package installs in this plan (pure-Python edits over already-installed sklearn/native bindings); no `T-58-SC` supply-chain gate required.
</threat_model>

<verification>
- MagnitudeShapeDetector subset-invariant, both predict classes, decision_function alias correct (Task 1 automated check).
- `.venv/bin/pytest tests/sklearn/test_outliers_compliance.py -q` green.
- No regression: `.venv/bin/pytest tests/sklearn/test_transformers_compliance.py tests/sklearn/test_classifiers_compliance.py -q` still green (shared file edited).
</verification>

<success_criteria>
MagnitudeShapeDetector passes the full parametrize_with_checks battery with zero exemptions; its score_samples is subset-invariant against the stored training reference (CR-03 resolved); `_coverage.py` records it PASS. The shared `_BaseFdarsOutlierDetector` now provides contamination→offset_→decision_function→predict for reuse by Plan 02.
</success_criteria>

<output>
Create `.planning/phases/58-clusterers-outlier-detectors-compliance-gate/58-01-SUMMARY.md` when done.
</output>

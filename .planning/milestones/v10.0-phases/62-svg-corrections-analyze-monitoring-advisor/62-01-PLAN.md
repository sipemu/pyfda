---
phase: 62-svg-corrections-analyze-monitoring-advisor
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEFECT-01, DEFECT-02, DEFECT-03, A11Y-01, A11Y-02, SPEC-01]
files_modified:
  - docs/assets/diagrams/tolerance-bands.svg
  - docs/assets/diagrams/clustering.svg
  - docs/assets/diagrams/gmm-clustering.svg
  - docs/assets/diagrams/elastic-clustering.svg
  - docs/assets/diagrams/outlier-detection.svg
  - docs/assets/diagrams/functional-outliers.svg
  - docs/assets/diagrams/functional-boxplot.svg
  - docs/assets/diagrams/seasonal-analysis.svg
  - docs/assets/diagrams/equivalence-testing.svg
  - docs/assets/diagrams/covariance-functions.svg
  - docs/assets/diagrams/scoring-metrics.svg
  - docs/assets/diagrams/functional-statistics.svg
  - docs/assets/diagrams/spm.svg
  - docs/assets/diagrams/advanced-spm.svg
  - docs/assets/diagrams/profile-partial-monitoring.svg
  - docs/assets/diagrams/advisor-loop.svg
  - docs/assets/diagrams/advisor-grounding-invariant.svg
  - docs/assets/diagrams/advisor-aspects.svg
  - docs/assets/diagrams/advisor-agent-skill.svg
  - docs/assets/diagrams/advisor-auto-tuning.svg
  - docs/assets/diagrams/advisor-comparative-selection.svg
  - docs/assets/diagrams/advisor-mcp.svg
  - docs/assets/diagrams/advisor-pipeline-report.svg
  - docs/assets/diagrams/advisor-providers.svg
  - docs/assets/diagrams/advisor-python-api.svg
  - docs/assets/diagrams/sklearn-pipeline-dataflow.svg

estimate:
  tokens: 118000
  raw_tokens: 62000
  tasks: 5
  confidence: med

must_haves:
  truths:
    - "All 26 concept SVGs carry a long-form <title>+<desc> wired via aria-labelledby on the root <svg> (A11Y-02)."
    - "Every root aria-label / aria-labelledby-title in the 26 matches its visible .ttl title text (A11Y-01)."
    - "elastic-clustering.svg method-accurately depicts elastic-distance-based clustering of curve families with real curve <path> imagery, not bare text-flow boxes (DEFECT-01, DEFECT-03)."
    - "outlier-detection.svg keeps the docs-prose-confirmed Magnitude/Shape/Amplitude taxonomy (DEFECT-03 conservative branch — 'Amplitude' is correct, NOT relabelled to 'Phase')."
    - "Flagged Minor geometry/overflow issues (advisor-mcp label straddling boundary, advisor-comparative-selection winner-box clip, sklearn-pipeline-dataflow predictor-label overflow, covariance-functions arrow-entity mismatch) are corrected without palette/typography change (DEFECT-01, DEFECT-02, SPEC-01)."
    - "Every changed SVG renders cleanly via rsvg-convert and remains SVGO-idempotent under svgo.config.mjs (SPEC-01)."
  artifacts:
    - docs/assets/diagrams/elastic-clustering.svg
    - docs/assets/diagrams/outlier-detection.svg
    - docs/assets/diagrams/advisor-mcp.svg
    - docs/assets/diagrams/advisor-comparative-selection.svg
    - docs/assets/diagrams/sklearn-pipeline-dataflow.svg
  key_links:
    - "root <svg aria-labelledby> id refs -> matching <title id> + <desc id> in same SVG"
    - "root title text (aria-label or <title>) -> visible .ttl <text> content"
    - "elastic-clustering redraw -> recorded in SUMMARY for Phase 64 thumb re-sync (SYNC-01)"
---

<objective>
Correct all 26 concept diagrams in the analyze / monitoring / advisor buckets (plus the sklearn edge-case diagram) on the accessibility, defect, and STYLE_SPEC axes, per `60-AUDIT.md` §1 rows and §5 Phase-62 worklist and the locked decisions in `62-CONTEXT.md`.

Purpose: Bring this batch to the diagram quality bar so screen readers convey what each diagram depicts (A11Y-01/02), the one Major content-mismatch (elastic-clustering) is redrawn method-accurately (DEFECT-01/03), and the handful of Minor geometry/overflow flags are fixed (DEFECT-01/02) — all without regressing STYLE_SPEC conformance (SPEC-01).

Output: 26 edited SVGs under `docs/assets/diagrams/`, each rsvg-convert-clean and SVGO-idempotent, with the elastic-clustering redraw recorded for the Phase-64 thumb re-sync.

Scope guard: DOCS-ONLY. Edit ONLY the 26 SVGs listed in `files_modified`. Do NOT touch `docs/assets/thumb/`, `docs/assets/cards/`, `STYLE_SPEC.md`, docs prose (`docs/**/*.md`), or any code. Do NOT run the whole-site `mkdocs build` (that is Phase 65). Verify per-file via `rsvg-convert`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-svg-corrections-analyze-monitoring-advisor/62-CONTEXT.md

# Audit worklist — §1 concept rows for these 26 diagrams (per-file defect/a11y notes) + §5 Phase-62 bucket
@.planning/phases/60-diagram-quality-audit/60-AUDIT.md

# Style contract — canonical <style> block, palette, stroke weights, viewBox, accessibility pattern, SVGO invocation
@docs/assets/diagrams/STYLE_SPEC.md

# Method prose for the two method-accuracy-sensitive diagrams (read before editing them)
@docs/analyze/outlier-detection.md
@docs/analyze/elastic-clustering.md
</context>

<method_accuracy_findings>
Two audit method-accuracy flags were pre-resolved during planning against the docs prose. Executors MUST honor these resolutions:

1. **outlier-detection.svg — "Amplitude" is CORRECT; do NOT relabel to "Phase".**
   `docs/analyze/outlier-detection.md` defines the canonical taxonomy as **Magnitude / Shape / Amplitude** (lines 7-9: "Amplitude — The curve has exaggerated peaks and troughs"), and line 252 uses `X[2] *= 2.5  # amplitude outlier`. The audit's speculation that "Amplitude" should be "Phase" is NOT confirmed by the prose. Per the `62-CONTEXT.md` conservative branch: KEEP the three panel labels "Magnitude Outlier / Shape Outlier / Amplitude Outlier". This diagram needs only the A11Y work (title/desc/aria + aria-label paraphrase check) — no taxonomy relabel.

2. **elastic-clustering.svg — redraw to depict the elastic method, not the align method.**
   `docs/analyze/elastic-clustering.md` defines the method as: **amplitude (elastic / Fisher-Rao / SRVF) distance matrix** via `elastic_self_distance_matrix`, then **distance-based clustering** (`hierarchical_cut`), grouping curves by shape while ignoring phase. The redraw MUST depict curve families being grouped by elastic distance (real curve `<path>` imagery), NOT the align "before/after warped waves" motif the thumb currently shows. The current concept file has 0 curve `<path>` elements (bare flow boxes) — the redraw must introduce curve paths.
</method_accuracy_findings>

<universal_a11y_pattern>
Applied to ALL 26 diagrams (A11Y-02, universal gap — none currently have `<desc>`/`aria-labelledby`):

On the root `<svg>`, replace the bare `aria-label="..."` accessibility pattern with the labelledby pattern:
- Add `aria-labelledby="<t-id> <d-id>"` on the root `<svg>` (id naming at executor discretion; keep unique per file, e.g. `t-elastic` / `d-elastic`).
- Immediately inside the `<svg>`, add `<title id="<t-id>">...</title>` whose text is the diagram's visible `.ttl` title text VERBATIM (this simultaneously satisfies A11Y-01 — the accessible name now equals the title, eliminating the paraphrase mismatch).
- Add `<desc id="<d-id>">...</desc>` — 1-2 sentences stating what the diagram depicts AND the method it illustrates (wording at executor discretion; must be method-accurate).
- If a legacy `aria-label` remains on the root, it MUST also match the visible `.ttl` title text (A11Y-01). Prefer `aria-labelledby` as the single source of the accessible name; if you keep `aria-label` too, keep it title-verbatim. Do not leave a paraphrase.

A11Y-01 known paraphrase specifics to fix (from §1): every diagram in this batch has an aria-label that paraphrases (rather than matches) its `.ttl` title — resolved by the `<title>`-verbatim step above. Special case `covariance-functions.svg`: the `.ttl` uses the HTML entity `&#8594;` (→ arrow) while aria-label spells "to"; make the `<title>` text match the rendered title (either keep the entity consistently or spell it consistently — the accessible name and the visible title must convey the same string).

STYLE_SPEC guard (SPEC-01): Do NOT change palette, typography, the canonical `<style>` block, viewBox, or stroke weights while doing A11Y work. `<title>`/`<desc>` are non-rendered metadata — adding them does not alter the visual. Keep each file SVGO-idempotent under `svgo.config.mjs`.
</universal_a11y_pattern>

<tasks>

<task type="tracer">
  <name>Task 1: A11Y tracer — title+desc+aria-labelledby end-to-end on one diagram, render + idempotence verified</name>
  <files>docs/assets/diagrams/spm.svg</files>
  <read_first>
    - @docs/assets/diagrams/STYLE_SPEC.md (Accessibility Pattern + SVGO Invocation sections)
    - @.planning/phases/60-diagram-quality-audit/60-AUDIT.md (spm.svg §1 row: monitoring section — aria "learn an in-control FPCA model in Phase I, then chart Hotelling T-squared and SPE" paraphrases title "Functional Statistical Process Monitoring")
    - docs/assets/diagrams/spm.svg (read the current root <svg> line and the visible .ttl title text)
  </read_first>
  <action>Prove the universal A11Y-02/A11Y-01 pattern end-to-end on ONE diagram before applying it to the other 25. On `spm.svg`: read the visible `.ttl` title text; add `<title id="t-spm">` with that text VERBATIM and `<desc id="d-spm">` (1-2 sentences: what it depicts + that it illustrates functional SPM — Phase I in-control FPCA model, Phase II Hotelling T-squared + SPE charting against control limits) as the first children inside the root `<svg>`; set `aria-labelledby="t-spm d-spm"` on the root `<svg>` and make any remaining root `aria-label` match the title text verbatim (per A11Y-01). Do NOT change palette, typography, `<style>`, viewBox, or any visible geometry. This is the reference implementation for Tasks 2-4; keep it exactly to the `universal_a11y_pattern` block above.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/assets/diagrams/spm.svg && grep -q '<desc' "$f" && grep -q 'aria-labelledby' "$f" && grep -q '<title' "$f" && rsvg-convert "$f" -o /tmp/spm-tracer.png && test -s /tmp/spm-tracer.png && a="$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -)" && b="$(printf '%s' "$a" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)" && [ "$a" = "$b" ] && echo TRACER_OK</automated>
    <fails_when>spm.svg has no <desc>/<title>/aria-labelledby; rsvg-convert cannot render it or produces an empty PNG; or the SVGO second pass differs from the first (not idempotent under svgo.config.mjs).</fails_when>
  </verify>
  <done>spm.svg carries `<title id="t-spm">`+`<desc id="d-spm">` wired via `aria-labelledby`, the accessible name equals the visible title, it renders to a non-empty PNG, and `svgo(svgo(x))==svgo(x)`. Command prints TRACER_OK.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the A11Y title+desc+aria-labelledby pattern to the remaining analyze diagrams (11) + fix covariance arrow-entity mismatch</name>
  <files>docs/assets/diagrams/tolerance-bands.svg, docs/assets/diagrams/clustering.svg, docs/assets/diagrams/gmm-clustering.svg, docs/assets/diagrams/outlier-detection.svg, docs/assets/diagrams/functional-outliers.svg, docs/assets/diagrams/functional-boxplot.svg, docs/assets/diagrams/seasonal-analysis.svg, docs/assets/diagrams/equivalence-testing.svg, docs/assets/diagrams/covariance-functions.svg, docs/assets/diagrams/scoring-metrics.svg, docs/assets/diagrams/functional-statistics.svg</files>
  <read_first>
    - @docs/assets/diagrams/spm.svg (the Task-1 reference implementation of the pattern)
    - @docs/assets/diagrams/STYLE_SPEC.md (Accessibility Pattern)
    - @.planning/phases/60-diagram-quality-audit/60-AUDIT.md (§1 analyze rows for these 11 — per-file aria-label paraphrase notes; note covariance-functions arrow-entity mismatch and functional-statistics/functional-outliers/scoring-metrics complex multi-panel layouts)
    - @docs/analyze/outlier-detection.md (confirm Magnitude/Shape/Amplitude taxonomy — see method_accuracy_findings #1)
    - each target SVG (read the visible .ttl title text before writing the matching <title>)
  </read_first>
  <action>Apply the exact `universal_a11y_pattern` (verbatim `<title>` + method-accurate `<desc>` + `aria-labelledby`, accessible name = visible title) to all 11 analyze diagrams NOT covered by Task 1. Use per-file unique ids. For each `<desc>`, name the method the diagram illustrates (e.g. tolerance-bands: FPCA/bootstrap/conformal tolerance region; functional-boxplot: median / 50% central region / whiskers / outliers; seasonal-analysis: the six-branch seasonal taxonomy; covariance-functions: Gaussian/Exponential/Matern/Periodic kernels → sample-path smoothness). For `outlier-detection.svg`: A11Y work ONLY — KEEP the three panel labels Magnitude/Shape/Amplitude Outlier exactly (method_accuracy_findings #1); do NOT relabel to Phase. For `covariance-functions.svg`: additionally make the accessible name and the visible `.ttl` title convey the same arrow string (resolve the `&#8594;` vs "to" mismatch — A11Y-01). Do NOT alter palette, typography, `<style>`, viewBox, stroke weights, or any curve/panel geometry.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ok=1; for f in tolerance-bands clustering gmm-clustering outlier-detection functional-outliers functional-boxplot seasonal-analysis equivalence-testing covariance-functions scoring-metrics functional-statistics; do p="docs/assets/diagrams/$f.svg"; grep -q '<desc' "$p" && grep -q 'aria-labelledby' "$p" && grep -q '<title' "$p" && rsvg-convert "$p" -o "/tmp/a11y-$f.png" && test -s "/tmp/a11y-$f.png" || { echo "FAIL $f"; ok=0; }; done; grep -q 'Amplitude Outlier' docs/assets/diagrams/outlier-detection.svg || { echo "FAIL outlier taxonomy changed"; ok=0; }; ! grep -qi '>Phase Outlier<' docs/assets/diagrams/outlier-detection.svg || { echo "FAIL relabelled to Phase"; ok=0; }; [ "$ok" = 1 ] && echo ANALYZE_A11Y_OK</automated>
    <fails_when>any of the 11 lacks <desc>/<title>/aria-labelledby; any fails to render to a non-empty PNG; outlier-detection.svg no longer contains "Amplitude Outlier" or was relabelled to "Phase Outlier".</fails_when>
  </verify>
  <done>All 11 analyze diagrams carry verbatim-title + method-accurate desc + aria-labelledby; each renders to a non-empty PNG; outlier-detection keeps the Magnitude/Shape/Amplitude taxonomy; covariance-functions arrow string is consistent between accessible name and visible title. Command prints ANALYZE_A11Y_OK.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the A11Y pattern to monitoring (2 remaining) + advisor (10) + sklearn (1), and fix the 3 Minor geometry/overflow flags</name>
  <files>docs/assets/diagrams/advanced-spm.svg, docs/assets/diagrams/profile-partial-monitoring.svg, docs/assets/diagrams/advisor-loop.svg, docs/assets/diagrams/advisor-grounding-invariant.svg, docs/assets/diagrams/advisor-aspects.svg, docs/assets/diagrams/advisor-agent-skill.svg, docs/assets/diagrams/advisor-auto-tuning.svg, docs/assets/diagrams/advisor-comparative-selection.svg, docs/assets/diagrams/advisor-mcp.svg, docs/assets/diagrams/advisor-pipeline-report.svg, docs/assets/diagrams/advisor-providers.svg, docs/assets/diagrams/advisor-python-api.svg, docs/assets/diagrams/sklearn-pipeline-dataflow.svg</files>
  <read_first>
    - @docs/assets/diagrams/spm.svg (Task-1 reference implementation)
    - @docs/assets/diagrams/STYLE_SPEC.md (Accessibility Pattern; panel patterns; stroke weights)
    - @.planning/phases/60-diagram-quality-audit/60-AUDIT.md (§1 monitoring/advisor/sklearn rows — per-file aria paraphrase notes + the three Minor geometry flags: advisor-comparative-selection winner-box "fdars-authoritative" clip at right panel edge; advisor-mcp "handle + / scalars" label centered x=178 straddling the dashed stdio boundary at x=175 and the "stdio" label crowding the top edge; sklearn-pipeline-dataflow "FPCLDAClassifier" predictor-box label overflowing the orange panel right edge)
    - docs/assets/diagrams/advisor-mcp.svg, docs/assets/diagrams/advisor-comparative-selection.svg, docs/assets/diagrams/sklearn-pipeline-dataflow.svg (read the exact geometry of the flagged elements before nudging)
    - each target SVG (read the visible .ttl title text before writing the matching <title>)
  </read_first>
  <action>Apply the `universal_a11y_pattern` to all 13 diagrams (verbatim `<title>` + method-accurate `<desc>` + `aria-labelledby`, accessible name = visible title, per-file unique ids). Method-accurate desc examples: advisor-loop = interpret→recommend→re-run→compare cyclic agentic loop with Python-API recommend-only exit; advisor-grounding-invariant = fdars-computes / LLM-explains boundary with "cites, no fabrication"; sklearn-pipeline-dataflow = (n_obs,n_points) ndarray → FPCA transformer stages → FPC scores → predictor. THEN fix the three Minor geometry flags WITHOUT palette/type change: (a) advisor-comparative-selection — bring the "fdars-authoritative" winner-box label fully inside the panel (widen the box, reduce label font a hair, or re-anchor text) so it no longer clips the right edge; (b) advisor-mcp — move the "handle + / scalars" return-path label clear of the dashed stdio boundary line so it sits unambiguously on one side, and nudge the "stdio" boundary label down off the top edge; (c) sklearn-pipeline-dataflow — fit "FPCLDAClassifier" inside the Predictor orange panel (widen panel or shrink label font within `.mono`/`.sm` sizing conventions) so it no longer overflows the right edge. Keep every fix STYLE_SPEC-conformant (existing palette, stroke weights, `<style>` classes) and SVGO-idempotent.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ok=1; for f in advanced-spm profile-partial-monitoring advisor-loop advisor-grounding-invariant advisor-aspects advisor-agent-skill advisor-auto-tuning advisor-comparative-selection advisor-mcp advisor-pipeline-report advisor-providers advisor-python-api sklearn-pipeline-dataflow; do p="docs/assets/diagrams/$f.svg"; grep -q '<desc' "$p" && grep -q 'aria-labelledby' "$p" && grep -q '<title' "$p" && rsvg-convert "$p" -o "/tmp/a11y-$f.png" && test -s "/tmp/a11y-$f.png" || { echo "FAIL $f"; ok=0; }; done; [ "$ok" = 1 ] && echo BATCH3_A11Y_OK</automated>
    <fails_when>any of the 13 lacks <desc>/<title>/aria-labelledby; any fails to render to a non-empty PNG. (Geometry-fix efficacy is confirmed at the visual/idempotence gate in Task 5 and the executor's rsvg render review.)</fails_when>
  </verify>
  <done>All 13 diagrams carry verbatim-title + method-accurate desc + aria-labelledby and render to non-empty PNGs; the advisor-comparative-selection winner label, advisor-mcp boundary/stdio labels, and sklearn FPCLDAClassifier label are contained within their panels/on the correct side of the boundary in the rendered PNG. Command prints BATCH3_A11Y_OK.</done>
</task>

<task type="auto">
  <name>Task 4: Redraw elastic-clustering.svg to method-accurately depict elastic-distance-based clustering of curve families</name>
  <files>docs/assets/diagrams/elastic-clustering.svg</files>
  <read_first>
    - @docs/analyze/elastic-clustering.md (method: elastic/Fisher-Rao/SRVF amplitude distance matrix via elastic_self_distance_matrix → distance-based clustering via hierarchical_cut; groups curves by shape ignoring phase)
    - @.planning/phases/60-diagram-quality-audit/60-AUDIT.md (elastic-clustering.svg §1 row: Major design defect — bare white flow boxes, non-standard all-caps labels bypassing .ttl/.sub/.lab/.sm classes, ~40% canvas fill, no curve imagery; Major sync drift vs thumb — thumb shows before/after aligned wave curves, concept shows text-only flowchart)
    - @docs/assets/diagrams/STYLE_SPEC.md (canonical <style> block, FDARS_COLORS data-curve palette, stroke weights for data curves 2.0-2.8, viewBox 0 0 720 300, panel patterns, accessibility pattern)
    - @docs/assets/diagrams/spm.svg (Task-1 A11Y reference) and one clean peer analyze diagram (e.g. clustering.svg) as a quality/style exemplar
    - docs/assets/diagrams/elastic-clustering.svg (current source — note it has 0 curve <path> elements)
  </read_first>
  <action>Full redraw to bring elastic-clustering.svg to the peer-diagram quality bar and make it method-accurate. Depict the elastic-clustering METHOD: raw curve family with phase (time-warp) variation → an elastic (Fisher-Rao / SRVF amplitude) distance step → curves GROUPED into clusters by shape (not by phase). Show real curve `<path>` imagery for the input curves and for the grouped clusters (use the FDARS_COLORS data-curve palette and data-curve stroke weights from STYLE_SPEC), so a reader sees curve families being clustered, not a text-only flowchart. Use the canonical `<style>` block and the `.ttl`/`.sub`/`.lab`/`.sm`/`.mono` classes for ALL text (remove the all-caps inline-styled labels and inline `style="fill:..."` text overrides the audit flagged). Keep viewBox `0 0 720 300` and fill the canvas to peer standard (no ~40% sparse layout). Reference `elastic_self_distance_matrix` for the distance step. Do NOT depict the align "before/after warped waves converging to a mean" motif (that is the align method, not clustering — method_accuracy_findings #2) — the far end must be clusters of curves. Add the universal A11Y pattern (`<title>` verbatim to the visible title + method-accurate `<desc>` + `aria-labelledby`). Stay within the STYLE_SPEC palette/typography (SPEC-01). This redraw changes the concept diagram only; the corresponding thumb re-sync is Phase 64 (SYNC-01) — record the redraw intent in the SUMMARY so Phase 64 can mirror it.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/assets/diagrams/elastic-clustering.svg && paths=$(grep -c '<path' "$f") && [ "$paths" -ge 4 ] && grep -q '<desc' "$f" && grep -q 'aria-labelledby' "$f" && grep -q 'viewBox="0 0 720 300"' "$f" && grep -q '.ttl{' "$f" && rsvg-convert "$f" -o /tmp/elastic-redraw.png && test -s /tmp/elastic-redraw.png && a="$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -)" && b="$(printf '%s' "$a" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)" && [ "$a" = "$b" ] && echo ELASTIC_OK</automated>
    <fails_when>elastic-clustering.svg has fewer than 4 curve <path> elements (still a text-only flowchart); lacks <desc>/aria-labelledby; viewBox is not 0 0 720 300; the canonical .ttl class is absent from the <style> block; rsvg-convert render is empty; or the SVGO second pass differs (not idempotent).</fails_when>
  </verify>
  <done>elastic-clustering.svg is a full redraw with >=4 curve `<path>` elements depicting curve families grouped by elastic distance (clusters at the far end, NOT the align before/after motif), uses the canonical `<style>` classes for all text, keeps viewBox 0 0 720 300, carries the A11Y title/desc/aria-labelledby, renders to a non-empty PNG, and is SVGO-idempotent. Command prints ELASTIC_OK. The redraw is recorded in the SUMMARY for Phase-64 thumb re-sync.</done>
</task>

<task type="auto">
  <name>Task 5: Batch gate — all 26 SVGs have title/desc/aria-labelledby, render clean, are SVGO-idempotent, and the diff touches ONLY the 26 diagram files</name>
  <files>docs/assets/diagrams/</files>
  <read_first>
    - @docs/assets/diagrams/STYLE_SPEC.md (SVGO Invocation — svgo@3.3.4 --config svgo.config.mjs; idempotence gate approach)
    - @.planning/phases/62-svg-corrections-analyze-monitoring-advisor/62-CONTEXT.md (scope guard: 26 SVGs only; no thumbs/cards/STYLE_SPEC/prose/code)
  </read_first>
  <action>Whole-batch verification and scope-containment check across all 26 target SVGs. Confirm every one carries `<title>`+`<desc>`+`aria-labelledby`, renders cleanly via rsvg-convert, and is SVGO-idempotent under `svgo.config.mjs`. Confirm the git working-tree diff touches ONLY files under `docs/assets/diagrams/` and specifically only the 26 files in `files_modified` — NO thumbs, cards, STYLE_SPEC.md, docs prose, or code. If any file is not idempotent, re-run svgo output through and hand-reconcile the source until `svgo(svgo(x))==svgo(x)` holds (never let the gate rewrite the committed hand-authored source — reconcile by hand). Do NOT run the whole-site mkdocs build.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && list="tolerance-bands clustering gmm-clustering elastic-clustering outlier-detection functional-outliers functional-boxplot seasonal-analysis equivalence-testing covariance-functions scoring-metrics functional-statistics spm advanced-spm profile-partial-monitoring advisor-loop advisor-grounding-invariant advisor-aspects advisor-agent-skill advisor-auto-tuning advisor-comparative-selection advisor-mcp advisor-pipeline-report advisor-providers advisor-python-api sklearn-pipeline-dataflow" && ok=1; n=0; for f in $list; do p="docs/assets/diagrams/$f.svg"; n=$((n+1)); grep -q '<title' "$p" && grep -q '<desc' "$p" && grep -q 'aria-labelledby' "$p" && rsvg-convert "$p" -o "/tmp/gate-$f.png" && test -s "/tmp/gate-$f.png" || { echo "A11Y/RENDER FAIL $f"; ok=0; }; a="$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$p" --output -)" && b="$(printf '%s' "$a" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)" && [ "$a" = "$b" ] || { echo "IDEMPOTENCE FAIL $f"; ok=0; }; done; [ "$n" -eq 26 ] || { echo "COUNT FAIL $n"; ok=0; }; changed="$(git status --porcelain -- docs/assets/diagrams | awk '{print $2}')"; stray="$(git status --porcelain -- . ':(exclude)docs/assets/diagrams' ':(exclude).planning' | wc -l)"; [ "$stray" -eq 0 ] || { echo "SCOPE FAIL: files changed outside docs/assets/diagrams (excluding .planning)"; git status --porcelain -- . ':(exclude)docs/assets/diagrams' ':(exclude).planning'; ok=0; }; [ "$ok" = 1 ] && echo BATCH_GATE_OK</automated>
    <fails_when>fewer than 26 target files checked; any target lacks title/desc/aria-labelledby; any fails to render to a non-empty PNG; any is not SVGO-idempotent under svgo.config.mjs; or the working tree shows changes outside docs/assets/diagrams (excluding .planning docs).</fails_when>
  </verify>
  <done>All 26 SVGs carry title+desc+aria-labelledby, render clean, and are SVGO-idempotent; the working-tree diff is confined to the 26 diagram files (plus .planning docs); no thumbs/cards/STYLE_SPEC/prose/code touched. Command prints BATCH_GATE_OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-edit → committed SVG asset | Manual SVG edits could inject non-conformant markup or corrupt the class-based structure |
| SVG asset → docs build (Phase 65) | A malformed SVG or non-idempotent file would fail the whole-site SVGO/build gate downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Tampering | Manual SVG edits corrupting class-based `<style>` structure (inline `style=` creep) | medium | mitigate | Task 5 idempotence gate under `svgo.config.mjs` catches non-conforming markup; elastic redraw explicitly removes inline `style="fill:..."` text overrides in favour of `.ttl/.sub/.lab/.sm/.mono` classes |
| T-62-02 | Information disclosure | Method-inaccurate `<desc>` / relabel misdepicting the method to screen-reader users | high | mitigate | method_accuracy_findings pre-resolved against docs prose (outlier taxonomy kept; elastic redraw method-checked); DEFECT-03 gate |
| T-62-03 | Denial of service (downstream) | A non-renderable or non-idempotent SVG breaking the Phase-65 whole-site gate | medium | mitigate | per-file rsvg-convert render + SVGO idempotence in every task and the Task-5 batch gate |
| T-62-04 | Tampering | Edits leaking outside the 26-file scope (thumbs/cards/STYLE_SPEC/prose/code) | medium | mitigate | Task 5 git-status scope check fails on any change outside docs/assets/diagrams (excluding .planning) |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase; `npx svgo@3.3.4` uses the pinned, STYLE_SPEC-mandated version already used across the diagram corpus (43/43 pass). No new dependency introduced. |
</threat_model>

<verification>
Phase-level checks (all runnable offline, docs-only, no whole-site build):

1. All 26 target SVGs contain `<title`, `<desc`, and `aria-labelledby` (A11Y-02) and their accessible name equals the visible `.ttl` title (A11Y-01):
   `for f in <26 files>; do grep -q '<title' "$f" && grep -q '<desc' "$f" && grep -q 'aria-labelledby' "$f"; done`
2. Every changed SVG renders to a non-empty PNG via `rsvg-convert` (DEFECT-01/02, DEFECT-03 render-check).
3. Every changed SVG is SVGO-idempotent under `svgo.config.mjs` with `svgo@3.3.4` (SPEC-01).
4. `elastic-clustering.svg` has >=4 curve `<path>` elements, canonical `.ttl` class, viewBox `0 0 720 300` (DEFECT-01/03 redraw).
5. `outlier-detection.svg` still contains "Amplitude Outlier" and NOT "Phase Outlier" (DEFECT-03 conservative branch).
6. `git status` shows changes ONLY under `docs/assets/diagrams/` (plus `.planning/` docs) — no thumbs/cards/STYLE_SPEC/prose/code.
</verification>

<success_criteria>
- [ ] All 26 concept SVGs carry a verbatim-title `<title>` + method-accurate `<desc>` wired via `aria-labelledby`; accessible name matches visible title (A11Y-01, A11Y-02).
- [ ] elastic-clustering.svg redrawn to method-accurately depict elastic-distance-based clustering of curve families (real curve paths, clusters at far end, canonical classes) (DEFECT-01, DEFECT-03).
- [ ] outlier-detection.svg keeps the docs-confirmed Magnitude/Shape/Amplitude taxonomy (DEFECT-03).
- [ ] advisor-mcp, advisor-comparative-selection, sklearn-pipeline-dataflow, covariance-functions Minor flags fixed without palette/type change (DEFECT-01, DEFECT-02).
- [ ] Every changed SVG renders clean via rsvg-convert and is SVGO-idempotent under svgo.config.mjs (SPEC-01).
- [ ] Diff confined to the 26 diagram files; no thumbs/cards/STYLE_SPEC/prose/code touched.
- [ ] elastic-clustering redraw recorded in SUMMARY for Phase-64 thumb re-sync (SYNC-01).
</success_criteria>

<artifacts>
## Artifacts this phase produces

- 26 corrected concept SVGs under `docs/assets/diagrams/` (see `files_modified`), each with A11Y title/desc/aria-labelledby, render-clean, SVGO-idempotent.
- A full redraw of `docs/assets/diagrams/elastic-clustering.svg` (method-accurate elastic-distance clustering) — flagged in the SUMMARY as a Phase-64 SYNC-01 thumb re-sync dependency.
- No changes to thumbs, cards, STYLE_SPEC.md, docs prose, or code.
</artifacts>

<output>
Create `.planning/phases/62-svg-corrections-analyze-monitoring-advisor/62-01-SUMMARY.md` when done. In the SUMMARY, explicitly record: (1) the elastic-clustering redraw and its new visual concept, flagged as the Phase-64 SYNC-01 thumb re-sync dependency; (2) confirmation that outlier-detection kept the Amplitude taxonomy (with the docs-prose citation); (3) any Minor geometry flag you judged not worth touching, with rationale.
</output>

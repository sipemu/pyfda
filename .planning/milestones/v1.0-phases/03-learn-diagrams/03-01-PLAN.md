---
phase: 03-learn-diagrams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/smoothing.svg
autonomous: false
requirements: [DIA-01]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "smoothing.svg Panel 3 ghost underlay no longer reuses Panel 1's noisy polyline coordinates (the shared L8 to L152 sequence is broken)"
    - "smoothing.svg passes the SVGO idempotence gate (svgo of svgo == svgo) under svgo.config.mjs with exit 0"
    - "smoothing.svg still carries the five CSS classes, viewBox 720x300, role=img, and aria-label after the edit (style axis stays conforming)"
    - "The smoothing page on the built site shows a smoothed panel whose faded before-reference is visually distinct from the noisy input panel"
  artifacts:
    - docs/assets/diagrams/smoothing.svg
  key_links:
    - "docs/learn/smoothing.md line 8 embeds ../assets/diagrams/smoothing.svg — the built-site render depends on this embed resolving"
  flagged_assumptions:
    - "adjacency edge (equal/touching inputs merge/collide/separate) — N/A: a hand-authored static SVG has no runtime input adjacency; the two panel paths are fixed authored coordinates, not computed."
    - "empty edge (empty/single/null input) — N/A: no runtime input; the diagram is a static asset with a fixed authored path set."
    - "ordering edge (equal-compare stable output order) — N/A: SVG element order is authored by hand and deterministic; the SVGO gate is idempotent (pass1==pass2 verified), so serialisation order is stable."
---

<objective>
Fix the one confirmed accuracy bug in the learn/ section (GAP-0001) and prove the full author to lint to build-verify loop end to end on that single diagram.

In `smoothing.svg`, Panel 3 ("Smooth Curve") draws a faint ghost "before" reference whose path (line 48) reuses Panel 1's noisy polyline coordinates verbatim from `L8` onward (Panel 1 line 18). This misrepresents what noisy data looks like on the smoothed-output panel. The fix redraws the ghost as a genuinely-distinct faded noisy path so the before/after contrast is honest, then runs the SVGO idempotence gate and rebuilds the site to confirm the corrected diagram renders. This implements DIA-01 (smoothing coordinate-reuse fix).

This is the tracer slice: it exercises every step (edit hand-authored SVG, SVGO gate, mkdocs build, visual confirmation) that the remaining 5 learn/ diagrams (Plan 02) are verified through. Prove the loop here before expanding.

Purpose: Deliver Success Criterion #2 (corrected smoothed panel, verified on the built site) and prove the section verification loop for DIA-01.
Output: Corrected `docs/assets/diagrams/smoothing.svg`.
</objective>

<user_story>
**As a** reader of the fdars smoothing docs, **I want to** see a before/after diagram whose "before" reference genuinely differs from a copy of the input noise, **so that** I can trust the diagram honestly depicts what smoothing does to the data.

Note: the ROADMAP Phase 3 Goal is not written in As-a / I-want-to / so-that form; this story is derived from the documentation-reader perspective for MVP framing only. It is not a scope change — it restates DIA-01 plus Success Criterion #2.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audit/02-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
@svgo.config.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Redraw the Panel 3 ghost underlay so its coordinates differ from Panel 1 (DIA-01)</name>
  <files>docs/assets/diagrams/smoothing.svg</files>
  <read_first>
    - docs/assets/diagrams/smoothing.svg (read the CURRENT file — do NOT assume coordinates; the live Panel 1 path is on line 18 inside g translate(44,124), and the Panel 3 ghost path is on line 48 inside g translate(520,124). Confirm the exact current coordinate strings before editing.)
    - docs/assets/diagrams/STYLE_SPEC.md (stroke weights: secondary/faded curves 1.4-1.6; data-curve palette; do not alter the style block)
  </read_first>
  <action>
    In `smoothing.svg`, replace ONLY the Panel 3 ghost path (currently line 48, the faded blue polyline with stroke-opacity .2 inside g transform translate(520,124)). Its current d reuses Panel 1's noisy coordinate sequence identical to line 18 from L8 onward.

    Replace that path d with a NEW hand-authored jagged noisy polyline that: (a) spans the same x-range 0 to 156 with roughly the same 8px x-step so it fills the panel, (b) has y-values that genuinely differ from Panel 1's sequence at every vertex beyond the first — no multi-vertex run may match Panel 1's y-values, and (c) still reads as noisy data (roughly plus/minus 20-30px vertical jitter around the smooth curve so the before/after contrast is legible). Keep the existing faded styling on that path (stroke #0d6efd, low opacity, thin stroke-width within the STYLE_SPEC faded-curve band).

    Do NOT touch: the Panel 1 path (line 18), the smooth cubic-Bezier curve (line 49), the style block, viewBox, role, aria-label, or any text. This is a coordinate-only edit to a single path element. Record in the SUMMARY: ghost is redrawn as a genuinely-distinct faded noisy path (not removed) to preserve the pedagogical before/after contrast, per the audit recommendation.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test "$(grep -c 'L8 70 L16 100 L24 62 L32 84 L40 54' docs/assets/diagrams/smoothing.svg)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The coordinate run `L8 70 L16 100 L24 62 L32 84 L40 54` (Panel 1 signature sequence) appears exactly ONCE in the file. Check: `grep -c 'L8 70 L16 100 L24 62 L32 84 L40 54' docs/assets/diagrams/smoothing.svg` returns `1`.
    - The Panel 3 ghost path still begins at `M0 ` and reaches x `156` (spans the full panel width).
    - The style block, `viewBox="0 0 720 300"`, `role="img"`, and `aria-label` are unchanged. Check: `grep -c 'viewBox="0 0 720 300"' docs/assets/diagrams/smoothing.svg` returns `1` and `grep -c 'role="img"' docs/assets/diagrams/smoothing.svg` returns `1`.
    - The smooth Bezier curve (line 49, stroke-width 3) is unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Coordinate-only edit to one path in a version-controlled SVG; git checkout reverts instantly.</reversibility>
  <done>The Panel 3 ghost path uses new coordinates that share no multi-vertex run with Panel 1; the style block, viewBox, role, aria-label, and smooth curve are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove smoothing.svg passes the SVGO idempotence gate and mkdocs builds (DIA-01)</name>
  <files>docs/assets/diagrams/smoothing.svg</files>
  <read_first>
    - svgo.config.mjs (the exact check-only config; idempotence gate pattern is in the header comment)
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO Invocation section: pinned svgo@3.3.4, mandatory --config flag)
    - docs/learn/smoothing.md (line 8 embed of ../assets/diagrams/smoothing.svg — confirm the build picks up the edited file)
  </read_first>
  <action>
    Run the SVGO idempotence gate on the edited `smoothing.svg` using the exact pinned invocation from STYLE_SPEC.md: capture pass 1 to stdout, feed it back through svgo for pass 2, and diff. A zero diff means conforming. Then run mkdocs build with DOCS_FAST=1 to keep it fast and confirm it exits 0 with no errors referencing the smoothing page or the SVG. If the gate is non-idempotent or the build errors, return to Task 1 and correct the path (likely a malformed d attribute). The gate is check-only — never let svgo rewrite the committed file (always output to stdout with --output -).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && bash scripts/svgo_gate.sh docs/assets/diagrams/smoothing.svg 2>/dev/null || { FIRST=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/smoothing.svg --output -); SECOND=$(printf '%s' "$FIRST" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); [ "$FIRST" = "$SECOND" ]; }</automated>
    <automated>cd /home/simonm/projects/rust/pyfda && DOCS_FAST=1 mkdocs build 2>&1 | tee /tmp/mkbuild.log; grep -Eiq 'error|traceback' /tmp/mkbuild.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - SVGO idempotence holds: pass 1 output equals pass 2 output for `smoothing.svg` (exit 0).
    - `DOCS_FAST=1 mkdocs build` completes with no `ERROR`/`Traceback` lines mentioning the smoothing page or the SVG.
    - The committed `smoothing.svg` on disk is unchanged by the gate run (gate is check-only, stdout mode). Check: `git diff --stat docs/assets/diagrams/smoothing.svg` shows only the Task 1 coordinate edit, not an SVGO reserialisation.
  </acceptance_criteria>
  <done>SVGO idempotence gate passes on the edited smoothing.svg and mkdocs build succeeds without errors on the smoothing page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Built-site review of the corrected smoothing diagram</name>
  <what-built>The corrected smoothing.svg — Panel 3's faded "before" ghost is now a genuinely-distinct noisy path (no longer a copy of Panel 1's coordinates), and the diagram passes the SVGO gate and builds.</what-built>
  <how-to-verify>
    Run `DOCS_FAST=1 mkdocs serve` and open http://127.0.0.1:8000/learn/smoothing/ (or open the built `site/learn/smoothing/index.html`). On the smoothing concept diagram at the top of the page:
    1. Confirm the smoothed (right) panel's faint blue "before" reference is visibly a DIFFERENT jagged shape from the noisy (left) panel — not the same wiggle shifted down.
    2. Confirm the bold smooth blue curve still passes cleanly through the noise as a legible before/after contrast.
    3. Confirm nothing else changed (title, subtitle, method panel, arrows, labels all intact).
  </how-to-verify>
  <resume-signal>Type "approved" if the smoothed panel's ghost is genuinely distinct and the diagram reads correctly, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
No new attack surface. This plan edits a single static, hand-authored SVG asset and runs local check-only tooling (SVGO in stdout mode) and a local mkdocs build. There is no runtime user input, no network endpoint, no authentication, no data persistence, and no package installs beyond the already-pinned `svgo@3.3.4` dev tool. ASVS L1 is not applicable to this phase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | SVGO gate rewriting the committed hand-authored SVG | low | mitigate | Gate is invoked in stdout mode (`--output -`) only; `git diff` acceptance check confirms the committed file is not reserialised. |
</threat_model>

<verification>
Maps to ROADMAP Phase 3 Success Criteria:
- SC #1 (SVGO lint zero errors): Task 2 idempotence gate on smoothing.svg.
- SC #2 (smoothed panel uses corrected path, verified on built site): Task 1 grep gate + Task 3 built-site human check.
- SC #4 (legacy outliers migrated — none remain): smoothing.svg style axis stays conforming (Task 1 acceptance checks viewBox/role/aria intact); the section-wide none-remain proof is completed in Plan 02.
</verification>

<success_criteria>
- `grep -c 'L8 70 L16 100 L24 62 L32 84 L40 54' docs/assets/diagrams/smoothing.svg` returns 1.
- SVGO idempotence gate passes on smoothing.svg (exit 0).
- `DOCS_FAST=1 mkdocs build` succeeds.
- Human reviewer approves the built-site smoothed panel (ghost genuinely distinct).
</success_criteria>

<output>
Create `.planning/phases/03-learn-diagrams/03-01-SUMMARY.md` when done. Record the GAP-0001 design decision (ghost redrawn, not removed) and the new Panel 3 ghost coordinate string.
</output>
---
phase: 04-represent-diagrams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/depth-functions.svg
autonomous: false
requirements: [DIA-02]
estimate:
  tokens: 44000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "depth-functions.svg carries the canonical five-class <style> block (.ttl/.sub/.lab/.sm/.mono) and no longer sets font-size/font-family inline on any element"
    - "depth-functions.svg root <svg> carries role=img and an aria-label matching the diagram title (Functional Depth: Ranking Curves by Centrality)"
    - "depth-functions.svg passes the SVGO idempotence gate (svgo of svgo == svgo) under svgo.config.mjs with exit 0"
    - "The depth-functions page on the built site renders the restyled diagram with identical CONTENT (deep/shallow/outlier curves, fd.depth(method) interface, three method categories, depth bars, bottom-row tools) and now style-consistent typography"
  artifacts:
    - docs/assets/diagrams/depth-functions.svg
  key_links:
    - "docs/represent/depth-functions.md embeds ../assets/diagrams/depth-functions.svg — the built-site render depends on this embed resolving"
  flagged_assumptions:
    - "adjacency edge (equal/touching inputs merge/collide/separate) — N/A: a hand-authored static SVG has no runtime input adjacency; every path/text is fixed authored markup, not computed."
    - "empty edge (empty/single/null input) — N/A: no runtime input; the diagram is a static asset with a fixed authored element set."
    - "ordering edge (equal-compare stable output order) — N/A: SVG element order is authored by hand and deterministic; the SVGO gate is idempotent (pass1==pass2 verified), so serialisation order is stable."
---

<objective>
Migrate the one legacy-outlier represent/ diagram — `depth-functions.svg` (GAP-0002) — to STYLE_SPEC conformance, and prove the full migrate → SVGO-gate → build-verify loop end to end on that single diagram.

The Phase 2 audit confirms `depth-functions.svg` is content-ACCURATE but style-nonconforming: it has NO `<style>` block (using inline `font-size`/`font-weight` on every `<text>`), sets `font-family="'Segoe UI', system-ui, sans-serif"` inline on the root, and is missing `role="img"` and `aria-label`. Its `viewBox="0 0 720 520"` is already the STYLE_SPEC extra-tall standard. This plan RESTYLES the diagram (add the canonical five-class `<style>` block, apply the classes to every text element, add `role`+`aria-label`, remove inline font attributes) WITHOUT changing any content or coordinates, then runs the SVGO idempotence gate and rebuilds the site to confirm the restyled diagram renders. This implements DIA-02 (depth-functions restyle to STYLE_SPEC).

This is the tracer slice: it exercises every step (migrate hand-authored SVG, SVGO gate, mkdocs build, visual confirmation) that the remaining 6 represent/ diagrams (Plan 02) are verified through. Prove the loop here before expanding.

Purpose: Deliver Success Criterion #4 (all legacy-outlier represent/ diagrams migrated to STYLE_SPEC) for the one outlier, and prove the section verification loop for DIA-02.
Output: Restyled `docs/assets/diagrams/depth-functions.svg`.
</objective>

<user_story>
**As a** reader of the fdars depth-functions docs, **I want to** see a concept diagram whose typography is style-consistent with every other represent/ diagram and that is screen-reader accessible, **so that** the represent/ section reads as one coherent, method-accurate whole.

Note: the ROADMAP Phase 4 Goal is not written in As-a / I-want-to / so-that form; this story is derived from the documentation-reader perspective for MVP framing only. It is not a scope change — it restates DIA-02 (GAP-0002) plus Success Criterion #4.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audit/02-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/assets/diagrams/streaming-depth.svg
@svgo.config.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Restyle depth-functions.svg to STYLE_SPEC — add class block, apply classes, add role+aria-label, remove inline font attrs (DIA-02)</name>
  <files>docs/assets/diagrams/depth-functions.svg</files>
  <read_first>
    - docs/assets/diagrams/depth-functions.svg (read the CURRENT file — do NOT assume markup; the root sets fill="none" and inline font-family; every <text> element carries inline font-size and some font-weight; there is no <style> block, no role, no aria-label. Confirm the exact current attributes before editing.)
    - docs/assets/diagrams/streaming-depth.svg (a CONFORMING represent/ diagram — use its root <svg> line and <style> block as the exact structural reference: verbatim five-class block, role="img", aria-label, class="ttl"/"sub"/"lab"/"sm"/"mono" on text)
    - docs/assets/diagrams/STYLE_SPEC.md (Canonical <style> Block section — copy the five-class block verbatim; Typography Classes table maps size→class; Accessibility Pattern — aria-label must match the .ttl title text)
  </read_first>
  <action>
    Restyle `depth-functions.svg` to STYLE_SPEC conformance. This is a STYLE-ONLY migration: preserve ALL content, coordinates, colors (per-element fill), path data, rects, lines, markers, and text strings exactly. Change only typography structure and accessibility attributes.

    Make these edits:
    1. On the root <svg>: keep viewBox="0 0 720 520" and fill="none". REMOVE the inline font-family="'Segoe UI', system-ui, sans-serif" attribute. ADD role="img" and aria-label="Functional Depth: Ranking Curves by Centrality" (matching the title text exactly).
    2. Immediately after the root <svg> opening tag, INSERT the canonical five-class <style> block verbatim from STYLE_SPEC.md (.ttl/.sub/.lab/.sm/.mono definitions — copy from streaming-depth.svg's block).
    3. For EVERY <text> element, REMOVE its inline font-size and font-weight attributes and ADD the appropriate class, mapping by role: the title (currently font-size 20 font-weight bold) → class="ttl"; the subtitle (font-size 14 fill #888) → class="sub"; bold panel/section labels (font-size 15/16 font-weight bold — Curve Sample, fd.depth(method), Depth Values, Depth-Based Tools, and the bold application-box headers font-size 13) → class="lab"; the monospace-style function-name labels if any read as code (fd.depth(method), outliergram(), geometric_median(fd), fpca_tolerance_band(), etc.) → keep as small annotation class="sm" UNLESS you judge them clearly code-token labels, in which case class="mono" (document the choice in the SUMMARY); all remaining small annotation text (font-size 10/11/12/13 — method lists, depth values, captions, bottom summary rows) → class="sm". Preserve each element's existing fill attribute (the classes set font only; .lab and .mono deliberately set no fill so per-element fill is retained; .ttl/.sub/.sm carry a default fill — where an element's existing fill differs from the class default, KEEP the explicit fill attribute so the color is unchanged).
    4. Do NOT alter any x/y coordinates, text-anchor, path d attributes, rect/line geometry, opacity, stroke, the <defs>/<marker>, or any text string content.

    Preserve the diagram's accurate content per the audit: deep/shallow/outlier curves, the fd.depth(method) unified interface, the three method categories (Pointwise: FM/MBD/BD/MEI; Projection: RP/RT/RPD; Kernel/Spatial: mode/FSD/KFSD), the depth bar chart, and the bottom-row depth-based tools. This is a restyle, not a redraw.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; for m in 'viewBox="0 0 720 520"' 'role="img"' 'aria-label' '.ttl' '.sub' '.lab' '.sm' '.mono' 'system-ui'; do grep -qF "$m" docs/assets/diagrams/depth-functions.svg || { echo "MISSING $m"; ok=0; }; done; grep -q "font-size" docs/assets/diagrams/depth-functions.svg && { echo "INLINE font-size still present"; ok=0; }; grep -q "Segoe UI" docs/assets/diagrams/depth-functions.svg && { echo "inline font-family still present"; ok=0; }; [ "$ok" = 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - The canonical five CSS classes are present in a <style> block: `grep -c '.ttl' docs/assets/diagrams/depth-functions.svg` >= 1 and likewise for `.sub .lab .sm .mono`, and `system-ui` is present.
    - `role="img"` and `aria-label` are present on the root, and the aria-label text matches the title "Functional Depth: Ranking Curves by Centrality". Check: `grep -c 'role="img"' docs/assets/diagrams/depth-functions.svg` returns `1`.
    - NO inline `font-size` attribute remains anywhere: `grep -c 'font-size' docs/assets/diagrams/depth-functions.svg` returns `0`.
    - NO inline `font-family="'Segoe UI'..."` remains: `grep -c 'Segoe UI' docs/assets/diagrams/depth-functions.svg` returns `0`.
    - `viewBox="0 0 720 520"` is unchanged (extra-tall STYLE_SPEC standard).
    - No path `d`, rect/line geometry, or text string content changed (content-preserving restyle). Check: the method-category tokens survive — `grep -c 'FM, MBD, BD, MEI' docs/assets/diagrams/depth-functions.svg` returns `1` and `grep -c 'mode, FSD, KFSD' docs/assets/diagrams/depth-functions.svg` returns `1`.
  </acceptance_criteria>
  <reversibility rating="reversible">Style-only edit to one path/text-attribute set in a version-controlled SVG; git checkout reverts instantly.</reversibility>
  <done>depth-functions.svg carries the canonical <style> block, role, and aria-label; every text element uses a CSS class; no inline font-size/font-family remains; all content, coordinates, and colors are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove depth-functions.svg passes the SVGO idempotence gate and mkdocs builds (DIA-02)</name>
  <files>docs/assets/diagrams/depth-functions.svg</files>
  <read_first>
    - svgo.config.mjs (the exact check-only config; the idempotence gate pattern is in the header comment; the --config flag preserves the CSS classes)
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO Invocation section: pinned svgo@3.3.4, mandatory --config flag, stdout-only --output -)
    - docs/represent/depth-functions.md (confirm it embeds ../assets/diagrams/depth-functions.svg so the build picks up the edited file)
  </read_first>
  <action>
    Run the SVGO idempotence gate on the restyled `depth-functions.svg` using the exact pinned invocation from STYLE_SPEC.md: capture pass 1 to stdout, feed it back through svgo for pass 2, and diff. A zero diff means conforming (and — critically for a restyle — that the --config flag preserved the CSS classes rather than inlining them). Then run mkdocs build with DOCS_FAST=1 and PYTHONPATH=scripts using the repo .venv to confirm it exits 0 with no errors referencing the depth-functions page or the SVG. If the gate is non-idempotent (likely a class got inlined or a malformed style block) or the build errors, return to Task 1 and correct the markup. The gate is check-only — never let svgo rewrite the committed file (always output to stdout with --output -).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && FIRST=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/depth-functions.svg --output -); SECOND=$(printf '%s' "$FIRST" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); [ "$FIRST" = "$SECOND" ]</automated>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build 2>&1 | tee /tmp/mkbuild-04-01.log; grep -Eiq 'error|traceback' /tmp/mkbuild-04-01.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - SVGO idempotence holds: pass 1 output equals pass 2 output for `depth-functions.svg` (exit 0), proving the --config flag preserved the CSS classes (no inlining).
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` completes with no `ERROR`/`Traceback` lines mentioning the depth-functions page or the SVG.
    - The committed `depth-functions.svg` on disk is unchanged by the gate run (gate is check-only, stdout mode). Check: `git diff --stat docs/assets/diagrams/depth-functions.svg` shows only the Task 1 restyle edits, not an SVGO reserialisation.
  </acceptance_criteria>
  <done>SVGO idempotence gate passes on the restyled depth-functions.svg and mkdocs build succeeds without errors on the depth-functions page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Built-site review of the restyled depth-functions diagram</name>
  <what-built>The restyled depth-functions.svg — now carrying the canonical five-class <style> block, role="img", and aria-label, with every text element using a CSS class (no inline font-size/font-family). Content (deep/shallow/outlier curves, fd.depth(method) interface, three method categories, depth bars, bottom-row tools) is unchanged; the diagram passes the SVGO gate and builds.</what-built>
  <how-to-verify>
    Run `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs serve` and open http://127.0.0.1:8000/represent/depth-functions/ (or open the built `site/represent/depth-functions/index.html`). On the depth-functions concept diagram:
    1. Confirm the diagram renders identically in CONTENT to before — the curve sample (deep blue central curves, grey peripheral, red outlier), the fd.depth(method) middle panel with the three method categories (Pointwise FM/MBD/BD/MEI, Projection RP/RT/RPD, Kernel/Spatial mode/FSD/KFSD), the depth-values bar chart, and the bottom-row depth-based tools (Outlier Detection, Functional Boxplot, Robust Statistics, Tolerance Bands, Streaming) are all present and unchanged.
    2. Confirm the typography now matches the other represent/ diagrams (same title/subtitle/label/annotation font treatment as streaming-depth or fpca) — no leftover Segoe UI look, consistent weights and sizes.
    3. Confirm nothing shifted position or lost color.
  </how-to-verify>
  <resume-signal>Type "approved" if the restyled diagram renders with identical content and now style-consistent typography, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
No new attack surface. This plan edits a single static, hand-authored SVG asset and runs local check-only tooling (SVGO in stdout mode) and a local mkdocs build. There is no runtime user input, no network endpoint, no authentication, no data persistence, and no package installs beyond the already-pinned `svgo@3.3.4` dev tool. ASVS L1 is not applicable to this phase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | SVGO gate rewriting the committed hand-authored depth-functions.svg | low | mitigate | Gate is invoked in stdout mode (`--output -`) only; `git diff --stat docs/assets/diagrams/depth-functions.svg` acceptance check confirms the committed file is not reserialised (only the Task 1 restyle edits present). |
</threat_model>

<verification>
Maps to ROADMAP Phase 4 Success Criteria:
- SC #1 (represent/ SVG pass SVGO lint zero errors): Task 2 idempotence gate on depth-functions.svg.
- SC #3 (every warranting represent/ page has an accurate diagram visible on the built site): Task 1 content-preserving restyle + Task 3 built-site human check.
- SC #4 (all legacy-outlier represent/ diagrams migrated to STYLE_SPEC): Task 1 migrates the ONE outlier (depth-functions.svg) — acceptance checks style block, role, aria, no inline font attrs; the section-wide none-remain proof is completed in Plan 02.
</verification>

<success_criteria>
- depth-functions.svg carries the canonical five-class <style> block, role="img", aria-label, and no inline font-size/font-family (Task 1 grep gates).
- SVGO idempotence gate passes on depth-functions.svg (exit 0).
- `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` succeeds.
- Human reviewer approves the built-site depth-functions diagram (identical content, style-consistent typography).
</success_criteria>

<output>
Create `.planning/phases/04-represent-diagrams/04-01-SUMMARY.md` when done. Record the GAP-0002 restyle decision (restyle, not redraw — content preserved), the class-mapping choices made per text element (especially any .mono vs .sm judgment call on code-token labels), and confirm no coordinates/colors changed.
</output>
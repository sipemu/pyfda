---
phase: 02-audit
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - .planning/phases/02-audit/02-AUDIT.md
autonomous: true
requirements: [AUD-02, AUD-03]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 37000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The R-era grep report covers BOTH docs/assets/diagrams/*.svg AND docs/**/*.md (all sections including reference/ and examples/) and reports every hit as file:line with the matched text, grouped by section (AUD-02, D-08)."
    - "The six named preliminary findings are reconciled in the grep report: spm.svg and basis-representation.svg R-era hits appear as file:line entries; each of the six is confirmed with a file:line or explicitly recorded as not-found."
    - "Legitimate ggplot2/R prose (narrative comparison, e.g. custom-plotting.md) is distinguished from actual R-era leftovers-to-remove so downstream sweeps do not delete legitimate content."
    - "A reference-API coverage sweep compares fdars exported functions across the 16 reference modules against documented/exampled capabilities and surfaces under-documented capabilities as EX-#### candidates."
    - "The ranked list contains Phase 9's five LOCKED baseline examples (EX-0001..EX-0005) PLUS reference-API-sweep extras, each with a stable ID and a user-selectable Selection column (AUD-03, D-06)."
    - "Every GAP-#### / EX-#### row carries the three ranking signals: zero worked-example/zero-accurate-diagram, method centrality/user value, authoring effort (D-06 ranking)."
  artifacts:
    - ".planning/phases/02-audit/02-AUDIT.md"
  key_links:
    - "R-era report file:line entries are reproducible by re-running the documented grep patterns against docs/assets/diagrams/*.svg and docs/**/*.md."
    - "Each EX-#### candidate names the fdars module/function it exercises so Phase 9 can trace it to the reference API."
---

<objective>
Complete the audit: (1) the full-scope R-era grep report across all diagrams and all prose including reference/ and examples/, with every hit as file:line and legitimate prose distinguished from leftovers; (2) the reference-API coverage sweep that compares fdars exported functions against documented/exampled capabilities to surface under-documented candidates; (3) the single ranked, user-selectable GAP/EX list (locked baseline five + sweep extras) with a Selection column the user marks before Phase 3.

Purpose: Finish AUD-02 and AUD-03. After this plan the whole nav is audited, all R-era content is catalogued for removal by the right sweep, and the user has one ranked list to select from at the Phase-3 gate.
Output: `02-AUDIT.md` §2 (full R-era report) and §3 (ranked user-selectable list) complete.
</objective>

## Artifacts this phase produces

Completes the single file `.planning/phases/02-audit/02-AUDIT.md` (§2 R-era report, §3 ranked list). No source code symbols created; no docs/ or fdars files modified. ID schemes finalized: GAP-#### (coverage gaps), EX-#### (new-example candidates).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-audit/02-CONTEXT.md
@mkdocs.yml
@.planning/codebase/STRUCTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/02-audit/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-scope R-era grep report (all diagrams + all prose, file:line, grouped by section)</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-AUDIT.md (§2 heading + scope statement + learn/ subsection seeded in Plan 01)
    - .planning/phases/02-audit/02-CONTEXT.md §"R-era Grep Report" (D-08: scope = docs/assets/diagrams/*.svg AND docs/**/*.md incl. reference/ and examples/; patterns extendr, autoplot, and R-specific identifiers ggplot, %>%, <- assignment, R package names at discretion)
    - .planning/ROADMAP.md §Phase 4 and §Phase 8 (basis-representation.svg and spm.svg named R-era targets)
    - the full docs tree: docs/**/*.md and docs/assets/diagrams/*.svg
  </read_first>
  <action>
    Fill §2 "R-era Grep Report" completely. Run grep across BOTH docs/assets/diagrams/*.svg AND docs/**/*.md (all sections: learn, represent, align, analyze, regression, monitoring, examples, reference). Patterns: `extendr`, `autoplot`, `ggplot`, `%>%`, `<-` (R assignment), and R package names (e.g. `fda`, `fdasrvf`, `roahd`) at discretion — record which patterns you ran. Report every hit as a `file:line — matched text` entry under a `### <section>/` subsection so each downstream sweep finds its own removals.

    Reconcile the six named preliminary findings: spm.svg (autoplot + extendr) and basis-representation.svg R-era hits MUST appear as file:line entries; for each of the six confirm with a file:line or mark "(not found)". Critically distinguish legitimate narrative prose (e.g. custom-plotting.md discusses ggplot2 as an intentional R→matplotlib translation guide) from actual R-era leftovers-to-remove — annotate each hit as `LEFTOVER` (remove in sweep) or `PROSE-OK` (legitimate, keep) so Phases 3–9 do not delete legitimate content. Keep the learn/ subsection from Plan 01 and extend to all sections.
  </action>
  <verify>
    <automated>grep -q "spm.svg" .planning/phases/02-audit/02-AUDIT.md && grep -q "autoplot" .planning/phases/02-audit/02-AUDIT.md && grep -q "extendr" .planning/phases/02-audit/02-AUDIT.md && grep -qE "### reference/|### examples/" .planning/phases/02-audit/02-AUDIT.md && grep -qE "LEFTOVER|PROSE-OK" .planning/phases/02-audit/02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - §2 groups R-era hits by section and includes subsections spanning all sections that have hits (incl. reference/ and examples/ if present).
    - spm.svg (autoplot, extendr) and basis-representation.svg R-era content appear as file:line entries.
    - Each of the six named preliminary findings is confirmed with a file:line or explicitly marked not-found.
    - Every hit is annotated LEFTOVER vs PROSE-OK so legitimate ggplot2 narrative (e.g. custom-plotting.md) is not slated for deletion.
    - The grep patterns actually run are documented so the report is reproducible.
  </acceptance_criteria>
  <done>§2 is the complete, reproducible, file:line R-era report across all diagrams and prose, with legitimate prose distinguished from leftovers and the six findings reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: Reference-API coverage sweep → under-documented EX candidates</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-CONTEXT.md §Claude's Discretion "New-example sourcing & ranking" (baseline five locked; reference-API sweep surfaces optional candidates)
    - .planning/codebase/STRUCTURE.md and .planning/codebase/INTEGRATIONS.md (nav↔module map; the 16 reference modules)
    - mkdocs.yml §nav: → Reference: (16 reference pages: fdata, depth, metric, basis, smoothing, clustering, regression, alignment, outliers, seasonal, spm, classification, tolerance, conformal, simulation, explain)
    - the 16 src/*_mod.rs files and python/fdars/*.py (fdars exported function surface — the #[pyfunction] entries and __all__ lists)
    - docs/examples/*.md (the 17 existing example pages — what capabilities already have a worked example)
  </read_first>
  <action>
    Run a reference-API coverage sweep: for each of the 16 reference modules, list the exported fdars functions (from src/*_mod.rs #[pyfunction] registrations and python/fdars __all__ / registered submodules), then mark each capability as: has a worked example (grep docs/examples/*.md), has an accurate concept diagram (cross-ref §1 coverage table), or under-documented (neither). Summarize as a short coverage-gap sub-table in §3 (module, capability, has-example?, has-accurate-diagram?).

    From the under-documented set, propose additional new-example EX-#### candidates BEYOND the locked baseline five — each naming the fdars module/function it would exercise. Do not touch the baseline five (EX-0001..EX-0005 stay locked); these are additive optional candidates the user may accept or drop. Keep this to genuinely under-documented, method-central capabilities; do not pad.
  </action>
  <verify>
    <automated>grep -qE "reference-API|API coverage|coverage sweep" .planning/phases/02-audit/02-AUDIT.md && grep -c "EX-00" .planning/phases/02-audit/02-AUDIT.md | awk '{ if ($1+0 >= 5) exit 0; else { print "expected >=5 EX rows"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - §3 contains a reference-API coverage sub-table covering all 16 reference modules (module, capability, has-example?, has-accurate-diagram?).
    - Each capability is classed as exampled / diagrammed / under-documented against docs/examples/ and the §1 table.
    - Additional EX-#### candidates (beyond EX-0001..EX-0005) each name the fdars module/function they exercise.
    - The five baseline EX-0001..EX-0005 are unchanged and still marked baseline-locked.
  </acceptance_criteria>
  <done>The reference-API sweep documents documented-vs-exported coverage across all 16 modules and surfaces additive under-documented EX candidates around the locked baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Final ranked, user-selectable GAP + new-example list</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-AUDIT.md (§1 coverage rollups → source of GAP rows; §3 EX candidates from Task 2; the GAP-####/EX-#### schemes + Selection column from Plan 01)
    - .planning/phases/02-audit/02-CONTEXT.md §D-06 (user-selectable in-document; stable ID; Selection column the user marks before Phase 3) and §Claude's Discretion ranking signals (zero worked-example/zero-accurate-diagram; method centrality/user value; authoring effort)
    - .planning/ROADMAP.md §Phase 9 (the five locked baseline example names)
  </read_first>
  <action>
    Assemble ONE ranked table in §3 combining: (a) GAP-#### rows derived from every §1 coverage row whose rollup is `missing` or `inconsistent` (each GAP names the page + whether it is a restyle, redraw, or new-diagram gap — read from the two axes), and (b) EX-#### rows = the five locked baseline examples (EX-0001..EX-0005, Selection = [baseline-locked]) PLUS the Task-2 additive candidates (Selection = blank for the user to mark).

    Give every row a stable ID, the three ranking signals as columns (zero-worked-example/zero-accurate-diagram; method-centrality/user-value: high/med/low; authoring-effort: low/med/high), a computed priority rank, and a `Selection` column the user marks before Phase 3 (D-06). Sort the table by priority. Add a one-line instruction at the top of §3 telling the user to mark the Selection column in-document; Phase 3 planning reads the marked set. Do not select on the user's behalf — leave non-baseline Selection cells blank.
  </action>
  <verify>
    <automated>grep -qE "GAP-00" .planning/phases/02-audit/02-AUDIT.md && grep -qiE "Selection" .planning/phases/02-audit/02-AUDIT.md && grep -qiE "authoring.effort|centrality" .planning/phases/02-audit/02-AUDIT.md && grep -qiE "baseline-locked" .planning/phases/02-audit/02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - §3 has a single ranked table with both GAP-#### and EX-#### rows, each with a stable ID.
    - Every GAP-#### traces to a §1 coverage row with rollup missing or inconsistent, and states restyle/redraw/new-diagram.
    - The five baseline examples appear as EX-0001..EX-0005 with Selection = [baseline-locked]; additive candidates have a blank Selection for the user to mark.
    - Each row carries the three ranking signal columns and a priority rank; the table is sorted by priority.
    - A top-of-section instruction tells the user to mark the Selection column before Phase 3 (D-06).
  </acceptance_criteria>
  <done>§3 is one ranked, user-selectable list (locked baseline five + GAP/EX extras) with stable IDs, ranking signals, and a Selection column ready for the Phase-3 gate. AUD-01/02/03 are all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Read-only analysis completing one Markdown planning artifact. Reads committed docs/, SVGs, src/*_mod.rs, python/fdars/*.py, mkdocs.yml, codebase maps; writes only `.planning/phases/02-audit/02-AUDIT.md`. No new external input, no code execution path introduced, no docs/ or fdars code modified. No new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | R-era report / ranked-list accuracy | low | accept | Grep patterns are documented and reproducible; LEFTOVER-vs-PROSE-OK annotation prevents wrongful deletion downstream; ranked list is human-selected at the Phase 3 gate before any action. |
| T-02-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this phase (read-only audit). |
</threat_model>

<verification>
- §2 R-era report covers all diagrams + all prose (incl. reference/, examples/), every hit file:line, LEFTOVER vs PROSE-OK annotated, six findings reconciled.
- §3 reference-API sweep covers all 16 modules; ranked list has GAP-#### + EX-#### with stable IDs, ranking signals, and a user Selection column; baseline five locked.
</verification>

<success_criteria>
AUD-02 (full file:line R-era report) and AUD-03 (ranked, user-selectable GAP + new-example list) are complete; 02-AUDIT.md is the single source of truth Phase 3 reads at the selection gate.
</success_criteria>

<output>
Create `.planning/phases/02-audit/02-03-SUMMARY.md` when done.
</output>

---
phase: 81-curation-paper-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/_references_map.json
  - tests/test_guard_sync_version_independent.py
autonomous: true
requirements: [CURATE-01, CURATE-02, CURATE-03, CURATE-05]
estimate:
  tokens: 62000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GATE-05 A/B/C stay green after curating two clear-root families (basis/smoothing + functional statistics) — the end-to-end pattern every later batch copies (CURATE-01)"
    - "callable_index records sub-method-accurate attribution (P-spline callables → eilers_marx_1996; N-W kernel → curated:false; not a shared over-broad pointer) (CURATE-02)"
    - "cross_language pointers with package+function+version+specific-function URL land for at least one covered paper; new publisher/cross-lang domains added to _ALLOWED_DOMAINS in the SAME commit (CURATE-03)"
    - "A new guard test emits the honest coverage fraction N/437 derived programmatically from _capability_map.json (NOT hardcoded 409) (CURATE-05)"
  artifacts:
    - python/fdars/_references_map.json
    - tests/test_guard_sync_version_independent.py
  key_links:
    - "callable_index keys ↔ papers[*].callables (GATE-05 A) — edit both together"
    - "callable_index keys ↔ _capability_map.json (GATE-05 B) — every key resolves"
    - "cross_language[*].url domain ↔ _ALLOWED_DOMAINS frozenset (GATE-05 C)"
    - "coverage denominator ↔ live _capability_map.json callable count (437, recomputed at test time)"
---

<objective>
Establish the curation pattern end-to-end on two clear-root families, and stand up the honest-coverage mechanism the whole phase reports against.

This is the TRACER plan: it wires one full path through every layer the phase touches — the JSON data file, the domain allowlist in the guard test, and a NEW coverage-emission guard test — proving the loop (add papers → extend allowlist → GATE-05 green → coverage fraction emitted) that plans 81-02..81-05 replay for the remaining families.

Purpose: De-risk the batch-curation loop on its best-context tokens. Every later batch copies this exact shape (edit both `papers` and `callable_index`, add domains same-commit, keep GATE-05 A/B/C green).
Output: `_references_map.json` grown from 5 seed papers to the basis/smoothing + functional-statistics families curated; `_ALLOWED_DOMAINS` extended; a new coverage-emission test reporting N/437.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-curation-paper-registry/81-CONTEXT.md
@.planning/phases/81-curation-paper-registry/81-RESEARCH.md
@docs/authoring/references-schema.md
@python/fdars/_references_map.json
@python/fdars/_capability_map.json
@tests/test_guard_sync_version_independent.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extend _ALLOWED_DOMAINS + add the N/437 coverage-emission guard test</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - tests/test_guard_sync_version_independent.py (lines 300-507: Guard Group 3 tests A/B/C, `_DOI_RE`, `_ALLOWED_DOMAINS` frozenset at 407-419, the `resources.files("fdars")` load pattern)
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §7 (verbatim current allowlist) + §4 "New URL domains needed" table + §1 (denominator is 437, derived from _capability_map.json)
    - python/fdars/_capability_map.json (the coverage denominator source — 31 modules, 437 callables)
    - docs/authoring/references-schema.md ("What the Guards Enforce (GATE-05)")
  </read_first>
  <action>
    In `tests/test_guard_sync_version_independent.py`, extend the `_ALLOWED_DOMAINS` frozenset (currently at ~line 407) by adding the six minimum-set cross-language/publisher hosts required this phase: `www3.stat.sinica.edu.tw`, `proceedings.mlr.press`, `tslearn.readthedocs.io`, `fda.readthedocs.io`, `www.sktime.net`, `journals.sagepub.com`. Also add the three conditionally-needed hosts so later batches never re-touch this frozenset: `epubs.siam.org`, `icml.cc`, `www.stat.ucdavis.edu`. Keep the existing entries; append only.

    Add a NEW test function `test_references_map_coverage_fraction` to Guard Group 3 that: loads `_references_map.json` and `_capability_map.json` via `importlib.resources`; computes the real denominator as `sum(len(v) for v in cap_data.values())` (do NOT hardcode 437 or 409 — recompute from the live capability map so a future crate-bump cannot desync it); computes the numerator as the count of DISTINCT `callable_index` keys whose sole/any paper entry has `curated: true` (i.e. author-verified curated callables — a callable is "curated" if at least one paper_key in its `callable_index` value has `papers[pk]["curated"] is True`); prints the fraction with `print(f"COVERAGE: {numerator}/{denominator} callables curated ...")` so it surfaces under `pytest -s`; and asserts only structural sanity — `denominator == 437` at authoring time as a drift tripwire with a message telling the executor to update the expected count if the capability map legitimately grew, and `0 <= numerator <= denominator`. This test is informational (no hard coverage floor — a floor is deferred to REF-FUT-01 per CONTEXT). Add a module-level comment above the test noting "409 was the stale roadmap-time estimate; the real denominator is derived from _capability_map.json (437 at authoring time)."
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map"</automated>
    <fails_when>The coverage test is missing, hardcodes 409/437 as the denominator instead of computing it from _capability_map.json, or any GATE-05 A/B/C test regresses.</fails_when>
  </verify>
  <done>`_ALLOWED_DOMAINS` contains the nine new domains; `test_references_map_coverage_fraction` exists, derives the denominator from _capability_map.json, and passes alongside A/B/C green.</done>
</task>

<task type="auto">
  <name>Task 2: Curate the basis/smoothing family (P-splines extend + GCV + N-W kernel)</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.1 (Basis / Smoothing — exact paper_keys, DOIs, callable lists, and the curated:false rulings for Nadaraya-Watson, Fourier, B-spline basis)
    - python/fdars/_references_map.json (the live seed — `eilers_marx_1996` already covers `basis.basis_nbasis_cv`; EXTEND it, do not duplicate)
    - python/fdars/_capability_map.json (module `basis`, `smoothing` — confirm every callable key you cite exists there for GATE-05 B)
    - docs/authoring/references-schema.md ("papers[*] Field Reference", "cross_language[*] Shape", "curated:false Tail Convention", "Author-Verification Workflow")
  </read_first>
  <action>
    Per 81-RESEARCH §3.1, grow `_references_map.json` for the basis/smoothing family. AUTHOR-VERIFY each paper against its DOI landing page via web fetch before setting `curated: true`; on any authors/year/title mismatch or unreachable landing page, set `curated: false` and note why in `notes` (per the CONTEXT best-effort-verify-and-flag rule).

    1. EXTEND the existing `eilers_marx_1996` entry's `callables` array (and the matching `callable_index` entries) to add the P-spline/basis callables listed in §3.1: `basis.pspline_fit_1d`, `basis.pspline_fit_gcv`, `basis.smooth_basis_gcv`, `basis.smooth_basis_aic`, `basis.fdata_to_basis_1d`, `basis.basis_to_fdata_1d`. Confirm each exists in `_capability_map.json["basis"]` first; drop any that do not resolve.
    2. Add `craven_wahba_1979` for GCV (`smoothing.gcv_smoother`, `smoothing.optim_bandwidth`) — DOI `10.1007/BF01404567` is [ASSUMED] in research; verify the landing page and mark `curated:false` if you cannot confirm it.
    3. Add a single `nadaraya_watson_1964` entry for `smoothing.nadaraya_watson` listing BOTH authors (Nadaraya, E.A.; Watson, G.S.), with `"doi": ""`, `"url": ""` omitted or empty, `type: "journal"`, `curated: false` (1964 Soviet-era journals, no DOI — GATE-05 C skips DOI/URL checks for curated:false).
    4. Add a cross_language pointer to `eilers_marx_1996` if not already present, per §4 (R `mgcv` `s(...,bs="ps")`, Python `scikit-fda` `BSplineSmoother`) — use the `fda.readthedocs.io` / `www.rdocumentation.org` domains now in the allowlist; include `version` where known, `confidence` per the schema.
    5. Leave the remaining basis callables (`fourier_basis*`, `bspline_basis*`, `constant_basis`, `select_basis_auto_1d`) UNCURATED (omit from callable_index) — they are handled as curated:false family tail in 81-05; do not force-cite them here.

    Edit `papers[*].callables` and `callable_index` TOGETHER for every change (GATE-05 A invariant).
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map"</automated>
    <fails_when>callable_index and papers[*].callables disagree (GATE-05 A), a cited callable does not resolve in _capability_map.json (GATE-05 B), a curated DOI fails the regex or a URL domain is not allowlisted (GATE-05 C).</fails_when>
  </verify>
  <done>The basis/smoothing family is curated per §3.1: eilers_marx_1996 extended, craven_wahba_1979 and nadaraya_watson_1964 added with correct curated flags, cross_language present, GATE-05 A/B/C green.</done>
</task>

<task type="auto">
  <name>Task 3: Curate the functional-statistics family (Ramsay-Silverman 2005 + geometric median)</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.2 (Functional statistics — `ramsay_silverman_2005` book callable list, `gervini_2008` geometric median; both flagged [CITED]/[ASSUMED])
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §4 (cross_language rows for `ramsay_silverman_2005`: R `fda` pca.fd, Python scikit-fda FPCA, Matlab fdaM pca_fd confidence:low)
    - python/fdars/_capability_map.json (module `fdata`, `_Fdata` — confirm mean_1d/mean_2d/functional_covariance/geometric_median_* exist; `_Fdata.geometric_median` is special-cased per schema)
    - docs/authoring/references-schema.md ("Sub-Method-Keyed Attribution Rule", "cross_language[*] Shape" — Matlab defaults to confidence:low)
  </read_first>
  <action>
    Per 81-RESEARCH §3.2, add the functional-statistics family. AUTHOR-VERIFY each entry against its DOI landing page before `curated:true`; flag `curated:false` on any mismatch/unreachable page.

    1. Add `ramsay_silverman_2005` (book, DOI `10.1007/b98888` — [ASSUMED] A2, so verify the Springer landing page; mark curated:false if the DOI suffix differs). Wire the `fdata.*`/`_Fdata` callables from §3.2 that resolve in `_capability_map.json`: `fdata.mean_1d`, `fdata.mean_2d`, `fdata.functional_covariance`, `fdata.functional_variance`, `fdata.functional_std`, `fdata.norm_lp_1d`, `fdata.deriv_1d`, `fdata.deriv_2d`, `fdata.center_1d`, `fdata.trim_mean`, `fdata.normalize`. Confirm each against the capability map; drop non-resolving keys. Add cross_language R/Python/Matlab per §4 (Matlab `confidence:low`, specific-function URLs on allowlisted domains).
    2. Add `gervini_2008` (Biometrika, DOI `10.1093/biomet/asn031` — [ASSUMED] A10, verify) for `fdata.geometric_median_1d`, `fdata.geometric_median_2d`, `_Fdata.geometric_median`. If the DOI/authors cannot be confirmed, set curated:false.
    3. Do NOT re-cite `_Fdata.to_pc` (already `ramsay_dalzell_1991` in seed) or `_Fdata.depth` (already FM 2001) — keep the sub-method attribution boundary.

    Edit `papers[*].callables` and `callable_index` together (GATE-05 A). After this task the coverage fraction printed by the new test should have grown; note the emitted N/437 in the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map" && python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); c=json.loads((r.files('fdars')/'_capability_map.json').read_text()); bad=[k for k in d['callable_index'] if '.' not in k or (lambda m,n: m not in c or n not in c[m])(*k.split('.',1))]; assert not bad, bad; print('OK papers=%d idx=%d' % (len(d['papers']), len(d['callable_index'])))"</automated>
    <fails_when>ramsay_silverman_2005 or gervini_2008 cite a callable absent from _capability_map.json, callable_index/papers disagree, or a curated entry's DOI/URL fails the structural gate.</fails_when>
  </verify>
  <done>Functional-statistics family curated per §3.2 with author-verified curated flags and cross_language pointers; GATE-05 A/B/C green; the printed coverage fraction has grown from the seed baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator → committed JSON | Hand-authored bibliographic data enters the repo; a wrong attribution is a correctness failure against the "provably correct" core value |
| cross-language URL → allowlist | External publisher/package-doc domains cross into the guarded structural gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-01 | Tampering | _references_map.json attribution | high | mitigate | Author-verify authors/year/title against the DOI landing page before curated:true; flag curated:false on mismatch; standing blocking human review at Phase 84 (GATE-03) |
| T-81-02 | Information Disclosure | coverage fraction | medium | mitigate | Coverage derived from live _capability_map.json (N/437), never hardcoded — cannot silently overstate coverage |
| T-81-03 | Tampering | _ALLOWED_DOMAINS allowlist | medium | mitigate | Only vetted publisher/package-doc hosts appended; same-commit rule with the URLs that use them; GATE-05 C blocks any un-allowlisted domain |
| T-81-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this phase — data + test edits only against the existing dev venv |
</threat_model>

<verification>
- `pytest tests/test_guard_sync_version_independent.py -q` — full Guard Group 3 (A/B/C + new coverage test) green.
- `pytest tests/test_guard_sync_version_independent.py -s -k coverage_fraction` — prints the honest `COVERAGE: N/437` line.
- Denominator recomputed from `_capability_map.json` (437), not hardcoded 409.
</verification>

<success_criteria>
- Two clear-root families (basis/smoothing + functional statistics) curated end-to-end with author-verified `curated` flags.
- `_ALLOWED_DOMAINS` extended with the nine Phase-81 domains in the same commit as the URLs that use them.
- A coverage-emission guard test reports N/437, derived from the live capability map.
- GATE-05 A/B/C stay green — the pattern 81-02..81-05 replay.
</success_criteria>

<output>
Create `.planning/phases/81-curation-paper-registry/81-01-SUMMARY.md` when done. Record: the exact new domains added, the coverage fraction emitted (N/437), and any paper flagged curated:false with the reason (unreachable/unverifiable DOI).
</output>

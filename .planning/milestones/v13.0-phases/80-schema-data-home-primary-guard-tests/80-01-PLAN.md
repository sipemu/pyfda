---
phase: 80-schema-data-home-primary-guard-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/_references_map.json
  - tests/test_guard_sync_version_independent.py
autonomous: true
requirements: [SCHEMA-01, SCHEMA-03, SCHEMA-04]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 42000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`_references_map.json` loads via importlib.resources from the installed fdars package (SCHEMA-01)"
    - "GATE-05 A passes: callable_index keys == union of papers[*].callables (SCHEMA-03)"
    - "GATE-05 B passes: every callable_index key resolves to a real callable in _capability_map.json, _Fdata special-cased (SCHEMA-03)"
    - "GATE-05 C passes: every paper DOI matches ^10\\.\\d{4,9}/\\S+$ and every URL is well-formed with an allowlisted domain, with NO live network resolve (SCHEMA-04)"
    - "The stub seeds 3–5 author-verified papers spanning distinct capability families (SCHEMA-01)"
  artifacts:
    - "python/fdars/_references_map.json"
    - "tests/test_guard_sync_version_independent.py (Group 3 A/B/C added)"
  key_links:
    - "callable_index keys ↔ papers[*].callables (internal redundancy, GATE-05 A)"
    - "callable_index keys ↔ _capability_map.json nested paths (cross-file, GATE-05 B)"
    - "_references_map.json ↔ wheel packaging via importlib.resources (no maturin include needed — files in python/fdars/ ship automatically)"
---

<objective>
Ship the `_references_map.json` seed stub AND its three primary GATE-05 guard tests (A internal-consistency, B cross-file resolution, C structural DOI/URL gate) in ONE atomic commit. This is the phase tracer: it wires the references data artifact end-to-end through every layer the milestone touches — JSON schema → `importlib.resources` load → guard tests → CI pytest run — and proves the whole path green before any curation (Phase 81), tool (Phase 82), or docs (Phase 83) work begins.

Purpose: The schema is never authored free-form. Every downstream entry is validated from day one by the tests landed here. The GATE-04 lesson (guard tests land in the SAME commit as the artifact they guard) is honored — if CI ran between an artifact commit and a test commit, the tests would fail on a file that does not exist.

Output: `python/fdars/_references_map.json` (paper-keyed `papers` + flat `callable_index`, seeded with the 5 research-verified papers), plus Group 3 (A/B/C) tests appended to `tests/test_guard_sync_version_independent.py` with `os`, `re`, `urllib.parse` added to the module imports. Covers SCHEMA-01, SCHEMA-03, SCHEMA-04.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-schema-data-home-primary-guard-tests/80-RESEARCH.md
@.planning/phases/80-schema-data-home-primary-guard-tests/80-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write _references_map.json seed stub (5 author-verified papers, paper-keyed papers + flat callable_index)</name>
  <files>python/fdars/_references_map.json</files>
  <read_first>
    - python/fdars/_capability_curation.json (identifier-space source of truth: keys are "module.callable" with exactly one dot; Fdata methods are "_Fdata.<method>")
    - python/fdars/_capability_map.json (cross-file resolution target: nested "<module>": { "<callable>": {...} }, and "_Fdata": { "<method>": {...} } — every callable you list MUST resolve here)
    - .planning/phases/80-schema-data-home-primary-guard-tests/80-RESEARCH.md (sections: "Recommended JSON Schema Shape", "Seed Papers — Verified Candidates")
  </read_first>
  <action>
Create `python/fdars/_references_map.json` as a UTF-8 JSON object with exactly two top-level keys: `papers` (object keyed by ASCII slug) and `callable_index` (flat object: `"module.callable"` or `"_Fdata.<method>"` → array of paper-key strings).

Seed exactly these 5 papers verbatim from 80-RESEARCH.md "Seed Papers" table (do NOT invent or alter DOIs — these are author-verified against DOI landing pages):

1. key `fraiman_muniz_2001` — title "Trimmed means for functional data", authors ["Fraiman, R.", "Muniz, G."], year 2001, doi "10.1007/BF02595706", url "https://link.springer.com/article/10.1007/BF02595706", type "journal", callables ["depth.fraiman_muniz_1d", "depth.fraiman_muniz_2d", "_Fdata.depth"], notes flagging "Year is 2001, not 1991.", curated true. Before writing, verify each callable resolves in `_capability_map.json` (depth.fraiman_muniz_1d, depth.fraiman_muniz_2d, and _Fdata.depth must all exist); if depth.fraiman_muniz_2d is absent from the capability map, drop it from callables rather than list an unresolvable key.
2. key `lopez_pintado_romo_2009` — title "On the concept of depth for functional data", authors ["López-Pintado, S.", "Romo, J."], year 2009, doi "10.1198/jasa.2009.0108", url "https://www.tandfonline.com/doi/abs/10.1198/jasa.2009.0108", type "journal", callables ["depth.band_1d", "depth.modified_band_1d"], notes "Introduces band depth (BD) and modified band depth (MBD) in the same paper.", curated true. Verify both callables resolve in `_capability_map.json` under the `depth` module; drop any that do not resolve.
3. key `srivastava_et_al_2011` — title "Registration of functional data using Fisher-Rao metric", authors ["Srivastava, A.", "Wu, W.", "Kurtek, S.", "Klassen, E.", "Marron, J.S."], year 2011, url "https://arxiv.org/abs/1103.3817", type "preprint", NO `doi` field (arXiv preprint, no journal DOI), callables ["alignment.karcher_mean", "alignment.elastic_align_pair", "alignment.srsf_transform"], notes "SRSF framework via Fisher-Rao metric; arXiv:1103.3817. NOT the TPAMI SRV/shape paper.", curated true. Verify each alignment callable resolves; drop unresolvable ones.
4. key `ramsay_dalzell_1991` — title "Some tools for functional data analysis", authors ["Ramsay, J.O.", "Dalzell, C.J."], year 1991, doi "10.1111/j.2517-6161.1991.tb01844.x", url "https://rss.onlinelibrary.wiley.com/doi/abs/10.1111/j.2517-6161.1991.tb01844.x", type "journal", callables ["_Fdata.to_pc"], notes "Seminal FDA paper introducing FPCA.", curated true. Verify _Fdata.to_pc resolves under the "_Fdata" top-level key in `_capability_map.json`.
5. key `eilers_marx_1996` — title "Flexible smoothing with B-splines and penalties", authors ["Eilers, P.H.C.", "Marx, B.D."], year 1996, doi "10.1214/ss/1038425655", url "https://projecteuclid.org/journals/statistical-science/volume-11/issue-2/Flexible-smoothing-with-B-splines-and-penalties/10.1214/ss/1038425655.full", type "journal", callables ["smoothing.gcv_smoother", "basis.basis_nbasis_cv"], notes "Introduces P-splines (penalised B-splines).", curated true. Verify both callables resolve; drop unresolvable ones.

Add ONE `cross_language` block to demonstrate the shape — attach it to `fraiman_muniz_2001` with an `R` entry: {"package": "fda.usc", "function": "depth.FM", "url": "https://rdrr.io/cran/fda.usc/man/depth.fdata.html", "confidence": "high"}. Do not add Matlab entries (they would require `confidence: "low"` per D-locked rule and add no value to the stub).

Then build `callable_index`: for every callable listed in ANY paper's `callables`, add an entry mapping that callable string to a JSON array of the paper keys that reference it. The union of all `papers[*].callables` MUST exactly equal the set of `callable_index` keys (this is the GATE-05 A invariant — Task 2 tests it).

Field shapes per paper (per 80-RESEARCH.md schema table): title (string), authors (array[string]), year (integer), doi (string, omit for preprint), url (string), type (one of journal/conference/preprint/book/book_chapter), callables (array[string]), cross_language (object, optional), notes (string, optional), curated (boolean, always true in this stub). No `_uncurated_placeholder` / `curated:false` entries this phase — the tail convention is documented in Plan 02, not exercised here.

Emit valid JSON only (double-quoted keys, no trailing commas, no comments).
  </action>
  <acceptance_criteria>
    - `python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); print(sorted(d))"` prints `['callable_index', 'papers']`
    - `python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); assert len(d['papers'])==5, len(d['papers'])"` exits 0
    - `python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); u=set(c for p in d['papers'].values() for c in p['callables']); assert u==set(d['callable_index']), (u-set(d['callable_index']), set(d['callable_index'])-u)"` exits 0
    - Every paper has integer `year`, string `title`, non-empty `authors`, and `curated: true`; `srivastava_et_al_2011` has NO `doi` key
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); c=json.loads((r.files('fdars')/'_capability_map.json').read_text()); bad=[k for k in d['callable_index'] if '.' not in k or (lambda m,n: m not in c or n not in c[m])(*k.split('.',1))]; assert not bad, bad; print('OK', len(d['papers']),'papers')"</automated>
    <fails_when>Non-zero exit with an AssertionError listing callable_index keys that do not resolve in _capability_map.json, OR a KeyError/JSONDecodeError if the file is malformed or a paper lacks `callables`.</fails_when>
  </verify>
  <done>`_references_map.json` exists in `python/fdars/`, loads via importlib.resources, has 5 curated papers spanning depth/alignment/FPCA/smoothing, and its callable_index is internally consistent and fully cross-file-resolvable against _capability_map.json.</done>
</task>

<task type="auto">
  <name>Task 2: Append Group 3 A + B guard tests (internal consistency + cross-file resolution) to test_guard_sync_version_independent.py</name>
  <files>tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - tests/test_guard_sync_version_independent.py (module imports at lines 34-41; Group 2 companion tests end at line 300 — append Group 3 after that; mirror the Group 1/2 header-comment structure and the importlib.resources load pattern already used for _capability_map.json)
    - .planning/phases/80-schema-data-home-primary-guard-tests/80-RESEARCH.md (section "Guard Test Placement and Structure (GATE-05)" — the Group 3 A/B template is implementation-ready and should be used near-verbatim)
    - python/fdars/_references_map.json (the artifact these tests load — created in Task 1)
  </read_first>
  <action>
Append two new test functions to the END of `tests/test_guard_sync_version_independent.py` (after line 300), under a new section header comment block `# Guard Group 3 — _references_map.json primary GATE-05 (SCHEMA-03)` matching the existing `# ====...` banner style.

Neither test imports `mcp` and neither has a `pytestmark`/`skipif` — both MUST run on Python 3.9+. They use only `json` and `resources` (already imported at module level).

Test A — `test_references_map_internal_consistency()` (SCHEMA-03 A): load `_references_map.json` via `resources.files("fdars") / "_references_map.json"`, then assert `frozenset(data["callable_index"].keys()) == frozenset(c for p in data["papers"].values() for c in p.get("callables", []))`. On failure, the assertion message must show both set differences (in-papers-not-in-index, in-index-not-in-any-paper). Use the docstring and body from the 80-RESEARCH.md Group 3 A template.

Test B — `test_references_map_cross_file_resolution()` (SCHEMA-03 B): load both `_references_map.json` and `_capability_map.json` via importlib.resources. For each key in `callable_index`: split on the FIRST dot into `module`, `callable_name`; if `module` not in cap_data OR `callable_name` not in `cap_data[module]`, collect an error string. This resolves `"_Fdata.<method>"` keys against the `"_Fdata"` top-level key naturally (no special branch needed — `_Fdata` is just another top-level module key in `_capability_map.json`). Assert the error list is empty; on failure list every unresolved key. Use the 80-RESEARCH.md Group 3 B template.

Do NOT add the Python 3.10+ companion tests (`_REFERENCES_MODULES` mirror, LLM-free boundary) — those are Phase 82 work and `_REFERENCES_MODULES` does not exist in server.py yet.
  </action>
  <acceptance_criteria>
    - `grep -c "def test_references_map_internal_consistency" tests/test_guard_sync_version_independent.py` prints `1`
    - `grep -c "def test_references_map_cross_file_resolution" tests/test_guard_sync_version_independent.py` prints `1`
    - Neither new test function body contains the substring `pytest.skip` or `importorskip` or `mcp` (verify by reading the appended block)
    - `pytest tests/test_guard_sync_version_independent.py::test_references_map_internal_consistency tests/test_guard_sync_version_independent.py::test_references_map_cross_file_resolution -q` reports 2 passed
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py::test_references_map_internal_consistency tests/test_guard_sync_version_independent.py::test_references_map_cross_file_resolution -q</automated>
    <fails_when>pytest exits non-zero, or output does not contain "2 passed" (a failing assertion prints the offending set difference / unresolved-key list).</fails_when>
  </verify>
  <done>Group 3 A and B guard tests exist in the file, run on Python 3.9+ with no mcp import, and both pass against the Task 1 stub.</done>
</task>

<task type="auto">
  <name>Task 3: Add module imports (os, re, urllib.parse) + Group 3 C structural DOI/URL gate</name>
  <files>tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - tests/test_guard_sync_version_independent.py (module import block lines 34-41 — currently imports ast, json, sys, resources, pytest; add os, re, urllib.parse)
    - tests/test_advisor_live_integration.py (FDARS_INTEGRATION=1 env-gate prior art the FDARS_ONLINE_CHECKS pattern inverts)
    - .planning/phases/80-schema-data-home-primary-guard-tests/80-RESEARCH.md (section "Structural DOI/URL Gate (SCHEMA-04)" — the Group 3 C template + `_DOI_RE` + `_ALLOWED_DOMAINS` allowlist are implementation-ready)
  </read_first>
  <action>
Two edits, one file.

Edit 1 — module imports: add `import os`, `import re`, and `import urllib.parse` to the stdlib import block (lines 34-41), keeping alphabetical grouping with the existing `ast`/`json`/`sys` block. Do NOT move `from importlib import resources` or `import pytest`.

Edit 2 — append Group 3 C after the Task 2 tests. Add module-level constants `_DOI_RE = re.compile(r"^10\.\d{4,9}/\S+$")` and `_ALLOWED_DOMAINS` (a frozenset) copied verbatim from the 80-RESEARCH.md "Structural DOI/URL Gate" allowlist (doi.org, link.springer.com, www.tandfonline.com, academic.oup.com, onlinelibrary.wiley.com, rss.onlinelibrary.wiley.com, www.sciencedirect.com, dl.acm.org, ieeexplore.ieee.org, www.jstor.org, projecteuclid.org, arxiv.org, www.researchgate.net, pubmed.ncbi.nlm.nih.gov, pmc.ncbi.nlm.nih.gov, rdrr.io, cran.r-project.org, www.rdocumentation.org, pypi.org, fdasrsf-python.readthedocs.io, sipemu.github.io). Ensure the allowlist covers every URL domain actually present in the Task 1 stub — in particular `rss.onlinelibrary.wiley.com` (Ramsay-Dalzell URL) and `rdrr.io` (the F&M cross_language R url); ADD any stub domain the research allowlist omits so the gate passes on the seeded data.

Then define `test_references_map_doi_url_structural_gate()` (SCHEMA-04), no mcp import, runs on Python 3.9+:
- First line of body: `if os.environ.get("FDARS_ONLINE_CHECKS") == "1": pytest.skip("FDARS_ONLINE_CHECKS=1 set — live resolve path; structural gate skipped.")` — the structural gate runs UNCONDITIONALLY in CI; only the opt-in live-resolve path (a future script, REF-FUT-02) sets the env var.
- Load `_references_map.json`. For each paper: if `curated` (default True) and a non-empty `doi` is present, assert it matches `_DOI_RE` (collect error otherwise). For `url` (paper) and each `cross_language[*].url`: parse with `urllib.parse.urlparse`; require scheme in ("http","https") and `netloc` in `_ALLOWED_DOMAINS`; collect an error string otherwise. Assert the collected error list is empty; on failure list every violation. Perform NO network I/O (no requests, no urlopen, no socket).
Use the 80-RESEARCH.md Group 3 C template body.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' tests/test_guard_sync_version_independent.py | grep -c "^import os$"` prints `1`
    - `grep -v '^#' tests/test_guard_sync_version_independent.py | grep -c "^import re$"` prints `1`
    - `grep -v '^#' tests/test_guard_sync_version_independent.py | grep -c "^import urllib.parse$"` prints `1`
    - `grep -c "def test_references_map_doi_url_structural_gate" tests/test_guard_sync_version_independent.py` prints `1`
    - The test body contains `urllib.parse.urlparse` and does NOT contain `urlopen`, `requests`, or `socket`
    - `pytest tests/test_guard_sync_version_independent.py::test_references_map_doi_url_structural_gate -q` reports 1 passed (structural gate green on the stub)
    - `FDARS_ONLINE_CHECKS=1 pytest tests/test_guard_sync_version_independent.py::test_references_map_doi_url_structural_gate -q` reports 1 skipped (env-gate honored)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q && FDARS_ONLINE_CHECKS=1 pytest tests/test_guard_sync_version_independent.py::test_references_map_doi_url_structural_gate -q</automated>
    <fails_when>Either pytest invocation exits non-zero; or the first run does not report all guard tests passing (including the three new Group 3 tests); or the second run does not report "1 skipped" (env-gate not honored, meaning the structural gate would attempt live resolve in an online run).</fails_when>
  </verify>
  <done>Module imports include os/re/urllib.parse; the structural DOI/URL gate runs offline in CI (passes on the stub) and skips cleanly under FDARS_ONLINE_CHECKS=1; no network I/O in the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON → test/CI | `_references_map.json` is author-authored data consumed by guard tests; a wrong attribution is a correctness failure, not a security one |
| test env var → resolve path | `FDARS_ONLINE_CHECKS=1` is the only switch that could enable network I/O; it must never be set under `pytest`/`mkdocs build` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-01 | Tampering | `_references_map.json` attribution (wrong year/author/DOI) | medium | mitigate | Seed papers used verbatim from author-verified 80-RESEARCH.md; DOIs not invented; F&M pinned to 2001 in `notes`; blocking human citation review deferred to Phase 84 (GATE-03) |
| T-80-02 | Information Disclosure | Live DOI/URL network resolve leaking into CI | low | mitigate | Structural gate performs NO network I/O; `FDARS_ONLINE_CHECKS=1` opt-in only, asserted to `pytest.skip` when set; acceptance test proves 1-skipped behavior |
| T-80-03 | Denial of Service | callable_index ↔ capability_map drift silently shipping | low | mitigate | GATE-05 A (internal) + B (cross-file) run in the SAME commit as the artifact (GATE-04 lesson); CI `pytest tests/ -v` catches any future drift |
</threat_model>

<verification>
Run the full guard file plus a fresh online-mode skip check:
- `pytest tests/test_guard_sync_version_independent.py -v` — all Group 1/2/3 tests green (3 new Group 3 tests included)
- `FDARS_ONLINE_CHECKS=1 pytest tests/test_guard_sync_version_independent.py::test_references_map_doi_url_structural_gate -q` — 1 skipped
- `pytest tests/ -v` — full suite green (wave-merge gate)
</verification>

<success_criteria>
- `python/fdars/_references_map.json` committed, loads via importlib.resources, 5 curated author-verified papers spanning distinct families (SCHEMA-01)
- GATE-05 A + B pass on Python 3.9+ with no mcp import (SCHEMA-03)
- Structural DOI/URL gate passes offline in CI and skips under FDARS_ONLINE_CHECKS=1 (SCHEMA-04)
- Artifact + all three guard tests land in ONE atomic commit (GATE-04 lesson)
</success_criteria>

<output>
Create `.planning/phases/80-schema-data-home-primary-guard-tests/80-01-SUMMARY.md` when done
</output>

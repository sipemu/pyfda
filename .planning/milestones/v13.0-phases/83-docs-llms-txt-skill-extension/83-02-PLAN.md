---
phase: 83-docs-llms-txt-skill-extension
plan: 02
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - mkdocs.yml
  - scripts/check_references_render.py
autonomous: true
requirements: [DOCS-03]
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "mkdocs.yml nav has a `Scientific References: references.md` entry immediately after `AI Capability Map: ai-capability-map.md`"
    - "docs/references.md exists at the exact path the nav entry resolves to"
    - "a fast render check reports no nav/link errors for references.md (no 25-min strict build)"
  artifacts:
    - mkdocs.yml
    - scripts/check_references_render.py
  key_links:
    - "nav entry `- Scientific References: references.md` sits between the AI Capability Map entry (line ~169) and the scikit-learn API entry"
    - "scripts/check_references_render.py validates nav placement + balanced fences + internal-link resolution, fast, without executing markdown-exec fences (the full 25-min --strict is Phase 84)"
---

<objective>
Wire the `docs/references.md` produced by Plan 01 into the MkDocs nav (DOCS-03), under the existing AI/capability section, and prove the page renders cleanly with a FAST check — NOT the whole-site 25-min `--strict` build (that is the Phase-84 close gate, deferred per standing decision).

Purpose: A generated page not in the nav is invisible; a nav entry pointing at a missing file breaks `--strict`. This plan closes that loop with a targeted validity check committed as a reusable script.
Output: One nav entry in `mkdocs.yml` + a small `scripts/check_references_render.py` validity checker.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- NEW nav entry `- Scientific References: references.md` in `mkdocs.yml`
- NEW `scripts/check_references_render.py` fast validity checker

<tasks>

<task type="auto">
  <name>Task 1: Insert the Scientific References nav entry in mkdocs.yml</name>
  <files>mkdocs.yml</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Design 3.1 exact edit, Design 3.2 strict gotchas)
    - mkdocs.yml lines 159-176 (AI Advisor block ends line 168; `AI Capability Map: ai-capability-map.md` at line 169; `scikit-learn API:` at line 170)
  </read_first>
  <precondition>docs/references.md exists (produced by Plan 83-01) so a strict build will not complain about a nav pointing at a missing file.</precondition>
  <action>
    In `mkdocs.yml`, insert exactly one nav line reading two-space-indent then `- Scientific References: references.md` between the `- AI Capability Map: ai-capability-map.md` line (169) and the `- scikit-learn API:` line (170). Match the two-space indentation of the surrounding flat top-level nav entries exactly (a flat list item at this level, not a nested sub-list). Per DOCS-03. Do not modify any other nav entry.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -A1 "AI Capability Map: ai-capability-map.md" mkdocs.yml | grep -q "Scientific References: references.md" && test -f docs/references.md && echo NAV_EDIT_OK</automated>
    <fails_when>the `Scientific References: references.md` entry is missing or not on the line directly after the AI Capability Map entry, or docs/references.md does not exist.</fails_when>
  </verify>
  <done>`mkdocs.yml` has the single new nav entry directly after the AI Capability Map entry with correct indentation, and the target file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add scripts/check_references_render.py and run the fast validity check</name>
  <files>scripts/check_references_render.py</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Design 3.3 lightweight local validation, Assumption A1 on DOCS_FAST)
    - docs/references.md (the page to validate)
    - mkdocs.yml (the nav to validate)
  </read_first>
  <action>
    Create `scripts/check_references_render.py` — a fast, dependency-free validator that runs in well under a minute and does NOT execute markdown-exec fences (the whole-site executed `--strict` is Phase 84, per standing decision; do not run it here). The script must assert, exiting non-zero with a clear message on any failure: (1) `docs/references.md` exists; (2) its code-fence markers are balanced (even count of lines starting with a triple-backtick); (3) `mkdocs.yml` contains the `Scientific References: references.md` nav entry directly after the AI Capability Map entry; (4) every relative `.md` link inside `docs/references.md` resolves to an existing file under `docs/` (skip `http` links). On success print `RENDER_VALIDITY_OK`. Keep it stdlib-only (`pathlib`, `re`, `sys`) so it runs in any docs env without a compiled fdars. Then run it to confirm the page + nav validate.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && { [ -x .venv/bin/python ] && P=.venv/bin/python || P=python; } && $P scripts/check_references_render.py</automated>
    <fails_when>docs/references.md is missing, its code fences are unbalanced, the nav entry is absent/misplaced, or any internal .md link in the page fails to resolve under docs/.</fails_when>
  </verify>
  <done>`scripts/check_references_render.py` exists, is stdlib-only, and prints `RENDER_VALIDITY_OK` — proving nav placement, balanced fences, and resolvable internal links without a 25-min strict build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated page → mkdocs nav | a nav entry pointing at a missing/renamed file breaks the site build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-04 | Denial of Service | broken nav/link entry | medium | mitigate | Task 1 precondition + Task 2 checker assert the file exists and every internal link resolves before the Phase-84 strict build |
| T-83-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — nav edit + stdlib-only checker, no new dependencies |
</threat_model>

<verification>
- `mkdocs.yml` has `Scientific References: references.md` directly after `AI Capability Map: ai-capability-map.md`.
- `scripts/check_references_render.py` prints `RENDER_VALIDITY_OK`.
- No 25-min whole-site `--strict` executed (deferred to Phase 84).
</verification>

<success_criteria>
DOCS-03 satisfied: `docs/references.md` is nav-wired under the AI/capability section and a fast checker confirms the page renders with valid nav + internal links, without the deferred whole-site strict build.
</success_criteria>

<output>
Create `.planning/phases/83-docs-llms-txt-skill-extension/83-02-SUMMARY.md` when done. Note that the whole-site `mkdocs build --strict` over references.md remains a Phase-84 close-gate item.
</output>
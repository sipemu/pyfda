---
phase: 75-deepen-analyze-family-thin-pages
plan: 04
type: execute
wave: 4
depends_on: ["75-03"]
files_modified:
  - docs/analyze/shapelets.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-02]
estimate:
  tokens: 60000
  raw_tokens: 39000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "shapelets.md has a '## When to use' decision section and a '## See also' block (DEPTH-02)"
    - "discover_shapelets is documented as returning a summary dict {n_shapelets, quality}, NOT a list of raw shapelet arrays"
    - "shapelet_distance is documented as returning a (float, int) tuple (min_distance, best_offset), NOT a scalar; the search-space params (min_length/max_length/max_candidates/max_shapelets) and shapelet_classifier_fit ncomp are documented"
    - "Page carries >=3 note/tip/warning/info admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK when run offline under .venv"
  artifacts:
    - docs/analyze/shapelets.md
  key_links:
    - "Fences import from fdars.shapelet (and fdars.metric for GAK) and run against the installed .venv package"
    - "See also links resolve to existing pages (clustering.md, advanced-clustering.md, ../represent/distance-metrics.md, outlier-detection.md, index.md)"
---

<objective>
Bring `docs/analyze/shapelets.md` to full mature-page parity (DEPTH-02), following the proven Phase 75 pattern: restructure to the UNNUMBERED mature analyze template, correct the confirmed API errors (chiefly that `discover_shapelets` returns a summary dict and `shapelet_distance` returns a tuple), document the search-space and PCA-reduction parameters, add >=3 admonitions, >=3 runnable fences (>=1 exec'd `html="1"` figure), cross-refs, and verify all fences offline under `.venv`.

Purpose: A reader can decide when shapelet features / GAK apply, call the discovery and classification API correctly, tune the search space, and interpret the results.
Output: A parity-grade `shapelets.md` whose exec fences all emit `FDARS_FENCE_OK` offline under `.venv`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new symbols, bindings, or files. This plan modifies exactly ONE existing tracked doc page: `docs/analyze/shapelets.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bring shapelets.md to full parity</name>
  <files>docs/analyze/shapelets.md</files>
  <read_first>
    - docs/analyze/shapelets.md (page to restructure — preserve the shapelet + GAK math intros, diagram link, the correct shapelet_transform_fit / shapelet_transform / shapelet_classifier_fit / PyShapeletFit / PyShapeletClassifierFit / GAK tables, and the three references)
    - docs/analyze/clustering.md (UNNUMBERED mature analyze template to mirror)
    - docs/analyze/functional-time-series.md (the Phase 75 tracer — mirror its applied parity structure)
    - src/shapelet_mod.rs (source of truth for every shapelet signature/default/return-type)
    - src/metric_mod.rs lines 87-198 (source of truth for gak/sigma_gak/gak_gram_* signatures)
    - .planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md (Sections 2.4, 3.4, 4, 5, 6.3, 7 Pitfalls 6-7, 8)
    - scripts/docs_fig.py (fig/render helpers for the html figure)
  </read_first>
  <action>
Restructure the page to mirror the UNNUMBERED mature analyze template (plain `## Title` headings, method-selector table near the top), preserving correct existing prose/examples. Deliver ALL of the following. (DEPTH-02)

CORRECT these confirmed API errors (all VERIFIED against src/shapelet_mod.rs per RESEARCH 2.4 / 3.4):
1. discover_shapelets: page says it "returns a list of 1D numpy arrays — the raw shapelet subsequences". Actual returns a DICT with keys n_shapelets (int) and quality (str) — NOT a list of arrays. Full signature discover_shapelets(data, labels, min_length=3, max_length=0, max_candidates=10000, max_shapelets=0, quality="info_gain", seed=0). Note: to obtain the actual shapelet arrays, use shapelet_transform_fit (shapelets stored inside the PyShapeletFit handle).
2. shapelet_distance: page says it "returns... a scalar float". Actual returns a tuple (float, int) = (min_distance, best_offset). Signature shapelet_distance(shapelet_z, series, best_so_far=inf) — shapelet_z must be pre-z-normalized (L,), series raw (m,) with m>=L.
3. Search-space parameters: document that shapelet_transform_fit and shapelet_classifier_fit both take min_length=3, max_length=0 (0 -> full length), max_candidates=10000 (0 -> exhaustive), max_shapelets=0 (0 -> auto min(10n, 1000)) — these control the quality/speed tradeoff. The page currently lists only quality and seed.
4. shapelet_classifier_fit: document the full signature including ncomp=None (optional PCA pre-reduction of the shapelet-feature matrix) and note the DEFAULT is k=1 (the page's k=3 example is fine as a usage example, but the default should be stated). classifier is "knn" or "lda".
5. Keep the correct GAK tables (gak / sigma_gak / gak_gram_matrix / gak_gram_train / gak_gram_predict), the PyShapeletFit / PyShapeletClassifierFit attribute lists, and the three references verbatim.

ADD these missing parity sections (mirroring the analyze template order):
- Keep the intro paragraphs and the concept-diagram image line.
- A method-selector table near the top (method | fdars function | key output) covering discover_shapelets, shapelet_transform_fit/shapelet_transform, shapelet_classifier_fit, shapelet_distance, and the GAK kernel family.
- A `## When to use` decision section: shapelet features vs elastic distances (GAK/DTW-style) vs FPCA features for functional classification; when interpretable local subsequences matter.
- Plain `## Title` method sections for shapelet discovery, the shapelet transform + classifier, shapelet distance, and GAK kernels — each with brief theory, a non-exec "what you'd call" syntax block, corrected Parameters/Returns tables, and a runnable fence where one lives.
- A parameter-selection subsection: the search-space tradeoff (min_length/max_length/max_candidates/max_shapelets — smaller max_candidates = faster, coarser search) and the optional ncomp PCA reduction.
- A result-interpretation subsection: reading .train_accuracy (a TRAINING metric, not a generalization estimate), using .predict() on new data, reading the (distance, offset) tuple from shapelet_distance.
- Keep any accurate R-vs-Python note if one exists; do not invent one.
- Keep the existing References; append a `## See also` bullet list (RESEARCH Section 5) linking clustering.md, advanced-clustering.md, ../represent/distance-metrics.md, outlier-detection.md, and index.md (3-5 links).

FENCES (>=3 runnable, each printing the sentinel; >=1 with html="1"; small deterministic np.random.default_rng(seed); keep fences fully synthetic to bound runtime — reference phoneme.csv only in a `!!! tip "Real data"` admonition, do NOT run an exhaustive phoneme search in a fence per RESEARCH Open Question 2):
- A 2-class synthetic classification fence: shapelet_transform_fit + shapelet_transform + shapelet_classifier_fit with max_candidates=200 (RESEARCH 6.3 "Shapelets minimal fence"); print n_shapelets, feature-matrix shape, and .train_accuracy.
- A discover_shapelets fence that prints result['n_shapelets'] and result['quality'] — the load-bearing proof that the return is a summary dict, not an array list.
- A GAK fence: build a small gak_gram_matrix (or gak_gram_train/gak_gram_predict) with sigma=None (auto sigma_gak heuristic); print the Gram matrix shape and unit diagonal.
- ONE html="1" figure fence: overlay a discovered/representative shapelet on a few training curves, OR plot the distribution of a shapelet-distance feature across the two classes; print render(f) then the sentinel.

ADMONITIONS (>=3, mixing types): a `!!! warning` that discover_shapelets returns a summary dict (not shapelet arrays) — use shapelet_transform_fit to get the shapelets; a `!!! note` that shapelet_distance returns a (distance, offset) tuple; a `!!! tip` on the search-space tradeoff (or real-data phoneme guidance). Ensure >=3 remain after restructuring.

Do NOT run the whole-site mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG. Do NOT document discover_shapelets as returning shapelet arrays, or shapelet_distance as returning a scalar.
  </action>
  <acceptance_criteria>
    - page contains a `## When to use` heading (grep matches >= 1)
    - page contains a `## See also` heading (grep matches >= 1)
    - occurrences of the FDARS_FENCE_OK sentinel token in the page >= 3
    - lines matching the admonition prefix `!!! (note|tip|warning|info)` >= 3
    - at least one fence opener declares html="1"
    - the page documents discover_shapelets returning n_shapelets AND quality keys (both tokens present in the discover_shapelets context)
    - a code line accesses the discover_shapelets dict result via `['n_shapelets']` (grep matches >= 1)
    - the page documents shapelet_distance returning a tuple: token `best_offset` (or `offset`) appears alongside a min-distance mention
    - the page mentions the search-space params: tokens `max_candidates` AND `min_length` both present
    - the shared fence runner reports every exec fence emitted the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/analyze/shapelets.md","utf8");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,discover_dict:/n_shapelets/.test(s)&&/quality/.test(s),discover_key_access:/\[.n_shapelets.\]/.test(s),distance_tuple:/best_offset|offset/.test(s),search_params:/max_candidates/.test(s)&&/min_length/.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>The page is missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; discover_shapelets not documented as returning the {n_shapelets, quality} dict (or no `['n_shapelets']` dict access in a fence); shapelet_distance tuple return not documented (no `best_offset`/`offset`); or the search-space params `max_candidates`/`min_length` absent.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/analyze/shapelets.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK` (nonzero exit from run_page_fences.py, or a `FENCE ... FAILED` line).</fails_when>
  </verify>
  <done>shapelets.md mirrors the UNNUMBERED mature analyze template with `## When to use` + `## See also`, discover_shapelets documented as a summary dict, shapelet_distance documented as a (float, int) tuple, search-space and ncomp params documented, >=3 admonitions, >=3 sentinel fences (>=1 html figure), and every exec fence emits FDARS_FENCE_OK offline under `.venv`.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — editing one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with synthetic in-fence data only. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes: When-to-use + See-also present, >=3 sentinel fences, >=3 admonitions, >=1 html figure, discover_shapelets dict return documented + accessed, shapelet_distance tuple documented, search-space params present.
- Fence gate: `scripts/run_page_fences.py docs/analyze/shapelets.md` exits 0.
- Whole-site `mkdocs build --strict` is intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
shapelets.md is at mature-page parity (DEPTH-02) with the discover_shapelets and shapelet_distance return-type corrections applied and every exec fence emitting FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/75-deepen-analyze-family-thin-pages/75-04-SUMMARY.md` when done.
</output>

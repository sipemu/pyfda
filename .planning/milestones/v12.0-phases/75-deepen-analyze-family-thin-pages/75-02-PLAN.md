---
phase: 75-deepen-analyze-family-thin-pages
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - docs/analyze/density-fda.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-02]
estimate:
  tokens: 58000
  raw_tokens: 38000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "density-fda.md has a '## When to use' decision section and a '## See also' block (DEPTH-02)"
    - "inverse_lqd is documented with its 3-argument signature inverse_lqd(psi, t_grid, target_argvals) — no 2-argument form remains"
    - "The page clarifies that lqd_transform / lqd_fpca output lives on the uniform quantile grid (n_q,), NOT the original argvals grid (m,); n_quantile_pts optional param documented"
    - "Page carries >=3 note/tip/warning/info admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK when run offline under .venv"
  artifacts:
    - docs/analyze/density-fda.md
  key_links:
    - "Fences import from fdars.density_fda and run against the installed .venv package (no new bindings)"
    - "See also links resolve to existing pages (../regression/frechet-regression.md, functional-statistics.md, outlier-detection.md, index.md)"
---

<objective>
Bring `docs/analyze/density-fda.md` to full mature-page parity (DEPTH-02), following the pattern proven on the Phase 75 tracer (`functional-time-series.md`): restructure to the UNNUMBERED mature analyze template, correct the confirmed API errors (chiefly the 3-argument `inverse_lqd` and the quantile-grid output shape), add >=3 admonitions, >=3 runnable fences (>=1 exec'd `html="1"` figure), cross-refs, and verify all fences offline under `.venv`.

Purpose: A reader can decide when density-as-data methods apply, round-trip a density through the LQD transform correctly, and interpret LQD-FPCA output.
Output: A parity-grade `density-fda.md` whose exec fences all emit `FDARS_FENCE_OK` offline under `.venv`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new symbols, bindings, or files. This plan modifies exactly ONE existing tracked doc page: `docs/analyze/density-fda.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bring density-fda.md to full parity</name>
  <files>docs/analyze/density-fda.md</files>
  <read_first>
    - docs/analyze/density-fda.md (page to restructure — preserve the LQD math intro, diagram link, the correct normalize_density / wasserstein_barycenter / lqd_fpca-6-key tables, and the two references)
    - docs/analyze/clustering.md (UNNUMBERED mature analyze template to mirror: method-selector table near top, plain `## Title` sections, admonition placement, `## See also`, `## References`)
    - docs/analyze/functional-time-series.md (the Phase 75 tracer — mirror its applied parity structure exactly)
    - src/density_fda_mod.rs (source of truth for every density_fda signature/default/return-key)
    - .planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md (Sections 2.2, 3.2, 4, 5, 6.3, 7 Pitfalls 1-2, 8 "Density FDA")
    - scripts/docs_fig.py (fig/render/fast helpers for the html figure)
  </read_first>
  <action>
Restructure the page to mirror the UNNUMBERED mature analyze template (plain `## Title` headings, method-selector table near the top), preserving correct existing prose/examples. Deliver ALL of the following. (DEPTH-02)

CORRECT these confirmed API errors (all VERIFIED against src/density_fda_mod.rs per RESEARCH 2.2 / 3.2):
1. inverse_lqd: page shows inverse_lqd(lqd, argvals) (2 args). Actual takes THREE positional args: inverse_lqd(psi, t_grid, target_argvals) — psi is the LQD values (n_q,) on the quantile grid, t_grid is the uniform quantile grid (n_q,) = np.linspace(0, 1, n_q), target_argvals is the (m,) reconstruction grid. Rewrite the signature, the table, and any prose that describes it as 2-argument.
2. lqd_transform: document the optional n_quantile_pts=None parameter (default max(m, 101)); full signature lqd_transform(density, argvals, n_quantile_pts=None). density must be STRICTLY positive.
3. lqd_fpca: document the optional n_quantile_pts=None parameter; full signature lqd_fpca(density_matrix, argvals, ncomp=3, n_quantile_pts=None).
4. Quantile-grid clarification (the most consequential fix): make explicit in prose AND an admonition that lqd_transform returns values of length n_q (NOT m) on the UNIFORM QUANTILE GRID [0,1], and that lqd_fpca mean/loadings are likewise on the (n_q,) quantile grid — you cannot compare LQD output directly to the original argvals grid, and inverse_lqd requires passing t_grid = np.linspace(0, 1, len(psi)) explicitly.
5. Keep the correct normalize_density, wasserstein_barycenter, and 6-key lqd_fpca return tables and the two references verbatim.

ADD these missing parity sections (mirroring the analyze template order):
- Keep the intro paragraphs and the concept-diagram image line.
- A method-selector table near the top (method | fdars function | key output) covering normalize_density, lqd_transform, inverse_lqd, wasserstein_barycenter, lqd_fpca.
- A `## When to use` decision section: when densities/distributions appear as your data objects (vs standard FPCA of raw curves); contrast LQD-space analysis (respects the density constraint / Wasserstein geometry) against naive L2-FPCA of raw densities; note the Frechet-regression cross-reference for density RESPONSES.
- Plain `## Title` method sections for density normalization, the LQD transform + inverse (round-trip), the Wasserstein barycenter, and LQD-FPCA — each with brief theory, a non-exec "what you'd call" syntax block, corrected Parameters/Returns tables, and a runnable fence where one lives.
- A parameter-selection subsection: choosing ncomp for lqd_fpca via the fve cumulative-variance vector.
- A result-interpretation subsection: reading LQD loadings as modes of density-shape variation (on the quantile grid), reading fve.
- An accurate "Methods available in R but not (yet) in Python" note covering ONLY RESEARCH 3.2 items (Frechet regression on density responses lives in fdars.frechet — cross-reference; density-on-scalar regression not directly exposed beyond frechet_global_reg).
- Keep the existing References; append a `## See also` bullet list (RESEARCH Section 5) linking ../regression/frechet-regression.md, functional-statistics.md, outlier-detection.md, and index.md (3-5 links).

FENCES (>=3 runnable, each printing the sentinel; >=1 with html="1"; small deterministic np.random.default_rng(seed)):
- A round-trip fence: normalize_density -> lqd_transform -> inverse_lqd back to the original grid using t_grid = np.linspace(0, 1, len(psi)); print psi.shape (showing it != m), the normalized integral, and the reconstructed integral (RESEARCH 6.3 "Density FDA minimal fence"). This is the load-bearing correctness proof of the 3-arg inverse_lqd fix.
- A wasserstein_barycenter + lqd_fpca fence over a small stack of synthetic densities (RESEARCH 8 "Density FDA: round-trip + barycenter fence"); print barycenter integral and lqd_fpca scores shape.
- ONE html="1" figure fence: overlay several original densities plus their Wasserstein barycenter, OR an LQD-FPCA score scatter across 2 components; print render(f) then the sentinel.

ADMONITIONS (>=3, mixing types): a `!!! warning` that lqd_transform output is on the (n_q,) quantile grid, not the (m,) argvals grid; a `!!! note` that inverse_lqd needs the explicit t_grid; a `!!! tip` on choosing ncomp via fve (and/or that density rows for wasserstein_barycenter must be non-negative and integrate to 1). Ensure >=3 remain after restructuring.

Do NOT run the whole-site mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG. Do NOT document inverse_lqd as a 2-argument function anywhere.
  </action>
  <acceptance_criteria>
    - page contains a `## When to use` heading (grep matches >= 1)
    - page contains a `## See also` heading (grep matches >= 1)
    - occurrences of the FDARS_FENCE_OK sentinel token in the page >= 3
    - lines matching the admonition prefix `!!! (note|tip|warning|info)` >= 3
    - at least one fence opener declares html="1"
    - the 3-argument form `inverse_lqd(psi, t_grid, target_argvals` appears at least once in the page
    - no 2-argument inverse_lqd call survives: grep for `inverse_lqd\([^,)]+,\s*[^,)]+\)` (exactly two args) matches 0 in code lines
    - the page mentions `n_quantile_pts` (optional-param coverage)
    - the page mentions the quantile grid explicitly (token `quantile grid` present)
    - the shared fence runner reports every exec fence emitted the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/analyze/density-fda.md","utf8");const code=s.split("\n").filter(l=>!l.trimStart().startsWith("#")).join("\n");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const twoArg=(code.match(/inverse_lqd\([^,)]+,\s*[^,)]+\)/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,inverse_lqd_3arg:/inverse_lqd\(psi,\s*t_grid,\s*target_argvals/.test(s),no_2arg_inverse_lqd:twoArg===0,has_n_quantile_pts:/n_quantile_pts/.test(s),mentions_quantile_grid:/quantile grid/i.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>The page is missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; the 3-argument `inverse_lqd(psi, t_grid, target_argvals` signature is absent; any 2-argument inverse_lqd call survives in code; `n_quantile_pts` absent; or the phrase "quantile grid" absent.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/analyze/density-fda.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK` (nonzero exit from run_page_fences.py, or a `FENCE ... FAILED` line).</fails_when>
  </verify>
  <done>density-fda.md mirrors the UNNUMBERED mature analyze template with `## When to use` + `## See also`, the 3-argument inverse_lqd corrected, the quantile-grid output shape clarified in prose and an admonition, n_quantile_pts documented, >=3 admonitions, >=3 sentinel fences (>=1 html figure), and every exec fence emits FDARS_FENCE_OK offline under `.venv`.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — editing one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with synthetic in-fence data only. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes: When-to-use + See-also present, >=3 sentinel fences, >=3 admonitions, >=1 html figure, 3-arg inverse_lqd present, no 2-arg inverse_lqd, n_quantile_pts and "quantile grid" present.
- Fence gate: `scripts/run_page_fences.py docs/analyze/density-fda.md` exits 0.
- Whole-site `mkdocs build --strict` is intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
density-fda.md is at mature-page parity (DEPTH-02) with the inverse_lqd and quantile-grid corrections applied and every exec fence emitting FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/75-deepen-analyze-family-thin-pages/75-02-SUMMARY.md` when done.
</output>

---
phase: 74-deepen-regression-family-thin-pages
plan: 04
type: execute
wave: 4
depends_on: [74-01]
files_modified:
  - docs/regression/concurrent-regression.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-01]
estimate:
  tokens: 52000
  raw_tokens: 35000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "concurrent-regression.md has a '## When to use' decision section and a '## See also' block (DEPTH-01)"
    - "Page includes a manual out-of-sample prediction example (no predict_concurrent_regression exists) using beta_curve (p,m) + intercept"
    - "Page includes a manual bandwidth-CV example (no built-in CV for the concurrent model) and a formal warning admonition about it"
    - "Page carries >=3 admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK offline under .venv"
  artifacts:
    - docs/regression/concurrent-regression.md
  key_links:
    - "Fences import from fdars.regression and run against the installed .venv package"
    - "See also links resolve to existing docs/regression/*.md pages"
---

<objective>
Bring `docs/regression/concurrent-regression.md` (already ~50% parity) to full mature-page parity (DEPTH-01), following the tracer pattern (74-01). Add the top-level decision section, a manual out-of-sample prediction example, a manual bandwidth-CV example, the missing formal warning admonition, and the See-also block — preserving the already-correct theory/parameter/return content and the existing figure.

Purpose: A reader can decide when to use the concurrent model, predict on new data by matrix algebra on beta_curve, and select bandwidth manually.
Output: Parity-grade page with every exec fence emitting FDARS_FENCE_OK offline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. Modifies exactly ONE existing tracked doc page: `docs/regression/concurrent-regression.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bring concurrent-regression.md to full parity (decision section + manual predict/CV)</name>
  <files>docs/regression/concurrent-regression.md</files>
  <read_first>
    - docs/regression/concurrent-regression.md (preserve the correct weighted-OLS theory, kernel table, parameters table, returns dict table, the "beta_curve shape: (p, m)" note, the existing html figure, the bandwidth/kernel caveats, the "Model scope: local-at-each-t" section, and references)
    - docs/regression/scalar-on-function.md (mature template — mainly for When-to-use + See-also shape)
    - docs/regression/frechet-regression.md (the completed tracer — copy its section and admonition style)
    - src/regression_mod.rs (source of truth: concurrent_regression signature + 5-key return dict — VERIFIED in RESEARCH 3.4; note there is NO predict_concurrent_regression)
    - .planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md (Sections 2.4, 3.4, 4, 5, pitfall 5, Section 7 Fence-4 manual-prediction pattern)
  </read_first>
  <action>
This page is already ~50% there — ADD around the correct content, do not rewrite it. Deliver ALL of the following. (DEPTH-01)

VERIFY/keep the accurate surface (RESEARCH 2.4 / 3.4): concurrent_regression(predictors, response, argvals=None, bandwidth=0.2, kernel="gaussian") where predictors is a non-empty list[np.ndarray (n, m)]; kernels are "gaussian", "epanechnikov", "tricube"; return dict is exactly beta_curve (p, m), intercept (m,), fitted (n, m), residuals (n, m), argvals (m,). Keep the "(p, m)" note verbatim.

ADD these parity elements:
- A "## When to use" decision section near the top: concurrent (effect of x(t) on y(t) at the SAME t — no lag/history) vs function-on-function (global coefficient surface, cross-t effects) vs scalar-on-function (scalar response). Small table + a "Start with ..." quick rule; link function-on-function.md and scalar-on-function.md inline.
- An out-of-sample PREDICTION example fence. There is NO predict_concurrent_regression — prediction is manual: y_pred(t) = intercept(t) + sum_k beta_curve[k, :] * x_k_new(t). Use the RESEARCH Section 7 Fence-4 pattern (fit, then apply beta_curve to a new predictor curve). Print predicted shape then the sentinel.
- A manual bandwidth-CV example fence. There is NO built-in CV for the concurrent model — show a small hand-rolled loop over candidate bandwidths using held-out prediction MSE (fit on train, predict on held-out via the manual formula, pick the argmin). Keep it small/fast. Print the chosen bandwidth then the sentinel.
- A formal `!!! warning "No built-in bandwidth CV"` admonition (the page currently only mentions this in prose) stating the concurrent model has no fregre_np_cv-style helper and CV must be coded manually as shown.
- Keep references; append a "## See also" bullet list linking (RESEARCH Section 4) function-on-function.md, scalar-on-function.md, function-on-scalar.md, regression-diagnostics.md, index.md (3-5 links).

FENCES: the page already has 1 exec html figure — keep it. Add the manual-prediction fence and the manual-CV fence so there are >=3 exec fences total, each printing the sentinel, with >=1 html=1 (the existing figure satisfies this; keep it valid). Keep n<=20, m<=40, deterministic rng.

ADMONITIONS (>=3, mixing types): keep the existing "(p, m)" note; add the "No built-in bandwidth CV" warning; ensure a third (e.g. a tip on kernel/bandwidth choice, or an info on the same-t model scope). Do NOT paste an acceptance-criterion literal as admonition body text.

Do NOT run mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG. Do NOT invent a predict_concurrent_regression symbol.
  </action>
  <acceptance_criteria>
    - grep for the When-to-use heading and the See-also heading each match >= 1
    - occurrences of the sentinel token in the page >= 3
    - lines matching the admonition prefix (note|tip|warning|info) >= 3
    - at least one fence opener declares html=1
    - the page contains a manual prediction using beta_curve and intercept (a fence that indexes beta_curve to predict), and does NOT reference a predict_concurrent_regression symbol
    - the page contains a formal warning admonition whose title mentions bandwidth CV
    - the shared fence runner reports ALL EXEC FENCES EMITTED the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/regression/concurrent-regression.md","utf8");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,no_predict_symbol:!/predict_concurrent_regression/.test(s),uses_beta_curve:/beta_curve/.test(s),cv_warning:/^!!! warning[^\n]*(bandwidth|CV)/mi.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>Missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; a `predict_concurrent_regression` symbol is referenced; no `beta_curve`-based manual prediction; or no bandwidth-CV warning admonition.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/regression/concurrent-regression.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK`.</fails_when>
  </verify>
  <done>concurrent-regression.md is at full parity: `## When to use` + `## See also`, a manual out-of-sample prediction fence on beta_curve, a manual bandwidth-CV fence, a formal "No built-in bandwidth CV" warning, >=3 admonitions, >=3 sentinel fences (>=1 html), all emitting FDARS_FENCE_OK offline; no invented predict symbol.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only — one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv`. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes.
- Fence gate: `scripts/run_page_fences.py docs/regression/concurrent-regression.md` exits 0.
- Whole-site `mkdocs build --strict` deferred to Phase 79.
</verification>

<success_criteria>
concurrent-regression.md is at mature-page parity (DEPTH-01); every exec fence emits FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/74-deepen-regression-family-thin-pages/74-04-SUMMARY.md` when done.
</output>

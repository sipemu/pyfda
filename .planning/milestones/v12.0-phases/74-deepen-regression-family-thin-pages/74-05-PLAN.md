---
phase: 74-deepen-regression-family-thin-pages
plan: 05
type: execute
wave: 5
depends_on: [74-01]
files_modified:
  - docs/regression/functional-glm.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-01]
estimate:
  tokens: 52000
  raw_tokens: 35000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "functional-glm.md has a '## When to use' decision section and a '## See also' block (DEPTH-01)"
    - "GLM binding surface is verified accurate: functional_glm(data, response, family, n_comp, ...) with families gaussian/binomial/poisson/gamma and the 15-key return dict"
    - "Page adds a Gaussian-family example (reduces to linear SoF) and a parameter-selection subsection (choosing n_comp)"
    - "Page carries >=3 admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK offline under .venv"
  artifacts:
    - docs/regression/functional-glm.md
  key_links:
    - "Fences import from fdars.regression and run against the installed .venv package"
    - "See also links resolve to existing docs/regression/*.md pages"
---

<objective>
Bring `docs/regression/functional-glm.md` (the best-documented of the five) to full mature-page parity (DEPTH-01), following the tracer pattern (74-01). Add the top-level decision section, a Gaussian-family example that closes the "reduces to linear SoF" claim, a parameter-selection subsection, an extra family example, and the See-also block — preserving the already-correct two-stage theory, family/link tables, the two verbatim warnings, and the existing figure.

Purpose: A reader can decide when to use the functional GLM vs functional logistic vs linear SoF, choose n_comp, and run each family.
Output: Parity-grade page with every exec fence emitting FDARS_FENCE_OK offline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. Modifies exactly ONE existing tracked doc page: `docs/regression/functional-glm.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bring functional-glm.md to full parity (decision + gaussian/param-selection)</name>
  <files>docs/regression/functional-glm.md</files>
  <read_first>
    - docs/regression/functional-glm.md (preserve the correct two-stage FPCA->GLM theory, the family/link table, the "Gamma family uses the inverse canonical link" warning, the "AIC is not comparable to R glm() AIC" note, the full parameters + 15-key return tables, the existing binomial+poisson figure, and references — all VERIFIED accurate in RESEARCH 2.5 / 3.5)
    - docs/regression/scalar-on-function.md (mature template — mainly for When-to-use + See-also shape; also fregre_lm for the gaussian-reduces-to-linear comparison)
    - docs/regression/frechet-regression.md (the completed tracer — copy its section and admonition style)
    - src/regression_mod.rs (source of truth: functional_glm signature, family_from_str dispatch, 15-key return dict — VERIFIED in RESEARCH 3.5)
    - .planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md (Sections 2.5, 3.5, 4, 5, pitfall 6, Section 7 Fence-3 gaussian pattern)
  </read_first>
  <action>
This page is the strongest of the five — VERIFY the existing surface is accurate and ADD the missing parity elements. Deliver ALL of the following. (DEPTH-01)

VERIFY/keep the accurate surface (RESEARCH 2.5 / 3.5): functional_glm(data, response, family="gaussian", n_comp=3, scalar_covariates=None, max_iter=25, tol=1e-6); families are lowercase "gaussian", "binomial", "poisson", "gamma"; domain constraints binomial y in {0,1}, poisson y>=0, gamma y>0; the 15-key return dict (intercept, beta_t, beta_se, gamma, fitted_values, linear_predictors, ncomp, coefficients, std_errors, log_likelihood, deviance, iterations, aic, bic, family). Keep the two verbatim admonitions (gamma inverse link; AIC not comparable to R). If any existing claim disagrees with the source, correct it; otherwise leave the accurate content intact.

ADD these parity elements:
- A "## When to use" decision section near the top: functional GLM (non-Gaussian response via FPCA-projected GLM) vs the specialized functional_logistic (returns predicted classes directly, has bootstrap_ci_functional_logistic — better for pure binary classification per RESEARCH 3.5) vs linear scalar-on-function (Gaussian identity-link case). Small table + a "Start with ..." quick rule; link scalar-on-function.md and classification.md inline.
- A Gaussian-family example fence that closes the "gaussian reduces to the SoF linear model" claim: fit functional_glm(..., family="gaussian") and compare against fregre_lm on the same data (correlation of fitted values, or r_squared proxy). Use the RESEARCH Section 7 Fence-3 pattern. Print the comparison then the sentinel.
- A parameter-selection subsection: choosing n_comp for the GLM (via model_selection_ncomp, or by inspecting deviance/AIC across n_comp). Reference the existing return keys.
- An additional family example (e.g. a gamma response with y>0, or keep the existing binomial+poisson figure and add gamma) with a tip about the family domain constraint (gamma requires y>0; binomial y in {0,1}; poisson y>=0) and the lowercase-only family strings (RESEARCH pitfall 6).
- Keep references; append a "## See also" bullet list linking (RESEARCH Section 4) scalar-on-function.md, classification.md, uncertainty-quantification.md, cross-validation.md, index.md (3-5 links).

FENCES: keep the existing binomial+poisson html figure. Add the gaussian-vs-linear comparison fence and at least one more family/param fence so there are >=3 exec fences total, each printing the sentinel, with >=1 html=1 (existing figure satisfies this). Keep n<=30, m<=50, deterministic rng.

ADMONITIONS (>=3, mixing types): keep the two existing (gamma inverse-link warning; AIC-not-comparable note); add at least one more (a tip on family domain constraints / lowercase family strings, or an info on when to prefer functional_logistic). Do NOT paste an acceptance-criterion literal as admonition body text.

Do NOT run mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG.
  </action>
  <acceptance_criteria>
    - grep for the When-to-use heading and the See-also heading each match >= 1
    - occurrences of the sentinel token in the page >= 3
    - lines matching the admonition prefix (note|tip|warning|info) >= 3
    - at least one fence opener declares html=1
    - the page contains a family="gaussian" functional_glm call and references fregre_lm for the reduces-to-linear comparison
    - the page references at least three of the four lowercase family strings (gaussian, binomial, poisson, gamma)
    - the shared fence runner reports ALL EXEC FENCES EMITTED the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/regression/functional-glm.md","utf8");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const fams=["gaussian","binomial","poisson","gamma"].filter(f=>s.includes(f)).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,gaussian_call:/family\s*=\s*"gaussian"/.test(s),fregre_lm_ref:/fregre_lm/.test(s),fams_ge3:fams>=3};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html+" fams="+fams);'</automated>
    <fails_when>Missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; no `family="gaussian"` call; no `fregre_lm` comparison reference; or fewer than 3 of the four family strings present.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/regression/functional-glm.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK`.</fails_when>
  </verify>
  <done>functional-glm.md is at full parity: `## When to use` + `## See also`, a gaussian-reduces-to-linear-SoF comparison fence, a parameter-selection subsection, an additional family example, >=3 admonitions (incl. the two verbatim gamma/AIC ones), >=3 sentinel fences (>=1 html), all emitting FDARS_FENCE_OK offline; existing accurate surface preserved.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only — one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv`. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes.
- Fence gate: `scripts/run_page_fences.py docs/regression/functional-glm.md` exits 0.
- Whole-site `mkdocs build --strict` deferred to Phase 79.
</verification>

<success_criteria>
functional-glm.md is at mature-page parity (DEPTH-01); every exec fence emits FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/74-deepen-regression-family-thin-pages/74-05-SUMMARY.md` when done.
</output>

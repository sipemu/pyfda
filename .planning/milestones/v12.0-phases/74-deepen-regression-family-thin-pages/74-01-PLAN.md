---
phase: 74-deepen-regression-family-thin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regression/frechet-regression.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-01]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "frechet-regression.md has a '## When to use' decision section and a '## See also' block (DEPTH-01)"
    - "The stale frechet_anova signature/defaults/return-keys are corrected: no objects/space/d params, default n_perm=999, return keys are p_value_asymptotic + p_value_permutation (no bare p_value)"
    - "frechet_local_reg return-key doc names predicted/xout/bandwidth (not x_bar); xout is documented 2D (n_out, p)"
    - "Page carries >=3 note/tip/warning/info admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK when run offline under .venv"
  artifacts:
    - docs/regression/frechet-regression.md
  key_links:
    - "Fences import from fdars.frechet and run against the installed .venv package (no new bindings)"
    - "See also links resolve to existing docs/regression/*.md pages"
---

<objective>
TRACER: Bring `docs/regression/frechet-regression.md` to full mature-page parity end-to-end (DEPTH-01) — one page carried through every layer the phase touches (prose restructure, API-accuracy correction, admonitions, runnable fences, exec'd figure, cross-refs, offline fence verification), verified, BEFORE the other four pages follow the same proven pattern.

Purpose: Prove the whole per-page pattern on the page with the MOST confirmed API errors (six) so any pattern problem surfaces after one page instead of five.
Output: A parity-grade `frechet-regression.md` whose exec fences all emit `FDARS_FENCE_OK` offline under `.venv`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new symbols, bindings, or files. This plan modifies exactly ONE existing tracked doc page: `docs/regression/frechet-regression.md`. (The shared fence-runner `scripts/run_page_fences.py` already exists — created during planning — and is used only by the verify step, not authored here.)
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Bring frechet-regression.md to full parity end-to-end (tracer)</name>
  <files>docs/regression/frechet-regression.md</files>
  <read_first>
    - docs/regression/frechet-regression.md (page to restructure — preserve its correct SPD intro, the frechet_mean fence, and the "Return type: naked array, not a dict" warning verbatim)
    - docs/regression/scalar-on-function.md (mature template to mirror: When-to-use intro table + Quick rule, numbered method sections, note/tip/warning/info admonitions, a diagnostics/interpretation section, method-selection guide, References, then a "## See also" bullet list)
    - src/frechet_mod.rs (source of truth for every signature/default/return-key on the page)
    - .planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md (Sections 2.1, 3.1, 4, 5, 6, 7)
    - scripts/docs_fig.py (fig/render/fast helpers + brand palette for the html figure)
  </read_first>
  <action>
Restructure the page to mirror the scalar-on-function.md section skeleton, preserving correct existing prose/examples and building around them. Deliver ALL of the following. (DEPTH-01)

CORRECT these six confirmed API errors (all VERIFIED against src/frechet_mod.rs per RESEARCH 2.1 / 3.1):
1. frechet_anova signature is frechet_anova(responses, argvals, group_labels, n_perm=999, seed=42) — NO objects, NO space, NO d. It operates on a 2D density-response matrix, not generic object lists. Rewrite the API block and the group-label note accordingly.
2. frechet_anova default n_perm is 999 (not 99). Fix every mention.
3. frechet_anova return keys are exactly: statistic, p_value_asymptotic, p_value_permutation, n_perm, group_frechet_variances, pooled_frechet_variance, fn_statistic, un_statistic, group_labels. There is NO bare p_value key. Replace the p_value-plus-7-summary-keys table with the accurate 9-key list.
4. frechet_local_reg returns keys predicted, xout, bandwidth — NOT x_bar. Fix the "same 3-key dict" claim so it names the local keys and states the difference from global (global has x_bar, local has bandwidth).
5. frechet_global_reg / frechet_local_reg xout is 2D shape (n_out, p) (2D even for p=1) — correct the xout (n_out,) type; document that p=1 predictors still need a (n_out, 1) array (RESEARCH pitfall 7).
6. frechet_global_reg predictors is a scalar predictor matrix (n, p); responses is the density-response matrix (n, m). Tighten those parameter descriptions.

ADD these missing parity sections (mirroring the mature template order):
- Keep the intro paragraphs and the existing concept-diagram image line.
- A "## When to use" decision section near the top: a table (method | fdars function | key idea) covering frechet_mean, frechet_global_reg, frechet_local_reg, frechet_anova, plus a "Start with ..." quick rule contrasting Frechet regression (non-Euclidean / density response) against ordinary scalar-on-function regression (Euclidean response); link the SoF page inline.
- Numbered method sections (## 1. ... etc.) for the unconditional mean, global regression, local regression, and Frechet ANOVA, each with a runnable fence and the corrected API/return tables.
- A parameter-selection subsection for the bandwidth in frechet_local_reg (smaller bandwidth = more local / higher variance; start moderate, inspect predicted-curve smoothness).
- A result-interpretation subsection (predicted rows are density curves that stay non-negative and integrate near 1; SPD means stay symmetric positive-definite).
- The accurate note headed "Methods available in the R package but not (yet) in Python" covering ONLY the two accurate items in RESEARCH 3.1 (Wasserstein barycenter; SPD affine-invariant geodesic regression).
- Keep the existing References; append a "## See also" bullet list linking (RESEARCH Section 4) scalar-on-function.md, regression-diagnostics.md, uncertainty-quantification.md, and index.md (3-5 links).

FENCES (>=3 runnable, each ending in a line that prints the sentinel; >=1 with html=1; keep n<=20, m<=40; deterministic np.random.default_rng(seed)):
- Keep the existing frechet_mean SPD fence (already emits the sentinel).
- Add a frechet_global_reg + frechet_local_reg fence on synthetic normalized-Gaussian-bump density responses driven by a scalar predictor. argvals strictly increasing; build valid density rows and normalise with np.trapezoid; xout a 2D (n_out, 1) array. Print the corrected return keys (global: predicted/x_bar/xout; local: predicted/xout/bandwidth). (VERIFIED runnable this session — density input accepted, keys match.)
- Add ONE html=1 figure fence (from docs_fig import fig, render) plotting predicted density curves at two xout predictor values, showing how the conditional Frechet mean shifts with the predictor; print render(f) then the sentinel.
- Add a frechet_anova fence using the CORRECTED signature with contiguous int64 group_labels and n_perm=fast(999, 99), printing p_value_asymptotic and p_value_permutation.

ADMONITIONS (>=3, mixing types): keep the existing warning "Return type: naked array, not a dict"; keep/repair the group-label note; add at least one tip (bandwidth guidance) and/or info (density-response input requirement). Ensure >=3 remain after restructuring.

Do NOT run the whole-site mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG. Do NOT introduce any predict_frechet_* symbol (none exists).
  </action>
  <acceptance_criteria>
    - grep for the When-to-use heading and the See-also heading each match >= 1
    - occurrences of the sentinel token in the page >= 3
    - lines matching the admonition prefix (note|tip|warning|info) >= 3
    - at least one fence opener declares html=1
    - both corrected ANOVA p-value keys appear; a bare p_value token no longer appears outside a heading
    - no frechet_anova signature on the page contains space, objects, or a d= arg
    - the claim "same 3-key dict" (local == global) no longer appears
    - the shared fence runner reports ALL EXEC FENCES EMITTED the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/regression/frechet-regression.md","utf8");const noHash=s.split("\n").filter(l=>!l.trimStart().startsWith("#")).join("\n");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,no_stale_anova_sig:!/frechet_anova\([^)]*(space|objects|\bd\b)/.test(s),has_asymptotic:/p_value_asymptotic/.test(s),has_permutation:/p_value_permutation/.test(s),no_bare_p_value:!/\bp_value\b(?!_)/.test(noHash),local_no_same3:!/same 3-key dict/i.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>The page is missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; a stale `frechet_anova(..., space/objects/d ...)` signature remains; `p_value_asymptotic`/`p_value_permutation` absent; a bare `p_value` token survives outside a heading; or the "same 3-key dict" local-vs-global claim survives.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/regression/frechet-regression.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK` (nonzero exit from run_page_fences.py).</fails_when>
  </verify>
  <done>frechet-regression.md mirrors the mature template with `## When to use` + `## See also`, all six confirmed API errors corrected, >=3 admonitions, >=3 sentinel fences (>=1 html figure), and every exec fence emits FDARS_FENCE_OK offline under `.venv`.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — editing one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with no external data or credentials. No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner) passes: When-to-use + See-also present, >=3 sentinel fences, >=3 admonitions, >=1 html figure, all frechet_anova corrections applied, no bare `p_value`.
- Fence gate: `scripts/run_page_fences.py docs/regression/frechet-regression.md` exits 0.
- Whole-site `mkdocs build --strict` is intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
frechet-regression.md is at mature-page parity (DEPTH-01) with all six confirmed API corrections and every exec fence emitting FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/74-deepen-regression-family-thin-pages/74-01-SUMMARY.md` when done.
</output>

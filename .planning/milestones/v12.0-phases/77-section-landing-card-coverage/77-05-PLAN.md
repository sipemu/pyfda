---
phase: 77-section-landing-card-coverage
plan: 05
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - docs/assets/thumb/functional-outlier-workflow.svg
  - docs/assets/thumb/canadian-depth-centrality.svg
  - docs/assets/thumb/tolerance-vs-conformal.svg
  - docs/examples/index.md
autonomous: true
use_worktrees: false
requirements: [CARD-05, CARD-06]
estimate:
  tokens: 46000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The examples landing page (docs/examples/index.md) gains fdars-gallery cards for the three previously-uncarded existing example pages functional-outlier-workflow, canadian-depth-centrality, tolerance-vs-conformal (CARD-05)"
    - "Three new hand-authored inline-SVG thumbnails exist at docs/assets/thumb/{functional-outlier-workflow,canadian-depth-centrality,tolerance-vs-conformal}.svg (NO ex- prefix), each viewBox 0 0 320 180, fill=none, role=img + aria-label, section hue #3f51b5, STYLE_SPEC-conformant, and SVGO-idempotent under svgo@3.3.4 (CARD-06)"
  artifacts:
    - docs/assets/thumb/functional-outlier-workflow.svg
    - docs/assets/thumb/canadian-depth-centrality.svg
    - docs/assets/thumb/tolerance-vs-conformal.svg
    - docs/examples/index.md
  key_links:
    - "Each new card's src=../assets/thumb/<slug>.svg (no ex- prefix) resolves to the new thumbnail file and href=<slug>/ resolves to the existing example page (no 404)"
    - "New thumbnails are SVGO-idempotent so the consolidated Phase 79 GATE-02 determinism gate passes without later fixes"
---

<objective>
Card the EXAMPLES section (CARD-05) — author three new 320×180 inline-SVG thumbnails in the examples hue #3f51b5 (functional-outlier-workflow, canadian-depth-centrality, tolerance-vs-conformal — NO ex- prefix), verify each is SVGO-idempotent and smoke-renders, and insert three fdars-gallery cards into docs/examples/index.md using the examples-gallery BLANK-LINE card format. Follows the pipeline proven by the align tracer (77-01).

Purpose: Card the three previously-uncarded EXISTING example pages (not the three new Phase-76 flagship pages, which stay uncarded this milestone per CARD-05 scope) using the proven author→SVGO-idempotence→insert→smoke-render pattern.
Output: Three SVGO-idempotent thumbnails + three new examples cards, committed on main.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-section-landing-card-coverage/77-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new code symbols, bindings, or scripts. This plan CREATES three new tracked SVG files under `docs/assets/thumb/` (functional-outlier-workflow, canadian-depth-centrality, tolerance-vs-conformal — no ex- prefix) and EDITS one existing file, `docs/examples/index.md` (inserting three `fdars-gallery` card blocks into the appropriate sub-galleries). No other file is touched; the examples index.md and these three thumb files are disjoint from every other plan in the phase. The three NEW Phase-76 flagship example pages (frechet-density-regression, fts-forecast, phoneme-shapelets) are explicitly NOT carded here (CARD-05 scope, per CONTEXT deferred items).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author three examples thumbnails (no ex- prefix) + insert three cards (blank-line format)</name>
  <files>docs/assets/thumb/functional-outlier-workflow.svg, docs/assets/thumb/canadian-depth-centrality.svg, docs/assets/thumb/tolerance-vs-conformal.svg, docs/examples/index.md</files>
  <read_first>
    - .planning/phases/77-section-landing-card-coverage/77-RESEARCH.md (§1 house style + skeleton; §2 examples hue #3f51b5 AND the ex-prefix note — the 3 new thumbs are NOT prefixed; §3 CARD-05 the 3-item table with exact Gallery Title / Gallery Desc / Thumbnail Concept; §4 examples/index.md insertion points AND the blank-line card format exception; §5 SVGO two-pass idempotence; §6 pitfalls 1/2/3/4/6/7/8/9 — pitfall 6 is the ex-prefix trap; §7 detailed concept sketches for all three)
    - docs/assets/thumb/ex-canadian-weather.svg (examples house-style reference — exact #3f51b5 stroke, axis lines, shared-stroke <g> bundles of curves at descending opacity; canadian-depth-centrality models the fan-of-curves depth ordering on this)
    - docs/assets/thumb/tolerance-bands.svg (reference for the shaded-band style needed by tolerance-vs-conformal — but recolor to #3f51b5)
    - docs/examples/index.md (MULTIPLE fdars-gallery blocks, one per sub-heading; this section's cards USE BLANK LINES between `<a>`/`<img>`/`<div>`/`</a>` — see the Classification / Seasonal blocks lines 87-119. Insertion targets per RESEARCH §4: functional-outlier-workflow → a NEW `## Depth &amp; outlier analysis` section+gallery inserted after the Classification section and before `## Seasonal &amp; regional analysis`; canadian-depth-centrality → appended after canadian-precipitation in the existing "Seasonal &amp; regional analysis" gallery; tolerance-vs-conformal → a NEW `## Tolerance &amp; uncertainty` section+gallery inserted after "Process monitoring" and before the `## What each example shows` table)
    - docs/assets/diagrams/STYLE_SPEC.md (decorative-thumbnail rule: img alt="" + aria-hidden="true"; SVG role="img" + aria-label; no title/desc/aria-labelledby on thumbnails)
  </read_first>
  <action>
Author three new minimal hand-authored inline-SVG thumbnails and insert their gallery cards into `docs/examples/index.md`. Author fresh minimal line-art per the RESEARCH §7 concept sketches — do NOT down-scale the full concept diagrams. (CARD-05, CARD-06)

CRITICAL NAMING (RESEARCH §2 + pitfall 6): the three thumb files are named WITHOUT the `ex-` prefix — `functional-outlier-workflow.svg`, `canadian-depth-centrality.svg`, `tolerance-vs-conformal.svg` — because the target page slugs (verified against mkdocs.yml) carry no prefix. Card src is `../assets/thumb/<slug>.svg` and href is `<slug>/` (matching the page filename). Using an `ex-` prefix would 404.

THUMBNAIL HOUSE STYLE (RESEARCH §1 — match the examples corpus exactly):
- Root: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 180" fill="none" role="img" aria-label="<3-6 word description>">`. viewBox ALWAYS `0 0 320 180` (RESEARCH pitfall 1).
- Section hue `#3f51b5` (indigo) on every stroke/fill (RESEARCH §2).
- L-shaped axis: x-axis `<line x1="30" y1="150" x2="298" y2="150" stroke="#3f51b5" stroke-opacity=".35" stroke-width="1.4"/>`; y-axis `<line x1="30" y1="150" x2="30" y2="28" stroke="#3f51b5" stroke-opacity=".35" stroke-width="1.4"/>` (tolerance-vs-conformal may use a vertical divider at x=164 in place of a single y-axis).
- 2-4 paths (a bundle thumb may use a shared-stroke <g>); primary `stroke-width="3" stroke-linecap="round"`; faint/secondary reduced `stroke-opacity`; dashed via `stroke-dasharray`. Coordinates within x∈[30,298], y∈[28,150] (RESEARCH pitfall 9).
- NO <title>/<desc>/aria-labelledby/<style>/xmlns:xlink/path-id (pitfalls 2/3). A shared-stroke <g> is allowed for bundles. If a <linearGradient> is used, unique 2-char id per file (`fo`,`cd`,`tc`) (RESEARCH pitfall 4). OMIT text labels. Target 700–1100 bytes; aria-label a short 3-6 word description.

THUMBNAIL 1 — `functional-outlier-workflow.svg` (RESEARCH §3 row 19 + §7): a tight bundle of ~5 parallel curves at medium opacity (shared-stroke <g>), with one outlier curving sharply upward (magnitude outlier, elevated level) and one outlier of a different oscillating shape; each outlier ends in a small open `<circle>` (stroke only). aria-label "Functional outlier workflow". Hue `#3f51b5`.

THUMBNAIL 2 — `canadian-depth-centrality.svg` (RESEARCH §3 row 20 + §7): five curves of decreasing opacity from bold (most central / median) to very faint (most peripheral), arranged in a fan resembling nested contours (depth rank implied by opacity layering). Model on ex-canadian-weather.svg's layered bundles. aria-label "Depth centrality ranking". Hue `#3f51b5`.

THUMBNAIL 3 — `tolerance-vs-conformal.svg` (RESEARCH §3 row 21 + §7): two shaded bands side by side divided by a vertical line at x=164 — left band wider with solid boundary lines (FPCA-based tolerance), right band narrower with dashed boundary lines (distribution-free conformal); each band has a median curve inside it. Model band fill on tolerance-bands.svg (recolored to #3f51b5). aria-label "Tolerance vs conformal bands". Hue `#3f51b5`.

SVGO IDEMPOTENCE (RESEARCH §5 — CARD-06, required before insertion): for EACH new thumbnail, from repo root:
`first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/thumb/<slug>.svg --output -)`; `second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)`; `diff <(printf '%s' "$first") <(printf '%s' "$second")` — MUST be empty. Never overwrite the committed hand-authored file with SVGO output.

SMOKE RENDER (RESEARCH §5): `rsvg-convert -w 320 -h 180 docs/assets/thumb/<slug>.svg -o /tmp/<slug>.png` — must exit 0 for each. rsvg-convert is at /usr/bin/rsvg-convert.

CARD INSERTION into `docs/examples/index.md` (RESEARCH §4 — examples cards USE BLANK LINES between `<a>`/`<img>`/`<div>`/`</a>`, matching the sibling examples cards; href trailing slash; src `../assets/thumb/<slug>.svg` with NO ex- prefix). Three insertions:

1. `canadian-depth-centrality` — append into the EXISTING "Seasonal &amp; regional analysis" gallery, immediately after the canadian-precipitation card's closing `</a>` (before that gallery's closing `</div>`):
```
<a class="fdars-gallery-item" href="canadian-depth-centrality/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/canadian-depth-centrality.svg" alt="">
<div class="fdars-gallery-title">Canadian depth &amp; centrality</div>
<div class="fdars-gallery-desc">Rank 35 Canadian weather curves from most central to most atypical.</div>
</a>
```
2. `functional-outlier-workflow` — insert a NEW section heading + gallery block AFTER the Classification section (after its closing `</div>`) and BEFORE `## Seasonal &amp; regional analysis`:
```
## Depth &amp; outlier analysis

<div class="fdars-gallery fdars-sec-examples">

<a class="fdars-gallery-item" href="functional-outlier-workflow/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/functional-outlier-workflow.svg" alt="">
<div class="fdars-gallery-title">Functional outlier workflow</div>
<div class="fdars-gallery-desc">Combine magnitude and shape scores to flag atypical curves end-to-end.</div>
</a>

</div>
```
3. `tolerance-vs-conformal` — insert a NEW section heading + gallery block AFTER the "Process monitoring" section (after its closing `</div>`) and BEFORE the `## What each example shows` table:
```
## Tolerance &amp; uncertainty

<div class="fdars-gallery fdars-sec-examples">

<a class="fdars-gallery-item" href="tolerance-vs-conformal/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/tolerance-vs-conformal.svg" alt="">
<div class="fdars-gallery-title">Tolerance vs conformal</div>
<div class="fdars-gallery-desc">Two band philosophies — FPCA-based vs distribution-free — compared side by side.</div>
</a>

</div>
```
Do NOT reorder existing cards or sections. Do NOT add cards for the three NEW Phase-76 flagship pages (frechet-density-regression, fts-forecast, phoneme-shapelets) — they are out of CARD-05 scope. Do NOT touch any other section index.md. Do NOT run the whole-site `mkdocs build --strict` (deferred to Phase 79).
  </action>
  <acceptance_criteria>
    - the three files docs/assets/thumb/{functional-outlier-workflow,canadian-depth-centrality,tolerance-vs-conformal}.svg all exist AND no ex-prefixed variant (docs/assets/thumb/ex-<slug>.svg) is created
    - each new thumbnail contains `viewBox="0 0 320 180"` (grep matches 1 per file)
    - each new thumbnail contains the examples hue `#3f51b5` (grep matches >= 1 per file) and no other section hex (#fd7e14/#198754/#dc3545/#6f42c1 all match 0)
    - each new thumbnail carries `role="img"` and an `aria-label="` attribute
    - no new thumbnail contains `<title`, `<desc`, `aria-labelledby`, or `<style` (grep matches 0 each)
    - each new thumbnail file is under ~1300 bytes
    - docs/examples/index.md contains `href="<slug>/"` and `src="../assets/thumb/<slug>.svg"` for each of the three slugs (with NO ex- prefix in the src)
    - docs/examples/index.md does NOT gain any card for frechet-density-regression, fts-forecast, or phoneme-shapelets (grep for those three slugs inside a `fdars-gallery-item` href matches 0)
    - for each new thumbnail the SVGO two-pass idempotence diff is empty (pass1 == pass2)
    - each new thumbnail smoke-renders via rsvg-convert with exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const idx=fs.readFileSync("docs/examples/index.md","utf8");const slugs=["functional-outlier-workflow","canadian-depth-centrality","tolerance-vs-conformal"];const bad=[];for(const slug of slugs){const f="docs/assets/thumb/"+slug+".svg";if(!fs.existsSync(f)){bad.push(slug+":missing");continue;}if(fs.existsSync("docs/assets/thumb/ex-"+slug+".svg")){bad.push(slug+":ex-prefix-created");}const s=fs.readFileSync(f,"utf8");const sz=Buffer.byteLength(s);const checks={viewBox:/viewBox="0 0 320 180"/.test(s),hue:/#3f51b5/i.test(s),no_other_hue:!/#fd7e14|#198754|#dc3545|#6f42c1/i.test(s),role:/role="img"/.test(s),aria:/aria-label="/.test(s),no_title:!/<title/.test(s),no_desc:!/<desc/.test(s),no_labelledby:!/aria-labelledby/.test(s),no_style:!/<style/.test(s),size:sz<1300,card_href:new RegExp("href=\""+slug+"/\"").test(idx),card_src:new RegExp("src=\"../assets/thumb/"+slug+".svg\"").test(idx)};for(const [k,v] of Object.entries(checks))if(!v)bad.push(slug+":"+k);}const p76=["frechet-density-regression","fts-forecast","phoneme-shapelets"];for(const s of p76){if(new RegExp("fdars-gallery-item\" href=\""+s+"/\"").test(idx))bad.push("phase76-carded:"+s);}if(bad.length){console.error("FAIL: "+bad.join(", "));process.exit(1);}console.log("STRUCT OK examples 3 thumbnails + 3 cards");'</automated>
    <fails_when>Any of the three thumbnail files is missing or an ex-prefixed variant was created; a thumbnail lacks viewBox="0 0 320 180"; a thumbnail lacks the #3f51b5 examples hue or carries another section's hex; a thumbnail lacks role="img" or aria-label; a thumbnail contains <title>/<desc>/aria-labelledby/<style>; a thumbnail exceeds ~1300 bytes; docs/examples/index.md is missing any card's href or src; or a Phase-76 flagship page (frechet-density-regression/fts-forecast/phoneme-shapelets) was carded.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && rc=0; for slug in functional-outlier-workflow canadian-depth-centrality tolerance-vs-conformal; do f="docs/assets/thumb/$slug.svg"; first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); if ! diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null; then echo "NON-IDEMPOTENT: $slug"; rc=1; fi; rsvg-convert -w 320 -h 180 "$f" -o /dev/null || { echo "RENDER FAIL: $slug"; rc=1; }; done; [ $rc -eq 0 ] && echo "SVGO idempotent + render OK"; exit $rc</automated>
    <fails_when>For any new thumbnail the two SVGO passes differ (non-idempotent — would fail the Phase 79 GATE-02 determinism gate), or rsvg-convert returns non-zero (the SVG does not parse/render).</fails_when>
  </verify>
  <done>docs/assets/thumb/{functional-outlier-workflow,canadian-depth-centrality,tolerance-vs-conformal}.svg exist as fresh minimal 320×180 #3f51b5 line-art (no ex- prefix, role="img" + aria-label, no title/desc/style, <1300 bytes each), all three SVGO-idempotent and rsvg-convert-clean, and docs/examples/index.md carries their three fdars-gallery cards (blank-line format: canadian-depth-centrality appended to Seasonal & regional analysis; functional-outlier-workflow in a new Depth & outlier analysis section; tolerance-vs-conformal in a new Tolerance & uncertainty section) — the three Phase-76 flagship pages remain uncarded per CARD-05 scope.</done>
  <reversibility rating="reversible">Docs-only: three new SVG files + additive card/section insertions into one index.md; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — creating three hand-authored static SVG assets and inserting three markdown card blocks (two in new sub-section headings) into one section index. No code, no network surface, no user-input handling. The SVGs are decorative line-art with no `<script>`, no `foreignObject`, no external references — no new attack surface. No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner): all three thumbnail files exist (no ex- prefix variant), each has viewBox="0 0 320 180", the #3f51b5 examples hue and no other section hex, role="img" + aria-label, no title/desc/aria-labelledby/style, <1300 bytes; docs/examples/index.md carries all three cards' href + src and no Phase-76 flagship page is carded.
- Determinism gate (shell loop): each thumbnail's SVGO two-pass diff is empty (idempotent) and rsvg-convert renders it without error.
- Whole-site `mkdocs build --strict` and the consolidated SVGO/determinism GATE-02 are intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
CARD-05 + CARD-06 (examples portion) delivered: the examples landing page carries three new SVGO-idempotent, STYLE_SPEC-conformant, decorative-accessible #3f51b5 thumbnails for the three previously-uncarded existing example pages (the three new Phase-76 flagship pages remain uncarded per scope).
</success_criteria>

<output>
Create `.planning/phases/77-section-landing-card-coverage/77-05-SUMMARY.md` when done.
</output>

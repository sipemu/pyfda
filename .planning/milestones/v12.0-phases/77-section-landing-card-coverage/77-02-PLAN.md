---
phase: 77-section-landing-card-coverage
plan: 02
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - docs/assets/thumb/pace-fpca.svg
  - docs/assets/thumb/interpolation.svg
  - docs/assets/thumb/imputation.svg
  - docs/represent/index.md
autonomous: true
use_worktrees: false
requirements: [CARD-02, CARD-06]
estimate:
  tokens: 46000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The represent landing gallery (docs/represent/index.md) is at 100% card coverage: it gains cards for pace-fpca, interpolation, imputation (CARD-02)"
    - "Three new hand-authored inline-SVG thumbnails exist at docs/assets/thumb/{pace-fpca,interpolation,imputation}.svg, each viewBox 0 0 320 180, fill=none, role=img + aria-label, section hue #198754, STYLE_SPEC-conformant, and SVGO-idempotent under svgo@3.3.4 (CARD-06)"
  artifacts:
    - docs/assets/thumb/pace-fpca.svg
    - docs/assets/thumb/interpolation.svg
    - docs/assets/thumb/imputation.svg
    - docs/represent/index.md
  key_links:
    - "Each new card's src=../assets/thumb/<slug>.svg resolves to the new thumbnail file and href=<slug>/ resolves to the existing represent method page (no 404)"
    - "New thumbnails are SVGO-idempotent so the consolidated Phase 79 GATE-02 determinism gate passes without later fixes"
---

<objective>
Card the REPRESENT section (CARD-02) — author three new 320×180 inline-SVG thumbnails in the represent hue #198754 (pace-fpca, interpolation, imputation), verify each is SVGO-idempotent and smoke-renders, and insert all three fdars-gallery cards into docs/represent/index.md so the represent gallery reaches 100% coverage. Follows the pipeline proven by the align tracer (77-01).

Purpose: Complete represent-section card coverage using the same author→SVGO-idempotence→insert→smoke-render pattern the align tracer established.
Output: Three SVGO-idempotent thumbnails + a 100%-covered represent gallery, committed on main.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-section-landing-card-coverage/77-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new code symbols, bindings, or scripts. This plan CREATES three new tracked SVG files — `docs/assets/thumb/pace-fpca.svg`, `docs/assets/thumb/interpolation.svg`, `docs/assets/thumb/imputation.svg` — and EDITS one existing file, `docs/represent/index.md` (inserting three `fdars-gallery` card blocks). No other file is touched; the represent index.md and these three thumb files are disjoint from every other plan in the phase.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author represent thumbnails (pace-fpca, interpolation, imputation) + insert three cards</name>
  <files>docs/assets/thumb/pace-fpca.svg, docs/assets/thumb/interpolation.svg, docs/assets/thumb/imputation.svg, docs/represent/index.md</files>
  <read_first>
    - .planning/phases/77-section-landing-card-coverage/77-RESEARCH.md (§1 house style + skeleton; §2 represent hue #198754; §3 CARD-02 the 3-item table with exact Gallery Title / Gallery Desc / Thumbnail Concept; §4 represent/index.md insertion point — all 3 cards after basis-representation and before andrews-transformation; §5 SVGO two-pass idempotence; §6 pitfalls 1/2/3/4/7/8/9; §7 detailed concept sketches for pace-fpca, interpolation, imputation)
    - docs/assets/thumb/fpca.svg (represent house-style reference — exact #198754 stroke, axis lines, dashed secondary paths stroke-dasharray="5 5", a <linearGradient id="fp"> area fill closed with `L298 150 L30 150 Z`, primary path stroke-width="3" stroke-linecap="round"; ~1172 bytes — note it also has an inline <text> label PC1, which the 3 new thumbs should OMIT)
    - docs/assets/thumb/basis-representation.svg (second represent reference for the plain line-art style)
    - docs/represent/index.md (the fdars-gallery block; existing cards fpca/elastic-fpca/basis-representation/andrews-transformation/depth-functions/streaming-depth/distance-metrics; insertion anchor is the `<a ... href="andrews-transformation/">` line — NO blank lines between tags in this section)
    - docs/assets/diagrams/STYLE_SPEC.md (decorative-thumbnail rule: img alt="" + aria-hidden="true"; SVG role="img" + aria-label; no title/desc/aria-labelledby on thumbnails)
  </read_first>
  <action>
Author three new minimal hand-authored inline-SVG thumbnails and insert their gallery cards into `docs/represent/index.md`, bringing the represent gallery to 100% coverage. Author fresh minimal line-art per the RESEARCH §7 concept sketches — do NOT down-scale the full concept diagrams. (CARD-02, CARD-06)

THUMBNAIL HOUSE STYLE (RESEARCH §1 — match the represent corpus exactly):
- Root: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 180" fill="none" role="img" aria-label="<3-6 word description>">`. viewBox ALWAYS `0 0 320 180` (RESEARCH pitfall 1).
- Section hue `#198754` (green) on every stroke/fill (RESEARCH §2).
- L-shaped axis: x-axis `<line x1="30" y1="150" x2="298" y2="150" stroke="#198754" stroke-opacity=".35" stroke-width="1.4"/>`; y-axis `<line x1="30" y1="150" x2="30" y2="28" stroke="#198754" stroke-opacity=".35" stroke-width="1.4"/>`.
- 2-4 paths; primary `stroke-width="3" stroke-linecap="round"`; faint/secondary reduced `stroke-opacity`; dashed via `stroke-dasharray`. Coordinates within x∈[30,298], y∈[28,150] (RESEARCH pitfall 9).
- NO <title>/<desc>/aria-labelledby/<style>/xmlns:xlink/path-id/stray <g> (RESEARCH pitfalls 2/3). If a <linearGradient> is used, unique 2-char id per file (`pf` for pace-fpca, `in` for interpolation, `im` for imputation) (RESEARCH pitfall 4). OMIT text labels (RESEARCH pitfall 3 / §1). Target 700–1100 bytes; aria-label a short 3-6 word description.

THUMBNAIL 1 — `docs/assets/thumb/pace-fpca.svg` (RESEARCH §3 row 3 + §7 pace-fpca): 5–8 scattered `<circle>` dots at irregular x positions and varying y (sparse observations), with a smooth bold cubic-Bézier curve (primary, stroke-width 3) passing through their region (the PACE-fitted smooth recovered from sparse data). aria-label "PACE sparse FPCA". Hue `#198754`.

THUMBNAIL 2 — `docs/assets/thumb/interpolation.svg` (RESEARCH §3 row 4 + §7 interpolation): a coarse source grid rendered as 4–5 short vertical tick marks with small `<circle>` tops, plus a smooth bold cubic-Bézier curve (primary) through those points, plus a second denser set of short tick marks along the x-axis showing the resampled output grid. aria-label "Spline interpolation". Hue `#198754`.

THUMBNAIL 3 — `docs/assets/thumb/imputation.svg` (RESEARCH §3 row 5 + §7 imputation): a curve that breaks at a middle section (a ~50px gap drawn as a dashed segment `stroke-dasharray`), flanked by two solid bold segments (the intact parts). The dashed segment is the imputed region. aria-label "Missing value imputation". Hue `#198754`.

SVGO IDEMPOTENCE (RESEARCH §5 — CARD-06, required before insertion): for EACH new thumbnail, from repo root:
`first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/thumb/<slug>.svg --output -)`; `second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)`; `diff <(printf '%s' "$first") <(printf '%s' "$second")` — MUST be empty. Never overwrite the committed hand-authored file with SVGO output.

SMOKE RENDER (RESEARCH §5): `rsvg-convert -w 320 -h 180 docs/assets/thumb/<slug>.svg -o /tmp/<slug>.png` — must exit 0 for each. rsvg-convert is at /usr/bin/rsvg-convert.

CARD INSERTION into `docs/represent/index.md` (RESEARCH §4 — exact template, NO blank lines between tags, href trailing slash, src `../assets/thumb/<slug>.svg`): insert all three card blocks as a contiguous group immediately BEFORE the existing `<a class="fdars-gallery-item" href="andrews-transformation/">` line (i.e. right after the basis-representation card's closing `</a>`), in the order pace-fpca → interpolation → imputation:
```
<a class="fdars-gallery-item" href="pace-fpca/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/pace-fpca.svg" alt="">
<div class="fdars-gallery-title">PACE FPCA</div>
<div class="fdars-gallery-desc">FPCA via conditional expectation for sparse, irregularly sampled curves.</div>
</a>
<a class="fdars-gallery-item" href="interpolation/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/interpolation.svg" alt="">
<div class="fdars-gallery-title">Spline Interpolation</div>
<div class="fdars-gallery-desc">Resample functional curves onto any grid with B-spline fits.</div>
</a>
<a class="fdars-gallery-item" href="imputation/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/imputation.svg" alt="">
<div class="fdars-gallery-title">Missing Value Imputation</div>
<div class="fdars-gallery-desc">Fill NaN gaps in functional curves with spline or mean strategies.</div>
</a>
```
Do NOT reorder existing cards. Do NOT touch any other section index.md. Do NOT run the whole-site `mkdocs build --strict` (deferred to Phase 79).
  </action>
  <acceptance_criteria>
    - the files docs/assets/thumb/pace-fpca.svg, docs/assets/thumb/interpolation.svg, docs/assets/thumb/imputation.svg all exist
    - each new thumbnail contains `viewBox="0 0 320 180"` (grep matches 1 per file)
    - each new thumbnail contains the represent hue `#198754` (grep matches >= 1 per file) and no other section hex (#fd7e14/#dc3545/#6f42c1/#3f51b5 all match 0)
    - each new thumbnail carries `role="img"` and an `aria-label="` attribute
    - no new thumbnail contains `<title`, `<desc`, `aria-labelledby`, or `<style` (grep matches 0 each)
    - each new thumbnail file is under ~1200 bytes
    - docs/represent/index.md contains `href="<slug>/"` and `src="../assets/thumb/<slug>.svg"` for each of pace-fpca, interpolation, imputation
    - for each new thumbnail the SVGO two-pass idempotence diff is empty (pass1 == pass2)
    - each new thumbnail smoke-renders via rsvg-convert with exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const idx=fs.readFileSync("docs/represent/index.md","utf8");const slugs=["pace-fpca","interpolation","imputation"];const bad=[];for(const slug of slugs){const f="docs/assets/thumb/"+slug+".svg";if(!fs.existsSync(f)){bad.push(slug+":missing");continue;}const s=fs.readFileSync(f,"utf8");const sz=Buffer.byteLength(s);const checks={viewBox:/viewBox="0 0 320 180"/.test(s),hue:/#198754/i.test(s),no_other_hue:!/#fd7e14|#dc3545|#6f42c1|#3f51b5/i.test(s),role:/role="img"/.test(s),aria:/aria-label="/.test(s),no_title:!/<title/.test(s),no_desc:!/<desc/.test(s),no_labelledby:!/aria-labelledby/.test(s),no_style:!/<style/.test(s),size:sz<1200,card_href:new RegExp("href=\""+slug+"/\"").test(idx),card_src:new RegExp("src=\"../assets/thumb/"+slug+".svg\"").test(idx)};for(const [k,v] of Object.entries(checks))if(!v)bad.push(slug+":"+k);}if(bad.length){console.error("FAIL: "+bad.join(", "));process.exit(1);}console.log("STRUCT OK represent 3 thumbnails + 3 cards");'</automated>
    <fails_when>Any of the three thumbnail files is missing; a thumbnail lacks viewBox="0 0 320 180"; a thumbnail lacks the #198754 represent hue or carries another section's hex; a thumbnail lacks role="img" or aria-label; a thumbnail contains <title>/<desc>/aria-labelledby/<style>; a thumbnail exceeds ~1200 bytes; or docs/represent/index.md is missing any card's href or src.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && rc=0; for slug in pace-fpca interpolation imputation; do f="docs/assets/thumb/$slug.svg"; first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); if ! diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null; then echo "NON-IDEMPOTENT: $slug"; rc=1; fi; rsvg-convert -w 320 -h 180 "$f" -o /dev/null || { echo "RENDER FAIL: $slug"; rc=1; }; done; [ $rc -eq 0 ] && echo "SVGO idempotent + render OK"; exit $rc</automated>
    <fails_when>For any new thumbnail the two SVGO passes differ (non-idempotent — would fail the Phase 79 GATE-02 determinism gate), or rsvg-convert returns non-zero (the SVG does not parse/render).</fails_when>
  </verify>
  <done>docs/assets/thumb/{pace-fpca,interpolation,imputation}.svg exist as fresh minimal 320×180 #198754 line-art (role="img" + aria-label, no title/desc/style, <1200 bytes each), all three SVGO-idempotent and rsvg-convert-clean, and docs/represent/index.md carries their three fdars-gallery cards inserted after basis-representation (before andrews-transformation) so the represent gallery is at 100% coverage.</done>
  <reversibility rating="reversible">Docs-only: three new SVG files + additive card insertions into one index.md; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — creating three hand-authored static SVG assets and inserting three markdown card blocks into one section index. No code, no network surface, no user-input handling. The SVGs are decorative line-art with no `<script>`, no `foreignObject`, no external references — no new attack surface. No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner): all three thumbnail files exist, each has viewBox="0 0 320 180", the #198754 represent hue and no other section hex, role="img" + aria-label, no title/desc/aria-labelledby/style, <1200 bytes; docs/represent/index.md carries all three cards' href + src.
- Determinism gate (shell loop): each thumbnail's SVGO two-pass diff is empty (idempotent) and rsvg-convert renders it without error.
- Whole-site `mkdocs build --strict` and the consolidated SVGO/determinism GATE-02 are intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
CARD-02 + CARD-06 (represent portion) delivered: the represent landing gallery is at 100% card coverage with three new SVGO-idempotent, STYLE_SPEC-conformant, decorative-accessible #198754 thumbnails.
</success_criteria>

<output>
Create `.planning/phases/77-section-landing-card-coverage/77-02-SUMMARY.md` when done.
</output>

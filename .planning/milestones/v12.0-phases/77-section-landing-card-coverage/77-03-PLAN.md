---
phase: 77-section-landing-card-coverage
plan: 03
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - docs/assets/thumb/concurrent-regression.svg
  - docs/assets/thumb/functional-glm.svg
  - docs/assets/thumb/function-on-function.svg
  - docs/assets/thumb/additive-sof.svg
  - docs/assets/thumb/frechet-regression.svg
  - docs/regression/index.md
autonomous: true
use_worktrees: false
requirements: [CARD-03, CARD-06]
estimate:
  tokens: 54000
  raw_tokens: 36000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The regression landing gallery (docs/regression/index.md) gains cards for concurrent-regression, functional-glm, function-on-function, additive-sof, frechet-regression so every focus-section regression page is carded (CARD-03)"
    - "Five new hand-authored inline-SVG thumbnails exist at docs/assets/thumb/{concurrent-regression,functional-glm,function-on-function,additive-sof,frechet-regression}.svg, each viewBox 0 0 320 180, fill=none, role=img + aria-label, section hue #dc3545, STYLE_SPEC-conformant, and SVGO-idempotent under svgo@3.3.4 (CARD-06)"
  artifacts:
    - docs/assets/thumb/concurrent-regression.svg
    - docs/assets/thumb/functional-glm.svg
    - docs/assets/thumb/function-on-function.svg
    - docs/assets/thumb/additive-sof.svg
    - docs/assets/thumb/frechet-regression.svg
    - docs/regression/index.md
  key_links:
    - "Each new card's src=../assets/thumb/<slug>.svg resolves to the new thumbnail file and href=<slug>/ resolves to the existing regression method page (no 404)"
    - "New thumbnails are SVGO-idempotent so the consolidated Phase 79 GATE-02 determinism gate passes without later fixes"
---

<objective>
Card the REGRESSION section (CARD-03) — author five new 320×180 inline-SVG thumbnails in the regression hue #dc3545 (concurrent-regression, functional-glm, function-on-function, additive-sof, frechet-regression), verify each is SVGO-idempotent and smoke-renders, and insert all five fdars-gallery cards into docs/regression/index.md. Follows the pipeline proven by the align tracer (77-01).

Purpose: Complete regression-section card coverage for the five v11.0-era regression methods using the proven author→SVGO-idempotence→insert→smoke-render pattern.
Output: Five SVGO-idempotent thumbnails + five new regression cards, committed on main.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-section-landing-card-coverage/77-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new code symbols, bindings, or scripts. This plan CREATES five new tracked SVG files under `docs/assets/thumb/` (concurrent-regression, functional-glm, function-on-function, additive-sof, frechet-regression) and EDITS one existing file, `docs/regression/index.md` (inserting five `fdars-gallery` card blocks). No other file is touched; the regression index.md and these five thumb files are disjoint from every other plan in the phase.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author five regression thumbnails + insert five cards</name>
  <files>docs/assets/thumb/concurrent-regression.svg, docs/assets/thumb/functional-glm.svg, docs/assets/thumb/function-on-function.svg, docs/assets/thumb/additive-sof.svg, docs/assets/thumb/frechet-regression.svg, docs/regression/index.md</files>
  <read_first>
    - .planning/phases/77-section-landing-card-coverage/77-RESEARCH.md (§1 house style + skeleton; §2 regression hue #dc3545; §3 CARD-03 the 5-item table with exact Gallery Title / Gallery Desc / Thumbnail Concept; §4 regression/index.md insertion point — all 5 cards after function-on-scalar and before classification; §5 SVGO two-pass idempotence; §6 pitfalls 1/2/3/4/7/8/9; §7 detailed concept sketches for all five)
    - docs/assets/thumb/scalar-on-function.svg (regression house-style reference — exact #dc3545 stroke, axis lines, a <linearGradient id="sof"> area fill, primary path stroke-width="3" stroke-linecap="round", a <circle> accent, a dashed accent path; note it has an inline <text> "y" label — the 5 new thumbs should OMIT text labels)
    - docs/assets/thumb/function-on-scalar.svg (second regression reference)
    - docs/regression/index.md (the fdars-gallery block; existing cards scalar-on-function/function-on-scalar/classification/...; insertion anchor is the `<a ... href="classification/">` line — NO blank lines between tags in this section)
    - docs/assets/diagrams/STYLE_SPEC.md (decorative-thumbnail rule: img alt="" + aria-hidden="true"; SVG role="img" + aria-label; no title/desc/aria-labelledby on thumbnails)
  </read_first>
  <action>
Author five new minimal hand-authored inline-SVG thumbnails and insert their gallery cards into `docs/regression/index.md`. Author fresh minimal line-art per the RESEARCH §7 concept sketches — do NOT down-scale the full concept diagrams. (CARD-03, CARD-06)

THUMBNAIL HOUSE STYLE (RESEARCH §1 — match the regression corpus exactly):
- Root: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 180" fill="none" role="img" aria-label="<3-6 word description>">`. viewBox ALWAYS `0 0 320 180` (RESEARCH pitfall 1).
- Section hue `#dc3545` (red) on every stroke/fill (RESEARCH §2).
- L-shaped axis: x-axis `<line x1="30" y1="150" x2="298" y2="150" stroke="#dc3545" stroke-opacity=".35" stroke-width="1.4"/>`; y-axis `<line x1="30" y1="150" x2="30" y2="28" stroke="#dc3545" stroke-opacity=".35" stroke-width="1.4"/>` (adapt/omit an axis where the concept is a surface/geodesic rather than a curve-over-t plot, e.g. frechet-regression — but keep at least one reference line).
- 2-4 paths; primary `stroke-width="3" stroke-linecap="round"`; faint/secondary reduced `stroke-opacity`; dashed via `stroke-dasharray`. Coordinates within x∈[30,298], y∈[28,150] (RESEARCH pitfall 9).
- NO <title>/<desc>/aria-labelledby/<style>/xmlns:xlink/path-id/stray <g> (pitfalls 2/3). If a <linearGradient> is used, unique 2-char id per file (`cr`,`gl`,`ff`,`as`,`fr`) (RESEARCH pitfall 4). OMIT text labels. Target 700–1100 bytes; aria-label a short 3-6 word description.

THUMBNAIL 1 — `docs/assets/thumb/concurrent-regression.svg` (RESEARCH §3 row 6 + §7 concurrent-regression): a wavy coefficient function β(t) as the hero path (bold, stroke-width 3) that starts positive, crosses zero, goes negative, then rises again; a faint horizontal zero-reference line at mid-y (y≈89, reduced opacity). aria-label "Concurrent regression coefficient". Hue `#dc3545`.

THUMBNAIL 2 — `docs/assets/thumb/functional-glm.svg` (RESEARCH §3 row 7 + §7 functional-glm): a two-panel sketch — a functional curve in the left ~40% of the width → a single short arrow (faint path + small filled arrowhead with Z fill) → a scatter of binary-outcome `<circle>` dots at two y-levels in the right ~40% with a bold logistic S-curve (primary) through them. aria-label "Functional GLM". Hue `#dc3545`.

THUMBNAIL 3 — `docs/assets/thumb/function-on-function.svg` (RESEARCH §3 row 8 + §7 function-on-function): a square-ish region filling most of the viewBox with 3–4 faint nested partial-ellipse contour paths (the bivariate coefficient surface β(s,t)); an s-axis line and a t-axis line on two sides. aria-label "Function-on-function surface". Hue `#dc3545`.

THUMBNAIL 4 — `docs/assets/thumb/additive-sof.svg` (RESEARCH §3 row 9 + §7 additive-sof): a positive arch curve f₁(t) and a negative trough curve f₂(t) (both medium opacity), a bold horizontal baseline, and a scalar-response `<circle>` dot at the right edge. Optionally a small "+" via a short pair of crossing lines between the two component curves (NOT a <text> element unless unavoidable). aria-label "Additive scalar-on-function". Hue `#dc3545`.

THUMBNAIL 5 — `docs/assets/thumb/frechet-regression.svg` (RESEARCH §3 row 10 + §7 frechet-regression): a stylised geodesic arc (a partial-circle bold path) representing a geodesic in metric space; two small filled `<circle>` observation points along/near the arc and a third open `<circle>` (stroke only) for the conditional Fréchet mean. A faint reference line. aria-label "Fréchet metric-space regression". Hue `#dc3545`.

SVGO IDEMPOTENCE (RESEARCH §5 — CARD-06, required before insertion): for EACH new thumbnail, from repo root:
`first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/thumb/<slug>.svg --output -)`; `second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)`; `diff <(printf '%s' "$first") <(printf '%s' "$second")` — MUST be empty. Never overwrite the committed hand-authored file with SVGO output.

SMOKE RENDER (RESEARCH §5): `rsvg-convert -w 320 -h 180 docs/assets/thumb/<slug>.svg -o /tmp/<slug>.png` — must exit 0 for each. rsvg-convert is at /usr/bin/rsvg-convert.

CARD INSERTION into `docs/regression/index.md` (RESEARCH §4 — exact template, NO blank lines between tags, href trailing slash, src `../assets/thumb/<slug>.svg`): insert all five card blocks as a contiguous group immediately BEFORE the existing `<a class="fdars-gallery-item" href="classification/">` line (i.e. right after the function-on-scalar card's closing `</a>`), in order concurrent-regression → functional-glm → function-on-function → additive-sof → frechet-regression:
```
<a class="fdars-gallery-item" href="concurrent-regression/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/concurrent-regression.svg" alt="">
<div class="fdars-gallery-title">Concurrent Regression</div>
<div class="fdars-gallery-desc">Point-wise coefficient function links functional predictor and response.</div>
</a>
<a class="fdars-gallery-item" href="functional-glm/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/functional-glm.svg" alt="">
<div class="fdars-gallery-title">Functional GLM</div>
<div class="fdars-gallery-desc">Project curves onto FPCs, then fit a GLM on the resulting scores.</div>
</a>
<a class="fdars-gallery-item" href="function-on-function/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/function-on-function.svg" alt="">
<div class="fdars-gallery-title">Function-on-Function</div>
<div class="fdars-gallery-desc">Bivariate coefficient surface links functional predictor to functional response.</div>
</a>
<a class="fdars-gallery-item" href="additive-sof/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/additive-sof.svg" alt="">
<div class="fdars-gallery-title">Additive Scalar-on-Function</div>
<div class="fdars-gallery-desc">Decompose functional effect into additive smooth component functions.</div>
</a>
<a class="fdars-gallery-item" href="frechet-regression/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/frechet-regression.svg" alt="">
<div class="fdars-gallery-title">Fréchet Regression</div>
<div class="fdars-gallery-desc">Regress onto a conditional Fréchet mean in a non-Euclidean metric space.</div>
</a>
```
Do NOT reorder existing cards. Do NOT touch any other section index.md. Do NOT run the whole-site `mkdocs build --strict` (deferred to Phase 79).
  </action>
  <acceptance_criteria>
    - the five files docs/assets/thumb/{concurrent-regression,functional-glm,function-on-function,additive-sof,frechet-regression}.svg all exist
    - each new thumbnail contains `viewBox="0 0 320 180"` (grep matches 1 per file)
    - each new thumbnail contains the regression hue `#dc3545` (grep matches >= 1 per file) and no other section hex (#fd7e14/#198754/#6f42c1/#3f51b5 all match 0)
    - each new thumbnail carries `role="img"` and an `aria-label="` attribute
    - no new thumbnail contains `<title`, `<desc`, `aria-labelledby`, or `<style` (grep matches 0 each)
    - each new thumbnail file is under ~1200 bytes
    - docs/regression/index.md contains `href="<slug>/"` and `src="../assets/thumb/<slug>.svg"` for each of the five slugs
    - for each new thumbnail the SVGO two-pass idempotence diff is empty (pass1 == pass2)
    - each new thumbnail smoke-renders via rsvg-convert with exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const idx=fs.readFileSync("docs/regression/index.md","utf8");const slugs=["concurrent-regression","functional-glm","function-on-function","additive-sof","frechet-regression"];const bad=[];for(const slug of slugs){const f="docs/assets/thumb/"+slug+".svg";if(!fs.existsSync(f)){bad.push(slug+":missing");continue;}const s=fs.readFileSync(f,"utf8");const sz=Buffer.byteLength(s);const checks={viewBox:/viewBox="0 0 320 180"/.test(s),hue:/#dc3545/i.test(s),no_other_hue:!/#fd7e14|#198754|#6f42c1|#3f51b5/i.test(s),role:/role="img"/.test(s),aria:/aria-label="/.test(s),no_title:!/<title/.test(s),no_desc:!/<desc/.test(s),no_labelledby:!/aria-labelledby/.test(s),no_style:!/<style/.test(s),size:sz<1200,card_href:new RegExp("href=\""+slug+"/\"").test(idx),card_src:new RegExp("src=\"../assets/thumb/"+slug+".svg\"").test(idx)};for(const [k,v] of Object.entries(checks))if(!v)bad.push(slug+":"+k);}if(bad.length){console.error("FAIL: "+bad.join(", "));process.exit(1);}console.log("STRUCT OK regression 5 thumbnails + 5 cards");'</automated>
    <fails_when>Any of the five thumbnail files is missing; a thumbnail lacks viewBox="0 0 320 180"; a thumbnail lacks the #dc3545 regression hue or carries another section's hex; a thumbnail lacks role="img" or aria-label; a thumbnail contains <title>/<desc>/aria-labelledby/<style>; a thumbnail exceeds ~1200 bytes; or docs/regression/index.md is missing any card's href or src.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && rc=0; for slug in concurrent-regression functional-glm function-on-function additive-sof frechet-regression; do f="docs/assets/thumb/$slug.svg"; first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); if ! diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null; then echo "NON-IDEMPOTENT: $slug"; rc=1; fi; rsvg-convert -w 320 -h 180 "$f" -o /dev/null || { echo "RENDER FAIL: $slug"; rc=1; }; done; [ $rc -eq 0 ] && echo "SVGO idempotent + render OK"; exit $rc</automated>
    <fails_when>For any new thumbnail the two SVGO passes differ (non-idempotent — would fail the Phase 79 GATE-02 determinism gate), or rsvg-convert returns non-zero (the SVG does not parse/render).</fails_when>
  </verify>
  <done>docs/assets/thumb/{concurrent-regression,functional-glm,function-on-function,additive-sof,frechet-regression}.svg exist as fresh minimal 320×180 #dc3545 line-art (role="img" + aria-label, no title/desc/style, <1200 bytes each), all five SVGO-idempotent and rsvg-convert-clean, and docs/regression/index.md carries their five fdars-gallery cards inserted after function-on-scalar (before classification) so every focus-section regression page is carded.</done>
  <reversibility rating="reversible">Docs-only: five new SVG files + additive card insertions into one index.md; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — creating five hand-authored static SVG assets and inserting five markdown card blocks into one section index. No code, no network surface, no user-input handling. The SVGs are decorative line-art with no `<script>`, no `foreignObject`, no external references — no new attack surface. No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner): all five thumbnail files exist, each has viewBox="0 0 320 180", the #dc3545 regression hue and no other section hex, role="img" + aria-label, no title/desc/aria-labelledby/style, <1200 bytes; docs/regression/index.md carries all five cards' href + src.
- Determinism gate (shell loop): each thumbnail's SVGO two-pass diff is empty (idempotent) and rsvg-convert renders it without error.
- Whole-site `mkdocs build --strict` and the consolidated SVGO/determinism GATE-02 are intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
CARD-03 + CARD-06 (regression portion) delivered: the regression landing gallery carries five new SVGO-idempotent, STYLE_SPEC-conformant, decorative-accessible #dc3545 thumbnails covering the v11.0-era regression methods.
</success_criteria>

<output>
Create `.planning/phases/77-section-landing-card-coverage/77-03-SUMMARY.md` when done.
</output>

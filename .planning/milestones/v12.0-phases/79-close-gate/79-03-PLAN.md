---
phase: 79-close-gate
plan: 03
type: execute
wave: 2
depends_on: ["79-01", "79-02"]
files_modified: []
autonomous: true
use_worktrees: false
requirements: [GATE-01, DEPTH-03]

estimate:
  tokens: 26000
  raw_tokens: 17000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "The whole-site `mkdocs build --strict` completes green offline and `check_docs_figures.py site` exits 0 — no fence across the 13 milestone pages rendered a traceback/ModuleNotFoundError/exec-error (GATE-01)."
    - "Every worked example on the DEPTH pages (regression + analyze families) runs offline and emits FDARS_FENCE_OK (DEPTH-03)."
  artifacts:
    - "site/  (the built static site — the artifact the GATE-03 human reviewer inspects)"
  key_links:
    - "check_docs_figures.py is the ONLY mechanism that catches a silently-errored fence — mkdocs --strict alone renders the traceback as HTML and continues (RESEARCH § Section 1)."
---

<objective>
GATE-01 + DEPTH-03 — build the whole documentation site under `mkdocs build --strict` OFFLINE
and prove every worked example fence ran clean (emitted `FDARS_FENCE_OK`, produced no rendered
traceback). This is the single offline/method-accuracy sweep for the DEPTH pages and it also
produces the built `site/` that the GATE-03 human reviewer inspects.

Purpose: `mkdocs build --strict` does NOT fail when a markdown-exec fence raises — it renders
the traceback as HTML and continues. `check_docs_figures.py` is the error-detection signal that
catches those silent failures; `FDARS_FENCE_OK` in each fence is the correctness signal. Both
must pass for GATE-01/DEPTH-03 to hold. RESEARCH ruled `DOCS_FAST=1` correct for this gate
(proves fence correctness in ~7 min; the marker is emitted regardless of iteration depth).
Output: A green strict build + exit-0 figure check, and the built `site/` directory for review.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-close-gate/79-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-01 + DEPTH-03 — whole-site strict offline build + figure-error sweep</name>
  <files>site/ (build output — regenerated, not committed)</files>
  <precondition>The `.venv` has mkdocs 1.6.1, markdown-exec, pydantic, anthropic, and the compiled `fdars._native` — a superset of docs requirements (RESEARCH-verified this session). No `maturin develop` rebuild is needed before the build.</precondition>
  <read_first>
    - .planning/phases/79-close-gate/79-RESEARCH.md § Section 1 (authoritative command sequence, DOCS_FAST=1 ruling, check_docs_figures.py anatomy, the 13 milestone pages with fence counts) and § Section 7 pitfalls 1/2/5
    - scripts/check_docs_figures.py (the three markers it greps site/**/index.html for; its exit codes)
  </read_first>
  <action>
    Run the whole-site strict build exactly as specified in 79-RESEARCH.md Section 1, OFFLINE,
    from the project root, using the main-tree `.venv` (never a worktree — the doc-build fences
    hardcode the main-tree `.venv/bin/mkdocs` path per the standing v6.0/v11.0 decision;
    use_worktrees is false for this phase):

      source .venv/bin/activate
      PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict
      python scripts/check_docs_figures.py site

    Use `DOCS_FAST=1` per RESEARCH's Claude's-Discretion ruling: it proves fence correctness (the
    fences run and emit FDARS_FENCE_OK) in ~7 min rather than ~25-45 min; the sentinel is emitted
    regardless of the reduced iteration count. `--strict` must exit 0 (no broken links / nav /
    warnings-as-errors). Then `check_docs_figures.py site` must exit 0 — it recursively scans
    `site/**/index.html` for `Traceback (most recent call last)`, `ModuleNotFoundError`, and
    `class="exec-error"`, and exit-1s if any marker is found (RESEARCH § Section 1 documents this
    verbatim from source).

    DEPTH-03 scope: every exec fence across the 13 milestone pages (5 regression-family, 5
    analyze-family, 3 examples — enumerated with fence counts in RESEARCH Section 1) ends with
    `print("FDARS_FENCE_OK")`. A green strict build + exit-0 figure check together prove every
    fence ran offline without error.

    Keep it OFFLINE — no network. Do not modify any docs page in this plan: RESEARCH confirmed
    every fence green this session, so no fence fix is expected. If a fence DOES fail (a
    traceback surfaces in site/ or check_docs_figures reports a hit), STOP and report the
    offending `rel_path (marker_hits)` — a page fix is out of scope for a pure verification plan
    and would be handled as a gap-closure follow-up, not silently patched here.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && source .venv/bin/activate && PYTHONPATH=scripts DOCS_FAST=1 mkdocs build --strict && python scripts/check_docs_figures.py site</automated>
    <fails_when>`mkdocs build --strict` exits non-zero (broken link/nav/strict warning), OR `check_docs_figures.py site` exits non-zero because a fence rendered `Traceback (most recent call last)` / `ModuleNotFoundError` / `class="exec-error"` into any `site/**/index.html`.</fails_when>
  </verify>
  <done>`mkdocs build --strict` exits 0 offline and `check_docs_figures.py site` prints `OK: no failed figure blocks in site` and exits 0. The `site/` directory is built and ready for the GATE-03 human review. No docs page was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown-exec fence → build process | fences execute Python at build time; run offline, no network egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-03 | Tampering | build-time fence execution | low | accept | Fences run offline (no network) against committed docs + `docs/data/`; the build is read-only verification producing throwaway `site/` output. No new fences or code introduced by this phase. |
</threat_model>

<verification>
GATE-01 + DEPTH-03 pass when the strict offline build exits 0 and `check_docs_figures.py site`
prints the OK line and exits 0, with the `site/` directory populated for GATE-03.
</verification>

<success_criteria>
Whole-site `mkdocs build --strict` is green offline and every worked example across the
milestone emits `FDARS_FENCE_OK` (GATE-01), satisfying DEPTH-03's offline/method-accuracy sweep
for the DEPTH pages. The built `site/` exists for the human reviewer.
</success_criteria>

<output>
Create `.planning/phases/79-close-gate/79-03-SUMMARY.md` when done, recording the strict-build
result, the check_docs_figures exit code, and confirming `site/` is built for GATE-03. Note that
`DOCS_FAST=1` was used (fence-correctness mode; not the byte-identical FND-03 publishing mode).
</output>

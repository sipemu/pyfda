---
phase: 79-close-gate
plan: 05
type: execute
wave: 4
depends_on: ["79-04"]
files_modified:
  - python/fdars/__init__.py
  - pyproject.toml
  - Cargo.toml
  - .planning/phases/79-close-gate/RELEASE-HANDOFF.md
autonomous: true
use_worktrees: false
requirements: [GATE-04]

estimate:
  tokens: 24000
  raw_tokens: 16000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The package version reads 0.11.0 in all three files (python/fdars/__init__.py, pyproject.toml, Cargo.toml) and the MCP fdars_list_capabilities tool reports 0.11.0 (reads fdars.__version__ dynamically)."
    - "The fast GATE-04 subset stays green AFTER the bump — no capability-map drift and no guard-sync break from the version tick."
    - "The irreversible publish (git push / tag push / PyPI) is PREPARED as a human-gated handoff note and is NOT executed by the autonomous run."
  artifacts:
    - ".planning/phases/79-close-gate/RELEASE-HANDOFF.md  (the exact human-only push/tag/publish commands)"
    - "python/fdars/__init__.py  (__version__ = 0.11.0)"
    - "pyproject.toml  (version = 0.11.0)"
    - "Cargo.toml  (version = 0.11.0)"
  key_links:
    - "fdars_list_capabilities reads `from fdars import __version__` — the __init__.py bump propagates to the MCP tool with no rebuild (RESEARCH § Section 5)."
---

<objective>
Release handoff — commit the package-version tick to `0.11.0` (a REVERSIBLE, autonomous change),
re-confirm GATE-04 stays green after the bump, and PREPARE (never execute) the irreversible
publish handoff. The version-bump commit runs autonomously; the git push / tag push / PyPI
publish are ONE-WAY doors that stay strictly human-gated — this plan writes the exact commands
into RELEASE-HANDOFF.md and stops there.

Purpose: v12 shipped importable code (the `fdars_list_capabilities` MCP tool, the
`fdars-capabilities` skill, the capability generator), and `v0.10.0` is already a released tag —
a fresh `0.11.0` is the correct release version. `python/fdars/__init__.py` is additionally
STALE at `0.4.0` (a real bug); the bump corrects it. RESEARCH confirmed no test asserts a
version string and the capability map carries no version, so the bump introduces no drift.
Output: three version-string edits committed + RELEASE-HANDOFF.md, with GATE-04 re-confirmed.

This plan runs ONLY after the GATE-03 human approval (depends_on 79-04).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-close-gate/79-RESEARCH.md
@python/fdars/__init__.py
@pyproject.toml
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump package version to 0.11.0 in all three files + re-confirm GATE-04 fast subset</name>
  <files>python/fdars/__init__.py, pyproject.toml, Cargo.toml</files>
  <reversibility rating="reversible">The version-string bump is a plain source edit + commit — trivially revertable with `git revert`; it triggers no external side effects (no rebuild, no publish).</reversibility>
  <precondition>GATE-03 (Plan 79-04) is APPROVED by the human reviewer — the version bump must not be committed before the diagram review passes.</precondition>
  <read_first>
    - .planning/phases/79-close-gate/79-RESEARCH.md § Section 5 (exact files/lines with verbatim current values; why no `maturin develop` rebuild is needed; the post-bump GATE-04 relevance) and § Section 7 pitfalls 4/8
  </read_first>
  <action>
    Edit exactly three version-string literals to `0.11.0`, at the lines RESEARCH § Section 5
    verified verbatim this session:
      - python/fdars/__init__.py line 35:  `__version__ = "0.4.0"`  becomes `__version__ = "0.11.0"`
        (corrects the STALE 0.4.0 bug)
      - pyproject.toml line 7:             `version = "0.10.0"`     becomes `version = "0.11.0"`
      - Cargo.toml line 3:                 `version = "0.10.0"`     becomes `version = "0.11.0"`

    Change ONLY the version literal on each line — do not touch surrounding metadata. No
    `maturin develop` / `cargo build` rebuild is needed: `fdars.__version__` is read from
    `__init__.py` at import time, and the MCP tool reads `from fdars import __version__`
    dynamically (RESEARCH § Section 5), so the bump propagates without recompilation. (`pip show
    fdars` will still report the stale dist-info version — harmless; no test reads it, per
    RESEARCH § Section 7 pitfall 8.)

    Then re-run the fast GATE-04 subset (the 4 quick files, ~2 s — NOT the slow test_mcp_server;
    Plan 02 already ran the authoritative full suite) to confirm the bump introduced no
    capability-map drift and no guard-sync break, and confirm `fdars.__version__` now reports
    0.11.0 (which is what the MCP tool surfaces dynamically).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q '0.11.0' python/fdars/__init__.py && grep -q '0.11.0' pyproject.toml && grep -q '0.11.0' Cargo.toml && .venv/bin/pytest tests/test_guard_sync_version_independent.py tests/test_mcp_import_smoke.py tests/test_capability_accuracy.py tests/test_advisor_grounding.py -q && .venv/bin/python -c "from fdars import __version__ as v; assert v=='0.11.0', v; print('VERSION OK', v)"</automated>
    <fails_when>Any of the three version files does not contain `0.11.0`, OR the fast GATE-04 subset reports a failure (capability-map drift / guard-sync break), OR `fdars.__version__` does not equal `0.11.0` after the edit.</fails_when>
  </verify>
  <done>All three version files read `0.11.0`; the fast GATE-04 subset is green; `fdars.__version__ == "0.11.0"` (so the MCP `fdars_list_capabilities` tool reports 0.11.0). The bump is ready to commit (a reversible change).</done>
</task>

<task type="auto">
  <name>Task 2: Author RELEASE-HANDOFF.md — the exact human-gated publish commands (PREPARED, NOT executed)</name>
  <files>.planning/phases/79-close-gate/RELEASE-HANDOFF.md</files>
  <reversibility rating="reversible">Writing a planning note is fully reversible; it documents commands but executes none of them.</reversibility>
  <read_first>
    - .planning/phases/79-close-gate/79-RESEARCH.md § Section 6 (the three human-only commands, the publish.yml trigger analysis, and the current-state facts: HEAD ~80+ ahead, remote URL, no v0.11.0/v12.0 tags exist)
  </read_first>
  <action>
    Write `.planning/phases/79-close-gate/RELEASE-HANDOFF.md` capturing EXACTLY the commands the
    USER must run — and stating plainly that the autonomous run does NOT execute any of them. Use
    the verified facts from RESEARCH § Section 6. Include:

    1. A header stating: "These commands are HUMAN-GATED and were NOT executed by the autonomous
       run. Pushing the semver tag fires PyPI immediately and cannot be undone."

    2. The three commands, in order, each annotated with what it triggers:
       - `git push origin main`  — pushes the ~80+ commits ahead of origin; fires the docs CI
         (docs.yml path filter matches docs/**, python/**, src/**, scripts/**, mkdocs.yml). The
         version-bump commit must be on main and pushed before tagging.
       - `git tag v0.11.0 && git push origin v0.11.0`  — FIRES PYPI (publish.yml trigger pattern
         `v[0-9]+.[0-9]+.[0-9]+` matches) — IRREVERSIBLE.
       - `git tag v12.0 && git push origin v12.0`  — milestone marker only; does NOT match the
         semver publish pattern (two numeric segments), so NO PyPI trigger.

    3. A short "Current state" section from RESEARCH § Section 6: HEAD is ~80+ commits ahead of
       origin/main; remote is the project's GitHub repo; no `v0.11.0` or `v12.0` tag exists yet.

    4. A note that the post-close autonomous lifecycle (audit → complete → cleanup) may run, but
       the tag push / PyPI publish remain the user's to execute.

    Do NOT run any `git push`, `git tag`, or PyPI publish command in this plan — write the note only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f .planning/phases/79-close-gate/RELEASE-HANDOFF.md && grep -q 'git push origin main' .planning/phases/79-close-gate/RELEASE-HANDOFF.md && grep -q 'v0.11.0' .planning/phases/79-close-gate/RELEASE-HANDOFF.md && grep -q 'v12.0' .planning/phases/79-close-gate/RELEASE-HANDOFF.md && grep -qi 'not.*execut' .planning/phases/79-close-gate/RELEASE-HANDOFF.md && echo "HANDOFF NOTE OK"</automated>
    <fails_when>RELEASE-HANDOFF.md is missing, OR it omits any of the three commands (`git push origin main`, the `v0.11.0` tag push, the `v12.0` tag push), OR it lacks the explicit statement that these are NOT executed by the autonomous run.</fails_when>
  </verify>
  <done>`.planning/phases/79-close-gate/RELEASE-HANDOFF.md` exists, lists the three exact human-only commands in order with their trigger annotations, states the current-state facts, and declares plainly that the autonomous run does NOT execute them. No push/tag/publish was performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous run → irreversible publish | git push / semver tag push / PyPI publish are one-way doors reserved for the human |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-05 | Elevation of Privilege | release publish (tag/PyPI/push) | high | mitigate | The plan PREPARES the publish as a handoff note only and executes NO push/tag/publish command; the irreversible door stays human-gated (CONTEXT success criterion 5). The version-bump commit is reversible and the only mutation this plan performs. |
| T-79-06 | Tampering | version-string edit | low | accept | Three literal edits, no rebuild, no external side effect; trivially revertable. Re-confirmed green by the fast GATE-04 subset. |
</threat_model>

<verification>
Plan passes when: all three version files read `0.11.0`, the fast GATE-04 subset is green,
`fdars.__version__ == 0.11.0`, and RELEASE-HANDOFF.md exists with the three human-only commands
and the explicit "not executed autonomously" statement. No push/tag/publish is performed.
</verification>

<success_criteria>
The package-version tick to `0.11.0` is committed (reversible, autonomous) with GATE-04
re-confirmed green post-bump; the irreversible publish stays human-gated and is handed off via
RELEASE-HANDOFF.md, not executed (CONTEXT success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/79-close-gate/79-05-SUMMARY.md` when done, recording the version bump,
the post-bump GATE-04 result, and confirming RELEASE-HANDOFF.md was written and NO push/tag/
publish command was executed.
</output>

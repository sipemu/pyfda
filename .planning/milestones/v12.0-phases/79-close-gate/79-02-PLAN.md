---
phase: 79-close-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
use_worktrees: false
requirements: [GATE-04]

estimate:
  tokens: 20000
  raw_tokens: 13000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "All 5 advisor/MCP test files pass green on current HEAD: the grounding invariant and the MCP LLM-free boundary (no provider/model keys in fdars_list_capabilities) are preserved after the SKILL-04 tool landed (GATE-04)."
  artifacts: []
  key_links:
    - "tests/test_guard_sync_version_independent.py enforces the three-way literal mirror server._CAPABILITY_MODULES == JSON keys == test _EXPECTED_CAPABILITY_MODULES."
---

<objective>
GATE-04 — run the advisor/MCP test suite green, proving the grounding invariant and the MCP
LLM-free boundary still hold after the Phase-78 SKILL-04 capability tool landed. This is the
pre-version-bump baseline confirmation (a fast post-bump re-confirm happens in Plan 05).

Purpose: The new `fdars_list_capabilities` MCP tool must stay provably LLM-free (returns the
static capability surface only — no provider/model keys, no model call) and the guard-sync
tests must stay green so the three-way literal mirror (server frozenset == JSON keys == test
expected set) holds. RESEARCH confirmed all 5 files green this session.
Output: A recorded PASS across all 5 GATE-04 test files (no file changes).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-close-gate/79-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-04 — run the 5 advisor/MCP test files green (grounding + LLM-free boundary)</name>
  <files>(none — read-only verification gate; modifies no files)</files>
  <precondition>The compiled `fdars._native` extension is importable under `.venv` and `mcp` is installed for the Python 3.10+ tests (RESEARCH-verified this session; 3.9-guarded tests skip cleanly).</precondition>
  <read_first>
    - .planning/phases/79-close-gate/79-RESEARCH.md § Section 4 (exact pytest command, the 5 files, per-file timing, what each test guards)
  </read_first>
  <action>
    Run the full GATE-04 suite exactly as specified in 79-RESEARCH.md Section 4, under the `.venv`
    interpreter, across all 5 test files in one invocation:
      - tests/test_guard_sync_version_independent.py  (guard-sync + capability mirror; the
        Py3.9-safe primary + Py3.10+ LLM-free companion)
      - tests/test_mcp_import_smoke.py                 (COMPAT-02 import smoke; covers the new tool)
      - tests/test_capability_accuracy.py              (SKILL-02 no-drift + all-importable + curation)
      - tests/test_advisor_grounding.py                (grounding invariant across advisors)
      - tests/test_mcp_server.py                       (~92 s — the slow one; run it here for the
        authoritative pre-bump confirmation)

    RESEARCH confirms this is the baseline BEFORE the version bump; no test asserts a specific
    `fdars.__version__` string, so the map carries no version and no drift is expected. Do not
    modify any test or source file — this is a read-only gate. If any test fails, report the
    failing test id(s) and the traceback tail, and stop (a fix would be a code concern, not part
    of this verification plan).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_guard_sync_version_independent.py tests/test_mcp_import_smoke.py tests/test_capability_accuracy.py tests/test_advisor_grounding.py tests/test_mcp_server.py -q</automated>
    <fails_when>Any of the 5 pytest files reports a failure (non-zero exit), OR a Py3.10+ MCP test errors instead of skipping cleanly on an unavailable interpreter.</fails_when>
  </verify>
  <done>All 5 GATE-04 test files pass (pytest exits 0; Py3.9-guarded tests skip, not fail). The grounding invariant and the MCP LLM-free boundary are confirmed intact after SKILL-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → compiled extension | pytest imports the local `.venv` `fdars` package + native extension; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-02 | Information Disclosure | MCP `fdars_list_capabilities` return shape | low | mitigate | `test_guard_sync_version_independent.py::test_capability_tool_llm_free_boundary` asserts the return has `modules` but NOT `provider`/`model` keys — the LLM-free boundary is test-enforced, not assumed. |
</threat_model>

<verification>
GATE-04 passes when all 5 test files exit 0 under `.venv/bin/pytest`, with the grounding and
LLM-free-boundary assertions green.
</verification>

<success_criteria>
Advisor/MCP tests (including guard-sync) are green after the SKILL-04 tool landed — grounding
invariant + MCP LLM-free boundary preserved (GATE-04).
</success_criteria>

<output>
Create `.planning/phases/79-close-gate/79-02-SUMMARY.md` when done, recording the per-file pass
counts and confirming no files were modified.
</output>

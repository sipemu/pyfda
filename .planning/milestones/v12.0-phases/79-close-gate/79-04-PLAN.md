---
phase: 79-close-gate
plan: 04
type: execute
wave: 3
depends_on: ["79-03"]
files_modified: []
autonomous: false
use_worktrees: false
requirements: [GATE-03]

estimate:
  tokens: 14000
  raw_tokens: 9000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "A human has explicitly approved the 21 Phase-77 thumbnails AND ruled on the two forwarded visual-accuracy notes (IN-02 functional-time-series dashed forecast; IN-03 density-fda LQD curve) on the built site before the phase closes (GATE-03)."
  artifacts: []
  key_links:
    - "This gate is BLOCKING and cannot self-approve — the phase (and the version bump in Plan 05) cannot proceed without recorded human approval (standing v6.0 hypograph/epigraph lesson)."
---

<objective>
GATE-03 — a BLOCKING human diagram/method-accuracy review of the 21 new Phase-77 thumbnails,
with the two forwarded visual-accuracy notes (IN-02, IN-03) explicitly put to the reviewer.
This gate pauses the autonomous run and cannot self-approve; the phase cannot close and the
version bump (Plan 05) cannot proceed without recorded human approval.

Purpose: Visual/method accuracy requires human judgment — the standing v6.0 lesson (a human
review caught an inverted hypograph/epigraph asymmetry that all automated gates passed). The
automated gates (GATE-02 SVGO, GATE-04 tests, GATE-01 strict build) run FIRST; this human gate
runs AFTER GATE-01 so the reviewer inspects the diagrams on the freshly-built `site/`.
Output: A recorded human APPROVE/REDRAW decision for the thumbnail set and the two notes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-close-gate/79-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: GATE-03 — blocking human diagram/method-accuracy review (21 thumbnails + IN-02/IN-03)</name>
  <files>(none — human review; modifies no files)</files>
  <precondition>Plan 79-03 has built `site/` offline (the reviewer inspects the thumbnails on the built section-landing galleries), and GATE-02 (Plan 79-01) has confirmed the same SVGs are determinism-stable.</precondition>
  <read_first>
    - .planning/phases/79-close-gate/79-RESEARCH.md § Section 3 (the full review packet: 21 thumbnail slugs mapped to their section-landing pages, the IN-02/IN-03 note text verbatim, and the two render options)
  </read_first>
  <review_packet>
    Present to the human reviewer:

    (a) The 21 Phase-77 thumbnails, each mapped to its section-landing gallery page (full table
        in RESEARCH § Section 3): additive-sof, advanced-clustering, banded-alignment,
        canadian-depth-centrality, concurrent-regression, density-fda (IN-03),
        frechet-regression, functional-boxplot, functional-glm, functional-outlier-workflow,
        functional-statistics, functional-time-series (IN-02), function-on-function, imputation,
        interpolation, multi-domain, pace-fpca, scoring-metrics, shapelets, shift-registration,
        tolerance-vs-conformal — reachable on the built site's regression/, analyze/, align/,
        represent/, examples/ index pages.

    (b) The two forwarded visual-accuracy notes for an explicit ruling:
        - IN-02 (functional-time-series.svg): the dashed forecast segment diverges only ~4px
          from the solid historical tail — at thumbnail/card size the dashed forecast motif may
          read as indistinct from the solid line. Reviewer judges: acceptable at card size, or
          redraw with a wider dash gap / darker contrast?
        - IN-03 (density-fda.svg): the LQD-transformed bold curve reads as a broader bell shape
          rather than a clearly non-bell unconstrained-domain object. Reviewer judges: acceptable,
          or mark for redraw (method-accuracy risk of misreading it as a standard density)?

    (c) How to render (RESEARCH § Section 3):
        - Option A — the built site: open `file:///home/simonm/projects/rust/pyfda/site/` and
          navigate the regression/analyze/align/represent/examples index galleries.
        - Option B — instant per-thumbnail PNG for close inspection, e.g.:
          `rsvg-convert -w 320 -h 180 docs/assets/thumb/functional-time-series.svg -o /tmp/thumb-fts.png`
          `rsvg-convert -w 320 -h 180 docs/assets/thumb/density-fda.svg -o /tmp/thumb-density.png`
  </review_packet>
  <action>
    PAUSE the autonomous run and hand off to the human reviewer with the review packet above.
    This is a hard human gate: do NOT self-approve, do NOT infer approval from the passing
    automated gates, and do NOT proceed to the version bump. Wait for an explicit decision:
      - APPROVE → record the approval and unblock Plan 05.
      - REDRAW (IN-02 and/or IN-03, or any other thumbnail) → record which slugs the reviewer
        flagged; those become a gap-closure follow-up (a redraw + re-run of GATE-02 for the
        changed SVG), and the phase does not close until the reviewer approves the corrected set.
    Record the reviewer's verbatim decision in the SUMMARY.
  </action>
  <what-built>Nothing new is built by this task — it presents the already-shipped 21 Phase-77 thumbnails (rendered on the `site/` built by Plan 79-03) plus the two forwarded visual-accuracy notes IN-02 and IN-03 for a human method-accuracy judgment.</what-built>
  <how-to-verify>Open `file:///home/simonm/projects/rust/pyfda/site/` and inspect the thumbnails on the regression/, analyze/, align/, represent/, and examples/ index galleries — OR render individual thumbnails with `rsvg-convert -w 320 -h 180 docs/assets/thumb/<slug>.svg -o /tmp/<slug>.png`. Pay particular attention to IN-02 (functional-time-series.svg dashed-forecast contrast at card size) and IN-03 (density-fda.svg LQD curve reads as non-bell).</how-to-verify>
  <resume-signal>The reviewer replies APPROVE (all 21 thumbnails acceptable, IN-02 and IN-03 ruled acceptable) — or REDRAW followed by the specific slug(s) to redraw. Any other reply means keep waiting.</resume-signal>
  <verify>
    <human-check>The reviewer has inspected all 21 thumbnails on the built site (or via rsvg-convert) and explicitly stated APPROVE or REDRAW-<slugs>, and has explicitly ruled on IN-02 and IN-03. The decision is recorded in 79-04-SUMMARY.md.</human-check>
  </verify>
  <done>A human has explicitly approved the 21 Phase-77 thumbnails (or flagged specific slugs for redraw) and ruled on IN-02 and IN-03; the decision is recorded. On APPROVE, Plan 05 is unblocked; on REDRAW, the phase stays open pending the corrected set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous run → human judgment | approval authority for visual/method accuracy rests solely with the human reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-04 | Elevation of Privilege | close-gate approval | medium | mitigate | The gate is `autonomous: false` / `checkpoint:human-verify` with `gate="blocking-human"` — the autonomous run cannot self-approve the diagram review (standing v6.0 lesson); `workflow.auto_advance` does not apply. |
</threat_model>

<verification>
GATE-03 passes only on recorded explicit human approval of the thumbnail set and a ruling on
IN-02/IN-03. There is no automated pass path.
</verification>

<success_criteria>
A blocking human diagram/method-accuracy review of the new thumbnails is approved before close
(GATE-03), with IN-02 and IN-03 explicitly ruled on and the decision recorded.
</success_criteria>

<output>
Create `.planning/phases/79-close-gate/79-04-SUMMARY.md` when done, recording the reviewer's
verbatim APPROVE/REDRAW decision and their explicit ruling on IN-02 and IN-03.
</output>

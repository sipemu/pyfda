---
phase: 78-ai-capability-discovery-skill
plan: 03
type: execute
wave: 3
depends_on: [78-01]
files_modified:
  - scripts/generate_capability_dataset.py
  - docs/llms.txt
  - docs/ai-capability-map.md
  - mkdocs.yml
autonomous: true
requirements: [SKILL-03]
use_worktrees: false
estimate:
  tokens: 48000
  raw_tokens: 26000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "`docs/llms.txt` exists as an llms.txt-convention whole-library API digest generated from the capability map, and MkDocs passes it through to site/llms.txt (SKILL-03)."
    - "An in-nav human-readable digest page `docs/ai-capability-map.md` is wired into mkdocs.yml so humans and site-search reach the capability surface (SKILL-03)."
    - "Both docs artifacts are generated OFFLINE from python/fdars/_capability_map.json (committed static), not built at mkdocs time — the docs build does not depend on fdars being importable."
  artifacts:
    - docs/llms.txt
    - docs/ai-capability-map.md
    - mkdocs.yml
  key_links:
    - "the generator's --llmstxt path reads python/fdars/_capability_map.json (78-01) → writes docs/llms.txt + docs/ai-capability-map.md → committed static → MkDocs static passthrough serves site/llms.txt"
---

<objective>
Ship SKILL-03: generate `docs/llms.txt` (the standard agent-readable root-path digest of the
whole library) from the committed capability map, AND wire an in-nav human-readable digest page
`docs/ai-capability-map.md` into `mkdocs.yml`. Both are generated OFFLINE and committed as
static files (the docs build must not import fdars). This is the second surface consuming the
proven map from 78-01.

Purpose: Give agents a canonical URL (`/pyfda/llms.txt`) for the full fdars API surface and give
humans/site-search a rendered page to the same digest.
Output: docs/llms.txt, docs/ai-capability-map.md, mkdocs.yml nav wiring, and the generator's
docs-emit path.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_produced>
NEW files this plan creates + the generator extension:
- `docs/llms.txt` — llms.txt-convention machine-readable whole-library digest (static, committed)
- `docs/ai-capability-map.md` — in-nav human-readable capability digest page
- `mkdocs.yml` — nav entry added under the AI Advisor section for the human page
- `scripts/generate_capability_dataset.py` — extended with a docs-emit path (--llmstxt) that reads the committed map
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend the generator to emit docs/llms.txt + docs/ai-capability-map.md from the map</name>
  <files>scripts/generate_capability_dataset.py, docs/llms.txt, docs/ai-capability-map.md</files>
  <read_first>
    - .planning/phases/78-ai-capability-discovery-skill/78-RESEARCH.md (Pattern 5 "docs/llms.txt Convention" — H1 title, blockquote summary, ## Core Modules with markdown links, ## Full API Reference grouped per module with `module.fn(sig) — one-line purpose`; the MkDocs static-passthrough VERIFIED note docs/*.txt -> site/*.txt; Anti-Pattern "Generating docs/llms.txt at docs-build time" — generate OFFLINE and commit)
    - scripts/generate_capability_dataset.py (the generator from 78-01 to extend — reuse its loaded dataset)
    - python/fdars/_capability_map.json (the source the docs digest is rendered from)
  </read_first>
  <action>
    Extend `scripts/generate_capability_dataset.py` with a docs-emit path (e.g. a `--llmstxt` flag or an always-run emit step) that reads the committed `python/fdars/_capability_map.json` (NOT a fresh introspection — the docs render the committed source of truth) and writes two files:
    (1) `docs/llms.txt` in the llms.txt convention per RESEARCH Pattern 5 — an H1 title, a blockquote one-paragraph library summary, a `## Core Modules` bullet list, and a `## Full API Reference` section grouped by module where each line is `module.fn(sig) — purpose` (include the curated `when` guidance where present). Deterministic output (iterate map keys in sorted order) so it is diff-stable.
    (2) `docs/ai-capability-map.md` — the human-readable in-nav page: a short intro, then the same per-module tables rendered as Markdown for MkDocs to render as HTML. Add MkDocs front-matter/title as needed to match the site's page conventions.
    Do NOT execute this at mkdocs build time; it is an offline generator step whose output is committed. Run it under `.venv` and commit both docs files. Keep the docs-emit deterministic so re-running does not churn the files.
  </action>
  <acceptance_criteria>
    - Re-running the generator's docs-emit path reproduces docs/llms.txt and docs/ai-capability-map.md byte-for-byte (no git diff).
    - docs/llms.txt follows the llms.txt convention (H1, blockquote, ## sections) and lists every module that is a top-level key in the capability map.
    - docs/ai-capability-map.md is valid Markdown renderable by MkDocs (no unclosed fences; has a title).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python scripts/generate_capability_dataset.py --llmstxt && git diff --quiet -- docs/llms.txt docs/ai-capability-map.md && echo "DOCS DIGEST STABLE (no drift on regenerate)"</automated>
    <fails_when>the docs-emit path errors, or regenerating changes docs/llms.txt or docs/ai-capability-map.md (non-deterministic or uncommitted output). NOTE: if the generator emits docs unconditionally rather than behind --llmstxt, drop the flag; the drift check is the load-bearing assertion.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import json,re
txt=open('docs/llms.txt',encoding='utf-8').read()
assert txt.lstrip().startswith('#'), 'llms.txt must start with an H1 title'
assert re.search(r'^>',txt,re.M), 'llms.txt must have a blockquote summary'
mapkeys=[k for k in json.load(open('python/fdars/_capability_map.json')) if not k.startswith('_')]
missing=[m for m in mapkeys if m not in txt]
assert not missing, f'llms.txt omits modules: {missing[:10]}'
md=open('docs/ai-capability-map.md',encoding='utf-8').read()
assert md.strip(), 'ai-capability-map.md is empty'
assert md.count('\`\`\`')%2==0, 'ai-capability-map.md has an unclosed code fence'
print('DIGEST CONTENT OK: llms.txt convention + all modules present + valid human page')
"</automated>
    <fails_when>llms.txt is missing the H1/blockquote, omits any capability-map module, or the human page is empty / has an unclosed fence</fails_when>
  </verify>
  <done>The generator emits deterministic docs/llms.txt (llms.txt convention, all modules) + docs/ai-capability-map.md (valid Markdown), both committed and stable on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the human capability page into mkdocs.yml nav</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (the `- AI Advisor:` nav section lines 159-168 — the placement pattern for a sibling page entry; the top-level `nav:` list style)
    - .planning/phases/78-ai-capability-discovery-skill/78-RESEARCH.md (Pattern 5 nav-wiring note — the docs/ai-capability-map.md is the human page rendered at /pyfda/ai-capability-map/; llms.txt stays raw/static and needs no nav entry)
  </read_first>
  <action>
    Add a single nav entry for `docs/ai-capability-map.md` to `mkdocs.yml` using a scoped Edit (do NOT rewrite the file). Place it as a new top-level nav section (e.g. `- AI Capability Map: ai-capability-map.md`) OR as an entry within a suitable existing section — choose the placement that reads naturally alongside the AI Advisor and Reference sections; a dedicated top-level entry is acceptable and simplest. Do NOT add a nav entry for `docs/llms.txt` (it is static passthrough, served raw at site root, not an HTML page). Keep YAML valid (2-space indent, consistent with surrounding nav lines).
  </action>
  <acceptance_criteria>
    - mkdocs.yml nav references `ai-capability-map.md` exactly once.
    - mkdocs.yml still parses as valid YAML.
    - No nav entry was added for llms.txt.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import yaml
raw=open('mkdocs.yml',encoding='utf-8').read()
# MkDocs uses custom !! tags; tolerate them with a permissive loader.
class L(yaml.SafeLoader): pass
L.add_multi_constructor('', lambda loader,suffix,node: None)
d=yaml.load(raw,Loader=L)
assert d is not None and 'nav' in d, 'mkdocs.yml did not parse or has no nav'
assert raw.count('ai-capability-map.md')==1, 'ai-capability-map.md must be referenced exactly once in nav'
assert 'llms.txt' not in raw or 'nav' not in raw.split('llms.txt')[0][-200:], 'do not add a nav entry for llms.txt'
print('NAV WIRING OK: human page in nav, llms.txt left as static passthrough')
"</automated>
    <fails_when>mkdocs.yml fails to parse, ai-capability-map.md is not referenced exactly once, or a nav entry was added for llms.txt</fails_when>
  </verify>
  <done>mkdocs.yml has exactly one nav entry for ai-capability-map.md, parses valid, and adds no nav entry for llms.txt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed map → docs digest | The offline generator renders the committed JSON into static docs files served publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-06 | Tampering | docs generated at build time | medium | mitigate | Digest is generated OFFLINE and committed static; the docs build never imports fdars (avoids DOCS_FAST/CI failures per RESEARCH Anti-Pattern/Pitfall 4) |
| T-78-07 | Information Disclosure | public llms.txt | low | accept | Contains only public API signatures/purposes (already public); no secrets |
</threat_model>

<verification>
- `.venv/bin/python scripts/generate_capability_dataset.py --llmstxt` reproduces docs/llms.txt + docs/ai-capability-map.md byte-for-byte (no drift).
- docs/llms.txt follows the llms.txt convention and lists every capability-map module.
- mkdocs.yml nav references ai-capability-map.md exactly once and parses valid; no nav entry for llms.txt.
- Whole-site `mkdocs build --strict` (static passthrough site/llms.txt) is DEFERRED to Phase 79 — verify here by file existence + nav entry only, NOT a whole-site build.
</verification>

<success_criteria>
The whole-library digest is generated offline and committed (docs/llms.txt + docs/ai-capability-map.md),
the human page is wired into nav, and both regenerate deterministically from the committed map.
</success_criteria>

<output>
Create `.planning/phases/78-ai-capability-discovery-skill/78-03-SUMMARY.md` when done.
</output>

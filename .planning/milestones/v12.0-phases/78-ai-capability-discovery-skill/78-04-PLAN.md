---
phase: 78-ai-capability-discovery-skill
plan: 04
type: execute
wave: 4
depends_on: [78-01]
files_modified:
  - python/fdars/mcp/server.py
  - tests/test_guard_sync_version_independent.py
  - tests/test_mcp_import_smoke.py
autonomous: true
requirements: [SKILL-04]
use_worktrees: false
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A single `@mcp.tool() fdars_list_capabilities(module: str | None = None)` is registered in python/fdars/mcp/server.py and returns the pre-generated capability surface (SKILL-04)."
    - "The tool is provably LLM-free: it loads python/fdars/_capability_map.json via importlib.resources and its return dict has no provider/model keys and makes no model call (SKILL-04 / GATE-04)."
    - "Guard-sync is extended: a PRIMARY Py3.9-safe test loads the JSON (no mcp import) and asserts the module surface stays literal-synced; a Py3.10+ companion calls the tool and asserts no provider/model keys (GATE-04)."
    - "The existing advisor loop and the other MCP tools' behavior are unchanged — this change is additive only."
  artifacts:
    - python/fdars/mcp/server.py
    - tests/test_guard_sync_version_independent.py
    - tests/test_mcp_import_smoke.py
  key_links:
    - "server._CAPABILITY_MODULES frozenset == keys of python/fdars/_capability_map.json == tests _EXPECTED_CAPABILITY_MODULES — three-way literal mirror enforced by guard-sync (GATE-04)"
    - "tool loads the committed map via importlib.resources at call time (LLM-free); companion guard test proves no provider/model keys leak"
---

<objective>
Ship SKILL-04 (highest-risk, GATE-04 feed): add ONE `@mcp.tool()` `fdars_list_capabilities` to
the existing MCP server that returns the pre-generated capability surface with NO model call, and
extend the version-independent guard-sync so the tool's surface stays literal-synced and its
LLM-free boundary is provable. This is the last surface consuming the proven map from 78-01 —
scheduled last so the map + accuracy test are already green. Additive only: the advisor loop and
existing MCP tools are untouched.

Purpose: Give agents a programmatic, LLM-free capability lookup and keep the grounding invariant /
MCP LLM-free boundary provable (GATE-04, confirmed at Phase 79).
Output: the new MCP tool + `_CAPABILITY_MODULES` frozenset, the guard-sync additions, and the
extended import smoke test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_produced>
NEW symbols + test additions:
- `fdars_list_capabilities` `@mcp.tool()` + `_CAPABILITY_MODULES` frozenset in `python/fdars/mcp/server.py`
- Guard-sync additions in `tests/test_guard_sync_version_independent.py`: `_EXPECTED_CAPABILITY_MODULES` + PRIMARY (Py3.9) JSON-keys test + Py3.10+ LLM-free-boundary companion + Py3.10+ server-frozenset test
- `fdars_list_capabilities` added to `tests/test_mcp_import_smoke.py` loadable-tools assertion
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the LLM-free fdars_list_capabilities MCP tool + _CAPABILITY_MODULES frozenset</name>
  <files>python/fdars/mcp/server.py</files>
  <read_first>
    - python/fdars/mcp/server.py (the `@mcp.tool()` pattern + existing frozensets `_RUNNABLE_METHODS` / `_DIAGNOSTICS_METHODS`; the last tool `fdars_auto_tune` after which the new tool goes — lines 51-88, 591+)
    - .planning/phases/78-ai-capability-discovery-skill/78-RESEARCH.md (Pattern 2 "MCP Tool fdars_list_capabilities" — the exact `_CAPABILITY_MODULES` frozenset (30 module names), the tool signature `(module: str | None = None) -> dict`, the importlib.resources JSON load, the ValueError allowlist-first design, and the LLM-free return dict `{modules, version, module_count, callable_count}`)
    - python/fdars/_capability_map.json (the committed source of truth loaded at call time — produced by 78-01)
  </read_first>
  <behavior>
    - `fdars_list_capabilities(module=None)` returns a dict with a `modules` key (all 30 modules, plus `_Fdata` in the underlying map) and NO `provider`/`model` keys, making NO model call.
    - `fdars_list_capabilities(module="depth")` returns only that module's slice.
    - `fdars_list_capabilities(module="bogus")` raises ValueError whose message lists the known modules (sorted).
    - Loading uses `importlib.resources.files('fdars') / '_capability_map.json'` — no hardcoded path, survives wheel install.
  </behavior>
  <action>
    Add the tool to `python/fdars/mcp/server.py` AFTER `fdars_auto_tune`, mirroring RESEARCH Pattern 2 exactly: (1) a module-level `_CAPABILITY_MODULES: frozenset[str]` = the 30-name set from RESEARCH (the introspected submodule keys; the map's `_Fdata` key is intentionally NOT in the frozenset — the frozenset mirrors the SUBMODULE surface, matching RESEARCH's design and the guard test's `_EXPECTED_CAPABILITY_MODULES`); (2) `@mcp.tool()` `def fdars_list_capabilities(module: str | None = None) -> dict:` that validates `module` against `_CAPABILITY_MODULES` FIRST (ValueError with sorted known-list on miss — prevents any path traversal and keeps the frozenset honest), then loads the committed JSON via `importlib.resources`, filters to the requested module if given, and returns `{"modules": ..., "version": ..., "module_count": ..., "callable_count": ...}`. NO `advise()`, no provider/model args, no network — the compute path only reads pre-generated static data. Do NOT modify `_RUNNABLE_METHODS`, `_DIAGNOSTICS_METHODS`, the advisor loop, or any existing tool.
    CRITICAL consistency: `_CAPABILITY_MODULES` must equal the SUBMODULE keys of `_capability_map.json` (i.e. all top-level keys except `_Fdata`). If 78-01's map keys differ from RESEARCH's 30-name list, make `_CAPABILITY_MODULES` match the actual committed map (minus `_Fdata`) and carry that same set into Task 2's `_EXPECTED_CAPABILITY_MODULES`.
  </action>
  <reversibility rating="reversible">Additive @mcp.tool() + a new frozenset; no change to existing tools or the advisor loop — trivially revertible by deleting the tool + frozenset.</reversibility>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import sys
if sys.version_info < (3,10):
    print('SKIP (<3.10): mcp unavailable'); raise SystemExit(0)
from fdars.mcp.server import fdars_list_capabilities, _CAPABILITY_MODULES
r=fdars_list_capabilities(module=None)
assert 'modules' in r and isinstance(r['modules'],dict), 'must return modules dict'
assert 'provider' not in r and 'model' not in r, 'LLM-free boundary: no provider/model keys'
import json
mapkeys={k for k in json.load(open('python/fdars/_capability_map.json')) if k!='_Fdata'}
assert set(_CAPABILITY_MODULES)==mapkeys, 'frozenset must equal submodule map keys (minus _Fdata)'
d=fdars_list_capabilities(module='depth'); assert set(d['modules'])=={'depth'}, 'module filter must slice'
try:
    fdars_list_capabilities(module='bogus'); raise AssertionError('expected ValueError for unknown module')
except ValueError as e:
    assert 'depth' in str(e), 'ValueError must list known modules'
print('MCP TOOL OK: LLM-free, filtered, frozenset synced, allowlist enforced')
"</automated>
    <fails_when>the tool is missing, returns provider/model keys, its frozenset diverges from the committed map's submodule keys, the module filter does not slice, or an unknown module does not raise a known-list ValueError. On Python <3.10 this check skips cleanly (mcp unavailable).</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -nE "_DIAGNOSTICS_METHODS\s*=|_RUNNABLE_METHODS\s*=" python/fdars/mcp/server.py | grep -c "=" | { read n; [ "$n" -eq 2 ] && echo "EXISTING FROZENSETS UNTOUCHED (2 defs)" || { echo "FAIL: existing frozenset defs changed"; exit 1; }; }</automated>
    <fails_when>the number of existing `_DIAGNOSTICS_METHODS`/`_RUNNABLE_METHODS` definitions changed (a sign the additive-only constraint was violated)</fails_when>
  </verify>
  <done>`fdars_list_capabilities` is registered, LLM-free (no provider/model keys, no model call), enforces the module allowlist, and its `_CAPABILITY_MODULES` frozenset equals the committed map's submodule keys; existing frozensets and tools are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend guard-sync — Py3.9 JSON-keys primary + Py3.10+ LLM-free companion (GATE-04)</name>
  <files>tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - tests/test_guard_sync_version_independent.py (the existing discipline: hard-coded `_EXPECTED_*` frozenset mirror, PRIMARY test runs on Py3.9 with NO mcp import, companion guarded to Py3.10+ via `if sys.version_info < (3,10): pytest.skip(...)` + `pytest.importorskip("mcp")` — extend BELOW the existing `_EXPECTED_DIAGNOSTICS_METHODS` block)
    - .planning/phases/78-ai-capability-discovery-skill/78-RESEARCH.md ("Guard-Sync Extension Skeleton (GATE-04)" — the exact `_EXPECTED_CAPABILITY_MODULES` frozenset, `test_capability_map_modules_match_expected` (Py3.9 JSON load, no mcp), `test_capability_tool_llm_free_boundary` (Py3.10+ calls the tool, asserts no provider/model), `test_capability_mcp_server_frozenset_matches` (Py3.10+ imports the server frozenset))
  </read_first>
  <behavior>
    - PRIMARY (Py3.9+, no mcp import): load `_capability_map.json` via importlib.resources, assert `frozenset(keys) - {'_Fdata'} == _EXPECTED_CAPABILITY_MODULES` (or assert on submodule keys per Task 1's committed frozenset) — catches a new module added without regenerating.
    - COMPANION (Py3.10+): call `fdars_list_capabilities(None)`, assert `modules` present, assert NO `provider`/`model` keys (LLM-free boundary), assert returned module set matches expected.
    - COMPANION (Py3.10+): import `server._CAPABILITY_MODULES` and assert it equals `_EXPECTED_CAPABILITY_MODULES`.
  </behavior>
  <action>
    Extend `tests/test_guard_sync_version_independent.py` below the existing `_EXPECTED_DIAGNOSTICS_METHODS` block, following the RESEARCH GATE-04 skeleton. Add `_EXPECTED_CAPABILITY_MODULES` as a hard-coded frozenset that MUST equal Task 1's `_CAPABILITY_MODULES` (the committed map's submodule keys, i.e. all top-level keys except `_Fdata`). Add: (a) `test_capability_map_modules_match_expected` — PRIMARY, Py3.9-safe, loads the JSON via importlib.resources and asserts the submodule key set equals `_EXPECTED_CAPABILITY_MODULES` (exclude `_Fdata` when comparing); NO mcp import. (b) `test_capability_tool_llm_free_boundary` — guarded `if sys.version_info < (3,10): pytest.skip(...)` + `pytest.importorskip("mcp")`, then import + call `fdars_list_capabilities` and assert `'provider' not in result` and `'model' not in result` and `'modules' in result`. (c) `test_capability_mcp_server_frozenset_matches` — same Py3.10+ guard, import `_CAPABILITY_MODULES` and assert it equals `_EXPECTED_CAPABILITY_MODULES`. Keep the existing `_DIAGNOSTICS_METHODS` guard tests unchanged. Add a MAINTENANCE note: update `_EXPECTED_CAPABILITY_MODULES` here, `_CAPABILITY_MODULES` in server.py, and regenerate the JSON in one atomic commit.
  </action>
  <acceptance_criteria>
    - The PRIMARY capability guard test imports no `mcp` (Py3.9-safe) and asserts JSON submodule keys equal the expected frozenset.
    - The Py3.10+ companion asserts no provider/model keys in the tool return (LLM-free boundary).
    - `_EXPECTED_CAPABILITY_MODULES` equals server `_CAPABILITY_MODULES` equals the committed map submodule keys.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_guard_sync_version_independent.py -q</automated>
    <fails_when>any guard-sync test fails — capability JSON keys diverge from the expected frozenset, the server frozenset diverges, or the tool return exposes provider/model keys</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import re
t=open('tests/test_guard_sync_version_independent.py',encoding='utf-8').read()
# The new capability PRIMARY test must not sit under a module-level mcp import.
assert '_EXPECTED_CAPABILITY_MODULES' in t, 'missing capability guard additions'
assert 'test_capability_map_modules_match_expected' in t, 'missing PRIMARY capability guard test'
assert re.search(r'provider.*not in|not in.*provider', t), 'missing LLM-free provider assertion'
# No new top-level (col-0) mcp import introduced.
assert not re.search(r'^(import|from)\s+mcp(\.|\s|$)', t, re.M), 'module-level mcp import breaks Py3.9 guard'
print('GUARD-SYNC ADDITIONS OK: Py3.9-safe primary + LLM-free companion present')
"</automated>
    <fails_when>the capability guard additions are absent, the LLM-free provider assertion is missing, or a module-level mcp import was introduced (breaks Py3.9)</fails_when>
  </verify>
  <done>Guard-sync carries the Py3.9-safe capability-modules primary test + the Py3.10+ LLM-free-boundary and server-frozenset companions, all green, with no module-level mcp import.</done>
</task>

<task type="auto">
  <name>Task 3: Add fdars_list_capabilities to the MCP import smoke test</name>
  <files>tests/test_mcp_import_smoke.py</files>
  <read_first>
    - tests/test_mcp_import_smoke.py (the module-level `pytestmark = pytest.mark.skipif(sys.version_info < (3,10), ...)` guard + the loadable-tools assertion listing the existing tool handlers)
  </read_first>
  <action>
    Extend `tests/test_mcp_import_smoke.py` to also import `fdars_list_capabilities` from `fdars.mcp.server` and assert it is callable, alongside the existing three tool handlers. Keep the module-level Py3.10+ skipif guard intact (do not import mcp on 3.9). Add the new name to the `expected_tool_names` set and the handler-lookup dict so the smoke test proves the tool loads over the stdio path.
  </action>
  <acceptance_criteria>
    - The smoke test references `fdars_list_capabilities` and asserts it is callable.
    - The module-level Py3.10+ skipif guard is unchanged (no 3.9 mcp import).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_mcp_import_smoke.py -q</automated>
    <fails_when>the smoke test fails to import or call fdars_list_capabilities (ImportError, or the tool not registered)</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q "fdars_list_capabilities" tests/test_mcp_import_smoke.py && echo "SMOKE COVERS NEW TOOL" || { echo "FAIL: smoke test does not reference the new tool"; exit 1; }</automated>
    <fails_when>the smoke test does not reference fdars_list_capabilities</fails_when>
  </verify>
  <done>The MCP import smoke test loads and asserts `fdars_list_capabilities` is callable, Py3.10+ guard intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → fdars_list_capabilities | An MCP client passes a `module` filter arg that selects a slice of the returned surface |
| tool → committed JSON | The tool loads the in-repo capability map at call time via importlib.resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-08 | Tampering | `module` arg path traversal | medium | mitigate | Frozenset allowlist check BEFORE any load; the JSON path is hardcoded (importlib.resources), the arg only filters returned keys — no path is derived from input |
| T-78-09 | Spoofing | LLM-free boundary erosion | high | mitigate | Companion guard test asserts no `provider`/`model` keys in the tool return; the tool makes no model call and imports no advisor entry point; runs on every PR (GATE-04) |
| T-78-10 | Tampering | frozenset/JSON drift | medium | mitigate | Three-way literal mirror (server `_CAPABILITY_MODULES` == JSON keys == test `_EXPECTED_CAPABILITY_MODULES`) enforced by PRIMARY Py3.9 guard-sync |
| T-78-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — all stdlib + existing `mcp`/`fdars` deps (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_guard_sync_version_independent.py tests/test_mcp_import_smoke.py -q` green.
- Tool import smoke: `.venv/bin/python -c "import sys; sys.exit(0) if sys.version_info<(3,10) else __import__('fdars.mcp.server')"` (ImportError => fail).
- `fdars_list_capabilities` returns a `modules` dict with no provider/model keys; frozenset == committed map submodule keys.
- Existing `_DIAGNOSTICS_METHODS`/`_RUNNABLE_METHODS` and the advisor loop unchanged (additive only).
- GATE-04 final confirmation + whole-site build are DEFERRED to Phase 79.
</verification>

<success_criteria>
An additive, LLM-free `fdars_list_capabilities` MCP tool is registered, the guard-sync is extended
with a Py3.9-safe primary + Py3.10+ LLM-free companions, the smoke test covers the tool, and the
three-way capability-module mirror is literal-synced — all green, advisor/MCP boundary preserved.
</success_criteria>

<output>
Create `.planning/phases/78-ai-capability-discovery-skill/78-04-SUMMARY.md` when done.
</output>

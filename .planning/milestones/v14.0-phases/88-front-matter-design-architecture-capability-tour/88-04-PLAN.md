---
phase: 88-front-matter-design-architecture-capability-tour
plan: "04"
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - paper/sections/design.tex
  - paper/sections/represent.tex
  - paper/sections/advisor.tex
autonomous: true
requirements: [MANU-03, MANU-04, MANU-06]
estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The design section describes the Rust fdars-core + PyO3 boundary honestly: read-only NumPy views (PyReadonlyArray) with a row-major<->column-major layout conversion, NOT byte-for-byte zero-copy for matrices"
    - "The design section gives a module map (via \\nsubmodules/\\npubliccallables macros), the Fdata container, and the sklearn estimator layer (\\nsklearnestimators passing check_estimator, 13 excluded honestly)"
    - "The data-representation section explains Fdata (regular grid, (n_obs,n_points) + argvals/rangeval) AND the irregular/sparse representation as pace_fpca.PyIrregFdata via irreg_fdata_from_lists (NOT a top-level IrregFdata)"
    - "A dedicated advisor+provenance section presents the grounded advisor (build_diagnostics/advise/auto_tune) + the _references_map.json provenance layer + the fdars_method_references MCP tool as a contribution absent from all peers, grounded in comparison_evidence.md"
    - "No hardcoded count integers; the zero-copy claim is not over-stated"
  artifacts:
    - paper/sections/design.tex
    - paper/sections/represent.tex
    - paper/sections/advisor.tex
  key_links:
    - "design.tex module-map/estimator counts <-> coverage macros (\\nsubmodules, \\npubliccallables, \\nsklearnestimators)"
    - "represent.tex irregular representation <-> pace_fpca.PyIrregFdata + irreg_fdata_from_lists (the MANU-04 correct answer)"
    - "advisor.tex 'absent from peers' claim <-> comparison_evidence.md + Table~\\ref{tab:comparison}"
---

<objective>
Write the qualitative software-design/architecture section, the data-representation section, and the dedicated grounded-advisor + scientific-provenance contribution section. (MANU-03, MANU-04, MANU-06.)

Purpose: These sections explain HOW fdars is built (Rust/PyO3 + sklearn), how it MODELS functional data (dense Fdata + irregular PyIrregFdata), and WHY the advisor + provenance layer is a novel contribution -- the paper's differentiators, stated accurately enough to survive peer review.
Output: paper/sections/design.tex, paper/sections/represent.tex, paper/sections/advisor.tex, filling the stubs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@paper/comparison_evidence.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: design.tex -- Rust/PyO3 boundary, module map, Fdata container, sklearn layer (qualitative, no benchmarks)</name>
  <files>paper/sections/design.tex</files>
  <precondition>Plan 88-01 wired \input{sections/design} and the \nsklearnestimators macro exists in coverage_counts.tex.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Architecture Facts -- convert.rs row-major<->column-major quote, the HONEST zero-copy nuance ("read-only NumPy views with a layout conversion", NOT byte-for-byte zero-copy for matrices); 27 native _mod.rs; 30 public submodules; 28 sklearn estimators PASS + 13 excluded; Pitfall 6 the-two-28s)
    - /home/simonm/projects/rust/pyfda/paper/coverage_counts.tex (macros: \nsubmodules, \npubliccallables, \nsklearnestimators, \nfdata)
    - /home/simonm/projects/rust/pyfda/.claude/CLAUDE.md (Architecture section -- component responsibilities, layers, PyReadonly GIL note, "NumPy Layout Conversion Overhead" anti-pattern)
  </read_first>
  <action>
    Write paper/sections/design.tex keeping \section{Software Design and Architecture}. Cover qualitatively (NO benchmarks / no timing numbers -- milestone lock):

    (1) The layered architecture: a Rust computation core (fdars-core) exposed through a thin PyO3 binding layer to an idiomatic Python API. Describe the PyO3 boundary HONESTLY per the RESEARCH nuance: input NumPy arrays are passed as read-only views (PyReadonlyArray) that release the GIL, and there is a row-major (NumPy C order) <-> column-major (FdMatrix) layout conversion at the matrix boundary. Do NOT claim byte-for-byte "zero-copy" for the 2D matrix path -- CLAUDE.md itself lists "NumPy Layout Conversion Overhead" as a known cost; be accurate. You may quote the convert.rs doc-comment intent.

    (2) A module map: fdars exposes \nsubmodules public submodules and \npubliccallables public callables (macros -- NOT literal integers), organized by method family (represent/basis/smoothing, alignment, depth/outliers, FPCA/PACE, clustering, classification, regression, FTS, SPM, conformal/tolerance, density/Frechet/metric). Keep it a qualitative map, not an exhaustive list.

    (3) The Fdata container: the OOP container bundling the observation matrix, evaluation grid, ids, and metadata, offering method wrappers; \nfdata public methods (macro).

    (4) The sklearn estimator layer: a pure-Python scikit-learn-compatible layer over the bindings; \nsklearnestimators estimators pass the full check_estimator battery, and honestly note that some methods are excluded (13, structural mismatch with the stateless fit/transform contract). Do NOT conflate \nsklearnestimators with \ncoverage (Pitfall 6).

    HARD rules: no hardcoded count integers (use macros); no benchmark/timing claims; no over-stated zero-copy; no \today/timestamp; peer-review-safe.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/design.tex && grep -qF '\section{Software Design and Architecture}' paper/sections/design.tex && ! grep -qi 'Placeholder' paper/sections/design.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform|milliseconds|microseconds' paper/sections/design.tex && grep -qiE 'PyReadonly|read-only' paper/sections/design.tex && grep -qiE 'row-major|column-major|layout conversion' paper/sections/design.tex && grep -qE '\\nsubmodules' paper/sections/design.tex && grep -qE '\\nsklearnestimators' paper/sections/design.tex && ! grep -qE '\b(30|409|28)\b' paper/sections/design.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/design.tex && echo DESIGN_OK</automated>
    <fails_when>design.tex has a placeholder/benchmark/timestamp/hardcoded count; over-claims zero-copy without the layout-conversion nuance; or omits the module-map / sklearn macros.</fails_when>
  </verify>
  <done>design.tex describes the Rust/PyO3 boundary honestly (read-only views + row/column-major layout conversion, no over-stated zero-copy), a macro-grounded module map, the Fdata container, and the sklearn layer (\nsklearnestimators + 13 excluded), with no benchmarks/hardcoded-counts/timestamps.</done>
  <acceptance_criteria>
    - design.tex covers the layered Rust/PyO3 architecture with the HONEST boundary nuance (PyReadonly views + row/column-major layout conversion)
    - module map + Fdata + sklearn layer stated via \nsubmodules/\npubliccallables/\nfdata/\nsklearnestimators macros; 13 excluded noted honestly
    - No benchmark, no hardcoded count, no timestamp
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: represent.tex -- Fdata dense model + irregular/sparse PyIrregFdata</name>
  <files>paper/sections/represent.tex</files>
  <precondition>Plan 88-01 wired \input{sections/represent}; the represent + fpca snippets exist (Plan 88-02) for cross-reference.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Data Model Facts -- Fdata constructor + row convention + properties; the IrregFdata answer: there is NO top-level fdars.IrregFdata; the irregular/sparse representation is fdars.pace_fpca.PyIrregFdata via irreg_fdata_from_lists, fed to pace_fpca; MANU-05 prose guidance)
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-CONTEXT.md (SS Data Representation section)
  </read_first>
  <action>
    Write paper/sections/represent.tex keeping \section{Data Representation}. Cover:

    (1) The dense representation: Fdata as an (n_obs, n_points) observation matrix on a shared evaluation grid (argvals) with a rangeval, plus ids and optional metadata. Describe argvals/grids and rangeval. Cross-reference the represent capability snippet (the reader already saw \input{snippets/represent} in the capability tour) -- you may reference it, do not duplicate a snippet here.

    (2) The irregular / sparse representation -- the MANU-04 answer, stated CORRECTLY: fdars represents irregularly/sparsely sampled functional data (per-curve grids of differing length) via fdars.pace_fpca.PyIrregFdata, constructed with fdars.pace_fpca.irreg_fdata_from_lists(argvals_list, values_list), which feeds the PACE FPCA (fdars.pace_fpca.pace_fpca). Do NOT call it a top-level IrregFdata -- the public container is PyIrregFdata and the constructor is irreg_fdata_from_lists (RESEARCH SS Data Model Facts). Frame the sparse/longitudinal PACE path as a genuine differentiator vs dense-only FPCA peers, citing Table~\ref{tab:comparison}.

    HARD rules: use the CORRECT names (PyIrregFdata / irreg_fdata_from_lists, never a fabricated IrregFdata); no hardcoded count integers; no \today/timestamp; no over-claim.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/represent.tex && grep -qF '\section{Data Representation}' paper/sections/represent.tex && ! grep -qi 'Placeholder' paper/sections/represent.tex && grep -qF 'PyIrregFdata' paper/sections/represent.tex && grep -qF 'irreg_fdata_from_lists' paper/sections/represent.tex && grep -qiE 'argvals|rangeval|grid' paper/sections/represent.tex && ! grep -qE '(^|[^y])IrregFdata' <(grep -v 'PyIrregFdata' paper/sections/represent.tex) && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/represent.tex && echo REPRESENT_OK</automated>
    <fails_when>represent.tex is missing the section header, PyIrregFdata, irreg_fdata_from_lists, or the argvals/grid/rangeval discussion; uses a bare fabricated "IrregFdata"; has a placeholder or timestamp.</fails_when>
  </verify>
  <done>represent.tex explains the dense Fdata model (matrix + argvals/rangeval + metadata) and the irregular/sparse representation via the CORRECT PyIrregFdata + irreg_fdata_from_lists -> pace_fpca path, cites Table~\ref{tab:comparison}, and has no fabricated names/placeholders/timestamps.</done>
  <acceptance_criteria>
    - represent.tex describes Fdata (dense (n_obs,n_points) + argvals/rangeval + metadata) and the irregular case via PyIrregFdata + irreg_fdata_from_lists -> pace_fpca
    - No bare "IrregFdata", no placeholder, no timestamp, no hardcoded count
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: advisor.tex -- grounded AI advisor + scientific-provenance contribution section</name>
  <files>paper/sections/advisor.tex</files>
  <precondition>Plan 88-01 created advisor.tex stub + wired \input{sections/advisor}; the advisor_diag snippet exists (Plan 88-02).</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Advisor + Provenance Facts -- public surface advise/auto_tune/build_diagnostics; the LLM-free build_diagnostics grounding step; _references_map.json 57 papers / \ndocpapers=47 / \ncoverage=28 callables backed; MCP tool fdars_method_references; the peer-review-safe "absent from peers" claim grounded in comparison_evidence.md; Pitfall 7 do-not-run-advise)
    - /home/simonm/projects/rust/pyfda/paper/comparison_evidence.md (the evidence that no peer provides advisor/provenance -- ground the differentiator claim HERE)
    - /home/simonm/projects/rust/pyfda/paper/coverage_counts.tex (macros: \ndocpapers, \ncoverage)
  </read_first>
  <action>
    Write paper/sections/advisor.tex keeping the \section{...} the stub set (Grounded AI Advisor and Scientific Provenance). Present the layer as fdars's novel contribution:

    (1) The grounded advisor: an interpret -> recommend -> explain workflow. Describe the LLM-free grounding step build_diagnostics(result, method, ...) that turns a method result into a structured diagnostics dict deterministically (the reader saw \input{snippets/advisor_diag} in the tour -- reference it). Describe advise(diagnostics, task=..., domain_context=..., provider=...) as the provider-agnostic LLM entry point that consumes the grounded diagnostics, and auto_tune(...) as the closed-loop parameter search with a grounding guard. State that the LLM step is grounded on the deterministic diagnostics (the grounding invariant) so recommendations trace to computed facts, not free-form generation. Do NOT claim to run advise() in the paper (it needs a provider; Pitfall 7).

    (2) The scientific-provenance layer: a machine-readable _references_map.json mapping callables to author-verified foundational papers (with DOIs) and cross-language (R/Python/Matlab) implementation pointers; \ndocpapers curated-eligible papers, \ncoverage callables backed by a curated paper (macros -- NOT literal integers). Describe the LLM-free fdars_method_references MCP tool that serves this provenance for any callable, and the offline-generated References documentation.

    (3) The differentiator: no peer FDA package (scikit-fda, FDApy, R fda/fda.usc/refund, funData/tidyfun, Matlab fdaM/PACE) provides a grounded parameter advisor OR a machine-readable provenance layer -- ground this in paper/comparison_evidence.md and cite Table~\ref{tab:comparison}. Do NOT assert the "absent from peers" claim from memory.

    HARD rules: no hardcoded count integers (use \ndocpapers/\ncoverage macros); no benchmark claims; no \today/timestamp; the "absent from peers" claim must be evidence-grounded; peer-review-safe.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/advisor.tex && grep -qF '\section{' paper/sections/advisor.tex && ! grep -qi 'Placeholder' paper/sections/advisor.tex && grep -qF 'build_diagnostics' paper/sections/advisor.tex && grep -qF 'fdars_method_references' paper/sections/advisor.tex && grep -qF 'Table~\ref{tab:comparison}' paper/sections/advisor.tex && grep -qE '\\ndocpapers|\\ncoverage' paper/sections/advisor.tex && ! grep -qE '\b(47|28|57|243)\b' paper/sections/advisor.tex && ! grep -qiE 'benchmark|faster than' paper/sections/advisor.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/advisor.tex && echo ADVISOR_OK</automated>
    <fails_when>advisor.tex is missing the section header, build_diagnostics, the MCP tool name, the comparison-table citation, or a provenance macro; has a placeholder/benchmark/timestamp; or hardcodes a provenance count (47/28/57/243).</fails_when>
  </verify>
  <done>advisor.tex presents the grounded advisor (build_diagnostics/advise/auto_tune, grounding invariant) + the provenance layer (_references_map.json, \ndocpapers/\ncoverage macros, fdars_method_references MCP tool) as a contribution absent from peers, grounded in comparison_evidence.md + Table~\ref{tab:comparison}, with no hardcoded counts/benchmarks/timestamps.</done>
  <acceptance_criteria>
    - advisor.tex describes the grounded advisor (LLM-free build_diagnostics + advise/auto_tune + grounding invariant; advise not executed) and the provenance layer (_references_map.json + fdars_method_references MCP tool) via \ndocpapers/\ncoverage macros
    - The "absent from peers" differentiator is grounded in comparison_evidence.md + Table~\ref{tab:comparison}
    - No hardcoded provenance count, no benchmark, no timestamp
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| architecture prose -> source facts | design claims must match convert.rs / CLAUDE.md, not over-claim zero-copy |
| differentiator prose -> evidence | "absent from peers" must trace to comparison_evidence.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-10 | Spoofing | over-claimed "zero-copy" or fabricated API name (IrregFdata) | high | mitigate | honest read-only-views + layout-conversion prose; use PyIrregFdata/irreg_fdata_from_lists verbatim; automated name gate + Phase 90 GATE-04 |
| T-88-11 | Spoofing | "advisor/provenance absent from peers" asserted from memory | high | mitigate | ground in comparison_evidence.md + Table~\ref{tab:comparison}; automated citation gate |
| T-88-12 | Tampering | hardcoded architecture/provenance count drifts vs the maps | medium | mitigate | all counts via macros; automated no-hardcoded-integer gate |
</threat_model>

<verification>
- design/represent/advisor .tex compile as \input fragments; no placeholder/benchmark/timestamp/hardcoded count.
- The zero-copy claim carries the layout-conversion nuance; irregular representation uses the correct PyIrregFdata names.
- The advisor/provenance differentiator is grounded in comparison_evidence.md + the comparison table.
</verification>

<success_criteria>
- design.tex: honest Rust/PyO3 boundary + macro-grounded module map + Fdata + sklearn layer, no benchmarks.
- represent.tex: dense Fdata + correct irregular PyIrregFdata representation.
- advisor.tex: grounded advisor + provenance as an evidence-grounded contribution absent from peers.
</success_criteria>

<output>
Create `.planning/phases/88-front-matter-design-architecture-capability-tour/88-04-SUMMARY.md` when done.
</output>

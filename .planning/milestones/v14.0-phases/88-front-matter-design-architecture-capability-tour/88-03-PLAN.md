---
phase: 88-front-matter-design-architecture-capability-tour
plan: "03"
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - paper/sections/abstract.tex
  - paper/sections/intro.tex
autonomous: true
requirements: [MANU-02]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The abstract positions fdars as a broad, method-accurate, Rust-accelerated, sklearn-compatible Python FDA library with a grounded advisor + provenance layer, with NO benchmark claims"
    - "The Introduction frames the Python FDA gap grounded in the Phase 87 comparison evidence (scikit-fda narrower, R fragmented, no single Python library spanning the surface with provenance + advisor)"
    - "A brief FDA-background paragraph cites canonical references from refs.bib (e.g. Ramsay & Silverman)"
    - "All breadth numbers are macro-derived (\\nsubmodules / \\npubliccallables); no hardcoded count integers"
  artifacts:
    - paper/sections/abstract.tex
    - paper/sections/intro.tex
  key_links:
    - "intro.tex Table~\\ref{tab:comparison} <-> Phase 87 comparison table (peer positioning citation)"
    - "intro.tex \\citep{...} <-> refs.bib cite-keys (FDA background citations resolve)"
---

<objective>
Write the front matter: a concise abstract and an Introduction / statement-of-need that frames the Python FDA gap honestly, plus a brief FDA-background paragraph citing canonical references. (MANU-02.)

Purpose: The front matter sets the paper's honest, under-claiming, evidence-grounded tone and states why fdars exists -- the reviewer's first read.
Output: paper/sections/abstract.tex and paper/sections/intro.tex, filling the Plan 88-01 stubs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@paper/comparison_evidence.md
@paper/refs.bib
</context>

<tasks>

<task type="auto">
  <name>Task 1: abstract.tex -- concise positioning, no benchmarks</name>
  <files>paper/sections/abstract.tex</files>
  <precondition>Plan 88-01 created the abstract.tex stub and wired \input{sections/abstract} inside \begin{abstract}.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-CONTEXT.md (SS Front Matter & Statement of Need -- abstract guidance, no benchmark claims)
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Statement-of-Need Facts -- the honest fdars position; SS Architecture Facts for the macro names)
    - /home/simonm/projects/rust/pyfda/paper/coverage_counts.tex (macros: \nsubmodules, \npubliccallables, \nsklearnestimators)
  </read_first>
  <action>
    Write paper/sections/abstract.tex as the body of the abstract (no \section; it is \input inside \begin{abstract}). Position fdars as a broad, method-accurate, Rust-accelerated (fdars-core), sklearn-compatible Python functional-data-analysis library exposing a grounded AI advisor and a machine-readable scientific-provenance layer. State breadth via the \nsubmodules / \npubliccallables macros (NOT literal integers) and the sklearn compatibility via \nsklearnestimators estimators. Keep it 4-7 sentences.

    HARD rules (milestone locks): NO benchmark / performance / "faster than" claims; NO hardcoded count integers (use macros); NO \today/timestamp; peer-review-safe / under-claiming prose. Do not promise JOSS/JSS (deferred VENUE items). Do not over-claim "zero-copy" -- if the abstract mentions the Rust boundary, say "Rust-accelerated" (the honest architecture nuance is detailed in the design section, Plan 88-04).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/abstract.tex && ! grep -qi 'Placeholder' paper/sections/abstract.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/abstract.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/abstract.tex && ! grep -qiE 'JOSS|JSS|SoftwareX' paper/sections/abstract.tex && grep -qE '\\nsubmodules|\\npubliccallables|\\nsklearnestimators' paper/sections/abstract.tex && ! grep -qE '\b(30|409|28)\b' paper/sections/abstract.tex && ! grep -qF '\section' paper/sections/abstract.tex && echo ABSTRACT_OK</automated>
    <fails_when>abstract has placeholder text, a benchmark/performance claim, a hardcoded count, a timestamp, a venue promise, a \section, or cites no breadth macro.</fails_when>
  </verify>
  <done>abstract.tex is a concise 4-7 sentence abstract body citing breadth via macros, no benchmarks, no hardcoded counts, no venue promises, no \section.</done>
  <acceptance_criteria>
    - abstract.tex positions fdars (broad, method-accurate, Rust-accelerated, sklearn-compatible, advisor + provenance) with breadth via macros
    - No benchmark claim, no hardcoded count, no timestamp, no venue promise, no \section header
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: intro.tex -- Introduction, statement of need, FDA background</name>
  <files>paper/sections/intro.tex</files>
  <precondition>Plan 88-01 wired \input{sections/intro}; refs.bib exists with canonical FDA cite-keys.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Statement-of-Need Facts -- the grounded peer positioning: scikit-fda v0.10.1 narrower, FDApy narrow, R fragmented, Matlab partially maintained, fdars the only Python lib spanning all 14 dimensions with sklearn + advisor + provenance; FDA background = Ramsay & Silverman + method-family papers in refs.bib)
    - /home/simonm/projects/rust/pyfda/paper/comparison_evidence.md (the version-stamped peer facts -- ground the gap claim HERE, not from memory)
    - /home/simonm/projects/rust/pyfda/paper/refs.bib (the exact cite-keys available for \citep)
    - /home/simonm/projects/rust/pyfda/paper/coverage_counts.tex (breadth macros)
  </read_first>
  <action>
    Write paper/sections/intro.tex keeping \section{Introduction}. Three logical parts:

    (1) Statement of need / the Python FDA gap: frame that Python FDA tooling is narrower and more fragmented than the breadth practitioners need -- scikit-fda is the strongest Python peer but narrower (lacks FTS, SPM, conformal, density/Frechet, sparse PACE, advisor/provenance), FDApy is narrow (representation + MFPCA + simulation), the R ecosystem is fragmented across fda / fda.usc / refund / funData / tidyfun with no single package spanning the surface, and Matlab fdaM/PACE is partially maintained. Ground EVERY peer claim in paper/comparison_evidence.md (Phase 87) and cite Table~\ref{tab:comparison}; do NOT re-assert peer facts from memory. State fdars's position honestly: the only Python library spanning all comparison dimensions WITH sklearn compatibility AND a grounded advisor + scientific provenance -- breadth via \nsubmodules / \npubliccallables macros. NO benchmark claims.

    (2) A brief FDA-background paragraph: 3-5 sentences on what functional data analysis is (curves as the unit of observation, smoothing/basis representation, FPCA, registration), citing canonical references via \citep{...} using cite-keys that EXIST in refs.bib (e.g. Ramsay & Silverman if present -- verify the key before citing; only cite keys that resolve). Keep brief.

    (3) A short contributions/paper-roadmap paragraph pointing forward to the design, data-representation, capability-tour, and advisor/provenance sections.

    HARD rules: every \citep key MUST exist in refs.bib (an unresolved key breaks the bibliography); no hardcoded count integers (use macros); no benchmark/performance claims; no \today/timestamp; no JOSS/JSS promise; peer-review-safe / under-claiming.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/intro.tex && grep -qF '\section{Introduction}' paper/sections/intro.tex && ! grep -qi 'Placeholder' paper/sections/intro.tex && grep -qF 'Table~\ref{tab:comparison}' paper/sections/intro.tex && grep -qE 'scikit-fda' paper/sections/intro.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/intro.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/intro.tex && grep -qE '\\nsubmodules|\\npubliccallables' paper/sections/intro.tex && ! grep -qE '\b(30|409)\b' paper/sections/intro.tex && for k in $(grep -oE '\\cite[pt]?\{[^}]+\}' paper/sections/intro.tex | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sed 's/ //g' | sort -u); do grep -qE "@[a-zA-Z]+\{$k," paper/refs.bib || { echo "UNRESOLVED CITE: $k"; exit 1; }; done && echo INTRO_OK</automated>
    <fails_when>intro is missing the section header, the comparison-table citation, or a breadth macro; has a placeholder/benchmark/timestamp/hardcoded count; or cites a \citep key that is absent from refs.bib.</fails_when>
  </verify>
  <done>intro.tex frames the Python FDA gap grounded in comparison_evidence.md + Table~\ref{tab:comparison}, includes a brief FDA-background paragraph whose \citep keys all resolve in refs.bib, cites breadth via macros, and has no benchmark/placeholder/timestamp/hardcoded-count.</done>
  <acceptance_criteria>
    - intro.tex has statement-of-need (peer gap grounded in comparison_evidence.md + Table~\ref{tab:comparison}), a brief FDA-background paragraph with resolving \citep keys, and a forward-pointing contributions paragraph
    - Breadth via macros; no benchmark claim, no hardcoded count, no timestamp; every cite-key resolves in refs.bib
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose claim -> evidence artifact | peer-package claims must trace to comparison_evidence.md, not memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-08 | Spoofing | prose asserts an unverified peer-package or over-claims fdars | high | mitigate | ground every peer claim in comparison_evidence.md + Table~\ref{tab:comparison}; breadth via macros; no benchmarks; Phase 90 GATE-04 human read-through backstop |
| T-88-09 | Tampering | an unresolved \citep key silently breaks the bibliography | medium | mitigate | verify every cite-key exists in refs.bib (automated gate) |
</threat_model>

<verification>
- abstract.tex and intro.tex compile as \input fragments; no placeholder, benchmark, timestamp, or hardcoded count.
- Every intro \citep key resolves in refs.bib; peer claims are grounded in comparison_evidence.md.
- Breadth is stated via coverage macros.
</verification>

<success_criteria>
- The abstract positions fdars honestly (breadth via macros, no benchmarks).
- The Introduction frames the Python FDA gap grounded in Phase 87 evidence + the comparison table, with a brief FDA background whose citations resolve.
</success_criteria>

<output>
Create `.planning/phases/88-front-matter-design-architecture-capability-tour/88-03-SUMMARY.md` when done.
</output>

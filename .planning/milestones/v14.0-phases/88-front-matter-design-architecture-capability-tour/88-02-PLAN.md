---
phase: 88-front-matter-design-architecture-capability-tour
plan: "02"
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - paper/code/gen_snippets.py
  - paper/snippets/basis.tex
  - paper/snippets/depth.tex
  - paper/snippets/fpca.tex
  - paper/snippets/clustering.tex
  - paper/snippets/classification.tex
  - paper/snippets/regression.tex
  - paper/snippets/fts.tex
  - paper/snippets/spm.tex
  - paper/snippets/tolerance.tex
  - paper/snippets/metric.tex
  - paper/snippets/alignment.tex
  - paper/snippets/advisor_diag.tex
  - paper/snippets/sklearn.tex
  - paper/sections/capabilities.tex
autonomous: true
requirements: [MANU-05]
estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Every capability-tour family snippet runs against fdars 0.12.0 and is emitted by gen_snippets.py (never hand-copied)"
    - "gen_snippets.py generate is byte-stable across re-runs (deterministic) and --check passes clean for the full snippet set"
    - "capabilities.tex walks the method families with an \\input of each generated snippet and honest breadth prose grounded in coverage macros + the comparison table"
    - "No capability-tour prose hardcodes a coverage/count integer that a macro derives"
  artifacts:
    - paper/snippets/basis.tex
    - paper/snippets/depth.tex
    - paper/snippets/fpca.tex
    - paper/snippets/clustering.tex
    - paper/snippets/classification.tex
    - paper/snippets/regression.tex
    - paper/snippets/fts.tex
    - paper/snippets/spm.tex
    - paper/snippets/tolerance.tex
    - paper/snippets/metric.tex
    - paper/snippets/alignment.tex
    - paper/snippets/advisor_diag.tex
    - paper/snippets/sklearn.tex
  key_links:
    - "gen_snippets.py SNIPPETS list <-> paper/snippets/*.tex <-> capabilities.tex \\input (single source of truth per snippet)"
    - "fpca.tex PACE branch <-> MANU-05 irregular representation (PyIrregFdata via irreg_fdata_from_lists)"
---

<objective>
Expand the proven Plan 88-01 harness to cover every capability-tour method family with a minimal, executed, deterministic snippet, and author the Capabilities section prose that walks the families breadth-first and \inputs each generated fragment. (MANU-05.)

Purpose: Delivers the breadth-showing capability tour -- the core deliverable of the phase -- with the milestone-critical guarantee that every snippet is executed-script-sourced, not hand-copied.
Output: ~13 additional entries in gen_snippets.py SNIPPETS, ~13 generated paper/snippets/*.tex fragments, and the completed paper/sections/capabilities.tex.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@paper/code/gen_snippets.py
@paper/sections/capabilities.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the core-family snippets (basis, depth, fpca+PACE, clustering, classification, regression) to gen_snippets.py</name>
  <files>paper/code/gen_snippets.py, paper/snippets/basis.tex, paper/snippets/depth.tex, paper/snippets/fpca.tex, paper/snippets/clustering.tex, paper/snippets/classification.tex, paper/snippets/regression.tex</files>
  <precondition>Plan 88-01 committed gen_snippets.py with a working represent entry; fdars 0.12.0 importable; scikit-learn present in the venv.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Validated Snippets #2 basis, #3 depth/outliers, #4 FPCA+PACE, #5 clustering, #6 classification, #7 SoF regression -- the EXACT validated call shapes and gotchas; Pitfall 2 dtype, Pitfall 3 signatures, Pitfall 4 float drift)
    - /home/simonm/projects/rust/pyfda/paper/code/gen_snippets.py (the SNIPPETS list to extend + _run/_render conventions)
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Dataset Suitability -- dataset shapes/orientations)
  </read_first>
  <action>
    Append six SNIPPETS entries to gen_snippets.py using the VERIFIED bodies from RESEARCH SS Validated Snippets. Every array passed to a native call MUST be .astype(np.float64) (labels .astype(np.int64)) and contiguous (Pitfall 2). Every snippet prints shapes / dict keys / counts / rounded scalars only -- never a raw float array (Pitfall 4). Use data_path("...") for CSV reads.

    - basis: fdata_to_basis_1d(X[:5], ARG, n_basis=8, basis_type="bspline") returns a (coefs, nbasis) TUPLE (unpack it); smooth_basis_gcv(...) returns a dict -- print coefs.shape, nbasis, list(sm.keys()), sm["fitted"].shape (Snippet #2 gotcha).
    - depth: use Fdata.depth(method="fraiman_muniz") (NOT module-level fraiman_muniz_1d, which needs contiguous float64 both-args -- Snippet #3 gotcha); print depths.shape + int(np.argmax(depths)); then outliers.muod(X) (NO argvals) printing out["shape_outliers"] / out["amplitude_outliers"].
    - fpca: fd.to_pc(n_comp=3) -> print pc["scores"].shape + np.round(pc["singular_values"], 2); THEN the sparse/irregular PACE branch (this doubles as the MANU-05 irregular snippet): np.random.seed(42); build argvals_list/values_list ragged lists; irreg = fdars.pace_fpca.irreg_fdata_from_lists(argvals_list, values_list); pace = fdars.pace_fpca.pace_fpca(irreg, ncomp=2); print pace["scores"].shape + pace["ncomp"] (Snippet #4).
    - clustering: fdars.clustering.kmeans_fd(X, ARG, k=3, seed=42); print np.bincount(km["cluster"]).tolist() + km["converged"] (NOT the float tot_withinss -- Snippet #5).
    - classification: read phoneme via data_path("phoneme.csv"); Xp = ph.values.astype(np.float64); build int64 label ndarray via a dict lookup on the row index (labels MUST be int64 ndarray -- lists AND uint32 both fail, Snippet #6 gotcha); fclassif_knn(Xp[:100], y[:100], ncomp=3, k=5) (NO argvals); print res["accuracy"].
    - regression: read tecator via data_path("tecator.csv"); Xt = tec.iloc[:, :100].values.astype(np.float64); yfat = tec["fat"].values.astype(np.float64); sof = fdars.regression.fregre_lm(Xt, yfat, n_comp=5); print round(sof["r_squared"], 4) (Snippet #7). Mention FoF availability in prose (Plan task 3), not a snippet (no natural functional response in docs/data/).

    Run generate mode; run twice more to confirm byte-stability; commit gen_snippets.py + the six new fragments. If any snippet errors, fix the call shape against the RESEARCH validated body -- do NOT weaken a snippet to a stub.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py && for f in basis depth fpca clustering classification regression; do test -f paper/snippets/$f.tex && grep -qF 'style=fdarsinput' paper/snippets/$f.tex && ! grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/snippets/$f.tex || { echo "BAD: $f"; exit 1; }; done && grep -qF 'PACE scores' paper/snippets/fpca.tex && grep -qF 'accuracy' paper/snippets/classification.tex && cp paper/snippets/fpca.tex /tmp/fpca_88_02.tex && PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py && diff -q /tmp/fpca_88_02.tex paper/snippets/fpca.tex && rm -f /tmp/fpca_88_02.tex && PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py --check && echo CORE_SNIPPETS_OK</automated>
    <fails_when>any core-family snippet errors under fdars, a fragment is missing/has a timestamp, the PACE (irregular) output or classification accuracy is absent, a re-run is not byte-stable, or --check drifts.</fails_when>
  </verify>
  <done>Six core-family snippets (basis, depth, fpca+PACE, clustering, classification, regression) run against fdars, emit deterministic fragments incl. the PACE irregular-representation output, and pass --check. gen_snippets.py + fragments committed.</done>
  <acceptance_criteria>
    - Each of basis/depth/fpca/clustering/classification/regression.tex is machine-generated with fdarsinput + (where illustrative) fdarsoutput blocks and no timestamp
    - fpca.tex contains the PACE (PyIrregFdata) branch output; classification.tex uses an int64 label ndarray; regression uses fregre_lm with n_comp=5
    - Re-run byte-stable; gen_snippets.py --check passes clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the remaining-family snippets (fts, spm, tolerance, metric, alignment, advisor_diag, sklearn)</name>
  <files>paper/code/gen_snippets.py, paper/snippets/fts.tex, paper/snippets/spm.tex, paper/snippets/tolerance.tex, paper/snippets/metric.tex, paper/snippets/alignment.tex, paper/snippets/advisor_diag.tex, paper/snippets/sklearn.tex</files>
  <precondition>Task 1 committed; scikit-learn importable (the sklearn Pipeline snippet needs sklearn.pipeline/linear_model/model_selection).</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Validated Snippets #8 FTS, #9 SPM, #10 conformal/tolerance, #11 metric, #12 alignment, #13 advisor build_diagnostics, #14 sklearn Pipeline -- EXACT shapes; the "could NOT get to a clean one-liner" note for frechet/density; Pitfall 3 signatures, Pitfall 6 the-two-28s, Pitfall 7 do-not-run-advise)
    - /home/simonm/projects/rust/pyfda/paper/code/gen_snippets.py (SNIPPETS list from Task 1)
  </read_first>
  <action>
    Append seven more SNIPPETS entries using the VERIFIED bodies. Same dtype/float/data_path rules as Task 1.

    - fts: read canadian_weather via data_path("canadian_weather.csv"); Xfts = cw.T.values.astype(np.float64); ARGd = np.arange(365, float)+1.0; fdars.fts.ftsm_forecast(Xfts, ARGd, h=3, ncomp=3) takes RAW data+argvals (NOT a fitted model -- Snippet #8 gotcha); print fc["forecast"].shape + fc["h"].
    - spm: p1 = fdars.spm.spm_phase1(X[:46], ARG, ncomp=3, alpha=0.05); mon = fdars.spm.spm_monitor(p1["mean"], p1["loadings"], p1["weights"], p1["eigenvalues"], p1["t2_limit"], p1["spe_limit"], X[46:], ARG) -- components UNPACKED as positional args, NOT the dict (Snippet #9 gotcha); print int(mon["t2_alarm"].sum()) + int(mon["spe_alarm"].sum()) (ints, not float T2).
    - tolerance: fdars.tolerance.fpca_tolerance_band(X, ncomp=3, nb=200, coverage=0.95, seed=42) -- NO argvals, NO alpha kwarg (Snippet #10 gotcha); print list(tol.keys()) + tol["half_width"].shape. This represents the conformal/tolerance family.
    - metric: fdars.metric.lp_self_1d(X, ARG, p=2.0) -> print D.shape; fdars.metric.dtw_self_1d(X[:10]) (NO argvals) -> print round(float(Ddtw[0,1]), 2). This represents the density/Frechet/metric family (frechet_mean and density_fda.wasserstein_barycenter are plumbing-heavy -- covered in prose citing the comparison table, per the RESEARCH note; do NOT force a broken snippet).
    - alignment: fdars.alignment.karcher_mean(X[:10], ARG) (10-obs subset for speed); print list(km_res.keys()) + km_res["converged"] + km_res["aligned_data"].shape (Snippet #12). karcher_mean converged is deterministic-shaped; print keys+shape (avoid a bare float).
    - advisor_diag: fd.to_pc(n_comp=3) -> diag = fdars.advisor.build_diagnostics(pc, "fpca", argvals=ARG) (LLM-free, deterministic); print np.round(diag["cumulative_variance_explained"], 3).tolist() (Snippet #13). Do NOT call advise() (Pitfall 7 -- needs an LLM). This is the advisor family's runnable representative; the full advisor+provenance narrative is Plan 88-04.
    - sklearn: from fdars.sklearn._skeletons import FPCATransformer (NOT re-exported at fdars.sklearn top level); build Pipeline([("fpca", FPCATransformer(n_components=5)), ("ridge", RidgeCV())]) -- param is n_components NOT n_comp (Snippet #14 gotcha); cross_val_score(pipe, Xt, yfat, cv=5, scoring="r2"); print round(float(scores.mean()), 3).

    Run generate; run twice more for byte-stability; commit gen_snippets.py + the seven fragments.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py && for f in fts spm tolerance metric alignment advisor_diag sklearn; do test -f paper/snippets/$f.tex && grep -qF 'style=fdarsinput' paper/snippets/$f.tex && ! grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/snippets/$f.tex || { echo "BAD: $f"; exit 1; }; done && grep -qF 'forecast' paper/snippets/fts.tex && grep -qF 'build_diagnostics' paper/snippets/advisor_diag.tex && grep -qF 'FPCATransformer' paper/snippets/sklearn.tex && ! grep -qF 'advise(' paper/snippets/advisor_diag.tex && cp paper/snippets/sklearn.tex /tmp/sk_88_02.tex && PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py && diff -q /tmp/sk_88_02.tex paper/snippets/sklearn.tex && rm -f /tmp/sk_88_02.tex && PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py --check && echo REST_SNIPPETS_OK</automated>
    <fails_when>any remaining-family snippet errors under fdars, a fragment is missing/timestamped, the advisor snippet calls advise(), the sklearn snippet uses n_comp, a re-run is not byte-stable, or --check drifts.</fails_when>
  </verify>
  <done>Seven more family snippets (fts, spm, tolerance, metric, alignment, advisor_diag, sklearn) run against fdars, emit deterministic fragments, advisor uses LLM-free build_diagnostics only, sklearn uses n_components, and --check passes clean. Committed.</done>
  <acceptance_criteria>
    - fts/spm/tolerance/metric/alignment/advisor_diag/sklearn.tex are machine-generated, timestamp-free, byte-stable
    - advisor_diag.tex uses build_diagnostics (never advise()); sklearn.tex imports FPCATransformer from _skeletons and uses n_components
    - gen_snippets.py --check passes clean for the full ~14-snippet set
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author capabilities.tex -- the breadth-first family walk with per-family \input</name>
  <files>paper/sections/capabilities.tex</files>
  <precondition>All snippet fragments generated and committed (Tasks 1-2); the represent \input already present from Plan 88-01.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-CONTEXT.md (SS Capability Tour Coverage -- one representative per family, breadth over depth, cite the comparison table + coverage macros)
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Validated Snippets family list; SS Statement-of-Need Facts for the honest breadth framing; the frechet/density prose-not-snippet note)
    - /home/simonm/projects/rust/pyfda/paper/sections/capabilities.tex (the Plan 88-01 intro + represent \input to build on)
    - /home/simonm/projects/rust/pyfda/paper/coverage_counts.tex (the macros to cite: \nsubmodules, \npubliccallables, \nsklearnestimators -- never hardcode the integers)
  </read_first>
  <action>
    Complete paper/sections/capabilities.tex as a breadth-first walk of the method families, keeping the Plan 88-01 intro paragraph and the represent \input. For each family add a short subsection (or paragraph with a bold lead-in) of 2-4 sentences describing what fdars offers in that family (grounded in the fdars-capabilities/_capability_map.json purposes -- accurate, no over-claim) immediately followed by \input{snippets/<family>} in this order: represent (already present), basis, depth, fpca (note the fpca snippet also demonstrates the sparse/irregular PACE path), clustering, classification, regression, fts, spm, tolerance, metric, alignment, advisor_diag.

    Frame the breadth honestly: cite \nsubmodules public submodules and \npubliccallables public callables (macros, NOT literal integers), and reference Table~\ref{tab:comparison} for the peer positioning. For the density/Frechet family, DO include the metric snippet as the family representative and add one sentence noting fdars additionally provides density-FDA (LQD/Wasserstein) and Frechet regression/means (cite the comparison table) -- do NOT fabricate a snippet for them (per the RESEARCH note). For regression, note in prose that function-on-function regression (fof_regression) is also available (SoF is the runnable representative). Mention the sklearn estimator layer briefly here citing \nsklearnestimators estimators passing check_estimator, but keep the detailed sklearn Pipeline snippet (sklearn.tex) -- \input it either here (as the "sklearn compatibility" family) or note it belongs to the design section; planner's discretion, but if \input-ed here it must be the sklearn.tex fragment.

    HARD rules: no hardcoded coverage/count integer that a macro derives (no literal 30, 409, 28); no \today/timestamp; no "Placeholder" text; every \input path must resolve to a committed paper/snippets/*.tex. Prose is peer-review-safe / under-claiming.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ! grep -qi 'Placeholder' paper/sections/capabilities.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/capabilities.tex && grep -qF 'Table~\ref{tab:comparison}' paper/sections/capabilities.tex && grep -qE '\\nsubmodules|\\npubliccallables' paper/sections/capabilities.tex && ! grep -qE '\b(30|409)\b' paper/sections/capabilities.tex && for s in represent basis depth fpca clustering classification regression fts spm tolerance metric alignment advisor_diag; do grep -qF "\\input{snippets/$s}" paper/sections/capabilities.tex || { echo "MISSING INPUT: $s"; exit 1; }; done && for s in $(grep -oE 'snippets/[a-z_]+' paper/sections/capabilities.tex | sed 's#snippets/##' | sort -u); do test -f paper/snippets/$s.tex || { echo "UNRESOLVED: $s"; exit 1; }; done && echo CAPABILITIES_OK</automated>
    <fails_when>capabilities.tex has a placeholder/timestamp/hardcoded count; is missing any family \input; cites no coverage macro or the comparison table; or an \input path does not resolve to a committed fragment.</fails_when>
  </verify>
  <done>capabilities.tex walks all method families breadth-first, each with prose + a resolving \input of its generated snippet, cites coverage macros + Table~\ref{tab:comparison}, mentions density/Frechet + FoF in prose (no fabricated snippet), and contains no hardcoded counts, placeholders, or timestamps.</done>
  <acceptance_criteria>
    - Every family (represent, basis, depth, fpca, clustering, classification, regression, fts, spm, tolerance, metric, alignment, advisor_diag) has prose + a resolving \input
    - Breadth is cited via \nsubmodules/\npubliccallables macros + Table~\ref{tab:comparison}; density/Frechet and FoF covered in prose per the RESEARCH note
    - No hardcoded count integer, no placeholder, no timestamp; every \input resolves to a committed snippet
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snippet source -> exec() | new SNIPPETS entries exec author-written fdars code (not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-05 | Tampering | a tour snippet silently goes stale vs the live API | high | mitigate | every snippet is emitted by gen_snippets.py; --check drift gate (Plan 88-01) + widened path filter re-run on API change |
| T-88-06 | Information Disclosure | advisor snippet leaks a key / hits an LLM | low | mitigate | advisor_diag uses LLM-free build_diagnostics only; advise() never executed (Pitfall 7) |
| T-88-07 | Tampering | float output drift causes false CI diffs | medium | mitigate | snippets print shapes/keys/rounded scalars; byte-stable re-run enforced (Pitfall 4) |
</threat_model>

<verification>
- gen_snippets.py runs all ~14 family snippets against fdars 0.12.0 without error and is byte-stable on re-run.
- gen_snippets.py --check passes clean for the full set.
- capabilities.tex \inputs every family snippet, cites breadth via macros + the comparison table, and has no hardcoded counts/placeholders/timestamps.
</verification>

<success_criteria>
- Every capability-tour family has an executed-script-sourced, deterministic snippet (never hand-copied) emitted by the harness.
- capabilities.tex is a complete breadth-first family walk with resolving \inputs and honest macro-grounded breadth prose.
- The full snippet set passes the drift gate.
</success_criteria>

<output>
Create `.planning/phases/88-front-matter-design-architecture-capability-tour/88-02-SUMMARY.md` when done.
</output>

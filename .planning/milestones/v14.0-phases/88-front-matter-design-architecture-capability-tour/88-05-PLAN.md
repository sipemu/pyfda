---
phase: 88-front-matter-design-architecture-capability-tour
plan: "05"
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - paper/sections/availability.tex
  - paper/sections/conclusion.tex
autonomous: true
requirements: [MANU-07]
estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The availability section states PyPI install, the plot extra, Python 3.9-3.14 support, and the license"
    - "The conclusion frames breadth + provenance + advisor honestly (roadmap-agnostic), with NO benchmark claims and NO JOSS/JSS venue promises"
    - "No hardcoded coverage/count integers; no timestamps"
  artifacts:
    - paper/sections/availability.tex
    - paper/sections/conclusion.tex
  key_links:
    - "availability.tex Python-version range <-> the abi3-py39 / CI 3.9-3.14 fact"
    - "conclusion.tex breadth framing <-> coverage macros (no hardcoded counts)"
---

<objective>
Write the Availability & installation section and the Conclusion. (MANU-07.)

Purpose: Availability tells a reader how to install and run fdars; the Conclusion closes the paper with an honest, under-claiming, roadmap-agnostic summary.
Output: paper/sections/availability.tex and paper/sections/conclusion.tex, filling the stubs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: availability.tex -- PyPI, extras, Python 3.9-3.14, license</name>
  <files>paper/sections/availability.tex</files>
  <precondition>Plan 88-01 wired \input{sections/availability} (already present pre-phase).</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Availability Facts -- Python 3.9-3.14, plot extra, PyPI wheels; abi3-py39)
    - /home/simonm/projects/rust/pyfda/.claude/CLAUDE.md (Runtime + Platform Requirements: CPython 3.9-3.13 CI, abi3-py39, wheels Linux/macOS/Windows, plot extra = matplotlib)
    - /home/simonm/projects/rust/pyfda/pyproject.toml (the exact package name, extras, license, Python requires -- verify before stating)
  </read_first>
  <action>
    Write paper/sections/availability.tex keeping \section{Availability and Installation}. State:
    - Install from PyPI: pip install fdars (verify the distribution name in pyproject.toml before writing it); the optional plot extra (pip install fdars[plot], matplotlib) and any sklearn extra if present.
    - Python support: 3.9-3.14 (abi3-py39 stable ABI); prebuilt wheels for Linux/macOS/Windows per CLAUDE.md. Verify the exact upper bound against pyproject.toml / CI before committing to "3.14".
    - License: state the project license as declared in pyproject.toml / LICENSE (verify; do not guess -- read the file).
    - Optionally a one-line source/repository pointer.

    Keep it brief and factual. Verify EVERY concrete fact (package name, extras, Python range, license) against pyproject.toml / CLAUDE.md before writing -- do not invent an extra or a license.

    HARD rules: no \today/timestamp; no hardcoded coverage/count integers (Python-version numbers like 3.9/3.14 and extra names are legitimate factual literals, NOT coverage counts -- those are fine); no benchmark claims; no JOSS/JSS promise.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/availability.tex && grep -qF '\section{Availability and Installation}' paper/sections/availability.tex && ! grep -qi 'Placeholder' paper/sections/availability.tex && grep -qiE 'pip install' paper/sections/availability.tex && grep -qiE 'PyPI' paper/sections/availability.tex && grep -qE '3\.9' paper/sections/availability.tex && grep -qiE 'plot' paper/sections/availability.tex && grep -qiE 'licen[sc]e' paper/sections/availability.tex && ! grep -qiE 'benchmark|JOSS|JSS|SoftwareX' paper/sections/availability.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/availability.tex && echo AVAILABILITY_OK</automated>
    <fails_when>availability.tex is missing the section header, the pip/PyPI install, the Python 3.9 range, the plot extra, or the license; has a placeholder/benchmark/venue-promise/timestamp.</fails_when>
  </verify>
  <done>availability.tex states the PyPI install, the plot extra, Python 3.9-3.14 (abi3-py39) wheels, and the license -- every fact verified against pyproject.toml/CLAUDE.md -- with no placeholder/benchmark/venue/timestamp.</done>
  <acceptance_criteria>
    - availability.tex covers PyPI install, plot extra, Python 3.9-3.14, and the (verified) license
    - No placeholder, no benchmark, no venue promise, no timestamp
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: conclusion.tex -- honest, roadmap-agnostic closing</name>
  <files>paper/sections/conclusion.tex</files>
  <precondition>Plan 88-01 created the conclusion.tex stub + wired \input{sections/conclusion}.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-CONTEXT.md (SS Availability & Conclusion -- breadth + provenance + roadmap-agnostic framing; JOSS/JSS are deferred VENUE items, do NOT promise them)
    - /home/simonm/projects/rust/pyfda/paper/coverage_counts.tex (breadth macros for the summary)
  </read_first>
  <action>
    Write paper/sections/conclusion.tex keeping \section{Conclusion}. In 4-7 sentences, summarize fdars honestly: a broad, method-accurate, Rust-accelerated, sklearn-compatible Python FDA library whose distinctive contributions are its breadth (via \nsubmodules/\npubliccallables macros -- NOT literal integers), its grounded AI advisor, and its machine-readable scientific-provenance layer -- differentiators absent from peer FDA packages (cite Table~\ref{tab:comparison}). Keep the framing roadmap-agnostic (do not commit to future features or timelines beyond a light "ongoing development" phrasing).

    HARD rules: NO benchmark/performance claims (milestone lock); NO JOSS/JSS/SoftwareX venue promise (deferred VENUE items); NO hardcoded count integers (use macros); NO \today/timestamp; peer-review-safe / under-claiming.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/sections/conclusion.tex && grep -qF '\section{Conclusion}' paper/sections/conclusion.tex && ! grep -qi 'Placeholder' paper/sections/conclusion.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/conclusion.tex && ! grep -qiE 'JOSS|JSS|SoftwareX' paper/sections/conclusion.tex && grep -qE '\\nsubmodules|\\npubliccallables' paper/sections/conclusion.tex && ! grep -qE '\b(30|409|28)\b' paper/sections/conclusion.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/conclusion.tex && echo CONCLUSION_OK</automated>
    <fails_when>conclusion.tex is missing the section header or a breadth macro; has a placeholder/benchmark/venue-promise/timestamp/hardcoded count.</fails_when>
  </verify>
  <done>conclusion.tex closes the paper honestly (breadth via macros + advisor + provenance, cite Table~\ref{tab:comparison}, roadmap-agnostic) with no benchmark/venue-promise/hardcoded-count/timestamp.</done>
  <acceptance_criteria>
    - conclusion.tex summarizes breadth (macros) + advisor + provenance as differentiators, cites Table~\ref{tab:comparison}, stays roadmap-agnostic
    - No benchmark, no venue promise, no hardcoded count, no timestamp
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| availability facts -> pyproject.toml | install/version/license claims must match the packaging config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-13 | Spoofing | wrong package name / license / Python range in availability | medium | mitigate | verify every fact against pyproject.toml/CLAUDE.md; Phase 90 GATE-04 human read-through |
| T-88-14 | Spoofing | conclusion promises a deferred venue or a benchmark | low | mitigate | automated gate forbids JOSS/JSS/benchmark tokens |
</threat_model>

<verification>
- availability.tex and conclusion.tex compile as \input fragments; no placeholder/benchmark/venue/timestamp.
- Availability facts match pyproject.toml/CLAUDE.md; conclusion breadth via macros.
</verification>

<success_criteria>
- availability.tex: PyPI + plot extra + Python 3.9-3.14 + license, all verified.
- conclusion.tex: honest breadth + advisor + provenance close, roadmap-agnostic, no venue promise.
</success_criteria>

<output>
Create `.planning/phases/88-front-matter-design-architecture-capability-tour/88-05-SUMMARY.md` when done.
</output>

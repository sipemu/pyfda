---
phase: 89-case-studies-reproducible-figures
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/code/casestudy1.py
  - paper/code/gen_figures.py
  - paper/figures/cs1_phoneme_smooth.pdf
  - paper/figures/cs1_phoneme_fpca.pdf
  - paper/sections/casestudy1.tex
  - paper/sections/casestudies.tex
  - Makefile
  - .github/workflows/paper.yml
autonomous: true
requirements: [CASE-01]

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running the case-study pipeline writes paper/figures/cs1_phoneme_smooth.pdf and cs1_phoneme_fpca.pdf; re-running leaves git diff paper/figures/ empty for those two files."
    - "paper/code/casestudy1.py runs against fdars 0.12.0 end-to-end (smooth_basis_gcv + Fdata.to_pc + FPCATransformer/FPCLDAClassifier 5-fold CV) without error and reports the real 88.2% CV accuracy."
    - "paper/sections/casestudies.tex \\input's four per-study subfiles (casestudy1..4); casestudy1.tex \\includegraphics resolves to the committed cs1_*.pdf figures."
    - "gen_figures.py main() invokes casestudy1's figure entry point so study figures ride make paper."
    - "A git diff --exit-code paper/figures/ determinism gate exists in paper.yml (after figure regeneration, before tectonic) and as a local make target."
  artifacts:
    - paper/code/casestudy1.py
    - paper/figures/cs1_phoneme_smooth.pdf
    - paper/figures/cs1_phoneme_fpca.pdf
    - paper/sections/casestudy1.tex
    - paper/sections/casestudies.tex
  key_links:
    - "gen_figures.py main() -> casestudy1.main() -> save_figure -> paper/figures/cs1_*.pdf"
    - "casestudies.tex -> \\input{sections/casestudy1} -> \\includegraphics{figures/cs1_*.pdf}"
    - "paper.yml: gen_figures.py -> git diff --exit-code paper/figures/ -> tectonic"
---

<objective>
Prove the entire case-study machinery on Study 1 (CASE-01, phoneme): a `paper/code/` script runs the
validated smooth + FPCA + classification pipeline against fdars 0.12.0, writes deterministic committed
figures under `paper/figures/`, a per-study narrative `.tex` `\includegraphics` them, `casestudies.tex`
is wired to `\input` all four future per-study subfiles, and the `git diff --exit-code paper/figures/`
determinism gate is established in CI (it does not exist yet — only manual re-run did) so SC-5 is
enforced by machine.

Purpose: This is the tracer. Studies 2–4 (Wave 2) are horizontal expansions that reuse the exact
mechanism proven here — their own scripts, figures, and subfiles — touching NO shared file
(`casestudies.tex`, `gen_figures.py`, `Makefile`, `paper.yml` are all owned and finalized by this plan).

Output: `casestudy1.py`, two committed `cs1_*.pdf` figures, `casestudy1.tex` narrative, `casestudies.tex`
wiring, and the wired determinism gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md
@paper/code/paper_utils.py
@paper/code/gen_figures.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: casestudy1.py — validated smooth+FPCA+classification pipeline → two deterministic committed figures</name>
  <files>paper/code/casestudy1.py, paper/figures/cs1_phoneme_smooth.pdf, paper/figures/cs1_phoneme_fpca.pdf</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 1 validated pipeline + Signature Reference Table + Determinism Pinning Summary — transcribe the exact call shapes and real numbers; do NOT re-derive)
    - paper/code/paper_utils.py (fig, FDARS_COLORS, save_figure with metadata CreationDate=None, data_path)
    - paper/code/gen_figures.py (_FIGURES_DIR + _smoke + save_figure pattern to mirror)
  </read_first>
  <behavior>
    - casestudy1.main() runs without error against fdars 0.12.0 and produces exactly two figures.
    - Re-running main() twice writes byte-identical PDFs (git diff paper/figures/cs1_*.pdf empty).
    - The pipeline reports the real CV accuracy 0.882 (mean of [0.900,0.875,0.813,0.938,0.888]); the
      code must NOT hardcode 0.882 as the source of truth — it computes it from cross_val_score and may
      print the rounded value.
  </behavior>
  <action>
    Create `paper/code/casestudy1.py` following the `gen_figures.py` module pattern (module-top
    `from paper_utils import fig, FDARS_COLORS, save_figure, data_path`; import `fdars`; import
    `FPCATransformer, FPCLDAClassifier` from `fdars.sklearn._skeletons`; `Pipeline` from
    `sklearn.pipeline`; `cross_val_score` from `sklearn.model_selection`; `_FIGURES_DIR =
    Path(__file__).resolve().parent.parent / "figures"`). Use `from __future__ import annotations`,
    snake_case, NumPy docstrings.

    Define `main() -> None` that pins determinism FIRST with `np.random.seed(42)` (per 89-RESEARCH
    Determinism Pinning Summary — no sklearn random_state needed; FPCATransformer/FPCLDAClassifier and
    KFold(5) no-shuffle are deterministic). Then transcribe the §Study 1 validated pipeline verbatim:
    load `pd.read_csv(data_path("phoneme.csv"), index_col=0)`, `X = ph.values.astype(np.float64)`
    (400×256), `ARG = np.arange(1, 257, dtype=np.float64)`; smooth a subset with
    `fdars.basis.smooth_basis_gcv(X[:20], ARG, n_basis=15, basis_type="bspline")`; FPCA via
    `fd = fdars.Fdata(X, argvals=ARG); pc = fd.to_pc(n_comp=4)` (`n_comp`, NOT `n_components` — Signature
    Reference Table). Build int64 labels exactly as in research: sorted-unique lut over `ph.index.tolist()`,
    `y = np.array([lut[l] for l in labels_str], dtype=np.int64)`. Run 5-fold CV with
    `Pipeline([("fpca", FPCATransformer(n_components=4)), ("lda", FPCLDAClassifier(ncomp=4))])` and
    `cross_val_score(pipe, X, y, cv=5, scoring="accuracy")` (GOTCHA: FPCATransformer uses `n_components`,
    FPCLDAClassifier uses `ncomp` — different names; DO NOT pipe FPCATransformer→FPCKNNClassifier which
    double-applies FPCA). Compute `acc = float(cv.mean())` and `print("CV accuracy:", round(acc, 3))`.

    Produce two figures via `fig()` + `save_figure(...)`:
    (1) `cs1_phoneme_smooth.pdf` — a small set of raw phoneme spectra (one color per class from
        FDARS_COLORS, a few curves each) with 3–5 GCV-smoothed curves (`sm["fitted"]`) overlaid,
        x = ARG. Demonstrates smoothing.
    (2) `cs1_phoneme_fpca.pdf` — FPCA scores scatter `pc["scores"][:,0]` vs `pc["scores"][:,1]`
        color-coded by class label. Demonstrates separability.
    Any text annotation that shows a float must use `round(float(x), n)` / `np.round` (never raw repr).
    Call `_FIGURES_DIR.mkdir(parents=True, exist_ok=True)` before the first save. Guard with
    `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/casestudy1.py && test -f paper/figures/cs1_phoneme_smooth.pdf && test -f paper/figures/cs1_phoneme_fpca.pdf && cp paper/figures/cs1_phoneme_smooth.pdf /tmp/cs1s.pdf && cp paper/figures/cs1_phoneme_fpca.pdf /tmp/cs1f.pdf && PYTHONPATH=scripts:paper/code python paper/code/casestudy1.py && diff -q /tmp/cs1s.pdf paper/figures/cs1_phoneme_smooth.pdf && diff -q /tmp/cs1f.pdf paper/figures/cs1_phoneme_fpca.pdf && rm -f /tmp/cs1s.pdf /tmp/cs1f.pdf && echo CS1_DETERMINISTIC_OK</automated>
    <fails_when>casestudy1.py errors against fdars 0.12.0, either figure is missing, OR a re-run produces byte-different PDFs (determinism broken — missing seed, timestamp, or non-Agg backend).</fails_when>
  </verify>
  <done>casestudy1.py runs the validated Study-1 pipeline against fdars 0.12.0, prints the real CV accuracy (~0.882), and writes two byte-stable committed figures cs1_phoneme_smooth.pdf + cs1_phoneme_fpca.pdf.</done>
</task>

<task type="auto">
  <name>Task 2: casestudy1.tex narrative subsection + casestudies.tex wired to \input all four subfiles</name>
  <files>paper/sections/casestudy1.tex, paper/sections/casestudies.tex</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 1 real output numbers + §casestudies.tex structure)
    - paper/sections/casestudies.tex (current Phase-85 stub to replace)
    - paper/sections/design.tex (an existing Phase-88 \section+figure-free narrative to mirror LaTeX conventions and citation style)
  </read_first>
  <action>
    Replace the placeholder body of `paper/sections/casestudies.tex` with the section shell only:
    `\section{Case Studies}`, one framing paragraph (breadth + correctness, honest, no benchmark/
    performance claims; may note each study is reproduced by a committed `paper/code/casestudyN.py`
    script producing committed figures), then exactly four `\input` lines in order:
    `\input{sections/casestudy1}`, `\input{sections/casestudy2}`, `\input{sections/casestudy3}`,
    `\input{sections/casestudy4}`. Do NOT put any `\subsection` or `\includegraphics` in casestudies.tex
    itself — each subfile owns its own `\subsection`. Remove all "Placeholder" language. No `\today`/
    timestamps. This file is finalized here and MUST NOT be edited by Wave-2 plans. (Case-study executed
    snippets are intentionally omitted per 89-RESEARCH Open Question 3 — the committed scripts are the
    provably-current source; the Phase-88 capability tour already carries per-family snippets.)

    Create `paper/sections/casestudy1.tex` (a `\subsection`, NOT a `\section`): title
    "Smoothing, FPCA, and Classification: Phoneme Data"; problem framing (5 phoneme classes ×
    80 log-periodogram curves); the fdars methods used (`smooth_basis_gcv`, `Fdata.to_pc`,
    `FPCATransformer`+`FPCLDAClassifier` under a 5-fold CV) described honestly, noting the analysis is
    reproduced by `paper/code/casestudy1.py`; a `figure` environment with
    `\includegraphics[width=...]{figures/cs1_phoneme_smooth.pdf}` and a second with
    `figures/cs1_phoneme_fpca.pdf`, each with a caption and a `\label`; and an interpretation paragraph
    stating the real numbers from research: FPCA variance explained [0.693, 0.230, 0.054, 0.023]
    (first three PCs 97.7%) and the 5-fold CV accuracy of 88.2%. Report the 88.2% as the honest
    CV figure (not the 0.8725 training-set metric). Use `\citep`/`\citet` only for keys that already
    resolve in paper/refs.bib or paper/refs_manual.bib — do not invent citation keys.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ! grep -qi 'Placeholder' paper/sections/casestudies.tex && grep -qF '\section{Case Studies}' paper/sections/casestudies.tex && for n in 1 2 3 4; do grep -qF "\input{sections/casestudy$n}" paper/sections/casestudies.tex || { echo "MISSING INPUT $n"; exit 1; }; done && ! grep -qF '\subsection' paper/sections/casestudies.tex && ! grep -qF '\includegraphics' paper/sections/casestudies.tex && grep -qF '\subsection' paper/sections/casestudy1.tex && ! grep -qF '\section' paper/sections/casestudy1.tex && grep -qF 'figures/cs1_phoneme_smooth.pdf' paper/sections/casestudy1.tex && grep -qF 'figures/cs1_phoneme_fpca.pdf' paper/sections/casestudy1.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/casestudy1.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/casestudy1.tex paper/sections/casestudies.tex && for k in $(grep -oE '\\cite[pt]?\{[^}]+\}' paper/sections/casestudy1.tex | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sed 's/ //g' | sort -u); do grep -qhE "@[a-zA-Z]+\{$k," paper/refs.bib paper/refs_manual.bib || { echo "UNRESOLVED CITE $k"; exit 1; }; done && echo CS1_NARRATIVE_OK</automated>
    <fails_when>casestudies.tex still contains Placeholder text, is missing any of the four \input lines, or itself carries a \subsection/\includegraphics; casestudy1.tex uses \section instead of \subsection, is missing either \includegraphics path, contains a benchmark claim, an unresolved \citep key, or a date.</fails_when>
  </verify>
  <done>casestudies.tex is a clean four-\input shell (no \subsection/\includegraphics of its own); casestudy1.tex \includegraphics both cs1 figures, references casestudy1.py, and reports the honest 88.2% CV accuracy; all cite keys resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Wire study figures into gen_figures.py main() + establish git diff --exit-code paper/figures/ determinism gate (make + paper.yml)</name>
  <files>paper/code/gen_figures.py, Makefile, .github/workflows/paper.yml</files>
  <read_first>
    - paper/code/gen_figures.py (existing main() calling _smoke(); append study calls here)
    - Makefile (paper-figures / paper-check / paper targets; PAPER_PYTHONPATH var)
    - .github/workflows/paper.yml (the "Regenerate figures (PIPE-02 determinism gate)" step at ~line 44 currently has NO git diff assertion; the checkout at line 27 provides committed figures to diff against)
  </read_first>
  <action>
    In `paper/code/gen_figures.py`: at module top add `import casestudy1` (importable under
    `PYTHONPATH=scripts:paper/code`); in `main()`, after `_smoke()`, call `casestudy1.main()`. Leave a
    marker comment "# Studies 2-4 (Wave 2) each register their own module.main() call here." so the
    Wave-2 plans know where to hook — BUT this plan wires only casestudy1 (Wave-2 plans append their own
    call). Because gen_figures.py is owned solely by this plan, add all four call sites now, importing
    `casestudy2/3/4` lazily inside main() in a try/except ImportError that silently skips a not-yet-created
    module — so this plan's run is green (studies 2-4 absent) and Wave-2 runs light up automatically once
    their modules exist. (This keeps gen_figures.py edited exactly once, avoiding a shared-file race.)

    In `Makefile`: add a new phony target `paper-verify` that regenerates figures then asserts
    byte-stability: `PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/gen_figures.py` followed by
    `git diff --exit-code paper/figures/`. Add `paper-verify` to `.PHONY`. This is the local SC-5 hard gate.

    In `.github/workflows/paper.yml`: immediately AFTER the "Regenerate figures (PIPE-02 determinism
    gate)" step and BEFORE "Assert coverage macros", insert a new step
    "Assert figures byte-stable (PIPE-02 / CASE SC-5 determinism gate)" that runs
    `git diff --exit-code paper/figures/`. This is the gate that 89-RESEARCH/CONTEXT calls "existing" but
    which does not actually exist yet — establish it here so every case-study figure is enforced. Keep
    the tectonic step last. Validate the YAML parses.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; grep -qE 'import casestudy1' paper/code/gen_figures.py && grep -qE 'casestudy1\.main\(\)' paper/code/gen_figures.py && PYTHONPATH=scripts:paper/code python paper/code/gen_figures.py && git diff --exit-code paper/figures/ && grep -vE '^\s*#' Makefile | grep -qE 'paper-verify' && awk '/^paper-verify:/{p=1} p&&/git diff --exit-code paper\/figures/{f=1} END{exit !f}' Makefile && python -c "import yaml; yaml.safe_load(open('.github/workflows/paper.yml'))" && grep -qE 'git diff --exit-code paper/figures' .github/workflows/paper.yml && awk '/Regenerate figures/{r=NR} /git diff --exit-code paper\/figures/{d=NR} /[Ss]etup tectonic/{t=NR} END{exit !(r>0 && d>r && t>d)}' .github/workflows/paper.yml && echo GATE_WIRED_OK</automated>
    <fails_when>gen_figures.py does not import/call casestudy1.main(); running the pipeline leaves paper/figures/ dirty; the Makefile paper-verify target or its git diff assertion is absent; paper.yml does not parse; OR the git diff --exit-code paper/figures/ step is missing or not ordered after figure regeneration and before tectonic.</fails_when>
  </verify>
  <done>gen_figures.py main() calls casestudy1.main() (with lazy try/except hooks reserved for studies 2-4); make paper-verify + a paper.yml step both run git diff --exit-code paper/figures/ after regeneration and before tectonic — SC-5 is machine-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset file → pipeline | CSV read via data_path() (traversal already rejected in paper_utils, T-85-01) |
| generated PDF → committed artifact | non-deterministic metadata could poison git diff gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-01 | Tampering | paper/figures/*.pdf determinism | medium | mitigate | save_figure sets CreationDate=None; Agg backend + seed(42); git diff --exit-code gate (Task 3) fails the build on any byte drift |
| T-89-02 | Information Disclosure | reported accuracy/R² numbers | low | mitigate | numbers computed live from cross_val_score, never invented; narrative reports the honest CV metric not the training-set metric |
| T-89-03 | Denial of Service | package installs | low | accept | no new packages — fdars/sklearn/pandas/numpy/matplotlib already in env (89-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `casestudy1.py` runs against fdars 0.12.0; two figures written; re-run byte-identical.
- `casestudies.tex` \input's four subfiles; `casestudy1.tex` \includegraphics resolve.
- `gen_figures.py main()` calls `casestudy1.main()`; `git diff --exit-code paper/figures/` clean.
- New determinism gate present in Makefile (`paper-verify`) and `paper.yml` (before tectonic).
</verification>

<success_criteria>
- CASE-01 pipeline runs end-to-end deterministically with two committed figures (SC-1, SC-5 for Study 1).
- Case-study LaTeX machinery (casestudies.tex four-\input shell + per-study subfile pattern) proven.
- git diff --exit-code paper/figures/ determinism gate established locally + in CI.
</success_criteria>

<output>
Create `.planning/phases/89-case-studies-reproducible-figures/89-01-SUMMARY.md` when done
</output>
---
phase: 86-single-source-of-truth-wiring-ci-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - .github/workflows/paper.yml
autonomous: true
requirements:
  - GATE-01
user_setup: []

estimate:
  tokens: 22000
  raw_tokens: 14000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "statement: .github/workflows/paper.yml exists, is valid YAML, is path-filtered to paper/** + python/fdars/_capability_map.json + python/fdars/_references_map.json + docs/data/**, and references wtfjoke/setup-tectonic@v4 — verification: local automated (structural)"
    - "statement: paper.yml runs the offline pipeline as a HARD gate (gen_figures → assert_coverage.py --check → gen_refs_bib.py) BEFORE the tectonic PDF compile step — verification: local automated (step-order structural)"
    - "statement: introducing a drift (stale coverage_counts.tex) causes the paper.yml assert_coverage --check step to exit non-zero rather than pass silently — verification: local automated (assert_coverage --check negative test, the same tripwire paper.yml invokes)"
    - "statement: paper.yml compiles paper/paper.tex to a PDF via tectonic with SOURCE_DATE_EPOCH=0 on GitHub Actions — verification: backstop (workflow runs only in GitHub Actions; cannot execute locally)"
  artifacts:
    - .github/workflows/paper.yml
  key_links:
    - "paper.yml assert_coverage step invokes `python paper/code/assert_coverage.py --check` against the committed paper/coverage_counts.tex from Plan 86-01 — the drift tripwire"
    - "step order is load-bearing: pipeline (offline, hard gate) runs before tectonic so a drift fails fast without spending PDF-compile time"
    - "path-filter includes both JSON map paths so a map edit triggers the workflow (GATE-01, research Pitfall 6)"
---

<objective>
Author the standalone `.github/workflows/paper.yml` GitHub Actions workflow that runs the reproducible paper pipeline offline as a hard gate and then compiles the manuscript PDF via tectonic — making the Phase 85 `verification: backstop` tectonic-compile item a live CI check and turning the Plan 86-01 drift tripwires into a CI gate.

Purpose: Satisfy GATE-01 — a drift (stale coverage count, missing ref) or a broken pipeline must fail CI rather than pass silently. This is the phase where the machine-derived-numbers guarantee becomes enforced on every push/PR touching the paper or its source maps.

Output: `.github/workflows/paper.yml` — path-filtered to `paper/**` + the two JSON maps + `docs/data/**`; steps: checkout, setup-python 3.12, pip install matplotlib numpy, gen_figures, assert_coverage --check, gen_refs_bib, setup-tectonic@v4, tectonic compile with SOURCE_DATE_EPOCH=0.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md
@.github/workflows/docs.yml
@.github/workflows/ci.yml
</context>

<artifacts_this_phase_produces>
This plan (86-02) produces `.github/workflows/paper.yml`.

It consumes (does not create) the committed Plan 86-01 artifacts: `paper/code/assert_coverage.py`, `paper/code/gen_refs_bib.py`, `paper/coverage_counts.tex`, `paper/refs.bib`, `paper/code/gen_figures.py` (Phase 85).

Note for downstream: paper.yml does NOT install maturin/fdars in Phase 86 — the two generators are stdlib-only and gen_figures needs only matplotlib+numpy. Phase 89 will extend paper.yml with a `maturin develop` step for the case-study scripts; do NOT add it now.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/paper.yml — path-filtered offline pipeline gate + tectonic compile</name>
  <files>.github/workflows/paper.yml</files>
  <read_first>
    - .planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md (Pattern 3: full paper.yml reference implementation; the "Step ordering is load-bearing" note; Anti-Patterns — no fetch-depth:0, no biber-version, no maturin in Phase 86)
    - .github/workflows/docs.yml (action versions to mirror: actions/checkout@v4, actions/setup-python@v5 with python-version "3.12")
    - .github/workflows/ci.yml (existing workflow conventions)
  </read_first>
  <action>
    Create .github/workflows/paper.yml per the research Pattern 3 reference implementation. Name the workflow `Paper`.

    Trigger on `push` (branches: [main]) and `pull_request`, each with a `paths:` filter listing exactly: `paper/**`, `python/fdars/_capability_map.json`, `python/fdars/_references_map.json`, `docs/data/**` (GATE-01 path-filter — the two JSON map paths are required so a map edit triggers the workflow).

    Single job `paper` on `ubuntu-latest` with these steps in this exact order:
    1. `actions/checkout@v4` (do NOT set `fetch-depth: 0` — no git-history checks here).
    2. `actions/setup-python@v5` with `python-version: "3.12"`.
    3. Install Python deps: `pip install matplotlib numpy` (no fdars/maturin — Phase 86 scripts are stdlib-only, gen_figures needs only matplotlib+numpy).
    4. Regenerate figures: `python paper/code/gen_figures.py` with `PYTHONPATH: scripts:paper/code` in the step env.
    5. Assert coverage macros (drift gate): `python paper/code/assert_coverage.py --check` — this is the HARD gate; a stale committed coverage_counts.tex fails here before tectonic runs.
    6. Regenerate refs.bib: `python paper/code/gen_refs_bib.py`.
    7. `wtfjoke/setup-tectonic@v4` with `github-token: ${{ secrets.GITHUB_TOKEN }}` (do NOT set `biber-version` — that is for biber; natbib+plain BibTeX needs only github-token).
    8. Compile PDF: `tectonic paper/paper.tex` with `SOURCE_DATE_EPOCH: "0"` in the step env (deterministic timestamps).

    The offline pipeline steps (4–6) MUST precede the tectonic steps (7–8) so a drift fails fast.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/paper.yml'))" && echo YAML_VALID && grep -qE 'wtfjoke/setup-tectonic@v4' .github/workflows/paper.yml && grep -qE 'assert_coverage\.py --check' .github/workflows/paper.yml && grep -qE 'gen_refs_bib\.py' .github/workflows/paper.yml && grep -qE 'gen_figures\.py' .github/workflows/paper.yml && grep -qE 'tectonic paper/paper\.tex' .github/workflows/paper.yml && grep -qE 'SOURCE_DATE_EPOCH' .github/workflows/paper.yml && grep -qE 'python/fdars/_capability_map\.json' .github/workflows/paper.yml && grep -qE 'python/fdars/_references_map\.json' .github/workflows/paper.yml && grep -qE 'paper/\*\*' .github/workflows/paper.yml && grep -qE 'docs/data/\*\*' .github/workflows/paper.yml && ! grep -qE 'fetch-depth' .github/workflows/paper.yml && ! grep -qE 'biber-version' .github/workflows/paper.yml && ! grep -qE 'maturin' .github/workflows/paper.yml && echo PAPER_YML_OK</automated>
    <fails_when>paper.yml is not valid YAML, is missing setup-tectonic@v4, any of the three pipeline script invocations, the tectonic compile, SOURCE_DATE_EPOCH, either JSON map path-filter, the paper/** or docs/data/** filters, OR it contains a forbidden fetch-depth/biber-version/maturin token — PAPER_YML_OK is not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && awk '/gen_figures\.py/{f=NR} /assert_coverage\.py --check/{c=NR} /gen_refs_bib\.py/{r=NR} /tectonic paper\/paper\.tex/{t=NR} END{ if (f>0 && f<c && c<r && r<t) {print "STEP_ORDER_OK"; exit 0} else {printf "STEP_ORDER_BAD f=%d c=%d r=%d t=%d\n", f,c,r,t; exit 1} }' .github/workflows/paper.yml</automated>
    <fails_when>The pipeline steps are not ordered gen_figures → assert_coverage --check → gen_refs_bib → tectonic (the offline hard gate does not precede the PDF compile) — STEP_ORDER_OK is not printed.</fails_when>
  </verify>
  <done>.github/workflows/paper.yml exists, is valid YAML, path-filtered to paper/** + both JSON maps + docs/data/**, references wtfjoke/setup-tectonic@v4 with only github-token, runs gen_figures → assert_coverage --check → gen_refs_bib before `tectonic paper/paper.tex` (SOURCE_DATE_EPOCH=0), and contains no fetch-depth/biber-version/maturin.</done>
  <reversibility rating="reversible">Adding a CI workflow file is fully reversible; no state change outside the repo.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove the drift gate fires — negative test on the tripwire paper.yml invokes</name>
  <files>.github/workflows/paper.yml</files>
  <read_first>
    - paper/code/assert_coverage.py (the `--check` mode this task exercises — the exact command paper.yml runs at step 5)
    - .planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md (Pattern 4: "introduce a drift → CI fails"; Validation Architecture GATE-01 negative-gate row)
  </read_first>
  <action>
    No file edit — this task is the demonstrable negative test for GATE-01's "drift fails rather than passes silently" success criterion. The tectonic PDF compile itself runs only in GitHub Actions (backstop), but the drift tripwire that paper.yml invokes at step 5 (`assert_coverage.py --check`) IS runnable locally and is the same command the workflow runs.

    Demonstrate that a deliberate drift makes that step exit non-zero: back up paper/coverage_counts.tex, mutate it (append a stray line so it no longer matches a fresh regeneration), run `assert_coverage.py --check` and confirm a non-zero exit, then restore the file byte-for-byte. Leave the repo clean (git diff on paper/coverage_counts.tex empty) after the demonstration. Record the demonstration outcome in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && cp paper/coverage_counts.tex /tmp/cc_86_02.tex && printf '\n%% drift-injection\n' >> paper/coverage_counts.tex && ( .venv/bin/python paper/code/assert_coverage.py --check ; rc=$? ; cp /tmp/cc_86_02.tex paper/coverage_counts.tex ; rm -f /tmp/cc_86_02.tex ; test $rc -ne 0 && git diff --quiet paper/coverage_counts.tex && echo GATE_FIRES )</automated>
    <fails_when>`assert_coverage.py --check` exits 0 after coverage_counts.tex is deliberately mutated, OR the file is not restored to a clean state afterward — GATE_FIRES is not printed, meaning the CI drift gate would pass silently on drift (GATE-01 negative test failed).</fails_when>
  </verify>
  <done>Running the exact drift-gate command paper.yml invokes (`assert_coverage.py --check`) exits non-zero after a deliberate mutation of the committed coverage_counts.tex, and the file is restored clean afterward — proving GATE-01's drift-fails-loudly criterion locally.</done>
  <reversibility rating="reversible">Read-only demonstration; the mutated file is restored byte-for-byte.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR/push event → paper.yml | GitHub Actions triggers the workflow on path-filtered changes; the workflow runs committed repo scripts against committed maps — no external untrusted input crosses into the build. |
| CI runner → tectonic package download | `wtfjoke/setup-tectonic@v4` downloads tectonic; `github-token` is passed to avoid rate-limiting. Pinned action tag `@v4`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-04 | Tampering | drift passes silently (stale count reaches PDF) | high | mitigate | `assert_coverage.py --check` runs as a hard gate BEFORE tectonic; Task 2 proves it exits non-zero on drift |
| T-86-05 | Denial of Service | tectonic download rate-limited in CI | low | mitigate | `github-token: ${{ secrets.GITHUB_TOKEN }}` supplied to setup-tectonic (research-recommended) |
| T-86-06 | Elevation of Privilege | pinned third-party action `wtfjoke/setup-tectonic@v4` | low | accept | Pinned to major tag `@v4` per milestone lock; carried from Phase 85 research decision |
| T-86-SC | Tampering | npm/pip/cargo installs | low | mitigate | Only `pip install matplotlib numpy` — both are ubiquitous, already in the docs/dev environment; no new/obscure package introduced (no legitimacy checkpoint required) |
</threat_model>

<verification>
- `paper.yml` is valid YAML, path-filtered to all four required paths, references `wtfjoke/setup-tectonic@v4`, and orders the offline pipeline before tectonic (PAPER_YML_OK, STEP_ORDER_OK).
- No forbidden tokens (fetch-depth, biber-version, maturin).
- The drift tripwire paper.yml invokes exits non-zero on a deliberate mutation and the repo is restored clean (GATE_FIRES).
- The tectonic PDF compile is a backstop truth — verified only when the workflow runs in GitHub Actions.
</verification>

<success_criteria>
- GATE-01: standalone `.github/workflows/paper.yml` path-filtered to `paper/**` + both JSON maps + `docs/data/**`; runs the offline pipeline as a hard gate then compiles the PDF via tectonic (`wtfjoke/setup-tectonic@v4`).
- Drift fails loudly: the `assert_coverage.py --check` gate exits non-zero on a deliberate drift (demonstrated locally; enforced in CI).
</success_criteria>

<output>
Create `.planning/phases/86-single-source-of-truth-wiring-ci-gate/86-02-SUMMARY.md` when done
</output>
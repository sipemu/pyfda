---
phase: 87-comparison-table-evidence-file
plan: "02"
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - paper/code/check_comparison.py
  - Makefile
  - .github/workflows/paper.yml
autonomous: true
requirements: [COMP-01, COMP-02]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "check_comparison.py additionally asserts every fdars table cell is ✓ AND is grounded — each dimension's submodule(s) exist with ≥1 public callable in _capability_map.json (COMP-02: fdars column grounded, not asserted)"
    - "check_comparison.py exits non-zero if any fdars dimension cell is not ✓ or its backing submodule is absent/empty in _capability_map.json"
    - "make paper-check runs check_comparison.py as part of the local drift-gate target"
    - "paper.yml runs check_comparison.py in the offline pipeline job before the tectonic PDF compile"
  artifacts:
    - paper/code/check_comparison.py
    - Makefile
    - .github/workflows/paper.yml
  key_links:
    - "check_comparison.py reads python/fdars/_capability_map.json → fdars column grounded"
    - "paper.yml offline job → check_comparison.py runs before tectonic (fails fast, no fdars install needed — stdlib only)"
    - "make paper-check → local mirror of the CI traceability + grounding gate"
  backstop:
    - statement: "The comparison table renders in the compiled PDF via tectonic in CI"
      verification: "CI-only (paper.yml tectonic step); Phase 90 GATE-03 backstops"
---

<objective>
Turn the Plan 87-01 traceability gate into an enforced part of the paper pipeline, and make COMP-02's "fdars column grounded from `_capability_map.json`" a machine assertion rather than a manual claim: extend `check_comparison.py` to verify each fdars cell is ✓ and backed by a real submodule with ≥1 public callable in the capability map, then wire the script into `make paper-check` and the offline `paper.yml` job (before the tectonic compile, alongside the existing drift gates).

Purpose: SC-3 and COMP-02's grounding requirement become live gates that fail CI on drift (e.g. a peer cell loses its evidence, or an fdars submodule is renamed/removed upstream), mirroring the Phase 86 SSoT pattern (offline stdlib gate before tectonic).
Output: extended paper/code/check_comparison.py, Makefile (paper-check target), .github/workflows/paper.yml (new offline step).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/87-comparison-table-evidence-file/87-RESEARCH.md
@.planning/phases/86-single-source-of-truth-wiring-ci-gate/86-02-SUMMARY.md
@Makefile
@.github/workflows/paper.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check_comparison.py with the fdars-column grounding assertion</name>
  <files>paper/code/check_comparison.py</files>
  <read_first>
    - paper/code/check_comparison.py (the traceability gate from Plan 87-01 — extend it, do not rewrite)
    - .planning/phases/87-comparison-table-evidence-file/87-RESEARCH.md (§fdars Column Derivation — the dimension → submodule mapping and the rule "a dimension is ✓ iff ≥1 public callable exists in the named submodule(s)")
    - paper/code/assert_coverage.py (how _capability_map.json is located/loaded and how public (non-underscore) callables are counted)
  </read_first>
  <action>
    Extend paper/code/check_comparison.py (keep it stdlib-only, no fdars import). Add a hard-coded DIMENSION_SUBMODULES mapping of the 14 dimension-label strings → the tuple of _capability_map.json top-level submodule keys that back them, transcribed from RESEARCH §fdars Column Derivation: "Representation / basis smoothing"→(basis, represent, smoothing); "Registration / alignment"→(alignment,); "Depth & outlier detection"→(depth, outliers); "FPCA / covariance / PACE sparse FPCA"→(pace_fpca, covariance); "Clustering"→(clustering,); "Classification"→(classification, shapelet); "Functional regression (SoF / FoF)"→(regression, scalar_on_function, famm); "Functional time series"→(fts,); "Statistical process monitoring"→(spm,); "Inference / hypothesis testing"→(inference,); "Conformal prediction & tolerance bands"→(conformal, tolerance); "Density / Frechet / metric-space"→(density_fda, frechet, metric); "Simulation & datasets"→(simulation, datasets); "Grounded advisor + scientific provenance"→(explain,).

    Load python/fdars/_capability_map.json (locate it relative to the repo root the same way assert_coverage.py does). For each of the 14 dimension rows: (a) assert the fdars column cell parsed from the table is ✓ (covered) — a non-✓ fdars cell is a failure; (b) assert grounding: at least one submodule in that dimension's DIMENSION_SUBMODULES tuple exists as a top-level key in the map AND has ≥1 public (non-underscore) callable. Collect misses. Print each grounding miss as "UNGROUNDED: <dimension> (submodules=<tuple>)" and each non-✓ fdars miss as "FDARS_NOT_CHECK: <dimension>" to stderr; `sys.exit(1)` if any. The existing peer-cell traceability check (Plan 87-01) stays; the success print stays "COMPARISON_TRACE_OK" only when BOTH the peer-traceability and fdars-grounding checks pass.

    Keep the negative-test contract from Plan 87-01 intact (removing a peer evidence section still fires the gate) and deterministic (sorted misses).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python paper/code/check_comparison.py && echo "---grounding-negative---" && cp python/fdars/_capability_map.json /tmp/cm_87_02.json && python - <<'PY'
import json
p='python/fdars/_capability_map.json'
d=json.load(open(p))
# Empty the fts submodule so dimension "Functional time series" loses grounding
d['fts']={}
json.dump(d, open(p,'w'))
PY
( python paper/code/check_comparison.py ; rc=$? ; cp /tmp/cm_87_02.json python/fdars/_capability_map.json ; rm -f /tmp/cm_87_02.json ; test $rc -ne 0 && git diff --quiet python/fdars/_capability_map.json && echo GROUNDING_GATE_FIRES )</automated>
    <fails_when>check_comparison.py exits non-zero on the unmodified repo, OR does not exit non-zero (GROUNDING_GATE_FIRES not printed) when the fts submodule is emptied in _capability_map.json, OR leaves _capability_map.json modified after the negative test.</fails_when>
  </verify>
  <acceptance_criteria>
    - check_comparison.py still exits 0 (COMPARISON_TRACE_OK) on the committed repo, now covering both peer-traceability and fdars-grounding.
    - A DIMENSION_SUBMODULES mapping transcribed from RESEARCH §fdars Column Derivation is present.
    - Emptying/removing an fdars-backing submodule in _capability_map.json makes the script exit non-zero (grounding gate fires); a non-✓ fdars cell also fails.
    - _capability_map.json restored byte-for-byte after the negative test.
  </acceptance_criteria>
  <done>COMPARISON_TRACE_OK on the committed repo covering peer-traceability + fdars-grounding; GROUNDING_GATE_FIRES when the fts submodule is emptied (map restored).</done>
</task>

<task type="auto">
  <name>Task 2: Wire check_comparison.py into make paper-check and paper.yml (before tectonic)</name>
  <files>Makefile, .github/workflows/paper.yml</files>
  <read_first>
    - Makefile (§paper targets — the paper-check target and the PAPER_PYTHONPATH convention, lines ~33–54)
    - .github/workflows/paper.yml (existing offline step order: gen_figures → assert_coverage --check → gen_refs_bib --check → gen_refs_bib → setup tectonic → tectonic compile)
    - .planning/phases/86-single-source-of-truth-wiring-ci-gate/86-02-SUMMARY.md (the load-bearing step-order pattern: offline gates before tectonic)
  </read_first>
  <action>
    Makefile: add a line to the existing `paper-check` target that runs `PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/check_comparison.py` alongside the existing assert_coverage/gen_refs_bib --check lines. Do not create a new target or a new .PHONY entry — extend the existing paper-check recipe so `make paper-check` exercises the traceability + grounding gate. Do NOT add check_comparison.py to the `paper:` generation target (it is a checker, not a generator, matching how --check gates live only in paper-check).

    paper.yml: add a new offline step named e.g. "Check comparison traceability + fdars grounding (COMP)" that runs `python paper/code/check_comparison.py`. Place it AFTER the existing `gen_refs_bib.py` steps and BEFORE the `Setup tectonic` step, so it runs in the offline stdlib phase and fails fast before the PDF compile (same load-bearing ordering as the Phase 86 gates). Do not add maturin/fdars install — the script is stdlib-only. Keep the existing path-filter and steps unchanged otherwise.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -vE '^\s*#' Makefile | grep -qE 'check_comparison\.py' && awk '/^paper-check:/{p=1} p&&/check_comparison\.py/{f=1} /^paper:/{p=0} END{exit !f}' Makefile && ! awk '/^paper:.*paper-figures/{print}' Makefile | grep -qE 'check_comparison' && python -c "import yaml; yaml.safe_load(open('.github/workflows/paper.yml'))" && grep -qE 'check_comparison\.py' .github/workflows/paper.yml && awk '/gen_refs_bib\.py/{g=NR} /check_comparison\.py/{c=NR} /[Ss]etup tectonic|setup-tectonic/{t=NR} END{exit !(g>0 && c>g && t>c)}' .github/workflows/paper.yml && ! grep -qE 'maturin' .github/workflows/paper.yml && echo WIRED_OK</automated>
    <fails_when>check_comparison.py is absent from the paper-check recipe or from paper.yml, is wrongly added to the `paper:` generation target, runs after the tectonic step in paper.yml, or a maturin install was introduced; or paper.yml is not valid YAML.</fails_when>
  </verify>
  <acceptance_criteria>
    - `make paper-check` invokes check_comparison.py (extended recipe, no new target); the `paper:` generation target is unchanged.
    - paper.yml runs check_comparison.py as an offline step after gen_refs_bib and before Setup tectonic; still valid YAML; no maturin install added.
    - Existing paper.yml path-filter and prior steps unchanged.
  </acceptance_criteria>
  <done>WIRED_OK: check_comparison.py runs in make paper-check and in paper.yml after gen_refs_bib and before Setup tectonic; paper.yml valid YAML; no maturin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _capability_map.json → check_comparison.py | Read-only ground truth for the fdars column; risk is an upstream submodule rename silently ungrounding a cell |
| check_comparison.py → paper.yml (CI gate) | The offline stdlib gate that must fail fast before tectonic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-04 | Tampering | fdars cell ✓ without a real callable in the map | high | mitigate | Task 1 grounding assertion (submodule exists + ≥1 public callable); negative test empties fts submodule and confirms the gate fires |
| T-87-05 | Denial of service (build) | traceability gate not enforced in CI | medium | mitigate | Task 2 wires check_comparison.py into paper.yml before tectonic and into make paper-check; step-order verify asserts placement |
| T-87-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed — stdlib-only checker; no maturin/fdars in the offline job |
</threat_model>

<verification>
- Task 1: check_comparison.py exits 0 on the committed repo (COMPARISON_TRACE_OK) covering peer-traceability + fdars-grounding; emptying the fts submodule fires the gate and the map is restored (GROUNDING_GATE_FIRES).
- Task 2: check_comparison.py wired into make paper-check and into paper.yml before tectonic; paper.yml still valid YAML; no maturin (WIRED_OK).
- PDF render of the table remains CI-only (tectonic) — Phase 90 GATE-03 backstop.
</verification>

<success_criteria>
- COMP-02: the fdars column is grounded from _capability_map.json by a machine gate (check_comparison.py grounding assertion), not asserted by hand.
- SC-3 (traceability) and COMP-02 (grounding) are enforced in CI (paper.yml) and locally (make paper-check), failing fast before the tectonic PDF compile.
</success_criteria>

<output>
Create `.planning/phases/87-comparison-table-evidence-file/87-02-SUMMARY.md` when done
</output>

---
phase: 87-comparison-table-evidence-file
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/comparison_evidence.md
  - paper/sections/comparison_table.tex
  - paper/sections/comparison.tex
  - paper/paper.tex
  - paper/code/check_comparison.py
autonomous: true
requirements: [COMP-01, COMP-02]

estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "paper/comparison_evidence.md records package/version/URL/access-date + per-dimension justification for scikit-fda, FDApy, fda, fda.usc, refund, funData, tidyfun, fdaM, PACE"
    - "paper/comparison_evidence.md marks scikit-fda and fda.usc as spot-checked against PyPI/CRAN with the checked version + date"
    - "paper/sections/comparison_table.tex is a booktabs table with 14 capability-dimension rows and 6 package-family columns, coverage marked ✓/partial/— (per COMP-02)"
    - "paper/sections/comparison.tex \\input{sections/comparison_table} with a short prose intro"
    - "paper/paper.tex preamble loads booktabs and adjustbox (per COMP-02, so the table renders)"
    - "paper/code/check_comparison.py parses the table + evidence file and exits non-zero when any non-fdars ✓/partial cell has no matching evidence line (SC-3 gate)"
    - "check_comparison.py exits 0 on the committed, complete evidence+table pair"
  artifacts:
    - paper/comparison_evidence.md
    - paper/sections/comparison_table.tex
    - paper/sections/comparison.tex
    - paper/paper.tex
    - paper/code/check_comparison.py
  key_links:
    - "every non-fdars ✓/partial table cell → a claim line in comparison_evidence.md (enforced by check_comparison.py)"
    - "comparison_table.tex \\input-ed by comparison.tex, which is \\input-ed by paper.tex"
    - "booktabs + adjustbox in paper.tex preamble → comparison_table.tex renders under tectonic"
  backstop:
    - statement: "The comparison table renders in the compiled PDF via tectonic"
      verification: "CI-only (paper.yml tectonic step); no local LaTeX toolchain — Phase 90 GATE-03 backstops"
---

<objective>
Produce the phase's core artifact set end-to-end: a version-stamped `comparison_evidence.md`, the `\input`-ed LaTeX comparison table (14 capability-dimension rows × 6 peer-package-family columns), the `paper.tex` preamble additions the table needs (booktabs, adjustbox), and a stdlib traceability gate (`check_comparison.py`) that proves every non-fdars cell traces to evidence. This is the thinnest complete path that proves the whole artifact chain (evidence → table → traceability gate) works together.

Purpose: This is the highest peer-review-rejection-risk artifact of the milestone. Defensibility comes from the evidence file — every non-fdars claim must be URL + version + date grounded, under-claiming rather than over-claiming. COMP-01 and COMP-02 are both delivered here; Plan 87-02 wires the gate into CI/Make and adds the fdars-column grounding assertion.
Output: paper/comparison_evidence.md, paper/sections/comparison_table.tex, paper/sections/comparison.tex (replaced stub), paper/paper.tex (preamble), paper/code/check_comparison.py.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/87-comparison-table-evidence-file/87-CONTEXT.md
@.planning/phases/87-comparison-table-evidence-file/87-RESEARCH.md
@paper/paper.tex
@paper/sections/comparison.tex
</context>

<tasks>

<task type="tracer">
  <name>Task 1: comparison_evidence.md — transcribe all nine peer-package dossiers from RESEARCH.md</name>
  <files>paper/comparison_evidence.md</files>
  <read_first>
    - .planning/phases/87-comparison-table-evidence-file/87-RESEARCH.md (§Peer-Package Evidence Dossiers — the source of truth; TRANSCRIBE, do NOT re-research or make web calls)
    - .planning/phases/87-comparison-table-evidence-file/87-CONTEXT.md (§Coverage Rubric, §Evidence Standard)
  </read_first>
  <action>
    Create paper/comparison_evidence.md as a peer to paper.tex (NOT under sections/ — a file there would be pulled into the LaTeX source scan). Header: title "Comparison Evidence", a "Generated: 2026-09-08" line, and an "Access date (all URLs): 2026-09-08" line.

    Add one `## <package>` section for EACH of the nine named packages, transcribing the version-stamped facts already verified in RESEARCH.md §Peer-Package Evidence Dossiers (no new web lookups — the versions/URLs/dates are already confirmed): scikit-fda 0.10.1 (PyPI, https://pypi.org/project/scikit-fda/, spot-checked YES), FDApy 1.0.3 (PyPI, https://pypi.org/project/FDApy/, spot-checked YES), fda 6.3.0 (CRAN, https://cran.r-project.org/web/packages/fda/index.html), fda.usc 2.2.0 (CRAN, https://cran.r-project.org/web/packages/fda.usc/index.html, spot-checked YES), refund 0.1-40 (CRAN, https://cran.r-project.org/web/packages/refund/index.html), funData 1.3-9 (CRAN, https://cran.r-project.org/web/packages/funData/index.html), tidyfun 0.2.0 (CRAN, https://cran.r-project.org/web/packages/tidyfun/index.html), fdaM (MATLAB, ~2014, https://www.psych.mcgill.ca/misc/fda/downloads/FDAfuns/Matlab/), PACE (MATLAB v2.17 / R port fdapace 0.6.0, https://anson.ucdavis.edu/~mueller/data/pace.html). Each section carries the package, version, registry, source URL, paper reference where present, and a Spot-checked line.

    Under each package add a "### Claims" markdown table with columns: Dimension, Cell, Justification, Source. Include one row per the 14 capability dimensions (row order per RESEARCH.md §fdars Column Derivation dimension numbering). Copy the Support value (✓ / partial / —) and the one-line evidence note VERBATIM from that package's dossier table in RESEARCH.md, and put the URL/section reference into the Source column. For funData/tidyfun use the combined dossier; for fdaM/PACE use the combined dossier BUT keep fdaM and PACE as two separate `## fdaM` and `## PACE` sections each with its own version/URL (per RESEARCH.md Pitfall 6 — they are distinct packages) while sharing the combined per-dimension cells.

    Use the exact dimension label strings that will appear in the table's first column (Task 2) so the traceability gate (Task 3) can match on them: "Representation / basis smoothing", "Registration / alignment", "Depth & outlier detection", "FPCA / covariance / PACE sparse FPCA", "Clustering", "Classification", "Functional regression (SoF / FoF)", "Functional time series", "Statistical process monitoring", "Inference / hypothesis testing", "Conformal prediction & tolerance bands", "Density / Frechet / metric-space", "Simulation & datasets", "Grounded advisor + scientific provenance".

    Honesty rule (from CONTEXT §Coverage Rubric): resolve ambiguity DOWN. Where the dossier says `partial` or `—` keep it — do not upgrade. For any `—` cell keep an explicit "not found in <source>" style note. Do not invent a ✓ for any peer cell not present in RESEARCH.md.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f paper/comparison_evidence.md && ! test -f paper/sections/comparison_evidence.md && grep -qiE 'Access date.*2026-09-08' paper/comparison_evidence.md && for pkg in 'scikit-fda' 'FDApy' 'fda 6.3.0' 'fda.usc' 'refund' 'funData' 'tidyfun' 'fdaM' 'PACE'; do grep -qF "$pkg" paper/comparison_evidence.md || { echo "MISSING PKG: $pkg"; exit 1; }; done && grep -qiE 'Spot-checked:? *YES' paper/comparison_evidence.md && grep -qF '0.10.1' paper/comparison_evidence.md && grep -qF '2.2.0' paper/comparison_evidence.md && echo EVIDENCE_OK</automated>
    <fails_when>the evidence file is missing, is misplaced under sections/, lacks the 2026-09-08 access date, omits any of the nine named packages, records no spot-check, or drops the transcribed scikit-fda 0.10.1 / fda.usc 2.2.0 versions.</fails_when>
  </verify>
  <acceptance_criteria>
    - paper/comparison_evidence.md exists at paper/ (not under sections/).
    - All nine named packages present, each with version, registry, source URL, and a Spot-checked line; scikit-fda and fda.usc marked spot-checked YES with version + 2026-09-08.
    - Each package has a Claims table with one row per the 14 dimensions using the exact dimension label strings, ✓/partial/— and note transcribed verbatim from RESEARCH.md, Source column carrying the URL/reference.
    - No peer cell over-claims relative to RESEARCH.md; ambiguity resolved DOWN.
  </acceptance_criteria>
  <done>EVIDENCE_OK: paper/comparison_evidence.md exists at paper/ with all nine packages, spot-checks recorded, and per-dimension Claims tables transcribed verbatim from RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 2: comparison_table.tex + comparison.tex + paper.tex preamble (booktabs/adjustbox)</name>
  <files>paper/sections/comparison_table.tex, paper/sections/comparison.tex, paper/paper.tex</files>
  <read_first>
    - .planning/phases/87-comparison-table-evidence-file/87-RESEARCH.md (§Recommended Table Structure — the final cell matrix and LaTeX skeleton to reproduce; §fdars Column Derivation — all 14 fdars cells are ✓)
    - paper/paper.tex (current preamble — booktabs and adjustbox are NOT present yet)
    - paper/sections/comparison.tex (the Phase 85 stub being replaced)
  </read_first>
  <action>
    Add `\usepackage{booktabs}` and `\usepackage{adjustbox}` to paper/paper.tex preamble, placed among the existing `\usepackage` block (after `\usepackage{amsmath}`, before `\input{coverage_counts}`). Do not touch `\date{}`, natbib, or the existing package list otherwise.

    Create paper/sections/comparison_table.tex containing a single `table` float (label `tab:comparison`) wrapping a `booktabs` `tabular` inside `\begin{adjustbox}{max width=\textwidth}...\end{adjustbox}`, reproducing the final cell matrix in RESEARCH.md §Recommended Table Structure → "Proposed Final Cell Matrix". Six columns in this order: Capability, fdars, scikit-fda, FDApy, "R (fda / fda.usc / refund)", "funData / tidyfun", "Matlab (fdaM / PACE)". Fourteen dimension rows in RESEARCH dimension order, using the EXACT row-label strings listed in Task 1's action (so check_comparison.py can match them). Cells use `$\checkmark$` for ✓ (never a bare Unicode ✓ glyph — per RESEARCH Don't-Hand-Roll), `\textit{partial}` for partial, and `---` for —. Copy the ✓/partial/— values verbatim from the Proposed Final Cell Matrix (fdars column is all ✓). Add the two trailing rows after a `\midrule`: a "sklearn-compatible API" row (fdars ✓, scikit-fda ✓, rest —) and a "Language" row (Rust/Python, Python, Python, R, R, MATLAB). Caption defines the ✓/partial/— legend and lists the access-date-stamped peer versions (scikit-fda 0.10.1; FDApy 1.0.3; fda 6.3.0; fda.usc 2.2.0; refund 0.1-40; funData 1.3-9; tidyfun 0.2.0; fdaM ~2014; PACE v2.17); use `\small`. For the fdars version in the caption define `\newcommand{\FdarsVersion}{0.12.0}` at the top of comparison_table.tex and reference `\FdarsVersion` (single point Phase 90 bumps to 0.13.0 for REL-01).

    Replace paper/sections/comparison.tex (the stub) with `\section{Comparison with Related Software}`, a 2–3 sentence prose intro noting fdars is uniquely broad among Python FDA packages and that ambiguity resolves down (under-claiming), a "Table~\ref{tab:comparison}..." sentence, then `\input{sections/comparison_table}`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qE '\\usepackage\{booktabs\}' paper/paper.tex && grep -qE '\\usepackage\{adjustbox\}' paper/paper.tex && awk '/usepackage\{booktabs\}/{b=NR} /usepackage\{adjustbox\}/{a=NR} /begin\{document\}/{d=NR} END{exit !(b>0 && a>0 && b<d && a<d)}' paper/paper.tex && test -f paper/sections/comparison_table.tex && grep -qE '\\begin\{adjustbox\}' paper/sections/comparison_table.tex && grep -qE '\\toprule' paper/sections/comparison_table.tex && grep -qE 'label\{tab:comparison\}' paper/sections/comparison_table.tex && grep -qE '\\newcommand\{\\FdarsVersion\}' paper/sections/comparison_table.tex && ! grep -qP '\x{2713}' paper/sections/comparison_table.tex && grep -qE '\\input\{sections/comparison_table\}' paper/sections/comparison.tex && ! grep -qi 'Placeholder' paper/sections/comparison.tex && test "$(grep -cE '\\checkmark' paper/sections/comparison_table.tex)" -ge 20 && echo TABLE_OK</automated>
    <fails_when>booktabs/adjustbox are absent from or ordered after \begin{document} in paper.tex, comparison_table.tex is missing, lacks the adjustbox wrap / booktabs \toprule / tab:comparison label / \FdarsVersion macro, uses a bare Unicode ✓ glyph, has fewer than 20 checkmark cells, or comparison.tex still contains the placeholder / does not \input the table.</fails_when>
  </verify>
  <acceptance_criteria>
    - paper.tex preamble loads booktabs and adjustbox before \begin{document}; nothing else in the preamble changed.
    - comparison_table.tex is one booktabs table (14 dimension rows + sklearn-API + Language rows × 6 family columns) inside an adjustbox max-width wrap, labelled tab:comparison, cells reproduced verbatim from the RESEARCH Proposed Final Cell Matrix, ✓ rendered as $\checkmark$, — as ---, partial as \textit{partial}.
    - \FdarsVersion macro defined once in comparison_table.tex and used in the caption.
    - comparison.tex replaces the stub with a prose intro + \ref{tab:comparison} + \input{sections/comparison_table}; no "Placeholder" text remains.
  </acceptance_criteria>
  <done>TABLE_OK: booktabs+adjustbox in paper.tex preamble before \begin{document}; comparison_table.tex is the labelled booktabs/adjustbox table; comparison.tex \input-s it with no placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: check_comparison.py — traceability gate (every non-fdars ✓/partial cell has evidence)</name>
  <files>paper/code/check_comparison.py</files>
  <read_first>
    - paper/code/assert_coverage.py (stdlib-only script style, argparse-free `--check` idiom, sys.exit(1) on failure, module docstring convention)
    - paper/sections/comparison_table.tex (the table this parses — produced in Task 2)
    - paper/comparison_evidence.md (the evidence this cross-references — produced in Task 1)
    - .claude/CLAUDE.md (snake_case, NumPy docstrings, `from __future__ import annotations`)
  </read_first>
  <action>
    Create paper/code/check_comparison.py — a stdlib-only script (no numpy, no fdars import; matches the assert_coverage.py convention so it runs in the offline paper.yml job). Begin with `from __future__ import annotations` and a module docstring stating its purpose (SC-3 traceability gate) and usage.

    Behaviour: (1) Parse paper/sections/comparison_table.tex — for each of the 14 dimension data rows, split on unescaped `&` into the six family columns and read each cell's coverage state by matching `\checkmark` → covered, `\textit{partial}` → partial, `---`/`$-$` → none. Column 0 is the dimension label (strip LaTeX). The first data column is fdars (skip it — the fdars column is grounded from _capability_map.json in Plan 87-02, not from evidence). Columns 2–6 are the peer families. Exclude the trailing "sklearn-compatible API" and "Language" rows from the traceability requirement (they are not capability dimensions). (2) Parse paper/comparison_evidence.md — build the set of (package-section, dimension-label, state) claims from each `## <package>` section's Claims table; map the five peer-family table columns to their member packages (scikit-fda→scikit-fda; FDApy→FDApy; "R (fda / fda.usc / refund)"→{fda,fda.usc,refund}; "funData / tidyfun"→{funData,tidyfun}; "Matlab (fdaM / PACE)"→{fdaM,PACE}); a family cell is considered evidenced if AT LEAST ONE member package has a matching (dimension-label, state≠—) claim line. (3) For every peer-family cell whose state is ✓ or partial, assert a matching evidence claim exists; collect all misses. Print each miss as "UNSOURCED: <column> / <dimension> (<state>)" to stderr and `sys.exit(1)` if any; otherwise print "COMPARISON_TRACE_OK" and exit 0.

    Match dimension labels by exact normalized string equality (strip whitespace and LaTeX escapes like `\&`→`&`) — this is why Task 1 and Task 2 use identical dimension-label strings. Keep the script dependency-free and deterministic (sorted iteration over misses).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python paper/code/check_comparison.py && echo "---negative---" && cp paper/comparison_evidence.md /tmp/ce_87_01.md && python - <<'PY'
import re,io
p='paper/comparison_evidence.md'
s=open(p).read()
# Remove the entire scikit-fda section to orphan its non-fdars cells
s2=re.sub(r'\n## scikit-fda\b.*?(?=\n## )', '\n', s, count=1, flags=re.S)
open(p,'w').write(s2)
PY
( python paper/code/check_comparison.py ; rc=$? ; cp /tmp/ce_87_01.md paper/comparison_evidence.md ; rm -f /tmp/ce_87_01.md ; test $rc -ne 0 && git diff --quiet paper/comparison_evidence.md && echo TRACE_GATE_FIRES )</automated>
    <fails_when>check_comparison.py exits non-zero on the committed complete pair, OR does not exit non-zero (TRACE_GATE_FIRES not printed) when the scikit-fda evidence section is removed, OR leaves comparison_evidence.md modified after the negative test.</fails_when>
  </verify>
  <acceptance_criteria>
    - paper/code/check_comparison.py is stdlib-only, starts with `from __future__ import annotations`, has a module docstring, snake_case.
    - Exits 0 (prints COMPARISON_TRACE_OK) on the committed evidence+table pair.
    - Exits non-zero listing UNSOURCED cells when a peer package's evidence is removed; skips the fdars column and the sklearn-API/Language rows.
    - Negative test restores comparison_evidence.md byte-for-byte.
  </acceptance_criteria>
  <done>COMPARISON_TRACE_OK on the committed pair, and TRACE_GATE_FIRES when scikit-fda evidence is removed (file restored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH.md → comparison_evidence.md | Human-verified evidence transcribed; risk is transcription error / silent over-claim |
| comparison_table.tex → paper.tex (LaTeX \input) | Table source compiled by tectonic in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Tampering/Information disclosure | comparison_evidence.md peer cells | high | mitigate | Every non-fdars ✓/partial cell traced to evidence by check_comparison.py (Task 3); ambiguity resolved DOWN per rubric; Phase 90 blocking human review backstops |
| T-87-02 | Denial of service (build) | paper.tex preamble missing booktabs/adjustbox | medium | mitigate | Task 2 adds both packages before \begin{document}; verify asserts ordering |
| T-87-03 | Tampering | Unicode ✓ glyph fails under tectonic | low | mitigate | Task 2 uses `$\checkmark$`; verify negative-greps for a raw ✓ codepoint |
| T-87-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase — peer packages are documented, not dependencies (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Task 1: `paper/comparison_evidence.md` present at paper/ with all nine packages, spot-checks, per-dimension claims tables (EVIDENCE_OK).
- Task 2: booktabs+adjustbox in preamble before document; comparison_table.tex booktabs+adjustbox table labelled tab:comparison; comparison.tex \input-s it, no placeholder (TABLE_OK).
- Task 3: check_comparison.py exits 0 on complete pair and non-zero when evidence removed, restoring the file (COMPARISON_TRACE_OK + TRACE_GATE_FIRES).
- PDF render of the table is CI-only (tectonic in paper.yml) — Phase 90 GATE-03 backstop; not gated locally.
</verification>

<success_criteria>
- COMP-01: comparison_evidence.md records a version-stamped source for every peer-package claim across all nine named packages, ≥1 spot-checked (scikit-fda + fda.usc).
- COMP-02: an \input-ed capability-dimension × peer-package comparison table renders (CI), fdars column all ✓, coverage marked ✓/partial/— honestly.
- SC-3: every non-fdars ✓/partial table cell traces to a claim in comparison_evidence.md, enforced by check_comparison.py.
</success_criteria>

<output>
Create `.planning/phases/87-comparison-table-evidence-file/87-01-SUMMARY.md` when done
</output>

---
phase: 43-svg-fix-learn-represent-align
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/smoothing.svg
  - docs/assets/diagrams/fpca.svg
  - docs/assets/diagrams/elastic-fpca.svg
  - docs/assets/diagrams/basis-representation.svg
  - docs/assets/diagrams/andrews-transformation.svg
  - docs/assets/diagrams/distance-metrics.svg
  - docs/assets/diagrams/pace-fpca.svg
  - docs/assets/diagrams/depth-functions.svg
  - docs/assets/diagrams/elastic-alignment.svg
  - docs/assets/diagrams/banded-alignment.svg
  - docs/assets/diagrams/shift-registration.svg
  - docs/assets/diagrams/ex-sonar-tsrvf.svg
autonomous: true
requirements: [SVGFIX-01, SVGFIX-02, SVGFIX-03, SVGFIX-04]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every flagged learn/represent/align diagram renders on the built site (rsvg-convert PNG) with no overlapping labels and consistent spacing/alignment/sizing (SC1, SVGFIX-01)."
    - "Every diagram in this batch conforms to STYLE_SPEC.md — 720-width viewBox, the .ttl/.sub/.lab/.sm/.mono CSS classes, system-ui fonts, role=\"img\" + aria-label (SC2, SVGFIX-02)."
    - "Every diagram in this batch is method-accurate against the shipped fdars bindings — no diagram misdepicts what its method does (SC3, SVGFIX-04)."
    - "Each changed diagram passes SVGO idempotence: svgo@3.3.4 --config svgo.config.mjs second pass byte-identical (SC4, SVGFIX-03)."
    - "The 16 OK diagrams in this batch are byte-unchanged — git diff --stat lists only the 12 flagged files (SC4, no-churn decision)."
    - "Each section (learn / represent / align) passed a built-site PNG review before its commit landed (SC5)."
  artifacts:
    - "docs/assets/diagrams/smoothing.svg (corrected — Panel-3 ghost)"
    - "docs/assets/diagrams/fpca.svg (XML cleanup)"
    - "docs/assets/diagrams/elastic-fpca.svg (XML cleanup)"
    - "docs/assets/diagrams/basis-representation.svg (XML cleanup)"
    - "docs/assets/diagrams/andrews-transformation.svg (XML cleanup)"
    - "docs/assets/diagrams/distance-metrics.svg (XML cleanup)"
    - "docs/assets/diagrams/pace-fpca.svg (subtitle overflow)"
    - "docs/assets/diagrams/depth-functions.svg (method-accuracy FLAG verified)"
    - "docs/assets/diagrams/elastic-alignment.svg (amplitude/phase clarity)"
    - "docs/assets/diagrams/banded-alignment.svg (label re-anchor)"
    - "docs/assets/diagrams/shift-registration.svg (elastic-warp contrast verified)"
    - "docs/assets/diagrams/ex-sonar-tsrvf.svg (full STYLE_SPEC migration)"
  key_links:
    - "STYLE_SPEC.md canonical <style> block is the source of truth for the ex-sonar-tsrvf.svg migration."
    - "Shipped bindings (src/depth_mod.rs, src/alignment_mod.rs) + referencing docs pages resolve every method-accuracy FLAG."
    - "svgo.config.mjs + pinned svgo@3.3.4 is the per-diagram determinism gate; .venv + rsvg-convert is the per-section visual gate."
---

<objective>
Correct all 12 flagged concept diagrams in the learn/ + represent/ + align/ batch (the Phase 43 bucket of 42-AUDIT.md §2) on all four fix axes — visual/layout, STYLE_SPEC conformance, XML source formatting, and method-accuracy against the shipped fdars bindings — and verify each on the built site via rendered PNG. The 16 OK diagrams stay byte-unchanged.

Purpose: This is the first of three section-batched SVG-fix phases; it sets the correction + per-diagram-gate + per-section-review pattern for Phases 44 and 45.
Output: 12 corrected `docs/assets/diagrams/*.svg` files (no new files, no docs `.md` edits), committed one commit per section (learn / represent / align).

Scope boundaries (from 43-CONTEXT.md — non-negotiable):
- Edit ONLY the 12 flagged SVGs listed in `files_modified`. Do NOT touch the 16 OK diagrams, any docs `.md` page, or create new diagrams.
- Do NOT run a whole-site `mkdocs build --strict` — that is deferred to Phase 49 (GATE-01). The per-diagram SVGO idempotence + per-section rsvg-convert PNG render IS the correct per-phase proof.
- PNGs are written to the scratchpad only and are NEVER committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-svg-fix-learn-represent-align/43-CONTEXT.md
@.planning/phases/42-diagram-audit/42-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
</context>

<pre_flight>
Method-accuracy FLAGs already resolved against shipped bindings during planning (executors: re-confirm, do not re-guess):
- `functional_boxplot` IS exported — registered in `src/depth_mod.rs:625` (`m.add_function(wrap_pyfunction!(functional_boxplot, m)?)`), reachable as `fdars.depth.functional_boxplot`. So `depth-functions.svg`'s reference is CORRECT → no method-content change (only its own XML/visual axis if flagged; per AUDIT it is OK on XML, so this file needs NO edit unless the ghost-of-a-doubt confirm surfaces a real defect — see Task 2).
- `shift-registration.svg`'s "shift (rigid)" vs "elastic warp" is a legend CONTRAST key, and the page `docs/align/shift-registration.md:9` teaches exactly that contrast ("a shift moves the entire curve... while an elastic warp can compress and stretch"). It is method-accurate AS a contrast; the fix is only to ensure the layout reads as a contrast key, not as a step inside shift registration.
- `elastic-alignment.md` explicitly teaches amplitude-vs-phase (`:6`, `:10-13`). The diagram's small unlabeled γ(t) inset is a pedagogical-judgment call, NOT a factual error → fix conservatively (a label) and SURFACE in SUMMARY for the Phase 49 human review.

Tooling confirmed present at plan time: `.venv/bin/mkdocs` (venv), `/usr/bin/rsvg-convert`, `npx` (fnm), `svgo.config.mjs`.
</pre_flight>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Fix smoothing.svg end-to-end through the full per-diagram + per-section gate (learn/ section)</name>
  <read_first>
    - docs/assets/diagrams/smoothing.svg (Panel 1 noisy path at line ~18; Panel 3 ghost polyline at line ~48 — `<path ... stroke="#0d6efd" stroke-opacity=".2" ...>` inside `<g transform="translate(520,124)">`)
    - docs/assets/diagrams/STYLE_SPEC.md (§SVGO Invocation, §viewBox, §Accessibility — smoothing.svg already conforms; do not regress it)
    - 42-AUDIT.md learn/ row for smoothing.svg (the ghost-polyline evidence note)
  </read_first>
  <files>docs/assets/diagrams/smoothing.svg</files>
  <action>
    This is the tracer: prove the full edit -> SVGO-idempotence -> rsvg-PNG -> section-review pipeline on ONE diagram before scaling to the rest.
    Panel 3 (translate(520,124), the "Smooth Curve — noise removed, signal kept" panel) contains a faint jagged ghost path that near-copies Panel 1's noisy path (similar amplitude, y-shifted ~5px), contradicting the "noise removed" message. Redraw or remove that faint ghost so Panel 3 shows an independently-drawn smooth reference (or nothing), NOT a jagged near-copy of the noise. The bold smooth blue curve (the `stroke-width="3"` cubic-bezier path) must remain the visual focus. Keep the diagram STYLE_SPEC-conformant (it already is: viewBox 0 0 720 300, the five CSS classes, system-ui, role="img"+aria-label — do not regress these). Do not change any other panel, label, or text. Exact redraw geometry is at your discretion so long as Panel 3 no longer presents jagged noise and SVGO idempotence holds.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/smoothing.svg --output "$SC/smoothing.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/smoothing.p1.svg" --output "$SC/smoothing.p2.svg" && cmp "$SC/smoothing.p1.svg" "$SC/smoothing.p2.svg" && rsvg-convert "docs/assets/diagrams/smoothing.svg" -o "$SC/smoothing.png" && test -s "$SC/smoothing.png" && echo GATE_OK'</automated>
    <human-check>Open $SC/smoothing.png: Panel 3 shows a clean smooth curve (no jagged ghost near-copy of Panel 1); no overlapping labels; spacing/alignment intact. This is the learn/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - smoothing.svg Panel 3 no longer contains a jagged path that near-duplicates Panel 1 within ~5px; the "noise removed, signal kept" panel reads as smooth.
    - SVGO idempotence: 2nd pass byte-identical (`cmp` exits 0).
    - rsvg-convert produces a non-empty PNG in the scratchpad (never committed).
    - viewBox `0 0 720 300`, five CSS classes, `role="img"`, `aria-label` all still present (grep-confirmable; unchanged).
    - `git diff --stat` for this task lists only docs/assets/diagrams/smoothing.svg.
  </acceptance_criteria>
  <done>smoothing.svg is corrected, passes SVGO idempotence + renders cleanly, the learn/ section PNG review passed, and it is committed as the learn/ section commit (`fix(diagrams): learn/ — redraw smoothing.svg Panel-3 reference`).</done>
</task>

<task type="auto">
  <name>Task 2: Correct the represent/ batch (7 diagrams) — XML cleanup + subtitle overflow + method-accuracy confirm</name>
  <read_first>
    - docs/assets/diagrams/fpca.svg, elastic-fpca.svg, basis-representation.svg, andrews-transformation.svg, distance-metrics.svg (each: locate inline `font-size=` attributes on elements that already carry a `.sm`/`.lab`/`.mono` class)
    - docs/assets/diagrams/pace-fpca.svg (line ~12: the ~130-char `.sub` subtitle that overflows the 720px viewBox)
    - docs/assets/diagrams/depth-functions.svg (the "Functional Boxplot" bottom-row reference)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical <style> Block, §Typography Classes — the class definitions supply the sizes the inline overrides duplicate)
    - src/depth_mod.rs line ~625 (confirms `functional_boxplot` is registered/exported)
    - 42-AUDIT.md represent/ rows for the seven files
  </read_first>
  <files>docs/assets/diagrams/fpca.svg, docs/assets/diagrams/elastic-fpca.svg, docs/assets/diagrams/basis-representation.svg, docs/assets/diagrams/andrews-transformation.svg, docs/assets/diagrams/distance-metrics.svg, docs/assets/diagrams/pace-fpca.svg, docs/assets/diagrams/depth-functions.svg</files>
  <action>
    XML cleanup (fpca.svg, elastic-fpca.svg, basis-representation.svg, andrews-transformation.svg, distance-metrics.svg): remove inline `font-size=` attributes that duplicate the size already carried by the element's CSS class (`.sm`=11px, `.lab`=13px, `.mono`=12px, `.sub`=12px, `.ttl`=17px). Where an inline `font-size` matches its class size, delete the attribute (the class supplies it). Where an inline size intentionally differs from the class and removing it would visibly change the render, keep the element visually identical by adjusting the class or leaving the override — favour deletion only when it does not change rendered geometry. Do NOT touch positions, colors, paths, or text content.
    pace-fpca.svg: the `.sub` subtitle at line ~12 (~130 chars) overflows 720px and clips. Shorten it, OR break it into two `.sub` lines (second line at a slightly larger y). Keep it method-accurate (sparse irregular grids -> PACE mean + smooth eigenfunctions) and within the 720 width. Exact wording is at your discretion.
    depth-functions.svg: CONFIRM against src/depth_mod.rs:625 that `functional_boxplot` is a real exported binding (it is). The reference is correct → make NO method-content change. AUDIT scores this file OK on XML/visual/STYLE_SPEC, so if the confirm holds, this file needs NO edit at all — leave it byte-unchanged and note the confirmation in the SUMMARY. Only edit it if re-inspection surfaces a genuine, previously-unlisted defect.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && ok=1; for f in fpca elastic-fpca basis-representation andrews-transformation distance-metrics pace-fpca depth-functions; do sv="docs/assets/diagrams/$f.svg"; git diff --quiet -- "$sv" && continue; npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/$f.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/$f.p1.svg" --output "$SC/$f.p2.svg" && cmp "$SC/$f.p1.svg" "$SC/$f.p2.svg" && rsvg-convert "$sv" -o "$SC/$f.png" && test -s "$SC/$f.png" || { echo "FAIL $f"; ok=0; }; done; grep -q "wrap_pyfunction!(functional_boxplot" src/depth_mod.rs || { echo "functional_boxplot NOT exported"; ok=0; }; [ $ok -eq 1 ] && echo GATE_OK'</automated>
    <human-check>Open the scratchpad PNGs for every changed represent/ file: pace-fpca.svg subtitle fits within the frame (no right-edge clip); the five XML-cleaned diagrams render pixel-identical to before the cleanup. This is the represent/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - For each CHANGED represent/ file: SVGO idempotence 2nd pass byte-identical (`cmp` exits 0) and a non-empty scratchpad PNG renders.
    - pace-fpca.svg subtitle no longer overflows 720px (fits within frame on PNG; shortened or wrapped to two lines).
    - fpca/elastic-fpca/basis-representation/andrews-transformation/distance-metrics: no inline `font-size=` remains that merely duplicates its element's CSS-class size; render is visually unchanged.
    - `grep -q 'wrap_pyfunction!(functional_boxplot' src/depth_mod.rs` succeeds → depth-functions.svg reference confirmed correct; file left unchanged unless a genuine new defect was found.
    - `git diff --name-only` for this task lists only files within the seven represent/ paths above (no OK diagram, no .md).
  </acceptance_criteria>
  <done>All flagged represent/ diagrams are corrected (or confirmed no-change for depth-functions.svg), each changed file passes SVGO idempotence + renders cleanly, the represent/ section PNG review passed, and the batch is committed as the represent/ section commit (`fix(diagrams): represent/ — XML cleanup + pace-fpca subtitle`).</done>
</task>

<task type="auto">
  <name>Task 3: Correct the align/ batch (3 diagrams) + full STYLE_SPEC migration of ex-sonar-tsrvf.svg</name>
  <read_first>
    - docs/assets/diagrams/ex-sonar-tsrvf.svg (line 1: non-conforming `viewBox="0 0 700 400"`, no role/aria; lines 2-9: inline `text { font-family: sans-serif }` + custom `.title/.label/.small/.acc/.box` classes)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical <style> Block — copy VERBATIM; §viewBox Conventions — 720 width; §Accessibility Pattern — role="img"+aria-label)
    - docs/assets/diagrams/elastic-alignment.svg (the small unlabeled warp γ(t) inset ~line 55)
    - docs/assets/diagrams/banded-alignment.svg (cost-matrix "upper/lower band edge" labels overlapping the grid near top-left)
    - docs/assets/diagrams/shift-registration.svg (legend at lines ~50/53: "shift (rigid)" vs "elastic warp" contrast key)
    - docs/align/shift-registration.md line 9 (page prose confirming the rigid-vs-elastic contrast is intentional)
    - docs/align/elastic-alignment.md lines 6-13 (page teaches amplitude vs phase — pedagogical context)
    - 42-AUDIT.md align/ + examples rows for these four files
  </read_first>
  <files>docs/assets/diagrams/elastic-alignment.svg, docs/assets/diagrams/banded-alignment.svg, docs/assets/diagrams/shift-registration.svg, docs/assets/diagrams/ex-sonar-tsrvf.svg</files>
  <action>
    ex-sonar-tsrvf.svg — full STYLE_SPEC migration (the one Major): change the root to `viewBox="0 0 720 {height}"` at 720 width (rescale content proportionally from the old 700-wide layout; pick 300/480/520 height per STYLE_SPEC that best fits the three-path framework — likely 480). Add `fill="none" role="img" aria-label="..."` matching the title "Validation-First Framework: Three Analysis Paths". Replace the inline `<style>` (`text { font-family: sans-serif }` + `.title/.label/.small/.acc/.box`) with the canonical five-class block (`.ttl/.sub/.lab/.sm/.mono`) copied VERBATIM from STYLE_SPEC.md, and remap every text element to the nearest canonical class (title->.ttl, section labels->.lab, body->.sm, function/accuracy mono->.mono). Preserve the TSRVF worked-example method content exactly — the three analysis paths, the phase/total ratio, the accuracy percentages — this is a style migration, NOT a content redraw.
    shift-registration.svg — the "shift (rigid)" vs "elastic warp" legend is a CONTRAST key and the page teaches exactly that contrast, so it is method-accurate. Only ensure the two legend entries visually read as a side-by-side contrast (rigid | elastic), not as a sequential step under the method. If already unambiguous, leave the file byte-unchanged and note the confirmation in the SUMMARY.
    banded-alignment.svg — re-anchor the cost-matrix "upper/lower band edge" labels that overlap the grid near the top-left corner so no label text overlaps a matrix line (nudge x/y or add a small leader). Method content (Sakoe-Chiba corridor, band_frac) is accurate → do not change it.
    elastic-alignment.svg — this is a pedagogical-judgment call, not a factual error (the page teaches amplitude vs phase). Fix CONSERVATIVELY: add a small "phase γ(t)" label to the warp inset so it reads as the phase component. Do NOT redesign the panel. SURFACE this change in the SUMMARY explicitly for the Phase 49 blocking human diagram review.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && ok=1; for f in ex-sonar-tsrvf banded-alignment elastic-alignment shift-registration; do sv="docs/assets/diagrams/$f.svg"; git diff --quiet -- "$sv" && continue; npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/$f.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/$f.p1.svg" --output "$SC/$f.p2.svg" && cmp "$SC/$f.p1.svg" "$SC/$f.p2.svg" && rsvg-convert "$sv" -o "$SC/$f.png" && test -s "$SC/$f.png" || { echo "FAIL $f"; ok=0; }; done; ex=docs/assets/diagrams/ex-sonar-tsrvf.svg; grep -q "viewBox=\"0 0 720" "$ex" && grep -q "role=\"img\"" "$ex" && grep -q "aria-label" "$ex" && grep -q "\.ttl{" "$ex" && grep -q "\.sub{" "$ex" && grep -q "\.lab{" "$ex" && grep -q "\.sm{" "$ex" && grep -q "\.mono{" "$ex" || { echo "ex-sonar migration incomplete"; ok=0; }; [ $ok -eq 1 ] && echo GATE_OK'</automated>
    <human-check>Open the scratchpad PNGs: ex-sonar-tsrvf.svg renders at 720 width with canonical typography and the three-path content intact; banded-alignment.svg cost-matrix labels no longer overlap the grid; elastic-alignment.svg warp inset now labeled "phase γ(t)". This is the align/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - ex-sonar-tsrvf.svg: `viewBox` width == 720, contains `role="img"` + `aria-label`, and all five canonical CSS classes (`.ttl .sub .lab .sm .mono`) are defined and used; the three analysis paths + accuracy percentages content is preserved.
    - banded-alignment.svg: cost-matrix band-edge labels no longer overlap the matrix grid on the PNG.
    - elastic-alignment.svg: warp inset carries an explicit "phase"/γ(t) label (conservative fix); the change is called out in the SUMMARY for Phase 49.
    - shift-registration.svg: legend confirmed to read as a rigid-vs-elastic contrast (changed only if it was ambiguous).
    - For each CHANGED align/ file: SVGO idempotence 2nd pass byte-identical + non-empty scratchpad PNG.
    - `git diff --name-only` for this task lists only the four align/examples paths above.
  </acceptance_criteria>
  <reversibility rating="reversible">All edits are hand-authored SVG changes tracked in git; any diagram can be reverted with `git checkout`.</reversibility>
  <done>All flagged align/ diagrams + ex-sonar-tsrvf.svg are corrected, each changed file passes SVGO idempotence + renders cleanly, the align/ section PNG review passed, and the batch is committed as the align/ section commit (ex-sonar-tsrvf.svg included), e.g. `fix(diagrams): align/ — ex-sonar STYLE_SPEC migration + label re-anchors`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed SVG | Hand-edited SVG source is committed and later rendered by MkDocs into the public site. |
| npx → svgo@3.3.4 | The idempotence gate shells out to an npm package by version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | Editing an SVG regresses STYLE_SPEC conformance (drops role/aria or a CSS class) | medium | mitigate | Task acceptance criteria grep-assert viewBox/role/aria/five-classes on every changed file; smoothing.svg tracer proves the check before scaling. |
| T-43-02 | Information Disclosure | Method-accuracy FLAG "fixed" by guessing, shipping a misleading diagram | high | mitigate | FLAGs resolved against shipped bindings (src/depth_mod.rs, alignment) + page prose during planning; pedagogical calls fixed conservatively and surfaced in SUMMARY for the Phase 49 blocking human review. |
| T-43-03 | Tampering | Accidental edit/churn to one of the 16 OK diagrams breaks determinism | medium | mitigate | `git diff --name-only` scoped to the 12 flagged paths in every task's acceptance criteria; OK diagrams must stay byte-unchanged. |
| T-43-SC | Tampering | npm/npx install of svgo@3.3.4 | low | accept | svgo@3.3.4 is the project-pinned, CI-established gate tool (STYLE_SPEC.md §SVGO Invocation) already in use since Phase 1; version is pinned, not `latest`. No new package introduced. |
</threat_model>

<verification>
Per changed diagram (all tasks): `npx svgo@3.3.4 --config svgo.config.mjs` run twice → 2nd pass byte-identical (`cmp` exit 0), and `rsvg-convert` → non-empty PNG in the scratchpad (never committed).
STYLE_SPEC conformance: grep the canonical markers (viewBox 720, the five CSS classes, `role="img"`, `aria-label`) on every changed file — mandatory for ex-sonar-tsrvf.svg.
Method-accuracy: `functional_boxplot` export re-confirmed via grep of src/depth_mod.rs; shift-registration/elastic-alignment FLAGs resolved against page prose.
No-churn guard: after all three commits, `git diff --stat main~3..HEAD -- docs/assets/diagrams` (or equivalent) lists ONLY the 12 flagged files.
Explicitly NOT run this phase: whole-site `mkdocs build --strict` (deferred to Phase 49 / GATE-01).
</verification>

<success_criteria>
- SC1: Every flagged learn/represent/align diagram renders cleanly on the PNG check — no overlapping labels, consistent spacing/alignment/sizing.
- SC2: Every diagram in this batch conforms to STYLE_SPEC.md (720 viewBox, five CSS classes, system-ui, role="img"+aria-label).
- SC3: Every diagram in this batch is method-accurate against the shipped fdars bindings.
- SC4: Each changed diagram passes SVGO idempotence; the 16 OK diagrams are byte-unchanged.
- SC5: Each section (learn / represent / align) passed a built-site PNG review before its commit landed (one commit per section; ex-sonar-tsrvf.svg lands with align/).
- Any conservative pedagogical fix (elastic-alignment.svg) is surfaced in the SUMMARY for the Phase 49 blocking human diagram review.
</success_criteria>

<output>
Create `.planning/phases/43-svg-fix-learn-represent-align/43-01-SUMMARY.md` when done.
Include: per-diagram outcome (changed / confirmed-no-change), the method-accuracy FLAG resolutions (functional_boxplot confirmed exported; shift-registration contrast confirmed; elastic-alignment conservative label added), and an explicit callout of any pedagogical-judgment fix for the Phase 49 human diagram review.
</output>

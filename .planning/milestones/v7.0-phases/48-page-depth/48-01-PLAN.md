---
phase: 48-page-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regression/functional-glm.md
  - docs/represent/pace-fpca.md
  - docs/inference/interval-inference.md
autonomous: true
requirements: [DEPTH-01, DEPTH-03]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "docs/regression/functional-glm.md worked example exercises MORE THAN the binomial family — it runs at least one additional exponential family (poisson or gamma) end-to-end and prints FDARS_FENCE_OK (DEPTH-01, DEPTH-03)."
    - "docs/regression/functional-glm.md caveats state Gamma uses the inverse canonical link g(mu)=1/mu (NOT log; differs from R glm default) and its AIC is not comparable to R glm() (DEPTH-01)."
    - "docs/represent/pace-fpca.md gains a NEW offline worked example comparing PACE-FPCA against standard FPCA (fdars.regression.fpca) and prints FDARS_FENCE_OK (DEPTH-01, DEPTH-03)."
    - "docs/inference/interval-inference.md gains a NEW offline worked example contrasting the ITP per-basis p-value vector with a single global permutation-test p-value (fdars.inference.t_perm_test) and prints FDARS_FENCE_OK (DEPTH-01, DEPTH-03)."
    - "Every new/modified fence runs OFFLINE under the repo .venv (PYTHONPATH=scripts) with small data (synthetic n <= 20 or subsampled) and emits FDARS_FENCE_OK (DEPTH-03)."
    - "No diagram or SVG is touched — the git diff for this plan lists only the three .md pages."
  artifacts:
    - docs/regression/functional-glm.md
    - docs/represent/pace-fpca.md
    - docs/inference/interval-inference.md
  key_links:
    - "Fence Python bodies import from docs_fig (fig/render/fast) resolved via PYTHONPATH=scripts; fdars.* bindings resolved via the repo .venv extension module."
    - "Param/return tables cite the SHIPPED bindings (src/regression_mod.rs functional_glm + fpca; src/inference_mod.rs t_perm_test/itp_*) — names/defaults/returns read from code, not invented."
---

<objective>
Bring the three page-depth targets that materially benefit from a NEW executable worked example to mature structure, and verify each new fence executes offline. These are the DEPTH-01 v6.0 pages whose gap is a richer worked example — functional-glm (multi-family), pace-fpca (PACE-vs-standard-FPCA), interval-inference (ITP-vs-permutation) — plus the associated caveats/comparison prose.

Purpose: DEPTH-03 says add a new offline FDARS_FENCE_OK worked example ONLY where it materially helps — these three pages are exactly those. Method-accuracy (the docs' core value) requires the new fences run against the CURRENT fdars API.
Output: three extended .md pages, each with a new/expanded fence proven to emit FDARS_FENCE_OK under the repo .venv.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-page-depth/48-CONTEXT.md

# Fence-gate recipe (memory: docs-diagram-verify-workflow):
#   run a fence body standalone with:  PYTHONPATH=scripts .venv/bin/python <fence.py>
#   DOCS_FAST=1 selects the fast branch of docs_fig.fast(full, fast_value)
# docs_fig provides: fig(...), render(fig), fast(full, fast_value). No network, no keys.
# SCRATCH = /tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: functional-glm.md — multi-family worked example (end-to-end exemplar) + fence gate</name>
  <files>docs/regression/functional-glm.md</files>
  <read_first>
    - docs/regression/functional-glm.md — the page as shipped ALREADY has a Theory section, a "Link functions by family" table, a Parameters table, a Returns table, two admonitions (Gamma inverse-link warning; AIC-not-comparable note), and a SINGLE binomial fence (lines ~74-99). The gap per 42-AUDIT.md section 4 and 48-CONTEXT.md is: the worked example shows ONLY binary/binomial response — extend it to demonstrate a second exponential family.
    - src/regression_mod.rs:1062-1164 — family_from_str dispatch confirms the four accepted family strings (gaussian, binomial, poisson, gamma); Gamma maps to GlmFamily::Gamma which uses the inverse canonical link g(mu)=1/mu (comment at :1091), NOT log; Poisson requires y>=0, Gamma requires y>0 (:1140). The returned dict keys are already documented in the page's Returns table — do NOT change them.
    - docs/analyze/outlier-detection.md — the mature-structure peer (Theory -> method -> worked example -> params -> caveats). functional-glm.md already matches this shape; this task only enriches the worked example and confirms the caveat wording.
  </read_first>
  <action>
    Extend the existing worked-example fence in functional-glm.md so it fits a SECOND exponential family in addition to binomial — prefer poisson (add a count response) because it needs no positivity trick; gamma is acceptable if a strictly-positive response is constructed. Keep the existing binomial fit and ADD the second family fit in the same fence (small synthetic data, n <= 30, m <= 60, one np.random.default_rng seed). Plot or print both coefficient functions beta(t) (reuse the existing fig/render idiom) and print each family's deviance and aic alongside its family string. The fence MUST still end with a single print of the FDARS success sentinel. Do NOT introduce docs_data (this page is synthetic-only). Do NOT touch the SVG or the diagram image line for functional-glm.svg.
    Confirm the caveats already present are accurate against src/regression_mod.rs: the Gamma admonition must state the inverse canonical link g(mu)=1/mu, not log, differing from R glm default; the AIC note must state the score-space AIC is not comparable to R glm() AIC. If either admonition wording is weaker than this, tighten it. Keep the Parameters and Returns tables byte-accurate to the shipped binding — do not add invented parameters.
    Then run the fence gate: extract the fence body to $SCRATCH/glm_fence.py and execute it with PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python — it must exit 0 and print the FDARS success sentinel.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; S=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad &amp;&amp; .venv/bin/python -c "import re; m=re.findall(r'\`\`\`python exec[^\n]*\n(.*?)\`\`\`', open('docs/regression/functional-glm.md').read(), re.S); open('$S/glm_fence.py','w').write(m[-1])" &amp;&amp; PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python $S/glm_fence.py | grep -q FDARS_FENCE_OK</automated>
  </verify>
  <done>functional-glm.md fence fits binomial PLUS a second exponential family, runs offline under .venv and prints FDARS_FENCE_OK; the Gamma-inverse-link and AIC-not-comparable caveats are present and accurate; no SVG touched.</done>
  <reversibility rating="reversible">Doc-only edit to a single .md page; revert by git checkout.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: pace-fpca.md — new PACE-vs-standard-FPCA comparison fence + fence gate</name>
  <files>docs/represent/pace-fpca.md</files>
  <read_first>
    - docs/represent/pace-fpca.md — as shipped it ALREADY has Theory, an irreg_fdata_from_lists param table, a pace_fpca param table, a Returns table, two admonitions, and one synthetic sparse-irregular fence (lines ~72-111) importing `import fdars.pace_fpca as pf`. Gap per 48-CONTEXT.md: missing a COMPARISON with standard FPCA (when PACE helps vs standard FPCA on a common grid).
    - src/regression_mod.rs:20-47 — the standard-FPCA binding `fpca(data, argvals, n_comp=3)` returns a dict with keys scores (n,n_comp), rotation (m,n_comp) [the eigenfunctions], singular_values (n_comp,), mean (m,), centered, weights. Import path in Python is `import fdars.regression as reg; reg.fpca(...)`. NOTE: standard fpca REQUIRES a dense common grid (a full 2-D matrix), which is exactly why PACE exists for sparse/irregular data.
    - docs/regression/functional-glm.md — a peer that already carries a params table + caveat + fence at mature depth, for tone/structure.
  </read_first>
  <action>
    Add a NEW section (e.g. "## PACE vs standard FPCA") with a short prose contrast (standard fpca needs a dense common grid and breaks on ragged sparse data; PACE recovers eigenfunctions from pooled sparse pairs via conditional expectation) plus caveats on when PACE fails vs basis-smoothing (too few points per curve, bandwidth too small for sparsity — cross-link the existing bandwidth guidance). Then add a NEW small offline fence: build ONE dense synthetic dataset (n <= 15, m <= 40) from a small KL model, run standard reg.fpca on the dense matrix, then subsample each curve to a ragged sparse set (5-8 points each), run pf.pace_fpca on the sparse handle, and compare the leading eigenfunction / eigenvalue between the two (e.g. correlate PC1 up to sign, print both leading eigenvalues). Reuse the existing docs_fig fig/render idiom; keep data small; end with a single print of the FDARS success sentinel. Do NOT touch the pace-fpca.svg image line.
    Run the fence gate on the NEW fence: extract it to $SCRATCH/pace_fence.py and run PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python — it must print the FDARS sentinel. (There will be two fences on the page; verify BOTH still emit the sentinel.)
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; S=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad &amp;&amp; .venv/bin/python -c "import re; ms=re.findall(r'\`\`\`python exec[^\n]*\n(.*?)\`\`\`', open('docs/represent/pace-fpca.md').read(), re.S); assert len(ms)&gt;=2, ('need 2+ fences, found %d'%len(ms)); [open('$S/pace_%d.py'%i,'w').write(b) for i,b in enumerate(ms)]" &amp;&amp; for f in $S/pace_*.py; do PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python "$f" | grep -q FDARS_FENCE_OK || exit 1; done &amp;&amp; grep -qi 'reg.fpca\|regression.fpca\|standard FPCA' docs/represent/pace-fpca.md</automated>
  </verify>
  <done>pace-fpca.md has a PACE-vs-standard-FPCA comparison section and a NEW fence that runs reg.fpca and pf.pace_fpca on the same small synthetic data offline; all fences on the page emit FDARS_FENCE_OK; no SVG touched.</done>
  <reversibility rating="reversible">Doc-only edit to a single .md page.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: interval-inference.md — new ITP-vs-permutation comparison fence + fence gate</name>
  <files>docs/inference/interval-inference.md</files>
  <read_first>
    - docs/inference/interval-inference.md — as shipped it ALREADY has Theory, a function-overview table (itp_one_pop / itp_two_pop / itp_flm), per-function Parameters tables, a shared Returns table, two admonitions, and one itp_one_pop fence (lines ~115-137) importing `import fdars.inference as fi`. Gap per 48-CONTEXT.md: missing caveats (sample-size requirements, basis sensitivity) and a COMPARISON with the permutation test.
    - src/inference_mod.rs:44-90 (t_perm_test) — `fi.t_perm_test(data_a, data_b, argvals, n_perm=999, seed=None)` returns a dict {"statistic","p_value","n_perm"} — a SINGLE global two-sample p-value (integrated L2 between mean curves). This is the natural contrast to itp_two_pop, which returns a VECTOR of per-basis adjusted p-values (localises WHERE the difference is). src/inference_mod.rs:707 (itp_two_pop) confirms the (data_a, data_b, argvals, basis_type, nbasis, n_perm, seed) signature already documented on the page.
    - docs/analyze/outlier-detection.md — mature-structure peer for the caveats/comparison tone.
  </read_first>
  <action>
    Add caveats prose to interval-inference.md: (a) sample-size requirement (each sample needs n>=2; small n makes the permutation null coarse — the smallest attainable p-value is 1/(n_perm+1)); (b) basis sensitivity (B-spline clamping can reduce the actual n_basis below the requested nbasis — cross-link the existing clamping admonition; too many basis functions dilute per-coefficient power). Add a NEW section "## ITP vs a global permutation test" contrasting the localising per-basis ITP p-value vector with the single global p-value from fi.t_perm_test: ITP tells you WHERE two populations differ; the global permutation test only tells you WHETHER. Add a NEW small offline fence: build two small synthetic samples (n_a, n_b <= 15, m <= 40) that differ only in a middle sub-interval, run fi.itp_two_pop (small nbasis, small n_perm via docs_fig.fast) and fi.t_perm_test on the SAME data, print the ITP adjusted-p vector alongside the single global p-value, and note that ITP flags only the coefficients over the differing interval while the global test returns one number. Reuse docs_fig.fast for n_perm; end with a single print of the FDARS success sentinel. Do NOT touch the itp-interval-inference.svg image line.
    Run the fence gate on the NEW fence: extract to $SCRATCH/itp_fence.py and run PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python — it must print the FDARS sentinel. Verify BOTH fences on the page still emit it.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; S=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad &amp;&amp; .venv/bin/python -c "import re; ms=re.findall(r'\`\`\`python exec[^\n]*\n(.*?)\`\`\`', open('docs/inference/interval-inference.md').read(), re.S); assert len(ms)&gt;=2, ('need 2+ fences, found %d'%len(ms)); [open('$S/itp_%d.py'%i,'w').write(b) for i,b in enumerate(ms)]" &amp;&amp; for f in $S/itp_*.py; do PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python "$f" | grep -q FDARS_FENCE_OK || exit 1; done &amp;&amp; grep -q 't_perm_test' docs/inference/interval-inference.md</automated>
  </verify>
  <done>interval-inference.md has sample-size and basis-sensitivity caveats plus an ITP-vs-permutation comparison section and a NEW fence running itp_two_pop and t_perm_test on the same small synthetic data offline; all fences emit FDARS_FENCE_OK; no SVG touched.</done>
  <reversibility rating="reversible">Doc-only edit to a single .md page.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs fence -> fdars extension module | Fence Python calls into the compiled fdars .venv extension; inputs are synthetic/subsampled arrays authored in the fence itself (no untrusted external input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | Method-inaccurate prose/params in an edited page | high | mitigate | Every param/return/caveat cited from the shipped binding via <read_first> (src/regression_mod.rs, src/inference_mod.rs); no invented parameters; Gamma inverse-link + AIC caveats verified against code. |
| T-48-02 | Denial of Service | New fence inflates the ~19-25 min docs build | medium | mitigate | New fence data kept small (n <= 20 / subsampled, m <= 60), n_perm via docs_fig.fast; fence-gate runs each fence standalone in seconds, not a whole-site build. |
| T-48-03 | Tampering | Accidental SVG/diagram edit outside phase scope | high | mitigate | Every task's <done> and verify asserts no SVG touched; git diff must list only the three .md pages. |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan — doc edits only against the already-built .venv; no legitimacy gate needed. |
</threat_model>

<verification>
- Each of the three pages parses and each fence on the page executes offline under `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python`, printing `FDARS_FENCE_OK`.
- functional-glm.md fence covers binomial PLUS a second family; the Gamma-inverse-link and AIC caveats are present.
- pace-fpca.md runs both `reg.fpca` and `pf.pace_fpca`; interval-inference.md runs both `itp_two_pop` and `t_perm_test`.
- `git diff --name-only docs/` for this plan lists only the three `.md` pages and no `.svg`.
</verification>

<success_criteria>
All three pages reach mature structure with a working new/expanded fence; every fence emits `FDARS_FENCE_OK` offline with small data; params/caveats are method-accurate against the shipped bindings; no diagram/SVG changed.
</success_criteria>

<output>
Create `.planning/phases/48-page-depth/48-01-SUMMARY.md` when done. In the SUMMARY, note for each page whether it got a NEW fence (all three do) and surface any genuine method-accuracy ambiguity found against the bindings.
</output>

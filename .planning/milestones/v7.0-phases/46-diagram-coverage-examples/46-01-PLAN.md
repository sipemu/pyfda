---
phase: 46-diagram-coverage-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/ex-canadian-weather.svg
  - docs/assets/diagrams/ex-canadian-depth-centrality.svg
  - docs/assets/diagrams/ex-canadian-function-on-scalar.svg
  - docs/assets/diagrams/ex-canadian-precipitation.svg
  - docs/assets/diagrams/ex-canadian-seasonal.svg
  - docs/assets/diagrams/ex-andrews-wine.svg
  - docs/assets/diagrams/ex-andrews-wine-intro.svg
  - docs/assets/diagrams/ex-andrews-wine-clustering.svg
  - docs/assets/diagrams/ex-andrews-wine-qc.svg
  - docs/examples/canadian-weather.md
  - docs/examples/canadian-depth-centrality.md
  - docs/examples/canadian-function-on-scalar.md
  - docs/examples/canadian-precipitation.md
  - docs/examples/canadian-seasonal.md
  - docs/examples/andrews-wine.md
  - docs/examples/andrews-wine-intro.md
  - docs/examples/andrews-wine-clustering.md
  - docs/examples/andrews-wine-qc.md
autonomous: true
requirements: [DIACOV-01]

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "canadian-weather (tracer) carries a method-accurate STYLE_SPEC-conformant workflow SVG embedded near page top (DIACOV-01, SC1, SC2)"
    - "The 4 canadian-family pages each carry a method-accurate workflow SVG matching what that example actually computes (DIACOV-01, SC1, SC2)"
    - "The 4 andrews-wine-family pages each carry a method-accurate workflow SVG matching what that example actually computes (DIACOV-01, SC1, SC2)"
    - "Every new ex-*.svg passes the svgo@3.3.4 idempotence gate — 2nd pass byte-identical (SC3)"
    - "Every new ex-*.svg renders to a non-empty PNG via rsvg-convert (SC4 built-site review precursor)"
    - "No method-page diagram and no other example page's SVG is modified"
  artifacts:
    - docs/assets/diagrams/ex-canadian-weather.svg
    - docs/assets/diagrams/ex-canadian-depth-centrality.svg
    - docs/assets/diagrams/ex-canadian-function-on-scalar.svg
    - docs/assets/diagrams/ex-canadian-precipitation.svg
    - docs/assets/diagrams/ex-canadian-seasonal.svg
    - docs/assets/diagrams/ex-andrews-wine.svg
    - docs/assets/diagrams/ex-andrews-wine-intro.svg
    - docs/assets/diagrams/ex-andrews-wine-clustering.svg
    - docs/assets/diagrams/ex-andrews-wine-qc.svg
  key_links:
    - "each ex-<slug>.svg is referenced by docs/examples/<slug>.md via the .fdars-diagram image line"
    - "the pipeline proven by the tracer (create -> embed -> svgo idempotence -> rsvg PNG) is reused verbatim for every subsequent diagram"
---

<objective>
Author 9 method-accurate, STYLE_SPEC-conformant hand-authored inline concept SVGs — one per example page — for the canadian-weather tracer plus the canadian and andrews-wine dataset families, and embed each near the top of its page. Each is a workflow/pipeline diagram (dataset -> the specific fdars methods that page calls -> result/insight) in the ex-sonar-tsrvf.svg genre, NOT a generic method concept diagram (DIACOV-01).

Purpose: Close the canadian + andrews half of the DIACOV-01 examples coverage gap, and — via the tracer — prove the full per-diagram pipeline before the sibling plan scales it to the remaining families.
Output: 9 new ex-<slug>.svg files + 9 example .md pages each gaining exactly one image-reference line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/assets/diagrams/ex-sonar-tsrvf.svg
@docs/assets/diagrams/STYLE_SPEC.md
@svgo.config.mjs
</context>

<shared_recipe>
Per-diagram gate (run for EACH new SVG): `bash .planning/phases/46-diagram-coverage-examples/check-ex.sh <slug> <viewbox-height>`. It asserts the SVG exists with a 720-width viewBox at the given height, `role="img"`, `aria-label`, all five canonical CSS classes; that the page embeds it via `.fdars-diagram`; svgo@3.3.4 idempotence (2nd pass byte-identical, check-only, never rewrites the source); and a non-empty rsvg-convert PNG. Choose height 300 for single-row flows, 480 for tall two-row flows.

Embed pattern (the ONLY .md edit allowed per page — do NOT rewrite prose; that is Phase 48): after the intro prose and before the first `##` heading, add one line:
`![<page title> — <short caption>](../assets/diagrams/ex-<slug>.svg){ .fdars-diagram }` (mirror docs/examples/sonar-tsrvf.md line ~22).

Authoring: hand-author inline SVG; copy the canonical five-class `<style>` block verbatim from STYLE_SPEC.md; palette + stroke weights per STYLE_SPEC; workflow/pipeline genre per ex-sonar-tsrvf.svg. Read each target page AND the fdars methods it calls before drawing — method-accuracy is the core value; do NOT invent steps the page does not perform. Scope each to 2-5 panels / a clear staged flow.

Escalation: if a page's "right" depiction is a judgment call, draw the best-supported version and note it in the SUMMARY for the Phase 49 blocking human diagram review.

No-regression guard (run once at end of each task): `bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh <slug>...` — confirms only this task's new ex-*.svg and its own pages changed.
</shared_recipe>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — author + embed ex-canadian-weather.svg end-to-end, proving the per-diagram pipeline</name>
  <files>docs/assets/diagrams/ex-canadian-weather.svg, docs/examples/canadian-weather.md</files>
  <read_first>
    - docs/examples/canadian-weather.md (full page — the multi-method overview; note the actual arc it teaches)
    - docs/assets/diagrams/ex-sonar-tsrvf.svg (the workflow-genre STYLE_SPEC template: viewBox 0 0 720 480, five-class style block, role=img, aria-label, staged flow with result boxes)
    - docs/assets/diagrams/STYLE_SPEC.md (palette, fonts, viewBox 720-width, classes, role/aria pattern)
    - svgo.config.mjs (the check-only idempotence config)
    - docs/examples/sonar-tsrvf.md line ~22 (embed pattern)
  </read_first>
  <action>
Author ONE exemplar diagram end-to-end and PROVE the pipeline before Task 2/3 scale it (this is the tracer's whole point — catch any recipe/style mismatch after one diagram, not after nine).

Method accuracy (read the page first): canadian-weather.md is the multi-method OVERVIEW of the Canadian Weather dataset. It loads via load_canadian_weather, then answers two linked questions with real fdars calls — functional ANOVA (fdars.regression.fanova) tests whether the 4 climatic regions genuinely differ; function-on-scalar regression (fdars.regression.fosr / fosr_fpc / predict_fosr) drives the seasonal temperature curve from latitude/longitude; then it predicts an unseen station's curve and classifies region from shape (fdars.classification.fclassif_cv / fclassif_lda). Depict as a staged workflow: dataset panel (35 stations, 4 regions, daily temp+precip) -> two analysis branches (fanova: regions differ?; fosr: beta(t) from lat/long) -> result panels (predicted curve + region classification). Use the FDARS data-curve palette per region.

Style + embed + gate: follow <shared_recipe> exactly. Use viewbox height 480 (tall two-row workflow). If the svgo config or rsvg path differs from what STATE/prior phases assumed, STOP and surface it — Task 2 and Task 3 reuse this exact recipe.
  </action>
  <verify>
    <automated>bash .planning/phases/46-diagram-coverage-examples/check-ex.sh canadian-weather 480</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/ex-canadian-weather.svg exists: viewBox 0 0 720 480, role=img, aria-label, the 5 canonical CSS classes
    - The diagram depicts the fanova + fosr + predict/classify arc the page actually runs (not a generic method concept)
    - docs/examples/canadian-weather.md contains the .fdars-diagram reference to ex-canadian-weather.svg, after intro prose, before the first ## heading
    - svgo@3.3.4 idempotence: 2nd pass byte-identical; rsvg PNG non-empty
    - No pre-existing diagram or other page changed (check-noregress.sh canadian-weather)
  </acceptance_criteria>
  <done>check-ex.sh canadian-weather 480 prints OK; the pipeline is proven and reusable for Tasks 2-3.</done>
</task>

<task type="auto">
  <name>Task 2: Canadian family — author + embed 4 workflow SVGs (depth-centrality, function-on-scalar, precipitation, seasonal)</name>
  <files>docs/assets/diagrams/ex-canadian-depth-centrality.svg, docs/assets/diagrams/ex-canadian-function-on-scalar.svg, docs/assets/diagrams/ex-canadian-precipitation.svg, docs/assets/diagrams/ex-canadian-seasonal.svg, docs/examples/canadian-depth-centrality.md, docs/examples/canadian-function-on-scalar.md, docs/examples/canadian-precipitation.md, docs/examples/canadian-seasonal.md</files>
  <read_first>
    - docs/examples/canadian-depth-centrality.md, canadian-function-on-scalar.md, canadian-precipitation.md, canadian-seasonal.md (each full page + the fdars methods it calls)
    - docs/assets/diagrams/ex-canadian-weather.svg (the tracer, now the concrete in-family template)
    - docs/assets/diagrams/STYLE_SPEC.md
  </read_first>
  <action>
Reuse the proven <shared_recipe> for all 4. Method-accurate depiction per page (confirm against each page before drawing):
- canadian-depth-centrality: depth-centrality ordering / functional boxplot — the page computes functional depth (.depth(...)) on the temperature curves to rank stations centre-to-outer; depict load -> depth ranking -> central curve + envelope / functional-boxplot view.
- canadian-function-on-scalar: function-on-scalar regression beta(t) per predictor — uses fdars.regression.fosr / predict_fosr with the station mean (.mean); depict predictors (lat/long) -> fosr fit -> beta(t) coefficient curves + a predicted temperature curve.
- canadian-precipitation: basis representation + smoothing pipeline — uses fdars.basis.pspline_fit_1d then fdars.regression.fanova / fosr / fosr_fpc; depict raw precip -> P-spline smoothing (pspline_fit_1d) -> fanova / fosr on the smoothed curves.
- canadian-seasonal: seasonal decomposition / period estimation — uses fdars.seasonal (stl_decompose, ssa_fdata, lomb_scargle_fdata, matrix_profile_fdata, detect_peaks, analyze_peak_timing, seasonal_strength/classify_seasonality); depict curve -> decomposition (trend/seasonal/residual) + period estimate -> seasonal-strength classification. Keep to 2-5 panels; do not cram every seasonal function — show the arc.

Height: 300 for single-row flows, 480 for tall ones (your discretion per content). Run check-ex.sh for each, then the no-regression guard.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for s in canadian-depth-centrality canadian-function-on-scalar canadian-precipitation canadian-seasonal; do h=$(grep -oE 'viewBox="0 0 720 [0-9]+"' docs/assets/diagrams/ex-$s.svg | grep -oE '[0-9]+"$' | tr -d '"'); bash .planning/phases/46-diagram-coverage-examples/check-ex.sh $s $h || exit 1; done && bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh canadian-depth-centrality canadian-function-on-scalar canadian-precipitation canadian-seasonal</automated>
  </verify>
  <acceptance_criteria>
    - All 4 ex-canadian-*.svg exist, STYLE_SPEC-conformant (720-width viewBox, role=img, aria-label, 5 classes)
    - Each diagram is method-accurate to that page's actual fdars calls (depth / fosr / pspline+fanova / seasonal decomposition respectively)
    - Each of the 4 pages contains its .fdars-diagram reference; no prose otherwise rewritten
    - Each SVG: svgo idempotent, rsvg PNG non-empty
    - Only these 4 new SVGs + their 4 pages changed
  </acceptance_criteria>
  <done>All 4 canadian-family diagrams authored, embedded, gate-green; no-regression guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: Andrews-wine family — author + embed 4 workflow SVGs (andrews-wine, -intro, -clustering, -qc)</name>
  <files>docs/assets/diagrams/ex-andrews-wine.svg, docs/assets/diagrams/ex-andrews-wine-intro.svg, docs/assets/diagrams/ex-andrews-wine-clustering.svg, docs/assets/diagrams/ex-andrews-wine-qc.svg, docs/examples/andrews-wine.md, docs/examples/andrews-wine-intro.md, docs/examples/andrews-wine-clustering.md, docs/examples/andrews-wine-qc.md</files>
  <read_first>
    - docs/examples/andrews-wine.md, andrews-wine-intro.md, andrews-wine-clustering.md, andrews-wine-qc.md (each full page + the fdars methods it calls)
    - docs/assets/diagrams/ex-canadian-weather.svg (proven template)
    - docs/assets/diagrams/STYLE_SPEC.md
  </read_first>
  <action>
Reuse the proven <shared_recipe> for all 4. Note: the Andrews transform is a docs-side encoding (no `andrews` binding) — depict it as the input encoding step, then the actual fdars methods each page runs:
- andrews-wine: core outlier overview — runs functional depth (modified_band_1d), magnitude-shape (fdars.outliers.magnitude_shape), outliergram (fdars.outliers.outliergram), and likelihood-ratio (detect_outliers_lrt) on the wine Andrews curves, cross-checked vs a Mahalanobis baseline; depict rows -> Andrews curves -> [depth | MS-plot | outliergram | LRT] -> flagged bottles.
- andrews-wine-intro: intro to the Andrews transformation — 13-D standardized row -> Andrews curve f_x(t), L2-distance-preserving (fdars.metric.lp_self_1d), central curve = central wine (mean_1d, modified_band_1d); depict the row->curve mapping and the "central curve = central row" idea.
- andrews-wine-clustering: cluster assignment — fdars.clustering.kmeans_fd / fuzzy_cmeans_fd with silhouette_score_data / calinski_harabasz_data selection, plus fpca / fanova; depict curves -> k selection (silhouette/CH) -> cluster assignment (before/after color) vs true cultivars.
- andrews-wine-qc: QC tolerance-band overlay — modified_band_1d depth, fdars.tolerance.fpca_tolerance_band, fdars.spm.spm_phase + t2_pc_contributions; depict in-control curves -> FPCA tolerance band -> new bottle flagged out-of-band + T2 contribution.

Height 300 or 480 per content. Run check-ex.sh for each, then the no-regression guard.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for s in andrews-wine andrews-wine-intro andrews-wine-clustering andrews-wine-qc; do h=$(grep -oE 'viewBox="0 0 720 [0-9]+"' docs/assets/diagrams/ex-$s.svg | grep -oE '[0-9]+"$' | tr -d '"'); bash .planning/phases/46-diagram-coverage-examples/check-ex.sh $s $h || exit 1; done && bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh andrews-wine andrews-wine-intro andrews-wine-clustering andrews-wine-qc</automated>
  </verify>
  <acceptance_criteria>
    - All 4 ex-andrews-wine*.svg exist, STYLE_SPEC-conformant
    - Each diagram is method-accurate to that page's actual fdars calls (depth+MS+outliergram+LRT / andrews-encoding / kmeans+selection / tolerance-band+spm respectively)
    - Each of the 4 pages contains its .fdars-diagram reference; no prose otherwise rewritten
    - Each SVG: svgo idempotent, rsvg PNG non-empty
    - Only these 4 new SVGs + their 4 pages changed
  </acceptance_criteria>
  <done>All 4 andrews-family diagrams authored, embedded, gate-green; no-regression guard passes.</done>
</task>

</tasks>

<verification>
- All 9 target pages (canadian-weather + 4 canadian + 4 andrews) reference a new ex-<slug>.svg via `.fdars-diagram`.
- Every new SVG: viewBox 720-width, role=img, aria-label, five canonical classes; svgo@3.3.4 2nd-pass byte-identical; rsvg PNG non-empty.
- `git status` shows ONLY the 9 new SVGs + 9 edited example pages changed (plus this plan's helper scripts) — no method-page diagram, no other example SVG.
- Confirm counts: `ls docs/assets/diagrams/ex-canadian-*.svg docs/assets/diagrams/ex-andrews-*.svg | wc -l` == 8, plus ex-canadian-weather.svg == 9 total new (ex-sonar-tsrvf.svg pre-existing, untouched).
</verification>

<success_criteria>
9 method-accurate, STYLE_SPEC-conformant workflow SVGs authored and embedded across the canadian-weather tracer + canadian + andrews-wine families; per-diagram svgo idempotence + rsvg PNG green; no unrelated file modified; any judgment-call depictions surfaced in the SUMMARY for Phase 49.
</success_criteria>

<output>
Create `.planning/phases/46-diagram-coverage-examples/46-01-SUMMARY.md` when done. In it, list the 9 new SVG filenames + 9 edited pages, note each diagram's depicted method arc, and flag any ambiguous/judgment-call depictions for the Phase 49 blocking human diagram method-accuracy review.
</output>

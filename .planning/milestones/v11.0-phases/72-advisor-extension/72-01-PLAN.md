---
phase: 72-advisor-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/aspects/fts.py
  - python/fdars/advisor/aspects/frechet.py
  - python/fdars/advisor/__init__.py
  - python/fdars/mcp/server.py
  - tests/test_guard_sync_version_independent.py
  - tests/test_advisor_fts.py
autonomous: true
requirements: [ADV-01, ADV-02]
estimate:
  tokens: 62000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "build_diagnostics(ftsm_result, method='fts') returns a JSON-serialisable dict whose method field == 'fts' (ADV-01)"
    - "Every value in the fts diagnostics dict is a native Python float/int/bool/list/None — no numpy scalar (ADV-01 grounding invariant)"
    - "'fts' and 'frechet' are added to advisor _supported, server._DIAGNOSTICS_METHODS, and the guard-sync test literal in ONE atomic commit (ADV-02)"
    - "test_guard_sync_version_independent.py passes: advisor._supported == _EXPECTED_DIAGNOSTICS_METHODS == server._DIAGNOSTICS_METHODS (ADV-02)"
    - "Neither 'fts' nor 'frechet' is added to _RUNNABLE_METHODS (SC3 — diagnostics-only)"
    - "build_diagnostics uses no LLM/anthropic import on the number path (ADV-02 LLM-free)"
  artifacts:
    - python/fdars/advisor/aspects/fts.py
    - tests/test_advisor_fts.py
  key_links:
    - "advisor.__init__._supported ↔ server._DIAGNOSTICS_METHODS ↔ test _EXPECTED_DIAGNOSTICS_METHODS (three co-equal sets, one commit)"
    - "method_lc == 'fts' dispatch branch → _build_fts_diagnostics"
    - "method_lc == 'frechet' dispatch branch → _build_frechet_diagnostics (registered here; builder body ships in 72-02)"
---

<objective>
Establish the new-aspect + guard-sync + grounding loop end-to-end by shipping the `fts`
diagnostics-only aspect wired through every layer, AND registering BOTH new method strings
(`fts`, `frechet`) atomically across the three guard-sync locations in a single commit.

This is the phase TRACER: it proves the whole aspect → register → guard-sync → grounding
serialization path on ONE aspect before 72-02/72-03 expand out. The `frechet` builder body
is deferred to 72-02, but the `frechet` method string is registered here (a placeholder
dispatch branch + a stub builder import) so the atomic guard-sync commit covers both new
strings and `test_guard_sync_version_independent.py` never sees an inconsistent set.

Purpose: de-risk the architecture — if the grounding/guard-sync/serialization contract is
wrong, catch it after one aspect, not after five.
Output: `fts.py` builder, `frechet.py` created with a real `_build_frechet_diagnostics`
(minimal frechet_mean + None-safe dict path — enough to be registered and serialize; 72-02
fills the anova/reg branches), atomic 3-location guard-sync for `fts`+`frechet`, and
`tests/test_advisor_fts.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-advisor-extension/72-RESEARCH.md
@.planning/phases/72-advisor-extension/72-CONTEXT.md
@python/fdars/advisor/aspects/regression.py
@python/fdars/advisor/__init__.py
@python/fdars/mcp/server.py
@tests/test_guard_sync_version_independent.py
@tests/test_advisor_group_b.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: fts aspect end-to-end — builder + register fts AND frechet atomically across all guard-sync locations</name>
  <files>python/fdars/advisor/aspects/fts.py, python/fdars/advisor/aspects/frechet.py, python/fdars/advisor/__init__.py, python/fdars/mcp/server.py, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §Section 1 (aspect pattern), §2A (fts field lists), §Section 3 (guard-sync 4-location mechanics)
    - python/fdars/advisor/aspects/regression.py (canonical grounding discipline — float()/int()/bool() casts, `if "k" in raw` guards, None fallback, `{"method": ...}` first key)
    - python/fdars/advisor/__init__.py:141-232 (the `_supported` set + the if-chain dispatch; add fts/frechet branches after the `inference` branch)
    - python/fdars/mcp/server.py:52-86 (`_RUNNABLE_METHODS` stays UNCHANGED; add fts/frechet ONLY to `_DIAGNOSTICS_METHODS`)
    - tests/test_guard_sync_version_independent.py:38-57 (`_EXPECTED_DIAGNOSTICS_METHODS` literal)
    - src/fts_mod.rs:48-58 (ftsm dict keys: mean, rotation, scores, fitted, weights, ncomp, ar_models — each ar_model has order/phi/sigma2), :249-262 (fplsr: forecast, fitted, ncomp), :298-312 (functional_acf: lags int64, acf, pacf, upper_band), :431-445 (stationarity_test: statistic, p_value, n_perm), :581-620 (dpca_result_to_dict: filters, scores, eigenvalues, n_freqs, filter_lag, ncomp, valid_range)
  </read_first>
  <behavior>
    - build_diagnostics(ftsm(data, argvals, ncomp=3), method="fts") returns dict with method=="fts", has_ftsm True, ncomp==3 (int), n_ar_models==3
    - build_diagnostics(stationarity_test(...), method="fts") returns has_stationarity True, stationarity_p_value a float in [0,1], n_perm int
    - build_diagnostics(functional_acf(...), method="fts") returns has_acf True, n_lags int
    - build_diagnostics(dpca(...), method="fts") returns has_dpca True, dpca_ncomp int, filter_lag int
    - build_diagnostics(fplsr(...), method="fts") returns has_fplsr True, fplsr_ncomp int
    - json.dumps(diag, sort_keys=True) succeeds for each; no value is an instance of np.generic
    - two calls on the same input return an equal dict (deterministic)
    - build_diagnostics({}, "__sentinel__") ValueError message lists 'fts' AND 'frechet' in Supported
  </behavior>
  <action>
    Create `_build_fts_diagnostics(raw, **kwargs) -> dict` in fts.py mirroring regression.py
    discipline VERBATIM. First key `{"method": "fts"}`. Use the RESEARCH §2A discriminator
    hierarchy — each `has_<fn>` is an independent key-presence test with a stable None fallback
    for its fields when the discriminator is False (so EVERY output key is always present
    regardless of which fts function produced `raw`, keeping the dict shape deterministic and
    every serialization test's key set stable):
      - stationarity: `"n_perm" in raw and "statistic" in raw and "p_value" in raw` → stationarity_statistic=float(raw["statistic"]), stationarity_p_value=float(raw["p_value"]), n_perm=int(raw["n_perm"])
      - acf: `"upper_band" in raw` → n_lags=int(len(np.asarray(raw["lags"]))), acf_at_lag1/acf_decay from np.asarray(raw["acf"]) with len>0 guards → float()
      - dpca: `"filter_lag" in raw and "n_freqs" in raw` → dpca_ncomp=int(raw["ncomp"]), n_freqs=int(raw["n_freqs"]), filter_lag=int(raw["filter_lag"]), dpca_eigenvalues=[float(v) for v in np.asarray(raw["eigenvalues"])]
      - fplsr: `"fitted" in raw and "forecast" in raw and "ncomp" in raw and np.asarray(raw["forecast"]).ndim == 2 and np.asarray(raw["forecast"]).shape[0] == 1` → fplsr_ncomp=int(raw["ncomp"]), fplsr_fitted_rmse=float(np.sqrt(np.mean(np.asarray(raw["fitted"])**2)))
      - ftsm: `"ar_models" in raw` → ncomp=int(raw["ncomp"]), n_obs=int(np.asarray(raw["scores"]).shape[0]), n_points=int(np.asarray(raw["mean"]).shape[0]), n_ar_models=int(len(raw["ar_models"])), ar_max_order=int(max(int(m["order"]) for m in raw["ar_models"])) if raw["ar_models"] else None, ar_sigma2_max=float(max(float(m["sigma2"]) for m in raw["ar_models"])) if raw["ar_models"] else None, fitted_rmse=float(np.sqrt(np.mean(np.asarray(raw["fitted"])**2)))
      - forecast (ftsm_forecast): `"forecast" in raw and "h" in raw` (and NOT the fplsr fitted+ncomp shape) → h=int(raw["h"]), forecast_mean=float(np.mean(np.asarray(raw["forecast"])))
    Order discriminators so fplsr (fitted+forecast+ncomp) is tested before the bare forecast
    branch to avoid mislabeling. Cast EVERY numeric value with float()/int()/bool() — np.max /
    np.mean return numpy scalars; per RESEARCH Pitfall 2 always wrap.
    Do NOT import anthropic or any provider. Use only numpy + raw dict access (LLM-free).

    Create frechet.py with a real `_build_frechet_diagnostics(raw, **kwargs) -> dict` per the
    RESEARCH §Code-Examples skeleton: `{"method": "frechet"}` first, `isinstance(raw, dict)`
    branch for the frechet_mean array path (frechet_mean_ndim/dim/trace via np.asarray), and a
    None-safe dict path that sets has_anova/has_global_reg/has_local_reg False with all fields
    None. This is enough to register+serialize now; 72-02 fills the real anova/reg field logic.

    In __init__.py: add `"fts"` and `"frechet"` to the `_supported` set (with a
    `# ADV-01 Phase 72 — diagnostics-only` comment). Add two dispatch branches after the
    `inference` branch (line ~229): `if method_lc == "fts": from fdars.advisor.aspects.fts
    import _build_fts_diagnostics; return _build_fts_diagnostics(raw, **kwargs)` and the
    matching `frechet` branch importing `_build_frechet_diagnostics`.

    In server.py: add `"fts"` and `"frechet"` to `_DIAGNOSTICS_METHODS` ONLY. Do NOT touch
    `_RUNNABLE_METHODS` (stays `{alignment,fpca,basis,smoothing,clustering,depth}`) — SC3.

    In test_guard_sync_version_independent.py: add `"fts"` and `"frechet"` to
    `_EXPECTED_DIAGNOSTICS_METHODS`.

    ATOMICITY: all five files (fts.py, frechet.py, __init__.py, server.py, guard-sync test)
    land in ONE commit per ADV-02 — the guard-sync test must never observe advisor._supported
    and _EXPECTED_DIAGNOSTICS_METHODS disagreeing. Commit fts.py + test_advisor_fts.py (Task 2)
    together with this atomic set, or ensure Task 2's test file joins the same commit.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -vE '^\s*#' python/fdars/mcp/server.py | sed -n '/_RUNNABLE_METHODS = frozenset/,/)/p' | grep -Ec 'fts|frechet' | grep -qx 0 && grep -q '"fts"' python/fdars/advisor/__init__.py && grep -q '"frechet"' python/fdars/advisor/__init__.py && grep -q '"fts"' tests/test_guard_sync_version_independent.py && .venv/bin/python -m pytest tests/test_guard_sync_version_independent.py -x -q</automated>
    <fails_when>fts or frechet is added to _RUNNABLE_METHODS (grep count != 0 — SC3 violated), OR fts/frechet is missing from advisor _supported / server _DIAGNOSTICS_METHODS / the guard-sync literal (guard-sync test fails on set mismatch).</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && for f in python/fdars/advisor/__init__.py python/fdars/mcp/server.py tests/test_guard_sync_version_independent.py; do git show HEAD --stat --name-only --pretty=format: | grep -qx "$f" || { echo "ATOMICITY FAIL: $f not in HEAD commit"; exit 1; }; git show "HEAD:$f" | grep -q '"fts"' || { echo "ATOMICITY FAIL: $f lacks fts"; exit 1; }; git show "HEAD:$f" | grep -q '"frechet"' || { echo "ATOMICITY FAIL: $f lacks frechet"; exit 1; }; done && echo "Atomicity verified: all 3 guard-sync locations carry fts+frechet in HEAD"</automated>
    <fails_when>Any of the three guard-sync files (advisor __init__.py, mcp server.py, guard-sync test literal) is absent from the HEAD commit, or any of the three lacks 'fts' or 'frechet' — i.e. the three frozenset edits did not land together in ONE atomic commit (ADV-02).</fails_when>
  </verify>
  <acceptance_criteria>
    - `_build_fts_diagnostics` exists in fts.py; imports only numpy (no anthropic/provider import)
    - `"fts"` and `"frechet"` present in advisor `_supported`, server `_DIAGNOSTICS_METHODS`, and test `_EXPECTED_DIAGNOSTICS_METHODS`; ABSENT from both `_RUNNABLE_METHODS` frozensets
    - `test_guard_sync_version_independent.py` passes (both the 3.9+ primary and 3.10+ companion assertions)
    - frechet.py exists with a serializable `_build_frechet_diagnostics` (frechet_mean array path + None-safe dict path)
  </acceptance_criteria>
  <reversibility rating="reversible">Additive Python: new aspect files + set membership; revert by removing the two strings and the two files.</reversibility>
  <done>The fts aspect is registered end-to-end; both new method strings are registered atomically across all three guard-sync locations; guard-sync test green; frechet method string reserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fts per-aspect serialization + grounding + determinism tests</name>
  <files>tests/test_advisor_fts.py</files>
  <read_first>
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §Section 4 (per-aspect serialization test skeleton, check_no_numpy helper)
    - tests/test_advisor_group_b.py:41-116 (TestElasticMultinomial fixture + serialization/no-numpy/method-field test pattern to mirror)
    - src/fts_mod.rs:48-58 (ftsm signature: data, argvals, ncomp), :431-445 (stationarity_test: data, argvals, n_perm, seed), :298-312 (functional_acf: data, argvals, max_lag, n_sim, ci, seed), :647-660 (dpca: data, argvals, ncomp, bandwidth, filter_lag), :249-262 (fplsr: data, argvals, ncomp)
    - python/fdars/advisor/aspects/fts.py (the builder shipped in Task 1 — assert on its actual output keys)
  </read_first>
  <behavior>
    - TestFtsAspect covers ftsm, stationarity_test, functional_acf, dpca, fplsr fixtures
    - For each: build_diagnostics(result, method="fts") — json.dumps(diag, sort_keys=True) does not raise
    - check_no_numpy(diag) passes (recursively asserts no np.generic value)
    - two calls on the same fixture return json-equal dicts (determinism)
    - diag["method"] == "fts" for every fixture
    - ftsm fixture asserts has_ftsm True and ncomp == 3; stationarity asserts 0.0 <= stationarity_p_value <= 1.0
  </behavior>
  <action>
    Create tests/test_advisor_fts.py mirroring the RESEARCH §4 skeleton and the
    TestElasticMultinomial structure. Include the `check_no_numpy(obj)` recursive helper
    (assert not isinstance(obj, np.generic); recurse into dict values and list items).
    Build fixtures from real fdars.fts calls with a seeded rng (np.random.default_rng(42)),
    non-square shape N=40, M=25, argvals=np.linspace(0,1,M): ftsm(data, argvals, ncomp=3);
    stationarity_test(data, argvals, n_perm=20, seed=42); functional_acf(data, argvals,
    max_lag=5, n_sim=20, ci=0.95, seed=42); dpca(data, argvals, ncomp=2); fplsr(data,
    argvals, ncomp=2). Use the ACTUAL keyword names from the Rust signatures read above.
    All tests offline — no ANTHROPIC_API_KEY. Assert grounding via check_no_numpy +
    json.dumps, plus a determinism assertion and the p_value-in-[0,1] range check.
    NO synthetic assertions on Python-derived numbers — only assert on values the builder
    reads from fdars output.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_fts.py -x -q</automated>
    <fails_when>a numpy scalar leaks into the fts diagnostics dict (check_no_numpy raises), json.dumps fails, or two calls on the same input return unequal dicts.</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_advisor_fts.py exists and passes offline (no network, no API key)
    - covers all five fts result shapes with json.dumps + check_no_numpy + determinism + method-field assertions
    - the p_value range assertion (0<=p<=1) and ncomp==3 assertion hold against real fdars output
  </acceptance_criteria>
  <done>fts aspect proven grounded, JSON-serialisable, and deterministic by an offline test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fdars result → build_diagnostics | `raw` dict/array is untrusted input; every key access must be guarded |
| build_diagnostics → json.dumps | numpy scalars must be cast out before serialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Tampering | fts.py key access | medium | mitigate | Guard every optional key with `if "k" in raw`; None fallback (ASVS V5, mirrors regression.py) |
| T-72-02 | Tampering | numpy scalar into json.dumps | high | mitigate | float()/int()/bool() cast on every value; check_no_numpy in tests |
| T-72-03 | Tampering | guard-sync set drift | high | mitigate | Atomic single-commit edit of all three sets; test_guard_sync_version_independent.py gate |
| T-72-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase (pure Python edits; fdars already built) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_guard_sync_version_independent.py tests/test_advisor_fts.py -x -q` green
- No `_RUNNABLE_METHODS` change (grep gate in Task 1)
- No anthropic import reachable from build_diagnostics number path
</verification>

<success_criteria>
- fts aspect registered end-to-end and serializes grounded native scalars (ADV-01)
- fts + frechet method strings added atomically across all three guard-sync locations (ADV-02)
- frechet NOT runnable (SC3); build_diagnostics number path LLM-free (ADV-02)
</success_criteria>

<output>
Create `.planning/phases/72-advisor-extension/72-01-SUMMARY.md` when done
</output>
---
phase: 71-shapelets-gak-metric
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - src/metric_mod.rs
  - tests/test_gak.py
autonomous: true
requirements: [SHAPE-02]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "gak, gak_gram_matrix, gak_gram_train, gak_gram_predict, sigma_gak extend fdars.metric (SHAPE-02, roadmap SC3)"
    - "gak_gram_matrix returns a symmetric (n,n) matrix with unit diagonal, directly usable as a precomputed kernel (SHAPE-02, roadmap SC3)"
    - "gak_gram_train returns a PyGakGramTrain opaque handle; gak_gram_predict(handle, new_data) returns (n_test, n_train) usable with sklearn metric=precomputed (SHAPE-02, roadmap SC3)"
  artifacts:
    - src/metric_mod.rs
    - tests/test_gak.py
  key_links:
    - "metric_mod::register adds PyGakGramTrain + 5 GAK functions to the existing fdars.metric module"
    - "PyGakGramTrain.inner is the whole GakGramTrain struct; gak_gram_predict passes &train.inner into core, which reads its pub(crate) fields"
---

<objective>
Extend `fdars.metric` (SHAPE-02) with the Global-Alignment-Kernel: bind `gak`, `sigma_gak`,
`gak_gram_matrix`, `gak_gram_train`, and `gak_gram_predict`, plus a `PyGakGramTrain` opaque
`#[pyclass]` handle (pyfda's 4th opaque handle), all inside the existing `src/metric_mod.rs`.
The Gram output is a precomputed-kernel contract: `gak_gram_matrix` → symmetric `(n,n)`,
`gak_gram_predict` → `(n_test, n_train)`.

Purpose: SHAPE-02 delivers roadmap Phase 71 success criterion 3 — GAK metric functions extend
`fdars.metric` with the Gram usable as a precomputed sklearn kernel.
Output: GAK additions to `src/metric_mod.rs` and a new `tests/test_gak.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-shapelets-gak-metric/71-RESEARCH.md
@.planning/phases/71-shapelets-gak-metric/71-CONTEXT.md
@src/metric_mod.rs
@src/multi_fdata_mod.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: gak_gram_matrix + gak + sigma_gak — metric extension proven end-to-end</name>
  <files>src/metric_mod.rs, tests/test_gak.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Sections 2.1 gak/sigma_gak/gak_gram_matrix signatures, 2.2 GakConfig non_exhaustive, 2.4 Gram shape contract, 4.2, 5.2 register extension, 5.4 import path, 6.2 fixtures)
    - src/metric_mod.rs (existing #[pyfunction] style, convert::* imports, register() to extend)
  </read_first>
  <behavior>
    - gak(X, Y, sigma) where X==Y returns ~1.0 (within 1e-9); gak on distinct series returns a value in [0, 1].
    - sigma_gak(TRAIN_MAT) returns a float > 0.
    - gak_gram_matrix(TRAIN_MAT) returns shape (8, 8) with diagonal all ~1.0 and G == G.T (symmetric) — non-square input (8 rows, 25 cols) proves the metric extension and no transposition.
  </behavior>
  <action>
    Extend src/metric_mod.rs (do NOT create a new file — GAK lives in fdars.metric). Lead with the one-shot Gram + scalars path; leave the train/predict handle for Tasks 2-3.

    Add imports at the top of metric_mod.rs: use fdars_core::metric::gak::{gak as core_gak, sigma_gak as core_sigma_gak, gak_gram_matrix as core_gak_gram_matrix, GakConfig}; (alias to avoid name collision with the #[pyfunction]s). Per RESEARCH 5.4 / Assumption A2, if that submodule path fails at cargo check, try the crate-root re-export; fix the path, add no unused imports (CI -D warnings).

    Bind gak(x: PyReadonlyArray1<f64>, y: PyReadonlyArray1<f64>, sigma: f64) -> PyResult<f64>: call core_gak(x.as_slice()?, y.as_slice()?, sigma), return the f64 (core_gak is infallible in 0.33, RESEARCH 2.1 — no to_pyresult needed).

    Bind sigma_gak(data: PyReadonlyArray2<f64>) -> PyResult<f64>: numpy2d_to_fdmatrix then core_sigma_gak(&mat), return the f64 (infallible).

    Bind gak_gram_matrix with #[pyo3(signature = (data, sigma=None))] taking sigma: Option<f64>: numpy2d_to_fdmatrix, build GakConfig { sigma } (single public field — no ..Default needed even though GakConfig is #[non_exhaustive], per RESEARCH Pitfall 7), call core_gak_gram_matrix(&mat, &config) via to_pyresult, return fdmatrix_to_numpy2d (already handles column-major → row-major; no extra transpose, RESEARCH 2.4).

    Extend the existing metric_mod::register() (the function at the bottom that already registers lp_/hausdorff_/dtw_ etc.) by adding wrap_pyfunction! lines for gak, sigma_gak, gak_gram_matrix. Do NOT touch lib.rs or __init__.py — fdars.metric is already registered.

    Create tests/test_gak.py with the RESEARCH 6.2 fixture block (RNG seed 7, N_TRAIN=8, N_PTS=25, TRAIN_MAT; N_TEST=3, TEST_MAT; 1D X, Y identical and Z) and tests: test_gak_self_similarity (gak(X, X, sigma)≈1.0; gak(X, Z, sigma) in [0,1]), test_sigma_gak (float > 0), test_gram_matrix_shape ((8,8), diagonal≈1.0, np.allclose(G, G.T)).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.metric as m; assert callable(m.gak) and callable(m.sigma_gak) and callable(m.gak_gram_matrix)" && .venv/bin/python -m pytest tests/test_gak.py::test_gak_self_similarity tests/test_gak.py::test_sigma_gak tests/test_gak.py::test_gram_matrix_shape -x -q</automated>
    <fails_when>gak/sigma_gak/gak_gram_matrix are not on fdars.metric, gram is not (8,8), the diagonal is not ~1.0, or the gram is not symmetric.</fails_when>
  </verify>
  <acceptance_criteria>
    - gak, sigma_gak, gak_gram_matrix are callable on fdars.metric after maturin develop.
    - gak(X,X,sigma)≈1.0; sigma_gak > 0; gram is (8,8), unit-diagonal, symmetric.
    - cargo builds clean under -D warnings (no unused imports).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive functions inside an existing module; revert by removing the new fns + register lines.</reversibility>
  <done>The GAK metric extension imports and the one-shot Gram + scalars path passes all three tracer tests on non-square data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PyGakGramTrain handle + gak_gram_train</name>
  <files>src/metric_mod.rs, tests/test_gak.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Sections 2.1 gak_gram_train, 2.3 GakGramTrain fields + pub(crate) restriction, 4.2, Pitfall 2)
    - src/multi_fdata_mod.rs (opaque #[pyclass] + getter template)
  </read_first>
  <behavior>
    - gak_gram_train(TRAIN_MAT) returns a PyGakGramTrain handle.
    - handle.gram is a (8, 8) float64 array; handle.sigma > 0; handle.n_train == 8.
    - handle.gram matches gak_gram_matrix(TRAIN_MAT) within 1e-12 (same underlying Gram).
  </behavior>
  <action>
    Add use fdars_core::metric::gak::{gak_gram_train as core_gak_gram_train, GakGramTrain}; to the imports (same submodule path as Task 1).

    Define #[pyclass(name = "PyGakGramTrain")] pub struct PyGakGramTrain { pub inner: GakGramTrain } — store the WHOLE struct, including its pub(crate) log_self/train_rows which pyfda cannot read but gak_gram_predict (in-crate) can (RESEARCH 2.3, Pitfall 2). Mirror PyMultiFunData.

    Add #[pymethods]: getter gram(&self, py) -> Bound<PyArray2<f64>> = fdmatrix_to_numpy2d(py, &self.inner.gram); getter sigma(&self) -> f64 = self.inner.sigma; getter n_train(&self) -> usize = self.inner.gram.nrows().

    Bind gak_gram_train with #[pyo3(signature = (data, sigma=None))]: numpy2d_to_fdmatrix, GakConfig { sigma }, core_gak_gram_train(&mat, &config) via to_pyresult, wrap in PyGakGramTrain, return Py<PyGakGramTrain>.

    Register PyGakGramTrain (m.add_class) and gak_gram_train in metric_mod::register.

    Add tests: test_gram_train_handle (handle.gram shape (8,8), handle.sigma > 0, handle.n_train == 8) and test_gram_train_matches_matrix (np.allclose(handle.gram, gak_gram_matrix(TRAIN_MAT), atol=1e-12)).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.metric as m; assert hasattr(m, 'PyGakGramTrain') and callable(m.gak_gram_train)" && .venv/bin/python -m pytest tests/test_gak.py::test_gram_train_handle tests/test_gak.py::test_gram_train_matches_matrix -x -q</automated>
    <fails_when>gak_gram_train / PyGakGramTrain are missing, the gram is not (8,8), sigma is not > 0, n_train != 8, or the handle gram diverges from gak_gram_matrix.</fails_when>
  </verify>
  <acceptance_criteria>
    - gak_gram_train returns a PyGakGramTrain with gram (8,8), sigma > 0, n_train == 8.
    - handle.gram matches gak_gram_matrix(TRAIN_MAT) within 1e-12.
  </acceptance_criteria>
  <done>The PyGakGramTrain handle and gak_gram_train are bound, registered, and pass both tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gak_gram_predict (n_test, n_train) precomputed-kernel contract + full GAK suite</name>
  <files>src/metric_mod.rs, tests/test_gak.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Sections 2.1 gak_gram_predict, 2.4 Gram shape contract + sklearn precomputed, Pitfall 2 + Pitfall 3, Validation Architecture)
  </read_first>
  <behavior>
    - gak_gram_predict(handle, TEST_MAT) returns shape (3, 8) — n_test=3 ≠ n_train=8, proving (n_test, n_train) orientation, not (n_train, n_test).
    - gak_gram_predict(handle, TRAIN_MAT) reproduces handle.gram within 1e-12 (predicting on the training set returns the train Gram).
    - The predict output is directly usable as an sklearn precomputed kernel: K_test.shape == (n_test, n_train).
  </behavior>
  <action>
    Add use fdars_core::metric::gak::gak_gram_predict as core_gak_gram_predict; to the imports.

    Bind gak_gram_predict(train: &PyGakGramTrain, new_data: PyReadonlyArray2<f64>) -> Bound<PyArray2<f64>>: numpy2d_to_fdmatrix on new_data, call core_gak_gram_predict(&train.inner, &mat) via to_pyresult (pass &train.inner — the in-crate function reads the pub(crate) log_self/train_rows itself, RESEARCH Pitfall 2), return fdmatrix_to_numpy2d. The upstream already returns (n_test, n_train); fdmatrix_to_numpy2d preserves it — do NOT transpose (RESEARCH Pitfall 3).

    Register gak_gram_predict in metric_mod::register.

    Add tests: test_gram_predict_shape (gak_gram_predict(handle, TEST_MAT).shape == (3, 8), asserting n_test ≠ n_train so a transpose would be caught) and test_gram_predict_reproduces_train (np.allclose(gak_gram_predict(handle, TRAIN_MAT), handle.gram, atol=1e-12)).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.metric as m; assert callable(m.gak_gram_predict)" && .venv/bin/python -m pytest tests/test_gak.py tests/sklearn/test_foundation.py -q</automated>
    <fails_when>gak_gram_predict returns (8,3) instead of (3,8), the train-set prediction diverges from handle.gram, or any GAK test / the FND-02 foundation guard regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - gak_gram_predict(handle, TEST_MAT) is shape (3, 8) — the (n_test, n_train) precomputed-kernel contract.
    - gak_gram_predict(handle, TRAIN_MAT) reproduces handle.gram within 1e-12.
    - Full tests/test_gak.py and tests/sklearn/test_foundation.py are green.
  </acceptance_criteria>
  <done>gak_gram_predict enforces the (n_test, n_train) contract and the full GAK suite plus FND-02 guard pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust binding | Untrusted numpy arrays + sigma float cross into fdars-core GAK routines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-04 | Tampering | gak / gak_gram_matrix sigma param | medium | mitigate | Upstream treats sigma<=0 as 0.0 / floors sigma_gak; gak_gram_matrix surfaces FdarError via to_pyresult → ValueError |
| T-71-05 | Denial of Service | gak_gram_matrix / gak_gram_predict on empty or width-mismatched matrices | medium | mitigate | Core returns FdarError::InvalidDimension; to_pyresult converts to ValueError; no panic reaches Python |
| T-71-06 | Information disclosure | PyGakGramTrain pub(crate) fields | low | accept | log_self / train_rows are pub(crate), unreadable from pyfda; only gram/sigma/n_train are exposed — no leak path |
</threat_model>

<verification>
- `.venv/bin/maturin develop` builds clean under CI `-D warnings` (aliased imports, no unused).
- `tests/test_gak.py` all green; `tests/sklearn/test_foundation.py` green (fdars.metric already registered — GAK is an extension, not a new submodule name).
- No new external Cargo dependency; Cargo.lock never git-added.
</verification>

<success_criteria>
- gak, sigma_gak, gak_gram_matrix, gak_gram_train, gak_gram_predict extend fdars.metric (roadmap SC3).
- gak_gram_matrix → symmetric (n,n) unit-diagonal; gak_gram_predict → (n_test, n_train) precomputed-kernel contract.
- PyGakGramTrain handle stores the whole GakGramTrain and feeds predict without touching pub(crate) fields.
</success_criteria>

<output>
Create `.planning/phases/71-shapelets-gak-metric/71-02-SUMMARY.md` when done
</output>
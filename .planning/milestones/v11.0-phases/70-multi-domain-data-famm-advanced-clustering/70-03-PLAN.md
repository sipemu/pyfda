---
phase: 70-multi-domain-data-famm-advanced-clustering
plan: 03
type: execute
wave: 3
depends_on: [70-01]
files_modified:
  - src/spm_mod.rs
  - tests/test_spm_mfpca.py
autonomous: true
requirements: [MULTI-03]
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "mfpca is callable via fdars.spm and returns a 6-key PyDict; eigenfunctions and means are lists of P entries (SC3)"
    - "spe_multivariate is callable via fdars.spm and returns a naked (n,) 1D numpy array (SC3)"
    - "Both take lists of 2D arrays converted to Vec<FdMatrix>/Vec<&FdMatrix> — neither consumes PyMultiFunData; the pub(super) fields combined_rotation/scale_threshold are NOT read (SC3)"
  artifacts:
    - tests/test_spm_mfpca.py
  key_links:
    - "mfpca + spe_multivariate added to the existing spm_mod::register fn (no new register_submodule! — spm already registered)"
---

<objective>
Extend the existing `fdars.spm` submodule with the multivariate/multi-domain SPM pair the user locked
in D-01: `mfpca` (multivariate FPCA over several variables) + `spe_multivariate` (multivariate SPE
monitoring statistic) (MULTI-03, ROADMAP SC3). Sequenced AFTER 70-01 to satisfy SC3's "built AFTER
PyMultiFunData within this phase" ordering constraint. This is the ONLY plan touching `src/spm_mod.rs`.

Purpose: complete the multi-domain-monitoring story per the user decision (skip frcc / other monitors,
which are deferred).

Output: two bindings appended to `src/spm_mod.rs` + its register fn; `tests/test_spm_mfpca.py` (new).

CRITICAL — read before writing: neither `mfpca` nor `spe_multivariate` consumes `MultiFunData`
(research grep-confirmed 0 references in spm/mfpca.rs). Both take `&[&FdMatrix]` slices built from a
Python list of 2D arrays. `MfpcaResult.combined_rotation` and `.scale_threshold` are `pub(super)` —
INACCESSIBLE from the binding; do NOT attempt to read them. Expose ONLY the 6 public fields.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md
@.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-CONTEXT.md
@src/convert.rs
@src/spm_mod.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Bind mfpca into fdars.spm (list of 2D arrays → Vec<&FdMatrix>; 6-key PyDict) — one representative multivariate-SPM path, end-to-end</name>
  <files>src/spm_mod.rs</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 4.1 (mfpca signature: variables: &[&FdMatrix]; MfpcaConfig ncomp=5/weighted=true; MfpcaResult 6 PUBLIC fields only — scores, eigenfunctions, eigenvalues, means, scales, grid_sizes; combined_rotation/scale_threshold are pub(super), NOT exposed; the Vec<FdMatrix>/Vec<&FdMatrix> collect pattern; building eigenfunctions/means Python lists) and section 9 (crate paths: fdars_core::spm::mfpca::mfpca) and section 8 (multi-variable fixture)
    - src/spm_mod.rs lines 1-10 and 845-873 (imports: `use crate::convert::*;`; existing register fn to extend)
    - src/convert.rs lines 30-115 (numpy2d_to_fdmatrix, fdmatrix_to_numpy2d, vec_to_numpy1d)
  </read_first>
  <action>
    Append `#[pyfunction] mfpca<'py>(py, variables: &Bound<'py, PyList>, ncomp=5, weighted=true) ->
    PyResult<Bound<'py, PyDict>>` to src/spm_mod.rs. Build
    `let mats: Vec<FdMatrix> = variables.iter().map(|item| { let arr =
    item.downcast::<PyReadonlyArray2<f64>>...; numpy2d_to_fdmatrix(arr) }).collect::<PyResult<Vec<_>>>()?;`
    then `let refs: Vec<&FdMatrix> = mats.iter().collect();`. Build MfpcaConfig (NOT #[non_exhaustive] —
    Default + mutation: config.ncomp = ncomp; config.weighted = weighted). Call
    `to_pyresult(fdars_core::spm::mfpca::mfpca(&refs, &config))?`.

    Emit the 6-key PyDict (ONLY the public fields): scores (fdmatrix_to_numpy2d, (n,ncomp)); eigenfunctions
    (build a PyList by mapping each FdMatrix in result.eigenfunctions through fdmatrix_to_numpy2d, each
    (m_p,ncomp)); eigenvalues (vec_to_numpy1d, (ncomp,)); means (build a PyList by mapping each Vec<f64> in
    result.means through vec_to_numpy1d); scales (vec_to_numpy1d, (P,)); grid_sizes (a list of ints, e.g.
    set_item with the Vec<usize> directly or map to Python ints). Do NOT read combined_rotation or
    scale_threshold.

    Add `m.add_function(wrap_pyfunction!(mfpca, m)?)?;` to spm_mod::register.

    Explicit imports only (-D warnings). Confirm PyList/PyReadonlyArray2 are imported (add explicit `use`
    if the existing file lacks them; spm_mod uses `use crate::convert::*;`).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.spm as s; assert callable(s.mfpca); print('fdars.spm.mfpca bound OK')"</automated>
    <fails_when>The crate fails to build, mfpca is absent from fdars.spm, or a compile error occurs from reading a pub(super) field.</fails_when>
  </verify>
  <acceptance_criteria>
    - mfpca is a #[pyfunction] taking a list of 2D arrays (Vec<FdMatrix> → Vec<&FdMatrix>), added to spm_mod::register
    - Returns a 6-key PyDict with ONLY scores, eigenfunctions, eigenvalues, means, scales, grid_sizes
    - eigenfunctions and means are Python lists of P entries
    - combined_rotation / scale_threshold are NOT referenced
    - maturin develop builds green; import + attribute check passes
  </acceptance_criteria>
  <done>fdars.spm.mfpca present and returns the 6 public-field dict; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Bind spe_multivariate into fdars.spm (3 lists → &[&FdMatrix]/&[&[f64]]; naked (n,) array) + tests/test_spm_mfpca.py</name>
  <files>src/spm_mod.rs, tests/test_spm_mfpca.py</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 4.2 (spe_multivariate signature: standardized_vars: &[&FdMatrix], reconstructed_vars: &[&FdMatrix], argvals_list: &[&[f64]]; returns Vec<f64> → naked (n,) array; full path fdars_core::spm::stats::spe_multivariate) and section 11 Pitfall 4 (build Vec<Vec<f64>> first, then Vec<&[f64]> from references to satisfy lifetimes) and section 8 (multi-variable fixture)
    - src/spm_mod.rs (mfpca binding from Task 1 — reuse the Vec<FdMatrix>/Vec<&FdMatrix> collect pattern)
    - src/convert.rs (numpy1d_to_vec, vec_to_numpy1d)
  </read_first>
  <action>
    Append `#[pyfunction] spe_multivariate<'py>(py, standardized_vars: &Bound<'py, PyList>,
    reconstructed_vars: &Bound<'py, PyList>, argvals_list: &Bound<'py, PyList>) ->
    PyResult<Bound<'py, PyArray1<f64>>>` to src/spm_mod.rs. Convert the two 2D lists to
    Vec<FdMatrix> then Vec<&FdMatrix> (same pattern as mfpca). For argvals: collect
    `let av_vecs: Vec<Vec<f64>> = argvals_list.iter().map(|item| { let arr =
    item.downcast::<PyReadonlyArray1<f64>>...; Ok(numpy1d_to_vec(arr)) }).collect::<PyResult<Vec<_>>>()?;`
    THEN `let av_refs: Vec<&[f64]> = av_vecs.iter().map(|v| v.as_slice()).collect();` (Pitfall 4: av_vecs
    must outlive av_refs). Call
    `to_pyresult(fdars_core::spm::stats::spe_multivariate(&std_refs, &rec_refs, &av_refs))?` and return
    `vec_to_numpy1d(py, result)` — a NAKED 1D array, not a PyDict. Add spe_multivariate to spm_mod::register.

    Then create `tests/test_spm_mfpca.py` (NON-SQUARE multi-variable fixture per research section 8:
    n_obs=20; var1 (20,30); var2 (20,25); av1 len 30; av2 len 25). Tests:
    - test_mfpca: result = fdars.spm.mfpca([var1, var2], ncomp=4); dict has exactly the 6 keys; scores.shape == (20,4); len(result["eigenfunctions"]) == 2; len(result["means"]) == 2; eigenvalues.shape == (4,); assert "combined_rotation" not in result and "scale_threshold" not in result.
    - test_spe_multivariate: build standardized + reconstructed lists (reuse var1/var2 shapes) and argvals [av1, av2]; result = fdars.spm.spe_multivariate(std, rec, [av1, av2]); assert result.ndim == 1 and result.shape == (20,).
    Import fdars.spm; repo pytest style.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_spm_mfpca.py -x -q 2>&1 | tail -20</automated>
    <fails_when>The crate fails to build, spe_multivariate is absent, mfpca returns the wrong key-set, or spe_multivariate does not return a (n,) 1D array.</fails_when>
  </verify>
  <acceptance_criteria>
    - spe_multivariate is a #[pyfunction] returning a naked (n,) numpy array, added to spm_mod::register
    - argvals built as Vec<Vec<f64>> then Vec<&[f64]> (lifetime-correct per Pitfall 4)
    - tests/test_spm_mfpca.py exists with mfpca (6-key, no pub(super) keys) + spe_multivariate ((20,) array) tests on NON-SQUARE fixtures and passes
  </acceptance_criteria>
  <done>tests/test_spm_mfpca.py passes: mfpca 6-key dict + spe_multivariate (n,) array on non-square fixtures.</done>
</task>

</tasks>

<verification>
- `maturin develop` builds green (no -D warnings failures).
- `import fdars.spm` works; mfpca + spe_multivariate callable.
- `pytest tests/test_spm_mfpca.py -x -q` passes.
- Full suite green: `.venv/bin/pytest tests/ -q 2>&1 | tail -5`.
</verification>

<success_criteria>
MULTI-03 satisfied: `mfpca` (6-key PyDict, P-length eigenfunctions/means) + `spe_multivariate` ((n,)
array) extend `fdars.spm`, built after PyMultiFunData within the phase, both taking plain 2D-array
lists (neither consuming PyMultiFunData). (ROADMAP SC3.)
</success_criteria>

<output>
Create `.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-03-SUMMARY.md` when done.
</output>

---
phase: 70-multi-domain-data-famm-advanced-clustering
plan: 04
type: execute
wave: 4
depends_on: [70-01]
files_modified:
  - src/clustering_mod.rs
  - tests/test_clustering_advanced.py
autonomous: true
requirements: [MULTI-04]
estimate:
  tokens: 78000
  raw_tokens: 42000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "dbscan_fd, kcfc_cluster, funfem_cluster, align_cluster_fd are callable via fdars.clustering, each returning a labels/result PyDict (SC4)"
    - "dbscan_fd cluster is an int64 (n,) array with -1 encoding noise; kcfc_cluster omits fpca_models; funfem_cluster membership is (n,k); align_cluster_fd templates is a length-k list (SC4)"
    - "All four are transposition-guarded via numpy2d_to_fdmatrix and tested on NON-SQUARE fixtures; none consumes PyMultiFunData (SC4)"
  artifacts:
    - tests/test_clustering_advanced.py
  key_links:
    - "the four functions added to the existing clustering_mod::register fn (no new register_submodule! — clustering already registered)"
---

<objective>
Extend `fdars.clustering` with the four advanced clustering algorithms added at fdars-core 0.33:
`dbscan_fd`, `kcfc_cluster`, `funfem_cluster`, `align_cluster_fd` (MULTI-04, ROADMAP SC4). This
requirement is independent of PyMultiFunData; it runs in the last wave here because worktrees are
disabled and each binding phase rebuilds sequentially on main.

Purpose: expose density-based, per-cluster-FPCA, discriminative (Fisher-EM), and elastic-alignment
functional clustering with documented labels/result PyDicts.

Output: four bindings appended to `src/clustering_mod.rs` + its register fn; `tests/test_clustering_advanced.py` (new).

CRITICAL — read before writing: all four take plain `(&FdMatrix, &[f64], &Config)` — none consumes
PyMultiFunData. `DbscanResult.cluster` is `Vec<Option<usize>>` — map None → -1i64 and Some(c) → c as i64
and emit via `.into_pyarray(py)` (usize_vec_to_numpy1d CANNOT be used). `KcfcResult.fpca_models` is
internal Rust state — OMIT it from the PyDict. All four configs are `#[non_exhaustive]` → use
`Default::default()` + field mutation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md
@src/convert.rs
@src/clustering_mod.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — bind dbscan_fd into fdars.clustering (int64 cluster with -1 noise; 4-key PyDict), end-to-end</name>
  <files>src/clustering_mod.rs</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 6.1 (dbscan_fd signature; DbscanConfig eps=0.5/min_points=3 is #[non_exhaustive]; DbscanResult cluster: Vec<Option<usize>>, n_clusters, n_noise, distances; NOISE ENCODING: None→-1i64, Some(c)→c as i64 via numpy::IntoPyArray .into_pyarray(py); PyDict keys; the #[pyo3(signature)] pattern) and section 9 (path fdars_core::clustering_advanced::dbscan_fd) and section 12 (i64 noise encoding via IntoPyArray)
    - src/clustering_mod.rs lines 1-5 and 150-180 (imports: `use crate::convert::*;`, numpy PyReadonlyArray1/2; gmm_cluster Default+mutation + PyDict pattern) and 289-297 (register fn to extend)
    - src/convert.rs (numpy2d_to_fdmatrix, numpy1d_to_vec, fdmatrix_to_numpy2d)
  </read_first>
  <action>
    Append `#[pyfunction] #[pyo3(signature = (data, argvals, eps=0.5, min_points=3))] dbscan_fd<'py>(py,
    data: PyReadonlyArray2<f64>, argvals: PyReadonlyArray1<f64>, eps, min_points) ->
    PyResult<Bound<'py, PyDict>>` to src/clustering_mod.rs. Convert data via numpy2d_to_fdmatrix, argvals
    via numpy1d_to_vec. Build DbscanConfig via Default + mutation (config.eps = eps; config.min_points =
    min_points). Call `to_pyresult(fdars_core::clustering_advanced::dbscan_fd(&mat, &av, &config))?`.

    Noise encoding: `let cluster_i64: Vec<i64> = result.cluster.iter().map(|c| match c { None => -1,
    Some(v) => *v as i64 }).collect();` then `dict.set_item("cluster", cluster_i64.into_pyarray(py))?;`
    (add `use numpy::IntoPyArray;`). Do NOT use usize_vec_to_numpy1d for this field.

    Emit the 4-key PyDict: cluster (int64 (n,)), n_clusters (int), n_noise (int), distances
    (fdmatrix_to_numpy2d, (n,n)). Add dbscan_fd to clustering_mod::register.

    Explicit imports only (-D warnings).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.clustering as c; assert callable(c.dbscan_fd); print('fdars.clustering.dbscan_fd bound OK')"</automated>
    <fails_when>The crate fails to build or dbscan_fd is absent from fdars.clustering.</fails_when>
  </verify>
  <acceptance_criteria>
    - dbscan_fd is a #[pyfunction] with eps/min_points defaults, added to clustering_mod::register
    - cluster field is an int64 (n,) array with None mapped to -1 via IntoPyArray (not usize_vec_to_numpy1d)
    - Returns a 4-key PyDict (cluster, n_clusters, n_noise, distances)
    - maturin develop builds green; import + attribute check passes
  </acceptance_criteria>
  <done>fdars.clustering.dbscan_fd present, returns an int64 -1-noise cluster in a 4-key dict; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Bind kcfc_cluster (omit fpca_models; 4-key PyDict) and funfem_cluster (6-key PyDict)</name>
  <files>src/clustering_mod.rs</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 6.2 (kcfc_cluster; KcfcConfig k=2/ncomp=3/max_iter=50/seed=42; KcfcResult — fpca_models OMITTED; PyDict keys cluster/reconstruction_errors/iterations/converged) and section 6.3 (funfem_cluster; FunFemConfig k=2/ncomp=10/p_disc=0/max_iter=50/tol=1e-6/seed=42; FunFemResult; PyDict keys; p_disc=0 = auto min(k-1,ncomp_eff) — document in docstring) and section 9 (crate paths)
    - src/clustering_mod.rs (dbscan_fd binding from Task 1; gmm Default+mutation + usize_vec_to_numpy1d for plain Vec<usize> cluster fields; bool_vec / bool handling)
  </read_first>
  <action>
    Append two #[pyfunction]s to src/clustering_mod.rs.

    kcfc_cluster: `#[pyo3(signature = (data, argvals, k=2, ncomp=3, max_iter=50, seed=42))]`. Convert data/
    argvals as in dbscan_fd. Build KcfcConfig via Default + mutation (k, ncomp, max_iter, seed). Call
    `fdars_core::clustering_advanced::kcfc_cluster` via to_pyresult. Emit a 4-key PyDict: cluster
    (usize_vec_to_numpy1d — this cluster is a plain Vec<usize>, so usize_vec_to_numpy1d is correct here,
    unlike dbscan), reconstruction_errors (fdmatrix_to_numpy2d, (n,k)), iterations (int), converged (bool).
    OMIT fpca_models entirely (internal FpcaResult state, not a #[pyclass]).

    funfem_cluster: `#[pyo3(signature = (data, argvals, k=2, ncomp=10, p_disc=0, max_iter=50, tol=1e-6,
    seed=42))]` with `#[allow(clippy::too_many_arguments)]`. Build FunFemConfig via Default + mutation
    (all six fields). Docstring notes p_disc=0 selects the discriminative dimension automatically as
    min(k-1, ncomp_eff). Call funfem_cluster via to_pyresult. Emit a 6-key PyDict: cluster
    (usize_vec_to_numpy1d), membership (fdmatrix_to_numpy2d, (n,k)), disc_subspace (fdmatrix_to_numpy2d),
    log_likelihood (f64), iterations (int), converged (bool).

    Add both to clustering_mod::register. Explicit imports only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.clustering as c; assert callable(c.kcfc_cluster) and callable(c.funfem_cluster); print('kcfc_cluster + funfem_cluster bound OK')"</automated>
    <fails_when>The crate fails to build, either function is absent, or a compile error occurs from referencing fpca_models.</fails_when>
  </verify>
  <acceptance_criteria>
    - kcfc_cluster returns a 4-key PyDict (cluster, reconstruction_errors, iterations, converged); fpca_models is NOT included
    - funfem_cluster returns a 6-key PyDict (cluster, membership, disc_subspace, log_likelihood, iterations, converged)
    - Both use Default + field mutation on #[non_exhaustive] configs; both added to register
    - maturin develop builds green; import + attribute check passes
  </acceptance_criteria>
  <done>kcfc_cluster (fpca_models-omitted) + funfem_cluster present and callable in fdars.clustering; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Bind align_cluster_fd (templates as length-k list; 5-key PyDict) + tests/test_clustering_advanced.py</name>
  <files>src/clustering_mod.rs, tests/test_clustering_advanced.py</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 6.4 (align_cluster_fd; AlignClusterConfig k=2/max_iter=20/seed=42/use_amplitude_only=true/elastic_lambda=0.0/karcher_max_iter=15/karcher_tol=1e-4; AlignClusterResult; templates: Vec<Vec<f64>> → PyList of (m,) arrays; PyDict keys) and section 8 (non-square fixtures + DBSCAN noise-encoding + kcfc fpca_models-omission test snippets)
    - src/clustering_mod.rs (the three bindings just added — reuse cluster/PyList conversion patterns)
    - src/convert.rs (vec_to_numpy1d for templates)
  </read_first>
  <action>
    Append `#[pyfunction] #[pyo3(signature = (data, argvals, k=2, max_iter=20, seed=42,
    use_amplitude_only=true, elastic_lambda=0.0, karcher_max_iter=15, karcher_tol=1e-4))]
    #[allow(clippy::too_many_arguments)] align_cluster_fd` to src/clustering_mod.rs. Build
    AlignClusterConfig via Default + mutation (all seven fields). Call align_cluster_fd via to_pyresult.
    Emit a 5-key PyDict: cluster (usize_vec_to_numpy1d), templates (build a PyList by mapping each Vec<f64>
    in result.templates through vec_to_numpy1d — k entries, each (m,)), distances (fdmatrix_to_numpy2d,
    (n,k)), iterations (int), converged (bool). Add align_cluster_fd to clustering_mod::register.

    Then create `tests/test_clustering_advanced.py` (NON-SQUARE fixture per research section 8: rng
    default_rng(42); data (20,30); argvals linspace(0,1,30)). Tests:
    - test_dbscan_fd: result = fdars.clustering.dbscan_fd(data, argvals, eps=0.5, min_points=3); result["cluster"].dtype == np.int64; noise encoded as -1 present-or-absent but dtype check + keys {cluster,n_clusters,n_noise,distances}; distances.shape == (20,20).
    - test_kcfc_cluster: result = fdars.clustering.kcfc_cluster(data, argvals, k=3); assert "fpca_models" not in result; "reconstruction_errors" in result; result["reconstruction_errors"].shape == (20,3).
    - test_funfem_cluster: result = fdars.clustering.funfem_cluster(data, argvals, k=2); result["membership"].shape == (20,2); keys present.
    - test_align_cluster_fd: result = fdars.clustering.align_cluster_fd(data, argvals, k=3); len(result["templates"]) == 3; each template shape (30,); result["distances"].shape == (20,3).
    Import fdars.clustering; repo pytest style.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_clustering_advanced.py -x -q 2>&1 | tail -25</automated>
    <fails_when>The crate fails to build, align_cluster_fd is absent, dbscan cluster dtype is not int64, kcfc result contains fpca_models, or templates list length is not k.</fails_when>
  </verify>
  <acceptance_criteria>
    - align_cluster_fd returns a 5-key PyDict; templates is a length-k list of (m,) arrays
    - tests/test_clustering_advanced.py exists with the four tests on NON-SQUARE fixtures and passes
    - dbscan int64/-1-noise, kcfc fpca_models-omission, funfem (n,k) membership, align length-k templates all asserted
  </acceptance_criteria>
  <done>tests/test_clustering_advanced.py passes: all four algorithms return documented dicts on non-square fixtures.</done>
</task>

</tasks>

<verification>
- `maturin develop` builds green (no -D warnings failures).
- `import fdars.clustering` works; dbscan_fd, kcfc_cluster, funfem_cluster, align_cluster_fd callable.
- `pytest tests/test_clustering_advanced.py -x -q` passes.
- Full suite green: `.venv/bin/pytest tests/ -q 2>&1 | tail -5`.
</verification>

<success_criteria>
MULTI-04 satisfied: `dbscan_fd` (int64 -1-noise), `kcfc_cluster` (fpca_models-omitted),
`funfem_cluster` ((n,k) membership), `align_cluster_fd` (length-k templates) extend
`fdars.clustering`, each returning a documented labels/result PyDict, transposition-guarded on
NON-SQUARE fixtures. (ROADMAP SC4.)
</success_criteria>

<output>
Create `.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-04-SUMMARY.md` when done.
</output>

---
phase: 69-frechet-regression-density-fda
plan: 04
type: execute
wave: 4
depends_on: [69-03]
files_modified:
  - src/density_fda_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_density_fda.py
autonomous: true
requirements: [FRE-02]
estimate:
  tokens: 56000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "import fdars.density_fda works — the submodule is registered in lib.rs and __init__.py"
    - "normalize_density, lqd_transform, inverse_lqd, wasserstein_barycenter each return a NAKED numpy 1D array (not a dict)"
    - "inverse_lqd(lqd_transform(density)) round-trips a density back to the original grid"
    - "lqd_fpca returns a 6-key PyDict {mean, singular_values, loadings, scores, fve, ncomp} with correct shapes"
  artifacts:
    - src/density_fda_mod.rs
    - tests/test_density_fda.py
  key_links:
    - "lib.rs -> register_submodule!(m, density_fda, density_fda_mod::register)"
    - "__init__.py _submodule_names -> density_fda"
    - "lqd_fpca exposes result.fpca.rotation as key loadings; excludes centered/weights"
---

<objective>
FRE-02: Create the new `src/density_fda_mod.rs` submodule, register it end-to-end
(`fdars.density_fda` importable), and bind all five density-FDA functions: `normalize_density`,
`lqd_transform`, `inverse_lqd`, `wasserstein_barycenter` (each returning a NAKED numpy 1D array),
and `lqd_fpca` (returning a 6-key PyDict).

Purpose: Complete the density functional-data-analysis surface. Lead with a tracer
(`normalize_density`) to prove the new submodule wires up end-to-end, then fan out to the LQD
transform pair, the barycenter, and the density FPCA.
Output: `fdars.density_fda` submodule with 5 functions per 69-RESEARCH.md sections 6, 7, 9.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/scalar_on_function_mod.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — register fdars.density_fda + bind normalize_density end-to-end (naked 1D array)</name>
  <files>src/density_fda_mod.rs, src/lib.rs, python/fdars/__init__.py, tests/test_density_fda.py</files>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (Section 6 normalize_density signature+validation; Section 7 naked-1D-array returns; Section 8 density_fda_mod.rs skeleton + lib.rs/__init__.py edits; Section 10 density fixtures)
    - src/lib.rs (mod list + register_submodule! block — density_fda lines go AFTER the frechet lines added in plan 69-02)
    - python/fdars/__init__.py (_submodule_names tuple + module docstring)
    - src/scalar_on_function_mod.rs (analog: convert::* imports, register() pattern, naked-array returns)
    - src/convert.rs (numpy1d_to_vec, vec_to_numpy1d, to_pyresult)
  </read_first>
  <action>
Create `src/density_fda_mod.rs` with the module doc comment and the Section 8 skeleton imports.
Import ONLY what this plan's tasks use to keep `-D warnings` green: `numpy1d_to_vec`,
`vec_to_numpy1d`, `to_pyresult` (Tasks 1-2), plus `numpy2d_to_fdmatrix`, `fdmatrix_to_numpy2d`
and `PyDict`, `PyReadonlyArray2` for Tasks 2-3. Add each import in the task that first references
it, OR import all up front and confirm every one is referenced by the end of Task 3.

Register end-to-end:
- `src/lib.rs`: add `mod density_fda_mod;` after `mod frechet_mod;`, and
  `register_submodule!(m, "density_fda", density_fda_mod::register);` after the `frechet`
  registration line.
- `python/fdars/__init__.py`: add the `density_fda` entry to `_submodule_names` after the
  `frechet` entry (comment: Phase 69 — Density FDA), and add the density-FDA bullet to the
  module docstring per Section 8.

Bind `normalize_density(vals, argvals)` per Section 6: both args `PyReadonlyArray1<f64>` →
`numpy1d_to_vec` → `&[f64]`; call `fdars_core::density_fda::normalize_density(&vals, &argvals)`
wrapped in `to_pyresult`; return `vec_to_numpy1d(py, result)` — a NAKED numpy 1D array (NOT a
PyDict) per Section 7. Add `normalize_density` to `register()`.

Write the tracer test in `tests/test_density_fda.py` using Section 10 fixtures (N_DENS=20,
M_DENS=60, argvals_dens on [0,1]; a single unnormalized non-negative density): call
`normalize_density`, assert the result is a numpy ndarray of shape (M_DENS,) and integrates to
~1 (`np.trapz(result, argvals_dens)` within 1e-6 of 1.0). Add a negative case: a density with a
negative value raises `ValueError` (upstream validation via to_pyresult).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.density_fda as d; assert callable(d.normalize_density); print('fdars.density_fda + normalize_density wired OK')" && .venv/bin/pytest tests/test_density_fda.py -x -q 2>&1 | tail -15</automated>
    <fails_when>the density_fda submodule is not registered (import fails), OR normalize_density is not callable, OR it returns a dict instead of a naked 1D array, OR the normalized density does not integrate to ~1, OR a negative-valued density does not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - `import fdars.density_fda` succeeds and `normalize_density` is callable.
    - `lib.rs` has `mod density_fda_mod;` + `register_submodule!(..., "density_fda", ...)`; `__init__.py` `_submodule_names` includes `"density_fda"`.
    - `normalize_density` returns a naked numpy 1D array (shape (m,)) that integrates to ~1.
    - Negative-valued density raises `ValueError`.
    - Build green under `-D warnings`.
  </acceptance_criteria>
  <done>The fdars.density_fda submodule is registered and normalize_density works end-to-end returning a naked 1D array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind lqd_transform, inverse_lqd, wasserstein_barycenter (all naked 1D arrays)</name>
  <files>src/density_fda_mod.rs, tests/test_density_fda.py</files>
  <behavior>
    - lqd_transform(density, argvals, n_quantile_pts=None) returns a naked 1D array (LQD psi values); requires strictly positive density (Pitfall 6).
    - inverse_lqd(psi, t_grid, target_argvals) returns a naked 1D array (reconstructed density on target_argvals).
    - inverse_lqd(lqd_transform(density)) round-trips: the reconstructed density is close to the original (allowing LQD tolerance).
    - wasserstein_barycenter(density_matrix(N,M), argvals, weights=None) returns a naked 1D array of length M integrating to ~1.
  </behavior>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (Section 6 lqd_transform/inverse_lqd/wasserstein_barycenter signatures+validation; Section 7 naked-array returns; Section 9 return notes; Section 10 fixtures; Pitfall 6)
    - src/density_fda_mod.rs (append after Task 1)
  </read_first>
  <action>
Append three functions to `src/density_fda_mod.rs`, each returning a naked numpy 1D array via
`vec_to_numpy1d`:

`lqd_transform(density, argvals, n_quantile_pts=None)` per Section 6: `density`, `argvals` via
`numpy1d_to_vec`; `n_quantile_pts: Option<usize>` default `None` (upstream applies
`argvals.len().max(101)`). Use `#[pyo3(signature = (density, argvals, n_quantile_pts=None))]`.
Call `fdars_core::density_fda::lqd_transform(&density, &argvals, n_quantile_pts)` via
`to_pyresult`. Docstring MUST note per Pitfall 6 that `density` must be STRICTLY positive
(> 0 everywhere), stricter than `normalize_density`.

`inverse_lqd(psi, t_grid, target_argvals)` per Section 6: all three `PyReadonlyArray1<f64>` →
`numpy1d_to_vec`; call `fdars_core::density_fda::inverse_lqd(&psi, &t_grid, &target_argvals)` via
`to_pyresult`; return `vec_to_numpy1d`.

`wasserstein_barycenter(density_matrix, argvals, weights=None)` per Section 6: `density_matrix`
(PyReadonlyArray2 f64) via `numpy2d_to_fdmatrix`; `argvals` via `numpy1d_to_vec`; `weights:
Option<PyReadonlyArray1<f64>>` → `Option<Vec<f64>>` via `.map(numpy1d_to_vec)`, `.as_deref()`
for the upstream `Option<&[f64]>`. Use `#[pyo3(signature = (density_matrix, argvals,
weights=None))]`. Call `fdars_core::density_fda::wasserstein_barycenter(&mat, &argvals,
weights_ref)` via `to_pyresult`; return `vec_to_numpy1d`.

Add all three to `register()`.

Append tests to `tests/test_density_fda.py` using Section 10 fixtures (a strictly-positive
`density_single` on `argvals_dens`; the (N_DENS, M_DENS) `density_matrix`):
- `test_lqd_round_trip`: `psi = lqd_transform(density_single, argvals_dens)`; obtain the
  quantile t-grid as `np.linspace(0,1,len(psi))`; `recon = inverse_lqd(psi, t_grid,
  argvals_dens)`; assert `recon.shape == (M_DENS,)` and `recon` is close to a re-normalized
  `density_single` within a loose tolerance (LQD round-trip is approximate — use atol ~1e-2 or
  compare after `normalize_density`). Assert both returns are naked ndarrays, not dicts.
  (If the exact t-grid construction differs, read the upstream `lqd_transform` return-grid
  contract in Section 6 and match it; the round-trip must be numerically stable.)
- `test_wass_bary`: `bary = wasserstein_barycenter(density_matrix, argvals_dens)`; assert
  `bary.shape == (M_DENS,)` and `np.trapz(bary, argvals_dens)` ≈ 1.0 within 1e-3.
- Negative case: `lqd_transform` on a density containing a zero raises `ValueError`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_density_fda.py -x -q 2>&1 | tail -20</automated>
    <fails_when>any of the three returns a dict instead of a naked 1D array, OR the LQD round-trip does not reconstruct a density of shape (M_DENS,) close to the original, OR the Wasserstein barycenter does not integrate to ~1, OR lqd_transform on a non-strictly-positive density does not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - `lqd_transform`, `inverse_lqd`, `wasserstein_barycenter` each return a naked numpy 1D array.
    - `inverse_lqd(lqd_transform(density))` reconstructs a density of shape (M_DENS,) close to the original.
    - `wasserstein_barycenter` returns length-M and integrates to ~1.
    - A non-strictly-positive density into `lqd_transform` raises `ValueError`.
  </acceptance_criteria>
  <done>The LQD transform pair round-trips and the Wasserstein barycenter is bound, all as naked 1D arrays, with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind lqd_fpca (6-key PyDict: mean, singular_values, loadings, scores, fve, ncomp)</name>
  <files>src/density_fda_mod.rs, tests/test_density_fda.py</files>
  <behavior>
    - lqd_fpca(density_matrix(N,M), argvals, ncomp=3, n_quantile_pts=None) returns a 6-key dict.
    - keys: mean (n_q,), singular_values (ncomp,), loadings (n_q, ncomp), scores (N, ncomp), fve (ncomp,), ncomp (int).
    - centered and weights are NOT exposed.
  </behavior>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (Section 6 lqd_fpca signature; Section 7 LqdFpcaResult + FpcaResult fields + convention rotation->loadings, exclude centered/weights; Section 9 lqd_fpca 6-key table; Section 10 fixtures)
    - src/density_fda_mod.rs (append after Task 2)
    - src/pace_fpca_mod.rs (analog: FPCA-style PyDict assembly, eigenfunctions/rotation exposure convention)
  </read_first>
  <action>
Append `lqd_fpca(density_matrix, argvals, ncomp=3, n_quantile_pts=None)` per Section 6:
`density_matrix` (PyReadonlyArray2 f64) via `numpy2d_to_fdmatrix`; `argvals` via `numpy1d_to_vec`;
`ncomp: usize` default 3; `n_quantile_pts: Option<usize>` default None. Use
`#[pyo3(signature = (density_matrix, argvals, ncomp=3, n_quantile_pts=None))]`. Call
`fdars_core::density_fda::lqd_fpca(&mat, &argvals, ncomp, n_quantile_pts)` via `to_pyresult`.

`LqdFpcaResult` is `#[non_exhaustive]` with fields `fpca: FpcaResult` and `fve: Vec<f64>`. Build
the 6-key PyDict EXACTLY per Section 9 lqd_fpca table — access nested fields by name only:
- `mean` → `vec_to_numpy1d(py, result.fpca.mean)` — (n_q,)
- `singular_values` → `vec_to_numpy1d(py, result.fpca.singular_values)` — (ncomp,)
- `loadings` → `fdmatrix_to_numpy2d(py, &result.fpca.rotation)` — (n_q, ncomp) — NOTE the key is
  `loadings` (project convention), the source field is `rotation`.
- `scores` → `fdmatrix_to_numpy2d(py, &result.fpca.scores)` — (N, ncomp)
- `fve` → `vec_to_numpy1d(py, result.fve)` — (ncomp,)
- `ncomp` → `result.fpca.scores.ncols() as i64` — actual retained components
EXCLUDE `result.fpca.centered` and `result.fpca.weights` (internal SVD state) per Section 7.
Add `lqd_fpca` to `register()`.

Append `test_lqd_fpca` to `tests/test_density_fda.py` using the Section 10 (N_DENS, M_DENS)
`density_matrix` with `ncomp=3`: assert the dict has exactly the 6 keys; assert shapes
`mean` (n_q,), `singular_values` (k,), `loadings` (n_q, k), `scores` (N_DENS, k), `fve` (k,)
where k == `result["ncomp"]` <= 3; assert `centered` and `weights` are NOT present as keys.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_density_fda.py -x -q 2>&1 | tail -25</automated>
    <fails_when>lqd_fpca is missing any of the 6 keys, OR exposes centered/weights, OR the loadings/scores shapes are transposed, OR the key is `rotation` instead of `loadings`, OR `ncomp` is not an int.</fails_when>
  </verify>
  <acceptance_criteria>
    - `lqd_fpca` returns exactly {mean, singular_values, loadings, scores, fve, ncomp}; no `centered`/`weights`.
    - Shapes: mean (n_q,), singular_values (k,), loadings (n_q, k), scores (N_DENS, k), fve (k,), ncomp int.
    - `loadings` (not `rotation`) is the key exposing `result.fpca.rotation`.
    - Full `tests/test_density_fda.py` passes (normalize + lqd pair + barycenter + fpca).
  </acceptance_criteria>
  <done>lqd_fpca returns the documented 6-key dict with correct shapes and the full density_fda test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust | numpy density vectors/matrices + argvals cross into the density_fda bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-09 | Tampering | Non-positive density into lqd_transform (log(0) = -inf) | medium | mitigate | Upstream validates `density[i] > 0.0` → `FdarError` → `PyValueError` via `to_pyresult` |
| T-69-10 | Tampering | Negative / non-normalized weights in wasserstein_barycenter | medium | mitigate | Upstream validates `w[i] >= 0.0` and normalization → `PyValueError` |
| T-69-11 | Tampering | Invalid argvals (non-increasing, len<2) or negative density in normalize_density | medium | mitigate | Upstream `normalize_density` validation returns `FdarError` (no panic) |
</threat_model>

<verification>
- `import fdars.density_fda` works; all five functions callable.
- `pytest tests/test_density_fda.py -x` passes (naked-array returns, LQD round-trip, barycenter integral, 6-key fpca).
- Build green under `-D warnings` (no unused imports).
</verification>

<success_criteria>
The `fdars.density_fda` submodule is registered and all five density-FDA functions return their
documented shapes — four naked 1D arrays and one 6-key PyDict (`lqd_fpca` with `rotation` exposed
as `loadings`, excluding internal SVD state).
</success_criteria>

<output>
Create `.planning/phases/69-frechet-regression-density-fda/69-04-SUMMARY.md` when done.
</output>

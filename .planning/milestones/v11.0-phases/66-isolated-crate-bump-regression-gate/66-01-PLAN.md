---
phase: 66-isolated-crate-bump-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 34000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "fdars-core is pinned at 0.33.0 (parallel feature only, no linalg) in Cargo.toml (DEP-01)"
    - "Cargo.lock records the 0.33.0 checksum for fdars-core after cargo update (DEP-01)"
    - "maturin develop --release builds green and `import fdars` works, MSRV 1.83 unchanged (DEP-01)"
    - "The full Python suite passes with zero new failures vs baseline; any tolerance change documented (DEP-02)"
    - "A 0.24→0.33 changelog + enum/match-arm API audit is recorded; the four 0.30-deprecated 2D depth functions are flagged for later migration (DEP-03)"
    - "Only Cargo.toml (tracked) and Cargo.lock (on-disk, gitignored) change; no src/ or test edits (success criterion #4)"
  artifacts:
    - "Cargo.toml (fdars-core = 0.33.0)"
    - "Cargo.lock (fdars-core 0.33.0 checksum)"
    - ".planning/phases/66-isolated-crate-bump-regression-gate/66-AUDIT.md (changelog + API audit + deprecation flags)"
  key_links:
    - "Cargo.toml version string -> cargo update -> Cargo.lock checksum -> maturin develop rebuilds .venv extension -> pytest exercises the rebuilt native surface"
---

<objective>
Bump the pinned `fdars-core` crate from `0.23.0` to `0.33.0` on the proven-green baseline, then gate the change through a full Python regression run and a mechanical enum/match-arm API audit. This isolates the sole numeric change (a 10-minor drift jump) from all later binding work so binding-correctness issues cannot hide behind an upgrade regression.

Purpose: A 10-minor jump (0.23→0.33) triples silent numeric-drift risk vs prior 3-minor waves. The full ~772-test Python suite — not `cargo build` alone — is the gate. Later phases (67–71) build new bindings on top of this proven baseline.

Output:
- `Cargo.toml` pin at `0.33.0` (parallel only, no linalg) + refreshed `Cargo.lock` (on-disk; gitignored).
- A recorded 0.24→0.33 changelog + API audit note (`66-AUDIT.md` in the phase dir) confirming every existing `match`-arm/enum-variant string in `src/*_mod.rs` still exists at 0.33, flagging the four 0.30-deprecated 2D depth functions for later migration.
- No new code symbols. No `src/` edits (except the documented CONTINGENCY in Task 2). No test edits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-isolated-crate-bump-regression-gate/66-RESEARCH.md

# Source of the sole edit
@Cargo.toml
</context>

<artifacts_produced>
This phase produces NO new code symbols. It produces:
- The bumped `Cargo.toml` pin (`fdars-core = { version = "0.33.0", features = ["parallel"] }`).
- The refreshed `Cargo.lock` on disk (fdars-core 0.33.0 checksum). NOTE: `Cargo.lock` is gitignored in this repo (`git ls-files` shows only `Cargo.toml`; `.gitignore` lists `Cargo.lock`). It MUST be updated on disk so the build resolves 0.33.0, but it is NOT committed — do not attempt to `git add Cargo.lock`.
- `.planning/phases/66-isolated-crate-bump-regression-gate/66-AUDIT.md` — the recorded 0.24→0.33 changelog summary, the enum/match-arm audit result, and the flagged four 0.30-deprecated 2D depth functions (plus `fanova`). Audit findings go HERE, never into `src/`.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Bump the fdars-core pin and refresh the lockfile</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (line ~18): current pin verbatim `fdars-core = { version = "0.23.0", features = ["parallel"] }` [66-RESEARCH.md, "Standard Stack", VERIFIED Cargo.toml:18]
    - 66-RESEARCH.md "Build and Test Recipe" step 2 — the `cargo update -p fdars-core` command and its rationale (restricts update to fdars-core + transitive deps only)
  </read_first>
  <action>
    This is the tracer slice for the phase — one path (Cargo.toml version string) through the whole stack (lockfile → build → runtime), proven end-to-end. Change ONLY the fdars-core version string in `Cargo.toml` from `0.23.0` to `0.33.0`, preserving `features = ["parallel"]` exactly (DEP-01). Do NOT add the `linalg` feature — user decision, no v11.0 capability needs it. Do NOT touch any other line of `Cargo.toml` and do NOT touch any other dependency. Then run `cargo update -p fdars-core` from the repo root to regenerate the `Cargo.lock` entry (do not hand-edit `Cargo.lock`). This refreshes fdars-core and only its transitive deps; nalgebra ^0.33 and rayon ^1.10 are identical at both versions so no other locked version should move. After the update, confirm the git working tree shows a change to `Cargo.toml` only among tracked files (Cargo.lock is gitignored and will not appear in `git status` for tracked paths).
    <reversibility rating="reversible">A dependency version bump is fully reversible via git revert of Cargo.toml + `cargo update`; no one-way-door checkpoint needed (REVERSIBILITY_GATES).</reversibility>
  </action>
  <acceptance_criteria>
    - Cargo.toml fdars-core line reads exactly `fdars-core = { version = "0.33.0", features = ["parallel"] }`.
    - No `linalg` feature present anywhere for fdars-core.
    - Cargo.lock on disk shows `version = "0.33.0"` for fdars-core with a new checksum.
    - `git status --porcelain` lists Cargo.toml as the only modified tracked source file (Cargo.lock is gitignored).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -n 'fdars-core' Cargo.toml && grep -A2 '^name = "fdars-core"' Cargo.lock | grep 'version'</automated>
    <fails_when>Cargo.toml still shows `version = "0.23.0"`, or the fdars-core line lacks `features = ["parallel"]`, or the line contains `linalg`, or Cargo.lock still shows `version = "0.23.0"` for fdars-core</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && git status --porcelain -- Cargo.toml src/ tests/ | grep -v '^ M Cargo.toml$'</automated>
    <fails_when>output is non-empty — meaning some tracked file other than Cargo.toml (any src/ or tests/ file) was modified, violating the scope boundary</fails_when>
  </verify>
  <done>Cargo.toml pins fdars-core at 0.33.0 (parallel only), Cargo.lock is refreshed to the 0.33.0 checksum, and no tracked file other than Cargo.toml changed.</done>
</task>

<task type="auto">
  <name>Task 2: Build gate — maturin develop --release green under CI flags</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - 66-RESEARCH.md "Build and Test Recipe" step 3 — CI uses `maturin develop --release` verbatim (ci.yml:73); `--release` is mandatory for a numeric gate
    - 66-RESEARCH.md "Common Pitfalls" #2 and #4 — CI sets `RUSTFLAGS="-D warnings"` (ci.yml:10); deprecation warnings become hard errors under it
    - 66-RESEARCH.md "The Four 0.30-Deprecated 2D Depth Functions" table — depth_mod.rs:50/94/138/182, plus fanova in regression_mod.rs (0.30-deprecated)
    - 66-RESEARCH.md "Open Questions" #2 and Assumption A3 — confirm deprecated-vs-removed by reading the 0.33 registry source
  </read_first>
  <action>
    Rebuild the PyO3 extension into the project `.venv` so pytest runs against 0.33.0, not the stale 0.23.0 `.so`. Activate the repo `.venv`, then run the build matching CI's strict-warnings posture: `RUSTFLAGS="-D warnings" maturin develop --release`. This is the honest gate — CI runs `-D warnings`, so a local build that ignores it would give a false green.

    CONTINGENCY (the ONLY circumstance under which `src/` may be touched, and it must be recorded as an explicit documented deviation): the build may fail because the four 0.30-deprecated 2D depth functions (`fraiman_muniz_2d` depth_mod.rs:50, `modal_2d` :94, `random_projection_2d` :138, `random_tukey_2d` :182) and/or `fanova` (regression_mod.rs) are `#[deprecated]` and `-D warnings` promotes their deprecation warnings to errors. BEFORE editing any src file, determine which case you are in:
      1. Read `~/.cargo/registry/src/index.crates.io-*/fdars-core-0.33.0/src/depth/` (and the function_on_scalar source) to confirm whether these are `#[deprecated]` (soft — still callable, warning) or hard-removed at 0.33.
      2. If HARD-REMOVED: STOP. This contradicts research assumption A3 and forces migration, which is out of this phase's scope — surface a CHECKPOINT to the operator rather than migrating. Do not proceed.
      3. If SOFT-DEPRECATED and the build fails only on those deprecation-as-error diagnostics: add `#[allow(deprecated)]` at exactly those call sites (the four depth wrappers and the fanova wrapper) — the MINIMAL change to keep the strict-warnings gate green. Record this as an explicit, documented scope deviation in `66-AUDIT.md` (which sites, why, and that migration is deferred to a later phase). Add nothing else to src/.
    Do NOT suppress warnings globally, do NOT unset RUSTFLAGS as the recorded solution (unsetting is diagnostic-only), and do NOT migrate the functions.

    Confirm the extension actually updated by checking the `_native*.so` timestamp is newer than the build start.
  </action>
  <acceptance_criteria>
    - `RUSTFLAGS="-D warnings" maturin develop --release` exits 0.
    - `python -c "import fdars; print('ok')"` prints `ok`.
    - The `.venv/.../fdars/_native*.so` modification time is fresh (post-build).
    - If any `#[allow(deprecated)]` was added, it is confined to the four depth call sites and the fanova call site, and is documented as an explicit deviation in 66-AUDIT.md. If nothing was added, src/ is untouched.
    - MSRV unchanged: no `rust-version` bump was needed (0.33.0 requires ≥1.81 < 1.83).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && source .venv/bin/activate && RUSTFLAGS="-D warnings" maturin develop --release 2>&1 | tail -5 && python -c "import fdars; print('IMPORT_OK')"</automated>
    <fails_when>maturin exits non-zero, output contains `error[` or `error: `, the build reports a deprecated-as-error diagnostic that was not resolved per the CONTINGENCY, or the final line is not `IMPORT_OK`</fails_when>
  </verify>
  <done>The 0.33.0 extension builds green under `RUSTFLAGS="-D warnings"` and imports cleanly; any deprecation-driven `#[allow(deprecated)]` is minimal, confined to the five known call sites, and documented as a deviation.</done>
</task>

<task type="auto">
  <name>Task 3: Regression gate — full Python suite, zero new failures</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - 66-RESEARCH.md "Build and Test Recipe" step 4/5 — CI runs `pytest tests/ -v` (ci.yml:87-88); add `-x` for fail-fast; ~772 items is the baseline
    - 66-RESEARCH.md "Common Pitfalls" #3 — numeric-drift handling; most-sensitive files test_fdata_stats.py / test_pace_fpca.py / test_depth.py
    - 66-RESEARCH.md "Don't run" list — no `cargo test` substitution, no `-k` filter, no sklearn-only subset
  </read_first>
  <action>
    Run the FULL Python suite as the regression gate against the freshly built 0.33.0 extension: from the activated `.venv`, run `pytest tests/ -x -v` and tee the output to a log. This is the real gate for the 10-minor jump — do NOT substitute `cargo build`, a `-k` filtered run, or a sklearn-only subset.

    Pass condition: zero NEW failures versus the pre-bump baseline (PASSED count identical or higher; previously-xfail/skip that now pass are acceptable and get recorded). On a failure, stop (fail-fast already does this) and classify it:
      - Numeric drift (same algorithm, a float path moved outside atol/rtol): tolerance changes live in TEST files, which are OUT OF SCOPE for this phase. If a genuine drift is found, do NOT edit tests — record the failing assertion, expected-vs-actual, and file:line in 66-AUDIT.md and surface a CHECKPOINT to the operator (tolerance changes are a scope decision, not a silent planner fix).
      - Removed/renamed API (a match-arm string no longer maps): this is an API-audit signal — capture it for Task 4 and surface it; it likely indicates a 0.31/0.32 surprise.
    Expected outcome per research: zero drift, suite green. Record the final pass/fail line and the total count in 66-AUDIT.md regardless.
  </action>
  <acceptance_criteria>
    - `pytest tests/ -x -v` completes with zero new failures (no `failed`/`error` in the summary line beyond the known baseline).
    - The final pytest summary line and total item count are recorded in 66-AUDIT.md.
    - No test file was edited (tests are out of scope). Any drift is documented + checkpointed, never silently patched.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && source .venv/bin/activate && pytest tests/ -x -q 2>&1 | tee /tmp/phase66-regression.log | tail -3; grep -E '[0-9]+ (passed|failed|error)' /tmp/phase66-regression.log | tail -1</automated>
    <fails_when>the pytest summary line contains ` failed` or ` error`, or pytest exits non-zero, or the summary line is absent (suite did not complete)</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && git status --porcelain -- tests/ | grep -c . | grep -qx 0 && echo TESTS_UNTOUCHED</automated>
    <fails_when>output is not `TESTS_UNTOUCHED` — meaning at least one file under tests/ was modified, violating the scope boundary</fails_when>
  </verify>
  <done>The full Python suite passes with zero new failures against the 0.33.0 extension, the summary is recorded, and no test file changed.</done>
</task>

<task type="auto">
  <name>Task 4: API audit + changelog record (66-AUDIT.md)</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - 66-RESEARCH.md "API Audit Surface" — the complete enum/match-arm checklist for all src/*_mod.rs (DepthMethod, NormalizationMethod, CvCriterion, BasisType/BasisCriterion, GlmFamily/SelectionCriterion, MultiplierDistribution/ProjectionBasisType, ExtrapolationPolicy/InterpolationMethod/ImputationMethod, alignment enums, PcaMethod, EFunType/EValType/CovKernel, NcompMethod/ControlLimitMethod, BandType/ExponentialFamily, StrengthMethod, DepthType)
    - 66-RESEARCH.md "0.24→0.33 Changelog Summary" table and the 0.31/0.32-gap note
    - 66-RESEARCH.md "The Four 0.30-Deprecated 2D Depth Functions" table (depth_mod.rs:50/94/138/182) + the fanova note
    - 66-RESEARCH.md "How to verify at 0.33" Methods A/B/C — prefer Method B (registry source at `~/.cargo/registry/src/index.crates.io-*/fdars-core-0.33.0/`); compile success (Method C) is corroborating but NOT sufficient for renamed dispatch strings
  </read_first>
  <action>
    Produce the DEP-03 audit artifact at `.planning/phases/66-isolated-crate-bump-regression-gate/66-AUDIT.md` — findings go into the phase dir, NEVER into src/. The clean 0.33.0 build from Task 2 is strong evidence variants still exist, but it does not catch a renamed dispatch STRING (that becomes a dead match arm, not a compile error). So cross-check the audit table against the recovered 0.33 source.

    The audit note MUST contain:
      1. A 0.24→0.33 changelog summary (use the research table; close the 0.31/0.32 gap by reading `~/.cargo/registry/src/index.crates.io-*/fdars-core-0.33.0/CHANGELOG.md` now that cargo update has landed the source).
      2. An enum/match-arm audit result: for each enum in the research checklist, state CONFIRMED-PRESENT at 0.33 (via registry source read) or FLAG any string that no longer maps to a live variant. If Task 3 surfaced a removed/renamed API, resolve it here.
      3. The FOUR 0.30-deprecated 2D depth functions FLAGGED for later migration (do NOT migrate): `fraiman_muniz_2d` (depth_mod.rs:50), `modal_2d` (:94), `random_projection_2d` (:138), `random_tukey_2d` (:182), plus `fanova` (regression_mod.rs). Include the note: "deprecation warnings expected; migration deferred to a later phase."
      4. If Task 2's CONTINGENCY added any `#[allow(deprecated)]`, record it here as an explicit documented scope deviation (which sites, why).
      5. The Task 3 pytest summary line + total count, and a statement of whether any numeric-tolerance change was needed (expected: none).
  </action>
  <acceptance_criteria>
    - 66-AUDIT.md exists in the phase dir and contains all five required sections above.
    - Every enum in the research checklist has an explicit CONFIRMED-PRESENT or FLAGGED verdict at 0.33.
    - The four deprecated 2D depth functions (+ fanova) are flagged for later migration and explicitly NOT migrated.
    - The 0.31/0.32 changelog gap is addressed by a direct read of the 0.33 registry CHANGELOG (or an explicit statement that it remains absent and why that is non-breaking).
    - No file under src/ or tests/ was edited by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f .planning/phases/66-isolated-crate-bump-regression-gate/66-AUDIT.md && for kw in changelog DepthMethod GlmFamily fraiman_muniz_2d modal_2d random_projection_2d random_tukey_2d fanova; do grep -qi "$kw" .planning/phases/66-isolated-crate-bump-regression-gate/66-AUDIT.md || { echo "MISSING: $kw"; exit 1; }; done && echo AUDIT_COMPLETE</automated>
    <fails_when>66-AUDIT.md is absent, or any required keyword (changelog, DepthMethod, GlmFamily, or any of the four deprecated 2D depth function names, or fanova) is missing, or the final line is not AUDIT_COMPLETE</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && git status --porcelain -- src/ tests/ | grep -c . | grep -qx 0 && echo SRC_TESTS_UNTOUCHED || echo DEVIATION_CHECK_66-AUDIT.md</automated>
    <fails_when>output is neither SRC_TESTS_UNTOUCHED (clean) nor an intentional, 66-AUDIT.md-documented Task 2 CONTINGENCY deviation — an undocumented src/ or tests/ edit means the scope boundary was violated</fails_when>
  </verify>
  <done>66-AUDIT.md records the 0.24→0.33 changelog, a per-enum audit verdict at 0.33, the four flagged 2D depth functions (+ fanova) deferred for migration, any documented CONTINGENCY deviation, and the regression-gate result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → local build | An external crate version (`fdars-core 0.33.0`) crosses into the build; its integrity is verified by the `Cargo.lock` checksum |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-SC | Tampering | `cargo update -p fdars-core` (crates.io fetch) | high | mitigate | `Cargo.lock` records the 0.33.0 checksum; crate is the same trusted publisher (sipemu/fdars); no new/unaudited package is introduced (research Package Legitimacy Audit: OK/Approved) |
| T-66-01 | Denial of Service | Numeric drift across the 10-minor jump silently corrupting downstream results | medium | mitigate | Full ~772-test Python suite is the gate (Task 3); fail-fast + documented tolerance handling; no silent test edits |
| T-66-02 | Tampering | Undocumented src/ edit slipping in under the "contingency" banner | low | mitigate | Scope-boundary verify gates in Tasks 1/3/4 assert only Cargo.toml changes among tracked files; any `#[allow(deprecated)]` must be documented in 66-AUDIT.md |
</threat_model>

<verification>
Phase-level gate (all must hold before /gsd-verify-work):
- Cargo.toml pins fdars-core at 0.33.0 (parallel only, no linalg); Cargo.lock refreshed on disk.
- `RUSTFLAGS="-D warnings" maturin develop --release` builds green; `import fdars` works; MSRV 1.83 unchanged.
- `pytest tests/ -x -v` green with zero new failures; result recorded.
- 66-AUDIT.md records the 0.24→0.33 changelog + per-enum audit + four flagged 2D depth functions (+ fanova).
- Only Cargo.toml (tracked) changed; Cargo.lock updated on disk (gitignored, not committed); no src/ or test edits (except a documented Task-2 CONTINGENCY, if any).
</verification>

<success_criteria>
1. DEP-01 — fdars-core pinned at 0.33.0 (parallel only, no linalg) in Cargo.toml + Cargo.lock; `maturin develop` builds green; MSRV 1.83 unchanged.
2. DEP-02 — full Python suite passes with zero new failures; any numeric-tolerance change documented (expected: none).
3. DEP-03 — recorded 0.24→0.33 changelog + API audit confirms every existing match-arm/enum-variant string in src/*_mod.rs still exists at 0.33, and flags the four 0.30-deprecated 2D depth functions for later migration.
4. Only Cargo.toml and Cargo.lock change — no new bindings, no test edits (Cargo.lock is gitignored, so only Cargo.toml is committed).
</success_criteria>

<output>
Create `.planning/phases/66-isolated-crate-bump-regression-gate/66-01-SUMMARY.md` when done. Fold the audit findings into `.planning/phases/66-isolated-crate-bump-regression-gate/66-AUDIT.md` (Task 4) and reference it from the SUMMARY.
</output>
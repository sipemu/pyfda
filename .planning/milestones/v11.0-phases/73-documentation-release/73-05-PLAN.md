---
phase: 73-documentation-release
plan: 05
type: execute
wave: 5
depends_on: ["73-04"]
files_modified:
  - Cargo.toml
  - pyproject.toml
autonomous: false
requirements: [REL-01]
estimate:
  tokens: 20000
  raw_tokens: 12000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The package version is bumped 0.9.0 → 0.10.0 in Cargo.toml and pyproject.toml and committed (REL-01)."
    - "The v0.10.0 git tag is NOT created or pushed by the executor — it is a human checkpoint (REL-01)."
  artifacts:
    - Cargo.toml
    - pyproject.toml
  key_links:
    - "The v0.10.0 semver tag → publish.yml (fires on v[0-9]+.[0-9]+.[0-9]+) → PyPI publish (irreversible, human-gated)."
---

<objective>
Prepare the release: bump the package version 0.9.0 → 0.10.0 in Cargo.toml and pyproject.toml
and commit it. Then PAUSE at a human checkpoint for the user to create and push the v0.10.0 tag
(which triggers the irreversible PyPI publish). The executor MUST NOT create or push the tag.

Purpose: REL-01 — apply the version bump at milestone close while keeping the outward-facing,
irreversible publish action strictly human-gated.
Output: a committed version bump; a human release-tag checkpoint.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump package version 0.9.0 → 0.10.0 in Cargo.toml and pyproject.toml, and commit</name>
  <precondition>The 73-04 blocking human diagram review has been approved (this plan depends_on 73-04; do not run the version bump before the diagram review passes).</precondition>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 5 REL-01 mechanics — Cargo.toml line 3 version = "0.9.0" → "0.10.0"; pyproject.toml line 7 version = "0.9.0" → "0.10.0"; no other files)
    - Cargo.toml (line 3); pyproject.toml (line 7)
  </read_first>
  <action>
    Edit Cargo.toml line 3 from `version = "0.9.0"` to `version = "0.10.0"`. Edit pyproject.toml line 7 from `version = "0.9.0"` to `version = "0.10.0"`. No other files need a version edit (maturin derives the package version from pyproject.toml; classifiers are not version-pinned). Commit the two edits with a message like `chore(73): bump package version 0.9.0 → 0.10.0 (REL-01)`. Do NOT create a git tag. Do NOT run `git tag` or `git push --tags`.
  </action>
  <reversibility rating="reversible">A version-line edit + commit is trivially revertible; the one-way door (the tag/publish) is deliberately deferred to Task 2's human checkpoint.</reversibility>
  <verify>
    <automated>grep -c 'version = "0.10.0"' Cargo.toml && grep -c 'version = "0.10.0"' pyproject.toml && ! git tag -l 'v0.10.0' | grep -q v0.10.0 && echo BUMP_OK_NO_TAG</automated>
    <fails_when>Either file still reads 0.9.0 (grep count 0), OR the version was changed to something other than 0.10.0, OR a v0.10.0 tag already exists locally (the executor must NOT have created it).</fails_when>
  </verify>
  <acceptance_criteria>
    - Cargo.toml and pyproject.toml both read version = "0.10.0".
    - The change is committed.
    - No v0.10.0 git tag exists (not created by the executor).
  </acceptance_criteria>
  <done>The version bump to 0.10.0 is committed in both files; no tag was created.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: HUMAN release checkpoint — user creates and pushes the v0.10.0 tag</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-CONTEXT.md (Hard human gates — release; the v0.10.0 tag triggers PyPI publish; DO NOT tag/publish autonomously)
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 5 tag → publish trigger; publish.yml fires on v[0-9]+.[0-9]+.[0-9]+; Pitfall 6 tagging autonomously)
  </read_first>
  <action>
    PAUSE. Inform the user that the version bump is committed and the release is ready. The tag
    push is an outward-facing, IRREVERSIBLE action (publish.yml fires on the v0.10.0 tag and
    publishes wheels + sdist to PyPI). Present the exact commands for the USER to run when ready:
    `git tag v0.10.0 && git push origin v0.10.0`. The executor MUST NOT run these. Confirm the
    diagram review (73-04 Task 3) has been approved before presenting this checkpoint. State that
    the milestone v11.0 is complete once the user pushes the tag and PyPI publish succeeds.
  </action>
  <verify>
    <human-check>The user has been shown the exact tag/push commands and understands the executor did not (and must not) create or push the v0.10.0 tag. The user triggers the release when ready.</human-check>
  </verify>
  <acceptance_criteria>
    - The user is presented with the `git tag v0.10.0 && git push origin v0.10.0` commands to run themselves.
    - The executor did not create or push any tag.
    - The user acknowledges the release is theirs to trigger.
  </acceptance_criteria>
  <done>The release is handed off to the user: version bump committed, tag/push commands presented, and the executor performed no publish action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → PyPI (external) | The v0.10.0 tag push triggers publish.yml → irreversible PyPI publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-09 | Elevation of Privilege | PyPI publish trigger | high | mitigate | Tag/push is a blocking human checkpoint (Task 2); executor MUST NOT run git tag/push — Pitfall 6 |
| T-73-10 | Tampering | version-line edits | low | mitigate | Grep-verified exact 0.10.0 in both files; commit is revertible before any tag exists |
</threat_model>

<verification>
- Cargo.toml and pyproject.toml both read version = "0.10.0"; change committed.
- No v0.10.0 tag created by the executor.
- Human release checkpoint reached; tag/push commands presented to the user for them to run.
</verification>

<success_criteria>
REL-01 prep complete: version bumped to 0.10.0 and committed; the irreversible tag/publish is
handed to the user as a blocking human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/73-documentation-release/73-05-SUMMARY.md` when done
</output>

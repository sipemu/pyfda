---
phase: 73-documentation-release
plan: 04
type: execute
wave: 4
depends_on: ["73-03"]
files_modified:
  - docs/advisor/aspects.md
autonomous: false
requirements: [DOCS-03]
estimate:
  tokens: 60000
  raw_tokens: 34000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Advisor aspects.md documents the new fts and frechet aspects and extends regression/spm/classification (DOCS-03)."
    - "The whole-site mkdocs build --strict passes green OFFLINE and check_docs_figures.py site exits 0 (DOCS-03)."
    - "The blocking human diagram method-accuracy review is explicitly requested and NOT self-approved (DOCS-03, standing v6.0 gate)."
  artifacts:
    - docs/advisor/aspects.md
    - site
  key_links:
    - "aspects.md fts/frechet fences → shipped fdars.fts / fdars.frechet bindings."
    - "The 7 diagrams from 73-01/02/03 → the human diagram-review checkpoint."
---

<objective>
Close out the documentation deliverable: update the advisor aspects.md for the new/extended
aspects, run the ONE whole-site --strict gate offline, then PAUSE for the standing v6.0
blocking human diagram method-accuracy review before any release action.

Purpose: DOCS-03 — the aspects doc must cover the new capabilities, the whole site must build
strict-green, and every diagram must be human-verified method-accurate before the milestone
closes.
Output: updated aspects.md, a strict-green site build, and an explicit human-review pause.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/advisor/aspects.md for the new/extended advisor aspects</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 4b aspects.md update — exact per-aspect structure; new fts + frechet sections + coverage rows; regression FoF keys has_fof_regression/fof_r_squared/beta_surface_shape; spm mfpca keys has_mfpca/mfpca_ncomp/mfpca_eigenvalues; classification shapelet-guard clarification)
    - docs/advisor/aspects.md (existing per-aspect structure to mirror — coverage table, section heading, fdars source line, key table, task families, optional fence)
    - .planning/phases/72-advisor-extension (VERIFICATION + SUMMARYs for the exact advisor key names shipped)
  </read_first>
  <action>
    Update docs/advisor/aspects.md per RESEARCH Section 4b. Add a NEW `## fts` section (fdars source fdars.fts.ftsm / stationarity_test / functional_acf / dpca / fplsr; key table for its discriminated branches; an offline small ftsm fence ending with the FDARS_FENCE_OK marker) and a NEW `## frechet` section (diagnostics-only, NOT in _RUNNABLE_METHODS; fdars source frechet_mean/frechet_anova/frechet_global_reg/frechet_local_reg; key table; an OFFLINE fence acceptable — no online fence). Add coverage-table rows for fts and frechet (insert after the fpca row per the ordering convention). Extend existing sections: `## regression` (add has_fof_regression, fof_r_squared, beta_surface_shape keys), `## spm` (add has_mfpca, mfpca_ncomp, mfpca_eigenvalues keys and spe_multivariate under spm fdars sources; note that when has_mfpca is True the spm_phase1 ncomp/eigenvalues keys are None), `## classification` (clarify the elastic_multinomial branch trigger so the shapelet classifier does not spuriously trigger it — train_accuracy present AND n_shapelets absent). Mirror the exact structure of the existing aspect sections. Any new fence must be OFFLINE and small.
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/aspects_build.log; python scripts/check_docs_figures.py site; grep -q '^## fts' docs/advisor/aspects.md && grep -q '^## frechet' docs/advisor/aspects.md && echo ASPECTS_SECTIONS_OK</automated>
    <fails_when>An aspects.md fence raises (traceback in HTML caught by check_docs_figures.py — e.g. missing pydantic in the docs venv, or a wrong advisor key), OR the fts/frechet sections are absent (grep fails).</fails_when>
  </verify>
  <acceptance_criteria>
    - aspects.md has new `## fts` and `## frechet` sections plus coverage-table rows.
    - regression/spm/classification sections carry the new/extended keys from RESEARCH Section 4b.
    - Any new aspects fence is offline and executes cleanly (check_docs_figures.py exits 0).
  </acceptance_criteria>
  <done>aspects.md documents the new and extended advisor aspects; the DOCS_FAST build with the new fences is green.</done>
</task>

<task type="auto">
  <name>Task 2: Run the ONE whole-site mkdocs build --strict offline gate</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 4 offline --strict recipe; prerequisites: PYTHONPATH=scripts, pydantic + anthropic in the docs venv, DOCS_FAST UNSET; build time budget 25-35 min)
    - .planning/STATE.md (build-time blocker: --strict is ~19-25+ min, run ONCE at close; docs-build-ci-env-gotchas — pydantic in docs env)
  </read_first>
  <action>
    Run the single final offline strict gate exactly once (DOCS_FAST MUST be unset). Ensure pydantic and anthropic are present in the docs venv (pip install -r docs/requirements.txt if a fence raises ModuleNotFoundError). Command: `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` followed by `python scripts/check_docs_figures.py site`. This validates: all 7 new pages + aspects.md fences execute at full iteration counts; nav/link correctness under --strict; no traceback-in-HTML. This is the DOCS-03 whole-site strict gate. Budget 25-35 min.
  </action>
  <verify>
    <automated>env -u DOCS_FAST PYTHONPATH=scripts .venv/bin/mkdocs build --strict 2>&1 | tee /tmp/strict_build.log; python scripts/check_docs_figures.py site && echo STRICT_GATE_GREEN</automated>
    <fails_when>--strict exits non-zero on any MkDocs warning (broken nav/link), OR check_docs_figures.py finds a Python traceback or exec-error in the built site (a fence raised — e.g. DOCS_FAST accidentally left set producing rough output, or pydantic missing).</fails_when>
  </verify>
  <acceptance_criteria>
    - `mkdocs build --strict` exits 0 with DOCS_FAST unset.
    - `check_docs_figures.py site` exits 0 (no traceback / exec-error in any page).
    - All 7 new pages and aspects.md fences executed at full iteration counts.
  </acceptance_criteria>
  <done>The whole-site strict build is green offline and check_docs_figures.py passes — DOCS-03 build gate satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: BLOCKING human diagram method-accuracy review (standing v6.0 gate)</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-CONTEXT.md (Hard human gates — blocking diagram review; do NOT self-approve; verification is human_needed until approved)
    - .planning/STATE.md (standing v6.0 blocking human diagram method-accuracy review — the hypograph/epigraph lesson)
  </read_first>
  <action>
    PAUSE. Present all 7 new diagrams for the user's method-accuracy review, one per family:
    functional-time-series.svg, function-on-function.svg, additive-sof.svg, frechet-regression.svg,
    density-fda.svg, advanced-clustering.svg, shapelets.svg. For each diagram, render a PNG for
    visual inspection (rsvg-convert -w 720 docs/assets/diagrams/<slug>.svg -o /tmp/<slug>.png, or
    ImageMagick convert / open in browser as fallback) and state the method the diagram is meant
    to depict (fts = FTSM mean + basis forecast extrapolation; fof = beta-surface predictor→response;
    additive-sof = sum of smooth partial effects; frechet = metric-space barycenter; density = LQD
    transform + Wasserstein barycenter; clustering = DBSCAN/kcfc with -1 noise; shapelets =
    discriminative subsequence). Ask the user to confirm each diagram is method-accurate against the
    shipped binding. DO NOT self-approve. Verification for this phase remains human_needed until the
    user explicitly approves every diagram. If the user flags a diagram as inaccurate, that diagram
    must be re-authored (loop back to its authoring plan) and this checkpoint re-run.
  </action>
  <verify>
    <human-check>The user has visually reviewed all 7 diagrams and explicitly confirmed each is method-accurate against the shipped binding.</human-check>
  </verify>
  <acceptance_criteria>
    - All 7 diagrams were presented (rendered) to the user with their intended method stated.
    - The user explicitly approved each diagram's method-accuracy.
    - No diagram was self-approved by the executor.
  </acceptance_criteria>
  <done>The user has approved every diagram as method-accurate; the DOCS-03 blocking human review is satisfied and the phase may proceed to release prep (73-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagram content → published site | Hand-authored SVGs ship to users; a method-inaccurate diagram misleads readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-07 | Repudiation | diagram method-accuracy | high | mitigate | Blocking human review (Task 3); executor may NOT self-approve — standing v6.0 gate |
| T-73-08 | Tampering | strict-build fence output | medium | mitigate | Single --strict gate + check_docs_figures.py; pydantic present in docs venv |
</threat_model>

<verification>
- aspects.md has new fts + frechet sections and extended regression/spm/classification keys.
- `mkdocs build --strict` (DOCS_FAST unset) exits 0 and check_docs_figures.py site exits 0.
- Human diagram review checkpoint reached and explicitly approved by the user (not self-approved).
</verification>

<success_criteria>
DOCS-03 complete: advisor aspects updated, whole-site strict build green offline, and the
blocking human diagram method-accuracy review approved before proceeding to release.
</success_criteria>

<output>
Create `.planning/phases/73-documentation-release/73-04-SUMMARY.md` when done
</output>

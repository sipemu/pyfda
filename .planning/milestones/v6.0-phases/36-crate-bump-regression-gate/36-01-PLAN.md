---
phase: 36-crate-bump-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
autonomous: true
requirements: [DEP-05, DEP-06]

estimate:
  tokens: 40000
  raw_tokens: 26000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Cargo.toml pins fdars-core = { version = \"0.23.0\", features = [\"parallel\"] } with no linalg feature (DEP-05)"
    - "cargo build exits 0 — every existing src/*_mod.rs enum-value match still compiles, proving no new #[non_exhaustive] break emerged between 0.20.0 and 0.23.0 (DEP-06)"
    - "maturin develop builds and installs the extension green in the project venv (DEP-05)"
    - "The full existing binding + advisor suite passes unchanged with ZERO failures — no new tests, no tolerance relaxations (DEP-06)"
    - "cargo fmt --check and cargo clippy -- -D warnings stay clean (no Rust source changed, so trivially clean) (DEP-06)"
    - "The bump lands as one isolated commit (Cargo.toml only; Cargo.lock NOT staged) before any Phase 37-41 binding work (DEP-06)"
  artifacts:
    - Cargo.toml
  key_links:
    - "Cargo.toml line 18 version string is the ONLY source change — no src/*_mod.rs edit is required this phase (the CvCriterion wildcard arms from v5.0 Phase 30 are already present)"
    - "maturin develop (not cargo build alone) must install the rebuilt _native.so into the venv, or pytest silently runs against the old 0.20.0 extension and the gate is not exercised"
---

<objective>
Bump the pinned `fdars-core` dependency from 0.20.0 to 0.23.0 (parallel-only, no `linalg`), rebuild the PyO3 extension via `maturin develop`, and prove the entire existing binding + advisor suite still passes unchanged with zero failures. This establishes the green baseline that Phases 37-41 (Group A/B/C bindings + advisor + docs) build on (DEP-05, DEP-06).

Purpose: A downstream binding regression must never be able to hide behind an upgrade regression. Landing the bump as an isolated, provably-green commit first isolates any future failure to the binding work that caused it. The 0.20.0→0.23.0 diff is additive-only (per DEP-05 research); the audit in 36-RESEARCH.md confirmed ZERO new wildcard fallback arms are required — but the crate must still be VERIFIED to compile, proving no non-exhaustive-match break emerged.
Output: Modified `Cargo.toml` (pin at 0.23.0); regenerated `Cargo.lock` on disk (gitignored — NOT committed); a passing full test suite. No `src/*.rs` files are modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-crate-bump-regression-gate/36-RESEARCH.md

# The exact pin to change (line 18):
@Cargo.toml
</context>

<artifacts_this_phase_produces>
This phase produces NO new public Python or Rust symbols. It is a pure-infrastructure regression gate. The only source change is a single field in `Cargo.toml`:
- `Cargo.toml`: the `fdars-core` version string moves from `0.20.0` to `0.23.0` (the `features = ["parallel"]` set is unchanged; `linalg` is NOT added).

No `src/*.rs` files are edited. Unlike v5.0 Phase 30 (which added two `CvCriterion` wildcard arms to `src/smoothing_mod.rs`), the 0.20.0→0.23.0 diff introduces no new `#[non_exhaustive]` annotation on any enum matched by existing pyfda code — the exhaustive enum-site audit in 36-RESEARCH.md ("The Compile-Break Audit") verified all five enum-value match sites and found zero fixes needed. All new `#[non_exhaustive]` types in the diff (`GlmFamily`, `PaceFpcaResult`, `TvdMssOutliers`, `MuodResult`, `SeqTransformOutliers`, `ItpResult`, ...) belong to NEW upstream surface bound only in Phases 37-39, not this phase.

No new bindings, no new submodules, no new tests, no new datasets, no API surface change. `Cargo.lock` regenerates on disk but is gitignored per repo policy and MUST NOT be committed.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Bump fdars-core 0.20.0 to 0.23.0 (parallel-only, no linalg) and rebuild the extension end-to-end via maturin develop</name>
  <files>Cargo.toml</files>
  <read_first>Cargo.toml (the pin is on line 18 — currently reads fdars-core = { version = "0.20.0", features = ["parallel"] }); .planning/phases/36-crate-bump-regression-gate/36-RESEARCH.md — "The exact Cargo.toml change" section (the precise before/after string), "The Compile-Break Audit" (why NO src/*.rs edits are needed), and "Common Pitfalls" (Pitfall 2 accidentally enabling linalg, Pitfall 4 cargo build vs maturin develop).</read_first>
  <action>This is the single end-to-end tracer slice for the phase: change the dependency pin and prove the crate builds and installs, touching the one layer the phase modifies (the Cargo pin) all the way through to the rebuilt venv extension. In /home/simonm/projects/rust/pyfda/Cargo.toml, edit only the fdars-core dependency line (line 18) so the version string reads exactly 0.23.0 while keeping features = ["parallel"] unchanged. Do NOT add the linalg feature (per DEP-05 and 36-RESEARCH.md Pitfall 2: linalg activates faer 0.23 and anofox-regression 0.4, which require Rust 1.84+ above pyfda's MSRV 1.83, and the build will fail). Do NOT touch pyo3, numpy, or any other dependency line. Do NOT edit any src/*.rs file — the enum-site audit in 36-RESEARCH.md verified that every existing match on an fdars-core enum value already has the fallback it needs (the CvCriterion, ProjectionBasisType, and SignificanceDirection wildcard arms from v5.0 are already present; BasisCriterion is not #[non_exhaustive] and its exhaustive match is fine), so zero wildcard arms are added this phase. After editing, run cargo build inside /home/simonm/projects/rust/pyfda to VERIFY the crate compiles against 0.23.0 — this is the load-bearing check that proves no non-exhaustive-match break emerged (per DEP-06); if cargo build reports a non-exhaustive patterns error on any enum, STOP and surface it (the research predicted none, so any such error is new information). Then rebuild and install the extension by running maturin develop using the project venv at /home/simonm/projects/rust/pyfda/.venv (per Pitfall 4, maturin develop — not cargo build alone — is what installs the rebuilt _native.so into the venv so the later pytest gate actually exercises 0.23.0). This regenerates Cargo.lock on disk; do NOT git-add Cargo.lock (it is gitignored per repo policy — see Pitfall 1). Keep the diff to exactly one line in Cargo.toml.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q 'fdars-core = { version = "0.23.0", features = \["parallel"\] }' Cargo.toml && [ "$(grep -v '^#' Cargo.toml | grep -c linalg)" = "0" ] && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml line 18 reads exactly: fdars-core = { version = "0.23.0", features = ["parallel"] }
    - Cargo.toml contains no linalg token anywhere (grep -v comments then grep -c returns 0).
    - cargo build exits 0 with no non-exhaustive patterns error — proving the 0.20.0-to-0.23.0 bump introduced no compile break in existing binding code (DEP-06).
    - maturin develop (run against /home/simonm/projects/rust/pyfda/.venv) exits 0 and installs the rebuilt _native extension.
    - No src/*.rs file is modified; only Cargo.toml changed (one line).
  </acceptance_criteria>
  <done>fdars-core is pinned at 0.23.0 (parallel-only, no linalg), the whole crate compiles against 0.23.0 with no non-exhaustive break, and maturin develop builds and installs the extension green in the venv.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full existing binding + advisor suite as the sole regression gate and confirm rustfmt/clippy stay clean</name>
  <files>Cargo.toml</files>
  <read_first>Cargo.toml (the only edited file, to confirm the diff is one line before gating); .planning/phases/36-crate-bump-regression-gate/36-RESEARCH.md — "Phase Regression Gate" table and Pitfall 3 (the test count is 604 collected; a full run excluding the 4 live-provider files yields ~558 passed / 1 skipped; the gate is ZERO failures, NOT an exact count) and Pitfall 5 (use the venv python explicitly).</read_first>
  <action>Run the entire existing pytest suite against the freshly rebuilt 0.23.0 extension in the project venv — this is the sole success criterion for DEP-06. Run .venv/bin/python -m pytest tests/ -q from /home/simonm/projects/rust/pyfda (per Pitfall 5, use the venv python explicitly so the compiled fdars._native extension is on the path). The gate criterion is ZERO failures — do NOT assert an exact pass count. Per Pitfall 3 the suite collects 604 tests and a full run (the 4 live-provider advisor files that need network credentials will error/skip without keys) reproduces roughly 558 passed / 1 skipped; accept whatever pass/skip split the current suite produces so long as failures are zero. Do NOT add new tests and do NOT relax any tolerance as a first move; the expected path (matching the v5.0 0.17-to-0.20 bump, which needed zero tolerance changes) is that the suite passes as-is. Only if a genuine numeric-drift failure surfaces on an existing test (there are no new bindings in this phase, so any failure is drift, not a missing arm) may you widen the single affected tolerance by the minimum amount (one ULP at a time, never a blanket rtol), and you MUST record the exact function and the widened value in the SUMMARY; treat zero changes as the planned outcome and any tolerance edit as a documented exception. Separately, confirm the Rust stays lint-clean under the CI-enforced gates: run cargo fmt --check and cargo clippy -- -D warnings from the project root. Because no Rust source was changed this phase, both are expected to pass trivially; if clippy surfaces a warning from a new transitive dep it is new information — surface it rather than silently editing source. Finally, confirm git staging discipline: only Cargo.toml is staged for the commit and Cargo.lock is NOT staged (git diff --cached --name-only must show Cargo.toml and not Cargo.lock). The phase is complete only when the full suite is green (zero failures) and both Rust gates pass.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -5 && cargo fmt --check && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/python -m pytest tests/ -q exits 0 with ZERO failures (roughly 558 passed / 1 skipped; exact pass count is not asserted — the gate is zero failures per DEP-06).
    - No new test files were added and no tolerance was relaxed (or, if one was, it is recorded in the SUMMARY with the exact drifted function named).
    - cargo fmt --check exits 0 and cargo clippy -- -D warnings exits 0 (no Rust source changed, so trivially clean).
    - git diff --cached --name-only shows only Cargo.toml; Cargo.lock is absent from the staged set.
  </acceptance_criteria>
  <done>The full existing suite passes with zero failures and both rustfmt and clippy (-D warnings) are clean, establishing the green 0.23.0 baseline for Phases 37-41; only Cargo.toml is staged (Cargo.lock not committed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyfda -> fdars-core crate registry | The bump pulls fdars-core 0.23.0 from crates.io; a compromised/typosquatted version would execute at build time. This is an EXISTING dependency (same author sipemu, same maintainer as pyfda), version-bumped — not a newly introduced package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Tampering | fdars-core 0.23.0 crate fetched at build | low | accept | Existing first-party dependency (same maintainer as pyfda, provenance verified in 36-RESEARCH.md against crates.io). Version bump of an already-trusted dep; not a new package introduction. Cargo.lock pins the exact resolved versions post-bump. |
| T-36-02 | Denial of Service | CI build time on the bump | low | accept | Additive/non-breaking bump; transitive dependency graph is additive-only for features=["parallel"] (36-RESEARCH.md); no new build-time expansion. |

Note: No `cargo add` of a NEW package occurs in this phase — only a version bump of the pre-existing `fdars-core` dependency. The Package Legitimacy Gate (which applies to newly installed packages) is therefore not triggered; the crates.io-verified provenance in 36-RESEARCH.md's Package Legitimacy Audit covers the bump.
</threat_model>

<verification>
- `Cargo.toml` pins `fdars-core = { version = "0.23.0", features = ["parallel"] }` with no `linalg` token (`grep`).
- `cargo build` exits 0 — every existing enum match compiles against 0.23.0 (no `non-exhaustive patterns` error).
- `maturin develop` (in `.venv`) exits 0 — the extension rebuilds and installs.
- `.venv/bin/python -m pytest tests/ -q` exits 0 with ZERO failures (~558 passed / 1 skipped; count not asserted, failures-must-be-zero is the gate).
- `cargo fmt --check` and `cargo clippy -- -D warnings` both exit 0.
- Only `Cargo.toml` is staged for the commit; `Cargo.lock` is NOT staged.
</verification>

<success_criteria>
1. `fdars-core` is pinned at `0.23.0` (parallel-only, no `linalg`) and `maturin develop` builds green (DEP-05).
2. `cargo build` compiles against 0.23.0 with no non-exhaustive-match break — verifying the audit's finding that no new wildcard fallback arms are required by existing code (DEP-06).
3. The full existing binding + advisor suite passes unchanged with zero failures — no new tests, no tolerance relaxations (DEP-06).
4. rustfmt + clippy (`-D warnings`) stay clean (DEP-06).
5. The change lands as one isolated commit (`Cargo.toml` only, not `Cargo.lock`) before any Phase 37-41 binding work (DEP-06).
</success_criteria>

<output>
Create `.planning/phases/36-crate-bump-regression-gate/36-01-SUMMARY.md` when done. Record: the pytest pass/skip counts (confirm zero failures; note the actual passed/skipped numbers), whether any tolerance was widened (expected: none — if any, name the exact drifted function and the widened value), confirmation that no `src/*.rs` file was edited (Cargo.toml-only diff), and confirmation that `Cargo.lock` was NOT committed.
</output>

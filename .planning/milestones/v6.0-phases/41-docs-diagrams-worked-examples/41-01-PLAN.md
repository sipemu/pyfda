---
phase: 41-docs-diagrams-worked-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regression/concurrent-regression.md
  - docs/regression/functional-glm.md
  - docs/assets/diagrams/concurrent-regression.svg
  - docs/assets/diagrams/functional-glm.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-08]
estimate:
  tokens: 72000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The built site has a Regression nav entry 'Concurrent Regression' -> regression/concurrent-regression.md and 'Functional GLM' -> regression/functional-glm.md (DOCS-08)."
    - "concurrent-regression.md documents fdars.regression.concurrent_regression with a hand-authored inline SVG showing one time-varying coefficient CURVE per predictor (p curves, NOT scalars), and an executed offline fence emitting FDARS_FENCE_OK (DOCS-08)."
    - "functional-glm.md documents fdars.regression.functional_glm with a hand-authored inline SVG showing the FPCA-score -> exponential-family-link pipeline (labelling the Gamma inverse link 1/mu), documents the Gamma inverse-link + non-R-comparable AIC caveats, and has an executed offline fence emitting FDARS_FENCE_OK (DOCS-08)."
    - "PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0 with both new pages' fences executing offline against the shipped fdars.regression bindings (DOCS-08)."
    - "Both new SVGs are SVGO-idempotent under svgo@3.3.4 --config svgo.config.mjs (DOCS-08)."
  artifacts:
    - docs/regression/concurrent-regression.md
    - docs/regression/functional-glm.md
    - docs/assets/diagrams/concurrent-regression.svg
    - docs/assets/diagrams/functional-glm.svg
  key_links:
    - "mkdocs.yml Regression nav section -> the two new regression pages"
    - "page SVG references (../assets/diagrams/concurrent-regression.svg, functional-glm.svg) -> the two new SVG files"
    - "executed fences -> fdars.regression.concurrent_regression / functional_glm shipped bindings"
---

<objective>
Document the two new v6.0 Regression bindings (`concurrent_regression`, `functional_glm`) to the project's method-accurate standard. Implements DOCS-08. Two new pages under `docs/regression/`, each with a hand-authored inline SVG and a tiny executed offline worked example emitting `FDARS_FENCE_OK`, wired into the Regression nav section.

This plan is TRACER-FIRST: Task 1 builds the `concurrent-regression.md` page end-to-end (page skeleton + its SVG + executed fence + nav wiring + a per-page DOCS_FAST strict build) to prove the whole docs toolchain runs against the real shipped `fdars.regression` bindings. Task 2 expands to the `functional-glm.md` page. Task 3 is the SVGO-idempotence + page-fence verification gate across both pages.

Purpose: Document the Group A regression surface (Phase 37) so the milestone can close, capturing the Gamma inverse-link (1/mu) and non-R-comparable AIC caveats surfaced during binding.
Output: Two new pages, two new SVGs, two Regression nav entries.

## Artifacts this phase produces (41-01)

- New pages: `docs/regression/concurrent-regression.md`, `docs/regression/functional-glm.md`
- New SVGs: `docs/assets/diagrams/concurrent-regression.svg`, `docs/assets/diagrams/functional-glm.svg`
- Nav entries (under `- Regression:` in mkdocs.yml, after "Robust Regression"): "Concurrent Regression", "Functional GLM"
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-diagrams-worked-examples/41-CONTEXT.md
@.planning/phases/41-docs-diagrams-worked-examples/41-RESEARCH.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/inference/functional-inference.md
@docs/assets/diagrams/functional-boxplot.svg
@src/regression_mod.rs
@scripts/docs_fig.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Concurrent Regression page — one page end-to-end (page + SVG + fence + nav + fast build)</name>
  <read_first>
    - docs/inference/functional-inference.md (page template: H1, intro, SVG include, ## Theory with KaTeX + parameter/returns tables, exec fence ending with the FDARS_FENCE_OK sentinel, ## References)
    - docs/assets/diagrams/functional-boxplot.svg (conformant SVG exemplar: viewBox 720x300, verbatim style block, role="img"+aria-label, two-panel layout)
    - docs/assets/diagrams/STYLE_SPEC.md (viewBox 720x300 standard, canonical 5-class style block copied verbatim, structural colours, data-curve palette #3f51b5 #e8710a #198754 ..., stroke weights)
    - src/regression_mod.rs lines 1035-1059 (concurrent_regression signature: predictors=list[np.ndarray] each (n,m); response (n,m); argvals positional-3rd optional; bandwidth=0.2; kernel="gaussian"; returns dict beta_curve (p,m), intercept (m,), fitted (n,m), residuals (n,m), argvals (m,))
    - scripts/docs_fig.py (fig, render, fast helpers; FDARS_COLORS; svg.hashsalt determinism)
    - mkdocs.yml lines 113-126 (Regression nav section; insert new entries after "Robust Regression: regression/robust-regression.md" at line 126)
  </read_first>
  <files>docs/regression/concurrent-regression.md, docs/assets/diagrams/concurrent-regression.svg, mkdocs.yml</files>
  <action>
    Author docs/assets/diagrams/concurrent-regression.svg as hand-authored inline SVG conforming to STYLE_SPEC (viewBox "0 0 720 300", fill="none", role="img" + aria-label matching the title, the 5-class style block copied verbatim). The diagram MUST be method-accurate: left panel shows several overlapping predictor curves; right panel shows the corresponding TIME-VARYING coefficient curves — one smooth curve per predictor (p curves), NOT a single scalar per predictor. This time-varying-coefficient nature is the load-bearing method fact; a scalar-coefficient depiction is a method-accuracy error. Use the data-curve palette in order (#3f51b5, #e8710a for two predictors). Label the right panel axis with the coefficient-function symbol.

    Create docs/regression/concurrent-regression.md following the functional-inference.md page template: H1 "Concurrent (Varying-Coefficient) Regression"; a short intro framing the varying-coefficient model (each predictor's effect is itself a function of the domain); the SVG include line using the pattern ![alt](../assets/diagrams/concurrent-regression.svg){ .fdars-diagram }; a ## Theory section with the model equation in KaTeX (response as intercept function plus a sum of predictor-times-coefficient-function terms plus error), a parameter table (predictors, response, argvals, bandwidth, kernel), and a Returns table (beta_curve (p,m), intercept, fitted, residuals, argvals). Include an admonition note that beta_curve is shaped (p, m) — predictors x grid, NOT (n_obs, m) — the top transposition pitfall.

    Add a fence using the exec/html/source-above attributes, following the blueprint in 41-RESEARCH.md ("DOCS-08: concurrent_regression fence"): tiny synthetic data (n=20, m=50), two predictor curves, call reg.concurrent_regression([x1, x2], y, t), extract beta=np.asarray(res["beta_curve"]) of shape (2, m), plot both coefficient curves with fig/render, print the beta_curve shape line, and end with the exact final line that prints the FDARS_FENCE_OK sentinel. Keep the data tiny for build speed.

    Wire nav: in mkdocs.yml, after the "Robust Regression: regression/robust-regression.md" line, add the "Concurrent Regression: regression/concurrent-regression.md" entry (match the existing 4-space indentation of sibling Regression entries).

    Run the per-page fast strict build to prove the toolchain end-to-end. Do NOT run the full (no-DOCS_FAST) build in this plan — that ~19-min gate runs once in 41-04. Do NOT edit any src/*.rs file; if the fence reveals an API mismatch, fix the FENCE to match the shipped binding (or record a real bug), never the binding.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "FDARS_FENCE_OK" site/regression/concurrent-regression/index.html && grep -q "concurrent-regression.svg" site/regression/concurrent-regression/index.html && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/concurrent-regression.svg exists, conforms to STYLE_SPEC (viewBox 720x300, verbatim style block, role="img"+aria-label), and shows p time-varying coefficient CURVES (one per predictor), not scalars.
    - docs/regression/concurrent-regression.md exists with H1, the SVG include, a KaTeX Theory section, parameter + Returns tables, the (p,m) transposition admonition, and an executed fence.
    - The fence emits the FDARS_FENCE_OK sentinel in the built HTML.
    - mkdocs.yml has a "Concurrent Regression" entry under Regression after "Robust Regression".
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify command prints TRACER_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>The concurrent-regression page builds offline under DOCS_FAST, its fence emits FDARS_FENCE_OK, its method-accurate SVG renders, and its nav entry is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Functional GLM page — SVG + fence + nav (Gamma inverse-link + AIC caveats)</name>
  <read_first>
    - docs/regression/concurrent-regression.md (the page authored in Task 1 — reuse its structure/heading conventions)
    - docs/assets/diagrams/STYLE_SPEC.md (SVG contract — same as Task 1)
    - src/regression_mod.rs lines 1166-1192 (functional_glm signature: data (n,m); response (n,) scalar; family in {gaussian,binomial,poisson,gamma}; n_comp=3; scalar_covariates=None; max_iter=25; tol=1e-6; returns dict with intercept, beta_t (m,), beta_se (m,), gamma (q,), fitted_values, linear_predictors, ncomp, coefficients, std_errors, log_likelihood, deviance, iterations, aic, bic, family)
    - src/regression_mod.rs line 1091 (code comment: "Gamma uses inverse canonical link g(mu)=1/mu, NOT log-link (unlike R default)")
    - mkdocs.yml (Regression nav; insert after the "Concurrent Regression" entry added in Task 1)
  </read_first>
  <files>docs/regression/functional-glm.md, docs/assets/diagrams/functional-glm.svg, mkdocs.yml</files>
  <action>
    Author docs/assets/diagrams/functional-glm.svg as hand-authored inline SVG conforming to STYLE_SPEC (viewBox "0 0 720 300", fill="none", role="img"+aria-label, verbatim style block). Method-accurate: a left-to-right flow diagram showing the two-stage pipeline — (1) FPCA projects the functional data onto FPC scores; (2) a GLM is fit in score space with the family-dependent link. Include a family/link dispatch fork labelling: Gaussian=identity, Binomial=logit, Poisson=log, Gamma=inverse (1/mu). The Gamma branch MUST be labelled with the inverse link 1/mu (not log) — this is the load-bearing method fact that prevents R-user confusion.

    Create docs/regression/functional-glm.md following the same page template as Task 1: H1 "Functional Generalized Linear Model"; intro framing the exponential-family GLM on FPC scores; SVG include ![alt](../assets/diagrams/functional-glm.svg){ .fdars-diagram }; ## Theory with the FPCA-projection + link-function description in KaTeX; parameter table (data, response, family, n_comp, scalar_covariates, max_iter, tol); Returns table (all returned keys). Add a warning admonition titled for the Gamma inverse link noting that fdars uses the inverse canonical link g(mu)=1/mu, NOT the log-link that R's glm() defaults to, and a second note that the functional_glm AIC magnitude is NOT comparable to R glm() AIC.

    Add a fence using exec/html/source-above per the blueprint in 41-RESEARCH.md ("DOCS-08: functional_glm fence"): tiny synthetic data (n=30, m=60), a binary (binomial) response, call reg.functional_glm(X, y, family="binomial", n_comp=3), plot beta_t, print a deviance/aic/family summary line, and end with the exact final line printing the FDARS_FENCE_OK sentinel.

    Wire nav: in mkdocs.yml, add "Functional GLM: regression/functional-glm.md" immediately after the "Concurrent Regression" entry (same indentation).

    Do NOT run the full build. Do NOT edit any src/*.rs file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "FDARS_FENCE_OK" site/regression/functional-glm/index.html && grep -q "functional-glm.svg" site/regression/functional-glm/index.html && echo GLM_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/functional-glm.svg exists, conforms to STYLE_SPEC, shows the FPCA-scores -> GLM-link pipeline, and explicitly labels the Gamma branch with the inverse link 1/mu.
    - docs/regression/functional-glm.md exists with H1, SVG include, KaTeX Theory, parameter + Returns tables, a Gamma inverse-link warning admonition, and the non-R-comparable AIC note.
    - The fence emits the FDARS_FENCE_OK sentinel in the built HTML.
    - mkdocs.yml has a "Functional GLM" entry under Regression after "Concurrent Regression".
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify prints GLM_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>The functional-glm page builds offline under DOCS_FAST, its fence emits FDARS_FENCE_OK, its SVG labels the Gamma inverse link, the Gamma + AIC caveats are documented, and its nav entry is wired.</done>
</task>

<task type="auto">
  <name>Task 3: SVGO-idempotence + fence-presence gate for both DOCS-08 SVGs/pages</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO invocation: svgo@3.3.4 --config svgo.config.mjs; idempotence gate svgo(svgo(svg)) == svgo(svg))
    - svgo.config.mjs (project SVGO config — confirm present)
  </read_first>
  <files>docs/assets/diagrams/concurrent-regression.svg, docs/assets/diagrams/functional-glm.svg</files>
  <action>
    For each new SVG (concurrent-regression.svg, functional-glm.svg), run the SVGO idempotence check with the PINNED version and the config: run svgo@3.3.4 --config svgo.config.mjs on the file to /tmp/pass1.svg, then on /tmp/pass1.svg to /tmp/pass2.svg, and confirm diff pass1 vs pass2 is empty (byte-identical second pass = idempotent/conforming). If a diagram fails idempotence, hand-fix the SVG markup to conform (do NOT let svgo rewrite the committed source — the gate is check-only). Also confirm each SVG has no embedded date/timestamp metadata (determinism-clean).

    Do NOT run the full build here; this task only gates the two SVGs. The whole-site strict build and human diagram review run in 41-04.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for f in docs/assets/diagrams/concurrent-regression.svg docs/assets/diagrams/functional-glm.svg; do npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output /tmp/p1.svg && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input /tmp/p1.svg --output /tmp/p2.svg && diff /tmp/p1.svg /tmp/p2.svg && echo "IDEMPOTENT: $f" || { echo "FAILS: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Both new SVGs pass the svgo@3.3.4 idempotence check (byte-identical second pass); verify prints IDEMPOTENT for each.
    - Neither SVG contains embedded date/timestamp metadata.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>Both DOCS-08 SVGs are SVGO-idempotent and determinism-clean, ready for the whole-site build and human review in 41-04.</done>
</task>

</tasks>

<threat_model>
DOCS-ONLY phase — no source code, no network calls (all fences execute offline), no user-supplied input paths. `security_enforcement` yields no applicable trust boundaries.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01-I | Information Disclosure | executed doc fences | low | accept | Fences use only tiny synthetic in-process data; no secrets, no I/O beyond the build |
</threat_model>

<verification>
- `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0.
- `grep -q FDARS_FENCE_OK site/regression/concurrent-regression/index.html` and `.../functional-glm/index.html` both succeed.
- Both new SVGs pass the svgo@3.3.4 idempotence check.
- `git diff --name-only` shows no `src/*.rs` change.
</verification>

<success_criteria>
Both new Regression pages render with executed offline fences emitting FDARS_FENCE_OK, each carries a method-accurate hand-authored SVG (concurrent = time-varying beta(t) curves; glm = FPCA-scores -> exponential-family link with the Gamma inverse-link labelled), the Gamma inverse-link + non-R-comparable AIC caveats are documented, both nav entries are wired, and both SVGs are SVGO-idempotent. The full whole-site strict build and human diagram review are deferred to 41-04.
</success_criteria>

<output>
Create `.planning/phases/41-docs-diagrams-worked-examples/41-01-SUMMARY.md` when done.
</output>
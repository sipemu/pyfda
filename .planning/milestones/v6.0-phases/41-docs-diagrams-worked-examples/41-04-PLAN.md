---
phase: 41-docs-diagrams-worked-examples
plan: 04
type: execute
wave: 4
depends_on: [41-01, 41-02, 41-03]
autonomous: false
files_modified:
  - docs/advisor/aspects.md
  - mkdocs.yml
requirements: [DOCS-11]
estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "docs/advisor/aspects.md documents the extended outliers/regression (and classification/fpca) diagnostics from Phase 40 using the ACTUAL emitted diagnostic keys, verified against python/fdars/advisor/aspects/{outliers,regression,classification,fpca}.py (DOCS-11)."
    - "All 7 new pages/sections are wired into mkdocs.yml nav (Concurrent Regression, Functional GLM, PACE FPCA, Interval-wise Inference; classification.md/depth-functions.md/outlier-detection.md already in nav) (DOCS-11)."
    - "The whole-site full build PYTHONPATH=scripts .venv/bin/mkdocs build --strict (NO DOCS_FAST) exits 0 with every new FDARS_FENCE_OK present (DOCS-11)."
    - "Every new SVG (7) is SVGO-idempotent (svgo@3.3.4 twice, byte-identical) and determinism-clean (no date/timestamp metadata) (DOCS-11)."
    - "The BLOCKING human diagram method-accuracy review is presented (rsvg-convert each new SVG -> PNG): depth hypograph/epigraph asymmetry, PACE ragged-obs -> smooth eigenfunctions, ITP closure direction (adjusted <= raw), concurrent-regression time-varying beta(t) curves (DOCS-11)."
  artifacts:
    - docs/advisor/aspects.md
  key_links:
    - "docs/advisor/aspects.md outliers/regression sections -> the actual diagnostic keys emitted by python/fdars/advisor/aspects/{outliers,regression}.py"
    - "mkdocs.yml nav -> all 4 new pages"
    - "full mkdocs build --strict (no DOCS_FAST) -> every new fence's FDARS_FENCE_OK"
    - "rsvg-convert PNG renders -> the blocking human diagram review"
---

<objective>
Close DOCS-11: update the advisor `aspects.md` for the Phase-40 extended diagnostics, confirm every new page is nav-wired, run the whole-site strict build ONCE as the source-of-truth gate, verify whole-corpus SVGO-idempotence + determinism on every new SVG, then present the BLOCKING human diagram method-accuracy review.

This plan runs LAST (wave 4, depends_on 41-01/02/03) so the expensive ~19-min full build and the human review run after all pages + SVGs exist. Task 1 updates aspects.md against the ACTUAL advisor keys. Task 2 confirms nav + runs the full strict build + whole-corpus SVGO/determinism gate. Task 3 renders every new SVG to PNG. Task 4 is the blocking human diagram review (the verifier will return human_needed).

Purpose: Finalize the documentation overhaul milestone with the whole-site build green and a human-verified method-accuracy gate on the 7 new diagrams.
Output: Updated aspects.md; a green whole-site build; a presented blocking human review.

## Artifacts this phase produces (41-04)

- Updated page: `docs/advisor/aspects.md` (outliers + regression diagnostics; classification/fpca as applicable)
- Nav confirmation across all 4 new pages in `mkdocs.yml` (entries added in 41-01/02/03; this plan asserts completeness)
- The whole-site `mkdocs build --strict` gate (run once here)
- The blocking human diagram review (6-7 new SVGs -> PNG)

## Confirmed advisor diagnostic keys (verified this session — NOT [ASSUMED])

Read directly from `python/fdars/advisor/aspects/*.py` at plan time. The advisor is a PACKAGE (not advisor.py). Use these EXACT keys:

- outliers (python/fdars/advisor/aspects/outliers.py):
  - tvdmss: `has_tvdmss`, `n_magnitude_outliers`, `n_shape_outliers`, `magnitude_outlier_fraction`, `shape_outlier_fraction`, `tvd_range`, `mss_range`
  - muod: `has_muod`, `n_muod_magnitude_outliers`, `n_muod_shape_outliers`, `n_amplitude_outliers`, `muod_magnitude_outlier_fraction`, `muod_shape_outlier_fraction`, `amplitude_outlier_fraction`, `shape_index_range`, `magnitude_index_range`, `amplitude_index_range`
  - sequential_transform_outliers: `has_sequential_transform`, `n_union_outliers`, `n_transforms`
  - depthgram: `has_depthgram`, `n_depthgram_shape_outliers`, `n_depthgram_magnitude_outliers`, `depthgram_shape_outlier_fraction`, `depthgram_magnitude_outlier_fraction`, `depthgram_mbd_range`, `depthgram_mei_range`
- regression (python/fdars/advisor/aspects/regression.py):
  - functional_glm: `has_functional_glm`, `deviance`, `aic`, `bic`, `log_likelihood`, `iterations`, `glm_ncomp`, `family`
  - concurrent_regression: `has_concurrent_regression`, `n_predictors`, `concurrent_residual_rms`, `concurrent_residual_max_abs`
- classification (python/fdars/advisor/aspects/classification.py): `has_elastic_multinomial` branch (trigger key "train_accuracy")
- fpca (python/fdars/advisor/aspects/fpca.py): `pace_fpca` branch (trigger key "eigenvalues")

NOTE: the [ASSUMED] key names in 41-RESEARCH.md (e.g. `tvdmss_n_magnitude_outliers`, `seq_transform_n_union`) are WRONG — use the confirmed keys above.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-diagrams-worked-examples/41-CONTEXT.md
@.planning/phases/41-docs-diagrams-worked-examples/41-RESEARCH.md
@docs/advisor/aspects.md
@python/fdars/advisor/aspects/outliers.py
@python/fdars/advisor/aspects/regression.py
@python/fdars/advisor/aspects/classification.py
@python/fdars/advisor/aspects/fpca.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update advisor/aspects.md for the extended outliers/regression (and classification/fpca) diagnostics</name>
  <read_first>
    - docs/advisor/aspects.md (current aspect coverage tables; outliers section ~lines 270-350; regression section ~lines 330-350 — locate the exact sections and their key tables)
    - python/fdars/advisor/aspects/outliers.py (grep the diag["..."] assignments — confirmed keys listed in this plan's objective)
    - python/fdars/advisor/aspects/regression.py (diag["..."] assignments: has_functional_glm/deviance/aic/bic/glm_ncomp/family and has_concurrent_regression/n_predictors/concurrent_residual_rms)
    - python/fdars/advisor/aspects/classification.py (has_elastic_multinomial branch)
    - python/fdars/advisor/aspects/fpca.py (pace_fpca branch)
  </read_first>
  <files>docs/advisor/aspects.md</files>
  <action>
    Re-grep the four advisor aspect modules at execution time to reconfirm the exact diag key names, then update docs/advisor/aspects.md so the documented fdars sources + diagnostic-key tables match reality:

    - outliers aspect: add tvdmss, muod, sequential_transform_outliers, depthgram to the documented fdars sources list, and add key-table rows for the CONFIRMED keys (has_tvdmss, n_magnitude_outliers, n_shape_outliers; has_muod, n_muod_magnitude_outliers, n_muod_shape_outliers, n_amplitude_outliers; has_sequential_transform, n_union_outliers, n_transforms; has_depthgram, n_depthgram_shape_outliers, n_depthgram_magnitude_outliers; plus the *_range/*_fraction keys as documented in the module docstrings). Do NOT use the wrong [ASSUMED] names from 41-RESEARCH.md.
    - regression aspect: add functional_glm and concurrent_regression to the fdars sources list, and document the confirmed keys (has_functional_glm, deviance, aic, bic, glm_ncomp, family; has_concurrent_regression, n_predictors, concurrent_residual_rms, concurrent_residual_max_abs).
    - If the classification and fpca aspect sections in aspects.md need the elastic_multinomial (has_elastic_multinomial) and pace_fpca branches documented to stay consistent with the Phase-40 code, add concise rows there too (the CONTEXT decision names classification/fpca among the extended diagnostics).

    Keep the edit text-only; do NOT edit any source or advisor .py file. Run a DOCS_FAST build of the advisor page to confirm it still renders (aspects.md has no executed fence to gate, so this is a render check).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "has_functional_glm\|functional_glm" site/advisor/aspects/index.html && grep -q "tvdmss" site/advisor/aspects/index.html && grep -q "concurrent_regression" site/advisor/aspects/index.html && echo ASPECTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/advisor/aspects.md outliers section documents tvdmss/muod/sequential_transform_outliers/depthgram with the CONFIRMED diagnostic keys (matching python/fdars/advisor/aspects/outliers.py).
    - docs/advisor/aspects.md regression section documents functional_glm + concurrent_regression with the confirmed keys (matching regression.py).
    - No [ASSUMED]/wrong key names (e.g. tvdmss_n_magnitude_outliers, seq_transform_n_union) appear.
    - The advisor page renders under DOCS_FAST; verify prints ASPECTS_OK.
    - No source/.py file modified.
  </acceptance_criteria>
  <done>aspects.md documents the Phase-40 extended outliers/regression (and classification/fpca) diagnostics with the actual emitted keys, and the advisor page renders.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm nav completeness + run the whole-site strict build + whole-corpus SVGO/determinism gate</name>
  <read_first>
    - mkdocs.yml (assert all 4 new nav entries present: "Concurrent Regression" + "Functional GLM" under Regression; "PACE FPCA" under Represent; "Interval-wise Inference" under Inference)
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO idempotence gate; svgo@3.3.4 --config svgo.config.mjs)
    - 41-RESEARCH.md "Full site build" + "SVGO idempotence check per new SVG" recipes
  </read_first>
  <files>mkdocs.yml</files>
  <action>
    Assert the four new nav entries exist in mkdocs.yml (Concurrent Regression, Functional GLM, PACE FPCA, Interval-wise Inference); if any is missing (e.g. a prior plan's nav edit was lost), add it in the correct section.

    Run the WHOLE-SITE full strict build ONCE — the source-of-truth gate — with NO DOCS_FAST: PYTHONPATH=scripts .venv/bin/mkdocs build --strict. Expect exit 0 (~19 min). Then confirm every new fence's FDARS_FENCE_OK is present in the built HTML across all new pages (concurrent-regression, functional-glm, pace-fpca, classification, depth-functions, outlier-detection, interval-inference).

    Run the SVGO idempotence check on ALL 7 new SVGs (concurrent-regression, functional-glm, pace-fpca, elastic-multinomial, itp-interval-inference, functional-outliers, and depth-asymmetry.svg IF a dedicated one was authored — otherwise 6) with the pinned svgo@3.3.4 + config: two passes, byte-identical second pass. Confirm each is determinism-clean (no date/timestamp metadata). Do NOT let svgo rewrite committed sources — hand-fix any non-conformance.

    Do NOT edit any src/*.rs file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q "concurrent-regression.md" mkdocs.yml && grep -q "functional-glm.md" mkdocs.yml && grep -q "pace-fpca.md" mkdocs.yml && grep -q "interval-inference.md" mkdocs.yml && PYTHONPATH=scripts .venv/bin/mkdocs build --strict && for p in regression/concurrent-regression regression/functional-glm represent/pace-fpca regression/classification represent/depth-functions analyze/outlier-detection inference/interval-inference; do grep -q FDARS_FENCE_OK "site/$p/index.html" || { echo "MISSING FENCE: $p"; exit 1; }; done && for f in docs/assets/diagrams/concurrent-regression.svg docs/assets/diagrams/functional-glm.svg docs/assets/diagrams/pace-fpca.svg docs/assets/diagrams/elastic-multinomial.svg docs/assets/diagrams/itp-interval-inference.svg docs/assets/diagrams/functional-outliers.svg; do npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output /tmp/p1.svg && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input /tmp/p1.svg --output /tmp/p2.svg && diff /tmp/p1.svg /tmp/p2.svg && ! grep -qi "dc:date\|<date" "$f" || { echo "SVG FAIL: $f"; exit 1; }; done && echo FULL_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new nav entries present in mkdocs.yml.
    - `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` (no DOCS_FAST) exits 0.
    - FDARS_FENCE_OK present in all 7 built pages (concurrent-regression, functional-glm, pace-fpca, classification, depth-functions, outlier-detection, interval-inference).
    - All new SVGs pass svgo@3.3.4 idempotence and carry no date/timestamp metadata.
    - verify prints FULL_GATE_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>Nav is complete, the whole-site strict build is green with every new FDARS_FENCE_OK present, and every new SVG is SVGO-idempotent + determinism-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Render all new SVGs to PNG for the human review</name>
  <read_first>
    - 41-RESEARCH.md rsvg-convert recipe (render each new SVG -> PNG at width 1440)
  </read_first>
  <files>(no repo files modified — renders to /tmp only)</files>
  <action>
    Render each new SVG to a PNG with rsvg-convert so the user can inspect them in Task 4. If rsvg-convert is missing, install librsvg first (e.g. sudo pacman -S librsvg). Render: for SVG in docs/assets/diagrams/concurrent-regression.svg docs/assets/diagrams/functional-glm.svg docs/assets/diagrams/pace-fpca.svg docs/assets/diagrams/elastic-multinomial.svg docs/assets/diagrams/functional-outliers.svg docs/assets/diagrams/itp-interval-inference.svg; do rsvg-convert -w 1440 "$SVG" -o "/tmp/$(basename ${SVG%.svg}).png"; done. Do NOT edit any repo file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for SVG in docs/assets/diagrams/concurrent-regression.svg docs/assets/diagrams/functional-glm.svg docs/assets/diagrams/pace-fpca.svg docs/assets/diagrams/elastic-multinomial.svg docs/assets/diagrams/functional-outliers.svg docs/assets/diagrams/itp-interval-inference.svg; do rsvg-convert -w 1440 "$SVG" -o "/tmp/$(basename ${SVG%.svg}).png" && test -s "/tmp/$(basename ${SVG%.svg}).png" || { echo "RENDER FAIL: $SVG"; exit 1; }; done && echo RENDER_OK</automated>
  </verify>
  <acceptance_criteria>
    - Every new SVG rendered to a non-empty PNG in /tmp; verify prints RENDER_OK.
    - No repo file modified.
  </acceptance_criteria>
  <done>All new diagrams are rendered to PNG, ready for the blocking human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: BLOCKING human diagram method-accuracy review</name>
  <what-built>
    Six (or seven, if a dedicated depth-asymmetry.svg was authored) new hand-authored SVG diagrams, each rendered to a PNG in /tmp (Task 3): concurrent-regression.png, functional-glm.png, pace-fpca.png, elastic-multinomial.png, functional-outliers.png, itp-interval-inference.png. This is the milestone's blocking human method-accuracy gate — it is NOT auto-approvable (workflow.auto_advance is ignored for gate="blocking-human").
  </what-built>
  <how-to-verify>
    Open each PNG in /tmp and confirm the diagram is method-accurate:
    1. /tmp/concurrent-regression.png — shows one TIME-VARYING coefficient curve per predictor (p curves), NOT scalars.
    2. /tmp/functional-glm.png — FPCA-scores -> exponential-family link pipeline, with the Gamma branch labelled inverse link 1/mu (NOT log).
    3. /tmp/pace-fpca.png — sparse IRREGULAR observation dots (ragged per-curve grids) -> smooth recovered eigenfunctions on the work grid (NOT regular-grid data).
    4. /tmp/elastic-multinomial.png — K one-vs-rest binary classifiers -> softmax (NOT LDA).
    5. /tmp/functional-outliers.png — hypograph vs epigraph ASYMMETRY (bottom-of-bundle curve high hypograph / low epigraph), with >= 2 reference curves and correct asymmetric labels.
    6. /tmp/itp-interval-inference.png — closure-adjusted p-value VECTOR with adjusted at or BELOW raw (correct closure direction), plus a 0.05 threshold line.
    If any diagram is method-inaccurate, describe the issue: Claude will hand-fix the SVG, re-run its SVGO idempotence check, re-render, and re-present.
  </how-to-verify>
  <resume-signal>Type "approved" once every diagram is method-accurate, or describe which diagram(s) need fixes.</resume-signal>
</task>

</tasks>

<threat_model>
DOCS-ONLY phase — no source code, no network calls, no user-supplied input paths. No package installs.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-04-I | Information Disclosure | whole-site executed fences | low | accept | All fences execute offline against in-repo data; no secrets, no external I/O |
| T-41-04-T | Tampering | hand-authored SVG method accuracy | medium | mitigate | Blocking human diagram method-accuracy review (Task 3) — not auto-approvable — gates milestone close |
</threat_model>

<verification>
- `grep -q` confirms all 4 new nav entries in mkdocs.yml.
- `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` (no DOCS_FAST) exits 0.
- FDARS_FENCE_OK present across all 7 new built pages.
- All new SVGs pass svgo@3.3.4 idempotence + carry no date/timestamp metadata.
- The blocking human diagram review is presented and approved.
</verification>

<success_criteria>
aspects.md documents the Phase-40 extended diagnostics with the actual emitted keys; all new pages are nav-wired; the whole-site strict build is green with every new FDARS_FENCE_OK present; every new SVG is SVGO-idempotent + determinism-clean; and the blocking human diagram method-accuracy review has been presented for approval. This plan's final task pauses for the user (verifier returns human_needed).
</success_criteria>

<output>
Create `.planning/phases/41-docs-diagrams-worked-examples/41-04-SUMMARY.md` when done.
</output>

---
phase: 41-docs-diagrams-worked-examples
plan: 03
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - docs/inference/interval-inference.md
  - docs/represent/depth-functions.md
  - docs/analyze/outlier-detection.md
  - docs/assets/diagrams/itp-interval-inference.svg
  - docs/assets/diagrams/functional-outliers.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-10]
estimate:
  tokens: 80000
  raw_tokens: 53000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The built site has an Inference nav entry 'Interval-wise Inference' -> inference/interval-inference.md (DOCS-10)."
    - "interval-inference.md documents fdars.inference.itp_one_pop/itp_two_pop/itp_flm with a hand-authored inline SVG showing the closure-adjusted p-value VECTOR (adjusted at or BELOW raw, correct closure direction), and an executed offline fence emitting FDARS_FENCE_OK (DOCS-10)."
    - "depth-functions.md folds in the 9 new functional_depth methods with a method table + a representative executed fence emitting FDARS_FENCE_OK (DOCS-10)."
    - "outlier-detection.md gains the 4 new detectors (tvdmss/muod/sequential_transform_outliers/depthgram) with a hand-authored inline SVG showing hypograph-vs-epigraph asymmetry and an executed offline fence emitting FDARS_FENCE_OK (DOCS-10)."
    - "PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0 with all three pages' fences executing offline against the shipped fdars.depth/outliers/inference bindings (DOCS-10)."
    - "Both new SVGs are SVGO-idempotent under svgo@3.3.4 --config svgo.config.mjs (DOCS-10)."
  artifacts:
    - docs/inference/interval-inference.md
    - docs/represent/depth-functions.md
    - docs/analyze/outlier-detection.md
    - docs/assets/diagrams/itp-interval-inference.svg
    - docs/assets/diagrams/functional-outliers.svg
  key_links:
    - "mkdocs.yml Inference nav section -> inference/interval-inference.md"
    - "page SVG references (../assets/diagrams/itp-interval-inference.svg, functional-outliers.svg) -> the two new SVG files"
    - "executed fences -> fdars.depth.functional_depth, fdars.outliers.{tvdmss,muod,sequential_transform_outliers,depthgram}, fdars.inference.itp_* shipped bindings"
---

<objective>
Document the new v6.0 Group C surface — 9 new functional depth methods, 4 new functional-outlier detectors, and interval-wise inference (ITP). Implements DOCS-10. One new page `docs/inference/interval-inference.md` (wired into Inference nav) plus extensions to `docs/represent/depth-functions.md` and `docs/analyze/outlier-detection.md`, with two new hand-authored SVGs and executed offline fences emitting `FDARS_FENCE_OK`.

This plan is TRACER-FIRST: Task 1 builds the `interval-inference.md` page end-to-end (page + SVG + ITP fence + nav wiring + DOCS_FAST strict build) to prove the ITP path (with its closure-adjusted p-value vector) runs offline against the shipped bindings. Tasks 2-3 expand to the depth-functions and outlier-detection extensions. Task 4 is the SVGO-idempotence gate.

Purpose: Document the largest-pitfall Group C surface (Phase 39) to the project's method-accurate standard, capturing the ITP closure direction, the sequential_transform depth_method param, and the ITP n_basis-vs-nbasis clamping fact.
Output: One new page, two page extensions, two new SVGs, one Inference nav entry.

## Artifacts this phase produces (41-03)

- New page: `docs/inference/interval-inference.md`
- Extended pages: `docs/represent/depth-functions.md` (9 new depth methods), `docs/analyze/outlier-detection.md` (4 new detectors)
- New SVGs: `docs/assets/diagrams/itp-interval-inference.svg`, `docs/assets/diagrams/functional-outliers.svg`
- Nav change (under `- Inference:` in mkdocs.yml): add "Interval-wise Inference" as a second entry after "Functional Inference"
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-diagrams-worked-examples/41-CONTEXT.md
@.planning/phases/41-docs-diagrams-worked-examples/41-RESEARCH.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/inference/functional-inference.md
@docs/represent/depth-functions.md
@docs/analyze/outlier-detection.md
@src/inference_mod.rs
@src/depth_mod.rs
@src/outliers_mod.rs
@scripts/docs_fig.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Interval-wise Inference page — one page end-to-end (page + SVG + ITP fence + nav + fast build)</name>
  <read_first>
    - docs/inference/functional-inference.md (page template + the sibling Inference page)
    - docs/assets/diagrams/STYLE_SPEC.md (SVG contract: viewBox 720x300, verbatim style block, role="img"+aria-label; the ITP diagram may use 720x300 with two side-by-side panels)
    - src/inference_mod.rs lines 637-792 (itp_one_pop(data, argvals, mu0=None, basis_type="bspline", nbasis=5, n_perm=999, seed=None); itp_two_pop(data_a, data_b, argvals, ...); itp_flm(data, response, argvals, ...); all return 5-key dict adjusted_pvalues (n_basis,), raw_pvalues (n_basis,), basis_type, n_basis (ACTUAL, may be < nbasis after bspline clamping), n_perm)
    - src/inference_mod.rs lines 617-619 (comment: B-spline clamping may reduce actual count; read n_basis from the returned dict for the array length)
    - scripts/docs_fig.py (fig, render, fast; fast(199, 19) for n_perm)
    - mkdocs.yml lines 132-133 (current single-item Inference section; convert to two items by appending the new page)
    - 41-RESEARCH.md "DOCS-10: itp_one_pop fence" blueprint + Pitfall 5 (n_basis return vs nbasis input) + SVG #6 method-accuracy notes (closure REDUCES p-values: adjusted at or BELOW raw)
  </read_first>
  <files>docs/inference/interval-inference.md, docs/assets/diagrams/itp-interval-inference.svg, mkdocs.yml</files>
  <action>
    Author docs/assets/diagrams/itp-interval-inference.svg as hand-authored inline SVG conforming to STYLE_SPEC (viewBox "0 0 720 300", fill="none", role="img"+aria-label, verbatim style block). Method-accurate (reviewer-flagged, CRITICAL): the ITP p-value is a VECTOR — one closure-adjusted p-value per basis function, plotted against basis-function index / domain interval (NOT a single scalar). Suggested layout: left panel = the test-statistic curve over the domain; right panel = side-by-side bars of raw p-values (light) and closure-adjusted p-values (darker), with the adjusted bars at or BELOW the raw bars (closure REDUCES p-values — makes rejection easier), plus a horizontal dashed 0.05 threshold line. A diagram showing closure INFLATING p-values (adjusted above raw) is a method-accuracy error and will fail the human review.

    Create docs/inference/interval-inference.md following the functional-inference.md template: H1 "Interval-wise Testing Procedure (ITP)"; intro framing ITP (tests where along the domain / on which basis coefficients two functional populations differ, with interval-wise family-wise control via closure); SVG include ![alt](../assets/diagrams/itp-interval-inference.svg){ .fdars-diagram }; ## Theory in KaTeX (basis expansion + permutation p-values + closure adjustment); parameter tables for the three functions (itp_one_pop / itp_two_pop / itp_flm — noting the differing first args: data+mu0, data_a+data_b, data+response); a Returns table (adjusted_pvalues, raw_pvalues, basis_type, n_basis, n_perm). Add an admonition note (Pitfall 5): n_basis in the returned dict is the ACTUAL basis count after bspline clamping and may be < the requested nbasis; the p-value arrays have length n_basis, not nbasis. Add a note that adjusted_pvalues are at or below raw_pvalues (closure only reduces).

    Add a fence using exec/html/source-above per the "DOCS-10: itp_one_pop fence" blueprint: n=20, m=40 synthetic curves with a local mean shift; n_perm = fast(199, 19); res = fi.itp_one_pop(X, t, mu0=None, basis_type="bspline", nbasis=5, n_perm=n_perm, seed=0); print n_basis(actual)/n_perm, adjusted_pvalues, raw_pvalues (rounded); end with the exact final line printing the FDARS_FENCE_OK sentinel. Use seed=0 for offline determinism.

    Wire nav: in mkdocs.yml, under the Inference section, keep "Functional Inference: inference/functional-inference.md" and add a second entry "Interval-wise Inference: inference/interval-inference.md" (match the existing indentation).

    Run the per-page fast strict build. Do NOT run the full build. Do NOT edit any src/*.rs file; fix the FENCE, never the binding.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "FDARS_FENCE_OK" site/inference/interval-inference/index.html && grep -q "itp-interval-inference.svg" site/inference/interval-inference/index.html && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/itp-interval-inference.svg exists, conforms to STYLE_SPEC, shows the p-value VECTOR (per basis function) with adjusted at or below raw and a 0.05 threshold line.
    - docs/inference/interval-inference.md exists with H1, SVG include, KaTeX Theory, parameter tables for the three itp_* functions, a Returns table, the n_basis-vs-nbasis clamping note, and an executed fence (seed=0, n=20).
    - The fence emits the FDARS_FENCE_OK sentinel in the built HTML.
    - mkdocs.yml Inference section has both "Functional Inference" and "Interval-wise Inference" entries.
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify prints TRACER_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>The interval-inference page builds offline under DOCS_FAST, its ITP fence emits FDARS_FENCE_OK, its method-accurate closure-direction SVG renders, and its nav entry is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Fold the 9 new functional_depth methods into depth-functions.md (method table + fence)</name>
  <read_first>
    - docs/represent/depth-functions.md lines 290-505 (existing ## Comparison table, ## API summary at ~488, ## References; append the new method table + fence in a new section before ## References)
    - src/depth_mod.rs lines 428-442, 518-531 (functional_depth(data, method="fraiman_muniz", scale=True, nproj=50, seed=None) -> (n,); 13 accepted method strings; the 9 NEW ones vs the pre-v6.0 set: "hypograph_index", "modified_hypograph_index", "epigraph_index", "half_region", "modified_half_region", "extremal", "extreme_rank_length", "l_infinity", "total_variation")
    - 41-RESEARCH.md "DOCS-10: functional_depth extension fence" blueprint
  </read_first>
  <files>docs/represent/depth-functions.md</files>
  <action>
    Add a new section to docs/represent/depth-functions.md (before ## References) titled e.g. "## New depth measures (v6.0)". Include a short method table listing the 9 new functional_depth method strings with a one-line meaning each: hypograph_index, modified_hypograph_index, epigraph_index, half_region, modified_half_region, extremal, extreme_rank_length, l_infinity, total_variation. Note the unified entry point fdars.depth.functional_depth(data, method=...) accepting all 13 method strings, and its params (scale for fraiman_muniz; nproj/seed for random_projection). Briefly note the hypograph/epigraph asymmetry (hypograph = proportion of curves BELOW; epigraph = proportion ABOVE) and reference the functional-outliers.svg authored in Task 3 for the visual (or add a one-line pointer to it).

    Add a fence using exec/source-above per the "DOCS-10: functional_depth extension fence" blueprint: simulate a small clean sample with one planted magnitude outlier, loop over the 9 new methods calling functional_depth(X, method=method), print per-method outlier_rank + min; end with the exact final line printing the FDARS_FENCE_OK sentinel. No new nav entry (depth-functions.md already in nav).

    Do NOT run the full build. Do NOT edit any src/*.rs file; fix the FENCE, never the binding.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "hypograph_index" site/represent/depth-functions/index.html && grep -q "FDARS_FENCE_OK" site/represent/depth-functions/index.html && echo DEPTH_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/represent/depth-functions.md gains a "new depth measures (v6.0)" section with a method table listing all 9 new method strings and their meanings, referencing the unified functional_depth entry point.
    - A representative fence loops over the 9 new methods and emits the FDARS_FENCE_OK sentinel in the built HTML.
    - `grep -q hypograph_index site/represent/depth-functions/index.html` succeeds.
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify prints DEPTH_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>depth-functions.md documents the 9 new depth methods with a method table and a representative fence that builds offline and emits FDARS_FENCE_OK.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 4 new outlier detectors to outlier-detection.md — SVG + fence</name>
  <read_first>
    - docs/analyze/outlier-detection.md lines 1-30, 424-534 (existing structure: LRT, outliergram, magnitude-shape sections + ## See also / ## References; append the new detectors section before ## See also)
    - docs/assets/diagrams/STYLE_SPEC.md (SVG contract)
    - src/outliers_mod.rs lines 211-495 (tvdmss(data, emp_factor_mss=1.5, emp_factor_tvd=1.5, central_region_tvd=0.5) -> magnitude_outliers/shape_outliers/tvd/mss, no argvals, no seed; muod(data, factor=1.5) requires n>=3 -> shape/magnitude/amplitude outliers + indices; sequential_transform_outliers(data, transforms, depth_method="modified_band", emp_factor=1.5) -> per_transform_outliers/union_outliers, transforms each one of t0/t1/t2/d1/d2, param is depth_method NOT method; depthgram(data, outliergram_factor=1.5, boxplot_factor=1.5) requires n>=2 -> mbd_mei_*/mei_mbd_* + shape/magnitude outliers + mbd/mei)
    - 41-RESEARCH.md "DOCS-10: tvdmss + muod fence" blueprint + Pitfall 4 (depth_method not method) + SVG #5 method-accuracy notes (hypograph vs epigraph asymmetry: bottom-of-bundle curve has HIGH hypograph / LOW epigraph; show >= 2 reference curves, one near top one near bottom, asymmetric counting)
  </read_first>
  <files>docs/analyze/outlier-detection.md, docs/assets/diagrams/functional-outliers.svg</files>
  <action>
    Author docs/assets/diagrams/functional-outliers.svg as hand-authored inline SVG conforming to STYLE_SPEC (viewBox "0 0 720 300", fill="none", role="img"+aria-label, verbatim style block). Method-accurate (reviewer-flagged): faithfully show the asymmetry between hypograph and epigraph depth. Hypograph index = proportion of curves BELOW the reference curve; epigraph index = proportion ABOVE. A curve at the bottom of the bundle has HIGH hypograph index (many curves above it) and LOW epigraph index; a curve at the top has the reverse. Show at least two reference curves — one near the top, one near the bottom — with the asymmetric counting correctly labelled. Drawing both indices as symmetric is a method-accuracy error.

    Append a new section to docs/analyze/outlier-detection.md (before ## See also) titled e.g. "## Functional-outlier detectors (v6.0)": the SVG include ![alt](../assets/diagrams/functional-outliers.svg){ .fdars-diagram }; a short intro; a subsection or table per detector (tvdmss, muod, sequential_transform_outliers, depthgram) with its signature, a one-line description, and its returned keys; parameter tables. Add notes: tvdmss and muod take no argvals and no seed (deterministic); tvdmss uses the column spacing as the implicit grid; muod requires n>=3, depthgram requires n>=2; sequential_transform_outliers' depth param is depth_method (NOT method) and its transforms are drawn from t0/t1/t2/d1/d2 (Pitfall 4).

    Add a fence using exec/source-above per the "DOCS-10: tvdmss + muod fence" blueprint: simulate a small sample with a planted magnitude + shape outlier; call tvdmss(X), muod(X), sequential_transform_outliers(X, transforms=["t0","d1"]), depthgram(X); print each detector's flagged outlier lists; end with the exact final line printing the FDARS_FENCE_OK sentinel. No new nav entry.

    Do NOT run the full build. Do NOT edit any src/*.rs file; fix the FENCE, never the binding.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "functional-outliers.svg" site/analyze/outlier-detection/index.html && grep -q "FDARS_FENCE_OK" site/analyze/outlier-detection/index.html && echo OUTLIERS_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/functional-outliers.svg exists, conforms to STYLE_SPEC, and shows hypograph-vs-epigraph asymmetry with >= 2 reference curves and correct asymmetric counting labels.
    - docs/analyze/outlier-detection.md gains a "functional-outlier detectors (v6.0)" section covering all 4 detectors (tvdmss, muod, sequential_transform_outliers, depthgram) with signatures, returned keys, the no-argvals/no-seed + n-minimum notes, and the depth_method (Pitfall 4) note.
    - A fence exercises all 4 detectors and emits the FDARS_FENCE_OK sentinel in the built HTML.
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify prints OUTLIERS_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>outlier-detection.md documents all 4 new detectors with a method-accurate asymmetry SVG and a fence that builds offline and emits FDARS_FENCE_OK.</done>
</task>

<task type="auto">
  <name>Task 4: SVGO-idempotence gate for both DOCS-10 SVGs</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO invocation svgo@3.3.4 --config svgo.config.mjs; idempotence gate)
    - svgo.config.mjs (confirm present)
  </read_first>
  <files>docs/assets/diagrams/itp-interval-inference.svg, docs/assets/diagrams/functional-outliers.svg</files>
  <action>
    For each new SVG (itp-interval-inference.svg, functional-outliers.svg), run the SVGO idempotence check with the pinned svgo@3.3.4 and the config: two svgo passes, confirm the second pass is byte-identical (empty diff). If a diagram fails, hand-fix the markup to conform (the gate is check-only; do not let svgo rewrite the committed source). Confirm no embedded date/timestamp metadata. Do NOT run the full build; the whole-site build + human review run in 41-04.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for f in docs/assets/diagrams/itp-interval-inference.svg docs/assets/diagrams/functional-outliers.svg; do npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output /tmp/p1.svg && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input /tmp/p1.svg --output /tmp/p2.svg && diff /tmp/p1.svg /tmp/p2.svg && echo "IDEMPOTENT: $f" || { echo "FAILS: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Both new SVGs pass the svgo@3.3.4 idempotence check (byte-identical second pass); verify prints IDEMPOTENT for each.
    - Neither SVG contains embedded date/timestamp metadata.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>Both DOCS-10 SVGs are SVGO-idempotent and determinism-clean, ready for the whole-site build and human review in 41-04.</done>
</task>

</tasks>

<threat_model>
DOCS-ONLY phase — no source code, no network calls (fences execute offline), no user-supplied input paths.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-03-I | Information Disclosure | executed doc fences | low | accept | Fences use only tiny synthetic in-process data; no secrets, no external I/O |
| T-41-03-D | Denial of Service | ITP permutation fence at build time | low | mitigate | n_perm = fast(199, 19) keeps DOCS_FAST iteration cheap; n=20/m=40 tiny data keeps the full build within budget |
</threat_model>

<verification>
- `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0.
- `grep -q FDARS_FENCE_OK` succeeds in site/inference/interval-inference/index.html, site/represent/depth-functions/index.html, site/analyze/outlier-detection/index.html.
- `grep -q hypograph_index site/represent/depth-functions/index.html` succeeds.
- Both new SVGs pass the svgo@3.3.4 idempotence check.
- `git diff --name-only` shows no `src/*.rs` change.
</verification>

<success_criteria>
The new interval-inference page, the 9-method depth extension, and the 4-detector outlier extension all render with executed offline fences emitting FDARS_FENCE_OK, the two new SVGs are method-accurate (ITP closure direction: adjusted at or below raw; hypograph/epigraph asymmetry) and SVGO-idempotent, and the Interval-wise Inference nav entry is wired. The full whole-site strict build and human diagram review are deferred to 41-04.
</success_criteria>

<output>
Create `.planning/phases/41-docs-diagrams-worked-examples/41-03-SUMMARY.md` when done.
</output>
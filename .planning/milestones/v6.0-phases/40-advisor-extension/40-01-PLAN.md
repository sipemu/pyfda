---
phase: 40-advisor-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/aspects/outliers.py
  - python/fdars/advisor/aspects/regression.py
  - python/fdars/advisor/aspects/classification.py
  - python/fdars/advisor/aspects/fpca.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor_outliers_v6.py
  - tests/test_advisor_regression_v6.py
  - tests/test_advisor_group_b.py
autonomous: true
requirements: [ADV-04, ADV-05]

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 5
  confidence: high

must_haves:
  truths:
    - "ADV-04: build_diagnostics(tvdmss_result, method='outliers') emits grounded scalars — n_magnitude_outliers, n_shape_outliers, magnitude/shape_outlier_fraction, tvd_range, mss_range, has_tvdmss=True — every value a native float/int/bool, detection trigger 'tvd' in raw and 'mss' in raw"
    - "ADV-04: muod (trigger 'amplitude_outliers'), sequential_transform_outliers (trigger 'union_outliers'; NO outlier_fraction — n_obs unrecoverable), and depthgram (trigger 'mbd_mei_d') each emit grounded scalars; depthgram is checked BEFORE the existing outliergram branch because both carry mbd/mei"
    - "ADV-05: build_diagnostics(functional_glm_result, method='regression') emits grounded deviance/aic/bic/log_likelihood/iterations/ncomp/family/has_functional_glm (key is 'iterations' NOT 'n_iter'; there is NO 'converged' key); trigger 'deviance' in raw"
    - "ADV-05: build_diagnostics(concurrent_regression_result, method='regression') emits residual_rms + residual_max_abs from the 2-D (n,m) residuals + n_predictors (rows of beta_curve) + has_concurrent_regression; trigger 'beta_curve' in raw"
    - "ADV-05 Group B: build_diagnostics(elastic_multinomial_result, method='classification') emits n_classes (from raw), train_accuracy, train_error_rate, has_elastic_multinomial (trigger 'train_accuracy'); build_diagnostics(pace_fpca_result, method='fpca') emits ncomp, sigma2, variance_explained_cumulative (via _eigenvalues_to_variance_cumulative), variance_explained_first, has_pace_fpca (trigger 'eigenvalues')"
    - "Grounding + determinism: every new diagnostic is native float/int/bool (no numpy scalar), json.dumps(sort_keys=True) is byte-identical across two runs, and _check_grounding passes for the LLM advice path on all six new result shapes"
    - "NO new aspect key added: outliers/regression/classification/fpca already in _supported and _DIAGNOSTICS_METHODS; tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported stays green with ZERO guard edits (MCP guard-sync is a strict no-op)"
    - "ITP interval-inference advisor coverage is DEFERRED (vector adjusted_pvalues, no single grounded scalar) — no ITP branch is added"
  artifacts:
    - python/fdars/advisor/aspects/outliers.py
    - python/fdars/advisor/aspects/regression.py
    - python/fdars/advisor/aspects/classification.py
    - python/fdars/advisor/aspects/fpca.py
    - tests/test_advisor_outliers_v6.py
    - tests/test_advisor_regression_v6.py
    - tests/test_advisor_group_b.py
  key_links:
    - "advisor/__init__.py dispatch (method_lc == 'outliers'/'regression'/'classification'/'fpca') -> the four extended _build_*_diagnostics builders (dispatch UNCHANGED — pure builder extension)"
    - "_prompts.py _ASPECT_PRIMERS['outliers'] and ['regression'] -> advise() system prompt aspect clause (primer edit lands atomically with its builder)"
    - "advisor/__init__.py _supported set <-> mcp/server.py _DIAGNOSTICS_METHODS frozenset — MUST stay identical; no edit this phase, drift-locked by test_diagnostics_methods_match_advisor_supported"
    - "each new branch's grounded scalars -> _check_grounding via _extract_numbers on json.dumps(diagnostics)"
---

<objective>
Extend four EXISTING advisor aspect builders so they surface grounded scalar diagnostics for the new v6.0 result dicts, with the grounding invariant intact (fdars computes every number; the builder only reduces to native scalars; the LLM only interprets/cites). Python-only — no maturin rebuild; the v6.0 bindings are already live in `.venv`.

- **ADV-04** — `outliers.py`: 4 new detector branches (tvdmss, muod, sequential_transform_outliers, depthgram), closing the v5.0 Phase-34 functional-boxplot-outlier deferral.
- **ADV-05** — `regression.py`: functional_glm + concurrent_regression branches.
- **ADV-05 Group B** (BOTH INCLUDED per 40-RESEARCH — genuinely grounded scalars exist): `classification.py` elastic_multinomial branch; `fpca.py` pace_fpca branch.
- **ITP: DEFERRED** — vector `adjusted_pvalues`, no single grounded scalar without arbitrary reduction; no ITP branch added.

NO new aspect KEY is introduced. `outliers`/`regression`/`classification`/`fpca` are already in the advisor `_supported` set and the MCP `_DIAGNOSTICS_METHODS` frozenset, so the MCP guard-sync is a strict NO-OP — `test_diagnostics_methods_match_advisor_supported` stays green with ZERO edits to `server.py`, `_runner.py`, or `advisor/__init__.py`'s `_supported`.

Purpose: a user can interpret the v6.0 detector/model results through the same grounded interpret/recommend surface as every other aspect, offline-deterministic and grounding-guarded.
Output: four extended builders, two extended primers, three offline test files. Run pytest; no Rust build/fmt/clippy needed (no `src/*.rs` touched).

## Artifacts this phase produces

Extended builders:
- `python/fdars/advisor/aspects/outliers.py` — `_build_outliers_diagnostics` gains 4 detector branches (ADV-04).
- `python/fdars/advisor/aspects/regression.py` — `_build_regression_diagnostics` gains functional_glm + concurrent_regression branches (ADV-05).
- `python/fdars/advisor/aspects/classification.py` — `_build_classification_diagnostics` gains an elastic_multinomial branch (ADV-05 Group B).
- `python/fdars/advisor/aspects/fpca.py` — `_build_fpca_diagnostics` gains a pace_fpca branch (ADV-05 Group B).
- `python/fdars/advisor/_prompts.py` — `_ASPECT_PRIMERS["outliers"]` and `["regression"]` (and optionally `["classification"]`) extended to describe the new fields.

New tests:
- `tests/test_advisor_outliers_v6.py` — offline grounding/determinism tests for all 4 detector branches + the depthgram-before-outliergram ordering guard.
- `tests/test_advisor_regression_v6.py` — offline tests for functional_glm + concurrent_regression.
- `tests/test_advisor_group_b.py` — offline tests for elastic_multinomial (classification) + pace_fpca (fpca).

Unchanged (confirm, do NOT edit): `python/fdars/mcp/server.py` (`_DIAGNOSTICS_METHODS`), `python/fdars/mcp/_runner.py` (`_RUNNABLE_METHODS`), `python/fdars/advisor/__init__.py` (`_supported` + dispatch — all four keys already present and route correctly).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-advisor-extension/40-CONTEXT.md
@.planning/phases/40-advisor-extension/40-RESEARCH.md

# The four builders to extend (read each fully before editing)
@python/fdars/advisor/aspects/outliers.py
@python/fdars/advisor/aspects/regression.py
@python/fdars/advisor/aspects/classification.py
@python/fdars/advisor/aspects/fpca.py
# Shared eigenvalue helper (reuse for pace_fpca — do NOT hand-roll cumsum)
@python/fdars/advisor/aspects/_utils.py
# Per-aspect primers (extend outliers + regression)
@python/fdars/advisor/_prompts.py
# Dispatch + _supported — confirm UNCHANGED (all four keys already route)
@python/fdars/advisor/__init__.py
# The drift-lock test that must stay green with NO edit
@tests/test_mcp_server.py
# The canonical offline test structure to mirror (check_no_numpy, determinism, _extract_numbers grounding)
@tests/test_advisor_inference.py
# Live v6.0 result-builders to call in the new tests
@tests/test_outliers.py
@tests/test_regression.py
@tests/test_classification.py
@tests/test_pace_fpca.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "tvdmss outliers" diagnostic — builder branch + primer + its offline test, one full slice</name>
  <files>python/fdars/advisor/aspects/outliers.py, python/fdars/advisor/_prompts.py, tests/test_advisor_outliers_v6.py</files>
  <read_first>
    - python/fdars/advisor/aspects/outliers.py (all 121 lines) — the existing four-shape builder. The n_obs inference loop at the top probes ("outliers", "magnitude", "shape", "mei", "mbd"); the existing outliergram block triggers on "mei" in raw and "mbd" in raw. New branches append at the END; all existing blocks stay unchanged.
    - .planning/phases/40-advisor-extension/40-RESEARCH.md "ADV-04: Exact Result-Dict Keys" — tvdmss section: keys are magnitude_outliers (list[int]), shape_outliers (list[int]), tvd (ndarray(n,)), mss (ndarray(n,)). Detection trigger is "tvd" in raw and "mss" in raw (disambiguates from muod which shares shape_outliers/magnitude_outliers but has amplitude_outliers).
    - tests/test_advisor_inference.py — the canonical offline test shape: module-level fixtures, a check_no_numpy recursive walker asserting not isinstance(obj, np.generic), a byte-identical json.dumps(sort_keys=True) determinism assertion, and grounding via _extract_numbers from fdars.advisor.providers._validate.
    - tests/test_outliers.py (TestTvdmss) — how to build a REAL tvdmss result live: from fdars import outliers as outl; outl.tvdmss(data) on a small deterministic array (use np.random.default_rng(0) or a fixed literal array, n >= 3).
  </read_first>
  <behavior>
    - build_diagnostics(real_tvdmss_result, method="outliers")["method"] == "outliers".
    - Output has n_magnitude_outliers (int) == len(raw["magnitude_outliers"]), n_shape_outliers (int) == len(raw["shape_outliers"]), n_obs (int) == len(raw["tvd"]).
    - magnitude_outlier_fraction == n_magnitude_outliers / n_obs (float), shape_outlier_fraction likewise; both native float.
    - tvd_range == [float(min), float(max)] of the tvd array; mss_range likewise; each element a native float.
    - has_tvdmss is True; the existing has_outliergram / has_magnitude_shape flags are unaffected (still present, False for a tvdmss input).
    - Every value native float/int/bool (check_no_numpy passes); two calls give byte-identical json.dumps(sort_keys=True); _check_grounding passes.
  </behavior>
  <action>
    In python/fdars/advisor/aspects/outliers.py, extend _build_outliers_diagnostics. First, extend the n_obs inference loop (currently probing "outliers","magnitude","shape","mei","mbd") to ALSO probe "tvd" and "shape_index" so a tvdmss/muod-shaped result gets a correct n_obs from its score array — append those two keys to the end of the probe tuple so existing precedence is preserved. Then, AFTER the existing outliergram block and BEFORE `return diag`, add a tvdmss branch guarded by `"tvd" in raw and "mss" in raw`. In it: read magnitude_outliers and shape_outliers as list[int] (they are already native Python lists) and take len() for the counts; compute the two fractions as float(count / n_obs) guarding n_obs is a positive int; compute tvd_range and mss_range as [float(np.min(arr)), float(np.max(arr))] via np.asarray(..., dtype=float); set has_tvdmss True. Emit every count via int(), every fraction/range element via float(). Do NOT assign any index list itself to a diagnostic key — only its len() is grounded. Keep the returned dict flat and keep all existing keys present. Update the module docstring's shape list to mention the tvdmss shape.

    In python/fdars/advisor/_prompts.py, extend the _ASPECT_PRIMERS["outliers"] clause with a sentence covering tvdmss: n_magnitude_outliers / n_shape_outliers are the separate magnitude- and shape-direction counts and tvd_range / mss_range are the Total-Variation-Depth and Modified-Shape-Similarity score spans. Wording is at your discretion; grounded strictly in the emitted keys.

    Do NOT place any fenced code block inside this action — identifiers and behavior are described in prose.

    Create tests/test_advisor_outliers_v6.py with (for this task) a TestTvdmss class only. Build a REAL tvdmss result live via `from fdars import outliers as outl; outl.tvdmss(data)` on a deterministic small array (fixed seed or literal). Assert the behavior above: method, the six-plus scalar values and their native types, has_tvdmss is True; add a check_no_numpy recursive walker (copied from test_advisor_inference.py) run on the output; add a determinism assertion (two build_diagnostics calls on the same raw result give equal dicts and byte-identical json.dumps(sort_keys=True)); add a grounding assertion using _extract_numbers from fdars.advisor.providers._validate that n_magnitude_outliers is discoverable in json.dumps(diag). Run tests with PYTHONPATH unset (use the installed .venv package) — the plain `from fdars import ...` import resolves the current compiled extension.

    Commit this task atomically (builder branch + primer edit + tvdmss test together).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor_outliers_v6.py -x -q</automated>
    <automated>.venv/bin/python -c "import numpy as np; from fdars import outliers as o; from fdars.advisor import build_diagnostics; import json; r=o.tvdmss(np.random.default_rng(0).standard_normal((12,25))); d=build_diagnostics(r, method='outliers'); assert d['method']=='outliers' and d['has_tvdmss'] is True and isinstance(d['n_magnitude_outliers'],int) and isinstance(d['magnitude_outlier_fraction'],float); assert d==build_diagnostics(r, method='outliers'); json.dumps(d, sort_keys=True); print('OK')"</automated>
    <automated>.venv/bin/python -m pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -x -q</automated>
  </verify>
  <done>build_diagnostics on a live tvdmss result returns method=="outliers" with grounded tvdmss scalars (all native float/int/bool), has_tvdmss True; determinism + no-numpy + grounding pass; the outliers primer mentions the new tvdmss fields; the MCP drift-lock test stays green with no guard edit; all three files committed together.</done>
  <reversibility rating="reversible">Additive key-presence branch appended after existing blocks; existing shapes untouched; revert one commit to remove.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Remaining outlier detector branches — muod, sequential_transform_outliers, depthgram (with depthgram-before-outliergram ordering)</name>
  <files>python/fdars/advisor/aspects/outliers.py, python/fdars/advisor/_prompts.py, tests/test_advisor_outliers_v6.py</files>
  <read_first>
    - python/fdars/advisor/aspects/outliers.py as extended in Task 1 — you are appending three more branches to the same builder.
    - .planning/phases/40-advisor-extension/40-RESEARCH.md "ADV-04" muod / sequential_transform_outliers / depthgram sections AND "Common Pitfalls" 1, 4, 5. Exact keys: muod has amplitude_outliers/shape_outliers/magnitude_outliers (list[int]) + shape_index/magnitude_index/amplitude_index (ndarray). sequential_transform_outliers has union_outliers (list[int]) + per_transform_outliers (list[dict]) and NO n_obs / score array. depthgram has mbd_mei_d + 5 more *_d/*_t/*_t2 arrays + shape_outliers/magnitude_outliers (list[int]) + mbd + mei (ndarray).
    - The CRITICAL ordering constraint: depthgram carries "mei" and "mbd", so its branch (trigger "mbd_mei_d" in raw) MUST fire BEFORE the existing outliergram block (trigger "mei" in raw and "mbd" in raw). Placement: put the depthgram check ahead of (or short-circuit) the outliergram block, or guard the outliergram block with `and "mbd_mei_d" not in raw`.
    - tests/test_outliers.py (TestMuod, TestSeqTransform, TestDepthgram) — how to build each real result live.
  </read_first>
  <action>
    Append three branches to _build_outliers_diagnostics.

    muod (trigger "amplitude_outliers" in raw, unique to muod): emit n_magnitude_outliers, n_shape_outliers, n_amplitude_outliers (int len of each list); n_obs from len(shape_index); magnitude/shape/amplitude_outlier_fraction (float count/n_obs, guard positive n_obs); shape_index_range, magnitude_index_range, amplitude_index_range as [float(min), float(max)] via np.asarray dtype=float; has_muod True.

    sequential_transform_outliers (trigger "union_outliers" in raw, unique): emit n_union_outliers = int(len(raw["union_outliers"])) and n_transforms = int(len(raw["per_transform_outliers"])); set has_sequential_transform True. Do NOT emit any outlier_fraction — n_obs is NOT recoverable from this result (per_transform_outliers is a list-of-dicts, union_outliers is an index list); emitting a fraction would be fabrication (Pitfall 4).

    depthgram (trigger "mbd_mei_d" in raw, unique — check BEFORE the existing outliergram block): emit n_obs from len(raw["mbd"]); n_shape_outliers, n_magnitude_outliers (int len of the two index lists); shape_outlier_fraction, magnitude_outlier_fraction (float); mbd_range, mei_range as [float(min), float(max)]; has_depthgram True. Ensure the existing has_outliergram block does NOT also fire for a depthgram input — guard it with `and "mbd_mei_d" not in raw` (or place the depthgram branch first and let the outliergram flag reflect the stricter condition). A depthgram input must yield has_depthgram True and has_outliergram False.

    Coerce every count via int(), every fraction/range element via float(); never assign an index list as a diagnostic value. Keep the dict flat with all prior keys present.

    Extend _ASPECT_PRIMERS["outliers"] with sentences for muod (three distinct magnitude/shape/amplitude counts + *_index_range spans), depthgram (combined MBD+MEI depthgram; n_shape_outliers/n_magnitude_outliers), and sequential_transform_outliers (n_union_outliers = union across all transform stages; n_transforms = stages applied). Wording at your discretion, grounded in the emitted keys.

    Add TestMuod, TestSeqTransform, TestDepthgram, and a TestOrdering class to tests/test_advisor_outliers_v6.py. Each builds a REAL result live (outl.muod / outl.sequential_transform_outliers / outl.depthgram on a small deterministic array). TestOrdering asserts a depthgram result yields diag["has_depthgram"] is True and diag["has_outliergram"] is False (proves the ordering guard). For sequential_transform_outliers assert no "outlier_fraction"-style key was fabricated (only n_union_outliers / n_transforms present). Run each result through the check_no_numpy walker and a determinism assertion.

    Commit atomically (three branches + primer edits + their tests).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor_outliers_v6.py -x -q</automated>
    <automated>.venv/bin/python -m pytest "tests/test_advisor_outliers_v6.py::TestOrdering" -x -q</automated>
  </verify>
  <done>build_diagnostics on live muod / sequential_transform_outliers / depthgram results emits the grounded scalars specified (native float/int/bool only); a depthgram input yields has_depthgram True and has_outliergram False; sequential_transform_outliers emits no fabricated fraction; determinism + no-numpy + grounding pass; the outliers primer covers all four detectors; committed atomically.</done>
  <reversibility rating="reversible">Additive branches + one guard tightening on the existing outliergram flag; revert one commit to remove.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Regression branches — functional_glm + concurrent_regression</name>
  <files>python/fdars/advisor/aspects/regression.py, python/fdars/advisor/_prompts.py, tests/test_advisor_regression_v6.py</files>
  <read_first>
    - python/fdars/advisor/aspects/regression.py (all 130 lines) — existing builder. n_obs is inferred from "fitted_values" then "fitted"; the residual block guards res.ndim == 1 (2-D residuals correctly fall through to None); has_fosr fires on a 2-D "fitted" key.
    - .planning/phases/40-advisor-extension/40-RESEARCH.md "ADV-05: Exact Result-Dict Keys" — functional_glm keys include intercept, deviance (float), aic/bic/log_likelihood (float), iterations (int — NOT n_iter; there is NO "converged" key), ncomp (int), family (str), fitted_values (ndarray). concurrent_regression keys: beta_curve (ndarray (p,m)), intercept (m,), fitted (n,m), residuals (n,m 2-D), argvals (m,).
    - "Common Pitfalls" 2 (iterations vs n_iter) and 3 (concurrent residuals are 2-D — must NOT go through the 1-D path).
    - tests/test_regression.py — how to build a REAL functional_glm and concurrent_regression result live (family choice, predictor shapes).
  </read_first>
  <action>
    Extend _build_regression_diagnostics with two branches added BEFORE `return diag`.

    functional_glm (trigger "deviance" in raw — unique among existing regression shapes): emit deviance, aic, bic, log_likelihood as float(raw[...]); iterations as int(raw["iterations"]) (the key is "iterations", NOT "n_iter"); ncomp as int(raw["ncomp"]); family as str(raw["family"]); set has_functional_glm True. There is NO residuals key and NO converged key — do not invent them. n_obs already resolves correctly from the existing "fitted_values" path.

    concurrent_regression (trigger "beta_curve" in raw — unique; fosr uses "beta" not "beta_curve"): compute residual_rms = float(np.sqrt(np.mean(res_2d ** 2))) and residual_max_abs = float(np.max(np.abs(res_2d))) from np.asarray(raw["residuals"]) when it is 2-D and non-empty, else None for both; n_predictors = int(np.asarray(raw["beta_curve"]).shape[0]) (rows = p predictors); set has_concurrent_regression True. n_obs already resolves from the existing "fitted" path. Note has_fosr will also be True for a concurrent_regression input (it has a 2-D "fitted") — this is a harmless documented overlap; has_concurrent_regression is the specific discriminator. Do NOT touch the existing 1-D residual block.

    Coerce every value via float()/int()/str(); keep the dict flat with all prior keys present.

    Extend _ASPECT_PRIMERS["regression"] to add: deviance measures GLM model fit (lower = better); aic/bic for model comparison; iterations is the IRLS iteration count; residual_rms is the concurrent-regression overall fit-quality scalar over the n×m residual grid. Wording at your discretion.

    Create tests/test_advisor_regression_v6.py with TestFunctionalGlm, TestConcurrentRegression, and a TestDeterminism class. Build both results live via fdars.regression. Assert functional_glm emits float deviance/aic/bic/log_likelihood, int iterations/ncomp, str family, has_functional_glm True, and that no "n_iter"/"converged" key is present (guard Pitfall 2). Assert concurrent_regression emits float residual_rms/residual_max_abs, int n_predictors, has_concurrent_regression True, and that the 1-D residual scalars (residual_mean etc.) are None (2-D residuals never went through the 1-D path — Pitfall 3). Run check_no_numpy + determinism (byte-identical json.dumps) + a grounding assertion (_extract_numbers finds deviance) per the inference-test pattern.

    Commit atomically (both branches + primer + tests).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor_regression_v6.py -x -q</automated>
  </verify>
  <done>build_diagnostics on live functional_glm / concurrent_regression results emits the grounded scalars specified (deviance/aic/bic/log_likelihood/iterations/ncomp/family; residual_rms/residual_max_abs/n_predictors) as native types; no n_iter/converged fabricated; 2-D residuals never hit the 1-D path; determinism + no-numpy + grounding pass; regression primer updated; committed atomically.</done>
  <reversibility rating="reversible">Additive branches before return; existing paths untouched; revert one commit.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Group B branches — elastic_multinomial (classification) + pace_fpca (fpca)</name>
  <files>python/fdars/advisor/aspects/classification.py, python/fdars/advisor/aspects/fpca.py, python/fdars/advisor/_prompts.py, tests/test_advisor_group_b.py</files>
  <read_first>
    - python/fdars/advisor/aspects/classification.py (all 133 lines) — existing builder; n_classes is currently a CALLER-supplied parameter (line 99); point-estimate results have "accuracy", fclassif_cv has "error_rate". elastic_multinomial's raw dict additionally carries "n_classes".
    - python/fdars/advisor/aspects/fpca.py (all 88 lines) — existing builder detects "singular_values" (NOT "eigenvalues"); pace_fpca's key is "eigenvalues" directly, so detection is unambiguous.
    - python/fdars/advisor/aspects/_utils.py — _eigenvalues_to_variance_cumulative accepts ALREADY-SCALED eigenvalues and returns a list[float] cumulative; pace_fpca eigenvalues are already scaled, so pass them directly (do NOT apply sv**2/(n-1)).
    - .planning/phases/40-advisor-extension/40-RESEARCH.md "Group B Feasibility Decision" — elastic_multinomial keys: n_classes (int), classes, train_probabilities (n,K), predicted_classes, train_accuracy (float). Trigger "train_accuracy" in raw (unique; existing results use "accuracy"/"error_rate", never "train_accuracy"). pace_fpca keys: mean, eigenvalues (ncomp,), eigenfunctions, scores, fitted/_lower/_upper, argvals, sigma2 (float), ncomp (int). Trigger "eigenvalues" in raw.
    - tests/test_classification.py + tests/test_pace_fpca.py — how to build each real result live.
  </read_first>
  <action>
    Extend _build_classification_diagnostics: add a branch guarded by "train_accuracy" in raw (unique to elastic_multinomial). In it emit n_classes = int(raw["n_classes"]) (override the caller-supplied value with the fdars-computed count), train_accuracy = float(raw["train_accuracy"]), train_error_rate = float(1.0 - raw["train_accuracy"]), has_elastic_multinomial = True. Do this WITHOUT breaking the existing point-estimate / CV paths (elastic_multinomial has no "accuracy" or "error_rate" key, so those blocks emit None as today; only add the new branch and the new keys). Keep the dict flat.

    Extend _build_fpca_diagnostics: add a branch guarded by "eigenvalues" in raw (pace_fpca; the standard FPCA path keys on "singular_values" and is unaffected). Emit ncomp = int(raw["ncomp"]), sigma2 = float(raw["sigma2"]), variance_explained_cumulative = _eigenvalues_to_variance_cumulative(raw["eigenvalues"]) (list[float]; pass the already-scaled eigenvalues directly — do NOT re-scale), variance_explained_first = float(variance_explained_cumulative[0]) when the list is non-empty else None, has_pace_fpca = True. Keep the existing singular_values path and all its keys intact.

    Extend _ASPECT_PRIMERS["classification"] with a sentence on elastic_multinomial train_accuracy (in-sample fit, distinct from CV accuracy). Optionally extend _ASPECT_PRIMERS for the fpca aspect if such a key exists; if there is no "fpca" primer entry, skip the fpca primer edit (do not invent a new primer key). Wording at your discretion.

    Create tests/test_advisor_group_b.py with TestElasticMultinomial, TestPaceFpca, and TestDeterminism. Build both results live via fdars.classification.elastic_multinomial and fdars.pace_fpca.pace_fpca. Assert elastic_multinomial emits int n_classes (from raw, matching the fdars-computed count), float train_accuracy in [0,1], float train_error_rate == 1 - train_accuracy, has_elastic_multinomial True. Assert pace_fpca emits int ncomp, float sigma2, a variance_explained_cumulative list[float] whose last element is ~1.0 (when eigenvalue sum > 0), float variance_explained_first, has_pace_fpca True; and that the standard-FPCA keys (singular_values path) are None for a pace_fpca input. Run check_no_numpy + determinism + grounding (_extract_numbers finds train_accuracy / sigma2) per the inference-test pattern.

    Commit atomically (both builder branches + any primer edit + tests).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor_group_b.py -x -q</automated>
  </verify>
  <done>build_diagnostics on live elastic_multinomial (method="classification") and pace_fpca (method="fpca") results emits the grounded scalars specified as native types; the existing point-estimate/CV and singular_values paths are unaffected; determinism + no-numpy + grounding pass; committed atomically.</done>
  <reversibility rating="reversible">Additive key-presence branches; existing detection paths untouched; revert one commit.</reversibility>
</task>

<task type="auto">
  <name>Task 5: Full-suite gate + no-op guard-sync + ITP-deferral confirmation</name>
  <files>tests/test_advisor_outliers_v6.py, tests/test_advisor_regression_v6.py, tests/test_advisor_group_b.py</files>
  <read_first>
    - tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported — the drift-lock that must be green with NO edit to server.py / _runner.py / advisor _supported (no new aspect key was added this phase).
    - python/fdars/mcp/server.py (_DIAGNOSTICS_METHODS) and python/fdars/advisor/__init__.py (_supported) — confirm both are UNCHANGED by git diff (this phase edits only aspect builders, _prompts.py, and test files).
    - .planning/phases/40-advisor-extension/40-VALIDATION.md — the phase gate: full suite 0 failed; NO cargo build/fmt/clippy needed (no src/*.rs touched).
  </read_first>
  <action>
    This is the phase gate. Run the full pytest suite and confirm 0 failed. Confirm the MCP guard-sync test stays green and that git diff shows NO change to python/fdars/mcp/server.py, python/fdars/mcp/_runner.py, or the _supported set in python/fdars/advisor/__init__.py — the MCP guard-sync is a strict no-op because no new aspect key was added (all four extended aspects were already registered). Confirm NO ITP advisor branch was added anywhere (ITP is deferred: vector adjusted_pvalues, no grounded scalar). Confirm NO src/*.rs file was touched (pure-Python phase) — if any was, run cargo fmt --check, cargo clippy -D warnings, and cargo build as an added gate; otherwise skip Rust checks.

    If the full suite reveals any regression in an existing advisor or MCP test, fix it in the responsible builder (do not weaken the drift-lock test). Add no new production code in this task beyond fixes surfaced by the gate.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -x -q</automated>
    <automated>.venv/bin/python -m pytest tests/ -q</automated>
    <automated>git diff --name-only HEAD~4 -- python/fdars/mcp/server.py python/fdars/mcp/_runner.py | grep -q . && echo "GUARD FILES CHANGED — REVIEW" || echo "guard files unchanged OK"</automated>
  </verify>
  <done>Full suite exits 0 failed; test_diagnostics_methods_match_advisor_supported green; MCP guard files and the advisor _supported set are unchanged (no-op guard-sync confirmed); no ITP branch added; no src/*.rs touched.</done>
  <reversibility rating="reversible">Verification-only gate; no production change beyond regression fixes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> build_diagnostics(method=...) | caller-supplied v6.0 result dict is structurally untrusted (keys vary by detector/model); validated by key-presence guards |
| advisor `_supported` <-> MCP `_DIAGNOSTICS_METHODS` | two independently-maintained guard sets that MUST stay identical; drift-locked by a test — this phase must NOT edit either |
| builder diagnostics -> LLM advice grounding | every emitted number must be citable by `_check_grounding`; a non-grounded/fabricated scalar breaks the invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | grounding invariant (fabricated scalar) | high | mitigate | Every diagnostic is a reduction (len/min/max/ratio/echo) of an fdars-computed value; sequential_transform emits NO outlier_fraction (n_obs unrecoverable); grounding tests via _extract_numbers on all six shapes (Tasks 1-4) |
| T-40-02 | Tampering | advisor/MCP guard drift | high | mitigate | No new aspect key added — guard-sync is a strict no-op; Task 5 asserts guard files + _supported unchanged and the drift-lock test stays green |
| T-40-03 | Information disclosure / correctness | depthgram misrouted into outliergram branch (shared mbd/mei) | medium | mitigate | depthgram trigger "mbd_mei_d" checked before the outliergram block; TestOrdering asserts has_depthgram True + has_outliergram False (Task 2) |
| T-40-04 | Tampering | 2-D concurrent_regression residuals through the 1-D residual path | medium | mitigate | Existing res.ndim==1 guard leaves 1-D scalars None; a dedicated 2-D branch computes residual_rms; test asserts residual_mean is None (Task 3) |
| T-40-05 | Denial of service | numpy scalar / non-serialisable leak | low | mitigate | All values cast via float()/int()/str(); check_no_numpy walker + byte-identical json.dumps determinism tests on every new branch |

No package installs in this phase (pure-Python, no new deps) — package-legitimacy gate not applicable. No `src/*.rs` change — no cargo build/fmt/clippy required.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor_outliers_v6.py tests/test_advisor_regression_v6.py tests/test_advisor_group_b.py -x -q` passes (all six new-capability branches).
- `.venv/bin/python -m pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -x -q` exits 0 with NO guard edit.
- `.venv/bin/python -m pytest tests/ -q` — full suite 0 failed (pure-Python; no maturin rebuild).
- `git diff` shows python/fdars/mcp/server.py, python/fdars/mcp/_runner.py, and the advisor `_supported` set UNCHANGED; no `src/*.rs` touched; no ITP advisor branch added.
</verification>

<success_criteria>
1. ADV-04: the existing `outliers` aspect summarizes tvdmss/muod/sequential_transform_outliers/depthgram as grounded scalar diagnostics (counts, fractions where n_obs is recoverable, score ranges — never raw index lists or numpy aggregates), closing the v5.0 Phase-34 deferral; no new aspect key; dispatch detects the new result-dict keys.
2. ADV-05: the existing `regression` aspect surfaces grounded diagnostics for functional_glm (deviance/aic/bic/log_likelihood/iterations/ncomp/family) and concurrent_regression (residual_rms/residual_max_abs/n_predictors); Group B included — elastic_multinomial (train_accuracy) via `classification` and pace_fpca (variance_explained_cumulative/sigma2/ncomp) via `fpca`; ITP deferred.
3. No change to `_DIAGNOSTICS_METHODS`/`_RUNNABLE_METHODS`/`_supported`; `test_diagnostics_methods_match_advisor_supported` stays green with no guard edit; offline determinism preserved (no numpy scalars, byte-identical `json.dumps`); the LLM only interprets/cites diagnostic values.
4. Full suite 0 failed.
</success_criteria>

<output>
Create `.planning/phases/40-advisor-extension/40-01-SUMMARY.md` when done.
</output>
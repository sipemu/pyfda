---
phase: 31-group-a-fdars-inference-bindings
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/inference_mod.rs
  - tests/test_inference.py
autonomous: true
requirements: [INFER-04, INFER-05]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "fdars.inference.mean_scb(data, argvals, bandwidth, nb=200, confidence=0.95, multiplier='gaussian') returns a dict {lower, upper, center, half_width}, each a 1-D ndarray of length m — INFER-04"
    - "multiplier is selected by string; an unknown string raises ValueError (non-exhaustive fallback arm) — INFER-04"
    - "SCB band arrays pass a shape/finite round-trip assertion: each is shape (m,) and all-finite — INFER-04"
    - "fdars.inference.scb_two_sample_test(data_a, data_b, argvals, bandwidth, nb=200, confidence=0.95, multiplier='gaussian') returns a TestResult dict {statistic, p_value, n_perm} — INFER-05"
    - "Degenerate SCB params (nb=0, confidence outside (0,1), unknown multiplier) raise ValueError — INFER-04, INFER-05"
  artifacts:
    - src/inference_mod.rs
    - tests/test_inference.py
  key_links:
    - "String→MultiplierDistribution match with _ => PyValueError fallback (non_exhaustive enum in fdars_core::tolerance)"
    - "mean_scb returns ToleranceBand (Vec<f64> fields) → vec_to_numpy1d per field (NOT fdmatrix_to_numpy2d)"
---

<objective>
Bind the two Degras simultaneous-confidence-band functions into `fdars.inference`: `mean_scb` (single-group mean band → `{lower, upper, center, half_width}` dict) and `scb_two_sample_test` (mean-difference band → `TestResult` dict). Both take a `multiplier` string dispatched to the non-exhaustive `MultiplierDistribution` enum with a `ValueError` fallback.

Purpose: Deliver simultaneous (not pointwise) confidence-band inference — the statistically-correct band for a functional mean — plus the geometrically-interpretable two-sample SCB test.
Output: `mean_scb` + `scb_two_sample_test` bindings in `src/inference_mod.rs` and tests in `tests/test_inference.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-group-a-fdars-inference-bindings/31-CONTEXT.md
@.planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md
@.planning/research/PITFALLS.md
@src/inference_mod.rs
@src/convert.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind mean_scb (Degras SCB for the mean → ToleranceBand dict)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (mean_scb signature; ToleranceBand fields lower/upper/center/half_width all Vec&lt;f64&gt;; MultiplierDistribution variants Gaussian/Rademacher in fdars_core::tolerance, non_exhaustive)
    - src/inference_mod.rs (existing binding conventions + register() fn from 31-01)
    - src/convert.rs (vec_to_numpy1d, numpy2d_to_fdmatrix, numpy1d_to_vec, to_pyresult — note ToleranceBand fields are Vec&lt;f64&gt;, so use vec_to_numpy1d per field, NOT fdmatrix_to_numpy2d)
    - .planning/research/PITFALLS.md (Pitfall 6: mean_scb returns ToleranceBand not TestResult; Pitfall 15: nb/confidence validation)
  </read_first>
  <behavior>
    - mean_scb returns a dict with exactly the keys lower, upper, center, half_width
    - each returned band array has shape (m,) matching the argvals length, and all values are finite
    - multiplier="gaussian" and multiplier="rademacher" both succeed
    - an unknown multiplier string raises ValueError
    - nb=0 raises ValueError; confidence outside (0,1) raises ValueError
  </behavior>
  <action>
Add a private string→MultiplierDistribution dispatch helper in src/inference_mod.rs: match the multiplier string, "gaussian" to fdars_core::tolerance::MultiplierDistribution::Gaussian, "rademacher" to fdars_core::tolerance::MultiplierDistribution::Rademacher, and a wildcard `_` arm returning a PyValueError describing the accepted values (the enum is non_exhaustive — the fallback arm is mandatory). Bind mean_scb as a #[pyfunction] with #[pyo3(signature = (data, argvals, bandwidth, nb=200, confidence=0.95, multiplier="gaussian"))] taking data as PyReadonlyArray2, argvals as PyReadonlyArray1, bandwidth: f64, nb: usize, confidence: f64, multiplier: &str. Convert data via numpy2d_to_fdmatrix and argvals via numpy1d_to_vec, resolve the multiplier via the dispatch helper, route fdars_core::inference::mean_scb through to_pyresult, then access the returned ToleranceBand fields individually (lower, upper, center, half_width — non_exhaustive struct, no struct literal) and build a PyDict setting each via vec_to_numpy1d. Document the returned dict keys, that each is a length-m 1-D array, and that ValueError is raised for nb=0, confidence outside the open interval (0,1), or an unknown multiplier — rely on fdars-core's own scb_mean_degras validation (routed via to_pyresult) plus the multiplier fallback arm; do not add redundant Rust-side guards that fdars-core already enforces. Register mean_scb in the register() fn. In tests/test_inference.py add a fixture using a single group with enough curves (load_canadian_weather(variable="temperature", return_fdata=False) → day, X, meta; use a small column subset e.g. X[:, ::30] with the matching day[::30] grid to keep m small and the test fast) and tests asserting: the four dict keys; each band array has shape (m,) and passes np.all(np.isfinite(...)); multiplier="rademacher" succeeds; pytest.raises(ValueError) for an unknown multiplier string, for nb=0, and for confidence=1.5.
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -c "import numpy as np, fdars.inference as inf; import fdars.datasets as d; day,X,_=d.load_canadian_weather(return_fdata=False); g=day[::30]; r=inf.mean_scb(X[:, ::30], g, 20.0, nb=50); print(sorted(r), r['lower'].shape)" &amp;&amp; python -m pytest tests/test_inference.py -x -q -k "mean_scb" &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>mean_scb returns {lower, upper, center, half_width} 1-D arrays of length m (finite); string multiplier dispatch with ValueError fallback works; nb/confidence validation raises ValueError; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind scb_two_sample_test (SCB test for the mean difference → TestResult dict)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (scb_two_sample_test signature; returns TestResult; statistic is max standardized excursion, p_value 0.0/1.0, n_perm 0)
    - src/inference_mod.rs (the TestResult→PyDict helper from 31-01 and the multiplier dispatch helper from Task 1)
  </read_first>
  <behavior>
    - scb_two_sample_test returns a dict with keys statistic, p_value, n_perm
    - result["n_perm"] equals 0 (SCB path, not permutation)
    - multiplier string dispatch reuses the same helper; unknown multiplier raises ValueError
    - mismatched grids / degenerate params raise ValueError
  </behavior>
  <action>
Add scb_two_sample_test to src/inference_mod.rs with #[pyo3(signature = (data_a, data_b, argvals, bandwidth, nb=200, confidence=0.95, multiplier="gaussian"))]. Convert both matrices via numpy2d_to_fdmatrix, argvals via numpy1d_to_vec, resolve multiplier via the shared dispatch helper from Task 1, route fdars_core::inference::scb_two_sample_test through to_pyresult, and build the result via the TestResult→PyDict helper (reused from 31-01). Document that this forms the paired mean-difference band and returns a TestResult where n_perm is 0, statistic is the maximum standardized excursion of the difference band from zero, and p_value is a conservative 0.0 (reject) / 1.0 (fail to reject) encoding at the requested confidence. Register it. In tests/test_inference.py add a test on the growth-subset two-group fixture (boys = X[:10], girls = X[39:49] on the shared age grid) asserting the three dict keys, that result["n_perm"] == 0, and a pytest.raises(ValueError) for an unknown multiplier string.
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -m pytest tests/test_inference.py -x -q &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>scb_two_sample_test returns {statistic, p_value, n_perm} with n_perm==0; multiplier fallback raises ValueError; full tests/test_inference.py green; clippy -D warnings clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 binding | multiplier string + numeric SCB params cross into Rust; string must map to a valid enum variant or error |
| PyO3 binding → fdars-core tolerance/inference | ToleranceBand/TestResult returned by upstream; Result must not be unwrapped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Denial of Service | mean_scb/scb_two_sample_test with nb=0, confidence outside (0,1), or mismatched grids | low | mitigate | Route through to_pyresult so fdars-core validation surfaces as ValueError; no .unwrap(); pytest.raises tests for nb=0 and confidence=1.5 |
| T-31-05 | Tampering | Unknown multiplier string silently mapped to a default variant | medium | mitigate | Explicit match with _ => PyValueError fallback arm (non_exhaustive enum); pytest.raises(ValueError) test for an unknown string |
| T-31-06 | Information Disclosure | ToleranceBand Vec fields transposed/wrong-shape crossing the boundary | low | mitigate | vec_to_numpy1d per field (fields are Vec<f64>, not FdMatrix); shape/finite round-trip assertion asserts (m,) and finite |
</threat_model>

<verification>
- `maturin develop` builds clean (rustfmt + clippy -D warnings).
- `mean_scb` returns `{lower, upper, center, half_width}`; each length-m and finite.
- `scb_two_sample_test` returns `{statistic, p_value, n_perm}` with n_perm==0.
- Unknown multiplier, nb=0, confidence outside (0,1) raise ValueError.
- `python -m pytest tests/test_inference.py -q` exits 0.
</verification>

<success_criteria>
- `mean_scb` and `scb_two_sample_test` importable from `fdars.inference`.
- `mean_scb` → `{lower, upper, center, half_width}` 1-D band arrays with a shape/finite round-trip assertion.
- `multiplier` selected by string with a `ValueError` fallback for unknown values.
- SCB parameter-validation degenerate cases raise `ValueError`; no `.unwrap()`; clippy `-D warnings` clean.
</success_criteria>

<output>
Create `.planning/phases/31-group-a-fdars-inference-bindings/31-02-SUMMARY.md` when done
</output>

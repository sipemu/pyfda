---
phase: 31-group-a-fdars-inference-bindings
plan: 03
type: execute
wave: 3
depends_on: [31-02]
files_modified:
  - src/inference_mod.rs
  - tests/test_inference.py
autonomous: true
requirements: [INFER-06, INFER-07, INFER-08]
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "fdars.inference.flm_f_test(data, response, n_comp=5) re-fits fregre_lm internally (no handle) and returns a TestResult dict {statistic, p_value, n_perm} — INFER-06"
    - "fdars.inference.flm_gof_test(data, response, n_comp=5) is bound symmetrically and returns the same dict shape — INFER-07"
    - "fdars.inference.oneway_anova_vstat(data, groups, argvals) accepts a Python int array/list of 0-indexed group labels and returns {statistic, p_value, n_perm} with n_perm==0 — INFER-08"
    - "Degenerate FLM fits (ncomp too large for n) and degenerate ANOVA groups (<2 distinct, length mismatch) raise ValueError — INFER-06, INFER-07, INFER-08"
  artifacts:
    - src/inference_mod.rs
    - tests/test_inference.py
  key_links:
    - "flm_f_test/flm_gof_test re-fit via fdars_core::scalar_on_function::fregre_lm(&mat, &resp, None, n_comp) then pass &FregreLmResult to the upstream test — FregreLmResult never crosses the boundary"
    - "oneway_anova_vstat groups: PyReadonlyArray1<i64> → numpy1d_to_usize_vec → Vec<usize>"
---

<objective>
Complete the `fdars.inference` surface: the two FLM post-hoc inference tests (`flm_f_test`, `flm_gof_test`) that re-fit `fregre_lm` internally from raw data/response/n_comp (no persistent handle — `FregreLmResult` never crosses the boundary), and the asymptotic one-way functional ANOVA V-statistic (`oneway_anova_vstat`) accepting a 0-indexed integer group-label array.

Purpose: Deliver first-class inference for the existing regression surface (overall-significance F-test + Ramsey-RESET goodness-of-fit) and a fast asymptotic grouped-mean test complementing the existing permutation `fanova`.
Output: `flm_f_test` + `flm_gof_test` + `oneway_anova_vstat` bindings in `src/inference_mod.rs` and tests in `tests/test_inference.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-group-a-fdars-inference-bindings/31-CONTEXT.md
@.planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md
@.planning/research/PITFALLS.md
@src/inference_mod.rs
@src/convert.rs
@src/regression_mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind flm_f_test (FLM overall-significance F-test, re-fit internally)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (flm_f_test takes &FregreLmResult; fregre_lm(data, y, scalar_covariates: Option, ncomp) — NO argvals; the p==0 and n<=p+1 degenerate-fit errors)
    - src/regression_mod.rs lines 111-132 (existing fregre_lm binding: param names data/response/n_comp, numpy1d_to_vec on response, fregre_lm(&mat, &resp, None, n_comp) call)
    - src/inference_mod.rs (TestResult→PyDict helper from 31-01)
    - .planning/research/PITFALLS.md (Pitfall 4: FLM takes &FregreLmResult; re-fit internally, FregreLmResult is in fdars_core::scalar_on_function and is non_exhaustive)
  </read_first>
  <behavior>
    - flm_f_test(data, response, n_comp=5) returns a dict with keys statistic, p_value, n_perm
    - the wrapper re-fits fregre_lm internally — the caller passes NO handle
    - a degenerate fit (n_comp large enough that n <= n_comp+1) raises ValueError
  </behavior>
  <action>
Add flm_f_test to src/inference_mod.rs with #[pyo3(signature = (data, response, n_comp=5))] taking data as PyReadonlyArray2 and response as PyReadonlyArray1 (mirroring the existing fdars.regression fregre_lm param names/order per the locked decision — note fregre_lm takes no argvals). Convert data via numpy2d_to_fdmatrix and response via numpy1d_to_vec, re-fit by calling fdars_core::scalar_on_function::fregre_lm(&mat, &resp, None, n_comp) routed through to_pyresult to obtain a FregreLmResult (which stays in Rust — never crosses the boundary), then pass &fit to fdars_core::inference::flm_f_test routed through to_pyresult, and build the result via the TestResult→PyDict helper. Document that the model is re-fitted internally (no persistent handle), that n_comp is the FPC component count (default 5), and that ValueError is raised for a degenerate fit (too few observations for the chosen n_comp). Register it. In tests/test_inference.py add a fixture using Tecator (load_tecator(return_fdata=False) → wavelength, X, meta with meta fat column, or the dataset's .y fat response) subset to a small number of rows/columns for speed, and a test asserting flm_f_test returns the three dict keys, plus a pytest.raises(ValueError) for an n_comp large enough to make the fit degenerate (n_comp >= n_rows-1 on a small subset).
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -m pytest tests/test_inference.py -x -q -k "flm_f" &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>flm_f_test re-fits fregre_lm internally and returns {statistic, p_value, n_perm}; degenerate-fit ValueError test passes; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind flm_gof_test (Ramsey-RESET goodness-of-fit test, symmetric with flm_f_test)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (flm_gof_test takes &FregreLmResult; needs n > Q+1 residual df where Q=3)
    - src/inference_mod.rs (the flm_f_test binding from Task 1 to mirror exactly)
  </read_first>
  <behavior>
    - flm_gof_test(data, response, n_comp=5) returns a dict with keys statistic, p_value, n_perm
    - bound symmetrically with flm_f_test (same signature, same re-fit path)
    - a degenerate fit raises ValueError
  </behavior>
  <action>
Add flm_gof_test to src/inference_mod.rs mirroring flm_f_test exactly: same #[pyo3(signature = (data, response, n_comp=5))], same numpy2d_to_fdmatrix / numpy1d_to_vec conversions, same internal fregre_lm re-fit routed through to_pyresult, then pass &fit to fdars_core::inference::flm_gof_test routed through to_pyresult, and build via the TestResult→PyDict helper. Document that this is a residual-based Ramsey-RESET lack-of-fit test (a significant result indicates the linear model misses a nonlinear effect of the functional predictor) and that the model is re-fitted internally. Register it. In tests/test_inference.py add a test on the same Tecator subset fixture asserting flm_gof_test returns the three dict keys.
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -m pytest tests/test_inference.py -x -q -k "flm" &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>flm_gof_test importable, re-fits internally, returns {statistic, p_value, n_perm}; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind oneway_anova_vstat (asymptotic one-way functional ANOVA V-statistic)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (oneway_anova_vstat(data, groups: &[usize], argvals) signature; validation groups.len()==n, >=2 distinct groups, n>=3; 0-indexed documented convention; n_perm==0)
    - src/convert.rs (numpy1d_to_usize_vec converts PyReadonlyArray1<i64> → Vec<usize>)
    - src/inference_mod.rs (TestResult→PyDict helper)
    - .planning/research/PITFALLS.md (Pitfall 9: groups must be i64 not f64; document 0-indexed)
  </read_first>
  <behavior>
    - oneway_anova_vstat(data, groups, argvals) returns a dict with keys statistic, p_value, n_perm
    - result["n_perm"] equals 0 (asymptotic)
    - groups accepts a 1-D int array (i64); 0-indexed labels
    - fewer than 2 distinct groups, or a groups length not matching n_rows, raises ValueError
  </behavior>
  <action>
Add oneway_anova_vstat to src/inference_mod.rs with #[pyo3(signature = (data, groups, argvals))] taking data as PyReadonlyArray2, groups as PyReadonlyArray1&lt;i64&gt;, argvals as PyReadonlyArray1&lt;f64&gt;. Convert data via numpy2d_to_fdmatrix, groups via numpy1d_to_usize_vec (the established i64→Vec&lt;usize&gt; converter — the Python-facing dtype MUST be i64, not f64), argvals via numpy1d_to_vec, route fdars_core::inference::oneway_anova_vstat through to_pyresult (fdars-core validates groups.len()==n, >=2 distinct groups, and n>=3), and build via the TestResult→PyDict helper. Document that groups are 0-indexed integer labels, that n_perm is always 0 (asymptotic), and that it complements the existing permutation fanova (asymptotic/fast vs permutation/exact); note that ValueError is raised for a groups length mismatch or fewer than 2 distinct groups. Register it. In tests/test_inference.py add a fixture using Canadian Weather with a 0-indexed region-label vector (load_canadian_weather(return_fdata=False) → day, X, meta; map the meta region column to 0-indexed integer codes via np.unique(regions, return_inverse=True)[1], subset columns e.g. X[:, ::30] with day[::30] for speed) and a test asserting the three dict keys, that result["n_perm"] == 0, and a pytest.raises(ValueError) for a single-group label vector (all zeros — fewer than 2 distinct groups).
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -m pytest tests/test_inference.py -x -q &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>oneway_anova_vstat returns {statistic, p_value, n_perm} with n_perm==0 from a 0-indexed i64 group array; degenerate-group ValueError test passes; full tests/test_inference.py green; clippy -D warnings clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 binding | Raw FLM data/response + integer group labels cross into Rust; dtype and label validity must be enforced |
| PyO3 binding → fdars-core scalar_on_function/inference | fregre_lm re-fit + FregreLmResult stays in Rust; upstream Results must not be unwrapped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Denial of Service | flm_f_test/flm_gof_test degenerate fit (n <= n_comp+1) or oneway_anova_vstat length mismatch / <2 groups | low | mitigate | Route fregre_lm and the tests through to_pyresult so fdars-core InvalidParameter/InvalidDimension surface as ValueError; no .unwrap(); pytest.raises(ValueError) tests |
| T-31-08 | Tampering | groups passed as float silently truncated to wrong usize labels | medium | mitigate | Python-facing groups dtype is i64 via PyReadonlyArray1<i64> + numpy1d_to_usize_vec; float arrays are rejected by the type binding; document 0-indexed |
| T-31-09 | Elevation of Privilege | Exposing FregreLmResult as a Python handle (rejected design) | low | accept | Re-fit internally per locked decision; FregreLmResult never crosses the boundary — no handle registry, matches predict_fregre_lm precedent |
</threat_model>

<verification>
- `maturin develop` builds clean (rustfmt + clippy -D warnings).
- `flm_f_test`, `flm_gof_test` re-fit `fregre_lm` internally and return `{statistic, p_value, n_perm}`.
- `oneway_anova_vstat` returns `{statistic, p_value, n_perm}` with n_perm==0 from a 0-indexed i64 group array.
- Degenerate FLM fits and degenerate group vectors raise ValueError.
- `python -m pytest tests/test_inference.py -q` exits 0.
</verification>

<success_criteria>
- `flm_f_test` and `flm_gof_test` importable from `fdars.inference`, re-fitting `fregre_lm` internally (no handle), returning `TestResult` dicts.
- `oneway_anova_vstat` importable, accepting a 0-indexed integer group-label array, returning `{statistic, p_value, n_perm}` with n_perm==0.
- All degenerate inputs raise `ValueError` (no `.unwrap()`); clippy `-D warnings` clean; full `tests/test_inference.py` green.
</success_criteria>

<output>
Create `.planning/phases/31-group-a-fdars-inference-bindings/31-03-SUMMARY.md` when done
</output>

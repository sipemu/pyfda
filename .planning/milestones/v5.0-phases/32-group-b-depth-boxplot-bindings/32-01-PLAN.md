---
phase: 32-group-b-depth-boxplot-bindings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/depth_mod.rs
  - tests/test_depth.py
autonomous: true
requirements: [DEPTH-01, DEPTH-02]
estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "fdars.depth.functional_depth(data, method='fraiman_muniz'|'band'|'modified_band'|'random_projection', **kwargs) returns ndarray of shape (n,) (DEPTH-01)"
    - "Unknown method string raises ValueError via #[non_exhaustive] wildcard fallback (DEPTH-01)"
    - "functional_depth(data, method='fraiman_muniz', scale=True) equals the existing fraiman_muniz_1d(data, data, scale=True) self-depth within tolerance (DEPTH-01)"
    - "method='random_projection' with an explicit seed (or seed=None) is byte-identical across two calls (DEPTH-01)"
    - "fdars.depth.functional_boxplot(data, method=..., factor=1.5, **kwargs) returns a 7-key dict {median, central_lower, central_upper, whisker_lower, whisker_upper, outliers, depths} (DEPTH-02)"
    - "The five band fields are 1-D ndarrays of length m (== number of evaluation points); depths is a 1-D ndarray of length n; outliers is a Python list of ints with 0-based row indices in [0, n) (DEPTH-02)"
    - "Degenerate inputs (empty data, <2 curves for band/boxplot, nproj=0, negative factor, unknown method) raise ValueError with pytest.raises coverage (DEPTH-01, DEPTH-02)"
  artifacts:
    - "src/depth_mod.rs (extended with depth_method_from_str, functional_depth, functional_boxplot)"
    - "tests/test_depth.py"
  key_links:
    - "depth_method_from_str: Python (method:str, scale, nproj, seed) -> fdars_core::depth::dispatch::DepthMethod with _ => PyValueError"
    - "functional_boxplot band fields (Vec<f64>, length m) -> vec_to_numpy1d; outliers (Vec<usize>) -> Python list of ints; depths (Vec<f64>, length n) -> vec_to_numpy1d"
    - "fdars.depth submodule already registered in lib.rs (register_submodule! m,'depth') — new #[pyfunction]s wired into depth_mod::register()"
---

<objective>
Extend the already-registered `fdars.depth` submodule with two fdars-core 0.20 functions — `functional_depth` (unified self-depth dispatcher) and `functional_boxplot` (López-Pintado–Romo numeric functional boxplot) — plus a shared `depth_method_from_str` string→`DepthMethod` helper. Covers DEPTH-01 and DEPTH-02.

Purpose: Make functional self-depth first-class (a single string-dispatched entry point instead of calling `fraiman_muniz_1d`/`band_1d`/etc. directly), and expose the canonical functional boxplot's numeric outputs (median / 50% central region / whiskers / flagged outliers / per-curve depths) for downstream plotting (PLOT-01, deferred) and advisor diagnostics (Phase 34, optional).

Output: `src/depth_mod.rs` extended with `depth_method_from_str` + `functional_depth` + `functional_boxplot`; `tests/test_depth.py` covering shape, cross-check vs `fraiman_muniz_1d`, seed determinism, the full boxplot dict contract, a multi-curve round-trip shape/finite test, and degenerate-input `ValueError` cases.

## Artifacts this phase produces

- `fdars.depth.functional_depth(data, method="fraiman_muniz", **kwargs)` — module-level function returning `ndarray (n,)`.
- `fdars.depth.functional_boxplot(data, method=..., factor=1.5, **kwargs)` — module-level function returning a 7-key dict.
- `depth_method_from_str(...)` — private Rust helper in `src/depth_mod.rs` shared by both functions.

No new submodule registration (the `depth` submodule is already registered in `src/lib.rs`). No Fdata convenience methods (deferred). No plotting (deferred → PLOT-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-group-b-depth-boxplot-bindings/32-CONTEXT.md

# Authoritative 0.20 signature reference (read before coding):
@/home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/depth/dispatch.rs

# The submodule to EXTEND and the conversion helpers:
@src/depth_mod.rs
@src/convert.rs

# Phase 31 analog to MIRROR (string-dispatch helper + seed=None→0 idiom + PyDict result mapping):
@src/inference_mod.rs

# Test conventions to follow:
@tests/test_inference.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: functional_depth end-to-end (string dispatch helper + binding + tests)</name>
  <files>src/depth_mod.rs, tests/test_depth.py</files>
  <read_first>
    - `/home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/depth/dispatch.rs` — the authoritative source. `DepthMethod` is `#[non_exhaustive]` with variants `FraimanMuniz { scale: bool }`, `Band`, `ModifiedBand`, `RandomProjection { nproj: usize, seed: u64 }`. `functional_depth(data: &FdMatrix, method: DepthMethod) -> Result<Vec<f64>, FdarError>` returns one depth per curve (length `n = data.nrows()`).
    - `src/inference_mod.rs` — mirror the `multiplier_from_str`-style string-dispatch pattern and the `seed.unwrap_or(0)` idiom (lines ~97). Note the module-doc comment style, `use crate::convert::*;`, and `to_pyresult(...)?` usage.
    - `src/depth_mod.rs` — existing `fraiman_muniz_1d` (the cross-check target) and the existing `register()` function that must gain the two new `add_function` lines.
    - `src/convert.rs` — `numpy2d_to_fdmatrix`, `vec_to_numpy1d`, `to_pyresult`.
    - `tests/test_inference.py` — fixture/import-path test style (module-scope fixtures, `pytest.raises`, both `import fdars.inference` and `from fdars.inference import ...` styles).
  </read_first>
  <behavior>
    - functional_depth returns ndarray shape (n,) for each of the four methods on a Canadian Weather temperature subset.
    - functional_depth(data, method="fraiman_muniz", scale=True) equals the existing fraiman_muniz_1d(data, data, scale=True) within numpy.allclose tolerance (self-depth cross-check).
    - functional_depth(data, method="random_projection", nproj=<small>, seed=7) is byte-identical (np.array_equal) across two calls; seed=None also reproducible across two calls.
    - functional_depth(data, method="band") and method="modified_band" run and return length-n arrays on a >=2-curve input.
    - Unknown method string (e.g. "not_a_method") raises ValueError.
    - Degenerate inputs raise ValueError: empty data; <2 curves with method="band"/"modified_band"; nproj=0 with method="random_projection".
  </behavior>
  <action>
    In `src/depth_mod.rs`, add `use fdars_core::depth::dispatch::DepthMethod;` and `use pyo3::exceptions::PyValueError;` (numpy `PyReadonlyArray2`/`PyArray1` and `crate::convert::*` are already imported). Write a private helper `depth_method_from_str(method: &str, scale: bool, nproj: usize, seed: Option<u64>) -> PyResult<DepthMethod>` that matches: `"fraiman_muniz" => DepthMethod::FraimanMuniz { scale }`, `"band" => DepthMethod::Band`, `"modified_band" => DepthMethod::ModifiedBand`, `"random_projection" => DepthMethod::RandomProjection { nproj, seed: seed.unwrap_or(0) }`, and a final `other => Err(PyValueError::new_err(...))` arm whose message names the four accepted method strings. Resolve `seed=None` to `0` here (mirror the Phase 31 seed contract) so `random_projection` is byte-reproducible by default. Do NOT construct `DepthMethod` via struct literal anywhere in tests (it is `#[non_exhaustive]`; construction from within this wrapper crate is fine because the wrapper is the constructor, but never in test code).

    Add `#[pyfunction]` `functional_depth` with `#[pyo3(signature = (data, method="fraiman_muniz", scale=true, nproj=50, seed=None))]`, params `py: Python<'py>`, `data: PyReadonlyArray2<'py, f64>`, `method: &str`, `scale: bool`, `nproj: usize`, `seed: Option<u64>`, returning `PyResult<Bound<'py, PyArray1<f64>>>`. Body: `let d = numpy2d_to_fdmatrix(data)?;`, `let m = depth_method_from_str(method, scale, nproj, seed)?;`, `let result = to_pyresult(fdars_core::depth::functional_depth(&d, m))?;`, `Ok(vec_to_numpy1d(py, result))`. Do NOT call `.unwrap()`. Write a module-doc-comment-style `///` docstring documenting parameters (data, method, scale, nproj, seed), the returned ndarray of length n, and the ValueError conditions — mirror the NumPy-style docstrings already in this file.

    In `register()`, add `m.add_function(wrap_pyfunction!(functional_depth, m)?)?;`.

    Create `tests/test_depth.py` (new file — no existing depth test file). Build a module-scope fixture loading a small Canadian Weather temperature subset via `from fdars.datasets import load_canadian_weather` with `return_fdata=False` and slicing to a small number of stations (e.g. first 12 rows) for fast CI; keep the full 365-point grid. Add: an import-path class (both `import fdars.depth` and `from fdars.depth import functional_depth`, assert `callable`); a shape test asserting `functional_depth(X, method=meth).shape == (n,)` for each of the four methods; a cross-check test asserting `np.allclose(functional_depth(X, method="fraiman_muniz", scale=True), fdars.depth.fraiman_muniz_1d(X, X, scale=True))`; a determinism test asserting `np.array_equal` between two `functional_depth(X, method="random_projection", nproj=<small e.g. 20>, seed=7)` calls AND between two `seed=None` calls; and a `pytest.raises(ValueError)` block for the unknown-method string, empty data, `<2` curves with band/modified_band, and `nproj=0` with random_projection. Do NOT reproduce the literal accepted-method-string list from the Rust helper as a bare comment in the Python test body.

    Build with `maturin develop` (see PROJECT.md dev recipe) before running pytest so the compiled extension reflects the new bindings.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && cargo fmt --check && cargo clippy -- -D warnings && maturin develop --quiet && python -m pytest tests/test_depth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0 and `cargo clippy -- -D warnings` exits 0 (no non-exhaustive-match error; wildcard arm present).
    - `python -c "import numpy as np; import fdars.depth; from fdars.datasets import load_canadian_weather; _,X,_=load_canadian_weather('temperature',return_fdata=False); X=X[:12]; d=fdars.depth.functional_depth(X, method='fraiman_muniz'); assert d.shape==(12,)"` exits 0.
    - `python -c "import numpy as np; import fdars.depth; from fdars.datasets import load_canadian_weather; _,X,_=load_canadian_weather('temperature',return_fdata=False); X=X[:12]; assert np.allclose(fdars.depth.functional_depth(X, method='fraiman_muniz', scale=True), fdars.depth.fraiman_muniz_1d(X, X, scale=True))"` exits 0.
    - `python -c "import fdars.depth; from fdars.datasets import load_canadian_weather; _,X,_=load_canadian_weather('temperature',return_fdata=False); fdars.depth.functional_depth(X[:12], method='nope')"` raises ValueError.
    - `python -m pytest tests/test_depth.py -x -q` exits 0.
  </acceptance_criteria>
  <done>All four methods return length-n depth arrays; the fraiman_muniz cross-check and random_projection seed-determinism tests pass; unknown-method and degenerate inputs raise ValueError; rustfmt/clippy clean; the single functional_depth path works end-to-end through maturin+pytest and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: functional_boxplot binding + full dict-contract and round-trip tests</name>
  <files>src/depth_mod.rs, tests/test_depth.py</files>
  <read_first>
    - `/home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/depth/dispatch.rs` — `functional_boxplot(data: &FdMatrix, method: DepthMethod, factor: f64) -> Result<FunctionalBoxplotResult, FdarError>`. `FunctionalBoxplotResult` is `#[non_exhaustive]` with fields `median`, `central_lower`, `central_upper`, `whisker_lower`, `whisker_upper` (each `Vec<f64>` of length `m = data.ncols()`), `outliers: Vec<usize>` (0-based row indices, variable length), `depths: Vec<f64>` (length `n = data.nrows()`). Note the core validates: empty data → err, `<2` curves → err, non-finite/negative `factor` → err.
    - `src/inference_mod.rs` — `test_result_to_pydict` shows the `PyDict::new(py)` + `set_item` compound-result-to-dict pattern and the `#[non_exhaustive]` field-access-only rule.
    - `src/depth_mod.rs` (as extended in Task 1) — reuse `depth_method_from_str` and the same signature/imports conventions.
    - `src/convert.rs` — `vec_to_numpy1d` for the six `Vec<f64>` fields.
  </read_first>
  <behavior>
    - functional_boxplot(X, method="modified_band", factor=1.5) returns a dict with exactly the seven keys median, central_lower, central_upper, whisker_lower, whisker_upper, outliers, depths.
    - median, central_lower, central_upper, whisker_lower, whisker_upper are each 1-D ndarrays of length m (== number of evaluation points, e.g. 365 for full-grid Canadian Weather), and all values finite.
    - depths is a 1-D ndarray of length n (== number of curves).
    - outliers is a Python list (isinstance list) of ints, each in range [0, n).
    - Round-trip / layout guard on a multi-curve input: band field lengths all equal m, depths length equals n, no band-field length accidentally equals n (would signal a transposition), every value finite.
    - Degenerate inputs raise ValueError: empty data; single curve (n<2); negative factor; unknown method string.
  </behavior>
  <action>
    In `src/depth_mod.rs`, add `use pyo3::types::PyDict;` (other imports already present from Task 1). Write a private helper `boxplot_result_to_pydict<'py>(py: Python<'py>, r: fdars_core::depth::dispatch::FunctionalBoxplotResult) -> PyResult<Bound<'py, PyDict>>` (mirror `test_result_to_pydict` in inference_mod.rs). Access each field individually (never struct-literal the `#[non_exhaustive]` result). Set the five band fields and `depths` via `dict.set_item("<key>", vec_to_numpy1d(py, r.<field>))?;` and set `outliers` as a Python list of ints via `dict.set_item("outliers", r.outliers.into_iter().map(|x| x as i64).collect::<Vec<i64>>())?;` — PyO3 converts a `Vec<i64>` to a Python list (NOT a numpy array), satisfying the locked "outliers as a Python list of ints" decision from 32-CONTEXT.md.

    Add `#[pyfunction]` `functional_boxplot` with `#[pyo3(signature = (data, method="modified_band", factor=1.5, scale=true, nproj=50, seed=None))]`, params matching Task 1 plus `factor: f64`, returning `PyResult<Bound<'py, PyDict>>`. Body: `let d = numpy2d_to_fdmatrix(data)?;`, `let m = depth_method_from_str(method, scale, nproj, seed)?;`, `let result = to_pyresult(fdars_core::depth::functional_boxplot(&d, m, factor))?;`, `boxplot_result_to_pydict(py, result)`. No `.unwrap()`. Add a NumPy-style `///` docstring documenting all params, the seven returned dict keys with their types/lengths (band fields length m, depths length n, outliers a list of ints), factor default 1.5 (Tukey), and the ValueError conditions.

    In `register()`, add `m.add_function(wrap_pyfunction!(functional_boxplot, m)?)?;`.

    Extend `tests/test_depth.py`. Add the import-path assertion for `functional_boxplot`. Add a dict-contract test: call `res = fdars.depth.functional_boxplot(X, method="modified_band", factor=1.5)` on the multi-curve fixture and assert `set(res) == {"median","central_lower","central_upper","whisker_lower","whisker_upper","outliers","depths"}`. Add the multi-curve round-trip / layout-guard test asserting: each band field is a numpy ndarray of `shape == (m,)` where `m == X.shape[1]`; `res["depths"].shape == (X.shape[0],)`; `isinstance(res["outliers"], list)` and every element is a Python int with `0 <= o < X.shape[0]`; every band-field value is finite (`np.isfinite(...).all()`); and assert no band field length equals `n` when `n != m` (transposition guard). Add `pytest.raises(ValueError)` cases for empty data, a single-curve input (n<2), negative `factor`, and an unknown method string. Keep any RandomProjection-based boxplot test's `nproj` small for CI. Do NOT reproduce the seven literal key names as a bare comment line in the test body outside of the assertion itself.

    Rebuild with `maturin develop` before running pytest.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && cargo fmt --check && cargo clippy -- -D warnings && maturin develop --quiet && python -m pytest tests/test_depth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy -- -D warnings` exits 0.
    - `python -c "import fdars.depth; from fdars.datasets import load_canadian_weather; _,X,_=load_canadian_weather('temperature',return_fdata=False); X=X[:12]; r=fdars.depth.functional_boxplot(X, method='modified_band', factor=1.5); assert set(r)=={'median','central_lower','central_upper','whisker_lower','whisker_upper','outliers','depths'}; assert isinstance(r['outliers'], list); assert r['median'].shape==(X.shape[1],); assert r['depths'].shape==(X.shape[0],)"` exits 0.
    - `python -c "import fdars.depth; from fdars.datasets import load_canadian_weather; _,X,_=load_canadian_weather('temperature',return_fdata=False); fdars.depth.functional_boxplot(X[:1], method='modified_band')"` raises ValueError.
    - `python -m pytest tests/test_depth.py -x -q` exits 0 (both tasks' tests green).
  </acceptance_criteria>
  <done>functional_boxplot returns the 7-key dict with band fields as length-m ndarrays, depths as a length-n ndarray, and outliers as a Python list of 0-based int indices; the multi-curve round-trip layout guard passes (no transposition); degenerate inputs raise ValueError; rustfmt/clippy clean; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 binding | User-supplied numpy arrays, `method` strings, and numeric kwargs cross into Rust; shapes/values are untrusted. |
| PyO3 binding → fdars-core | Converted `FdMatrix` + `DepthMethod` + `factor` cross into the pure-computation core. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | `depth_method_from_str` (unknown method string) | low | mitigate | `#[non_exhaustive]` wildcard `_ => PyValueError` arm; `pytest.raises(ValueError)` on unknown method. |
| T-32-02 | Denial of Service | `functional_depth` / `functional_boxplot` degenerate inputs (empty, <2 curves, nproj=0, non-finite/negative factor) | low | mitigate | Route all fallible core calls through `to_pyresult()` (no `.unwrap()`); core validates and returns `FdarError`→`ValueError`; degenerate-input tests assert the raises. |
| T-32-03 | Information Disclosure | Silent numpy↔FdMatrix transposition mislabeling axes (the #33 layout class) | medium | mitigate | Multi-curve round-trip layout-guard test: band fields length m, depths length n, outliers ∈ [0,n), all finite, no band length == n when n≠m. |
| T-32-04 | Repudiation | Non-reproducible RandomProjection results | low | mitigate | `seed=None`→0 default inside `depth_method_from_str`; determinism test asserts byte-identical results across two calls. |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages installed this phase — extends existing deps (fdars-core 0.20, pyo3, numpy) already in Cargo.toml; no legitimacy gate needed. |
</threat_model>

<verification>
- `cargo fmt --check` and `cargo clippy -- -D warnings` both exit 0 (wildcard match arm present; no `#[non_exhaustive]` construction in tests; no `.unwrap()` in the new bindings).
- `maturin develop` builds green; `python -m pytest tests/test_depth.py -q` exits 0.
- Both import styles resolve: `import fdars.depth` and `from fdars.depth import functional_depth, functional_boxplot`.
- functional_depth: four methods return length-n arrays; fraiman_muniz cross-check vs `fraiman_muniz_1d`; random_projection seed determinism; unknown-method + degenerate ValueError.
- functional_boxplot: 7-key dict; band fields length m, depths length n, outliers a Python list of ints in [0,n); multi-curve round-trip layout guard; degenerate ValueError.
- Do NOT commit `Cargo.lock` (gitignored per project convention).
</verification>

<success_criteria>
- DEPTH-01: `fdars.depth.functional_depth(data, method=..., **kwargs)` → `ndarray (n,)` with string→`DepthMethod` dispatch and a `#[non_exhaustive]` wildcard `ValueError` fallback. ✅ when Task 1 verify passes.
- DEPTH-02: `fdars.depth.functional_boxplot(data, method=..., factor=1.5, **kwargs)` → 7-key dict with band fields as 1-D arrays via `vec_to_numpy1d`, `outliers` as a Python list of ints, guarded by a multi-curve round-trip layout test. ✅ when Task 2 verify passes.
- All success criteria from ROADMAP Phase 32 (three items) satisfied.
</success_criteria>

<output>
Create `.planning/phases/32-group-b-depth-boxplot-bindings/32-01-SUMMARY.md` when done.
</output>

---
phase: 30-crate-bump-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/smoothing_mod.rs
autonomous: true
requirements: [DEP-03, DEP-04]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Cargo.toml pins fdars-core = { version = \"0.20.0\", features = [\"parallel\"] } with no linalg feature (DEP-03)"
    - "The optim_bandwidth binding compiles against 0.20.0's #[non_exhaustive] CvCriterion via wildcard fallback arms in both match directions (DEP-04)"
    - "maturin develop builds the extension green in the project venv (DEP-03)"
    - "The full existing binding + advisor suite (~426 tests) passes unchanged — no new tests, no tolerance relaxations (DEP-04)"
    - "cargo clippy -- -D warnings and cargo fmt --check stay clean on the edited Rust (DEP-04)"
    - "The bump lands as one isolated commit before any Phase 31/32/33 binding work (DEP-04)"
  artifacts:
    - Cargo.toml
    - src/smoothing_mod.rs
  key_links:
    - "src/smoothing_mod.rs CvCriterion string-to-enum match (line ~193) must gain a wildcard arm — without it the crate does not compile against 0.20.0"
    - "src/smoothing_mod.rs CvCriterion enum-to-string match (line ~209) must gain a wildcard arm — same #[non_exhaustive] compile blocker"
---

<objective>
Bump the pinned `fdars-core` dependency from 0.17.0 to 0.20.0 (parallel-only, no `linalg`), add the forward-compatible `CvCriterion` wildcard fallback arms that 0.20.0's `#[non_exhaustive]` enum now requires to compile, rebuild via `maturin develop`, and prove the entire existing ~426-test binding + advisor suite still passes unchanged. This establishes the green baseline that Phases 31/32/33 build on (DEP-03, DEP-04).

Purpose: A downstream binding regression must never be able to hide behind an upgrade regression. Landing the bump + compile-fix as an isolated, provably-green commit first isolates any future failure to the binding work that caused it.
Output: Modified `Cargo.toml` (pin at 0.20.0) and `src/smoothing_mod.rs` (two wildcard match arms); regenerated `Cargo.lock` on disk (gitignored — NOT committed); a passing full test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

# The exact pin to change and the exact match arms to fix:
@Cargo.toml
@src/smoothing_mod.rs
</context>

<artifacts_this_phase_produces>
This phase produces NO new public Python or Rust symbols. It is a pure-infrastructure regression gate. The only source changes are:
- `Cargo.toml`: the `fdars-core` version string moves from `0.17.0` to `0.20.0` (the `features = ["parallel"]` set is unchanged; `linalg` is NOT added).
- `src/smoothing_mod.rs`: two additional wildcard match arms on the existing `optim_bandwidth` binding's `CvCriterion` matches (a compile fix, not new behavior).

No new bindings, no new submodules, no new tests, no new datasets, no API surface change. `Cargo.lock` regenerates on disk but is gitignored per repo policy and MUST NOT be committed.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Bump fdars-core 0.17.0 to 0.20.0 and add the CvCriterion wildcard fallback arms so the crate compiles end-to-end</name>
  <files>Cargo.toml, src/smoothing_mod.rs</files>
  <read_first>Cargo.toml (the pin is on line 18); src/smoothing_mod.rs (the CvCriterion string-to-enum match is at lines 193-201, the enum-to-string match is at lines 209-212); .planning/research/PITFALLS.md (Pitfall 2 — the #[non_exhaustive] compile blocker) and .planning/research/STACK.md (section 1 — the exact pin change and the "do NOT enable linalg" rationale).</read_first>
  <action>This is the single end-to-end tracer slice for the phase: change the dependency and make the crate build, touching both layers the phase modifies (the Cargo pin and the Rust binding). In Cargo.toml, edit the fdars-core dependency line so the version string reads exactly 0.20.0 while keeping features = ["parallel"] unchanged; do NOT add the linalg feature (per DEP-03 and STACK.md section 4, linalg requires Rust 1.84 which exceeds pyfda's MSRV 1.83 and pulls in faer plus anofox-regression). Then, in src/smoothing_mod.rs, fix the two exhaustive matches on fdars_core::smoothing::CvCriterion that will no longer compile because 0.20.0 marks the enum #[non_exhaustive] (per DEP-04 and PITFALLS.md Pitfall 2). To the string-to-enum match (the one mapping "cv" and "gcv" to CvCriterion variants), the existing final arm already returns a PyValueError for unrecognized strings and satisfies the non-exhaustive requirement structurally — confirm it compiles and, if the compiler still flags exhaustiveness, ensure the catch-all arm remains the last arm covering the wildcard. To the enum-to-string match (the one converting result.criterion back into a "cv"/"gcv" string literal), add a trailing wildcard arm mapping any other variant to the string "unknown", mirroring the existing #[non_exhaustive] fallback idiom already used for string-enum dispatch elsewhere in the codebase (InterpolationMethod, BasisCriterion). Do not add any new accepted criterion value or new behavior — the Aic variant is bound in Phase 33, not here; this change is purely to restore compilation. Rebuild the extension by running maturin develop inside the project venv at /home/simonm/projects/rust/pyfda/.venv so Cargo.lock regenerates on disk; do NOT git-add Cargo.lock (it is gitignored per repo policy). Keep the diff minimal — only the CvCriterion match arms and the Cargo pin should change.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q 'fdars-core = { version = "0.20.0", features = \["parallel"\] }' Cargo.toml && ! grep -q 'linalg' Cargo.toml && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml line 18 reads exactly: fdars-core = { version = "0.20.0", features = ["parallel"] }
    - Cargo.toml contains no "linalg" token anywhere.
    - cargo build exits 0 (no "non-exhaustive patterns" error on CvCriterion).
    - maturin develop (run in /home/simonm/projects/rust/pyfda/.venv) exits 0 and installs the rebuilt _native extension.
    - src/smoothing_mod.rs enum-to-string CvCriterion match has a trailing wildcard arm returning "unknown"; the string-to-enum match still compiles under #[non_exhaustive].
  </acceptance_criteria>
  <done>fdars-core is pinned at 0.20.0 (parallel-only), the CvCriterion matches compile against the #[non_exhaustive] enum, and maturin develop builds the extension green.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full existing binding + advisor suite as the sole regression gate and confirm rustfmt/clippy stay clean</name>
  <files>Cargo.toml, src/smoothing_mod.rs</files>
  <read_first>Cargo.toml and src/smoothing_mod.rs (the only edited files, to confirm the diff is minimal before gating); .planning/research/PITFALLS.md (Pitfall 12 — numeric drift on the existing suite, and the "widen by one ULP only, in this phase, and record it" recovery rule); .planning/STATE.md (the v4.0 close baseline of 426 passed / 4 skipped that this phase must reproduce).</read_first>
  <action>Run the entire existing pytest suite against the freshly rebuilt extension in the project venv at /home/simonm/projects/rust/pyfda/.venv — this is the sole success criterion for DEP-04. The expected result is the v4.0 close baseline reproduced unchanged: approximately 426 passed and 4 skipped, zero failures. Do NOT add new tests and do NOT relax any tolerance as a first move; the expected path (matching the v4.0 0.14 to 0.17 bump, which needed zero tolerance changes) is that the suite passes as-is. Only if a genuine faer/FPCA numeric-drift failure surfaces on an existing test (not a new-binding failure — there are no new bindings in this phase) may you widen the single affected FPCA tolerance by the minimum amount (one ULP at a time, never a blanket rtol=1e-5), and you MUST record the exact function and the widened tolerance in the plan's SUMMARY notes; treat zero changes as the planned outcome and any tolerance edit as an exception to be documented. Separately, confirm the edited Rust stays lint-clean under the CI-enforced gates: run cargo fmt --check and cargo clippy with -D warnings. If cargo fmt --check reports the edited smoothing_mod.rs is unformatted, run cargo fmt to fix it (formatting only — no logic change). The phase is complete only when the full suite is green and both Rust gates pass.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest -q 2>&1 | tail -5 && cargo fmt --check && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/python -m pytest exits 0 with roughly 426 passed and 4 skipped and zero failures.
    - No new test files were added and no tolerance was relaxed (or, if one was, it is recorded in the SUMMARY with the exact drifted function named).
    - cargo fmt --check exits 0 (edited Rust is formatted).
    - cargo clippy -- -D warnings exits 0 (no warnings on the edited match arms).
  </acceptance_criteria>
  <done>The full ~426-test suite passes unchanged and both rustfmt and clippy (-D warnings) are clean, establishing the green baseline for Phases 31/32/33.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyfda -> fdars-core crate registry | The bump pulls fdars-core 0.20.0 from crates.io; a compromised/typosquatted version would execute at build time. This is an EXISTING dependency (same author sipemu), version-bumped — not a newly introduced package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | fdars-core 0.20.0 crate fetched at build | low | accept | Existing first-party dependency (same maintainer as pyfda, verified in STACK.md sources against crates.io registry API: version 3012586, publish 2026-08-16, not yanked, MSRV 1.81). Caret pin ^0.20.0 accepts only 0.20.x patches. No new package introduced — a version bump of an already-trusted dep. |
| T-30-02 | Denial of Service | CI build time on the bump | low | accept | Additive/non-breaking bump with byte-identical dependency tree for features=["parallel"] (STACK.md section 3); no new transitive deps, no build-time expansion. |

Note: No `npm/pip/cargo add` of a NEW package occurs in this phase — only a version bump of the pre-existing `fdars-core` dependency. The Package Legitimacy Gate (which applies to newly installed packages) is therefore not triggered; the STACK.md crates.io-verified provenance above covers the bump.
</threat_model>

<verification>
- `Cargo.toml` pins `fdars-core = { version = "0.20.0", features = ["parallel"] }` with no `linalg` token (`grep`).
- `cargo build` exits 0 — the `#[non_exhaustive]` `CvCriterion` matches compile.
- `maturin develop` (in `.venv`) exits 0 — the extension rebuilds and installs.
- `.venv/bin/python -m pytest -q` exits 0 with ~426 passed / 4 skipped / 0 failed.
- `cargo fmt --check` and `cargo clippy -- -D warnings` both exit 0.
- Only `Cargo.toml` and `src/smoothing_mod.rs` are staged for the commit; `Cargo.lock` is NOT staged.
</verification>

<success_criteria>
1. `fdars-core` is pinned at `0.20.0` (parallel-only, no `linalg`) and `maturin develop` builds green (DEP-03).
2. The `optim_bandwidth` binding compiles against 0.20.0's `#[non_exhaustive]` `CvCriterion` because the wildcard fallback arm(s) are present (DEP-04).
3. The full existing ~426-test binding + advisor suite passes unchanged — no new tests, no tolerance relaxations (DEP-04).
4. rustfmt + clippy (`-D warnings`) stay clean on the edited Rust (DEP-04).
5. The change lands as one isolated commit (`Cargo.toml` + `src/smoothing_mod.rs`, not `Cargo.lock`) before any Phase 31/32/33 binding work (DEP-04).
</success_criteria>

<output>
Create `.planning/phases/30-crate-bump-regression-gate/30-01-SUMMARY.md` when done. Record: the pytest pass/skip counts (confirm ~426 passed / 4 skipped), whether any FPCA tolerance was widened (expected: none — if any, name the exact drifted function and the widened value), and confirmation that `Cargo.lock` was NOT committed.
</output>

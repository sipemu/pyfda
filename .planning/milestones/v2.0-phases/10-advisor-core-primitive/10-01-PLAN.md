---
phase: 10-advisor-core-primitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor.py
autonomous: true
requirements: [CORE-02, CORE-03, CORE-04, ADVISE-01]
estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "build_diagnostics(result, method='alignment') returns a deterministic dict computed only from fdars + numpy — no LLM, no network — and two runs on the same input yield an identical dict (CORE-01 alignment slice)"
    - "With anthropic uninstalled, importing fdars.advisor and calling build_diagnostics both succeed; calling advise raises ImportError whose message names 'pip install fdars[advisor]' (CORE-04)"
    - "advise(diagnostics, task='interpretation', domain_context=...) returns a schema-validated Advice via client.messages.parse(model='claude-opus-4-8', output_format=Advice) (CORE-02, ADVISE-01)"
    - "Every Recommendation carries action, kind in {parameter, method, none}, rationale, expected_effect, and evidence[] (CORE-03)"
  artifacts:
    - "python/fdars/advisor.py — new pure-Python module with Advice/Recommendation Pydantic models, build_diagnostics (alignment), advise, __all__"
  key_links:
    - "advise -> lazy anthropic import guard -> client.messages.parse(output_format=Advice) -> .parsed_output"
    - "advise system prompt <- grounding invariant text (reason only from diagnostics; every evidence item cites a value) -> Advice.recommendations[].evidence"
---

<objective>
Deliver the end-to-end tracer for the fdars advisor: a new `python/fdars/advisor.py` that
(1) defines the grounded-output Pydantic schema, (2) computes deterministic OFFLINE diagnostics
for one method (alignment), and (3) makes one grounded Claude call through
`client.messages.parse` that returns a schema-validated `Advice`. This proves the whole
architecture — offline diagnostics → grounded LLM → structured, evidence-citing advice —
on a single vertical path before any other method or task family is built out.

Purpose: prove the offline-first / grounded-LLM architecture works end-to-end and lock the
`anthropic` SDK version-floor decision, so Phase 10's expansion plans build on a validated skeleton.
Output: `python/fdars/advisor.py` (schema + alignment diagnostics + advise), production-quality,
offline-testable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/llm-cluster-narration.md
@.planning/codebase/CONVENTIONS.md

# Existing pure-Python module patterns to mirror (lazy imports, __all__, NumPy docstrings)
@python/fdars/metrics.py
@python/fdars/results.py
@python/fdars/__init__.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — advisor.py skeleton: Advice schema + lazy anthropic guard + build_diagnostics(alignment) + advise, wired end-to-end</name>
  <files>python/fdars/advisor.py</files>
  <action>
Create `python/fdars/advisor.py` as a pure-Python module (start with `from __future__ import annotations`,
NumPy-style docstrings, `__all__ = ["build_diagnostics", "advise", "Advice", "Recommendation"]`). Import numpy at
module top; do NOT import anthropic at module top.

Define the grounded-output Pydantic models (import `BaseModel` from pydantic, `Literal`/`List` from typing).
`Recommendation` has fields: `action: str`, `kind: Literal["parameter", "method", "none"]`, `rationale: str`,
`expected_effect: str`, `evidence: list[str]`. `Advice` has fields: `interpretation: str`,
`recommendations: list[Recommendation]`, `caveats: list[str]`. Keep the models within Claude structured-output
JSON-schema limits: no numeric min/max constraints, no string length constraints, no default values that would
introduce `minLength`/`maximum`; the SDK emits `additionalProperties:false` from these plain models. Do NOT add
Field(...) numeric or length validators.

Implement `build_diagnostics(result, method, *, argvals=None, **kwargs) -> dict`. This function is OFFLINE and
DETERMINISTIC — it uses only numpy and (optionally) fdars submodules; it must NOT import anthropic, open a network
connection, use any RNG, or read wall-clock time. For this tracer, implement the `method="alignment"` branch only;
raise a clear `ValueError` naming the supported methods for any other value (the expansion plan 10-02 adds fpca,
basis, smoothing, clustering). Accept `result` as either a dict (native fdars alignment output) or an
`fdars.results.AlignmentResult` (read its `.raw` when present). From the alignment result compute a deterministic
diagnostics dict with plain Python floats / lists (JSON-serialisable, no numpy scalars): the Karcher/template mean
summary (length, min, max, mean of the mean curve), warp/amplitude/phase separation using
`fdars.alignment.amplitude_distance` and `fdars.alignment.phase_distance` on the aligned curves vs the mean when
`aligned_data` and a mean are present (guard for absence), a convergence flag (`converged`) and `n_iter` when
present, and a `method: "alignment"` key. Round or cast to float via `float(...)` so repeated runs are byte-identical.
Import the fdars alignment submodule lazily INSIDE build_diagnostics (`from fdars import alignment`) so importing
advisor never forces a heavy import chain.

Implement a private `_require_anthropic()` helper that does `import anthropic` inside a try/except ImportError and,
on failure, re-raises `ImportError` with a message that literally contains the install hint token so the
`pip install fdars[advisor]` string is present in the raised message (CORE-04). Return the imported module on success.

Implement `advise(diagnostics, *, task, domain_context, model="claude-opus-4-8") -> Advice`. Call
`_require_anthropic()` first (so the ImportError fires before any other work). Construct
`client = anthropic.Anthropic()` (API key is read from the ANTHROPIC_API_KEY env var by the SDK — never hardcode a
key, never pass api_key=). Build a system prompt via a private `_system_prompt(task)` helper (Task 2) that encodes the
grounding invariant. Call `client.messages.parse(model=model, max_tokens=16000, thinking={"type": "adaptive"},
system=<system prompt>, output_format=Advice, messages=[{"role": "user", "content": <user content built from
diagnostics + domain_context + task>}])` and return `response.parsed_output`. Build the user content by serialising
the diagnostics dict as text (e.g. via json.dumps with sort_keys=True for stable ordering) together with the
domain_context and the requested task family so the model reasons only over provided numbers.

Reference the design source of truth (.planning/design/llm-cluster-narration.md, Stage 1/Stage 2 and the Schema
block) and CONTEXT decisions: model claude-opus-4-8, adaptive thinking, offline-first build_diagnostics, grounding
invariant enforced by schema AND system prompt. This task implements CORE-02, CORE-03, CORE-04, and the alignment
slice of CORE-01, and the interpretation task family ADVISE-01 (via task="interpretation").
  </action>
  <verify>
    <automated>ANTHROPIC_SDK_TMP=$(python -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('anthropic') is None else 1)"; echo $?); PYTHONPATH=/home/simonm/projects/rust/pyfda/python python -c "import ast,sys; t=ast.parse(open('/home/simonm/projects/rust/pyfda/python/fdars/advisor.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.ClassDef))}; req={'build_diagnostics','advise','Advice','Recommendation','_require_anthropic'}; missing=req-names; print('MISSING',missing) if missing else print('OK all symbols present')"</automated>
  </verify>
  <done>
advisor.py exists and defines Advice, Recommendation, build_diagnostics, advise, and _require_anthropic.
build_diagnostics has an alignment branch computed only from numpy + fdars (no anthropic import, no RNG, no
wall-clock). advise calls _require_anthropic() first, then client.messages.parse(model="claude-opus-4-8",
output_format=Advice, thinking adaptive) and returns .parsed_output. anthropic is imported lazily only inside
advise/_require_anthropic. __all__ lists the public names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Grounding-invariant system prompt (interpretation task family) + FDA primer</name>
  <files>python/fdars/advisor.py</files>
  <action>
Add a private `_system_prompt(task: str) -> str` helper used by `advise`. The base prompt MUST encode the grounding
invariant as directive text: instruct the model to reason ONLY from the diagnostics provided in the user message; to
make every `evidence` entry cite a specific diagnostic value that appears in the input; to omit any claim not
supported by a provided value; and to never invent or estimate numbers not present in the diagnostics. Include a
short FDA primer so interpretations are method-accurate: define amplitude vs phase variation, Karcher/template mean,
warp penalty, GCV, and variance explained in one or two lines each (sourced from the design doc's grounding-mechanics
section). Add a task-family clause selected by `task`: for `task="interpretation"` (ADVISE-01), instruct the model to
explain what the computed result means in domain terms and set recommendation `kind` to "none" unless a concrete
parameter or method change is clearly warranted. Accept the task string case-insensitively and raise a clear
ValueError for an unknown task (the expansion plan 10-02 adds the "parameter" and "method" task clauses; keep the
dispatch structured so those branches slot in without rewriting the base prompt).

Do NOT place any fenced code block in this file's prose; write the prompt as ordinary Python string literals. Ensure
the invariant wording (reason only from provided diagnostics; every evidence item cites a value; omit unsupported
claims; never fabricate numbers) is present verbatim as string content so it is greppable.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/simonm/projects/rust/pyfda/python python -c "import ast; src=open('/home/simonm/projects/rust/pyfda/python/fdars/advisor.py').read(); t=ast.parse(src); assert any(isinstance(n,ast.FunctionDef) and n.name=='_system_prompt' for n in ast.walk(t)), 'missing _system_prompt'; low=src.lower(); assert 'evidence' in low and 'diagnostic' in low and 'fabricate' in low or 'never invent' in low, 'grounding invariant text not found'; print('OK grounding prompt present')"</automated>
  </verify>
  <done>
_system_prompt(task) exists, returns a string containing the grounding-invariant instructions (reason only from
provided diagnostics, every evidence item cites a value, omit unsupported claims, never fabricate numbers) plus an
FDA primer, with an interpretation-task clause for task="interpretation" and a ValueError for unknown tasks.
  </done>
</task>

<task type="auto">
  <name>Task 3: Offline determinism smoke check + record anthropic SDK version-floor decision</name>
  <files>python/fdars/advisor.py</files>
  <action>
Make the tracer testable offline without a network call or the anthropic package installed. Add a module-level
constant `ADVISOR_ANTHROPIC_MIN_VERSION = "0.72.0"` and reference it in the ImportError message emitted by
`_require_anthropic` (e.g. the message includes both `pip install fdars[advisor]` and the minimum-version note) — this
RESOLVES the deferred OPEN DECISION for Phase 10 (D: anthropic SDK version floor supporting messages.parse +
claude-opus-4-8). Add a short NumPy-style module docstring note stating: the version floor is DECIDED here in Phase 10;
the [advisor] extra itself (declaring anthropic>=0.72.0) is DECLARED and TESTED in Phase 11 per the phase split — do
not add the extra to pyproject.toml in this phase.

Add a private, offline `_selfcheck_alignment_diagnostics()` helper (or an equivalent guarded `if __name__` block is
NOT required) that constructs a tiny deterministic synthetic alignment result dict (fixed numpy arrays defined inline,
no RNG) and asserts that two calls to build_diagnostics on it return equal dicts — this is the offline determinism
proof the executor can run. Keep it importable and side-effect-free (do not run it at import time).

Do NOT touch pyproject.toml, __init__.py, or any test file in this phase (registration + tests are Phase 11).
  </action>
  <verify>
    <automated>PYTHONPATH=/home/simonm/projects/rust/pyfda/python python -c "
import sys
# Simulate anthropic missing so the offline path is exercised even if installed.
import importlib, types
import fdars.advisor as A
importlib.reload(A)
assert hasattr(A, 'ADVISOR_ANTHROPIC_MIN_VERSION'), 'version floor constant missing'
res={'mean':[0.0,1.0,2.0,1.0,0.0],'aligned_data':[[0.0,1.0,2.0,1.0,0.0],[0.1,1.1,2.0,0.9,0.0]],'converged':True,'n_iter':3}
d1=A.build_diagnostics(res, method='alignment', argvals=[0.0,0.25,0.5,0.75,1.0])
d2=A.build_diagnostics(res, method='alignment', argvals=[0.0,0.25,0.5,0.75,1.0])
assert d1==d2, 'build_diagnostics not deterministic'
assert d1.get('method')=='alignment'
import json; json.dumps(d1)  # must be JSON-serialisable (plain floats/lists)
try:
    A._require_anthropic()
except ImportError as e:
    assert 'fdars[advisor]' in str(e), 'install hint missing from ImportError'
    print('OK offline determinism + ImportError hint verified')
else:
    print('OK offline determinism verified (anthropic installed; skipped ImportError assertion)')
"</automated>
  </verify>
  <done>
ADVISOR_ANTHROPIC_MIN_VERSION="0.72.0" is defined and cited in the _require_anthropic ImportError message; two
build_diagnostics(alignment) calls on the same synthetic input return an equal, JSON-serialisable dict; the module
docstring records the version-floor decision and the Phase-10/Phase-11 split; pyproject.toml and tests are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python process -> Anthropic API | diagnostics + domain_context leave the process as an LLM prompt over the network (only in advise, never in build_diagnostics) |
| env -> process | ANTHROPIC_API_KEY read from the environment by the anthropic SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information disclosure | advise() prompt payload | medium | accept | build_diagnostics is offline; only advise sends data, and only when the user explicitly calls it with their own diagnostics + context — documented, not automatic |
| T-10-02 | Spoofing/Tampering | ANTHROPIC_API_KEY | high | mitigate | key is read from env by the SDK; never hardcoded, never accepted as a function argument, never logged |
| T-10-03 | Tampering (fabricated numbers) | Advice.recommendations[].evidence | high | mitigate | grounding invariant enforced by BOTH the Pydantic schema (evidence is required) and the system prompt (every evidence item must cite a provided value; never invent numbers) |
| T-10-SC | Tampering | no package installs in this phase | low | accept | this phase adds no package-manager install task; the [advisor] extra is declared/tested in Phase 11 |
</threat_model>

<verification>
- advisor.py parses and exposes build_diagnostics, advise, Advice, Recommendation, _require_anthropic (Task 1 gate).
- build_diagnostics(alignment) is deterministic and JSON-serialisable; no anthropic import, no RNG, no wall-clock (Task 3 gate).
- _require_anthropic raises ImportError containing the install hint when anthropic is absent (Task 3 gate).
- _system_prompt encodes the grounding invariant and interpretation task clause (Task 2 gate).
- advise wires client.messages.parse(model="claude-opus-4-8", output_format=Advice, thinking adaptive) and returns .parsed_output (code review against Task 1 done-criteria).
</verification>

<success_criteria>
- The tracer path is proven end-to-end: build_diagnostics(alignment) → advise → schema-validated Advice.
- CORE-02, CORE-03, CORE-04 satisfied; CORE-01 satisfied for the alignment method; ADVISE-01 satisfied via task="interpretation".
- The anthropic SDK version-floor decision is recorded in-module (>=0.72.0), resolving the Phase 10 open decision.
- pyproject.toml, __init__.py, and tests are untouched (deferred to Phase 11).
</success_criteria>

<output>
Create `.planning/phases/10-advisor-core-primitive/10-01-SUMMARY.md` when done.
</output>

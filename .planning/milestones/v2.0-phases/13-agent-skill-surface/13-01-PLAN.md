---
phase: 13-agent-skill-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_skill.py
  - .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py
  - .claude/skills/fdars-advisor/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Running the walkthrough script offline (no ANTHROPIC_API_KEY) exits 0 and prints a non-empty before/after delta with >=1 finite numeric key (Success Criterion 3, offline half; SKILL-01)"
    - "SKILL.md parses as YAML front-matter + Markdown body, with required `name` (== directory `fdars-advisor`) and `description` fields (SKILL-01)"
    - "SKILL.md documents the execution environment: Python >=3.10 and the git-URL install workaround for fdars+mcp (SKILL-02)"
  artifacts:
    - "tests/test_skill.py — new pytest module (Wave 0 scaffold + tracer smoke tests)"
    - ".claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py — offline end-to-end compare walkthrough"
    - ".claude/skills/fdars-advisor/SKILL.md — skill manifest with agentskills.io-compliant front-matter"
  key_links:
    - "walkthrough script imports fdars.mcp._registry.registry / _runner.run_method / _compare.compare_run directly (no stdio, no async) — the compute path that produces the deterministic delta"
    - "test_skill.py invokes the script via subprocess and asserts returncode 0 + non-empty delta — the end-to-end tracer verification"
    - "SKILL.md `name:` value string-equals its parent directory name `fdars-advisor` (agentskills.io discovery requirement)"
---

<objective>
TRACER: prove the fdars-advisor skill end-to-end on its thinnest complete path — an
offline walkthrough script that loads Canadian Weather, runs smoothing, and prints a
grounded before/after diagnostics delta; a minimal but spec-valid SKILL.md manifest;
and a `tests/test_skill.py` module whose smoke tests drive the script end-to-end and
parse the manifest. This is the vertical slice through every layer this phase touches
(manifest -> script -> fdars.mcp compute -> test harness), wired for keeps.

Purpose: catch any packaging / import / dataset-access / delta-shape dead-end after one
commit, before Plan 02 expands the SKILL.md body, the `advise()` LLM path, and the
Python-3.9 / front-matter edge tests.
Output: `tests/test_skill.py`, `.claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py`,
and a first-cut `.claude/skills/fdars-advisor/SKILL.md`, all committed with green smoke tests.

## Artifacts this phase produces (Phase 13, both plans)

New files:
- `.claude/skills/fdars-advisor/SKILL.md` — skill manifest (Plan 01 first-cut, Plan 02 completes body)
- `.claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py` — offline walkthrough (Plan 01) + env-gated advise() step (Plan 02)
- `tests/test_skill.py` — new pytest module

New symbols / test functions:
- `main()` (walkthrough entry point) in fdars_advisor_walkthrough.py
- `_parse_frontmatter(path)` helper + `SKILL_MD` / `SCRIPT_PATH` constants in tests/test_skill.py
- Plan 01 tests: `test_skill_md_frontmatter`, `test_walkthrough_script_offline`, `test_walkthrough_delta_nonempty`
- Plan 02 tests: `test_skill_md_name_matches_dir`, `test_skill_md_compatibility`, `test_walkthrough_py39_exit0`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-skill-surface/13-CONTEXT.md
@.planning/phases/13-agent-skill-surface/13-RESEARCH.md
@.planning/phases/13-agent-skill-surface/13-PATTERNS.md
@.planning/phases/13-agent-skill-surface/13-VALIDATION.md
@examples/mcp_recipe.py
@tests/test_mcp_server.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wave-0 scaffold + tracer smoke tests in tests/test_skill.py</name>
  <files>tests/test_skill.py</files>
  <read_first>
    - tests/test_mcp_server.py (lines 1-66) — module docstring, `pytestmark = pytest.mark.skipif(sys.version_info < (3, 10), ...)`, autouse `_clear_registry` fixture, `canadian_weather` fixture
    - .planning/phases/13-agent-skill-surface/13-PATTERNS.md (section on tests/test_skill.py — the `_parse_frontmatter` helper using `re.match` DOTALL + `yaml.safe_load`; the subprocess.run assertion pattern)
    - .planning/phases/13-agent-skill-surface/13-VALIDATION.md (Per-Task Verification Map — exact test-function names 13-01-01..13-01-04)
    - .planning/phases/13-agent-skill-surface/13-RESEARCH.md (Validation Architecture — Phase Requirements to Test Map)
    - examples/mcp_recipe.py (the delta-printing format the script will emit — the Delta header string and value lines)
  </read_first>
  <behavior>
    - Module skips cleanly on Python below 3.10 via module-level `pytestmark` skipif (mirrors test_mcp_server.py).
    - Module defines module-level `pathlib.Path` constants `SKILL_MD` and `SCRIPT_PATH` resolved from `pathlib.Path(__file__).resolve().parents[1]`.
    - Module defines a `_parse_frontmatter(path)` helper: regex-splits the leading front-matter block and `yaml.safe_load`s it into a dict.
    - test_skill_md_frontmatter: `_parse_frontmatter(SKILL_MD)` returns a dict with a non-empty string `name` and a non-empty string `description`.
    - test_walkthrough_script_offline: runs `subprocess.run([sys.executable, str(SCRIPT_PATH)], capture_output=True, text=True, timeout=120)` with `ANTHROPIC_API_KEY` removed from the environment; asserts `returncode == 0`.
    - test_walkthrough_delta_nonempty: from the same subprocess stdout, asserts the Delta header substring is present AND at least one delta value line follows it (delta is non-empty).
  </behavior>
  <action>Create tests/test_skill.py mirroring tests/test_mcp_server.py conventions: `from __future__ import annotations`, imports `os`, `pathlib`, `re`, `subprocess`, `sys`, `pytest`, `yaml`; set `pytestmark = pytest.mark.skipif(sys.version_info < (3, 10), reason="mcp requires Python 3.10+")`. Define `SKILL_MD` and `SCRIPT_PATH` as `pathlib.Path` constants resolved from `pathlib.Path(__file__).resolve().parents[1]`. Add the `_parse_frontmatter(path)` helper (regex matching the leading front-matter fence, DOTALL, then `yaml.safe_load`). Implement the four functions above. For the subprocess offline run, build a copy of `os.environ` and pop the ANTHROPIC key so the offline branch is exercised deterministically, passing it as `env=`. Keep assertion messages descriptive, matching test_mcp_server.py assertion style. Do NOT add an autouse registry fixture here — the script runs in its own subprocess, so there is no in-process registry to clear. This is the Nyquist scaffold: it MUST exist before Tasks 2-3 so their verify commands resolve. The header substring the delta test greps for must match exactly what Task 2 prints; agree on the literal Delta header and reference it here.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_skill.py --collect-only -q 2>&1 | grep -E "test_skill_md_frontmatter|test_walkthrough_script_offline|test_walkthrough_delta_nonempty"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_skill.py --collect-only -q` lists test_skill_md_frontmatter, test_walkthrough_script_offline, test_walkthrough_delta_nonempty
    - Module contains `pytestmark = pytest.mark.skipif(sys.version_info < (3, 10)`
    - Module defines `SKILL_MD` and `SCRIPT_PATH` Path constants and a `_parse_frontmatter` helper
    - The offline test removes ANTHROPIC_API_KEY from the subprocess env
  </acceptance_criteria>
  <done>tests/test_skill.py exists and collects the three tracer smoke tests without import/collection errors.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Offline walkthrough script — fdars_advisor_walkthrough.py</name>
  <files>.claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py</files>
  <read_first>
    - examples/mcp_recipe.py (full file) — version guard (lines 37-44), direct imports (lines 46-51), dataset load pattern `ds.data.data` / `ds.argvals` (lines 57-62), run_method + store_result (lines 73-78), compare_run call (lines 85-90), delta loop (lines 110-117)
    - .planning/phases/13-agent-skill-surface/13-PATTERNS.md (section on fdars_advisor_walkthrough.py — wrap in `def main()`, `registry.clear()` at top, separator-comment style)
    - .planning/phases/13-agent-skill-surface/13-RESEARCH.md (Code Examples — full script skeleton; Pitfall 4 version-guard-before-import; Pitfall 7 registry.clear)
    - .claude/CLAUDE.md (conventions — `from __future__ import annotations`, NumPy docstrings, private helpers underscore-prefixed)
  </read_first>
  <behavior>
    - On Python below 3.10: prints an informative message and `sys.exit(0)` BEFORE any `from fdars.mcp import ...` line (version guard first).
    - On Python 3.10+ offline (no ANTHROPIC_API_KEY): loads Canadian Weather, runs smoothing n_basis=15, compares against n_basis=25, and prints the delta block; exits 0.
    - Prints a header line beginning with the agreed Delta header string, then one signed value line per scalar delta key (4 keys expected: gcv_aic_approx, gcv_bic_approx, optimal_gcv, optimal_edf).
    - Calls `registry.clear()` at the top of `main()` so repeated in-process runs do not leak handles.
  </behavior>
  <action>Create the walkthrough script modelled on examples/mcp_recipe.py but restructured into a `def main() -> None:` entry point with an `if __name__ == "__main__": main()` guard (CLAUDE.md convention). Start with a NumPy-style module docstring describing offline vs API-key runs. Add `from __future__ import annotations`, then `import os, sys`. Place the Python-version guard (`if sys.version_info < (3, 10): print(...); sys.exit(0)`) BEFORE importing numpy or any `fdars.mcp` symbol (Pitfall 4). Then `import numpy as np`; `from fdars import datasets`; `from fdars.mcp._registry import registry`; `from fdars.mcp._runner import run_method`; `from fdars.mcp._compare import compare_run`. In `main()`: call `registry.clear()` first (Pitfall 7); load `ds = datasets.load_canadian_weather()`; extract `X = np.asarray(ds.data.data, dtype=float)` and `day = np.asarray(ds.argvals, dtype=float)` using the EXACT access pattern from mcp_recipe.py (do not invent alternate attribute paths — Assumption A5); `dataset_id = registry.store_dataset(X, day)`; `before_result = run_method(dataset_id, "smoothing", n_basis=15)`; `before_result_id = registry.store_result(before_result)`; `compare_result = compare_run(dataset_id, "smoothing", before_result_id, {"n_basis": 25})`; then print the before/after/delta block iterating the delta items with the signed-value formatting from mcp_recipe.py lines 110-117, using the same Delta header literal that Task 1's test greps for. Leave the `advise()` / ANTHROPIC_API_KEY branch for Plan 02 — this tracer is offline-only. Use separator comments per section. This task lands the tracer numerics; do NOT stub the compute path.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && env -u ANTHROPIC_API_KEY .venv/bin/python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py && .venv/bin/python -m pytest tests/test_skill.py::test_walkthrough_script_offline tests/test_skill.py::test_walkthrough_delta_nonempty -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Running the script offline (ANTHROPIC_API_KEY unset) exits 0
    - Script stdout contains a Delta header line followed by >=1 value line
    - test_walkthrough_script_offline and test_walkthrough_delta_nonempty both pass
    - The version guard `if sys.version_info < (3, 10)` appears textually BEFORE the first `from fdars.mcp import` line
  </acceptance_criteria>
  <done>The offline walkthrough runs end-to-end, prints the 4-key delta, and the two smoke tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: First-cut SKILL.md manifest (frontmatter + install docs)</name>
  <files>.claude/skills/fdars-advisor/SKILL.md</files>
  <read_first>
    - .planning/phases/13-agent-skill-surface/13-RESEARCH.md (Pattern 1 SKILL.md frontmatter; SKILL.md Body minimal form; Pitfall 3 git-URL install; Pitfall 5 name==dir)
    - .planning/phases/13-agent-skill-surface/13-PATTERNS.md (section on SKILL.md — agentskills.io-compliant frontmatter; body structure Setup / Offline Walkthrough / Grounding Invariant)
    - ~/.claude/skills/graphify/SKILL.md (body format reference — numbered steps + fenced bash)
    - .planning/phases/13-agent-skill-surface/13-CONTEXT.md (D3 — Managed Agents env with allow_package_managers is the locked runtime)
  </read_first>
  <behavior>
    - File begins with a YAML front-matter block delimited by leading and trailing `---` lines.
    - Front-matter contains `name: fdars-advisor` (string-equal to the parent directory name) and a `description` naming the workflow (interpret / recommend / re-run / compare) and trigger scenarios (clustering, smoothing, FPCA, alignment, basis, parameter guidance, before/after).
    - Front-matter contains a `compatibility` field stating Python >=3.10 and pip/network requirement, plus the optional ANTHROPIC_API_KEY (per D3, SKILL-02).
    - Body documents the git-URL install as the authoritative path and the `fdars[mcp,advisor]` one-liner as the future path (Pitfall 3), and the offline run command for the walkthrough script.
    - Front-matter uses ONLY spec-permitted keys (name, description, compatibility, optional allowed-tools, metadata) — no `trigger`, no `dependencies`.
  </behavior>
  <action>Author `.claude/skills/fdars-advisor/SKILL.md` using the agentskills.io-compliant frontmatter from RESEARCH.md Pattern 1: `name: fdars-advisor`, a multi-line `description` (what + when, naming the trigger keywords), a `compatibility` field documenting Python >=3.10 + pip/network + optional ANTHROPIC_API_KEY, and `allowed-tools: Bash Read`. Keep the frontmatter keys strictly within the spec set. In the body, follow the graphify progressive-disclosure shape: a `## Setup` section giving the authoritative git-URL install command (`pip install "fdars @ git+https://github.com/sipemu/pyfda" mcp>=2.0.0 anthropic>=0.72.0 pydantic>=2.0`) AND the future one-liner (`pip install "fdars[mcp,advisor]"`) clearly labelled as available once fdars 0.3.0 ships the extras; an `## Offline Walkthrough` section giving the run command `python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py` and noting the 4-key delta output; and a short `## Grounding Invariant` note that every recommendation cites an fdars-computed diagnostics value. Reference the Phase 12 tools (fdars_run_method / fdars_compare_run) and the Phase 11 advisor by name so Success Criterion 1's "reference the Phase 12 tools" is met. Keep the body concise (well under the 500-line spec limit); detail lives in the script. Do NOT include any credentials or API-key values in the file.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_skill.py::test_skill_md_frontmatter -x -q && .venv/bin/python -c "import pathlib,re,yaml; t=pathlib.Path('.claude/skills/fdars-advisor/SKILL.md').read_text(); m=re.match(r'^---\n(.*?)\n---', t, re.DOTALL); fm=yaml.safe_load(m.group(1)); assert fm['name']=='fdars-advisor'; assert set(fm).issubset({'name','description','compatibility','allowed-tools','metadata'}); print('OK', sorted(fm))"</automated>
  </verify>
  <acceptance_criteria>
    - test_skill_md_frontmatter passes (name + description present and non-empty)
    - Parsed frontmatter `name` equals `fdars-advisor`
    - Frontmatter key set is a subset of {name, description, compatibility, allowed-tools, metadata}
    - Body contains the git-URL install string and the offline run command for the walkthrough script
  </acceptance_criteria>
  <done>SKILL.md exists with spec-valid frontmatter (name==dir), a compatibility field, and a Setup/Walkthrough body documenting the git-URL install and offline run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill runtime -> package index (git URL / PyPI) | `pip install` of fdars from a git URL crosses into remote code fetch at skill run time (D3 Managed Agents env) |
| env -> script | `ANTHROPIC_API_KEY` is read from the process environment (never from a file in the skill package) |
| caller -> compare_run | `params_after` values pass into `compare_run`, already allowlist-guarded by `_ALLOWED_PARAMS` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | git-URL pip install at runtime | medium | accept | Install only fixed, known packages (fdars from the project's own repo, mcp, anthropic, pydantic) named literally in SKILL.md; no user-supplied package names. Documented, not user-parameterised. |
| T-13-02 | Information Disclosure | ANTHROPIC_API_KEY | low | mitigate | Script and SKILL.md read the key only via `os.environ.get`; never hardcode, never write it to a file or stdout. Plan 01 keeps the offline path key-free. |
| T-13-03 | Tampering | params_after into compare_run | low | accept | Fixed literal `{"n_basis": 25}` in the walkthrough; `_ALLOWED_PARAMS` frozenset at the compare boundary already rejects unknown keys (Phase 12). No new surface added here. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_skill.py -x -q` — the three tracer smoke tests pass (or skip cleanly on Python <3.10).
- `env -u ANTHROPIC_API_KEY .venv/bin/python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py` exits 0 and prints the 4-key delta.
- SKILL.md frontmatter parses; `name == fdars-advisor`; key set within the spec-permitted set.
</verification>

<success_criteria>
- Offline walkthrough runs end-to-end and prints a non-empty deterministic delta (Success Criterion 3, offline portion).
- SKILL.md is spec-valid and documents the execution environment (SKILL-02).
- The tracer path (manifest -> script -> fdars.mcp compute -> test harness) is proven green in one commit.
</success_criteria>

<output>
Create `.planning/phases/13-agent-skill-surface/13-01-SUMMARY.md` when done.
</output>

---
phase: 11-python-api-surface
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - examples/advisor_recipe.py
autonomous: true
requirements: [PYAPI-03]
estimate:
  tokens: 40000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "examples/advisor_recipe.py runs to completion offline (load data → cluster → build_diagnostics → print) with no ANTHROPIC_API_KEY set (PYAPI-03)"
    - "When ANTHROPIC_API_KEY is present, the recipe additionally calls describe_cluster_differences(run_llm=True) and prints the grounded advice (PYAPI-03)"
  artifacts:
    - examples/advisor_recipe.py  # standalone end-to-end recipe script
  key_links:
    - "os.environ ANTHROPIC_API_KEY guard ↔ the describe_cluster_differences(run_llm=True) step"
    - "kmeans_fd result (centers/cluster/k) ↔ build_diagnostics(method='clustering', argvals=day)"
  prohibitions:
    - "examples/advisor_recipe.py MUST run to completion offline (through build_diagnostics) with the advise step guarded — it must not hard-fail without a key"
    - "Do NOT add the recipe to docs/examples/ or mkdocs.yml nav (markdown-exec would require a key at build time — RESEARCH.md Pitfall 5)"
    - "Do NOT modify python/fdars/advisor.py"
---

<objective>
Ship a standalone, runnable end-to-end recipe (`examples/advisor_recipe.py`) demonstrating the advisor workflow against a real dataset: load Canadian weather → cluster → build offline diagnostics → optionally get grounded LLM interpretation. The recipe degrades gracefully offline so it runs to completion without an API key.

Purpose: Satisfy PYAPI-03 — an `examples/` recipe demonstrating the advisor end-to-end against a real dataset.
Output: `examples/advisor_recipe.py` standalone script.
</objective>

## Artifacts this phase produces

This plan (11-03) produces:
- `examples/advisor_recipe.py` — standalone end-to-end advisor recipe script

Depends on 11-01, which wired `fdars.advisor` into the public API (the recipe imports `from fdars.advisor import build_diagnostics, describe_cluster_differences`).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-python-api-surface/11-RESEARCH.md
@.planning/phases/11-python-api-surface/11-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Standalone offline recipe (load → cluster → build_diagnostics → print)</name>
  <files>examples/advisor_recipe.py</files>
  <read_first>
    - 11-RESEARCH.md §5 "Recipe Page Format" (why a standalone .py, not a docs page) and §9 "PYAPI-03: Recipe script skeleton" and Pitfall 5 / Pitfall 6
    - examples/outlier_detection.py (read ONLY its module-docstring + `if __name__ == "__main__":` structure to match the existing script convention)
    - python/fdars/datasets.py (read ONLY load_canadian_weather return shape: raw matrix `ds.data.data`, grid `ds.argvals`)
    - python/fdars/advisor.py (read ONLY build_diagnostics clustering-branch keys L619-626 — do NOT modify)
  </read_first>
  <action>
    Create `examples/advisor_recipe.py` as a standalone script (NOT a docs/examples markdown page — Pitfall 5) following the existing `examples/*.py` convention: a module docstring describing the workflow and a `pip install fdars[advisor]` run hint, then the demo body. The offline core (must run with no key): load `fdars.datasets.load_canadian_weather()`, extract `X = np.asarray(ds.data.data, dtype=float)` and `day = np.asarray(ds.argvals, dtype=float)`, run `fdars.clustering.kmeans_fd(X, day, k=4, seed=42)` (use kmeans_fd directly per Pitfall 6 so the result carries `centers`/`cluster`/`k`), call `build_diagnostics(result, method="clustering", argvals=day)`, and print the cluster assignments and key diagnostic values (e.g. cluster sizes and an amplitude-separation figure). Import via `from fdars.advisor import build_diagnostics, describe_cluster_differences`. Do NOT call `advise()` or `describe_cluster_differences(run_llm=True)` in this task — the LLM step is added guarded in Task 2. The script must run end-to-end and exit 0 with no API key.
  </action>
  <verify>
    <automated>env -u ANTHROPIC_API_KEY python examples/advisor_recipe.py</automated>
  </verify>
  <acceptance_criteria>
    - `env -u ANTHROPIC_API_KEY python examples/advisor_recipe.py` exits 0 (runs to completion offline).
    - The script loads Canadian weather, clusters via `kmeans_fd`, calls `build_diagnostics(method="clustering", argvals=day)`, and prints diagnostic output.
    - The script does not import `anthropic` at module top and does not call `advise()`/`run_llm=True` in this task.
    - The script is at `examples/advisor_recipe.py`, not under `docs/examples/`.
  </acceptance_criteria>
  <precondition>The compiled `fdars` extension is importable (`maturin develop` has been run in the current env).</precondition>
  <done>The recipe runs the full offline advisor path (data → cluster → diagnostics → printed output) end-to-end with no API key.</done>
</task>

<task type="auto">
  <name>Task 2: Add the guarded LLM interpretation step</name>
  <files>examples/advisor_recipe.py</files>
  <read_first>
    - examples/advisor_recipe.py (as created by Task 1 — extend the body)
    - 11-RESEARCH.md §9 "PYAPI-03: Recipe script skeleton" (the `if os.environ.get("ANTHROPIC_API_KEY"):` guard block) and Pitfall 5
    - python/fdars/advisor.py (read ONLY the describe_cluster_differences signature L989-1100 and the Advice fields — do NOT modify)
  </read_first>
  <action>
    Extend `examples/advisor_recipe.py` with an `import os` and a guarded LLM step: `if os.environ.get("ANTHROPIC_API_KEY"):` call `describe_cluster_differences(result, argvals=day, domain_context="<Canadian weather stations clustered by daily temperature; 4 climate regions>", run_llm=True)`, then print `advice.interpretation` and iterate `advice.recommendations` printing each recommendation's `kind` and `action`. In the `else` branch, print a short message telling the user to set `ANTHROPIC_API_KEY` for the interpretation and note that offline diagnostics are already available. The guard ensures the script still exits 0 offline (Pitfall 5 / prohibition). Keep the Task 1 offline body intact above this block.
  </action>
  <verify>
    <automated>env -u ANTHROPIC_API_KEY python examples/advisor_recipe.py && python -c "import ast,sys; src=open('examples/advisor_recipe.py').read(); assert 'ANTHROPIC_API_KEY' in src and 'run_llm=True' in src and 'describe_cluster_differences' in src; ast.parse(src); print('recipe guard OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `env -u ANTHROPIC_API_KEY python examples/advisor_recipe.py` still exits 0 (LLM step skipped, offline path completes).
    - The script contains a `if os.environ.get("ANTHROPIC_API_KEY"):` guard wrapping a `describe_cluster_differences(..., run_llm=True)` call.
    - The `else` branch prints guidance to set the key; the script never hard-fails on a missing key.
    - The file parses as valid Python (`ast.parse`).
  </acceptance_criteria>
  <done>The recipe demonstrates the full interpret path when a key is present and degrades gracefully to offline diagnostics when it is not, satisfying PYAPI-03 end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe process → Anthropic API | Only crossed inside the ANTHROPIC_API_KEY guard |
| user data → LLM | Cluster diagnostics sent to Anthropic only when the user runs with a key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Denial of Service | recipe run without key | low | mitigate | LLM step wrapped in `os.environ.get("ANTHROPIC_API_KEY")` guard; offline path completes and exits 0 (Task 1/2 verify) |
| T-11-05 | Information Disclosure | API key in recipe source | low | accept | Key read from env only; never hardcoded (RESEARCH.md §13) |
| T-11-01 | Information Disclosure | user data → Anthropic (T-10-01 carryover) | low | accept | Only when the user explicitly runs with a key; documented accepted risk from Phase 10 |
</threat_model>

<verification>
- `env -u ANTHROPIC_API_KEY python examples/advisor_recipe.py` — exits 0, prints offline diagnostics, skips the LLM step.
- Static check: the file contains the `ANTHROPIC_API_KEY` guard, `run_llm=True`, and `describe_cluster_differences`, and parses as valid Python.
</verification>

<success_criteria>
- PYAPI-03: `examples/advisor_recipe.py` demonstrates the advisor end-to-end against a real dataset (build diagnostics → get advice → read recommendations), runs offline to completion, and uses the LLM only behind an env-key guard.
</success_criteria>

<output>
Create `.planning/phases/11-python-api-surface/11-03-SUMMARY.md` when done
</output>

---
phase: 23-packaging-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_aspect_provider_matrix.py
autonomous: true
requirements: [QUAL-01, QUAL-02]
estimate:
  tokens: 45000
  raw_tokens: 24000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The aspect × provider contract is exercised network-free and deterministically: every supported aspect's diagnostics flow through a fake native provider AND a fake fallback provider to produce a validated Advice (QUAL-01)."
    - "The offline aspect × provider matrix test runs with no network, no API key, no provider SDK installed (QUAL-01, QUAL-04 parity)."
    - "A confirmation assertion proves tests/test_advisor_live_integration.py holds exactly one env-gated live test per provider (openai, gemini, ollama) that skips cleanly with no keys/server (QUAL-02)."
  artifacts:
    - "tests/test_aspect_provider_matrix.py"
  key_links:
    - "build_diagnostics(aspect) → fake provider complete_structured → advise() grounding check → Advice"
    - "live-integration module introspection → one gated test per provider"
---

<objective>
Close the two remaining offline-coverage confirmations for Phase 23 with the
SMALLEST net-new surface that does not duplicate existing tests:
  - QUAL-01: add the MISSING aspect × provider CROSS-coverage assertion — feed
    each supported aspect's deterministic offline diagnostics through both a
    native-structured and a fallback fake provider and assert a grounded Advice
    comes back, network-free. Per-aspect determinism and per-adapter behavior
    already exist (Phases 20–21); this is the cross-product they do not cover.
  - QUAL-02: add a confirmation test that introspects
    tests/test_advisor_live_integration.py and asserts it has exactly one
    env-gated live test per provider that skips cleanly with no env — codifying
    the "one-per-provider, skips clean" contract rather than duplicating the live
    tests themselves.

Purpose: prove the aspect × provider contract holds offline+deterministically
(per D — QUAL-01) and lock the env-gated live-test contract (per D — QUAL-02)
without re-implementing existing per-aspect / per-adapter suites.
Output: `tests/test_aspect_provider_matrix.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-packaging-ci/23-CONTEXT.md

@tests/test_advisor_providers.py
@tests/test_advisor_live_integration.py
@python/fdars/advisor/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aspect × provider offline cross-coverage matrix test (QUAL-01)</name>
  <files>tests/test_aspect_provider_matrix.py</files>
  <behavior>
    - For each aspect that build_diagnostics produces deterministic offline
      diagnostics for (clustering, depth, outliers, classification, represent,
      regression, regression_cv, spm — plus alignment/fpca/basis/smoothing where a
      cheap offline fixture exists), running advise(diag, provider=FakeNative)
      returns an Advice instance whose evidence is grounded (no GroundingViolationError).
    - The same diagnostics flowed through a FakeFallback provider (non-native,
      supports_native_structured_output=False) also returns an Advice via the
      validate-and-retry path.
    - The whole test runs with no network, no API key, and no provider SDK
      importable (assert none of anthropic/openai/google.genai/ollama get imported).
    - The advice returned for a given aspect is grounded against THAT aspect's
      diagnostics — the fake provider's returned evidence cites only values present
      in the diagnostics dict, so _check_grounding passes.
  </behavior>
  <action>
Create tests/test_aspect_provider_matrix.py, the QUAL-01 aspect × provider
cross-coverage test. Do NOT duplicate the per-aspect determinism tests in
tests/test_advisor.py or the per-adapter tests in test_advisor_openai/ollama/
gemini.py — this file only covers the CROSS-product (aspect diagnostics ×
provider machinery) which no existing file exercises.

Reuse the fake-provider pattern from tests/test_advisor_providers.py: a
native-structured fake (supports_native_structured_output=True whose
complete_structured returns schema.model_validate(response)) and a fallback fake
(supports_native_structured_output=False returning a raw dict for the
validate-and-retry loop). Define lightweight local copies (or import if cleanly
importable) so this file is self-contained.

For each supported aspect, build its diagnostics offline via
fdars.advisor.build_diagnostics using a small deterministic numpy fixture
appropriate to that aspect (mirror the fixtures already used in
tests/test_advisor.py — e.g. represent uses {"data": np.ones((20,50)),
"argvals": np.linspace(0,1,50)}; depth/outliers/classification/regression/spm use
the same shapes those existing tests use). Then construct a fake provider whose
returned Advice cites an evidence value that IS present in the built diagnostics
(so grounding passes) — pull one real numeric key/value out of the freshly-built
diagnostics dict to form the evidence string, keeping the assertion robust across
aspects. Call advise(diag, task="interpretation", domain_context="matrix-test",
provider=<fake>) and assert the result is an Advice.

Parametrize over (aspect, fake_provider_kind) so the matrix is explicit in test
output. Where an aspect needs an argument beyond result+method (classification
needs n_classes; represent may need argvals), pass it through build_diagnostics.

Add one assertion that the module-level import surface stayed provider-free:
assert "anthropic" not in sys.modules or that importing this test module does not
require any provider SDK (guard SDK imports out entirely — none should appear).
Use pytest.importorskip("pydantic") at the top since Advice validation needs it.
Keep every fixture tiny and deterministic; no RNG, no wall-clock, no network.
  </action>
  <verify>
    <automated>/home/simonm/projects/rust/pyfda/.venv/bin/python -m pytest tests/test_aspect_provider_matrix.py -q</automated>
  </verify>
  <done>tests/test_aspect_provider_matrix.py passes fully offline; it parametrizes across every supported aspect × {native, fallback} fake provider and asserts a grounded Advice per cell with no network / no key / no provider SDK. The existing per-aspect and per-adapter suites are NOT duplicated (this file only adds the cross-product).</done>
</task>

<task type="auto">
  <name>Task 2: Live-integration contract confirmation — one gated test per provider, skips clean (QUAL-02)</name>
  <files>tests/test_aspect_provider_matrix.py</files>
  <action>
Append a confirmation test (QUAL-02) to tests/test_aspect_provider_matrix.py that
introspects tests/test_advisor_live_integration.py WITHOUT running its live
bodies, asserting the env-gated contract holds. Do NOT add new live tests and do
NOT call any provider — this only confirms the existing file's shape.

The confirmation must assert:
  - The live-integration module collects exactly three provider live tests, one
    each for openai, gemini, ollama (introspect the module's test function names,
    e.g. names containing "openai"/"gemini"/"ollama" and starting with "test_live").
  - With no FDARS_INTEGRATION / no API keys / no Ollama daemon in the environment,
    those three tests are SKIPPED, not errored — assert the module imports cleanly
    (no SDK ImportError at collection) and that each live test's skip gate
    expression is falsy in a clean env. Implement by importing the module and
    asserting the module-level gate booleans (_OPENAI_GATE, _GEMINI_GATE,
    _OLLAMA_GATE) are all False when the relevant env vars are unset — monkeypatch
    the env to be empty for the assertion so the check is deterministic regardless
    of the developer's real environment. If those gate names change, fall back to
    asserting each test carries a pytest skipif marker whose condition is falsy in
    a clean env.
  - Importing the live-integration module does not import any provider SDK (SDK
    imports are inside test bodies) — assert no anthropic/openai/google.genai/
    ollama entry appears in sys.modules purely as a result of importing it.

Keep this test offline and deterministic. It is a guard that the QUAL-02
contract (one env-gated live test per provider, clean skip) stays true, without
re-testing the providers themselves.
  </action>
  <verify>
    <automated>/home/simonm/projects/rust/pyfda/.venv/bin/python -m pytest tests/test_aspect_provider_matrix.py -q -k "live or contract"</automated>
  </verify>
  <done>The confirmation test passes offline: it introspects test_advisor_live_integration.py and asserts exactly one gated live test per provider (openai/gemini/ollama), that all three gates are falsy in a clean env (clean skip), and that importing the module pulls in no provider SDK. No live provider call is made; the existing live tests are not duplicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env → provider SDKs | tests must NOT import provider SDKs at collection; SDK imports stay inside live test bodies |
| fake provider → advise() grounding check | fabricated evidence must be rejected; only diagnostics-present values pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03 | Information Disclosure | live test env gates | medium | mitigate | Task 2 confirms gates are falsy in a clean env → no accidental live call / key usage in CI |
| T-23-04 | Spoofing | fake provider grounded evidence | low | mitigate | Task 1 evidence is pulled from real built diagnostics; _check_grounding still runs, proving grounding not bypassed |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs NO packages — pure test file using existing deps (numpy, pydantic already present); no new supply-chain surface |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_aspect_provider_matrix.py -q` passes fully offline.
- Full suite stays green: `.venv/bin/python -m pytest tests/ -q` (no regressions; live tests still skip).
- No provider SDK is importable/imported during these tests (offline + deterministic, QUAL-01).
</verification>

<success_criteria>
- New tests/test_aspect_provider_matrix.py passes offline, network-free, key-free.
- Aspect × {native, fallback} provider cross-product asserted with grounded Advice per cell (QUAL-01).
- Live-integration contract confirmed: one gated test per provider, clean skip in empty env (QUAL-02).
- No duplication of existing per-aspect determinism or per-adapter tests.
</success_criteria>

<output>
Create `.planning/phases/23-packaging-ci/23-02-SUMMARY.md` when done.
</output>

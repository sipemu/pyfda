---
phase: 28-advisor-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/aspects/scoring.py
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_prompts.py
  - python/fdars/mcp/server.py
  - tests/test_advisor_scoring.py
autonomous: true
requirements: [ADV-01]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "build_diagnostics(result, method='scoring') returns a deterministic JSON-serialisable dict summarizing the 5 caller-supplied fdars.scoring metrics (per D: caller computes, builder summarizes)"
    - "test_diagnostics_methods_match_advisor_supported stays GREEN after the atomic commit (advisor _supported and MCP _DIAGNOSTICS_METHODS both grow to 13, in ONE commit, per D)"
    - "_RUNNABLE_METHODS stays 6 (scoring is diagnostics-only in MCP, per D)"
    - "advise(...) supports scoring via _ASPECT_PRIMERS['scoring'] for the interpretation/parameter/method task families (full grounded treatment, per D)"
    - "the scoring diagnostics are offline-deterministic (byte-identical repeat json.dumps) with NO numpy scalars, and evidence cites a real fdars-computed metric value (grounding invariant, per D)"
  artifacts:
    - python/fdars/advisor/aspects/scoring.py
    - "scoring entry in advisor.build_diagnostics._supported + dispatch branch (advisor/__init__.py)"
    - "scoring entry in mcp/server.py _DIAGNOSTICS_METHODS (13 total)"
    - "_ASPECT_PRIMERS['scoring'] primer (advisor/_prompts.py)"
    - tests/test_advisor_scoring.py
  key_links:
    - "advisor._supported (advisor/__init__.py) == mcp._DIAGNOSTICS_METHODS (mcp/server.py) — asserted by tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported; both edited in the SAME commit"
    - "build_diagnostics dispatch method_lc=='scoring' -> aspects/scoring.py::_build_scoring_diagnostics"
---

<objective>
ADV-01 tracer: add `scoring` as the NEW 13th advisor diagnostics aspect, wired end-to-end
in a SINGLE atomic commit so the MCP guard-sync test never goes red mid-phase.

This plan LEADS the phase (tracer-first, per planning_context). It proves the full advisor
wiring path — new aspect builder + build_diagnostics dispatch + `_supported` set + MCP
`_DIAGNOSTICS_METHODS` guard + `_ASPECT_PRIMERS` primer — on one method before the ADV-02
represent/alignment extensions build on the same pattern.

Purpose: cover functional prediction-scoring in the grounded advisor while preserving the
grounding invariant (fdars computes every number; the builder only summarizes; the LLM only
cites) and the guard-sync invariant (advisor supported set == MCP diagnostics set).
Output: `aspects/scoring.py`, the three guarded-set/dispatch edits, the primer, and offline
determinism + grounding tests.

LOCKED decisions honored (from 28-CONTEXT.md):
- The CALLER passes the 5 fdars-computed metrics (`functional_mae`/`mse`/`mape`/`msle`/
  `explained_variance`) as the `result` dict; the builder SUMMARIZES them. Do NOT add
  y_true/y_pred params to build_diagnostics and do NOT recompute metrics in Python.
- The three guarded edits (`scoring.py`+dispatch, `_supported`, `_DIAGNOSTICS_METHODS`) land
  in ONE commit so `test_diagnostics_methods_match_advisor_supported` stays green.
- `_RUNNABLE_METHODS` stays 6 — scoring is diagnostics-only in MCP.
- `scoring` gets the FULL grounded treatment: builder + primer, so `advise()` supports the
  interpretation/parameter/method task families for scoring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-advisor-extension/28-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- `python/fdars/advisor/aspects/scoring.py` — `_build_scoring_diagnostics(raw, **kwargs)` builder.
- `scoring` in `advisor.build_diagnostics._supported` + a `method_lc == "scoring"` dispatch branch (`advisor/__init__.py`).
- `scoring` in `mcp/server.py` `_DIAGNOSTICS_METHODS` (13 total; `_RUNNABLE_METHODS` unchanged at 6).
- `_ASPECT_PRIMERS["scoring"]` primer (`advisor/_prompts.py`).
- `tests/test_advisor_scoring.py` — offline-determinism + grounding + guard-sync tests.
(The represent/alignment diagnostic additions are delivered by 28-02.)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: scoring aspect wired end-to-end in ONE atomic commit — guard-sync green</name>
  <files>python/fdars/advisor/aspects/scoring.py, python/fdars/advisor/__init__.py, python/fdars/mcp/server.py</files>
  <read_first>
    - python/fdars/advisor/aspects/depth.py — closest analog: consumes a raw fdars object (array), returns a plain-Python JSON-serialisable dict, no numpy scalars. Model the new builder on this file's shape and docstring style.
    - python/fdars/advisor/__init__.py — lines ~124-138 (`_supported` set + the unsupported-method ValueError) and lines ~154-203 (the `method_lc == "..."` dispatch chain with lazy per-aspect imports). Add the `scoring` entry and dispatch branch here.
    - python/fdars/mcp/server.py — lines ~49-81 (`_RUNNABLE_METHODS` = 6, and `_DIAGNOSTICS_METHODS` = 12). Add `scoring` to `_DIAGNOSTICS_METHODS` ONLY.
    - tests/test_mcp_server.py — lines 503-566 `test_diagnostics_methods_match_advisor_supported`: it provokes the advisor's unsupported-method ValueError, parses the sorted `_supported` list via ast.literal_eval, and asserts set-equality with `_DIAGNOSTICS_METHODS`. This test MUST stay green — read it so you understand the exact set-equality contract before editing either set.
    - python/fdars/advisor/aspects/represent.py — the input-key resolution + None-guard + `float(...)` casting idiom (attribute-first / dict-fallback), reused for reading the 5 metric values out of the caller's result dict.
  </read_first>
  <behavior>
    - _build_scoring_diagnostics reads up to 5 metric values from the caller-supplied result dict by key: functional_mae, functional_mse, functional_mape, functional_msle, functional_explained_variance (accept the short aliases mae/mse/mape/msle/explained_variance too). Each present value is cast to a native float; absent keys map to None.
    - The returned dict has method == "scoring" and one field per metric plus summary fields: which error metric is largest among the present error metrics (a string key referencing the metric name), and whether explained_variance is high (>= 0.9) / moderate / low (< 0.5) when present — all derived by comparing the caller's fdars-computed numbers, never by recomputing a metric.
    - All returned values are native Python float/int/str/bool/None — no numpy scalars. Two calls on the same input dict return an equal dict and byte-identical json.dumps(sort_keys=True).
    - build_diagnostics(result, method="scoring") dispatches to _build_scoring_diagnostics via a lazy import branch matching the existing chain.
    - "scoring" is present in BOTH advisor._supported AND mcp._DIAGNOSTICS_METHODS; _RUNNABLE_METHODS is unchanged (still 6).
  </behavior>
  <action>
    Create `python/fdars/advisor/aspects/scoring.py` with `_build_scoring_diagnostics(raw, **kwargs) -> dict` following the depth.py pattern (module docstring stating offline/deterministic/no-numpy-scalar guarantees; native-Python casting on every value). The builder SUMMARIZES the caller's already-fdars-computed metrics per D — it does NOT import fdars.scoring and does NOT recompute any metric. Resolve each metric via the attribute-first/dict-fallback idiom from represent.py; cast present values with `float(...)`; absent -> None. Compute the "largest error metric" summary by comparing the present error metrics (mae/mse/mape/msle) and the explained-variance band by thresholding the present explained_variance value.

    In `python/fdars/advisor/__init__.py`: add `"scoring"` to the `_supported` set (with a trailing comment tag consistent with the existing entries, e.g. ADV-01), and add a `if method_lc == "scoring":` dispatch branch that lazy-imports `_build_scoring_diagnostics` and returns `_build_scoring_diagnostics(raw, **kwargs)`, placed adjacent to the existing branches.

    In `python/fdars/mcp/server.py`: add `"scoring"` to the `_DIAGNOSTICS_METHODS` frozenset (diagnostics-only tier, alongside outliers/classification/represent/…). Do NOT touch `_RUNNABLE_METHODS` — per D it stays 6.

    Commit all three edits (scoring.py + __init__.py dispatch/_supported + server.py _DIAGNOSTICS_METHODS) together as ONE atomic commit so the guard-sync set-equality test is green at the commit boundary. Do NOT split across commits.

    Note: the raw result reaching build_diagnostics for scoring is a plain dict of metric values; it hits the existing `dict(raw)` coercion guard harmlessly (it is already a dict). No change to the dispatcher's unwrap/coerce logic is required.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/python -c "from fdars.advisor import build_diagnostics; d=build_diagnostics({'functional_mae':0.8,'functional_mse':1.2,'functional_explained_variance':0.95}, method='scoring'); print(d['method'])"` prints `scoring`.
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/python -c "from fdars.mcp.server import _DIAGNOSTICS_METHODS, _RUNNABLE_METHODS; assert 'scoring' in _DIAGNOSTICS_METHODS and len(_DIAGNOSTICS_METHODS)==13 and len(_RUNNABLE_METHODS)==6, (len(_DIAGNOSTICS_METHODS), len(_RUNNABLE_METHODS)); print('ok')"` prints `ok`.
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -q` exits 0.
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; git log --oneline -1 --stat | grep -E "scoring.py|__init__.py|server.py"` shows all three files in the single latest commit (atomicity, per D).
  </acceptance_criteria>
  <done>build_diagnostics dispatches scoring; the guard-sync test is green; _DIAGNOSTICS_METHODS==13, _RUNNABLE_METHODS==6; all three edits are in one commit.</done>
  <reversibility rating="costly">Growing advisor _supported / MCP guarded set is reversible on a green suite (REVERSIBILITY_GATES=true) — no decision checkpoint needed.</reversibility>
</task>

<task type="auto">
  <name>Task 2: scoring primer for advise() — full grounded treatment</name>
  <files>python/fdars/advisor/_prompts.py</files>
  <read_first>
    - python/fdars/advisor/_prompts.py — `_ASPECT_PRIMERS` dict (lines ~45-95; `represent` primer at ~66) and `_system_prompt(task, aspect)` (lines ~102-238), which appends `_ASPECT_PRIMERS.get(aspect.lower(), "")` after the FDA primer and supports `_supported_tasks = {interpretation, parameter, method}`.
    - python/fdars/advisor/aspects/scoring.py — the diagnostic KEYS this builder emits (from Task 1), so the primer references the exact key names the LLM will see.
  </read_first>
  <action>
    Add an `_ASPECT_PRIMERS["scoring"]` entry: a short FDA-primer clause explaining the 5 functional prediction-scoring metrics and what "large error" / "high vs low explained_variance" mean in domain terms, referencing the exact diagnostic key names the scoring builder emits (from Task 1). Keep the clause style/length consistent with the neighbouring `represent`/`regression` primers. This makes `advise(diagnostics, task=..., aspect="scoring")` inject the scoring context for the interpretation/parameter/method task families (full grounded treatment, per D). Do NOT modify `_supported_tasks` or `_system_prompt`'s structure — only add the dict entry.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/python -c "from fdars.advisor._prompts import _ASPECT_PRIMERS, _system_prompt; assert 'scoring' in _ASPECT_PRIMERS; sp=_system_prompt('interpretation','scoring'); assert _ASPECT_PRIMERS['scoring'].strip()[:20] in sp; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/python -c "from fdars.advisor._prompts import _ASPECT_PRIMERS; print('scoring' in _ASPECT_PRIMERS)"` prints `True`.
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/python -c "from fdars.advisor._prompts import _system_prompt; sp=_system_prompt('parameter','scoring'); assert 'explained_variance' in sp; print('ok')"` prints `ok` (primer references a real diagnostic key).
  </acceptance_criteria>
  <done>_ASPECT_PRIMERS['scoring'] exists and is injected by _system_prompt for the interpretation/parameter/method tasks; no change to _supported_tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: offline-determinism + grounding + no-numpy-scalar tests for scoring</name>
  <files>tests/test_advisor_scoring.py</files>
  <read_first>
    - tests/test_advisor.py — lines 76-108 `test_depth_deterministic` (the `check_no_numpy` recursive walker + byte-identical `json.dumps(sort_keys=True)` pattern) and lines 60-74 `test_depth_build_diagnostics_basic`. Model the new scoring tests on these exactly.
    - python/fdars/advisor/providers/_validate.py — `_check_grounding(advice, diagnostics)` (line ~115) and `_extract_numbers(text)` (line ~180): grounding passes when every numeric token an evidence string cites also appears in the diagnostics. The grounding test asserts each scoring diagnostic value is discoverable in the built dict (no fabricated numbers) — it does NOT call the LLM.
    - python/fdars/advisor/aspects/scoring.py — the emitted keys (from Task 1), so the fixture and assertions reference real keys.
  </read_first>
  <behavior>
    - test_scoring_build_diagnostics_basic: build_diagnostics of a fixed 5-metric dict returns method=='scoring', each present metric echoed as a float, and json.dumps succeeds.
    - test_scoring_deterministic: two calls on the same fixed input produce an equal dict AND byte-identical json.dumps(sort_keys=True); the check_no_numpy recursive walker finds no np.generic anywhere in the output.
    - test_scoring_grounding: every numeric value present in the built scoring diagnostics is a finite plain float/int that appears when the dict is json-serialised — asserting the builder cites real fdars-supplied numbers, never fabricated ones (reuse _extract_numbers over json.dumps(diag) and assert each metric value's string form is present).
    - test_scoring_offline_no_network: the whole builder path runs with no ANTHROPIC_API_KEY and imports no anthropic/openai/network module (assert via building diagnostics without any provider import).
  </behavior>
  <action>
    Create `tests/test_advisor_scoring.py` with the four tests above, mirroring the `test_depth_deterministic` structure in tests/test_advisor.py (same `check_no_numpy` walker, same byte-identical json.dumps assertion). Use fixed literal metric dicts (no RNG, no fdars call in the test — the caller-supplied-metrics contract means the test passes literal numbers). For grounding, build the diagnostics and assert every metric float you put in is discoverable via `_extract_numbers(json.dumps(diag))` (import the helper from `fdars.advisor.providers._validate`). Do NOT gate any test on an API key — build_diagnostics is the offline core.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/pytest tests/test_advisor_scoring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/pytest tests/test_advisor_scoring.py -q` exits 0 with 4 tests passing.
    - `cd /home/simonm/projects/rust/pyfda &amp;&amp; ANTHROPIC_API_KEY= .venv/bin/pytest tests/test_advisor_scoring.py -q` exits 0 (offline, no key required).
    - Full suite has no regressions: `cd /home/simonm/projects/rust/pyfda &amp;&amp; .venv/bin/pytest tests/ -q` exits 0 with the collected count >= 396 (392 baseline + 4 new) and 0 failures. Do NOT pipe this run through tail.
  </acceptance_criteria>
  <done>Four scoring tests pass offline (no key), no numpy scalars leak, grounding holds, and the full suite is green with no regressions vs the 392 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build_diagnostics | Caller supplies the 5 fdars-computed scoring metrics as a plain dict; the builder trusts and summarizes them (does not recompute) |
| build_diagnostics → advise() (LLM) | Diagnostics dict crosses to the LLM which may only cite values present in it (grounding invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | advisor `_supported` vs MCP `_DIAGNOSTICS_METHODS` drift | high | mitigate | Both sets edited in ONE atomic commit (Task 1); `test_diagnostics_methods_match_advisor_supported` fails on any drift |
| T-28-02 | Information Disclosure | LLM fabricating a scoring number not from fdars | high | mitigate | Grounding invariant: builder only echoes caller-supplied fdars metrics; `_check_grounding` rejects cited numbers absent from diagnostics; grounding test in Task 3 |
| T-28-03 | Tampering | numpy scalar leaking into "JSON-serialisable" output → non-deterministic/unserialisable | medium | mitigate | Native `float(...)` casts on every value; `check_no_numpy` recursive-walker test asserts no `np.generic` |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages installed this plan (pure-Python advisor edits against already-shipped bindings) — no legitimacy gate needed |
</threat_model>

<verification>
- Guard-sync: `.venv/bin/pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -q` green.
- Scoring offline suite: `.venv/bin/pytest tests/test_advisor_scoring.py -q` green (4 tests), including with `ANTHROPIC_API_KEY=` unset.
- No regressions: `.venv/bin/pytest tests/ -q` exits 0, collected >= 396, 0 failures (do not pipe through tail).
- Atomicity: latest commit contains scoring.py + advisor/__init__.py + mcp/server.py together.
</verification>

<success_criteria>
- `scoring` is a supported build_diagnostics method producing a deterministic, JSON-serialisable, no-numpy-scalar dict summarizing the 5 caller-supplied fdars metrics.
- advisor `_supported` and MCP `_DIAGNOSTICS_METHODS` are both 13 and equal; `_RUNNABLE_METHODS` stays 6; guard-sync test green; the three edits are one commit.
- `_ASPECT_PRIMERS['scoring']` enables `advise()` scoring coverage for interpretation/parameter/method.
- Grounding invariant holds (evidence cites real fdars numbers); full suite green with no regressions.
</success_criteria>

<output>
Create `.planning/phases/28-advisor-extension/28-01-SUMMARY.md` when done.
</output>
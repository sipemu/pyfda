---
phase: 45-svg-fix-regression-inference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/functional-glm.svg
  - docs/assets/diagrams/elastic-multinomial.svg
  - docs/assets/diagrams/scalar-on-function.svg
  - docs/assets/diagrams/inference-permutation-test.svg
autonomous: true
requirements: [SVGFIX-01, SVGFIX-02, SVGFIX-03, SVGFIX-04]
estimate:
  tokens: 85000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every flagged regression/inference diagram renders on the built site (rsvg-convert PNG) with no overlapping labels and no text overflowing its container rect — consistent spacing/alignment/sizing; specifically elastic-multinomial.svg's OvR panels read cleanly (SC1, SVGFIX-01)."
    - "Every diagram in this batch conforms to STYLE_SPEC.md — 720-width viewBox (300 or 480 height), the .ttl/.sub/.lab/.sm/.mono CSS classes, system-ui fonts, role=\"img\" + aria-label (SC2, SVGFIX-02)."
    - "Every diagram in this batch is method-accurate against the shipped fdars bindings — functional-glm.svg's Gamma-family link annotation (inverse g(μ)=1/μ, ≠ log-link) matches src/regression_mod.rs behavior and docs/regression/functional-glm.md; no diagram misdepicts what its method does (SC3, SVGFIX-04)."
    - "Each changed diagram passes SVGO idempotence: svgo@3.3.4 --config svgo.config.mjs second pass byte-identical (SC4, SVGFIX-03)."
    - "The 15 OK regression/inference diagrams are byte-unchanged — git diff --name-only lists only files within the 4 flagged paths (SC4/SC5, no-churn decision)."
    - "Across Phases 43–45 all AUDIT-01 fix-list diagrams are addressed — this is the final section batch; the regression/ and inference/ sections passed a built-site PNG review before their commits landed (SC5)."
  artifacts:
    - "docs/assets/diagrams/functional-glm.svg (Gamma-link method-accuracy VERIFIED correct → kept; XML zero redundant strips → likely byte-unchanged; confirmation recorded in SUMMARY)"
    - "docs/assets/diagrams/elastic-multinomial.svg (de-cramped: viewBox height bump 300→480 with re-spaced OvR panels, WIDTH stays 720; no redundant overrides stripped — all inline .mono font-size=10 differ from class 12)"
    - "docs/assets/diagrams/scalar-on-function.svg (β̂(t) inset prominence: conservative enlargement OR left as-is; judgment surfaced in SUMMARY for Phase 49 human review)"
    - "docs/assets/diagrams/inference-permutation-test.svg (XML: zero redundant strips — the lone inline .mono font-size=11 differs from class 12 → likely byte-unchanged; confirmation recorded in SUMMARY)"
  key_links:
    - "STYLE_SPEC.md class sizes (.ttl=17 .sub=12 .lab=13 .sm=11 .mono=12) decide which inline font-size= is REDUNDANT (strip) vs INTENTIONAL (keep) — the confirmed Phase 43/44 lesson. All four files' inline font-size are on .mono elements with value 10 or 11 ≠ 12, so ALL are intentional (zero strips)."
    - "The shipped GLM binding (src/regression_mod.rs:1091,1143) + docs/regression/functional-glm.md:34,36-37 resolve the functional-glm.svg Gamma-link method-accuracy FLAG: inverse canonical link g(μ)=1/μ, explicitly NOT log-link — the diagram is CORRECT as shipped."
    - "docs/regression/scalar-on-function.md:11 (β(t) is 'the object of interest') is the reference for the scalar-on-function.svg β̂(t)-prominence pedagogical judgment surfaced for Phase 49."
    - "svgo.config.mjs + pinned svgo@3.3.4 is the per-diagram determinism gate; .venv + rsvg-convert is the section visual gate. NO whole-site mkdocs build (Phase 49 / GATE-01)."
---

<objective>
Correct all flagged concept diagrams in the regression/ and inference/ sections (the Phase 45 bucket of 42-AUDIT.md §2 — 4 flagged of 19) on all four fix axes — visual/layout, STYLE_SPEC conformance, XML source formatting, and method-accuracy against the shipped fdars bindings — and verify each on the built site via rendered PNG. This is the THIRD and final section-batched SVG-fix phase; it completes the full 61-diagram sweep across Phases 43–45. The 15 OK diagrams in this batch stay byte-unchanged.

Purpose: Complete SVGFIX-01..04 for the regression/inference batch, reusing the correction + per-diagram-gate + per-section-review pattern established in Phases 43/44.
Output: Up to 4 corrected `docs/assets/diagrams/*.svg` files (no new files, no docs `.md` edits), committed as section commits (regression / inference). Per the plan-time verification below, only elastic-multinomial.svg (and possibly scalar-on-function.svg) is expected to change materially; functional-glm.svg and inference-permutation-test.svg are expected byte-unchanged after per-element re-confirmation.

Scope boundaries (from 45-CONTEXT.md — non-negotiable):
- Edit ONLY the 4 flagged SVGs listed in `files_modified`. Do NOT touch the 15 OK diagrams (function-on-scalar, classification, elastic-regression, scalar-on-shape, concurrent-regression, cross-validation, regression-diagnostics, uncertainty-quantification, explainability, conformal-prediction, conformal-classification, robust-regression, inference-anova, inference-scb, itp-interval-inference), any docs `.md` page, or create new diagrams.
- Do NOT run a whole-site `mkdocs build --strict` — deferred to Phase 49 (GATE-01). The per-diagram SVGO idempotence + rsvg-convert PNG render IS the correct per-phase proof.
- PNGs are written to the scratchpad only and are NEVER committed.
- Genuine pedagogical judgment calls (scalar-on-function.svg β̂(t) prominence) are fixed conservatively and SURFACED in the SUMMARY for the Phase 49 blocking human diagram review — do NOT guess a redesign.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-svg-fix-regression-inference/45-CONTEXT.md
@.planning/phases/42-diagram-audit/42-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
</context>

<pre_flight>
The redundant-vs-intentional override classification and the method-accuracy FLAGs were RESOLVED against the class sizes and shipped bindings during planning. Executors: re-confirm per element, do not re-guess.

**Class-size reference (STYLE_SPEC.md §Canonical style Block):** `.ttl`=17px, `.sub`=12px, `.lab`=13px, `.sm`=11px, `.mono`=12px. An inline `font-size=` is REDUNDANT (strip it — the class already supplies it) ONLY when its value equals the element's class value. When the inline value DIFFERS from the class value it is an INTENTIONAL per-element override → KEEP it byte-for-byte (the confirmed Phase 43/44 lesson: Phase 43 left 5 files byte-unchanged for exactly this reason). Verify per element before removing. `fill="..."` presentation attributes (not `style="fill:..."`) that carry a semantic data-curve-palette color are INTENTIONAL → KEEP.

**Per-file override survey done at plan time (re-verify, then act):**
- `functional-glm.svg`: the ONLY inline `font-size=` are 6 `.mono` elements at `font-size="10"` (class `.mono`=12) → all DISTINCT → INTENTIONAL → KEEP. The Gamma annotation uses `fill="#dc3545"` (a presentation attribute, a real FDARS_COLORS red — NOT a `style="fill:..."` override and NOT a font-size) → semantic → KEEP. Expected: ZERO strips → BYTE-UNCHANGED.
- `elastic-multinomial.svg`: the ONLY inline `font-size=` are 6 `.mono` elements at `font-size="10"` (class 12) → all DISTINCT → INTENTIONAL → KEEP. Zero redundant overrides. The real fix here is VISUAL (de-cramp), not XML.
- `inference-permutation-test.svg`: the ONLY inline `font-size=` is 1 `.mono` element at `font-size="11"` (class 12) → DISTINCT → INTENTIONAL → KEEP. Expected: ZERO strips → BYTE-UNCHANGED.
- `scalar-on-function.svg`: NO inline `font-size=` at all → zero XML work. The only open item is the β̂(t)-prominence pedagogical judgment.

**functional-glm.svg Gamma-link method-accuracy FLAG — RESOLVED at plan time against shipped sources (LOAD-BEARING):**
The diagram's Gamma branch reads `inverse   g(μ) = 1/μ` and `≠ log-link (R default)`. This is CORRECT and matches the shipped fdars GLM exactly — do NOT change it. Evidence:
  1. `src/regression_mod.rs:1091` (internal caveat): "Gamma uses inverse canonical link g(μ)=1/μ, NOT log-link (unlike R default)."
  2. `src/regression_mod.rs:1143` (public docstring): family `"gamma"` → "(inverse link, NOT log)".
  3. `docs/regression/functional-glm.md:34` (link table): `"gamma"` → **inverse (canonical)** → `1/μ` → "NOT log-link".
  4. `docs/regression/functional-glm.md:36-37` (warning admonition): "uses the inverse canonical link g(μ) = 1/μ, not the log-link that R's glm(..., family=Gamma) defaults to."
The diagram's `≠ log-link (R default)` note is pedagogically ACCURATE (R contrast is confirmed by the page's own warning), not misleading. Method-accuracy axis for functional-glm.svg = PASS, no content edit. The other three link branches (gaussian identity, binomial logit, poisson log) are also standard and correct.

**scalar-on-function.svg β̂(t)-prominence FLAG — pedagogical judgment (NOT a factual error):**
The page prose states β(t) is "the object of interest" (`docs/regression/scalar-on-function.md:11`). The diagram currently shows β̂(t) only as a small 164×40 inset (`scalar-on-function.svg` group `translate(516,210)`) at the bottom of Panel 3, secondary to the fitted-vs-actual scatter. This is a genuine design judgment, not a method error — nothing in the diagram is WRONG. Per 45-CONTEXT.md: apply a CONSERVATIVE improvement (e.g. mild enlargement / relabel of the existing inset within Panel 3, without adding a new panel or moving the other panels) OR leave it, and SURFACE the decision in the SUMMARY for the Phase 49 blocking human review. Do NOT redesign the panel.

**elastic-multinomial.svg visual FLAG — the one substantive edit this phase:**
Layout is single-row: Functional Data (x14 w120) | One-vs-Rest panel (x168 w256, holding 3 stacked OvR rows) | Softmax (x458 w122) | Output (x612 w94), all inside `viewBox="0 0 720 300"`. The three OvR rows (y=88/138/188, each h=42) are stacked tightly in the 184px-tall orange panel; per the audit they read cramped at 300 height. Per 45-CONTEXT.md the de-cramp is a HEIGHT bump 720×300 → 720×480 (viewBox WIDTH stays 720 — STYLE_SPEC-conformant, 480 is a canonical allowed height) with the OvR rows re-spaced to give each row breathing room. Method content (K one-vs-rest elastic binary classifiers → softmax → class probabilities, class labels aa/ao/dcl, scores s₁/s₂/s₃) is accurate — PRESERVE it verbatim; only geometry/spacing changes.

**Tooling confirmed present at plan time:** `svgo.config.mjs` (repo root), `svgo@3.3.4` (via `npx`), `/usr/bin/rsvg-convert`. All 4 target files already have `viewBox="0 0 720 300"`, `role="img"`, `aria-label`, and the five canonical CSS classes — so NO viewBox width migration is needed (only elastic-multinomial.svg's HEIGHT changes, width stays 720).
</pre_flight>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Verify + finalize functional-glm.svg end-to-end through the full per-diagram gate — the load-bearing Gamma-link method-accuracy check + XML re-confirmation</name>
  <read_first>
    - docs/assets/diagrams/functional-glm.svg (the Gamma branch: `<text class="mono" ... fill="#dc3545" font-size="10">gamma</text>` and the two annotation lines `inverse   g(μ) = 1/μ` and `≠ log-link (R default)` at the bottom of the orange "Link function by family" panel; the four link branches gaussian/binomial/poisson/gamma; the 6 `.mono font-size="10"` labels X:(n,m), S:(n,K), gaussian, binomial, poisson, gamma)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical style Block — .mono=12 decides the inline font-size=10 is INTENTIONAL not redundant; §viewBox; §Accessibility — the file already conforms, do not regress it; §Data-Curve Palette — #dc3545 is the canonical red, so fill="#dc3545" is a semantic keep)
    - src/regression_mod.rs lines 1069-1092 (family_from_str dispatch + the DOCS caveat block: "Gamma uses inverse canonical link g(μ)=1/μ, NOT log-link (unlike R default)") and lines 1130-1167 (functional_glm docstring: family "gamma" = "inverse link, NOT log")
    - docs/regression/functional-glm.md lines 27-39 (the Link-functions-by-family table row for gamma = inverse (canonical) 1/μ NOT log-link, and the "Gamma family uses the inverse canonical link, not log" warning admonition)
    - 42-AUDIT.md §1 regression row + §2 Phase 45 entry for functional-glm.svg (Method-accuracy + Minor XML FLAG)
  </read_first>
  <files>docs/assets/diagrams/functional-glm.svg</files>
  <action>
    This is the tracer: prove the full verify -> SVGO-idempotence -> rsvg-PNG -> section-review pipeline on the diagram that carries the load-bearing method-accuracy check, before scaling to the other three.

    Axis A (method-accuracy, SVGFIX-04) — the load-bearing check: CONFIRM (do not change) that the Gamma branch annotation `inverse   g(μ) = 1/μ` and `≠ log-link (R default)` matches the SHIPPED fdars GLM. The shipped behavior (verified at plan time) is: Gamma family uses the INVERSE canonical link g(μ)=1/μ, explicitly NOT the log-link R defaults to (src/regression_mod.rs:1091 and :1143; docs/regression/functional-glm.md:34 and the :36-37 warning). The diagram is therefore CORRECT as shipped — leave the Gamma annotation, the R-contrast note, and the other three link branches (gaussian identity g(μ)=μ, binomial logit, poisson log) unchanged. Do NOT default to R conventions; the whole point of the annotation is that fdars differs from R here. Record the confirmation (with the src line evidence) in the SUMMARY.

    Axis C (XML, SVGFIX-02/03): The 6 inline `.mono font-size="10"` are all DISTINCT from the class value (.mono=12) → INTENTIONAL per-element size reductions → KEEP them byte-for-byte (the confirmed Phase 43/44 lesson). The Gamma `fill="#dc3545"` is a presentation attribute carrying the canonical FDARS red (STYLE_SPEC Data-Curve Palette) — semantic, NOT a redundant style override → KEEP. There are NO redundant overrides to strip. Do NOT touch viewBox, role, aria-label, the five classes, positions, colors, or paths.

    Expected outcome: functional-glm.svg is BYTE-UNCHANGED (method-accuracy confirmed correct, zero redundant XML to strip). This is a CORRECT outcome, not a miss — it mirrors the Phase 43 "5 files left byte-unchanged" precedent. Only edit if per-element re-inspection surfaces a genuine exact class-size duplicate (none expected). Still run the full gate below to PROVE the file passes SVGO idempotence + renders + conforms, establishing the pipeline for Tasks 2-3.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && sv=docs/assets/diagrams/functional-glm.svg && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/fglm.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/fglm.p1.svg" --output "$SC/fglm.p2.svg" && cmp "$SC/fglm.p1.svg" "$SC/fglm.p2.svg" && rsvg-convert "$sv" -o "$SC/functional-glm.png" && test -s "$SC/functional-glm.png" && grep -q "viewBox=\"0 0 720" "$sv" && grep -q "role=\"img\"" "$sv" && grep -q "aria-label" "$sv" && grep -qE "g\(.\) = 1/." "$sv" && grep -q "log-link" "$sv" && grep -qE "pub fn functional_glm|fn family_from_str" src/regression_mod.rs && grep -qiE "inverse canonical link|NOT log-link|inverse link, NOT log" src/regression_mod.rs && echo GATE_OK'</automated>
    <human-check>Open $SC/functional-glm.png: the Gamma branch still reads "inverse g(μ) = 1/μ" and "≠ log-link (R default)" in red; the gaussian/binomial/poisson branches are unchanged; no panel moved. Confirms the load-bearing method-accuracy annotation matches the shipped inverse-link behavior. This is the regression/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - functional-glm.svg Gamma-link annotation `inverse g(μ)=1/μ` / `≠ log-link (R default)` is UNCHANGED and matches src/regression_mod.rs (grep confirms `inverse canonical link` / `NOT log-link` in the binding) — the diagram is verified method-accurate against shipped behavior, not R conventions.
    - Every inline `font-size=` that remains is on a `.mono` element with value 10 (≠ class 12) — no remaining exact class-size duplicate; the semantic `fill="#dc3545"` preserved.
    - viewBox `0 0 720 300`, `role="img"`, `aria-label`, and all five CSS classes still present (grep-confirmable; unchanged).
    - SVGO idempotence: 2nd pass byte-identical (`cmp` exits 0); non-empty PNG in scratchpad (never committed).
    - functional-glm.svg is byte-unchanged (`git diff --quiet` passes) UNLESS a genuine exact class-size duplicate was found and stripped; the confirmation (with src line evidence) is recorded in the SUMMARY.
    - `git diff --name-only` for this task lists at most docs/assets/diagrams/functional-glm.svg (expected: nothing).
  </acceptance_criteria>
  <reversibility rating="reversible">Verification-only for the method-accuracy axis; any XML edit is a hand-authored SVG change tracked in git, revertible with `git checkout`.</reversibility>
  <done>functional-glm.svg's Gamma-link annotation is confirmed method-accurate against the shipped inverse-canonical-link GLM (src/regression_mod.rs:1091,1143 + docs/regression/functional-glm.md:34,36-37) and left unchanged; the file passes SVGO idempotence + renders cleanly + stays STYLE_SPEC-conformant (expected byte-unchanged). The full per-diagram pipeline is proven, ready to scale to Tasks 2-3. It lands in the regression/ section commit.</done>
</task>

<task type="auto">
  <name>Task 2: De-cramp elastic-multinomial.svg (viewBox height 300→480 + re-space OvR panels, width stays 720) + conservative scalar-on-function.svg β̂(t) prominence — then commit the regression/ section</name>
  <read_first>
    - docs/assets/diagrams/elastic-multinomial.svg (viewBox line 1 `0 0 720 300`; input panel x14 w120 h184; the One-vs-Rest orange panel x168 y58 w256 h184 with its three OvR rows at y=88/138/188 each h=42 lines 37-56; the Softmax panel x458 y88 w122 h142; the Output panel x612 y88 w94 h142; the 6 `.mono font-size="10"` labels — all distinct from class 12, KEEP)
    - docs/regression/elastic-multinomial.md (the method: K one-vs-rest binary elastic classifiers → softmax → class probabilities; confirm the class labels aa/ao/dcl and score notation s₁/s₂/s₃ match the page — preserve the method content verbatim)
    - docs/assets/diagrams/scalar-on-function.svg (Panel 3 group `translate(500,70)` scalar output; the β̂(t) inset group `translate(516,210)` — a 164×40 rect with `β̂(t)` label + a small red coefficient curve, currently secondary to the fitted-vs-actual scatter; NO inline font-size in this file)
    - docs/regression/scalar-on-function.md lines 7-11, 34, 39 (β(t) is "the object of interest"; the coefficient function is what the method estimates — the pedagogical reason the inset may warrant mild prominence)
    - docs/assets/diagrams/STYLE_SPEC.md (§viewBox — 720×480 is a canonical allowed height; §Canonical style Block; §Colour Palette — #dc3545 red)
  </read_first>
  <files>docs/assets/diagrams/elastic-multinomial.svg, docs/assets/diagrams/scalar-on-function.svg</files>
  <action>
    elastic-multinomial.svg (visual, SVGFIX-01): De-cramp the OvR panels by bumping the viewBox HEIGHT 300 → 480 (WIDTH stays 720 — STYLE_SPEC-conformant, 480 is a canonical allowed height) and re-spacing so the three OvR rows read cleanly. Give the One-vs-Rest orange panel more vertical room and space the three OvR rows (currently y=88/138/188, h=42, ~8px gaps) further apart; you have the full extra 180px of height to distribute. Keep the single-row left-to-right flow (Functional Data → OvR panel → Softmax → Output) and the arrows between panels aligned to the new vertical centers. PRESERVE all method content verbatim: the OvR class labels (class 0 (aa)/1 (ao)/2 (dcl) vs rest), the "elastic binary classifier → score sₖ" captions, the softmax formula P(y=k|x)=exp(sₖ)/Σexp(sⱼ), and the (n,K) probs / argmax→class / accuracy outputs. Keep the 6 `.mono font-size="10"` labels (distinct from class 12 → intentional). Update the `viewBox` and any panel `height`/`y` geometry only; do NOT change colors, text content, role, aria-label, or the five classes. Ensure no text overflows its rect on the PNG.
    scalar-on-function.svg (method-accuracy pedagogical judgment, SVGFIX-04): The β̂(t) coefficient curve — which the page calls "the object of interest" — is only a small 164×40 inset at the bottom of Panel 3. This is a DESIGN JUDGMENT, not a factual error (nothing in the diagram is wrong). Apply a CONSERVATIVE improvement OR leave it: e.g. mildly enlarge the existing β̂(t) inset within Panel 3 and/or make its `β̂(t)` label clearer, WITHOUT adding a new panel, without moving the Predictor-Curves / method / scalar-output panels, and without changing the fitted-vs-actual scatter. If on inspection the inset already reads adequately, LEAVE the file byte-unchanged. EITHER WAY, SURFACE the decision explicitly in the SUMMARY (what you changed vs deliberately left) for the Phase 49 blocking human diagram review. Do NOT redesign the panel or guess a large restructuring.
    After both changed files pass their gates and the regression/ PNG review, commit the regression/ section (this covers Task 1's functional-glm.svg confirmation + these two): `fix(diagrams): regression/ — elastic-multinomial de-cramp (720x480), scalar-on-function β(t) prominence, functional-glm Gamma-link verified`.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && ok=1; for f in elastic-multinomial scalar-on-function; do sv="docs/assets/diagrams/$f.svg"; git diff --quiet -- "$sv" && { echo "unchanged $f (ok)"; continue; }; npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/$f.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/$f.p1.svg" --output "$SC/$f.p2.svg" && cmp "$SC/$f.p1.svg" "$SC/$f.p2.svg" && rsvg-convert "$sv" -o "$SC/$f.png" && test -s "$SC/$f.png" && grep -qE "viewBox=\"0 0 720 (300|480)\"" "$sv" && grep -q "role=\"img\"" "$sv" && grep -q "aria-label" "$sv" || { echo "FAIL $f"; ok=0; }; done; grep -qE "viewBox=\"0 0 720 480\"" docs/assets/diagrams/elastic-multinomial.svg && grep -q "class 0 (aa) vs rest" docs/assets/diagrams/elastic-multinomial.svg && grep -q "Softmax" docs/assets/diagrams/elastic-multinomial.svg && [ $ok -eq 1 ] && echo GATE_OK'</automated>
    <human-check>Open the scratchpad PNGs for every changed file: elastic-multinomial.svg's three OvR rows now read cleanly with clear vertical spacing (no cramping) at 720×480, arrows still connect panel centers, all method text (aa/ao/dcl, scores, softmax formula) intact; scalar-on-function.svg's β̂(t) inset (if enlarged) is legible and Panel 3's other content is undisturbed. Part of the regression/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - elastic-multinomial.svg: viewBox is `0 0 720 480` (width 720 preserved), the three OvR rows are re-spaced so none is cramped/overlapping on the PNG, arrows connect the new panel centers, and ALL method content (OvR class labels aa/ao/dcl, scores s₁/s₂/s₃, softmax formula, outputs) is preserved verbatim (grep-confirmable for the class-0 label + Softmax); the 6 `.mono font-size="10"` kept.
    - scalar-on-function.svg: either a conservative β̂(t) inset enlargement/relabel within Panel 3 (no new panel, other panels unmoved) OR byte-unchanged; the decision is explicitly surfaced in the SUMMARY for Phase 49; no method content altered.
    - For each CHANGED file: SVGO idempotence 2nd pass byte-identical (`cmp` exits 0) + non-empty scratchpad PNG; viewBox 720-width / role / aria / five classes preserved (grep-confirmable).
    - `git diff --name-only` for this task lists only elastic-multinomial.svg and (if changed) scalar-on-function.svg (no OK diagram, no .md).
    - The regression/ section is committed as a single section commit (covering functional-glm confirmation + these two).
  </acceptance_criteria>
  <reversibility rating="reversible">All edits are hand-authored SVG changes tracked in git; either diagram can be reverted with `git checkout`.</reversibility>
  <done>elastic-multinomial.svg is de-cramped to 720×480 with re-spaced OvR panels and method content preserved; scalar-on-function.svg's β̂(t) prominence is either conservatively improved or deliberately left (surfaced for Phase 49); both changed files pass SVGO idempotence + render cleanly; the regression/ batch is committed as one section commit.</done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm inference-permutation-test.svg XML (expect byte-unchanged) + no-churn guard on the 15 OK diagrams + commit the inference/ section</name>
  <read_first>
    - docs/assets/diagrams/inference-permutation-test.svg (the ONLY inline `font-size=` is line 58 `<text class="mono" ... font-size="11">T_obs</text>` — value 11 ≠ class .mono 12 → INTENTIONAL, KEEP; everything else uses classes cleanly; viewBox `0 0 720 300`, role, aria present)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical style Block — .mono=12 makes the lone font-size=11 an intentional distinct value, not a redundant duplicate)
    - 42-AUDIT.md §1 inference row + §2 Phase 45 entry for inference-permutation-test.svg (Minor XML flag only; content OK)
    - 42-AUDIT.md §2 Phase 45 "OK diagrams (15)" list — the files that MUST stay byte-unchanged (function-on-scalar, classification, elastic-regression, scalar-on-shape, concurrent-regression, cross-validation, regression-diagnostics, uncertainty-quantification, explainability, conformal-prediction, conformal-classification, robust-regression, inference-anova, inference-scb, itp-interval-inference)
  </read_first>
  <files>docs/assets/diagrams/inference-permutation-test.svg</files>
  <action>
    For inference-permutation-test.svg, for the single text element carrying BOTH a CSS class and an inline `font-size=`: compare the inline value to the element's class value. The lone inline override is `class="mono" ... font-size="11"` on the `T_obs` label; `.mono`=12, so `11 ≠ 12` → this is an INTENTIONAL per-element size reduction → KEEP it byte-for-byte (the confirmed Phase 43/44 lesson). There are NO redundant overrides to strip. Do NOT touch positions, colors, paths, layout, text content, viewBox, role, aria-label, or the five classes.
    Expected outcome: inference-permutation-test.svg is BYTE-UNCHANGED (zero redundant overrides) → record the confirmation in the SUMMARY (a correct outcome, not a miss). Only strip if re-inspection finds a genuine exact class-size duplicate (none expected).
    No-churn guard: confirm that ONLY the 4 flagged files appear in `git diff --name-only -- docs/assets/diagrams` across the whole phase — the 15 OK regression/inference diagrams must be byte-unchanged. Then commit the inference/ section: `fix(diagrams): inference/ — permutation-test XML re-confirmed (no redundant overrides; byte-unchanged)`. (If inference-permutation-test.svg is byte-unchanged AND no other inference file changed, note in the SUMMARY that the inference/ section needs no diagram commit — mirror the Phase 44 "monitoring/advisor had nothing to commit" precedent — and the phase's only commit is the regression/ section commit from Task 2.)
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && sv=docs/assets/diagrams/inference-permutation-test.svg; if git diff --quiet -- "$sv"; then echo "unchanged permutation-test (ok)"; else npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/ipt.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/ipt.p1.svg" --output "$SC/ipt.p2.svg" && cmp "$SC/ipt.p1.svg" "$SC/ipt.p2.svg" && rsvg-convert "$sv" -o "$SC/ipt.png" && test -s "$SC/ipt.png" && grep -q "viewBox=\"0 0 720" "$sv" && grep -q "role=\"img\"" "$sv" || { echo FAIL; exit 1; }; fi; changed=$(git diff --name-only -- docs/assets/diagrams | grep -vE "functional-glm|elastic-multinomial|scalar-on-function|inference-permutation-test" | grep -c "\.svg" || true); [ "$changed" = "0" ] && echo GATE_OK'</automated>
    <human-check>If inference-permutation-test.svg changed, open $SC/ipt.png: renders pixel-identical (the intent — no redundant override to remove). Confirm via git status that the 15 OK regression/inference diagrams (function-on-scalar, classification, cross-validation, conformal-prediction, inference-anova, inference-scb, itp-interval-inference, etc.) are byte-unchanged. Final inference/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - inference-permutation-test.svg: byte-unchanged (`git diff --quiet` passes) UNLESS a genuine exact class-size duplicate was found and stripped; the lone `.mono font-size="11"` (≠ class 12) is preserved; the confirmation is noted in the SUMMARY.
    - If changed: SVGO idempotence 2nd pass byte-identical + non-empty scratchpad PNG; viewBox 720 / role / aria preserved.
    - `git diff --name-only -- docs/assets/diagrams` across the whole phase lists ONLY files within the 4 flagged paths — the 15 OK regression/inference diagrams are byte-unchanged.
    - The inference/ section is committed (or the SUMMARY records that inference/ needed no diagram commit because permutation-test was byte-unchanged, per the Phase 44 precedent).
  </acceptance_criteria>
  <reversibility rating="reversible">Verification-only / hand-authored SVG change tracked in git, revertible with `git checkout`.</reversibility>
  <done>inference-permutation-test.svg's lone inline override is confirmed intentional and the file is byte-unchanged (or minimally corrected if a real duplicate surfaced); the 15 OK regression/inference diagrams are confirmed byte-unchanged via the no-churn guard; the inference/ section is committed or recorded as needing no diagram commit — completing the full 61-diagram sweep across Phases 43–45.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed SVG | Hand-edited SVG source is committed and later rendered by MkDocs into the public site. |
| npx → svgo@3.3.4 | The idempotence gate shells out to an npm package by pinned version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Information Disclosure | functional-glm.svg misstates the Gamma link (e.g. "changed" to log to match R), teaching behavior that contradicts the shipped inverse-canonical-link GLM | high | mitigate | Task 1 CONFIRMS the annotation against src/regression_mod.rs:1091,1143 + docs/regression/functional-glm.md:34 and leaves it unchanged; acceptance criteria grep the diagram's `1/μ`/`log-link` text AND the binding's `inverse canonical link`/`NOT log-link` caveat. |
| T-45-02 | Tampering | Over-aggressive override stripping deletes an INTENTIONAL distinct inline size (the .mono font-size=10/11 tweaks), changing rendered geometry | medium | mitigate | Per-element compare against the class-size reference (only exact duplicates stripped; all four files' inline sizes differ from class → zero strips expected); SVGO idempotence + rsvg PNG catch geometry regressions. |
| T-45-03 | Tampering | The elastic-multinomial.svg height bump drops STYLE_SPEC conformance (non-canonical height, or width ≠ 720, or dropped role/aria) | medium | mitigate | Task 2 gate grep-asserts `viewBox="0 0 720 480"` (canonical height, width 720) + role + aria; method content grep-asserted preserved (class-0 label + Softmax). |
| T-45-04 | Tampering | Accidental edit/churn to one of the 15 OK regression/inference diagrams breaks the no-churn determinism guarantee | medium | mitigate | Task 3 gate greps `git diff --name-only -- docs/assets/diagrams` and fails if any non-flagged .svg changed; OK diagrams must stay byte-unchanged. |
| T-45-SC | Tampering | npm/npx install of svgo@3.3.4 | low | accept | svgo@3.3.4 is the project-pinned, CI-established gate tool (STYLE_SPEC.md §SVGO Invocation) in use since Phase 1; version pinned, not `latest`. No new package introduced. |
</threat_model>

<verification>
Per changed diagram (all tasks): `npx svgo@3.3.4 --config svgo.config.mjs` run twice → 2nd pass byte-identical (`cmp` exit 0), and `rsvg-convert` → non-empty PNG in the scratchpad (never committed). STYLE_SPEC conformance grep on every changed file: viewBox 720-width (300 or 480 height), `role="img"`, `aria-label` (the five classes are already present and untouched).
Method-accuracy: functional-glm.svg — the Gamma-link annotation (`1/μ` inverse, `≠ log-link (R default)`) is confirmed against the shipped binding (src/regression_mod.rs:1091,1143 "inverse canonical link ... NOT log-link") and docs/regression/functional-glm.md:34,36-37, and left unchanged; scalar-on-function.svg — the β̂(t)-prominence judgment is conservative and surfaced for Phase 49.
No-churn guard: `git diff --name-only -- docs/assets/diagrams` lists ONLY files within the 4 flagged paths — the 15 OK regression/inference diagrams (function-on-scalar, classification, elastic-regression, scalar-on-shape, concurrent-regression, cross-validation, regression-diagnostics, uncertainty-quantification, explainability, conformal-prediction, conformal-classification, robust-regression, inference-anova, inference-scb, itp-interval-inference) are byte-unchanged.
Explicitly NOT run this phase: whole-site `mkdocs build --strict` (deferred to Phase 49 / GATE-01).
</verification>

<success_criteria>
- SC1: Every flagged regression/inference diagram renders cleanly on the PNG check — no overlapping labels, no text overflowing its container rect (elastic-multinomial OvR panels de-cramped), consistent spacing/alignment/sizing.
- SC2: Every diagram in this batch conforms to STYLE_SPEC.md (720-width viewBox at a canonical 300/480 height, five CSS classes, system-ui, role="img"+aria-label) — preserved on every changed file.
- SC3: Every diagram in this batch is method-accurate against the shipped fdars bindings — functional-glm.svg's Gamma inverse-canonical-link annotation confirmed correct against src/regression_mod.rs (not R conventions).
- SC4: Each changed diagram passes SVGO idempotence; the 15 OK regression/inference diagrams are byte-unchanged.
- SC5: The regression/ and inference/ sections passed a built-site PNG review before their commits landed.
- Across Phases 43–45 all AUDIT-01 fix-list diagrams are corrected — this is the final section batch; any conservative pedagogical judgment (scalar-on-function.svg β̂(t) prominence) is surfaced in the SUMMARY for the Phase 49 blocking human diagram review.
</success_criteria>

<output>
Create `.planning/phases/45-svg-fix-regression-inference/45-01-SUMMARY.md` when done.
Include: per-diagram outcome (changed / confirmed-byte-unchanged); the functional-glm.svg Gamma-link method-accuracy resolution (VERIFIED correct — inverse canonical link g(μ)=1/μ, ≠ log-link, per src/regression_mod.rs:1091,1143 + docs/regression/functional-glm.md:34,36-37 → annotation kept unchanged); the elastic-multinomial.svg de-cramp outcome (720×300 → 720×480, OvR re-spacing); the redundant-vs-intentional override tally per file (functional-glm 0 strips / byte-unchanged; elastic-multinomial 0 strips; inference-permutation-test 0 strips / byte-unchanged); an explicit callout of the scalar-on-function.svg β̂(t)-prominence decision (conservatively improved OR deliberately deferred) for the Phase 49 human diagram review; and confirmation that the full 61-diagram sweep across Phases 43–45 is complete with the 15 OK regression/inference diagrams byte-unchanged.
</output>

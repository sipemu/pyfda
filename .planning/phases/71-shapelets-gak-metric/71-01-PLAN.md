---
phase: 71-shapelets-gak-metric
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shapelet_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_shapelet.py
autonomous: true
requirements: [SHAPE-01]
estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "import fdars.shapelet works and exposes discover_shapelets, shapelet_transform_fit, shapelet_transform, shapelet_classifier_fit, shapelet_distance (SHAPE-01, roadmap SC1)"
    - "shapelet_transform_fit returns a PyShapeletFit opaque handle whose transform output on non-square data has shape (n_new, K) (SHAPE-01, roadmap SC1)"
    - "shapelet_classifier_fit returns a PyShapeletClassifierFit handle exposing predict + train_accuracy (SHAPE-01, roadmap SC1)"
    - "QualityMeasure and ShapeletClassifier are dispatched by string; an invalid name raises ValueError listing valid variants (SHAPE-01, roadmap SC2)"
  artifacts:
    - src/shapelet_mod.rs
    - tests/test_shapelet.py
  key_links:
    - "lib.rs register_submodule!(m, \"shapelet\", shapelet_mod::register) wires the module into _native"
    - "python/fdars/__init__.py _submodule_names contains \"shapelet\" so fdars.shapelet imports"
    - "PyShapeletFit.inner is ShapeletTransformFit; shapelet_transform calls fit.inner.shapelets() to get &ShapeletSet"
---

<objective>
Create the new `fdars.shapelet` submodule (SHAPE-01): a `src/shapelet_mod.rs` binding five
shapelet functions, two opaque `#[pyclass]` handles (`PyShapeletFit` wrapping
`ShapeletTransformFit`, `PyShapeletClassifierFit` wrapping `ShapeletClassifierFit`), and two
string-dispatched `#[non_exhaustive]` enums (`QualityMeasure`, `ShapeletClassifier`) each with
a mandatory `Err` fallback arm. Register the module in `lib.rs` and `python/fdars/__init__.py`.

Purpose: SHAPE-01 delivers roadmap Phase 71 success criteria 1 (discover/transform/classifier
with a `PyShapeletFit` handle) and 2 (string enum dispatch with ValueError Err arms).
Output: `src/shapelet_mod.rs`, `tests/test_shapelet.py`, edits to `src/lib.rs` and
`python/fdars/__init__.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-shapelets-gak-metric/71-RESEARCH.md
@.planning/phases/71-shapelets-gak-metric/71-CONTEXT.md
@src/multi_fdata_mod.rs
@src/frechet_mod.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: shapelet_transform_fit → PyShapeletFit → shapelet_transform, end-to-end, registered</name>
  <files>src/shapelet_mod.rs, src/lib.rs, python/fdars/__init__.py, tests/test_shapelet.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Sections 1.1, 1.2, 1.4, 4.1, 5.1, 5.3, 6.1 — signatures, PyShapeletFit wraps ShapeletTransformFit, ShapeletDiscoveryConfig fields, registration, non-square fixture)
    - src/multi_fdata_mod.rs (opaque-handle #[pyclass] + guarded builder + register() template)
    - src/frechet_mod.rs (i64→usize label-array conversion + ValueError guard analog)
  </read_first>
  <behavior>
    - fdars.shapelet is importable after maturin develop.
    - shapelet_transform_fit(TRAIN, TRAIN_Y) returns a PyShapeletFit handle; fit.n_shapelets > 0; fit.n_train == 16.
    - shapelet_transform(fit, TEST) returns a 2D float64 array of shape (4, fit.n_shapelets) — non-square (n_test=4 ≠ n_train=16) proves no transposition bug.
  </behavior>
  <action>
    Create src/shapelet_mod.rs with the ONE end-to-end path only (discovery→handle→transform); leave the other functions for Tasks 2-3.

    Imports: use fdars_core::shapelet::{shapelet_transform_fit, shapelet_transform, QualityMeasure, ShapeletDiscoveryConfig, ShapeletTransformFit}; use crate::convert::{numpy2d_to_fdmatrix, fdmatrix_to_numpy2d, to_pyresult}; numpy PyReadonlyArray1/2, PyArray2; pyo3 prelude + PyValueError. If fdars_core::shapelet::* import fails at cargo check, fall back to the fully-qualified submodule paths (per RESEARCH Assumptions A1) — do not add unused imports (CI runs -D warnings).

    Define #[pyclass(name = "PyShapeletFit")] pub struct PyShapeletFit { pub inner: ShapeletTransformFit } mirroring PyMultiFunData. Add #[pymethods] getters n_shapelets (self.inner.shapelets().len()) and n_train (self.inner.features().nrows()).

    Add a private helper quality_from_str(s: &str) -> PyResult<QualityMeasure> matching "info_gain" and "f_statistic", Err wildcard raising PyValueError listing both valid names (per RESEARCH 3.1). Full implementation of the FStatistic arm is required now (not deferred) — it is one match arm.

    Add a private helper labels_i64_to_usize(labels: PyReadonlyArray1<i64>) -> PyResult<Vec<usize>> that guards each value >= 0 before casting (per RESEARCH Pitfall 5), raising PyValueError naming the offending index on a negative label.

    Bind shapelet_transform_fit with #[pyo3(signature = (data, labels, min_length=3, max_length=0, max_candidates=10000, max_shapelets=0, quality="info_gain", seed=0))]. Convert data via numpy2d_to_fdmatrix, labels via labels_i64_to_usize, quality via quality_from_str. Build ShapeletDiscoveryConfig { min_length, max_length, max_candidates: (if max_candidates == 0 { None } else { Some(max_candidates) }), max_shapelets, quality, seed } — struct is NOT non_exhaustive so all-named-fields construction is fine (RESEARCH 1.4, Pitfall 6). Call the core fn through to_pyresult, wrap the returned ShapeletTransformFit in PyShapeletFit, return Py<PyShapeletFit>.

    Bind shapelet_transform(fit: &PyShapeletFit, data) -> Bound<PyArray2<f64>>: convert data via numpy2d_to_fdmatrix, call fdars_core::shapelet::shapelet_transform(fit.inner.shapelets(), &mat) via to_pyresult (pass fit.inner.shapelets(), NOT fit.inner — per RESEARCH Pitfall 1), return fdmatrix_to_numpy2d.

    Add pub fn register(m) that adds PyShapeletFit and the two functions (mirror multi_fdata_mod::register). In src/lib.rs add `mod shapelet_mod;` (alongside the other mod lines) and `register_submodule!(m, "shapelet", shapelet_mod::register);` (after the famm line). In python/fdars/__init__.py add `"shapelet",` to _submodule_names after the "famm" entry with a Phase-71 comment.

    Create tests/test_shapelet.py with the RESEARCH 6.1 fixture block (RNG seed 42, N_OBS=20, N_PTS=30 triangular-motif 2-class data, TRAIN/TEST split with n_test=4 ≠ n_train=16) and tests test_fit_handle_accessors + test_transform_shape covering the behavior above.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qE 'register_submodule!\(m, "shapelet"' src/lib.rs && grep -q '"shapelet"' python/fdars/__init__.py && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -m pytest tests/test_shapelet.py::test_fit_handle_accessors tests/test_shapelet.py::test_transform_shape -x -q</automated>
    <fails_when>fdars.shapelet is not registered, PyShapeletFit is missing, shapelet_transform wraps ShapeletSet incorrectly, or the transform output is not (4, K).</fails_when>
  </verify>
  <acceptance_criteria>
    - `import fdars.shapelet` succeeds after `maturin develop`.
    - shapelet_transform_fit returns a PyShapeletFit with n_shapelets > 0 and n_train == 16.
    - shapelet_transform(fit, TEST) has shape (4, n_shapelets); dtype float64.
    - No unused-import warnings (cargo builds clean under -D warnings).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive new module + registration; revert by removing files and the two registration lines.</reversibility>
  <done>fdars.shapelet imports; the transform_fit→handle→transform path passes both tracer tests on non-square data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: discover_shapelets (summary dict) + shapelet_distance (tuple)</name>
  <files>src/shapelet_mod.rs, tests/test_shapelet.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Sections 1.1 discover_shapelets/shapelet_distance signatures, 1.2 ShapeletSet fields, 4.1, Open Question 1 — discover returns summary dict)
    - src/frechet_mod.rs (PyDict construction + scalar-tuple return analog)
  </read_first>
  <behavior>
    - discover_shapelets(TRAIN, TRAIN_Y) returns a dict with keys n_shapelets (int > 0) and quality (str "info_gain").
    - shapelet_distance(shapelet_z, series) returns a (float, int) tuple; distance is ~0.0 (< 1e-6) when the shapelet is an exact z-normalized window of the series, and best_offset is the correct start index.
    - An invalid quality string raises ValueError whose message lists info_gain and f_statistic.
  </behavior>
  <action>
    Bind discover_shapelets with the same signature as shapelet_transform_fit (RESEARCH 4.1). Call fdars_core::shapelet::discover_shapelets(&mat, &labels, &config) via to_pyresult to get a ShapeletSet. Return a PyDict (per RESEARCH Open Question 1 recommendation — discovery-only summary, NOT a handle): key "n_shapelets" = set.len() as int, key "quality" = the string form of the configured quality ("info_gain" or "f_statistic"). Do not attempt to return the ShapeletSet directly.

    Bind shapelet_distance with #[pyo3(signature = (shapelet_z, series, best_so_far=f64::INFINITY))] taking two PyReadonlyArray1<f64>. Convert each to a slice via .as_slice()? (or numpy1d_to_vec), call fdars_core::shapelet::shapelet_distance(shapelet_z_slice, series_slice, best_so_far) via to_pyresult, return the (f64, usize) as a Python tuple (dist, offset).

    Register both functions in shapelet_mod::register.

    Add tests: test_discover (asserts n_shapelets > 0 and quality == "info_gain"), test_distance (build a series and take an exact z-normalized window as the shapelet; assert returned distance < 1e-6 and offset equals the known window start), and test_quality_err_arm (pytest.raises(ValueError) on discover_shapelets(..., quality="bogus")).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -m pytest tests/test_shapelet.py::test_discover tests/test_shapelet.py::test_distance tests/test_shapelet.py::test_quality_err_arm -x -q</automated>
    <fails_when>discover_shapelets does not return the summary dict, shapelet_distance returns the wrong tuple shape/offset, or an invalid quality string does not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - discover_shapelets returns {n_shapelets: int, quality: str} with n_shapelets > 0.
    - shapelet_distance returns (float, int); exact-window distance < 1e-6 at the correct offset.
    - quality="bogus" raises ValueError listing info_gain and f_statistic.
  </acceptance_criteria>
  <done>discover_shapelets and shapelet_distance are bound, registered, and pass their three tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shapelet_classifier_fit → PyShapeletClassifierFit handle (predict + train_accuracy)</name>
  <files>src/shapelet_mod.rs, tests/test_shapelet.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Sections 1.3 ShapeletClassifierFit methods, 1.5 ShapeletClassifierConfig, 3.2 ShapeletClassifier dispatch, 4.1, Open Question 2 — recommended PyShapeletClassifierFit handle)
    - src/multi_fdata_mod.rs (opaque-handle + accessor pattern)
  </read_first>
  <behavior>
    - shapelet_classifier_fit(TRAIN, TRAIN_Y) returns a PyShapeletClassifierFit handle exposing n_shapelets (int), train_accuracy (float in [0,1]), classes (int64 array), n_classes (int).
    - handle.predict(TEST) returns a 1D int64 numpy array of length n_test (=4).
    - classifier="knn" (default k=1) and classifier="lda" both fit without error.
    - classifier="bogus" raises ValueError listing knn and lda.
  </behavior>
  <action>
    Per the resolved design decision (RESEARCH Open Question 2 recommendation), shapelet_classifier_fit returns a PyShapeletClassifierFit OPAQUE HANDLE (not a bare dict) — this is the phase's 3rd opaque handle and mirrors every other fitted model in pyfda. It takes raw data+labels (the independent path, RESEARCH 1.3 option 1), NOT a PyShapeletFit.

    Define #[pyclass(name = "PyShapeletClassifierFit")] pub struct PyShapeletClassifierFit { pub inner: ShapeletClassifierFit }. Add #[pymethods]: getter n_shapelets (self.inner.shapelets().len()), getter train_accuracy (self.inner.train_accuracy()), getter classes (self.inner.classes as int64 1D numpy array — cast usize→i64), getter n_classes (self.inner.classes.len()), and method predict(&self, py, new_data: PyReadonlyArray2<f64>) -> Bound<PyArray1<i64>>: convert new_data via numpy2d_to_fdmatrix, call self.inner.predict(&mat) via to_pyresult to get Vec<usize>, cast each to i64, return as a 1D int64 numpy array (use PyArray1::from_vec or vec-to-numpy path).

    Add a private classifier_from_str(classifier: &str, k: usize) -> PyResult<ShapeletClassifier> matching "knn" => Knn { k }, "lda" => Lda (unit variant — no fields, per RESEARCH Pitfall 4), Err wildcard listing knn and lda.

    Bind shapelet_classifier_fit with #[pyo3(signature = (data, labels, min_length=3, max_length=0, max_candidates=10000, max_shapelets=0, quality="info_gain", seed=0, classifier="knn", k=1, ncomp=None))]. Build ShapeletDiscoveryConfig (reuse the same construction as Task 1) then ShapeletClassifierConfig { discovery, classifier: classifier_from_str(classifier, k)?, ncomp } (ShapeletClassifierConfig derives Default and is not non_exhaustive — all-named construction fine). Call core through to_pyresult, wrap in PyShapeletClassifierFit, return Py<PyShapeletClassifierFit>.

    Register PyShapeletClassifierFit and shapelet_classifier_fit in shapelet_mod::register.

    Add tests: test_classifier_handle_accessors (assert n_shapelets > 0, 0.0 <= train_accuracy <= 1.0, classes dtype int64, n_classes == 2), test_classifier_predict_shape (handle.predict(TEST) is 1D int64 of length 4), test_classifier_lda (classifier="lda" fits and predicts without error), and test_classifier_err_arm (pytest.raises(ValueError) on classifier="bogus").
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -m pytest tests/test_shapelet.py::test_classifier_handle_accessors tests/test_shapelet.py::test_classifier_predict_shape tests/test_shapelet.py::test_classifier_lda tests/test_shapelet.py::test_classifier_err_arm -x -q</automated>
    <fails_when>the classifier handle lacks predict/train_accuracy, predict returns the wrong shape/dtype, the lda unit variant does not fit, or classifier="bogus" does not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - shapelet_classifier_fit returns a PyShapeletClassifierFit with n_shapelets > 0, train_accuracy in [0,1], int64 classes, n_classes == 2.
    - handle.predict(TEST) is a length-4 int64 1D array.
    - classifier="lda" fits and predicts; classifier="bogus" raises ValueError listing knn and lda.
  </acceptance_criteria>
  <done>The classifier handle with predict + train_accuracy is bound, registered, and passes all four tests.</done>
</task>

<task type="auto">
  <name>Task 4: Full shapelet suite + FND-02 registration guard green</name>
  <files>tests/test_shapelet.py</files>
  <read_first>
    - .planning/phases/71-shapelets-gak-metric/71-RESEARCH.md (Section 5.3 FND-02 superset invariant; Validation Architecture test map)
  </read_first>
  <action>
    Confirm the whole shapelet test file plus the FND-02 foundation guard pass together. Do NOT weaken FND-02 — adding "shapelet" to _submodule_names satisfies its superset invariant (RESEARCH 5.3); if it fails, the cause is a missing/mis-registered submodule, fix the registration, not the test. If any earlier test is flaky under low-signal random data, tighten only the fixture seed/motif strength documented in RESEARCH 6.1 — never relax an acceptance assertion.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_shapelet.py tests/sklearn/test_foundation.py -q</automated>
    <fails_when>any shapelet test fails or the FND-02 foundation registration guard regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - All tests in tests/test_shapelet.py pass.
    - tests/sklearn/test_foundation.py passes with "shapelet" present in _submodule_names.
  </acceptance_criteria>
  <done>The full shapelet suite and the FND-02 guard are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust binding | Untrusted numpy arrays + label arrays + enum strings cross into fdars-core |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering | labels_i64_to_usize | high | mitigate | Guard every label >= 0 before the i64→usize cast; negative label raises ValueError naming the index (RESEARCH Pitfall 5) |
| T-71-02 | Tampering | quality_from_str / classifier_from_str | medium | mitigate | #[non_exhaustive] enum Err wildcard raises ValueError listing valid variant names |
| T-71-03 | Denial of Service | numpy2d_to_fdmatrix / core discovery | medium | mitigate | Empty/mismatched matrices surface FdarError via to_pyresult → ValueError; no panic reaches Python |
</threat_model>

<verification>
- `.venv/bin/maturin develop` builds clean under CI `-D warnings` (no unused imports).
- `tests/test_shapelet.py` all green; `tests/sklearn/test_foundation.py` green (FND-02 superset holds).
- No new external Cargo dependency; Cargo.lock never git-added.
</verification>

<success_criteria>
- `import fdars.shapelet` works; the five functions + PyShapeletFit + PyShapeletClassifierFit are callable (roadmap SC1).
- QualityMeasure and ShapeletClassifier dispatch by string with ValueError Err arms (roadmap SC2).
- Non-square fixtures prove no transposition bug in transform and predict.
</success_criteria>

<output>
Create `.planning/phases/71-shapelets-gak-metric/71-01-SUMMARY.md` when done
</output>

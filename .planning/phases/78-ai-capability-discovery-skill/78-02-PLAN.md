---
phase: 78-ai-capability-discovery-skill
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - .claude/skills/fdars-capabilities/SKILL.md
autonomous: true
requirements: [SKILL-01]
use_worktrees: false
estimate:
  tokens: 45000
  raw_tokens: 24000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A new standalone skill `.claude/skills/fdars-capabilities/SKILL.md` exists with spec-valid frontmatter (name, description, compatibility, allowed-tools) mirroring the fdars-advisor schema (SKILL-01)."
    - "The SKILL.md description triggers on capability discovery AND explicitly says to use fdars-advisor for tuning/diagnostics/comparison — the non-duplication boundary is machine-readable (SKILL-01)."
    - "The SKILL.md body maps every public fdars module (a per-module summary table, not the full 409-callable inline) and references docs/llms.txt + the fdars_list_capabilities MCP tool for the full surface."
  artifacts:
    - .claude/skills/fdars-capabilities/SKILL.md
  key_links:
    - "SKILL.md body's per-module table is derived from python/fdars/_capability_map.json (78-01) so it cannot drift from the live surface; the module set matches the committed map keys"
---

<objective>
Ship SKILL-01: a NEW standalone capability-discovery Agent Skill at
`.claude/skills/fdars-capabilities/SKILL.md`, spec-valid and explicitly disjoint from the
existing `fdars-advisor` skill. It maps every public fdars module (what each does, when to
reach for it, how to call it) with a per-module summary table, and points to the full surface
(docs/llms.txt + the MCP tool). This is the first surface consuming the proven capability map
from 78-01.

Purpose: Give agents a discovery entry point ("what can fdars do?", "which method for X?",
"how do I call Y?") that routes distinctly from the advisor's tune/diagnose/compare loop.
Output: the new skill directory + SKILL.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_produced>
NEW files this plan creates:
- `.claude/skills/fdars-capabilities/SKILL.md` — the standalone capability-discovery skill (SKILL-01)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author the fdars-capabilities SKILL.md frontmatter (spec-valid + disjoint trigger)</name>
  <files>.claude/skills/fdars-capabilities/SKILL.md</files>
  <read_first>
    - .claude/skills/fdars-advisor/SKILL.md (the spec-valid reference schema to MIRROR: frontmatter keys name, description block scalar with Trigger phrasing, compatibility block scalar, allowed-tools space-separated — lines 1-28)
    - .planning/phases/78-ai-capability-discovery-skill/78-RESEARCH.md (Pattern 3 "SKILL.md for fdars-capabilities" — the exact frontmatter block, the trigger-boundary table, and the explicit "Do NOT use this skill for parameter tuning, diagnostics, or before/after comparison — use fdars-advisor for those tasks." disjoint-boundary sentence)
  </read_first>
  <action>
    Create `.claude/skills/fdars-capabilities/SKILL.md` with frontmatter mirroring the RESEARCH Pattern 3 block exactly: `name: fdars-capabilities`; a `description:` block scalar whose trigger is capability discovery ("what can fdars do?", "which method for X?", "how do I call Y?", "list/browse the fdars API") AND which ends with the literal disjoint-boundary directive steering tuning/diagnostics/before-after-comparison to fdars-advisor; a `compatibility:` block scalar noting fdars install (Python 3.9+, no API key required) and pointing at docs/llms.txt + the fdars_list_capabilities MCP tool; and `allowed-tools: Bash Read WebFetch`. The name MUST be `fdars-capabilities` (distinct from `fdars-advisor`, no collision). Keep the description phrasing so an agent parsing skill descriptions can machine-read the boundary.
  </action>
  <acceptance_criteria>
    - Frontmatter parses as YAML with all four keys present: name, description, compatibility, allowed-tools.
    - `name` is exactly `fdars-capabilities`.
    - The description contains capability-discovery trigger phrasing AND names `fdars-advisor` as the alternative for tuning/diagnostics/comparison.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import re,sys
t=open('.claude/skills/fdars-capabilities/SKILL.md',encoding='utf-8').read()
m=re.match(r'^---\n(.*?)\n---',t,re.S); assert m,'no frontmatter block'
fm=m.group(1)
try:
    import yaml; d=yaml.safe_load(fm)
except Exception:
    d=None
if d is not None:
    for k in ('name','description','compatibility','allowed-tools'):
        assert k in d, f'missing frontmatter key: {k}'
    assert d['name']=='fdars-capabilities', 'name must be fdars-capabilities'
    desc=d['description']
else:
    for k in ('name:','description:','compatibility:','allowed-tools:'):
        assert k in fm, f'missing frontmatter key: {k}'
    assert 'fdars-capabilities' in fm
    desc=fm
assert 'fdars-advisor' in desc, 'description must name fdars-advisor as the alternative'
assert re.search(r'do not use', desc, re.I), 'description must carry the disjoint-boundary directive'
print('SKILL.md frontmatter OK (spec-valid + disjoint)')
"</automated>
    <fails_when>frontmatter is missing any of the four keys, name is not fdars-capabilities, or the description omits the fdars-advisor boundary directive</fails_when>
  </verify>
  <done>SKILL.md frontmatter is spec-valid (four keys), name is `fdars-capabilities`, and the description carries capability-discovery triggers plus the explicit fdars-advisor boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Author the SKILL.md body — per-module map + how-to-find, referencing the source map</name>
  <files>.claude/skills/fdars-capabilities/SKILL.md</files>
  <read_first>
    - .planning/phases/78-ai-capability-discovery-skill/78-RESEARCH.md (Pattern 4 "SKILL.md Body Structure" + Pitfall 6 — do NOT inline the full 409-callable map; show a per-module summary table, reference docs/llms.txt as the agent URL, and note the fdars_list_capabilities MCP tool)
    - python/fdars/_capability_map.json (the authoritative module set + one-line purposes to summarize; produced by 78-01)
  </read_first>
  <action>
    Write the SKILL.md body below the frontmatter following RESEARCH Pattern 4: (1) a "Capability Map" section giving the full-surface pointers — the agent URL `https://sipemu.github.io/pyfda/llms.txt`, the MCP tool `fdars_list_capabilities(module="depth")`, and the offline generator; (2) a "Module Overview" table with ONE row per top-level key in `_capability_map.json` (module name, one-line purpose, a minimal `fdars.<module>.<fn>(...)` call example) — derive rows from the committed map so the table cannot claim a module that is not in the map; include the `_Fdata` entry as the Fdata OOP class row; (3) a "How to Find a Method" section with the `python -c "import fdars; print([n for n in dir(fdars.depth) if not n.startswith('_')])"` snippet and the MCP-tool alternative. Do NOT inline all 409 callables (context-window + drift risk per Pitfall 6). The body is prose/tables only — no runnable FDARS_FENCE_OK fences are required here (this file is not built by mkdocs).
  </action>
  <acceptance_criteria>
    - The body contains a Module Overview table whose module names are all present as keys in python/fdars/_capability_map.json (no phantom modules).
    - The body references docs/llms.txt and the fdars_list_capabilities MCP tool as the full-surface access paths.
    - The full 409-callable list is NOT inlined (the file stays a summary, not a dump).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import json,re
body=open('.claude/skills/fdars-capabilities/SKILL.md',encoding='utf-8').read()
mapkeys=set(json.load(open('python/fdars/_capability_map.json')))
# Table module tokens: fdars.<mod> mentions in the body must all be real map keys (ignore _Fdata alias 'Fdata').
mentioned=set(re.findall(r'fdars\.([a-z_][a-z0-9_]*)', body))
phantom={m for m in mentioned if m not in mapkeys}
assert not phantom, f'body names modules not in the capability map: {sorted(phantom)}'
assert 'llms.txt' in body, 'body must reference docs/llms.txt'
assert 'fdars_list_capabilities' in body, 'body must reference the MCP tool'
# Non-duplication: a summary, not a full dump — far fewer than 409 fdars.<mod>.<fn>( call sites.
callsites=len(re.findall(r'fdars\.[a-z_]+\.[a-z_][a-z0-9_]*\(', body))
assert callsites < 100, f'body looks like a full callable dump ({callsites} call sites) — keep it a summary'
print('SKILL.md body OK: module table grounded, full-surface pointers present, summary not dump')
"</automated>
    <fails_when>the body names a module absent from the capability map, omits the docs/llms.txt or MCP-tool pointer, or inlines the full callable dump</fails_when>
  </verify>
  <done>The SKILL.md body has a map-grounded Module Overview table, points to docs/llms.txt + the MCP tool for the full surface, and stays a summary (no 409-callable dump).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill description → agent routing | An agent reads the SKILL.md description to decide whether to load this skill vs fdars-advisor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-04 | Spoofing | skill-routing overlap | medium | mitigate | Description explicitly directs tuning/diagnostics/comparison to fdars-advisor — machine-readable boundary prevents the two skills from being confused |
| T-78-05 | Tampering | module-table drift | low | mitigate | Body table grounded against `_capability_map.json` keys; verify gate rejects phantom modules |
</threat_model>

<verification>
- SKILL.md frontmatter parses with name/description/compatibility/allowed-tools; name is `fdars-capabilities`.
- Description carries capability-discovery triggers + the explicit fdars-advisor boundary.
- Body Module Overview table modules all exist in `_capability_map.json`; references docs/llms.txt + MCP tool; not a full-callable dump.
- Whole-site build + GATE-04 confirmation DEFERRED to Phase 79.
</verification>

<success_criteria>
A spec-valid, disjoint `fdars-capabilities` skill ships with a map-grounded module overview and
full-surface pointers, distinct from and non-duplicating fdars-advisor.
</success_criteria>

<output>
Create `.planning/phases/78-ai-capability-discovery-skill/78-02-SUMMARY.md` when done.
</output>

---
phase: 15-python-api-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/advisor/python-api.md
autonomous: false
requirements: [PYDOC-01, PYDOC-02, PYDOC-03]

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The docs build executes an OFFLINE build_diagnostics fence (no ANTHROPIC_API_KEY) and its printed diagnostic values render inline on the page (PYDOC-02)"
    - "The page documents build_diagnostics, advise, and describe_cluster_differences with signatures/arguments/returns accurate against python/fdars/advisor.py (PYDOC-01)"
    - "The page documents the recommend-only nature and shows the Recommendation (action/kind/rationale/expected_effect/evidence) and Advice (interpretation/recommendations/caveats) schema fields (PYDOC-03)"
    - "advise() appears only as an ILLUSTRATIVE non-executed fence marked as requiring ANTHROPIC_API_KEY and not run in the build"
    - "describe_cluster_differences is documented including its run_llm=False offline escape hatch returning the raw diagnostics dict"
  artifacts:
    - "docs/advisor/python-api.md"
  key_links:
    - "docs/advisor/python-api.md exec fence -> scripts/docs_data.load_canadian_weather (reads docs/data/canadian_weather.csv)"
    - "docs/advisor/python-api.md exec fence -> fdars.clustering.kmeans_fd -> fdars.advisor.build_diagnostics(method='clustering')"
    - "docs/advisor/python-api.md <-> docs/advisor/index.md (cross-link)"
---

<objective>
Author `docs/advisor/python-api.md`: the method-accurate Python API reference for the recommend-only advisor surface (`build_diagnostics`, `advise`, `describe_cluster_differences`) plus the `Advice`/`Recommendation` schema, anchored by a worked example whose OFFLINE `build_diagnostics` fence executes during the docs build against the Canadian Weather dataset with no API key.

Purpose: Give readers a runnable, provably-correct entry point to the Python advisor surface (covers PYDOC-01/02/03), consistent with the offline-first recipe in `examples/advisor_recipe.py` and the grounding invariant on `docs/advisor/index.md`.
Output: One new page, `docs/advisor/python-api.md`, whose single executable fence runs offline and renders its printed diagnostics inline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-python-api-page/15-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- `docs/advisor/python-api.md` — new Python API reference page for the advisor surface, with one offline-executed `build_diagnostics` fence.

Explicitly NOT in this phase:
- `mkdocs.yml` nav wiring (deferred to Phase 18, NAVDOC-01).
- Any change to `python/fdars/advisor.py` or other advisor code (documentation-only milestone).
- MCP page (Phase 16) / Agent Skill page (Phase 17) — only cross-linked from here.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Page skeleton + OFFLINE executed diagnostics fence (proven to run in the build)</name>
  <files>docs/advisor/python-api.md</files>
  <read_first>
    - docs/analyze/gmm-clustering.md — copy the EXACT executed-fence directive line and the `from docs_fig import fig, render` / `print(...)` mechanism; note this repo runs offline fences via markdown-exec with `scripts/` on PYTHONPATH.
    - docs/examples/canadian-weather.md (lines ~59-65) — canonical exec-fence pattern that loads data via `from docs_data import load_canadian_weather` returning `day, X, meta`.
    - examples/advisor_recipe.py — the canonical offline recipe to mirror: load Canadian Weather → `kmeans_fd(X, day, k=4, seed=42)` → `build_diagnostics(result, method="clustering", argvals=day)` → print diagnostic values.
    - python/fdars/advisor.py (lines 188-255, 632-740) — `build_diagnostics(result, method, *, argvals=None, **kwargs)` signature and the clustering-branch keys it returns (`k`, `cluster_means`, `cluster_sizes`, `mean_amplitude_separation`, `mean_phase_separation`).
    - scripts/docs_data.py (`load_canadian_weather`) — returns `day` (365,), `X` (35,365), `meta`; reads `docs/data/canadian_weather.csv`.
  </read_first>
  <action>
    Create `docs/advisor/python-api.md` with an H1 and a short intro paragraph naming the recommend-only Python advisor surface, then a "Worked example" section containing ONE executed offline fence. Open the fence with the exact directive used by existing exec-fence pages (`python exec="1" html="1" source="above"`, matching docs/analyze/gmm-clustering.md and docs/examples/canadian-weather.md). Inside the fence: import via `from docs_data import load_canadian_weather`, `from fdars.clustering import kmeans_fd`, and `from fdars.advisor import build_diagnostics`; load `day, X, meta = load_canadian_weather("temperature")`; cluster with `result = kmeans_fd(X, day, k=4, seed=42)`; build diagnostics with `build_diagnostics(result, method="clustering", argvals=day)`; then `print(...)` a few diagnostic values — specifically `k`, `cluster_sizes`, `mean_amplitude_separation`, and `mean_phase_separation` (the exact keys returned by the clustering branch of advisor.py). The fence MUST run OFFLINE: it may NOT import `anthropic`, may NOT call `advise`, and may NOT read `ANTHROPIC_API_KEY`. Use the dataset filename `docs/data/canadian_weather.csv` (loaded via the `load_canadian_weather("temperature")` helper). Keep `kmeans_fd` deterministic with the fixed `seed=42` (Claude's discretion on exact k/max_iter, but the fence must run deterministically). Do NOT wire `mkdocs.yml` (Phase 18). Cross-link back to the overview with a section-relative link to `index.md`.
    Then VERIFY the fence actually executes in the build before expanding prose in Task 2 — build the page and confirm the printed diagnostic values appear in the generated HTML.
  </action>
  <acceptance_criteria>
    - `test -f docs/advisor/python-api.md` succeeds.
    - `grep -q 'exec="1"' docs/advisor/python-api.md` succeeds (page has an executed fence) and the same fence contains `build_diagnostics` and `method="clustering"` (grep both tokens).
    - `grep -c 'exec="1"' docs/advisor/python-api.md` equals 1 (exactly one executed fence in this phase).
    - The executed fence body contains none of `anthropic`, `advise(`, or `ANTHROPIC_API_KEY` (offline guarantee): running `sed -n` over the fence region and grepping for each token returns no match inside the fence.
    - Build the site with `PYTHONPATH=scripts DOCS_FAST=1 mkdocs build -f mkdocs.yml -d site 2>&1` (per the repo Makefile `docs` recipe: `export PYTHONPATH := scripts`) and then confirm the built HTML for the page contains the printed diagnostic labels — grep the generated `site/advisor/python-api/index.html` for `mean_amplitude_separation` (or the exact printed label used) returns a match, proving the offline fence ran and rendered inline. If a full strict build is too heavy, build just this page's output and assert the rendered diagnostic value string is present.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=scripts DOCS_FAST=1 mkdocs build -f mkdocs.yml -d site >/tmp/mkbuild.log 2>&1; grep -q 'mean_amplitude_separation\|mean_phase_separation' site/advisor/python-api/index.html && echo FENCE_RENDERED</automated>
  </verify>
  <done>The page exists with exactly one executed offline fence that loads Canadian Weather, runs `kmeans_fd` + `build_diagnostics(method="clustering")`, and whose printed diagnostic values render in the built HTML — with no `advise`/`anthropic`/API-key reference inside the fence.</done>
</task>

<task type="auto">
  <name>Task 2: Expand API reference prose, schema tables, and illustrative advise() fence</name>
  <files>docs/advisor/python-api.md</files>
  <read_first>
    - python/fdars/advisor.py — verify every documented name/argument/return against source:
      `build_diagnostics(result, method, *, argvals=None, **kwargs)` (supported methods: alignment, fpca, basis, smoothing, clustering; returns a JSON-serialisable dict; raises ValueError on unsupported method);
      `advise(diagnostics, *, task, domain_context, model="claude-opus-4-8") -> Advice` (requires anthropic; raises ImportError without it);
      `describe_cluster_differences(result, *, argvals=None, domain_context="", model="claude-opus-4-8", run_llm=True, **kwargs)` — `run_llm=False` returns the raw clustering diagnostics dict (offline), `run_llm=True` returns `Advice`;
      `Recommendation` fields: `action: str`, `kind: Literal["parameter","method","none"]`, `rationale: str`, `expected_effect: str`, `evidence: list[str]`;
      `Advice` fields: `interpretation: str`, `recommendations: list[Recommendation]`, `caveats: list[str]`.
    - docs/advisor/index.md — match tone; cross-link targets `mcp.md` and `agent-skill.md` are used there and resolve once Phases 16-17 land.
    - docs/analyze/gmm-clustering.md — reuse the existing "Parameters" / "Returns" markdown-table style for the schema tables.
  </read_first>
  <action>
    Expand `docs/advisor/python-api.md` (do not touch the Task-1 executed fence). Add a "Functions" section documenting the three functions with signature, arguments, and returns EXACTLY as in advisor.py: `build_diagnostics(result, method, *, argvals=None, **kwargs)` (list the five supported `method` values and note it is offline/deterministic and raises `ValueError` for an unsupported method); `advise(diagnostics, *, task, domain_context, model="claude-opus-4-8")` returning `Advice` and requiring the `[advisor]` extra; and `describe_cluster_differences(result, *, argvals=None, domain_context="", model="claude-opus-4-8", run_llm=True, **kwargs)`. Explicitly document the `run_llm=False` offline escape hatch: state it returns the raw clustering diagnostics dict with no `anthropic` import or network call, usable without a key. Add a "Recommend-only surface" note stating the Python API returns `Advice` and stops (it does not re-run fdars or compute a before/after delta — that is the MCP/Skill surface). Add two schema tables: a `Recommendation` table with rows `action`, `kind`, `rationale`, `expected_effect`, `evidence` (with their types from source) and an `Advice` table with rows `interpretation`, `recommendations`, `caveats`. Add ONE illustrative, NON-executed fence (plain ```python with NO `exec` attribute) showing an `advise(...)`/`describe_cluster_differences(..., run_llm=True)` call plus a representative `Advice` value; immediately precede or annotate it with a callout stating it "requires `ANTHROPIC_API_KEY`, not run in the docs build." Add section-relative cross-links: back to the overview (`index.md`) and forward to the MCP page (`mcp.md`) and Agent Skill page (`agent-skill.md`) — noting those pages arrive in later phases. Do not invent any parameter, argument name, or schema field not present in advisor.py.
  </action>
  <acceptance_criteria>
    - The page names all three functions: `grep -q 'build_diagnostics' docs/advisor/python-api.md`, `grep -q 'advise' docs/advisor/python-api.md`, and `grep -q 'describe_cluster_differences' docs/advisor/python-api.md` all succeed.
    - The page documents the offline escape hatch: `grep -q 'run_llm' docs/advisor/python-api.md` succeeds.
    - All five `Recommendation`/`Advice` field names appear: `grep -q 'action' && grep -q 'kind' && grep -q 'rationale' && grep -q 'expected_effect' && grep -q 'evidence' && grep -q 'interpretation' && grep -q 'recommendations' && grep -q 'caveats'` over the page all succeed.
    - The illustrative advise() fence is marked not-run: `grep -q 'ANTHROPIC_API_KEY' docs/advisor/python-api.md` succeeds and the fence introducing `advise(` does NOT carry `exec="1"` — the executed-fence count remains exactly 1 (`grep -c 'exec="1"' docs/advisor/python-api.md` equals 1).
    - Cross-links present: `grep -q 'index.md' docs/advisor/python-api.md` succeeds (overview back-link) and `grep -q 'mcp.md' docs/advisor/python-api.md` and `grep -q 'agent-skill.md' docs/advisor/python-api.md` succeed (forward links).
    - No fabricated signature: the documented `advise` signature keyword-only args match source — `grep -q 'domain_context' docs/advisor/python-api.md` and `grep -q 'task' docs/advisor/python-api.md` succeed, and `model="claude-opus-4-8"` appears verbatim.
    - Page still builds and the single offline fence still renders: re-running the Task-1 build command still yields `FENCE_RENDERED`.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=scripts DOCS_FAST=1 mkdocs build -f mkdocs.yml -d site >/tmp/mkbuild2.log 2>&1; test "$(grep -c 'exec=\"1\"' docs/advisor/python-api.md)" = "1" && grep -q 'ANTHROPIC_API_KEY' docs/advisor/python-api.md && grep -q 'run_llm' docs/advisor/python-api.md && grep -q 'expected_effect' docs/advisor/python-api.md && echo API_SECTIONS_OK</automated>
  </verify>
  <done>The page documents all three functions with source-accurate signatures/returns, both schema tables (Recommendation + Advice) with the exact field names, the recommend-only note, the `run_llm=False` offline path, and one non-executed illustrative `advise()` fence marked as requiring an API key — while the single executed offline fence still renders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Per-section review gate — verify the built/served Python API page</name>
  <what-built>A new `docs/advisor/python-api.md` page documenting the recommend-only Python advisor surface (`build_diagnostics`, `advise`, `describe_cluster_differences`), the `Recommendation`/`Advice` schema tables, an illustrative non-run `advise()` fence, and one OFFLINE executed `build_diagnostics` fence that ran during the docs build and rendered its diagnostic values inline. This checkpoint is the project's per-section review gate on the built/served site.</what-built>
  <how-to-verify>
    1. Build and serve the docs from the repo root: `PYTHONPATH=scripts make docs-serve` (or `PYTHONPATH=scripts DOCS_FAST=1 mkdocs serve`), then open the served site at the `advisor/python-api/` page. If you use the /pyfda base-path serve recipe from memory, use that.
    2. Confirm the "Worked example" fence rendered its OUTPUT inline — you should see printed diagnostic values (`k`, `cluster_sizes`, `mean_amplitude_separation`, `mean_phase_separation`) on the page, not just source code. Confirm the build did NOT require or read `ANTHROPIC_API_KEY` (it must build with no key set).
    3. Spot-check method accuracy against `python/fdars/advisor.py`: the three function signatures, the five `build_diagnostics` `method` values, the `run_llm=False` offline note, and both schema tables' field names (`action`/`kind`/`rationale`/`expected_effect`/`evidence`; `interpretation`/`recommendations`/`caveats`).
    4. Confirm the `advise()` illustrative fence is clearly marked "requires ANTHROPIC_API_KEY, not run in the docs build" and did not execute.
    5. Confirm the cross-links to the overview (`index.md`) and forward to MCP/Skill pages read sensibly.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any inaccuracy / rendering issue to fix.</resume-signal>
</task>

</tasks>

<verification>
- Exactly one executed fence on the page (`grep -c 'exec="1"' docs/advisor/python-api.md` == 1) and it is the offline `build_diagnostics` fence.
- The docs build succeeds with NO `ANTHROPIC_API_KEY` set and the offline fence's printed diagnostics render in `site/advisor/python-api/index.html`.
- Every function name, argument, return, and schema field on the page is present in `python/fdars/advisor.py` (no invented parameters).
- Cross-links to `index.md`, `mcp.md`, `agent-skill.md` are present as section-relative links.
- Human review gate on the served site is approved.
</verification>

<success_criteria>
1. Page covers `build_diagnostics`, `advise`, `describe_cluster_differences` with signatures/arguments/returns accurate against `python/fdars/advisor.py` (PYDOC-01).
2. Page includes a worked example whose offline `build_diagnostics` fence runs in the docs build against `docs/data/canadian_weather.csv` with no API key (PYDOC-02).
3. Page documents the recommend-only nature and the `Advice`/`Recommendation` schema fields (`action`/`kind`/`rationale`/`expected_effect`/`evidence`) (PYDOC-03).
4. Page builds cleanly and every executable code fence runs against the current API.
</success_criteria>

<output>
Create `.planning/phases/15-python-api-page/15-01-SUMMARY.md` when done.
</output>

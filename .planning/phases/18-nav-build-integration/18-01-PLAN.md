---
phase: 18-nav-build-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
autonomous: true
requirements: [NAVDOC-01, NAVDOC-02]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A top-level 'AI Advisor' section appears in mkdocs.yml nav, after Analyze and before Examples, with Overview / Python API / MCP Server / Agent Skill entries in that order (NAVDOC-01)"
    - "The full docs build succeeds (exit 0) and renders all four advisor pages under site/advisor/ (NAVDOC-02)"
    - "The Phase 15 offline executed fence still runs in the full build (FDARS_FENCE_OK present in built HTML) (NAVDOC-02)"
    - "Both advisor SVGs still pass the SVGO idempotence gate (pass2 == pass1) (NAVDOC-02)"
    - "No broken internal links between the four advisor pages (mkdocs --strict passes) (NAVDOC-02)"
  artifacts:
    - "mkdocs.yml (with the added 'AI Advisor' nav section)"
    - "site/advisor/index/index.html"
    - "site/advisor/python-api/index.html"
    - "site/advisor/mcp/index.html"
    - "site/advisor/agent-skill/index.html"
  key_links:
    - "mkdocs.yml nav 'AI Advisor' section -> docs/advisor/{index,python-api,mcp,agent-skill}.md (the four pages authored in Phases 14-17)"
    - "docs/advisor/python-api.md executed fence -> FDARS_FENCE_OK marker in site/advisor/python-api/index.html"
---

<objective>
Wire the four already-authored advisor pages into a new top-level "AI Advisor" section
in `mkdocs.yml` nav, then run the full-build gate proving the whole section builds
cleanly: build exits 0, all four pages render, the Phase 15 offline fence still executes,
both advisor SVGs still pass the SVGO idempotence gate, and no advisor cross-link is broken.

This is the milestone's final integration phase. Per D-scope, NO advisor page body is
authored or edited — the only content change is the `mkdocs.yml` nav section. If the build
surfaces a genuine broken link or a fence/SVG regression, fixing that minimal issue is in
scope; otherwise no page-body edits.

Purpose: Make the shipped advisor documentation reachable from the site nav and prove the
whole section is build-clean and method-accurate against the current API (NAVDOC-01, NAVDOC-02).
Output: The `mkdocs.yml` "AI Advisor" nav section and a clean full-build `site/`.

## Artifacts this phase produces
- The `mkdocs.yml` top-level "AI Advisor" nav section (4 entries), placed after `- Analyze:`
  and before `- Examples:`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-nav-build-integration/18-CONTEXT.md

# Build recipe: bare `mkdocs`/`svgo` are NOT on PATH.
# Use the project venv: `PYTHONPATH=scripts .venv/bin/mkdocs build`.
# SVGO gate uses `npx svgo@3.3.4` (idempotence pass2 == pass1); recipe is documented
# at the top of svgo.config.mjs.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire the "AI Advisor" nav section and prove a build shows it</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml — study the existing `- Analyze:` block (a top-level section: 2-space
      indent `- Analyze:`, then a 4-space-indent `- analyze/index.md` landing entry, then
      `- Label: path` entries). The new section sits between the `- Analyze:` block and the
      `- Examples:` block. Match this idiom exactly.
    - .planning/phases/18-nav-build-integration/18-CONTEXT.md — the locked nav decisions
      (D: section title, entry order, placement).
  </read_first>
  <action>
    Add exactly ONE new top-level section titled "AI Advisor" to the `nav:` list in
    mkdocs.yml, placed immediately after the `- Analyze:` block (the line
    `- Covariance Functions: analyze/covariance-functions.md`) and immediately before the
    `- Examples:` line. Mirror the `- Analyze:` idiom: a 2-space-indent `- AI Advisor:`
    header, then a 4-space-indent landing entry, then the three remaining labelled entries.
    The four entries, in this exact order per the locked nav decision (NAVDOC-01): the
    landing entry `Overview: advisor/index.md`, then `Python API: advisor/python-api.md`,
    then `MCP Server: advisor/mcp.md`, then `Agent Skill: advisor/agent-skill.md`. Use the
    concise labels exactly as given ("MCP Server", "Agent Skill") to stay consistent with
    sibling section labels. Do NOT touch any other nav entry, any advisor page body, or any
    other file — the diff is this one added section block only. Then run the fast full build
    to confirm the site builds with the new section present and the four pages reachable.
  </action>
  <acceptance_criteria>
    - `grep -n 'AI Advisor' mkdocs.yml` shows exactly one match (the new section header).
    - `grep -A5 'AI Advisor' mkdocs.yml` shows, in order: `advisor/index.md`,
      `Python API: advisor/python-api.md`, `MCP Server: advisor/mcp.md`,
      `Agent Skill: advisor/agent-skill.md`.
    - The `AI Advisor` header line appears after the `analyze/covariance-functions.md` line
      and before the `- Examples:` line in mkdocs.yml.
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` exits 0.
    - After the build, `site/advisor/index/index.html` exists.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -c 'AI Advisor' mkdocs.yml && grep -A5 'AI Advisor' mkdocs.yml && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build && test -f site/advisor/index/index.html && echo TRACER_OK</automated>
  </verify>
  <done>The "AI Advisor" nav section exists in mkdocs.yml (after Analyze, before Examples) with the four ordered entries, and a fast full build succeeds and renders the Overview page under site/advisor/.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full-build gate — pages, fence, SVGO idempotence, strict links</name>
  <files>mkdocs.yml</files>
  <read_first>
    - svgo.config.mjs — the idempotence gate recipe is documented at the top: run
      `npx svgo@3.3.4 --config svgo.config.mjs --quiet --input <svg> --output -` to get
      pass1, feed pass1 back through the same command via `--input -` to get pass2; the
      diff of pass1 vs pass2 must be empty.
    - docs/advisor/python-api.md — line ~28 emits the `FDARS_FENCE_OK` marker from the
      Phase 15 offline executed fence; this marker must appear in the built HTML.
    - .planning/phases/18-nav-build-integration/18-CONTEXT.md — the full-build gate decision
      (build exit 0, FDARS_FENCE_OK present, SVGO idempotence, no broken advisor links; no
      `[mcp]`/`[advisor]` extras or API key required).
  </read_first>
  <action>
    Run the complete build gate that proves NAVDOC-02, using the project venv (bare mkdocs
    is not on PATH). First run a non-DOCS_FAST full build with mkdocs strict mode so any
    broken internal link (including the advisor cross-links) fails the build; confirm exit 0
    and that all four advisor pages rendered under site/advisor/ (Overview, Python API, MCP
    Server, Agent Skill). Then confirm the Phase 15 offline executed fence actually ran by
    asserting the FDARS_FENCE_OK marker is present in the built Python API HTML — this proves
    the fence executed offline against the current API with no extras and no API key. Then run
    the SVGO idempotence gate on both advisor diagrams (advisor-grounding-invariant.svg and
    advisor-loop.svg): compute pass1 and pass2 per the svgo.config.mjs recipe and assert the
    pass1-vs-pass2 diff is empty for each. Do NOT run svgo with `--output` writing back to the
    file; this is a check-only gate. If — and only if — the strict build surfaces a genuine
    broken advisor cross-link, fix that single minimal link in the offending advisor page and
    re-run; otherwise make no page-body edits. If the fence marker is absent or an SVG fails
    idempotence, treat it as a real integration regression, report it, and stop rather than
    masking it.
  </action>
  <acceptance_criteria>
    - `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` exits 0 (no broken internal links,
      no warnings promoted to errors).
    - All four rendered pages exist: `site/advisor/index/index.html`,
      `site/advisor/python-api/index.html`, `site/advisor/mcp/index.html`,
      `site/advisor/agent-skill/index.html`.
    - `grep -q FDARS_FENCE_OK site/advisor/python-api/index.html` succeeds (Phase 15 fence
      executed in the full build).
    - SVGO idempotence holds for `docs/assets/diagrams/advisor-grounding-invariant.svg`
      (pass2 diff pass1 is empty).
    - SVGO idempotence holds for `docs/assets/diagrams/advisor-loop.svg`
      (pass2 diff pass1 is empty).
    - No advisor page body was edited unless a genuine broken cross-link was fixed
      (`git diff --stat docs/advisor/` is empty in the no-defect case).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts .venv/bin/mkdocs build --strict && for p in index python-api mcp agent-skill; do test -f "site/advisor/$p/index.html" || { echo "MISSING site/advisor/$p"; exit 1; }; done && grep -q FDARS_FENCE_OK site/advisor/python-api/index.html && for svg in docs/assets/diagrams/advisor-grounding-invariant.svg docs/assets/diagrams/advisor-loop.svg; do P1=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$svg" --output -) && P2=$(printf '%s' "$P1" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && diff <(printf '%s' "$P1") <(printf '%s' "$P2") || { echo "SVGO idempotence FAILED: $svg"; exit 1; }; done && echo GATE_OK</automated>
  </verify>
  <done>The full strict build succeeds with all four advisor pages rendered, the FDARS_FENCE_OK marker is present in the built Python API HTML, both advisor SVGs pass the SVGO idempotence gate, and no advisor page body was edited (unless a real broken link was fixed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain -> published site | mkdocs renders local docs into static HTML; only local, already-reviewed content crosses. No untrusted external input in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | mkdocs.yml nav edit accidentally alters unrelated nav entries | low | mitigate | Diff is scoped to one added section block; acceptance criteria assert placement (after Analyze, before Examples) and exact entry order; `git diff mkdocs.yml` reviewed. |
| T-18-02 | Information Disclosure | advisor fence requires an API key / extras, leaking a build-time secret dependency | low | mitigate | Gate confirms the build succeeds offline with no `[mcp]`/`[advisor]` extras and no ANTHROPIC_API_KEY; only the offline FDARS_FENCE_OK fence executes. |
| T-18-03 | Tampering | SVGO gate run with `--output` writing back to the diagram files would silently mutate reviewed SVGs | low | mitigate | Gate is check-only (idempotence via `--output -` to stdout); action explicitly forbids writing back to the file. |
</threat_model>

<verification>
Phase-level checks (all automated, no human gate needed — the integration gate is objective):
1. Nav: `grep -A5 'AI Advisor' mkdocs.yml` shows the four ordered entries; section sits between Analyze and Examples.
2. Build: `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` exits 0.
3. Pages: all four `site/advisor/{index,python-api,mcp,agent-skill}/index.html` exist.
4. Fence: `FDARS_FENCE_OK` present in `site/advisor/python-api/index.html`.
5. SVGs: both advisor SVGs pass SVGO idempotence (pass2 == pass1).
6. No unintended body edits: `git diff --stat docs/advisor/` empty in the no-defect case.
</verification>

<success_criteria>
- A top-level "AI Advisor" section is wired into mkdocs.yml nav after Analyze and before
  Examples, with Overview / Python API / MCP Server / Agent Skill in that order (NAVDOC-01).
- The full strict docs build succeeds and every advisor page renders; the Phase 15 offline
  fence executes (FDARS_FENCE_OK), no extras/API key required (NAVDOC-02).
- Both advisor SVGs still pass the SVGO idempotence/determinism gate (NAVDOC-02).
- No advisor page body edited unless fixing a genuine broken internal link.
</success_criteria>

<output>
Create `.planning/phases/18-nav-build-integration/18-01-SUMMARY.md` when done.
</output>

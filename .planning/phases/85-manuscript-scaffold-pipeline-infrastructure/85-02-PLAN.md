---
phase: 85-manuscript-scaffold-pipeline-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/paper.tex
  - paper/refs.bib
  - paper/sections/intro.tex
  - paper/sections/design.tex
  - paper/sections/represent.tex
  - paper/sections/capabilities.tex
  - paper/sections/comparison.tex
  - paper/sections/casestudies.tex
  - paper/sections/availability.tex
  - CITATION.cff
autonomous: true
requirements: [MANU-01, MANU-08]
user_setup:
  - service: cffconvert
    why: "Local validation of CITATION.cff against the CFF v1.2.0 schema (dev-only, not a runtime dep)"
    env_vars: []
    dashboard_config:
      - task: "Install cffconvert into the dev venv: .venv/bin/pip install cffconvert"
        location: "local terminal (one-off; already present in CI is not assumed for this phase's local gate)"
estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "paper/paper.tex uses \\documentclass{article} with \\usepackage{natbib} + \\bibliographystyle{plain} + \\bibliography{refs} and contains NO biblatex/biber and NO \\today or timestamp macro [MANU-01]"
    - "paper/paper.tex \\input-s seven per-section stub files that each carry a \\section heading and a placeholder paragraph [MANU-01]"
    - "the author block names Simon Müller with sm@data-zoo.de and \\date{} is empty (no \\today) [MANU-01, MANU-08]"
    - "CITATION.cff declares cff-version 1.2.0, a preferred-citation block, and an authors entry family-names Müller / given-names Simon / email sm@data-zoo.de [MANU-08]"
    - "cffconvert --validate exits zero against CITATION.cff [MANU-08]"
  artifacts:
    - paper/paper.tex
    - paper/refs.bib
    - paper/sections/intro.tex
    - paper/sections/design.tex
    - paper/sections/represent.tex
    - paper/sections/capabilities.tex
    - paper/sections/comparison.tex
    - paper/sections/casestudies.tex
    - paper/sections/availability.tex
    - CITATION.cff
  key_links:
    - "paper/paper.tex -> \\input{sections/*} (seven stubs) and \\bibliography{refs} -> paper/refs.bib"
    - "CITATION.cff -> cffconvert --validate (CFF v1.2.0 schema) — local gate"
  backstop:
    - statement: "paper/paper.tex + section stubs + refs.bib compile to a minimal PDF via CI tectonic (wtfjoke/setup-tectonic@v4, SOURCE_DATE_EPOCH set) with a BibTeX pass"
      verification: backstop
---

<objective>
Author the minimal compiling LaTeX manuscript skeleton and the citation metadata: `paper/paper.tex` (`article` class, natbib + BibTeX, seven `\input` section stubs, no timestamp macros), a stub `paper/refs.bib`, and a `cffconvert`-valid `CITATION.cff` with a `preferred-citation` block.

Purpose: Locks the bibliography tooling (natbib + BibTeX, never biblatex/biber — tectonic silently emits `[?]` citations under biber) and the timestamp-free authoring convention before any prose is written, and surfaces the software's citation metadata for GitHub/Zenodo. Later phases (86 CI gate, 88 prose) fill these stubs. Satisfies MANU-01, MANU-08.

Output: `paper/paper.tex`, `paper/refs.bib`, seven `paper/sections/*.tex` stubs, and root `CITATION.cff`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files:
- `paper/paper.tex` — root manuscript (article class, natbib+BibTeX, `\input` stubs, `\date{}`)
- `paper/refs.bib` — stub bibliography (populated by `gen_refs_bib.py` in Phase 86)
- `paper/sections/intro.tex`, `design.tex`, `represent.tex`, `capabilities.tex`, `comparison.tex`, `casestudies.tex`, `availability.tex` — seven section stubs
- `CITATION.cff` — repo-root citation metadata (CFF v1.2.0, `preferred-citation` block)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: LaTeX manuscript skeleton — paper.tex + seven section stubs + stub refs.bib</name>
  <files>paper/paper.tex, paper/refs.bib, paper/sections/intro.tex, paper/sections/design.tex, paper/sections/represent.tex, paper/sections/capabilities.tex, paper/sections/comparison.tex, paper/sections/casestudies.tex, paper/sections/availability.tex</files>
  <read_first>
    - .planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md — Pattern 5 (LaTeX skeleton, exact `\documentclass` / natbib / `\input` layout), Anti-Patterns (no biblatex, no `\today`), Pitfall 1 (biber silent failure), Pitfall 6 (`\today` in stubs).
  </read_first>
  <action>
    Create `paper/paper.tex` per RESEARCH Pattern 5: `\documentclass[12pt,a4paper]{article}`; `\usepackage[numbers,sort&compress]{natbib}`; standard packages (`fontenc` T1, `inputenc` utf8, `hyperref`, `url`, `graphicx`, `amsmath`); title `fdars: Functional Data Analysis in Rust with Python Bindings`; author `Simon M\"{u}ller` with `\texttt{sm@data-zoo.de}`; `\date{}` (EMPTY — never `\today` or any timestamp macro). Body: `\maketitle`, a placeholder `abstract` environment, then `\input{sections/intro}` … `\input{sections/availability}` for all seven sections in the order intro, design, represent, capabilities, comparison, casestudies, availability; close with `\bibliographystyle{plain}` and `\bibliography{refs}`. Do NOT use `\usepackage{biblatex}`, `\addbibresource`, `\printbibliography`, or `\date{\today}`.
    Create the seven `paper/sections/*.tex` stubs — each contains only a `\section{...}` heading (Introduction, Software Design and Architecture, Data Representation, Capabilities, Comparison with Related Software, Case Studies, Availability and Installation respectively) plus one placeholder sentence. No `\today`, no dynamic date/time macro in any stub.
    Create `paper/refs.bib` as a stub: a single valid placeholder BibTeX entry (e.g. `@misc{fdars-placeholder, title={Placeholder}, year={2026}}`) so `\bibliography{refs}` resolves; note in a `%` comment that Phase 86's `gen_refs_bib.py` regenerates this file from `_references_map.json`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; grep -q 'documentclass\[12pt,a4paper\]{article}' paper/paper.tex &amp;&amp; grep -q 'usepackage\[numbers,sort&amp;compress\]{natbib}' paper/paper.tex &amp;&amp; grep -qE '\\bibliography\{refs\}' paper/paper.tex &amp;&amp; ! grep -qE 'biblatex|addbibresource|printbibliography' paper/paper.tex &amp;&amp; ! grep -rlE '\\today|\\currenttime' paper/paper.tex paper/sections/ &amp;&amp; test "$(ls paper/sections/*.tex | wc -l)" -eq 7 &amp;&amp; for f in paper/sections/*.tex; do grep -qE '\\section\{' "$f" || { echo "no section in $f"; exit 1; }; done &amp;&amp; test -f paper/refs.bib &amp;&amp; echo TEX_OK</automated>
    <fails_when>TEX_OK not printed: documentclass is not `article`; natbib line missing; `\bibliography{refs}` missing; a biblatex/biber macro present; `\today`/`\currenttime` found in paper.tex or any stub; fewer/more than seven section stubs; a stub lacks a `\section{}` heading; refs.bib missing.</fails_when>
  </verify>
  <acceptance_criteria>
    - `paper/paper.tex` uses `\documentclass{article}`, `natbib`, `\bibliographystyle{plain}`, `\bibliography{refs}`, `\date{}`, author Simon Müller / sm@data-zoo.de.
    - No biblatex/biber macro and no `\today`/timestamp macro anywhere in `paper.tex` or the stubs.
    - Exactly seven `paper/sections/*.tex` stubs, each with a `\section{}` heading and a placeholder line.
    - `paper/refs.bib` exists with a valid placeholder entry.
  </acceptance_criteria>
  <done>The manuscript skeleton is authored: `paper.tex` `\input`s seven timestamp-free section stubs and references a stub `refs.bib` via natbib+BibTeX, ready for the Phase 86 CI tectonic compile.</done>
</task>

<task type="auto">
  <name>Task 2: CITATION.cff — v1.2.0 with preferred-citation, cffconvert-valid</name>
  <files>CITATION.cff</files>
  <read_first>
    - .planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md — Pattern 2 (full CITATION.cff example), Finding 7 (preferred-citation required fields: type/authors/title), Pitfall 5 (placeholder DOI fails validation — omit doi, use identifiers with a repo URL or an empty list), Open Question 2 (arXiv id added at Phase 90).
  </read_first>
  <precondition>cffconvert is installed in the dev venv (`.venv/bin/pip install cffconvert`); if absent, the executor installs it before running the validation gate. cffconvert is not assumed pre-installed. [user_setup]</precondition>
  <action>
    Create root `CITATION.cff` per RESEARCH Pattern 2: `cff-version: "1.2.0"`; `message`; `type: software`; `title: fdars`; `version: "0.12.0"`; `date-released: "2026-09-08"`; `license: MIT`; `repository-code` + `url` (the sipemu/pyfda repo + docs site); `abstract`; `authors` list with a single entry `family-names: "Müller"`, `given-names: "Simon"`, `email: "sm@data-zoo.de"`; `keywords`. Add a `preferred-citation` block with `type: article`, `title` (the paper title), an `authors` list (same single author), `year: 2026`. Do NOT put a placeholder `doi` (a `"TBD"` DOI fails schema validation — Pitfall 5): either omit `doi` entirely or use an `identifiers` entry `{type: url, value: <repo url>, description: "Repository (arXiv URL added at submission)"}`. The arXiv id/journal is populated in Phase 90.
    Do NOT hardcode any coverage integer or numeric claim in the abstract (all numbers are machine-derived in later phases); prose-only description is fine.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; { .venv/bin/python -c "import cffconvert" 2>/dev/null || .venv/bin/pip install -q cffconvert; } &amp;&amp; .venv/bin/cffconvert --validate -i CITATION.cff &amp;&amp; grep -q 'cff-version: *"\?1\.2\.0"\?' CITATION.cff &amp;&amp; grep -q 'preferred-citation' CITATION.cff &amp;&amp; grep -q 'sm@data-zoo.de' CITATION.cff &amp;&amp; grep -qi 'family-names: *"\?M.ller"\?' CITATION.cff &amp;&amp; echo CFF_OK</automated>
    <fails_when>CFF_OK not printed: `cffconvert --validate` exits non-zero (schema violation — e.g. a bad placeholder DOI or missing required preferred-citation field type/authors/title); cff-version is not 1.2.0; no preferred-citation block; the author email or family-name (Müller) is absent.</fails_when>
  </verify>
  <acceptance_criteria>
    - `CITATION.cff` is CFF v1.2.0 and passes `cffconvert --validate` (exit zero).
    - It has a `preferred-citation` block (type article) and an author entry Müller / Simon / sm@data-zoo.de.
    - No placeholder `"TBD"` DOI; arXiv identifier deferred to Phase 90.
  </acceptance_criteria>
  <done>`cffconvert --validate` passes against a v1.2.0 `CITATION.cff` carrying a `preferred-citation` block and the placeholder Simon Müller author entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static file authoring only — no runtime input, no network service, no dynamic execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-03 | Tampering | biblatex/biber in `paper.tex` | low | mitigate | Enforce natbib+BibTeX only via the negative grep gate (biber silently produces `[?]` citations in tectonic CI) |
| T-85-04 | Information Disclosure | `\today`/timestamp macro leaking build wall-clock into the PDF | low | mitigate | `\date{}` + negative grep for `\today`/`\currenttime` across `paper.tex` and all stubs |
</threat_model>

<verification>
- `paper.tex`: `article` class + natbib + `\bibliography{refs}`; no biblatex/biber; no `\today`.
- Exactly seven `sections/*.tex` stubs, each with a `\section{}`.
- `paper/refs.bib` resolves `\bibliography{refs}` (placeholder entry).
- `cffconvert --validate` exits zero; `preferred-citation` block + Müller/sm@data-zoo.de present.
- BACKSTOP (CI, Phase 86): tectonic compiles `paper.tex` to a minimal PDF with a BibTeX pass — NOT gated locally this phase (no local TeX).
</verification>

<success_criteria>
- MANU-01: `paper/paper.tex` (article + natbib + BibTeX, seven `\input` stubs, no timestamp macros) authored; CI tectonic compile is the Phase 86 backstop.
- MANU-08: `CITATION.cff` v1.2.0 with `preferred-citation` block, author Simon Müller <sm@data-zoo.de>, passes `cffconvert --validate`.
</success_criteria>

<output>
Create `.planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-02-SUMMARY.md` when done.
</output>

---
phase: 85-manuscript-scaffold-pipeline-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/code/paper_utils.py
  - paper/code/gen_figures.py
  - paper/code/.gitkeep
  - paper/figures/smoke.pdf
  - Makefile
autonomous: true
requirements: [PIPE-01, PIPE-02]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "paper/code/paper_utils.py imports fig and FDARS_COLORS from scripts/docs_fig.py (does NOT reimplement the palette or figure factory) [PIPE-01]"
    - "data_path(name) returns an absolute path into docs/data/ and rejects traversal ('..' / absolute name), copying no dataset [PIPE-01]"
    - "matplotlib runs on the Agg backend with the docs_fig.py module-top rcParams in effect and each figure script calls np.random.seed before any stochastic draw [PIPE-02]"
    - "save_figure writes PDF with metadata CreationDate suppressed (set to None) [PIPE-02]"
    - "make paper regenerates every figure in one command via PYTHONPATH=scripts:paper/code [PIPE-01]"
    - "re-running make paper leaves git diff paper/figures/ empty (byte-identical deterministic PDF output) [PIPE-02]"
  artifacts:
    - paper/code/paper_utils.py
    - paper/code/gen_figures.py
    - paper/figures/smoke.pdf
    - Makefile (paper, paper-figures targets)
  key_links:
    - "paper_utils.py -> scripts/docs_fig.py (import fig, FDARS_COLORS) via three-level sys.path/PYTHONPATH injection"
    - "gen_figures.py -> paper_utils.save_figure -> paper/figures/*.pdf (committed deterministic output)"
    - "Makefile paper target -> PYTHONPATH=scripts:paper/code python paper/code/gen_figures.py"
    - "data_path() -> docs/data/ (canonical, no duplication)"
---

<objective>
Build the deterministic reproducible-code framework for the paper as a single proven vertical slice: a `paper_utils.py` helper that reuses the established `scripts/docs_fig.py` figure factory + palette and resolves datasets through `data_path()` into `docs/data/`, plus one seeded figure script driven by a one-command `make paper` Makefile target whose committed PDF output is byte-identical on re-run.

Purpose: This locks every reproducibility decision (Agg backend, module-top rcParams, per-figure seeding, suppressed PDF `CreationDate`, zero data duplication, one-command runner) that Phases 88/89 depend on when they add real figure scripts. Proving the full path end-to-end now — including the `git diff paper/figures/` determinism gate on a real committed PDF — catches any architectural determinism flaw on the first commit instead of after ten figure scripts exist. Satisfies PIPE-01, PIPE-02.

Output: `paper/code/paper_utils.py` (`fig`/`FDARS_COLORS` re-export, `data_path`, `save_figure`), `paper/code/gen_figures.py` (seeded smoke figure), committed `paper/figures/smoke.pdf`, and `paper`/`paper-figures` Makefile targets.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md

# Reused, do NOT reimplement — read to confirm the exact importable surface:
@scripts/docs_fig.py
@scripts/docs_data.py
@Makefile
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files:
- `paper/code/paper_utils.py` — helper module
- `paper/code/gen_figures.py` — pipeline entry that regenerates all paper figures
- `paper/code/.gitkeep` — keep the dir tracked (removed once real scripts land)
- `paper/figures/smoke.pdf` — first committed deterministic figure (proves the gate)

New symbols (in `paper/code/paper_utils.py`):
- re-exported `fig`, `FDARS_COLORS` (from `scripts/docs_fig.py`)
- `data_path(name: str) -> pathlib.Path`
- `save_figure(fig, path) -> None`

Modified files:
- `Makefile` — new `paper`, `paper-figures` targets + `PAPER_PYTHONPATH`
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end reproducible figure path — paper_utils.py + one seeded figure, run manually once</name>
  <files>paper/code/paper_utils.py, paper/code/gen_figures.py, paper/code/.gitkeep</files>
  <read_first>
    - scripts/docs_fig.py — the importable surface: `fig(nrows=1, ncols=1, **kwargs)` returns `(figure, axes)`; `FDARS_COLORS` is a 7-element hex list; module top already calls `matplotlib.use("Agg")` and `plt.rcParams.update({...})` (figsize, dpi, prop_cycle, svg.hashsalt, etc). Import these; do NOT redefine them.
    - scripts/docs_data.py — `_DATA_DIR = Path(__file__).resolve().parent.parent / "docs" / "data"` and `_path(name)` raises `FileNotFoundError` if missing. `data_path()` mirrors this pattern but from `paper/code/` (three levels up, not two).
    - .planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md — Pattern 1 (paper_utils.py) and Pattern 4 (deterministic matplotlib PDF).
  </read_first>
  <action>
    Create `paper/code/paper_utils.py` (snake_case, NumPy docstrings, `from __future__ import annotations`):
    (1) Inject `scripts/` onto `sys.path`: `_SCRIPTS = Path(__file__).resolve().parent.parent.parent / "scripts"`; `if str(_SCRIPTS) not in sys.path: sys.path.insert(0, str(_SCRIPTS))`. `paper/code/` is three levels under the repo root, so `.parent.parent.parent` is the repo root — this is why the depth differs from docs_data.py (which is two levels up from `scripts/`).
    (2) Re-export the docs_fig surface: `from docs_fig import fig, FDARS_COLORS`. Importing docs_fig also runs its module-top `matplotlib.use("Agg")` and rcParams block (per PIPE-02) — do NOT set rcParams or the Agg backend again here; rely on the import side effect. Add `import matplotlib.pyplot as plt` (needed by `save_figure`).
    (3) `data_path(name: str) -> Path`: reject traversal — raise `ValueError` if `name` contains `..` or is an absolute path (`Path(name).is_absolute()` or ".." in Path(name).parts); then `root = Path(__file__).resolve().parent.parent.parent`; `p = root / "docs" / "data" / name`; raise `FileNotFoundError(f"dataset not found: {p}")` if not `p.exists()`; return `p`. Docstring: state that no dataset is copied — the paper pipeline resolves against the canonical docs/data/ directory (per PIPE-01, per D-14.0 data-reuse lock).
    (4) `save_figure(fig, path) -> None`: the PDF analog of docs_fig.render()'s SVG determinism. Call `fig.savefig(path, format="pdf", bbox_inches="tight", metadata={"CreationDate": None})` then `plt.close(fig)`. The suppressed CreationDate is what makes `git diff paper/figures/` empty on re-run (per PIPE-02). Docstring must NOT contain the literal token "CreationDate" as prose commentary describing suppression — name the mechanism only in the `metadata=` call itself (see comment-text discipline below).
    Then create `paper/code/gen_figures.py` (the pipeline entry Phases 88/89 extend): `from paper_utils import fig, save_figure` plus `import numpy as np`. Define one function `_smoke()` that seeds first — `np.random.seed(20260908)` on its own line BEFORE any random draw — draws a small deterministic random plot (e.g. `y = np.random.randn(50).cumsum()`), builds `f, ax = fig()`, plots, and calls `save_figure(f, Path(__file__).resolve().parent.parent / "figures" / "smoke.pdf")`. A `main()` calls `_smoke()` (Phases 88/89 append more figure calls here). Guard with `if __name__ == "__main__": main()`. Create `paper/code/.gitkeep` (empty).
    Create the `paper/figures/` directory by having gen_figures.py `mkdir(parents=True, exist_ok=True)` its output dir before saving. Then run the script once manually to produce the first `smoke.pdf`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; PYTHONPATH=scripts:paper/code .venv/bin/python paper/code/gen_figures.py &amp;&amp; test -f paper/figures/smoke.pdf &amp;&amp; PYTHONPATH=scripts:paper/code .venv/bin/python -c "import paper_utils as p; assert p.fig.__module__=='docs_fig'; assert len(p.FDARS_COLORS)==7; assert p.data_path('growth.csv').is_absolute() and p.data_path('growth.csv').exists(); rejected=False
try: p.data_path('../secret')
except ValueError: rejected=True
assert rejected, 'traversal not rejected'; print('OK')"
    <fails_when>gen_figures.py raises (e.g. ModuleNotFoundError: No module named 'docs_fig' → sys.path injection wrong, or wrong nesting depth); smoke.pdf not created; p.fig.__module__ != 'docs_fig' (palette/factory reimplemented instead of imported); data_path returns a relative path, fails to resolve growth.csv, or does not raise ValueError on '../secret'.</fails_when>
  </verify>
  <acceptance_criteria>
    - `paper/code/paper_utils.py` imports `fig`, `FDARS_COLORS` from `docs_fig` (no reimplementation), exposes `data_path` and `save_figure`.
    - `data_path('growth.csv')` returns an existing absolute path under `docs/data/`; `data_path('../x')` raises `ValueError`.
    - `paper/code/gen_figures.py` runs under `PYTHONPATH=scripts:paper/code`, seeds via `np.random.seed` before drawing, and writes `paper/figures/smoke.pdf`.
    - `save_figure` writes PDF with `metadata={"CreationDate": None}`.
  </acceptance_criteria>
  <done>The single figure path works end-to-end: `PYTHONPATH=scripts:paper/code python paper/code/gen_figures.py` produces `paper/figures/smoke.pdf` from a seeded draw using the imported docs_fig factory.</done>
</task>

<task type="auto">
  <name>Task 2: One-command Makefile runner — paper / paper-figures targets</name>
  <files>Makefile</files>
  <read_first>
    - Makefile — existing docs targets pattern: `export PYTHONPATH := scripts` at file top, `.PHONY` list, two-space-doc `##` comment convention, TAB-indented recipes. The existing `export PYTHONPATH := scripts` only covers docs; paper targets need `scripts:paper/code`.
    - .planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md — Pattern 3 (Makefile) and Pitfall 4 (PYTHONPATH must include both scripts/ and paper/code/).
  </read_first>
  <action>
    Extend the existing repo `Makefile` (do NOT overwrite it — use Edit). Add near the other variable/target definitions:
    - A variable `PAPER_PYTHONPATH := scripts:paper/code`.
    - Add `paper` and `paper-figures` to the `.PHONY` line.
    - `paper-figures:` recipe (TAB-indented) `PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/gen_figures.py` with a `##` doc comment "Regenerate all paper figures deterministically".
    - `paper:` target depending on `paper-figures` with a `##` doc comment "Run the one-command reproducible paper pipeline". (CFF validation is added in Plan 02's cff target and folded into `paper` there — for now `paper: paper-figures`.)
    Do NOT invoke tectonic or compile any PDF in these targets — LaTeX PDF compilation is CI-only (Phase 86 GATE-01); the local gate is the Python figure pipeline only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; grep -vE '^\s*#' Makefile | grep -qE '^\.PHONY:.*\bpaper\b' &amp;&amp; grep -vE '^\s*#' Makefile | grep -qE 'PAPER_PYTHONPATH.*scripts:paper/code' &amp;&amp; grep -vE '^\s*#' Makefile | grep -qE 'gen_figures\.py' &amp;&amp; ! grep -vE '^\s*#' Makefile | grep -qiE 'tectonic|pdflatex' &amp;&amp; echo MAKE_OK</automated>
    <fails_when>MAKE_OK not printed: `paper` missing from `.PHONY`; `PAPER_PYTHONPATH` not set to `scripts:paper/code`; recipe does not invoke `gen_figures.py`; or a tectonic/pdflatex invocation leaked into a Makefile recipe (PDF compile must stay CI-only).</fails_when>
  </verify>
  <acceptance_criteria>
    - `Makefile` defines `PAPER_PYTHONPATH := scripts:paper/code` and lists `paper`, `paper-figures` in `.PHONY`.
    - `paper-figures` runs `gen_figures.py` under the extended PYTHONPATH; `paper` depends on `paper-figures`.
    - No Makefile recipe invokes tectonic or pdflatex.
  </acceptance_criteria>
  <done>`make paper` regenerates all paper figures in one command with the correct PYTHONPATH; the existing docs targets are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove determinism — commit smoke.pdf, re-run make paper, assert empty git diff</name>
  <files>paper/figures/smoke.pdf</files>
  <read_first>
    - .planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-RESEARCH.md — Pitfall 2 (matplotlib PDF timestamp non-determinism) and Pitfall 3 (RNG seeding scope). This task is the determinism gate for PIPE-02.
  </read_first>
  <action>
    Stage and commit the generated `paper/figures/smoke.pdf` (and the Task 1/2 files) so it is a tracked artifact. Then re-run the pipeline via `make paper` and confirm `git diff --exit-code paper/figures/` reports no changes — proving the committed PDF is byte-identical on regeneration (suppressed `CreationDate` + per-figure seed). If the diff is non-empty, the determinism contract is broken: check that `save_figure` passes `metadata={"CreationDate": None}` and that `gen_figures.py` seeds before drawing. This is the local hard gate the milestone relies on (PIPE-02); no tectonic/PDF compile is involved.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; git add paper/figures/smoke.pdf paper/code/paper_utils.py paper/code/gen_figures.py paper/code/.gitkeep Makefile &amp;&amp; git commit -q -m "feat(85-01): reproducible paper figure pipeline" &amp;&amp; PYTHONPATH=scripts:paper/code .venv/bin/python paper/code/gen_figures.py &amp;&amp; git diff --exit-code paper/figures/ &amp;&amp; echo DETERMINISTIC_OK</automated>
    <fails_when>`git diff --exit-code paper/figures/` returns non-zero (smoke.pdf changed on re-run → non-deterministic: CreationDate not suppressed or RNG not seeded before draw); commit fails because an expected file is missing.</fails_when>
  </verify>
  <acceptance_criteria>
    - `paper/figures/smoke.pdf` is committed to git.
    - A second `make paper` / `gen_figures.py` run leaves `git diff paper/figures/` empty (byte-identical PDF).
  </acceptance_criteria>
  <done>Re-running the pipeline produces a byte-identical `paper/figures/smoke.pdf`; `git diff paper/figures/` is empty — the PIPE-02 determinism gate passes on a real committed artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data_path(name)` argument | `name` is a caller-supplied string used to build a filesystem path under `docs/data/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-01 | Tampering | `data_path(name)` path resolution | low | mitigate | Reject `name` containing `..` or an absolute path (raise `ValueError`); resolve only against the fixed `docs/data/` directory |
| T-85-02 | Information Disclosure | committed `paper/figures/*.pdf` metadata | low | mitigate | Suppress `CreationDate` (`metadata={"CreationDate": None}`) — no wall-clock/host leakage, and a deterministic byte-identical artifact |
</threat_model>

<verification>
- `PYTHONPATH=scripts:paper/code python paper/code/gen_figures.py` runs clean and writes `paper/figures/smoke.pdf`.
- `paper_utils.fig.__module__ == 'docs_fig'` (reuse, not reimplementation); `len(FDARS_COLORS) == 7`.
- `data_path('growth.csv')` resolves absolute + exists; `data_path('../secret')` raises `ValueError`.
- `make paper` is the one-command runner; no Makefile recipe invokes tectonic/pdflatex.
- Second pipeline run leaves `git diff paper/figures/` empty.
</verification>

<success_criteria>
- PIPE-01: `paper/code/paper_utils.py` reuses `scripts/docs_fig.py` (`fig`/`FDARS_COLORS`) and `data_path()` resolves `docs/data/` with no duplication; `make paper` is a one-command runner.
- PIPE-02: Agg backend + module-top rcParams (via docs_fig import) + per-figure `np.random.seed` + suppressed `CreationDate`; re-run leaves `git diff paper/figures/` empty.
</success_criteria>

<output>
Create `.planning/phases/85-manuscript-scaffold-pipeline-infrastructure/85-01-SUMMARY.md` when done.
</output>
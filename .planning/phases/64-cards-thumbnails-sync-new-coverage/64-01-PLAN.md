---
phase: 64-cards-thumbnails-sync-new-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/thumb/elastic-clustering.svg
  - docs/assets/cards/examples.svg
  - docs/assets/diagrams/sklearn-transformers.svg
  - docs/assets/diagrams/sklearn-regressors-classifiers.svg
  - docs/assets/diagrams/sklearn-clusterers-outliers.svg
  - docs/sklearn/transformers.md
  - docs/sklearn/regressors-classifiers.md
  - docs/sklearn/clusterers-outliers.md
  - docs/align/index.md
  - docs/analyze/index.md
  - docs/examples/index.md
  - docs/learn/index.md
  - docs/monitoring/index.md
  - docs/represent/index.md
  - docs/regression/index.md
autonomous: true
requirements: [SYNC-01, SYNC-02, A11Y-03, COVER-01]

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "The elastic-clustering gallery thumb depicts the NEW concept (curve families grouped into shape-based clusters), not the old before/after alignment motif (SYNC-01)."
    - "examples.svg card is reviewed against SYNC-02; either improved without losing the gallery motif, or explicitly recorded as acceptable in the SUMMARY (SYNC-02)."
    - "Every gallery <img class=fdars-gallery-thumb> across all 7 section index pages carries aria-hidden=true (A11Y-03)."
    - "docs/sklearn/{transformers,regressors-classifiers,clusterers-outliers}.md each reference a new hand-authored concept SVG that renders and is method-accurate (COVER-01)."
  artifacts:
    - docs/assets/diagrams/sklearn-transformers.svg
    - docs/assets/diagrams/sklearn-regressors-classifiers.svg
    - docs/assets/diagrams/sklearn-clusterers-outliers.svg
    - docs/assets/thumb/elastic-clustering.svg
  key_links:
    - "Each new sklearn SVG is wired into its page via a .fdars-diagram image reference — the diagram file must exist AND the page must reference it."
    - "aria-hidden edit must land on the img tags only; empty alt stays intact (do not remove alt)."
---

<objective>
Bring pyfda's derivative documentation assets into sync with the concept diagrams corrected in Phases 61-63, fix decorative-thumbnail accessibility semantics, and add the three audit-identified missing sklearn concept diagrams.

Delivers SYNC-01 (redraw the one drifted thumb), SYNC-02 (review the 8 cards), A11Y-03 (aria-hidden on decorative gallery thumbs), COVER-01 (3 new method-family concept diagrams wired into their pages).

Purpose: Close the sync and coverage gaps recorded in 60-AUDIT.md sections 3 and 4 so every gallery thumbnail matches its now-corrected concept diagram, decorative thumbs no longer double-announce to screen readers, and the three sklearn method-family pages each carry a method-accurate concept diagram.

Output: 1 redrawn thumb, up to 1 reviewed card, 3 new inline SVG concept diagrams, 3 sklearn pages wired to them, and 7 gallery index pages with aria-hidden applied.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-cards-thumbnails-sync-new-coverage/64-CONTEXT.md

# Style + method reference for the new diagrams (READ before drawing)
@docs/assets/diagrams/STYLE_SPEC.md
@docs/assets/diagrams/sklearn-pipeline-dataflow.svg

# The redrawn concept whose thumb must now be synced (READ before redrawing thumb)
@docs/assets/diagrams/elastic-clustering.svg

# Page prose the 3 new diagrams must faithfully depict (READ before drawing each)
@docs/sklearn/transformers.md
@docs/sklearn/regressors-classifiers.md
@docs/sklearn/clusterers-outliers.md
</context>

<constraints>
- Hand-authored inline SVG ONLY (locked decision). No SVG generators, no scripts that emit SVG.
- Docs-only phase. Do NOT touch fdars-core, Rust bindings, the advisor, or the Python package.
- Do NOT run the whole-site MkDocs build — that is Phase 65. Verify by rendering changed/new SVGs to PNG with rsvg-convert.
- Do NOT edit docs/assets/diagrams/STYLE_SPEC.md (Phase 65).
- Do NOT re-edit the 90 concept diagrams already corrected in Phases 61/62/63.
- Do NOT regenerate the 57 faithful thumbs — only elastic-clustering.svg thumb has Major drift.
- Do NOT add a concept diagram to docs/sklearn/index.md (has one), coverage.md, or gridsearch-example.md.
- New concept diagrams: viewBox 0 0 720 300, canonical five-class style block copied verbatim from STYLE_SPEC, STYLE_SPEC palette + stroke weights + panel patterns, and accessible from creation (role=img + aria-label + title + desc + aria-labelledby wiring both ids). Match the sklearn-pipeline-dataflow.svg house style.
</constraints>

<tasks>

<task type="tracer">
  <name>Task 1: COVER-01 tracer — sklearn-transformers.svg end-to-end (draw + wire + render-verify)</name>
  <files>docs/assets/diagrams/sklearn-transformers.svg, docs/sklearn/transformers.md</files>
  <read_first>
    - docs/assets/diagrams/sklearn-pipeline-dataflow.svg — the house-style reference: root svg attributes, aria-labelledby wiring to title id + desc id, verbatim style block, orange method-panel pattern (fill #fff4ea / stroke #fd7e14), neutral panel pattern (fill #f8f9fa / stroke #ced4da), arrowhead marker defs, title at y approx 28 + subtitle at y approx 46.
    - docs/assets/diagrams/STYLE_SPEC.md — canonical style block (lines 40-48), palette table, stroke-weight table, viewBox conventions (720x300), panel patterns, accessibility pattern.
    - docs/sklearn/transformers.md — method prose the diagram must faithfully depict.
  </read_first>
  <action>Create docs/assets/diagrams/sklearn-transformers.svg as a hand-authored inline concept diagram, method-accurate to transformers.md (per COVER-01). Depict what the 8 TransformerMixin estimators actually do: raw functional data (n_obs, n_points) entering, passing through the transformer family, emerging as a downstream-ready numeric matrix. Show the two accurate output-shape families the page describes — reducing transformers that emit (n_obs, k) score/coefficient matrices (FPCATransformer to FPC scores, BasisRepresentation to coefficients) rendered as orange method panels, and per-curve transformers that emit (n_obs, 1) or reshape in place (DepthTransformer, NormTransformer to a single scalar column; Imputer, BSplineSmoother, LocalPolynomialSmoother, SplineInterpolator to cleaned/resampled (n_obs, n_points) curves) as neutral panels. Label the input as X ndarray (n_obs, n_points) and outputs with their true shapes. Do NOT invent estimators not on the page; do NOT claim an Fdata object is constructed (the page states estimators call fdars._native.* directly and never build an Fdata). Use viewBox 0 0 720 300, the verbatim canonical style block, STYLE_SPEC palette and stroke weights, and the neutral/orange panel patterns. Add root role=img plus aria-label matching the diagram title, plus a title element id t-sktr and a long-form desc element id d-sktr that names the transformer families and their real output shapes, and wire aria-labelledby to the pair t-sktr d-sktr on the root svg. Then wire it into transformers.md as an image reference using the .fdars-diagram class pointing at ../assets/diagrams/sklearn-transformers.svg with descriptive alt text (raw curves to cleaning/smoothing/reduction to downstream-ready matrix), placed just after the section-hero div (mirror the placement convention used in docs/sklearn/index.md, where the diagram sits near the top under the intro prose).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && f=sklearn-transformers; p=docs/assets/diagrams/$f.svg; ok=1; test -f "$p" || { echo "MISSING $p"; ok=0; }; rsvg-convert -o /tmp/64-$f.png "$p" 2>/tmp/64-$f.err && test -s /tmp/64-$f.png || { echo "RENDER FAIL $f"; cat /tmp/64-$f.err; ok=0; }; grep -q 'viewBox="0 0 720 300"' "$p" || { echo "VIEWBOX WRONG"; ok=0; }; grep -q 'role="img"' "$p" && grep -q 'aria-labelledby' "$p" && grep -q '<title' "$p" && grep -q '<desc' "$p" || { echo "A11Y MISSING"; ok=0; }; grep -q '\.ttl{' "$p" && grep -q '\.mono{' "$p" || { echo "STYLE BLOCK MISSING"; ok=0; }; grep -q 'assets/diagrams/sklearn-transformers.svg){ .fdars-diagram }' docs/sklearn/transformers.md || { echo "PAGE NOT WIRED"; ok=0; }; test "$ok" -eq 1</automated>
    <fails_when>The SVG file is missing; it does not render via rsvg-convert; viewBox is not 720x300; role/aria-labelledby/title/desc are absent; the canonical .ttl/.mono style classes are absent; or transformers.md does not reference the new diagram with the .fdars-diagram class.</fails_when>
  </verify>
  <done>docs/assets/diagrams/sklearn-transformers.svg exists, renders to a non-empty PNG, is STYLE_SPEC-conformant (720x300, canonical style block, palette/panels), is accessible from creation (role/aria-label/title/desc/aria-labelledby), and transformers.md references it via the .fdars-diagram convention. This proves the full COVER-01 path (draw to conform to accessible to wire to render) end-to-end for the remaining two diagrams to follow.</done>
</task>

<task type="auto">
  <name>Task 2: COVER-01 expansion — sklearn-regressors-classifiers.svg + sklearn-clusterers-outliers.svg (draw + wire + render-verify)</name>
  <files>docs/assets/diagrams/sklearn-regressors-classifiers.svg, docs/assets/diagrams/sklearn-clusterers-outliers.svg, docs/sklearn/regressors-classifiers.md, docs/sklearn/clusterers-outliers.md</files>
  <read_first>
    - docs/assets/diagrams/sklearn-transformers.svg — the just-created sibling; match its layout language, title/subtitle placement, panel spacing, and aria-labelledby id-pairing pattern.
    - docs/sklearn/regressors-classifiers.md — 5 regressors + 6 classifiers; the shared FPC-score predict pattern (FPC basis stored at fit, new curves projected to scores at predict, stored sklearn model predicts — no re-fit, no vstack).
    - docs/sklearn/clusterers-outliers.md — 3 ClusterMixin + 6 OutlierMixin; note the method-accuracy honesty box (only MagnitudeShapeDetector is method-faithful; the other five use a subset-invariant modified-band-depth surrogate score).
  </read_first>
  <action>Create both remaining COVER-01 diagrams as hand-authored inline SVGs, each viewBox 0 0 720 300, canonical style block, STYLE_SPEC palette/strokes/panels, and accessible from creation (role=img + aria-label + title + desc + aria-labelledby wiring both ids), matching the house style established in Task 1 and sklearn-pipeline-dataflow.svg.

For sklearn-regressors-classifiers.svg (method-accurate to regressors-classifiers.md): depict the shared FPC-score predict pipeline the page centers on — (n_obs, n_points) curves to FPC scores (n_obs, k) via the stored FPC basis (orange method panel) to two prediction branches: a RegressorMixin branch outputting scalar y-hat (FPCRegressor/PLSRegressor/RobustFPCRegressor/GLMRegressor/NonparametricRegressor) and a ClassifierMixin branch outputting class labels (FPCLDA/FPCQDA/FPCKNN/DD/LogisticFPC/ElasticMultinomial). Convey the accurate story via a short annotation: stored FPC basis, project new curves, stored sklearn model predicts — no re-fit; do NOT depict a re-fit at predict time. Use aria-labelledby wiring the pair t-skrc d-skrc with matching title id and desc id.

For sklearn-clusterers-outliers.svg (method-accurate to clusterers-outliers.md): depict two families — Clusterers (FunctionalKMeans/FuzzyFunctionalCMeans/FunctionalGMM) grouping curves into clusters via fit_predict to integer cluster labels; and Outlier detectors labelling each curve inlier (+1) / outlier (-1) via predict with a continuous score_samples. Method-accuracy is a hard gate: represent that MagnitudeShapeDetector is the method-faithful MS-plot detector while the other five rank by a subset-invariant modified-band-depth surrogate — e.g. distinguish the faithful detector from the surrogate-scored group by panel treatment or a small annotation, so the diagram does not imply all six use their eponymous batch statistic at score time. Use aria-labelledby wiring the pair t-skco d-skco with matching title id and desc id.

Wire each into its page just after the section-hero div: regressors-classifiers.md gets a .fdars-diagram image reference to ../assets/diagrams/sklearn-regressors-classifiers.svg (alt: curves to stored FPC scores to scalar y-hat or class labels); clusterers-outliers.md gets a .fdars-diagram image reference to ../assets/diagrams/sklearn-clusterers-outliers.svg (alt: curves to cluster labels or inlier/outlier flags).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; for f in sklearn-regressors-classifiers sklearn-clusterers-outliers; do case "$f" in sklearn-regressors-classifiers) pg=regressors-classifiers;; sklearn-clusterers-outliers) pg=clusterers-outliers;; esac; p=docs/assets/diagrams/$f.svg; test -f "$p" || { echo "MISSING $p"; ok=0; }; rsvg-convert -o /tmp/64-$f.png "$p" 2>/tmp/64-$f.err && test -s /tmp/64-$f.png || { echo "RENDER FAIL $f"; cat /tmp/64-$f.err; ok=0; }; grep -q 'viewBox="0 0 720 300"' "$p" || { echo "VIEWBOX WRONG $f"; ok=0; }; grep -q 'role="img"' "$p" && grep -q 'aria-labelledby' "$p" && grep -q '<title' "$p" && grep -q '<desc' "$p" || { echo "A11Y MISSING $f"; ok=0; }; grep -q '\.ttl{' "$p" && grep -q '\.mono{' "$p" || { echo "STYLE BLOCK MISSING $f"; ok=0; }; grep -q "assets/diagrams/$f.svg){ .fdars-diagram }" "docs/sklearn/$pg.md" || { echo "PAGE NOT WIRED $f"; ok=0; }; done; test "$ok" -eq 1</automated>
    <fails_when>Either SVG is missing; does not render; viewBox is not 720x300; role/aria-labelledby/title/desc are absent; canonical style classes are absent; or its target page (regressors-classifiers.md / clusterers-outliers.md) does not reference the diagram with the .fdars-diagram class.</fails_when>
  </verify>
  <done>Both new SVGs exist, render to non-empty PNGs, are STYLE_SPEC-conformant and accessible from creation, are method-accurate to their page prose (regressor/classifier FPC-score pipeline; clusterer/outlier families with the MS-plot-vs-surrogate honesty distinction), and each is wired into its page via the .fdars-diagram convention.</done>
</task>

<task type="auto">
  <name>Task 3: SYNC-01 — redraw elastic-clustering thumb + SYNC-02 — review 8 cards</name>
  <files>docs/assets/thumb/elastic-clustering.svg, docs/assets/cards/examples.svg</files>
  <read_first>
    - docs/assets/diagrams/elastic-clustering.svg — the NEW concept (redrawn Phase 62): a mixed-phase curve family to elastic (SRVF/Fisher-Rao amplitude) distance to clusters grouped by amplitude shape (two clusters: low-amplitude indigo, high-amplitude orange). The thumb must abstract THIS, not the old before/after alignment motif.
    - docs/assets/thumb/elastic-clustering.svg — the current drifted thumb (old before/after alignment wave motif; purple #6f42c1). Note the thumb canvas convention: viewBox 0 0 320 180, role=img + aria-label, inline path draws, no style block (thumbs are a separate asset class from concept diagrams).
    - docs/assets/thumb/clustering.svg — a faithful same-section thumb, for the clusters-of-curve-families visual idiom and thumb styling.
    - docs/assets/cards/examples.svg — the one Minor-flagged card (six abstract icon panels; the examples-gallery motif). 60-AUDIT section 3 judged it cosmetic-only / deliberate motif.
  </read_first>
  <action>SYNC-01: Redraw docs/assets/thumb/elastic-clustering.svg so it abstracts the NEW concept — curve families grouped into shape-based clusters (mirror the redrawn concept clusters-of-curves motif: two distinct clusters of same-shape curves at different amplitude levels). Keep the thumb-class conventions: viewBox 0 0 320 180, role=img with an aria-label describing the new concept (e.g. Elastic clustering of curve families), inline path draws consistent with sibling thumbs, and NO style block (thumbs do not use the concept STYLE_SPEC five-class system — see 60-AUDIT section 4 baseline). Do NOT depict the old alignment arrow or a before/after transition. It is acceptable to reuse the palette idiom of the concept (indigo #3f51b5 / orange #e8710a for the two clusters) or a single-hue treatment, provided the motif clearly reads as two clusters of curve families. The redraw must render cleanly via rsvg-convert.

SYNC-02: Review all 8 cards in docs/assets/cards/ against 60-AUDIT section 3. Seven scored OK; only examples.svg scored Minor (abstract icons rather than section-representative — cosmetic-only, deliberate motif). Decide: improve examples.svg ONLY if a change clearly increases representativeness WITHOUT losing the deliberate six-panel gallery motif; otherwise leave it unchanged and record it as acceptable. Do NOT touch the other 7 cards. Record the SYNC-02 decision (improved vs. accepted-as-is, with rationale) in the plan SUMMARY.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; t=docs/assets/thumb/elastic-clustering.svg; rsvg-convert -o /tmp/64-thumb-ec.png "$t" 2>/tmp/64-thumb-ec.err && test -s /tmp/64-thumb-ec.png || { echo "THUMB RENDER FAIL"; cat /tmp/64-thumb-ec.err; ok=0; }; grep -q 'viewBox="0 0 320 180"' "$t" || { echo "THUMB VIEWBOX WRONG"; ok=0; }; grep -q 'role="img"' "$t" || { echo "THUMB ROLE MISSING"; ok=0; }; git -C /home/simonm/projects/rust/pyfda diff --quiet -- docs/assets/thumb/elastic-clustering.svg && { echo "THUMB NOT CHANGED"; ok=0; }; if grep -qiE 'before|after|align' "$t"; then echo "THUMB STILL SHOWS ALIGNMENT MOTIF"; ok=0; fi; rsvg-convert -o /dev/null docs/assets/cards/examples.svg 2>/dev/null || { echo "CARD RENDER FAIL"; ok=0; }; test "$ok" -eq 1</automated>
    <fails_when>The thumb fails to render; its viewBox is not 320x180; role=img is missing; the thumb file is unchanged from HEAD; the thumb text/aria still mentions before/after/align (old alignment motif retained); or examples.svg no longer renders.</fails_when>
  </verify>
  <done>The elastic-clustering thumb is redrawn to the new clusters-of-curve-families motif (renders cleanly, 320x180, role=img, no alignment-motif text, changed from HEAD). All 8 cards are reviewed; examples.svg is either improved without losing its gallery motif or recorded as accepted-as-is in the SUMMARY. No other cards or thumbs are touched.</done>
</task>

<task type="auto">
  <name>Task 4: A11Y-03 — aria-hidden on all decorative gallery thumbnails</name>
  <files>docs/align/index.md, docs/analyze/index.md, docs/examples/index.md, docs/learn/index.md, docs/monitoring/index.md, docs/represent/index.md, docs/regression/index.md</files>
  <read_first>
    - docs/analyze/index.md — sample gallery page; confirm the img shape is class fdars-gallery-thumb with src to ../assets/thumb/NAME.svg and alt empty. The 58 gallery imgs across 7 pages share this exact shape.
  </read_first>
  <action>Add aria-hidden="true" to every gallery img element carrying class fdars-gallery-thumb across all 7 section index pages (align, analyze, examples, learn, monitoring, represent, regression). These thumbs duplicate their adjacent link title text, so they are decorative (A11Y-03). Keep the existing empty alt attribute intact — do NOT remove alt, and do NOT change src. Apply consistently to all 58 occurrences. A reliable approach: transform each occurrence of the fixed substring class="fdars-gallery-thumb" into class="fdars-gallery-thumb" aria-hidden="true" (replace-all within each file). Verify no occurrence is missed and no img gains a duplicate aria-hidden.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && total=$(grep -h 'fdars-gallery-thumb' docs/*/index.md | wc -l); hidden=$(grep -h 'fdars-gallery-thumb' docs/*/index.md | grep -c 'aria-hidden="true"'); dup=$(grep -ho 'aria-hidden="true".*aria-hidden="true"' docs/*/index.md | wc -l); noalt=$(grep -h 'fdars-gallery-thumb' docs/*/index.md | grep -cv 'alt=""'); echo "total=$total hidden=$hidden dup=$dup noalt=$noalt"; test "$total" -eq 58 && test "$hidden" -eq 58 && test "$dup" -eq 0 && test "$noalt" -eq 0</automated>
    <fails_when>The count of fdars-gallery-thumb imgs is not 58; fewer than 58 carry aria-hidden=true; any img has a duplicated aria-hidden attribute; or any gallery-thumb img lost its empty alt attribute.</fails_when>
  </verify>
  <done>All 58 gallery img elements across the 7 section index pages carry aria-hidden="true" alongside their preserved empty alt; no duplicates; src unchanged. Decorative thumbs no longer double-announce to screen readers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SVG/markdown -> MkDocs build (Phase 65) | Hand-authored content is rendered on the public docs site; malformed SVG or a broken image reference would surface as a build/visual defect downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | New/edited inline SVG assets | low | mitigate | Each SVG is render-verified with rsvg-convert (non-empty PNG) and grep-checked for viewBox + a11y markers before commit; no external/generated content is introduced. |
| T-64-02 | Information Disclosure | Method-accuracy of new diagrams | medium | mitigate | Diagrams are drawn from the current page prose (transformers/regressors-classifiers/clusterers-outliers.md); the MS-plot-vs-surrogate honesty distinction and the no-re-fit predict story are represented so the docs do not overstate method fidelity. Carried into the Phase-65 blocking human review checklist. |
| T-64-03 | Denial of Service | Broken image references in sklearn pages | low | mitigate | Verify gates grep each page for the exact .fdars-diagram reference to the file that must exist, so a page cannot ship pointing at a missing asset. |
</threat_model>

<verification>
Run all four task <automated> gates. Additionally, whole-phase scope + sync checks:

```bash
cd /home/simonm/projects/rust/pyfda && ok=1
# 3 new sklearn diagrams exist, render, are a11y-complete
for f in sklearn-transformers sklearn-regressors-classifiers sklearn-clusterers-outliers; do
  p=docs/assets/diagrams/$f.svg
  test -f "$p" && rsvg-convert -o /dev/null "$p" 2>/dev/null || { echo "DIAGRAM FAIL $f"; ok=0; }
  grep -q 'aria-labelledby' "$p" && grep -q '<desc' "$p" || { echo "A11Y FAIL $f"; ok=0; }
done
# 3 sklearn pages wired
for pg in transformers regressors-classifiers clusterers-outliers; do
  grep -q "assets/diagrams/sklearn-$pg.svg){ .fdars-diagram }" "docs/sklearn/$pg.md" 2>/dev/null \
    || grep -q "assets/diagrams/sklearn-${pg}.svg){ .fdars-diagram }" "docs/sklearn/$pg.md" \
    || { echo "PAGE NOT WIRED $pg"; ok=0; }
done
# A11Y-03 coverage
test "$(grep -h 'fdars-gallery-thumb' docs/*/index.md | grep -c 'aria-hidden="true"')" -eq 58 || { echo "A11Y-03 INCOMPLETE"; ok=0; }
# elastic-clustering thumb changed + renders
git diff --quiet -- docs/assets/thumb/elastic-clustering.svg && { echo "THUMB UNCHANGED"; ok=0; }
rsvg-convert -o /dev/null docs/assets/thumb/elastic-clustering.svg 2>/dev/null || { echo "THUMB RENDER FAIL"; ok=0; }
# Scope guard: forbidden paths untouched
git diff --name-only -- docs/assets/diagrams/STYLE_SPEC.md | grep -q . && { echo "STYLE_SPEC TOUCHED"; ok=0; }
test "$ok" -eq 1 && echo "PHASE 64 VERIFICATION PASS"
```
Note the 3-page wiring naming maps regressors-classifiers.md -> sklearn-regressors-classifiers.svg and clusterers-outliers.md -> sklearn-clusterers-outliers.svg.
</verification>

<success_criteria>
- SYNC-01: elastic-clustering thumb redrawn to the new clusters-of-curve-families concept, renders cleanly, no old alignment motif.
- SYNC-02: all 8 cards reviewed; examples.svg improved-or-accepted decision recorded in SUMMARY; other 7 cards untouched.
- A11Y-03: all 58 decorative gallery imgs carry aria-hidden="true" with empty alt preserved; no duplicates.
- COVER-01: 3 new method-accurate, STYLE_SPEC-conformant, accessible sklearn concept diagrams created and wired into their 3 pages; each renders via rsvg-convert.
- Scope: STYLE_SPEC.md untouched; the 90 corrected concept diagrams untouched; the 57 faithful thumbs untouched; no fdars-core/binding/package edits.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces
- docs/assets/diagrams/sklearn-transformers.svg (NEW concept diagram)
- docs/assets/diagrams/sklearn-regressors-classifiers.svg (NEW concept diagram)
- docs/assets/diagrams/sklearn-clusterers-outliers.svg (NEW concept diagram)
- docs/assets/thumb/elastic-clustering.svg (REDRAWN thumb)
- docs/sklearn/{transformers,regressors-classifiers,clusterers-outliers}.md (diagram references added)
- docs/{align,analyze,examples,learn,monitoring,represent,regression}/index.md (aria-hidden added to gallery imgs)
- docs/assets/cards/examples.svg (reviewed; edited only if it clearly improves representativeness)

These 3 new SVGs + the redrawn thumb carry into the Phase-65 blocking human review checklist for method-accuracy sign-off.
</artifacts_produced>

<output>
Create `.planning/phases/64-cards-thumbnails-sync-new-coverage/64-01-SUMMARY.md` when done. Record the SYNC-02 decision (examples.svg improved vs accepted-as-is, with rationale) in the SUMMARY.
</output>

---
phase: 79-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
use_worktrees: false
requirements: [GATE-02]

estimate:
  tokens: 22000
  raw_tokens: 15000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "The SVGO two-pass transform is byte-stable (pass2 == pass1) for every one of the 21 Phase-77 thumbnails and every concept diagram in docs/assets/diagrams/ (GATE-02)."
  artifacts: []
  key_links:
    - "svgo.config.mjs governs both the CI diagrams gate and the Phase-77 thumbnail check — same config, both directories."
---

<objective>
GATE-02 — prove SVGO idempotence + build-determinism across all new/changed SVGs this
milestone: the 21 Phase-77 thumbnails in `docs/assets/thumb/` (CARD-06 requirement) plus the
CI-scope concept diagrams in `docs/assets/diagrams/`. A two-pass SVGO transform must produce
byte-identical output on the second pass for every file.

Purpose: The published SVG source must be conformant and stable under the project's SVGO
config so the determinism gate (FND-03 / GATE-02) holds; unstable SVGs would drift on every
CI run. RESEARCH pre-verified 0/21 thumbs + 0/101 diagrams unstable this session — this plan
re-confirms it as a committed gate result.
Output: A recorded PASS (no file changes; this is a read-only verification gate).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-close-gate/79-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-02 — SVGO two-pass idempotence across all thumbnails + concept diagrams</name>
  <files>(none — read-only verification gate; modifies no files)</files>
  <read_first>
    - .planning/phases/79-close-gate/79-RESEARCH.md § Section 2 (exact command, the 21 thumbnail slugs, SVGO version safety, pre-verification results)
    - svgo.config.mjs (the config both loops pass to --config)
  </read_first>
  <action>
    Run the SVGO idempotence gate exactly as specified in 79-RESEARCH.md Section 2 (GATE-02),
    covering BOTH SVG sets that changed this milestone. Use `npx svgo@3.3.4` explicitly (NOT a
    bare `svgo`) and always pass `--config svgo.config.mjs` — RESEARCH confirmed no global svgo
    is installed and node v24 + npx-fetched 3.3.4 is the safe toolchain.

    Set 1 — the 21 Phase-77 thumbnails (CARD-06 requirement, `docs/assets/thumb/<slug>.svg`):
    for each thumbnail, transform once (pass1), feed pass1 back through SVGO (pass2), and diff
    pass1 vs pass2. Any non-empty diff = that thumbnail is unstable = FAIL. The 21 slugs are
    enumerated in RESEARCH Section 2 (additive-sof, advanced-clustering, banded-alignment,
    canadian-depth-centrality, concurrent-regression, density-fda, frechet-regression,
    functional-boxplot, functional-glm, functional-outlier-workflow, functional-statistics,
    functional-time-series, function-on-function, imputation, interpolation, multi-domain,
    pace-fpca, scoring-metrics, shapelets, shift-registration, tolerance-vs-conformal).

    Set 2 — the CI-scope concept diagrams (`docs/assets/diagrams/*.svg`), matching the
    `.github/workflows/docs.yml` gate glob exactly: same two-pass diff per file. RESEARCH
    confirmed no concept diagram changed in v12 (function-on-function.svg edit noted upstream is
    already accounted for in the thumbnail/diagram sets), but re-run the full glob to re-confirm
    stability across the committed diagram set.

    Do NOT overwrite any SVG in place — the transform output goes to stdout/temp only; this gate
    reads source files and compares transform passes, it does not edit committed SVGs. If any
    file is unstable, do not attempt an autonomous rewrite: report the offending file(s) and stop
    (a redraw is a human diagram concern, folded into GATE-03).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && FAILED=0; for svg in docs/assets/thumb/additive-sof.svg docs/assets/thumb/advanced-clustering.svg docs/assets/thumb/banded-alignment.svg docs/assets/thumb/canadian-depth-centrality.svg docs/assets/thumb/concurrent-regression.svg docs/assets/thumb/density-fda.svg docs/assets/thumb/frechet-regression.svg docs/assets/thumb/functional-boxplot.svg docs/assets/thumb/functional-glm.svg docs/assets/thumb/functional-outlier-workflow.svg docs/assets/thumb/functional-statistics.svg docs/assets/thumb/functional-time-series.svg docs/assets/thumb/function-on-function.svg docs/assets/thumb/imputation.svg docs/assets/thumb/interpolation.svg docs/assets/thumb/multi-domain.svg docs/assets/thumb/pace-fpca.svg docs/assets/thumb/scoring-metrics.svg docs/assets/thumb/shapelets.svg docs/assets/thumb/shift-registration.svg docs/assets/thumb/tolerance-vs-conformal.svg; do first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$svg" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null || { echo "SVGO UNSTABLE (thumb): $svg"; FAILED=1; }; done; for svg in docs/assets/diagrams/*.svg; do first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$svg" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null || { echo "SVGO UNSTABLE (diagram): $svg"; FAILED=1; }; done; [ $FAILED -eq 0 ] && echo "GATE-02 PASS: all thumbnails + diagrams SVGO-stable" || { echo "GATE-02 FAIL"; exit 1; }</automated>
    <fails_when>Any thumbnail OR any concept diagram produces a non-empty diff between its first and second SVGO pass (pass2 != pass1), i.e. the file is not idempotent under svgo.config.mjs.</fails_when>
  </verify>
  <done>Every one of the 21 Phase-77 thumbnails and every `docs/assets/diagrams/*.svg` is byte-stable under a two-pass `npx svgo@3.3.4 --config svgo.config.mjs` transform; the gate prints `GATE-02 PASS` and exits 0. No SVG files were modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx package fetch → local exec | `npx svgo@3.3.4` fetches a pinned package version from the npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Tampering | `npx svgo@3.3.4` fetch | low | accept | Version is pinned (`@3.3.4`); no global svgo installed (RESEARCH-verified); read-only transform, no file writes; matches the committed CI gate. No new attack surface introduced by this phase. |
</threat_model>

<verification>
GATE-02 passes when the automated two-pass SVGO check reports `GATE-02 PASS` for both the 21
Phase-77 thumbnails and the full `docs/assets/diagrams/*.svg` glob, exit 0, with no committed
SVG modified.
</verification>

<success_criteria>
SVGO idempotence + build-determinism gate is green across all new/changed SVGs this milestone
(GATE-02), confirmed by an exit-0 two-pass check over `docs/assets/thumb/` (21 files) and
`docs/assets/diagrams/` (CI-scope glob).
</success_criteria>

<output>
Create `.planning/phases/79-close-gate/79-01-SUMMARY.md` when done, recording the GATE-02 PASS
result and confirming no SVG files were modified.
</output>

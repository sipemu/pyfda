---
phase: 60-diagram-quality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/60-diagram-quality-audit/60-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
user_setup: []
estimate:
  tokens: 115000
  raw_tokens: 58000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "60-AUDIT.md exists and inventories all 90 concept SVGs in docs/assets/diagrams/ (maxdepth 1), one scored row per file, grouped by owning docs section (learn, represent, align, analyze, monitoring, advisor, sklearn, regression, inference, examples) (AUDIT-01)."
    - "Each concept diagram is scored on all four axes — design/geometry quality, STYLE_SPEC conformance, accessibility, sync — on the OK/Minor/Major/Critical scale, with a one-line evidence note on every non-OK cell (AUDIT-01, AUDIT-02)."
    - "The design/geometry verdict for each concept diagram is backed by an actual rsvg-convert PNG render (rendered and visually inspected), not judged from XML source alone — the only reliable way to catch mismatched-line / misaligned-endpoint / overlapping-element / layout defects (per 60-CONTEXT.md visual-inspection decision)."
    - "The accessibility axis records, per concept diagram, whether role img plus a title-matching aria-label are present (A11Y-01 basis) AND whether long-form title/desc/aria-labelledby is present/adequate on complex/multi-panel diagrams (A11Y-02 basis) — flagging the universal long-form gap that Phases 61/62/63 must close."
    - "60-AUDIT.md records the authoritative section-to-concept-diagram map and the correction-phase bucket assignment (61 learn/represent/align, 62 analyze/monitoring/advisor plus the sklearn edge case, 63 regression/inference/examples) so all 90 concept diagrams land in exactly one downstream fix bucket, none dropped or duplicated."
  artifacts:
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md
  key_links:
    - "60-AUDIT.md concept scoring table plus section-to-bucket map -> consumed by Plan 60-02 to derive the ranked per-section fix worklists and the SYNC drift comparison base."
    - "60-AUDIT.md per-section concept fix rows -> consumed by Phases 61/62/63 as their evidence-backed correction worklists."
---

<objective>
Begin `.planning/phases/60-diagram-quality-audit/60-AUDIT.md` — the single evidence document that gates the entire v10.0 milestone — by establishing its skeleton and scoring all 90 concept SVGs in `docs/assets/diagrams/` on the four axes (design/geometry quality, STYLE_SPEC conformance, accessibility, sync), with each design/geometry verdict backed by an actual `rsvg-convert` PNG render, and by recording the authoritative section-to-concept-diagram map plus the correction-phase bucket assignment (61/62/63).

Purpose: the concept-diagram scored inventory is the milestone-critical core of AUDIT-01/AUDIT-02 — it drives the Phase 61/62/63 correction worklists (assembled in Plan 60-02) and provides the base against which Plan 60-02 detects thumb/card drift. This plan depends on nothing.
Output: `.planning/phases/60-diagram-quality-audit/60-AUDIT.md` (skeleton + concept-diagram scoring table + section/bucket map).

READ-ONLY ANALYSIS PHASE: no SVG is edited, no docs page is rewritten, STYLE_SPEC.md is NOT edited (its stale counts are refreshed later in Phase 65 / SPEC-02). PNGs rendered for visual inspection are scratch artifacts — write them under the scratchpad or a gitignored temp dir; do NOT commit any PNG and do NOT modify any committed SVG. SVGO, if invoked as a conformance cross-check, is run check-only (idempotence), never with rewrite output onto a source file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-diagram-quality-audit/60-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@.planning/milestones/v7.0-phases/42-diagram-audit/42-AUDIT.md

Section-to-concept-diagram map already established during planning (owning docs section per SVG, from `grep -rl "diagrams/<file>" docs --include='*.md'`, taking the referencing non-index method page). Tally: examples 21, regression 15, analyze 12, represent 10, advisor 10, align 8, learn 6, inference 4, monitoring 3, sklearn 1 = 90 concept diagrams.

Correction-phase buckets (all 90 partitioned, none dropped/duplicated):
- Phase 61 = learn (6) + represent (10) + align (8) = 24
- Phase 62 = analyze (12) + monitoring (3) + advisor (10) = 25, PLUS the sklearn edge case `sklearn-pipeline-dataflow.svg` (the only sklearn-section concept diagram; the CONTEXT bucket list predates the v9.0 sklearn section — assign it to Phase 62 as the closest surface-family fit and state this explicitly) = 26
- Phase 63 = regression (15) + inference (4) + examples (21) = 40
- 24 + 26 + 40 = 90.

Accessibility calibration confirmed during planning (grep over docs/assets/diagrams/*.svg):
- All 90 concept diagrams already carry `role="img"` and `aria-label` (A11Y-01 basis broadly present — still verify per diagram that the aria-label MATCHES the title text).
- ZERO concept diagrams carry `<title>`, `<desc>`, or `aria-labelledby` — the A11Y-02 long-form gap is universal; the audit must flag it per complex/multi-panel diagram so Phases 61/62/63 have the worklist.

Tooling confirmed during planning: `rsvg-convert` at /usr/bin/rsvg-convert; `svgo.config.mjs` present at repo root; scratch dir available at the session scratchpad. STYLE_SPEC markers are grep-reproducible: viewBox width 720 with height 300/480/520; five CSS classes `.ttl .sub .lab .sm .mono`; `system-ui` font stack; `role="img"` + `aria-label` on root `<svg>`. Known-stale STYLE_SPEC diagrams (non-720 viewBox): `elastic-clustering.svg`, `outlier-detection.svg`, `covariance-functions.svg`, `ex-sonar-tsrvf.svg`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end audit slice — skeleton plus render plus 4-axis score for one concept diagram wired into 60-AUDIT.md</name>
  <files>.planning/phases/60-diagram-quality-audit/60-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v7.0-phases/42-diagram-audit/42-AUDIT.md (report-format precedent: Purpose, scoring legend, per-diagram x per-axis table with evidence-bearing cell notes, ranked lists, self-check — the shape to follow, extended here with the accessibility and sync axes and the OK/Minor/Major/Critical scale)
    - docs/assets/diagrams/STYLE_SPEC.md (conformance rubric: viewBox 720x{300|480|520}, five CSS classes, system-ui, role img + aria-label, SVGO idempotence gate — score AGAINST it, do NOT edit it)
    - .planning/phases/60-diagram-quality-audit/60-CONTEXT.md (OK/Minor/Major/Critical per-axis scale plus overall per-diagram verdict; design/geometry judged by rsvg render; the four axes)
    - docs/assets/diagrams/smoothing.svg (the exemplar to score end-to-end — v7.0 found a real geometry defect here: a Panel-3 ghost polyline reusing Panel-1 coordinates, so it exercises the design/geometry axis)
  </read_first>
  <action>Prove the full audit pipeline on ONE diagram before scaling out. First, create the 60-AUDIT.md skeleton: a Purpose section; a scoring-legend section defining the OK/Minor/Major/Critical severity scale and the four axes (design/geometry quality, STYLE_SPEC conformance, accessibility, sync) plus the overall per-diagram verdict; a section-grouped concept scoring table with columns Diagram, Section, Fix bucket, Design/geometry, STYLE_SPEC, Accessibility, Sync, Overall, Notes; and placeholder headers for the section/bucket map, the ranked fix worklists for 61/62/63, the COVER coverage-gap section, and the SYNC drift section (those latter three filled by Plan 60-02). Then for smoothing.svg: render it to PNG with rsvg-convert into the session scratchpad (NOT committed) and visually inspect for the design/geometry verdict; grep the STYLE_SPEC markers for the conformance verdict; assess the accessibility axis by checking role img presence, whether the aria-label text matches the title, and whether long-form title/desc/aria-labelledby is present (record it as absent); mark the Sync cell as deferred-to-Plan-60-02 for the concept table. Fill smoothing.svg's row with all axis cells on the OK/Minor/Major/Critical scale, each non-OK cell carrying a one-line evidence note citing file:line where relevant, and assign it to the Phase 61 bucket. Do NOT modify smoothing.svg, STYLE_SPEC.md, or any docs page, and do NOT commit any PNG.</action>
  <verify>
    <automated>test -f .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -q 'smoothing.svg' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qE 'Critical|Major|Minor' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && git diff --quiet -- docs/assets/diagrams docs/assets/cards docs/assets/thumb docs/assets/diagrams/STYLE_SPEC.md</automated>
    <fails_when>60-AUDIT.md is absent, smoothing.svg has no scored row, no severity verdict appears, or any committed SVG / STYLE_SPEC.md was modified (git diff non-empty).</fails_when>
  </verify>
  <acceptance_criteria>
    - 60-AUDIT.md exists with a Purpose section, a scoring legend defining OK/Minor/Major/Critical and the four axes plus overall verdict, and the section-grouped concept scoring table header (columns: Diagram, Section, Fix bucket, Design/geometry, STYLE_SPEC, Accessibility, Sync, Overall, Notes).
    - Placeholder headers exist for the section/bucket map, the 61/62/63 ranked fix worklists, the COVER coverage-gap section, and the SYNC drift section.
    - smoothing.svg has a complete four-axis scored row on the OK/Minor/Major/Critical scale with an overall verdict, evidence notes on non-OK cells, and a Phase 61 bucket assignment; its design/geometry verdict is backed by a scratch rsvg-convert render.
    - No committed SVG, no STYLE_SPEC.md, and no docs page was modified; no PNG was committed.
  </acceptance_criteria>
  <done>60-AUDIT.md exists with the scoring legend, the four-axis concept-table structure, and all downstream-section placeholders; smoothing.svg has a complete four-axis scored row with an overall verdict, evidence notes, and a fix-bucket assignment; the render-then-read-then-grep pipeline is proven; nothing committed was changed.</done>
</task>

<task type="auto">
  <name>Task 2: Score all 90 concept diagrams on the four axes, grouped by section</name>
  <files>.planning/phases/60-diagram-quality-audit/60-AUDIT.md</files>
  <read_first>
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md (the skeleton plus the smoothing.svg exemplar row from Task 1 — the shape every other row copies)
    - docs/assets/diagrams/STYLE_SPEC.md (conformance rubric, re-read only if a marker is ambiguous)
    - the referencing docs page(s) for each diagram (docs/<section>/<page>.md) — read the page prose to sanity-check the diagram depicts what the shipped fdars method does; FLAG suspected method-accuracy mismatches in the Notes column, do not re-derive the method
  </read_first>
  <action>Scale the proven pipeline across the remaining 89 concept SVGs (glob docs/assets/diagrams/*.svg at maxdepth 1; cards/ and thumb/ are out of scope for this plan). For EACH diagram produce the four axis verdicts: (a) Design/geometry — render to PNG with rsvg-convert into the scratchpad and visually inspect for mismatched lines, misaligned endpoints, overlapping/misplaced elements, cramped spacing, text/label overlap, inconsistent panel sizing — this is the user's PRIMARY concern, so be thorough and cite the specific visual defect; (b) STYLE_SPEC conformance — grep the source for viewBox-720 width and 300/480/520 height, the five CSS classes, system-ui, role img, aria-label, recording each failing marker by name (the four known non-720 diagrams — elastic-clustering.svg, outlier-detection.svg, covariance-functions.svg, ex-sonar-tsrvf.svg — must be flagged); (c) Accessibility — record role img presence, whether the aria-label matches the title text, and whether long-form title/desc/aria-labelledby exists and is adequate for complex/multi-panel diagrams (flag the absent long-form as a Minor-or-worse A11Y-02 gap on every complex diagram); (d) Sync — leave as deferred-to-Plan-60-02 in the concept table (drift is computed there against thumbs/cards). Score each cell OK/Minor/Major/Critical with a one-line evidence note on every non-OK cell (cite file:line where the evidence lives), and give each diagram an overall verdict = its worst axis. Group rows by docs section in this order: learn, represent, align, analyze, monitoring, advisor, sklearn, regression, inference, examples. Every one of the 90 files must have exactly one fully-scored row. Note that method-accuracy suspicions belong in the Notes column as FLAGs (they feed the correction phases' method-accuracy checks), not as a separate axis.</action>
  <verify>
    <automated>missing=0; for f in $(find docs/assets/diagrams -maxdepth 1 -name '*.svg' -exec basename {} \;); do grep -q "$f" .planning/phases/60-diagram-quality-audit/60-AUDIT.md || { echo "MISSING $f"; missing=1; }; done; rows=$(grep -cE '^\| [a-z0-9].*\.svg ' .planning/phases/60-diagram-quality-audit/60-AUDIT.md); echo "scored-rows=$rows"; test "$missing" -eq 0 && test "$rows" -ge 90</automated>
    <fails_when>Any of the 90 concept SVG filenames is absent from 60-AUDIT.md, or fewer than 90 scored table rows are present.</fails_when>
  </verify>
  <acceptance_criteria>
    - All 90 concept SVGs (find docs/assets/diagrams -maxdepth 1 -name '*.svg') have exactly one fully-scored row in 60-AUDIT.md.
    - Every row carries a verdict on all four axes (design/geometry, STYLE_SPEC, accessibility, sync) on the OK/Minor/Major/Critical scale plus an overall verdict; every non-OK cell has a one-line evidence note.
    - The design/geometry verdict for each diagram is backed by a scratch rsvg-convert render (not judged from source alone); the four known non-720 diagrams are flagged on the STYLE_SPEC axis; the universal long-form title/desc gap is flagged on the accessibility axis for complex/multi-panel diagrams.
    - Rows are grouped by docs section in order learn, represent, align, analyze, monitoring, advisor, sklearn, regression, inference, examples.
    - No committed SVG, STYLE_SPEC.md, or docs page was modified; no PNG committed.
  </acceptance_criteria>
  <done>All 90 concept diagrams have a fully-scored four-axis row with an overall verdict, grouped by section; every non-OK cell has an evidence note; design/geometry verdicts are render-backed; nothing committed was changed.</done>
</task>

<task type="auto">
  <name>Task 3: Record the authoritative section-to-concept map and 61/62/63 bucket assignment</name>
  <files>.planning/phases/60-diagram-quality-audit/60-AUDIT.md</files>
  <read_first>
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md (the completed 90-row concept scoring table from Task 2 — the source the bucket map partitions)
    - .planning/phases/60-diagram-quality-audit/60-CONTEXT.md (correction-phase buckets: 61 learn/represent/align, 62 analyze/monitoring/advisor, 63 regression/inference/examples)
  </read_first>
  <action>Fill the section/bucket-map section of 60-AUDIT.md (the ranked Major-first worklists themselves are assembled in Plan 60-02; this task records the partition and the raw per-section grouping they will rank). Produce: (1) the authoritative section-to-concept-diagram map — each of the 90 diagrams with its owning docs section (learn 6, represent 10, align 8, analyze 12, monitoring 3, advisor 10, sklearn 1, regression 15, inference 4, examples 21 = 90). (2) The correction-phase bucket assignment partitioning all 90 into exactly one bucket each: Phase 61 = learn+represent+align (24); Phase 62 = analyze+monitoring+advisor (25) PLUS the sklearn edge case sklearn-pipeline-dataflow.svg — state explicitly that the CONTEXT bucket list predates the v9.0 sklearn section and this one diagram is assigned to Phase 62 as the closest surface-family fit (26 total in bucket 62); Phase 63 = regression+inference+examples (40). Assert the arithmetic 24 + 26 + 40 = 90 in the document. (3) A per-diagram bucket column already present in the Task-2 table must agree with this map — reconcile any mismatch. State that no diagram is dropped or duplicated across buckets.</action>
  <verify>
    <automated>grep -q 'sklearn-pipeline-dataflow.svg' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qE '24 \+ 26 \+ 40 = 90|24\+26\+40 = 90|24\+26\+40=90' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qiE 'phase 61|phase 62|phase 63' .planning/phases/60-diagram-quality-audit/60-AUDIT.md</automated>
    <fails_when>The sklearn edge-case diagram is unassigned, the 24+26+40=90 partition arithmetic is absent, or the three correction-phase bucket labels are missing.</fails_when>
  </verify>
  <acceptance_criteria>
    - 60-AUDIT.md contains the section-to-concept-diagram map covering all 90 diagrams with correct per-section tallies (learn 6, represent 10, align 8, analyze 12, monitoring 3, advisor 10, sklearn 1, regression 15, inference 4, examples 21).
    - Every concept diagram is assigned to exactly one correction-phase bucket (61/62/63), the sklearn edge case is explicitly assigned to Phase 62 with the reason stated, and the partition arithmetic 24 + 26 + 40 = 90 is asserted.
    - The per-diagram bucket column in the scoring table agrees with the map; none dropped or duplicated.
  </acceptance_criteria>
  <done>60-AUDIT.md records the authoritative section-to-concept map and the 61/62/63 bucket partition with all 90 diagrams placed exactly once, the sklearn edge case resolved explicitly, and the 24+26+40=90 arithmetic stated.</done>
</task>

</tasks>

<threat_model>
N/A — docs-only audit, no threat surface. This plan renders SVGs to scratch PNGs and writes a Markdown report; it modifies no code, no runtime, no build, and no committed asset. The only external tool invocations (rsvg-convert already installed; optional check-only svgo idempotence) touch nothing writable. No trust boundary is crossed.
</threat_model>

<verification>
- 60-AUDIT.md exists and every one of the 90 concept SVGs in docs/assets/diagrams/ (maxdepth 1) has exactly one four-axis-scored row with an overall verdict.
- Every non-OK cell has a one-line evidence note; design/geometry verdicts are backed by rsvg-convert renders.
- The section-to-concept map and the 61/62/63 bucket partition place all 90 diagrams exactly once (24 + 26 + 40 = 90); the sklearn edge case is explicitly resolved.
- The accessibility axis flags the universal long-form title/desc/aria-labelledby gap on complex diagrams; the four known non-720 diagrams are flagged on STYLE_SPEC.
- No committed docs/assets/**/*.svg, docs/assets/diagrams/STYLE_SPEC.md, or docs/**/*.md was modified; no scratch PNG was committed.
</verification>

<success_criteria>
AUDIT-01 (concept portion) and AUDIT-02 (defect-severity portion) satisfied for the 90 concept diagrams: a per-diagram x four-axis scored inventory on the OK/Minor/Major/Critical scale with an overall verdict, grouped by section, plus the authoritative section-to-concept map and the 61/62/63 correction-phase bucket partition — the evidence base Plan 60-02 turns into ranked worklists and the SYNC/COVER lists.
</success_criteria>

<output>
Create `.planning/phases/60-diagram-quality-audit/60-01-SUMMARY.md` when done.
</output>
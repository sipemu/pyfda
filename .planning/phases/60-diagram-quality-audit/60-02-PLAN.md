---
phase: 60-diagram-quality-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 60-01
files_modified:
  - .planning/phases/60-diagram-quality-audit/60-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
user_setup: []
estimate:
  tokens: 120000
  raw_tokens: 60000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "60-AUDIT.md inventories all 8 section cards (docs/assets/cards/) and all 58 gallery thumbnails (docs/assets/thumb/), each scored on design/geometry, STYLE_SPEC, accessibility, and sync on the OK/Minor/Major/Critical scale, so all 156 SVGs (90 concept + 8 cards + 58 thumbs) now carry a scored row (AUDIT-01)."
    - "Each of the 58 thumbnails is mapped to its concept diagram via the index-page gallery href (not by filename alone — e.g. thumb process-monitoring.svg maps to the spm/ page = spm.svg concept), each of the 8 cards to its section, and the rendered thumb/card PNG is compared to the rendered concept PNG to flag content/visual drift as the SYNC axis verdict (SYNC-01/SYNC-02 base for Phase 64)."
    - "60-AUDIT.md contains a COVER coverage-gap section listing the docs pages/methods that lack a concept diagram — the COVER-01 scope for Phase 64 — derived by cross-referencing docs method pages against the concept-diagram set."
    - "60-AUDIT.md contains a SYNC drift section naming every thumbnail and card flagged as drifted from its current concept diagram, split into the SYNC-01 (thumbs) and SYNC-02 (cards) worklists for Phase 64, plus an A11Y-03 note on thumbnail decorative-semantics consistency."
    - "60-AUDIT.md contains ranked, per-section concept fix worklists for Phases 61, 62, and 63 — each diagram carried with its worst-axis severity and non-OK notes, ordered Critical-then-Major-first then by section — derived from the Plan 60-01 concept scoring table (AUDIT-02)."
    - "60-AUDIT.md ends with a self-check confirming all 156 SVGs are scored (90 + 8 + 58), the three fix worklists cover all flagged concept diagrams, and the COVER and SYNC sections are present (AUDIT-01/AUDIT-02)."
  artifacts:
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md
  key_links:
    - "60-AUDIT.md ranked 61/62/63 fix worklists -> consumed by Phases 61/62/63 as their evidence-backed correction worklists."
    - "60-AUDIT.md COVER-01 gap list -> consumed by Phase 64 new-coverage plans."
    - "60-AUDIT.md SYNC-01/SYNC-02 drift list + A11Y-03 note -> consumed by Phase 64 cards/thumbs sync plans."
---

<objective>
Complete `.planning/phases/60-diagram-quality-audit/60-AUDIT.md` — the milestone-gating artifact begun in Plan 60-01 — by scoring the remaining 66 SVGs (8 section cards + 58 gallery thumbnails) with thumb/card-to-concept drift detection, deriving the COVER-01 coverage-gap list and the SYNC-01/SYNC-02 drift list for Phase 64, and assembling the ranked per-section concept fix worklists for Phases 61/62/63 from the Plan 60-01 concept scoring table.

Purpose: finishes the four-axis inventory across all 156 SVGs and produces the three downstream consumables — the 61/62/63 ranked correction worklists, the Phase-64 COVER-01 coverage-gap list, and the Phase-64 SYNC-01/SYNC-02 drift list — so every downstream v10.0 phase executes against evidence. Depends on Plan 60-01 (the concept scoring table and section/bucket map).
Output: the completed `.planning/phases/60-diagram-quality-audit/60-AUDIT.md`.

READ-ONLY ANALYSIS PHASE: no SVG is edited, no docs page is rewritten, STYLE_SPEC.md is NOT edited (refreshed later in Phase 65 / SPEC-02). PNGs rendered for visual comparison are scratch artifacts — write them under the scratchpad or a gitignored temp dir; do NOT commit any PNG and do NOT modify any committed SVG. SVGO, if invoked, is check-only (idempotence), never rewriting a source file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-diagram-quality-audit/60-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@.planning/phases/60-diagram-quality-audit/60-AUDIT.md

This plan continues the SAME 60-AUDIT.md file produced by Plan 60-01 (which is why this plan is Wave 2, depends_on 60-01 — same file, no parallel edit). Plan 60-01 left: the skeleton, the 90-diagram concept scoring table (Sync cells deferred to this plan), the section-to-concept map, and the 61/62/63 bucket partition. This plan fills the Sync cells for concept diagrams, adds the 8 card + 58 thumb rows, and writes the ranked worklists + COVER + SYNC sections + self-check.

Thumb-to-concept mapping (established during planning — MUST go through the index-page gallery href, not filename alone):
- 57 of 58 thumbs share their concept diagram's filename (thumb/<name>.svg -> diagrams/<name>.svg).
- Edge case: thumb `process-monitoring.svg` has NO same-named concept; the monitoring index gallery links it to the `spm/` page (docs/monitoring/index.md), so it maps to concept `spm.svg`. Record this explicitly.
- Each gallery item in docs/<section>/index.md is `<a ... href="<page>/"><img src="../assets/thumb/<name>.svg" ...></a>` — the href resolves the owning page, whose concept diagram is the drift comparison target.

Card-to-section mapping: docs/assets/cards/{learn,represent,align,analyze,monitoring,regression,examples,hero}.svg. Seven cards map to their same-named docs section; `hero.svg` is the site landing card (docs/index.md) — a section-agnostic card, score it on design/STYLE_SPEC/accessibility and mark Sync N/A (no single concept counterpart).

Accessibility calibration confirmed during planning (grep): all 58 thumbs carry `role="img"` in the SVG file, but the referencing gallery `<img>` tags use `alt=""` (decorative intent). This mismatch — decorative-in-markup but role="img" in the asset — is the A11Y-03 finding for Phase 64: note it. All 8 cards carry `role="img"`.

Coverage-gap source: all 21 docs/examples/*.md pages already reference a concept diagram (confirmed during planning). The COVER-01 gap is therefore in the non-example method pages — cross-reference each docs/<section>/*.md method page against the concept-diagram set to find pages/methods lacking a diagram. Report the concrete list (state "none found" only after checking every section's method pages).

Tooling: rsvg-convert at /usr/bin/rsvg-convert; scratch dir at the session scratchpad. Render thumb, card, and concept to PNG at a comparable size for the visual drift comparison.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score 8 cards + 58 thumbs with thumb/card-to-concept drift (SYNC axis)</name>
  <files>.planning/phases/60-diagram-quality-audit/60-AUDIT.md</files>
  <read_first>
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md (the concept scoring table + section map from Plan 60-01 — the drift comparison base and the row-format to copy)
    - docs/assets/diagrams/STYLE_SPEC.md (conformance rubric for the STYLE_SPEC axis on cards/thumbs)
    - docs/monitoring/index.md (the gallery-href mapping exemplar — shows thumb process-monitoring.svg linking to the spm/ page; the same <a href><img> pattern resolves every thumb's owning page across docs/<section>/index.md)
    - docs/index.md (the landing gallery + where hero.svg is used)
  </read_first>
  <action>Add scored rows for all 8 cards (docs/assets/cards/*.svg) and all 58 thumbs (docs/assets/thumb/*.svg) to 60-AUDIT.md, in two new section-grouped tables (Cards, Thumbnails) using the same columns as the concept table. For EACH card/thumb produce the four axis verdicts: (a) Design/geometry — render to PNG with rsvg-convert into the scratchpad and visually inspect for the same defect classes as the concept pass; (b) STYLE_SPEC conformance — grep the STYLE_SPEC markers (thumbs/cards have their own smaller-canvas conventions, so note where they legitimately differ vs. where they violate palette/stroke/class rules); (c) Accessibility — for thumbs record the A11Y-03 finding (SVG carries role="img" but the gallery <img> uses alt="" — decorative-semantics inconsistency); for cards record role/aria-label presence; (d) Sync — resolve each thumb to its concept diagram via the index-page gallery href (NOT filename alone), render both the thumb PNG and the concept PNG, compare them, and flag content/visual drift (Major/Critical if the thumb depicts a materially different or outdated figure than its concept, Minor for cosmetic-only divergence, OK if faithful); resolve process-monitoring.svg -> spm.svg explicitly; for cards flag drift from the section's family of concept diagrams; mark hero.svg Sync = N/A. Score each cell OK/Minor/Major/Critical with a one-line evidence note on non-OK cells, plus an overall verdict per row. ALSO backfill the Sync cell for each of the 90 concept rows in the Plan-60-01 table: OK if a faithful thumb/card exists or none is expected, else note the drift direction. Every one of the 8 cards and 58 thumbs must have exactly one fully-scored row.</action>
  <verify>
    <automated>cm=0; for f in $(find docs/assets/cards docs/assets/thumb -name '*.svg' -exec basename {} \;); do grep -q "$f" .planning/phases/60-diagram-quality-audit/60-AUDIT.md || { echo "MISSING $f"; cm=1; }; done; total=$(for d in docs/assets/diagrams docs/assets/cards docs/assets/thumb; do find $d -maxdepth 1 -name '*.svg'; done | wc -l); echo "total-svgs=$total"; test "$cm" -eq 0 && test "$total" -eq 156</automated>
    <fails_when>Any card or thumb filename is absent from 60-AUDIT.md, or the total SVG count is not 156 (mapping the audit scope drifted).</fails_when>
  </verify>
  <acceptance_criteria>
    - All 8 cards and all 58 thumbs have exactly one fully-scored four-axis row with an overall verdict; every non-OK cell has an evidence note; design/geometry verdicts are render-backed.
    - Each thumb's Sync verdict is computed by comparing its rendered PNG to its concept diagram's rendered PNG, with the concept resolved via the index-page gallery href; process-monitoring.svg is explicitly mapped to spm.svg; hero.svg Sync is N/A.
    - The A11Y-03 thumbnail decorative-semantics inconsistency (asset role="img" vs gallery alt="") is recorded.
    - The Sync cells for the 90 concept rows are backfilled.
    - No committed SVG, STYLE_SPEC.md, or docs page modified; no PNG committed.
  </acceptance_criteria>
  <done>All 8 cards + 58 thumbs are scored on four axes with drift-based Sync verdicts, the concept-row Sync cells are backfilled, and all 156 SVGs now carry a scored row; nothing committed was changed.</done>
</task>

<task type="auto">
  <name>Task 2: COVER-01 coverage-gap list + SYNC-01/SYNC-02 drift list</name>
  <files>.planning/phases/60-diagram-quality-audit/60-AUDIT.md</files>
  <read_first>
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md (the completed 156-row scoring table incl. drift verdicts from Task 1 — the source the COVER and SYNC lists derive from)
    - .planning/phases/60-diagram-quality-audit/60-CONTEXT.md (COVER-01 = pages/methods lacking a concept diagram, for Phase 64; SYNC-01 = drifted thumbs, SYNC-02 = drifted cards, for Phase 64)
    - the docs method pages under docs/{learn,represent,align,analyze,monitoring,advisor,regression,inference,sklearn}/*.md (to find which method pages lack a concept diagram reference — grep each for assets/diagrams/)
  </read_first>
  <action>Write two derived sections into 60-AUDIT.md. (1) COVER coverage-gap section (COVER-01, Phase 64): cross-reference every non-index method page under docs/<section>/ against the concept-diagram set — for each page grep for an assets/diagrams/ reference; enumerate the pages/methods that lack a concept diagram and for each state whether a diagram is warranted (a method page with no illustrative concept SVG warrants one; pure API-reference or nav pages may not). Note that all 21 examples pages already have diagrams (no examples-coverage gap). If a section has zero gaps, state so explicitly after checking. (2) SYNC drift section (SYNC-01/SYNC-02, Phase 64): from the Task-1 drift verdicts, list every thumbnail flagged Minor/Major/Critical on the Sync axis as the SYNC-01 worklist (name, mapped concept, drift description, severity), and every card flagged on the Sync axis as the SYNC-02 worklist; add the A11Y-03 thumbnail decorative-semantics note as a sub-item for Phase 64. Both lists must be directly consumable by the Phase 64 plans.</action>
  <verify>
    <automated>grep -qiE '## .*coverage|COVER-01' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qiE '## .*sync|SYNC-01' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qE 'SYNC-02' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qE 'A11Y-03' .planning/phases/60-diagram-quality-audit/60-AUDIT.md</automated>
    <fails_when>The COVER coverage-gap section, the SYNC-01/SYNC-02 drift lists, or the A11Y-03 decorative-semantics note is absent from 60-AUDIT.md.</fails_when>
  </verify>
  <acceptance_criteria>
    - 60-AUDIT.md has a COVER section enumerating the docs pages/methods lacking a concept diagram (COVER-01, Phase 64), each with a warranted-yes/no judgement; sections with zero gaps are stated explicitly; the no-examples-gap fact is recorded.
    - 60-AUDIT.md has a SYNC section with a SYNC-01 thumbs worklist and a SYNC-02 cards worklist, each drifted item naming its mapped concept, drift description, and severity, plus the A11Y-03 decorative-semantics note for Phase 64.
    - Both lists are directly consumable by the Phase 64 plans.
  </acceptance_criteria>
  <done>60-AUDIT.md contains the COVER-01 coverage-gap list and the SYNC-01/SYNC-02 drift lists (with the A11Y-03 note), each derived from the scoring table and ready for Phase 64.</done>
</task>

<task type="auto">
  <name>Task 3: Ranked 61/62/63 concept fix worklists + full-inventory self-check</name>
  <files>.planning/phases/60-diagram-quality-audit/60-AUDIT.md</files>
  <read_first>
    - .planning/phases/60-diagram-quality-audit/60-AUDIT.md (the completed 156-row scoring table + section/bucket map — the source the ranked worklists are derived from)
    - .planning/phases/60-diagram-quality-audit/60-CONTEXT.md (ranked per-section fix worklists mapped to phases 61/62/63; ordering Critical/Major-first then by section)
    - .planning/milestones/v7.0-phases/42-diagram-audit/42-AUDIT.md (the ranked-fix-list + self-check format precedent)
  </read_first>
  <action>Write the ranked fix worklists and the closing self-check into 60-AUDIT.md. (1) Ranked per-section concept fix worklists for Phase 61 (learn/represent/align, 24), Phase 62 (analyze/monitoring/advisor + sklearn-pipeline-dataflow.svg, 26), and Phase 63 (regression/inference/examples, 40): within each phase bucket, order diagrams Critical-first, then Major, then Minor, then OK-no-action, and within a severity group by section; carry each flagged diagram's worst-axis severity and its non-OK notes (design/geometry, STYLE_SPEC, accessibility) into the worklist so each downstream phase reads a prioritized, evidence-backed list. Every concept diagram on the Phase-60 fix list must appear in exactly one phase worklist (the union of the three worklists = all 90 concept diagrams). (2) Closing self-check section: assert and show (via inline counts) that all 156 SVGs have a scored row (90 concept + 8 cards + 58 thumbs), that the three worklists together cover all 90 concept diagrams with none dropped or duplicated, and that the COVER and SYNC sections are present. State the phase deliverable AUDIT-01/AUDIT-02 as satisfied.</action>
  <verify>
    <automated>grep -qiE 'phase 61.*(worklist|fix)|## .*61' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qiE 'phase 62' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qiE 'phase 63' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && grep -qiE 'self-check|self check' .planning/phases/60-diagram-quality-audit/60-AUDIT.md && all=$(for d in docs/assets/diagrams docs/assets/cards docs/assets/thumb; do find $d -maxdepth 1 -name '*.svg' -exec basename {} \;; done); miss=0; for f in $all; do grep -q "$f" .planning/phases/60-diagram-quality-audit/60-AUDIT.md || miss=1; done; test "$miss" -eq 0</automated>
    <fails_when>Any of the three ranked phase worklists or the self-check section is absent, or any of the 156 SVG filenames is missing from 60-AUDIT.md.</fails_when>
  </verify>
  <acceptance_criteria>
    - 60-AUDIT.md has three ranked concept fix worklists (Phase 61 = 24, Phase 62 = 26 incl. sklearn edge case, Phase 63 = 40), each ordered Critical/Major-first then by section, carrying worst-axis severity + non-OK notes per diagram.
    - The union of the three worklists is exactly the 90 concept diagrams, none dropped or duplicated.
    - A closing self-check shows all 156 SVGs scored (90 + 8 + 58), the worklists cover all 90 concept diagrams, and COVER + SYNC sections are present; AUDIT-01/AUDIT-02 stated satisfied.
  </acceptance_criteria>
  <done>60-AUDIT.md ends with the three ranked 61/62/63 fix worklists partitioning all 90 concept diagrams and a self-check confirming all 156 SVGs are scored and the COVER/SYNC sections present — the milestone-gating artifact is complete.</done>
</task>

</tasks>

<threat_model>
N/A — docs-only audit, no threat surface. This plan renders SVGs to scratch PNGs and writes a Markdown report; it modifies no code, no runtime, no build, and no committed asset. External tool invocations (rsvg-convert; optional check-only svgo idempotence) touch nothing writable. No trust boundary is crossed.
</threat_model>

<verification>
- All 156 SVGs (90 concept + 8 cards + 58 thumbs) have exactly one four-axis-scored row with an overall verdict in 60-AUDIT.md.
- Every thumb/card Sync verdict is drift-based (rendered-PNG comparison to the concept resolved via the gallery href); process-monitoring.svg -> spm.svg is explicit; hero.svg Sync is N/A.
- The COVER section lists the pages/methods lacking a concept diagram (COVER-01); the SYNC section has the SYNC-01 (thumbs) + SYNC-02 (cards) drift worklists + the A11Y-03 note.
- The three ranked 61/62/63 fix worklists partition all 90 concept diagrams, none dropped/duplicated, Critical/Major-first.
- The closing self-check confirms 90 + 8 + 58 = 156 scored and the worklist/COVER/SYNC completeness.
- No committed docs/assets/**/*.svg, STYLE_SPEC.md, or docs/**/*.md was modified; no scratch PNG was committed.
</verification>

<success_criteria>
AUDIT-01 and AUDIT-02 fully satisfied: the completed 60-AUDIT.md is a per-diagram x four-axis scored inventory of all 156 SVGs on the OK/Minor/Major/Critical scale, with ranked per-section concept fix worklists for Phases 61/62/63, the COVER-01 coverage-gap list for Phase 64, and the SYNC-01/SYNC-02 drift list (plus A11Y-03 note) for Phase 64 — the evidence base that gates the rest of the v10.0 milestone.
</success_criteria>

<output>
Create `.planning/phases/60-diagram-quality-audit/60-02-SUMMARY.md` when done.
</output>
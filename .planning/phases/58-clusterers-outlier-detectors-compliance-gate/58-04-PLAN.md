---
phase: 58-clusterers-outlier-detectors-compliance-gate
plan: 04
type: execute
wave: 4
depends_on: ["58-03"]
files_modified:
  - tests/sklearn/test_triage.py
  - tests/sklearn/test_compliance_gate.py
  - tests/sklearn/test_interop.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [COMPLY-01, COMPLY-02]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The whole tests/sklearn/ suite is green under .venv/bin/pytest (no red test_triage.py, no PASS-WITH-FIXES estimators)"
    - "A single aggregate compliance gate runs parametrize_with_checks over all 28 wrapped estimators across all five families with zero exemptions"
    - "_coverage.TRIAGE_VERDICTS contains zero PASS-WITH-FIXES values among wrapped estimators (all 28 PASS); EXCLUDED_METHODS holds only structural design exclusions"
    - "A Pipeline([FPCATransformer(), RandomForestClassifier()]) fits and predicts end-to-end (fdars transformer → native sklearn estimator)"
    - "The [sklearn] compliance job is wired into .github/workflows/ci.yml across the Python 3.9–3.14 matrix"
  artifacts:
    - "tests/sklearn/test_compliance_gate.py — aggregate all-family parametrize_with_checks gate + zero-PASS-WITH-FIXES assertion"
    - "tests/sklearn/test_interop.py — FPCATransformer → RandomForestClassifier Pipeline interop test (COMPLY-02)"
    - ".github/workflows/ci.yml — sklearn compliance CI job"
  key_links:
    - "test_triage.py is reconciled (now that all 28 candidates PASS) so it goes green rather than staying red for unpromoted skeletons"
    - "CI job installs the [sklearn] extra and runs tests/sklearn/ so the gate executes on the matrix (shim spans sklearn 1.3→1.8)"
---

<objective>
Lock the full-matrix compliance gate now that all five families are present (COMPLY-01) and prove native-sklearn interop (COMPLY-02). Reconcile the milestone-long-red `test_triage.py`, add an authoritative aggregate compliance suite, assert `_coverage.py` is clean (zero PASS-WITH-FIXES among wrapped estimators), add the interop test, and wire the `[sklearn]` compliance job into CI across the Python 3.9–3.14 matrix.

Purpose: This is the capstone that makes the whole `tests/sklearn/` tree green and CI-clean. All 28 candidates are now PASS (Plans 01-03 flipped the last 8), so `test_triage.py` — which was intentionally non-asserting/red for unpromoted skeletons — can be repurposed into (or superseded by) a green authoritative gate. Interop (FPCATransformer scores → RandomForestClassifier) proves the whole point of the milestone.

Output: A green aggregate compliance gate, a reconciled test_triage.py, an interop test, and a CI job — the whole sklearn suite green offline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-clusterers-outlier-detectors-compliance-gate/58-CONTEXT.md
@python/fdars/sklearn/_coverage.py
@tests/sklearn/test_triage.py
@tests/sklearn/test_classifiers_compliance.py
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate compliance gate + zero-PASS-WITH-FIXES assertion + reconcile test_triage.py</name>
  <files>tests/sklearn/test_compliance_gate.py, tests/sklearn/test_triage.py, python/fdars/sklearn/_coverage.py</files>
  <read_first>
    - tests/sklearn/test_triage.py lines 84-137 (the `_ALL_SKELETONS` list of all 28 constructed estimators + the non-asserting `test_sklearn_triage` harness — this is the reconcile target).
    - python/fdars/sklearn/_coverage.py lines 163-310 (TRIAGE_VERDICTS — after Plans 01-03 every value should be "PASS"; this task ADDS the assertion that enforces it).
    - tests/sklearn/test_classifiers_compliance.py (per-estimator pattern) — the aggregate gate constructs all 28 with battery-valid hyperparameters (e.g. ncomp=10 for classifiers, n_components=10 for FPC regressors, contamination=0.1 for detectors, n_bootstrap=50 for LRT) mirroring the per-family compliance files.
  </read_first>
  <action>
    Create tests/sklearn/test_compliance_gate.py as the authoritative COMPLY-01 gate:
    - A `_ALL_WRAPPED` list constructing all 28 wrapped estimators with the SAME battery-valid hyperparameters used in the per-family compliance tests (import them from _skeletons; use ncomp=10 / n_components=10 where the per-family tests do, contamination=0.1 for the six detectors, n_bootstrap=50 for LRTOutlierDetector).
    - `@parametrize_with_checks(_ALL_WRAPPED)` → `test_full_matrix_compliance(estimator, check)` with body `check(estimator)` — the zero-exemption all-family gate.
    - A separate `def test_no_pass_with_fixes_remaining()`: import TRIAGE_VERDICTS; assert every value for a wrapped estimator starts with "PASS" and NONE contains "PASS-WITH-FIXES"; assert the count of "PASS" verdicts == 28. Document that EXCLUDED_METHODS is asserted structural-only by the existing test_coverage.py.
    Reconcile test_triage.py: it stayed red/non-asserting for unpromoted skeletons all milestone. Now that all 28 PASS, repurpose it to defer to the authoritative gate — replace its module docstring's "intentionally does NOT assert all-green" note with a note that Phase 58 promoted all 28 to PASS and the authoritative gate now lives in test_compliance_gate.py; keep the `test_sklearn_triage` parametrized harness but let it assert green (it already runs `check(estimator)`, which now passes for all 28). Do NOT delete test_triage.py (other docs/CI reference it); make it green.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_compliance_gate.py tests/sklearn/test_triage.py -q</automated>
  </verify>
  <done>test_compliance_gate.py runs the 28-estimator all-family battery green, asserts zero PASS-WITH-FIXES and exactly 28 PASS verdicts, and test_triage.py is green (no longer red for unpromoted skeletons).</done>
  <reversibility rating="reversible">New test file + docstring/assertion edits; git-checkout revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Interop test — FPCATransformer → RandomForestClassifier Pipeline (COMPLY-02)</name>
  <files>tests/sklearn/test_interop.py</files>
  <read_first>
    - python/fdars/sklearn/_skeletons.py lines 236-399 (FPCATransformer — `fit`/`transform` mapping (n_obs,n_points)→(n_obs,n_components) scores; the transformer that feeds the native sklearn estimator).
    - tests/sklearn/test_predictive_pipeline.py (Phase 57's Pipeline/GridSearchCV end-to-end test) for the established Pipeline construction + assertion idioms and the sklearn-gated import structure.
  </read_first>
  <behavior>
    - `Pipeline([("fpca", FPCATransformer(n_components=5)), ("rf", RandomForestClassifier(random_state=0))])` fits on (n_obs>=20, n_points>=15) X with a 2-class y and then `.predict(X)` returns an array of length n_obs with values drawn from the training classes.
    - `pipe.score(X, y)` returns a float in [0, 1].
  </behavior>
  <action>
    Create tests/sklearn/test_interop.py (inherits the conftest sklearn gate). Import Pipeline, RandomForestClassifier, FPCATransformer. Write `def test_fpca_to_random_forest_pipeline()`: build a small deterministic dataset (e.g. RandomState(0), two Gaussian-shifted curve groups of ~15 obs each on a 20-point grid, y = group labels); fit `Pipeline([("fpca", FPCATransformer(n_components=5)), ("rf", RandomForestClassifier(n_estimators=20, random_state=0))])`; assert `pipe.predict(X)` has shape (n_obs,) and every predicted label is in `set(y)`; assert `0.0 <= pipe.score(X, y) <= 1.0`. Add a short docstring naming COMPLY-02 (fdars transformer scores → native sklearn estimator inside one Pipeline). Keep the dataset tiny so the test is fast.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_interop.py -q</automated>
  </verify>
  <done>The FPCATransformer → RandomForestClassifier Pipeline fits, predicts labels from the training classes, and scores in [0,1] (COMPLY-02 proven).</done>
</task>

<task type="auto">
  <name>Task 3: Wire the [sklearn] compliance job into CI across the 3.9–3.14 matrix</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 50-89 (the existing `test-python` job: venv creation, `maturin develop --release`, extras install, `pytest tests/ -v`) — mirror its structure for the new job.
    - pyproject.toml lines 53-56 (the `[sklearn]` extra with its python_version markers — the job installs `.[sklearn]`).
    - tests/sklearn/conftest.py (the `pytest.importorskip("sklearn")` gate — the new job installs the extra so the tree actually runs).
  </read_first>
  <action>
    Add a new `sklearn-compliance` job to .github/workflows/ci.yml mirroring `test-python`: same `strategy.matrix.python-version: ["3.9","3.10","3.11","3.12","3.13","3.14"]` with `fail-fast: false`; checkout + rust-toolchain@stable + rust-cache (key `sklearn-py${{ matrix.python-version }}`) + setup-python; create venv; `pip install maturin numpy pandas pytest`; `maturin develop --release`; `pip install -e ".[sklearn]"`; then `pytest tests/sklearn/ -v`. Name it "sklearn compliance (${{ matrix.python-version }})". Place it after the existing `test-python` job. Do NOT modify the existing `test-python` job (it keeps running `pytest tests/ -v`, which importorskips the sklearn tree when the extra is absent — leave that behavior intact). The new job is the one that exercises the compliance gate on the matrix with the shim spanning sklearn 1.3→1.8.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['sklearn-compliance']; steps=[s.get('run','') for s in j['steps']]; ok=('3.14' in [str(v) for v in j['strategy']['matrix']['python-version']] and any('.[sklearn]' in r for r in steps) and any('tests/sklearn/' in r for r in steps)); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>ci.yml defines a `sklearn-compliance` job over the 3.9–3.14 matrix that installs `.[sklearn]` and runs `pytest tests/sklearn/`; the existing test-python job is unchanged and the YAML parses.</done>
  <reversibility rating="reversible">Additive CI job; git-checkout revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → PyPI (sklearn extra) | The compliance job installs scikit-learn from PyPI; pinned via the [sklearn] extra markers in pyproject.toml |
| test data → FPCATransformer → RandomForestClassifier | interop passes fdars-computed FPC scores (plain ndarray) into a native sklearn estimator; no Fdata, no dtype side-effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-04 | Tampering | sklearn install in CI | low | accept | scikit-learn is a well-known PyPI package already pinned in pyproject.toml's [sklearn]/[dev] extras (installed and audited since Phase 55); the CI job reuses that pin |
| T-58-04-I | Information Disclosure | interop score leakage | low | accept | test uses synthetic RandomState data only; no real datasets |

No NEW package added to the dependency set (scikit-learn already pinned since Phase 55); no supply-chain legitimacy checkpoint required.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/ -q` — WHOLE sklearn tree green (compliance gate + triage + interop + all per-family + coverage + foundation + pipelines).
- Task 3 YAML assertion passes (job wired correctly).
- `_coverage.py`: zero PASS-WITH-FIXES among wrapped estimators, 28 PASS (Task 1 assertion).
</verification>

<success_criteria>
Every wrapped estimator passes the full check_estimator battery with zero exemptions in an aggregate gate; the whole tests/sklearn/ suite is green (test_triage.py reconciled); interop is proven (FPCATransformer → RandomForestClassifier); the [sklearn] compliance job runs on the 3.9–3.14 CI matrix (COMPLY-01 + COMPLY-02 complete).
</success_criteria>

<output>
Create `.planning/phases/58-clusterers-outlier-detectors-compliance-gate/58-04-SUMMARY.md` when done.
</output>

---
phase: 11-python-api-surface
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tests/test_advisor.py
autonomous: true
requirements: [PYAPI-02]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "build_diagnostics runs offline against a real docs/data/ dataset (canadian weather → kmeans_fd → clustering diagnostics) and passes with no network (PYAPI-02)"
    - "build_diagnostics is deterministic — two runs on identical input return identical output (PYAPI-02)"
    - "advise() raises a clear ImportError naming `pip install fdars[advisor]` when anthropic is absent (PYAPI-02)"
    - "The advise LLM integration test SKIPS (not fails) when ANTHROPIC_API_KEY is absent (PYAPI-02)"
  artifacts:
    - tests/test_advisor.py  # expanded: full TestBuildDiagnosticsOffline + TestAdvisorIntegration
  key_links:
    - "ANTHROPIC_API_KEY env presence ↔ pytest.mark.skipif on TestAdvisorIntegration"
    - "monkeypatched sys.modules['anthropic']=None ↔ advise() ImportError with install hint"
  prohibitions:
    - "The advise LLM integration test MUST skip (not fail) when ANTHROPIC_API_KEY is absent"
    - "Offline tests MUST NOT import anthropic or call advise() unguarded — no network in CI"
    - "Do NOT modify python/fdars/advisor.py logic; tests only observe existing behavior"
---

<objective>
Expand `tests/test_advisor.py` (created by 11-01) into the full offline unit-test surface for `build_diagnostics` against real `docs/data/` datasets, plus an env-gated `advise` integration test that skips cleanly without `ANTHROPIC_API_KEY`. This builds out horizontally from the 11-01 tracer's single offline test.

Purpose: Satisfy PYAPI-02 — offline unit tests for `build_diagnostics` and a stubbed/env-gated LLM integration test that keeps CI network-free.
Output: Expanded `tests/test_advisor.py` with a complete `TestBuildDiagnosticsOffline` class and an env-gated `TestAdvisorIntegration` class.
</objective>

## Artifacts this phase produces

This plan (11-02) produces:
- Expanded `TestBuildDiagnosticsOffline` in `tests/test_advisor.py` (real-dataset test, determinism test, ImportError-guard test)
- `TestAdvisorIntegration` class in `tests/test_advisor.py` (env-gated `advise` test, skipped without `ANTHROPIC_API_KEY`)

Depends on 11-01, which created `tests/test_advisor.py` with the initial `TestBuildDiagnosticsOffline::test_clustering_offline_with_synthetic` reachability test and wired the advisor into the public API.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-python-api-surface/11-RESEARCH.md
@.planning/phases/11-python-api-surface/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline build_diagnostics tests against real docs/data/ dataset</name>
  <files>tests/test_advisor.py</files>
  <read_first>
    - tests/test_advisor.py (as created by 11-01 — extend the existing TestBuildDiagnosticsOffline class)
    - 11-RESEARCH.md §9 "PYAPI-02: Offline unit test structure" (test_clustering_with_real_dataset, test_build_diagnostics_deterministic) and §4 "Dataset Loader API for Offline Tests" and Pitfall 3 / Pitfall 6
    - python/fdars/advisor.py (read ONLY the clustering branch expectations L619-626 and the basis branch signature — do NOT modify)
    - python/fdars/datasets.py (read ONLY the load_canadian_weather return shape: `.data` is an Fdata, raw matrix is `ds.data.data`, grid is `ds.argvals`)
  </read_first>
  <behavior>
    - test_clustering_with_real_dataset: load canadian weather, run `clustering.kmeans_fd(X, day, k=4, seed=42)`, then `build_diagnostics(result, method="clustering", argvals=day)` returns `method == "clustering"`, `k == 4`, `len(cluster_sizes) == 4`, and `pairwise_amplitude_distance is not None`.
    - test_build_diagnostics_deterministic: `build_diagnostics(result, method="basis")` on a fixed dict returns byte-identical dicts across two calls (d1 == d2).
  </behavior>
  <action>
    Add two offline methods to the existing `TestBuildDiagnosticsOffline` class in tests/test_advisor.py. First, `test_clustering_with_real_dataset`: load `fdars.datasets.load_canadian_weather()`, extract the raw matrix as `np.asarray(ds.data.data, dtype=float)` and grid as `np.asarray(ds.argvals, dtype=float)`, run `fdars.clustering.kmeans_fd(X, day, k=4, seed=42)` to produce a result dict carrying `centers`/`cluster`/`k` (use kmeans_fd directly, not cluster_optim, per Pitfall 6), then call `build_diagnostics(result, method="clustering", argvals=day)` and assert on `method`, `k`, `len(cluster_sizes)`, and that `pairwise_amplitude_distance` is not None. Second, `test_build_diagnostics_deterministic`: build diagnostics twice on a fixed `method="basis"` result dict and assert the two returned dicts are equal. Neither test imports anthropic; both are fully offline. Keep the 11-01 synthetic test intact.
  </action>
  <verify>
    <automated>pytest tests/test_advisor.py::TestBuildDiagnosticsOffline -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_clustering_with_real_dataset -x` passes.
    - `pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_build_diagnostics_deterministic -x` passes.
    - The real-dataset test uses `kmeans_fd` directly (not `cluster_optim`) and passes `argvals=day`.
    - No test in `TestBuildDiagnosticsOffline` imports `anthropic` or calls `advise()`.
    - All `TestBuildDiagnosticsOffline` tests pass with no network access.
  </acceptance_criteria>
  <precondition>The compiled `fdars` extension is importable in the test env (`maturin develop` has been run) — CI does this before `pytest`.</precondition>
  <done>`build_diagnostics` is exercised offline against a real `docs/data/` dataset and proven deterministic, network-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline ImportError-guard test for advise() without anthropic</name>
  <files>tests/test_advisor.py</files>
  <read_first>
    - tests/test_advisor.py (extend TestBuildDiagnosticsOffline)
    - 11-RESEARCH.md §9 "test_advise_raises_importerror_without_anthropic" and §3 "Lazy Import + ImportError Guard Convention" and Pitfall 2
    - python/fdars/advisor.py (read ONLY `_require_anthropic()` at L722-754 to confirm the ImportError message contains `pip install fdars[advisor]` — do NOT modify)
  </read_first>
  <behavior>
    - With `sys.modules["anthropic"]` monkeypatched to None, calling `advise(diag, task="interpretation", domain_context="test")` raises `ImportError` whose message matches the install-hint substring `pip install fdars[advisor]`.
    - `build_diagnostics(..., method="alignment")` still succeeds under the same monkeypatch (offline path never touches anthropic).
  </behavior>
  <action>
    Add `test_advise_raises_importerror_without_anthropic(self, monkeypatch)` to `TestBuildDiagnosticsOffline`. Use `monkeypatch.setitem(sys.modules, "anthropic", None)` to simulate anthropic being unavailable (per RESEARCH.md §9), build an alignment diagnostics dict via `build_diagnostics(...)` (proving the offline path is unaffected), then assert `advise(diag, task="interpretation", domain_context="test")` raises `ImportError` using `pytest.raises(ImportError, match="pip install fdars\\[advisor\\]")` (escape the bracket in the regex). This test is deliberately in the offline class because the monkeypatch guarantees no real anthropic import — it verifies the CORE-04 guard surfaces the install hint.
  </action>
  <verify>
    <automated>pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_advise_raises_importerror_without_anthropic -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The test passes with anthropic NOT installed (monkeypatch forces the ImportError path regardless).
    - The raised `ImportError` message matches the escaped regex for the `fdars[advisor]` install hint.
    - `build_diagnostics(method="alignment")` succeeds within the same test under the monkeypatch.
  </acceptance_criteria>
  <done>The advisor's missing-dependency guard is proven to raise a clear, install-hinted `ImportError`, offline, in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Env-gated advise() LLM integration test (skips without key)</name>
  <files>tests/test_advisor.py</files>
  <read_first>
    - tests/test_advisor.py (add a new TestAdvisorIntegration class)
    - 11-RESEARCH.md §4 "Env-Gated Integration Test Idiom" and §9 "TestAdvisorIntegration" and Pitfall 2
    - 11-VALIDATION.md "Per-Task Verification Map" (PYAPI-02 integration row: `pytest tests/test_advisor.py::TestAdvisorIntegration -v`) and "Manual-Only Verifications"
    - python/fdars/advisor.py (read ONLY the `advise` signature L915-982 and the `Advice` schema fields — do NOT modify)
  </read_first>
  <action>
    Add a `TestAdvisorIntegration` class to tests/test_advisor.py gated with a class-level `pytestmark = pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"), reason="ANTHROPIC_API_KEY not set — skipping LLM integration test")`. Add one method `test_advise_returns_advice_schema` that first calls `pytest.importorskip("anthropic")` and `pytest.importorskip("pydantic")` (belt-and-suspenders per Pitfall 2 — skip cleanly if the extra is not installed even when a key is set), builds a `method="basis"` diagnostics dict via `build_diagnostics`, calls `advise(diag, task="parameter", domain_context=...)`, and asserts the result is an `Advice` instance with a string `interpretation` and a list `recommendations`. Import `os` and `pytest` at module top. The test MUST skip — never fail — when the key is absent. Do not embed a real API key; the key is read from the environment by advisor.py.
  </action>
  <verify>
    <automated>pytest tests/test_advisor.py::TestAdvisorIntegration -v</automated>
  </verify>
  <acceptance_criteria>
    - With `ANTHROPIC_API_KEY` unset, `pytest tests/test_advisor.py::TestAdvisorIntegration -v` reports the test as SKIPPED (exit code 0, not failed/errored).
    - The skip reason references `ANTHROPIC_API_KEY not set`.
    - The class also `importorskip`s `anthropic` and `pydantic` so a set key with missing extra still skips (does not error).
    - No API key is hardcoded anywhere in the file.
  </acceptance_criteria>
  <precondition>[user_setup] Running this test against a live model (not just verifying the skip) requires `ANTHROPIC_API_KEY` set in the environment and the `[advisor]` extra installed. Absent the key, the test skips by design.</precondition>
  <done>The LLM `advise` call is covered by an env-gated integration test that skips (not fails) without `ANTHROPIC_API_KEY`, keeping CI network-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Anthropic API | Only crossed when ANTHROPIC_API_KEY is present; gated by skipif |
| test env → anthropic package | Offline tests must not require anthropic; monkeypatch/importorskip enforce this |
| user data → LLM (integration test) | Diagnostics numbers may be sent to Anthropic only in the env-gated test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Denial of Service | CI network access | low | mitigate | Integration test gated by `pytest.mark.skipif` on ANTHROPIC_API_KEY + `importorskip`; skips in CI (Task 3) |
| T-11-05 | Information Disclosure | API key handling in tests | low | accept | Key read from env only; never hardcoded (Task 3 acceptance criterion asserts this) — RESEARCH.md §13 |
| T-11-01 | Information Disclosure | user data → Anthropic (T-10-01 carryover) | low | accept | Only occurs when the user sets a key and runs the integration test explicitly; documented accepted risk from Phase 10 |
</threat_model>

<verification>
- `pytest tests/test_advisor.py::TestBuildDiagnosticsOffline -q` — all offline tests green, no network, no anthropic.
- `pytest tests/test_advisor.py::TestAdvisorIntegration -v` — reports SKIPPED with the ANTHROPIC_API_KEY reason when the key is unset.
- `pytest tests/ -v` — full suite green (offline + skipped integration).
</verification>

<success_criteria>
- PYAPI-02: `build_diagnostics` has offline unit tests against `docs/data/` that pass network-free; the `advise` LLM call is covered by an env-gated integration test that SKIPS (not fails) without `ANTHROPIC_API_KEY`.
- The missing-anthropic ImportError guard is exercised offline.
- `advisor.py` logic is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-python-api-surface/11-02-SUMMARY.md` when done
</output>

---
phase: 72-advisor-extension
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - python/fdars/advisor/aspects/frechet.py
  - tests/test_advisor_frechet.py
autonomous: true
requirements: [ADV-01, ADV-02]
estimate:
  tokens: 48000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "build_diagnostics(frechet_anova_result, method='frechet') returns has_anova True with anova_p_value_permutation a float in [0,1] (ADV-01)"
    - "build_diagnostics on a frechet_mean numpy array (not a dict) returns has_frechet_mean True without raising (ADV-01, non-dict handling)"
    - "build_diagnostics(frechet_global_reg_result) sets has_global_reg True and has_local_reg False; local_reg sets bandwidth (float) (ADV-01)"
    - "Every frechet diagnostic value is native Python float/int/bool/list/None — no numpy scalar (grounding invariant)"
    - "json.dumps(build_diagnostics(...)) succeeds for anova/global_reg/local_reg/mean and is deterministic (ADV-02)"
  artifacts:
    - python/fdars/advisor/aspects/frechet.py
    - tests/test_advisor_frechet.py
  key_links:
    - "method_lc == 'frechet' dispatch (registered in 72-01) → _build_frechet_diagnostics full field logic"
    - "isinstance(raw, dict) branch splits array (frechet_mean) vs dict (anova/reg) paths"
---

<objective>
Fill in the real `_build_frechet_diagnostics` field logic for all four frechet result shapes
using the exact PyDict keys confirmed from `src/frechet_mod.rs`. The method string and dispatch
branch were registered atomically in 72-01; this plan replaces the minimal stub body with the
grounded anova/global_reg/local_reg/mean branches and ships the per-aspect test.

Purpose: extend the proven aspect loop to the second new aspect (frechet), handling the
non-dict `frechet_mean` array return and the two regression dict shapes.
Output: complete frechet.py builder + tests/test_advisor_frechet.py.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/72-advisor-extension/72-RESEARCH.md
@python/fdars/advisor/aspects/regression.py
@python/fdars/advisor/aspects/frechet.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: frechet builder — anova / global_reg / local_reg / frechet_mean grounded branches</name>
  <files>python/fdars/advisor/aspects/frechet.py</files>
  <read_first>
    - src/frechet_mod.rs:95-110 (frechet_anova 9 keys — CONFIRMED: statistic, p_value_asymptotic, p_value_permutation, n_perm, group_frechet_variances, pooled_frechet_variance, fn_statistic, un_statistic, group_labels), :164-166 (frechet_global_reg: predicted, xout, x_bar — NO bandwidth), :224-226 (frechet_local_reg: predicted, xout, bandwidth), :342-346 (frechet_mean returns a naked numpy array via .into_any(), NOT a dict)
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §2B (frechet field lists + array/dict discriminator) and §Code-Examples frechet.py skeleton
    - python/fdars/advisor/aspects/regression.py (grounding discipline)
    - python/fdars/advisor/aspects/frechet.py (the 72-01 stub — replace its body, keep the frechet_mean array path)
  </read_first>
  <behavior>
    - frechet_anova result → has_anova True, has_global_reg/has_local_reg/has_frechet_mean False; anova_p_value_permutation float in [0,1]; anova_p_value_asymptotic float; n_perm int; n_groups==int(len(unique group_labels)); pooled_frechet_variance float; group_frechet_variance_max float
    - frechet_global_reg result → has_global_reg True, has_local_reg False (no bandwidth key); predicted_n_obs int; bandwidth None
    - frechet_local_reg result → has_local_reg True, has_global_reg False; bandwidth float; predicted_n_obs int
    - frechet_mean 1-D array (spherical) → has_frechet_mean True, frechet_mean_ndim==1, frechet_mean_dim==int(arr.shape[-1]), frechet_mean_trace None
    - frechet_mean 2-D array (spd/corr) → frechet_mean_ndim==2, frechet_mean_trace float
    - every output key present in all branches (stable shape, None fallbacks); json.dumps succeeds; no np.generic
  </behavior>
  <action>
    Replace the 72-01 stub body of `_build_frechet_diagnostics(raw, **kwargs)`. Keep
    `{"method": "frechet"}` as the first key. FIRST branch: `if not isinstance(raw, dict):`
    treat `raw` as the frechet_mean array (`arr = np.asarray(raw)`), set has_frechet_mean True,
    all other has_* False, frechet_mean_ndim=int(arr.ndim), frechet_mean_dim=int(arr.shape[-1]),
    frechet_mean_trace=float(np.trace(arr)) if arr.ndim == 2 else None, and set every
    anova/reg field to None; return. This guards Pitfall 4 (array, not dict).

    Dict path discriminators using the CONFIRMED keys:
      - has_anova = `"p_value_permutation" in raw and "group_labels" in raw` (both unique to anova)
      - has_local_reg = `"bandwidth" in raw` (unique to local_reg)
      - has_global_reg = `"predicted" in raw and "x_bar" in raw and not has_local_reg`
    Fields (all cast, all guarded):
      - anova: anova_p_value_permutation=float(raw["p_value_permutation"]); anova_p_value_asymptotic=float(raw["p_value_asymptotic"]) if "p_value_asymptotic" in raw else None; n_perm=int(raw["n_perm"]) if "n_perm" in raw else None; n_groups=int(len(np.unique(np.asarray(raw["group_labels"])))) if "group_labels" in raw else None; pooled_frechet_variance=float(raw["pooled_frechet_variance"]) if "pooled_frechet_variance" in raw else None; group_frechet_variance_max=float(np.max(np.asarray(raw["group_frechet_variances"]))) if "group_frechet_variances" in raw else None
      - reg (global or local): predicted_n_obs=int(np.asarray(raw["predicted"]).shape[0]) if "predicted" in raw else None; bandwidth=float(raw["bandwidth"]) if "bandwidth" in raw else None
    Set has_frechet_mean False and the three frechet_mean fields to None on the dict path.
    Provide stable None fallbacks so every key is always present regardless of branch.
    Cast EVERY numeric with float()/int() (np.max/np.trace return numpy scalars — Pitfall 2).
    Import only numpy — no anthropic/provider (LLM-free).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "
import numpy as np, json
from fdars import frechet
from fdars.advisor import build_diagnostics
rng = np.random.default_rng(0)
objs = [(lambda A: A @ A.T + np.eye(3)*0.1)(rng.standard_normal((3,3))) for _ in range(5)]
mean = frechet.frechet_mean(objs, space='spd', d=3)
d = build_diagnostics(mean, method='frechet')
assert d['method']=='frechet' and d['has_frechet_mean'] is True and d['frechet_mean_ndim']==2, d
json.dumps(d, sort_keys=True)
def no_np(o):
    assert not isinstance(o, np.generic), type(o)
    [no_np(v) for v in (o.values() if isinstance(o,dict) else o if isinstance(o,list) else [])]
no_np(d)
print('OK')"</automated>
    <fails_when>frechet_mean array input crashes the builder (dict-only assumption — Pitfall 4), a numpy scalar leaks (no_np raises), or json.dumps fails.</fails_when>
  </verify>
  <acceptance_criteria>
    - `_build_frechet_diagnostics` handles all four shapes: anova dict, global_reg dict, local_reg dict, frechet_mean array
    - anova uses the CONFIRMED key `p_value_permutation` (NOT a guessed `p_value`); n_groups derived from unique group_labels
    - global_reg (no bandwidth) → has_global_reg True; local_reg (bandwidth present) → has_local_reg True, bandwidth float
    - every output key present in every branch (stable shape); no anthropic import
  </acceptance_criteria>
  <reversibility rating="reversible">Additive Python; body of an already-registered builder.</reversibility>
  <done>frechet builder produces grounded native-scalar diagnostics for all four result shapes, including the non-dict frechet_mean array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: frechet per-aspect serialization + grounding + determinism tests</name>
  <files>tests/test_advisor_frechet.py</files>
  <read_first>
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §Section 4 (test skeleton, check_no_numpy)
    - tests/test_frechet.py:230-295 (real fixture construction: _make_spd, _OBJECTS_SPD, frechet_mean(space='spd', d=D_SPD); frechet_anova / frechet_global_reg / frechet_local_reg call signatures)
    - src/frechet_mod.rs:55-110 (frechet_anova signature + keys), :145-166 (global_reg), :205-226 (local_reg), :342-350 (frechet_mean signature: objects list, space, d)
    - python/fdars/advisor/aspects/frechet.py (the builder from Task 1 — assert its actual keys)
  </read_first>
  <behavior>
    - TestFrechetAspect covers frechet_mean (spd array), frechet_anova, frechet_global_reg, frechet_local_reg fixtures
    - For each: build_diagnostics(result, method="frechet") — json.dumps(diag, sort_keys=True) does not raise; check_no_numpy passes; two calls equal (determinism); diag["method"]=="frechet"
    - anova fixture asserts has_anova True and 0.0 <= anova_p_value_permutation <= 1.0
    - frechet_mean spd fixture asserts has_frechet_mean True and frechet_mean_ndim == 2
    - global_reg asserts has_global_reg True and has_local_reg False; local_reg asserts has_local_reg True and bandwidth is a float
  </behavior>
  <action>
    Create tests/test_advisor_frechet.py mirroring RESEARCH §4 and the fts test structure.
    Reuse the exact fixture construction from tests/test_frechet.py (import or copy _make_spd /
    the SPD object list; call frechet.frechet_mean(objs, space='spd', d=3)). Build frechet_anova,
    frechet_global_reg, frechet_local_reg fixtures with the same call signatures those tests use
    (read tests/test_frechet.py for exact args). Include check_no_numpy. All offline. Assert
    grounding (check_no_numpy + json.dumps), determinism, method field, and the two branch-flag /
    range assertions above. Only assert on values the builder reads from fdars output.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_frechet.py -x -q</automated>
    <fails_when>a numpy scalar leaks (check_no_numpy raises), json.dumps fails, determinism fails, or the frechet_mean array path raises.</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_advisor_frechet.py exists and passes offline
    - covers frechet_mean (array), anova, global_reg, local_reg with json.dumps + check_no_numpy + determinism + method-field + branch-flag assertions
  </acceptance_criteria>
  <done>frechet aspect proven grounded, JSON-serialisable, deterministic across all four shapes by an offline test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fdars result → build_diagnostics | frechet result is dict OR naked numpy array; both untrusted |
| build_diagnostics → json.dumps | numpy scalars must be cast out |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-04 | Spoofing | frechet_mean array vs dict | high | mitigate | isinstance(raw, dict) guard before any dict key access (Pitfall 4) |
| T-72-05 | Tampering | guessed anova keys → KeyError | medium | mitigate | Use CONFIRMED keys from frechet_mod.rs read; guard every optional key |
| T-72-06 | Tampering | numpy scalar into json.dumps | high | mitigate | float()/int() cast on every value; check_no_numpy in tests |
| T-72-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (pure Python; fdars already built) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor_frechet.py tests/test_guard_sync_version_independent.py -x -q` green
- frechet still absent from both _RUNNABLE_METHODS frozensets (SC3, unchanged since 72-01)
</verification>

<success_criteria>
- frechet aspect produces grounded native-scalar diagnostics for anova/global_reg/local_reg/mean (ADV-01)
- non-dict frechet_mean array handled without crashing (ADV-01)
- json.dumps deterministic and numpy-free (ADV-02)
</success_criteria>

<output>
Create `.planning/phases/72-advisor-extension/72-02-SUMMARY.md` when done
</output>
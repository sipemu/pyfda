---
phase: 72-advisor-extension
plan: 03
type: execute
wave: 3
depends_on: [72-01]
files_modified:
  - python/fdars/advisor/aspects/regression.py
  - python/fdars/advisor/aspects/classification.py
  - python/fdars/advisor/aspects/spm.py
  - python/fdars/advisor/__init__.py
  - tests/test_advisor_regression_v6.py
  - tests/test_advisor_group_b.py
  - tests/test_advisor_spm_v11.py
autonomous: true
requirements: [ADV-01, ADV-02]
estimate:
  tokens: 70000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "build_diagnostics(fof_regression_result, method='regression') sets has_fof_regression True with beta_surface_shape a [int,int] list (ADV-01)"
    - "build_diagnostics(fof_re_regression_result) sets has_fof_re_regression True with n_subjects int; fam sets has_fam True; gkam sets has_fregre_gkam True with gkam_converged bool (ADV-01)"
    - "build_diagnostics(shapelet_classifier_fit_handle, method='classification') sets has_shapelet_classifier True with shapelet_train_accuracy float — the opaque handle is converted, not dict()-coerced (ADV-01, handle handling)"
    - "build_diagnostics(mfpca_result, method='spm') sets has_mfpca True; spe_multivariate array sets has_spe_multivariate True (ADV-01, non-dict handling)"
    - "Every extended diagnostic value is native Python float/int/bool/list/None; json.dumps deterministic (grounding invariant + ADV-02)"
  artifacts:
    - tests/test_advisor_spm_v11.py
  key_links:
    - "__init__.py coercion guard: shapelet opaque handle (hasattr train_accuracy) must bypass dict(raw)"
    - "regression.py has_fof_regression / has_fof_re_regression / has_fam / has_fregre_gkam branches"
    - "spm.py has_mfpca / has_spe_multivariate branches"
---

<objective>
Extend the three existing aspect builders for the newly-bound v11.0 methods, holding the
grounding invariant. All method strings already exist in the guard-sync sets (regression,
classification, spm are pre-existing) — no guard-sync change is needed here; only builder
branches + one coercion guard in __init__.py for the shapelet opaque handle.

Purpose: complete ADV-01 coverage for the extended aspects using the exact PyDict keys
confirmed from the Rust bindings.
Output: extended regression.py, classification.py, spm.py, a shapelet-handle coercion guard
in __init__.py, and the tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/72-advisor-extension/72-RESEARCH.md
@python/fdars/advisor/aspects/regression.py
@python/fdars/advisor/aspects/classification.py
@python/fdars/advisor/aspects/spm.py
@python/fdars/advisor/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: regression.py — fof_regression / fof_re_regression / fam / fregre_gkam / fregre_gsam branches</name>
  <files>python/fdars/advisor/aspects/regression.py, tests/test_advisor_regression_v6.py</files>
  <read_first>
    - src/regression_mod.rs:1295-1304 (fof_regression keys — CONFIRMED: beta_surface (m_y,m_x), fitted (n,m_y), residuals, r_squared_t, r_squared, ncomp_x, ncomp_y, coef_matrix), :1553-1566 (fof_re_regression adds random_effects (n_subjects,m_y), sigma2_u, sigma2_eps, n_subjects)
    - src/scalar_on_function_mod.rs:107-117 (fam keys — CONFIRMED: fitted_values, residuals, component_fits, intercept, bandwidths, ncomp, r_squared), :189-199 (fregre_gsam same 7 keys), :280-291 (fregre_gkam adds iterations, converged)
    - python/fdars/advisor/aspects/regression.py:150-208 (the has_functional_glm / has_concurrent_regression branch pattern to mirror — add new branches at the bottom before `return diag`)
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §2C (discriminators + fields)
  </read_first>
  <behavior>
    - fof_regression → has_fof_regression True; beta_surface_shape==[m_y, m_x] (ints); beta_surface_max_abs float; fof_r_squared float; fof_ncomp==[ncomp_x, ncomp_y]
    - fof_re_regression → has_fof_re_regression True; n_subjects int; sigma2_u_max float; re_dims [int,int]; (has_fof_regression also True since beta_surface present — has_fof_re_regression is the specific discriminator)
    - fam / fregre_gsam → has_fam True; fam_n_obs int; fam_n_components==int(len(component_fits)); fam_r_squared float; fam_ncomp int
    - fregre_gkam → has_fregre_gkam True; gkam_converged bool; gkam_bandwidths list[float]; gkam_n_predictors int
    - existing fregre_lm/pls/fosr/functional_glm/concurrent paths unaffected (regression suite still green)
    - all new keys present (None fallback) on every result; json.dumps succeeds; no np.generic
  </behavior>
  <action>
    Add new branches at the BOTTOM of `_build_regression_diagnostics`, before `return diag`,
    mirroring the has_functional_glm pattern. Use the CONFIRMED discriminators:
      - has_fof_regression = `"beta_surface" in raw`
      - has_fof_re_regression = `"random_effects" in raw and "n_subjects" in raw`
      - has_fam = `"component_fits" in raw and "fitted_values" in raw`  (covers fam AND gsam — same 7 keys)
      - has_fregre_gkam = `"converged" in raw and "bandwidths" in raw`
    Fields (all cast, all guarded, stable None fallback):
      - fof_regression: beta_surface_shape=[int(bs.shape[0]), int(bs.shape[1])]; beta_surface_max_abs=float(np.max(np.abs(bs))); fof_r_squared=float(raw["r_squared"]) if "r_squared" in raw else None; fof_ncomp=[int(raw["ncomp_x"]), int(raw["ncomp_y"])] if "ncomp_x" in raw and "ncomp_y" in raw else None (bs=np.asarray(raw["beta_surface"]))
      - fof_re_regression: n_subjects=int(raw["n_subjects"]); sigma2_u_max=float(np.max(np.asarray(raw["sigma2_u"]))) if "sigma2_u" in raw else None; re_dims=[int(re.shape[0]), int(re.shape[1])] if "random_effects" in raw else None; sigma2_eps=float(raw["sigma2_eps"]) if "sigma2_eps" in raw else None
      - fam/gsam: fam_n_obs=int(np.asarray(raw["fitted_values"]).shape[0]); fam_n_components=int(len(raw["component_fits"])); fam_r_squared=float(raw["r_squared"]) if "r_squared" in raw else None; fam_ncomp=int(raw["ncomp"]) if "ncomp" in raw else None
      - fregre_gkam: gkam_converged=bool(raw["converged"]); gkam_bandwidths=[float(v) for v in np.asarray(raw["bandwidths"])]; gkam_n_predictors=int(len(np.asarray(raw["bandwidths"])))
    NOTE: gkam ALSO has component_fits+fitted_values, so has_fam will be True for gkam too —
    that overlap is acceptable (has_fregre_gkam is the specific discriminator). Do NOT invent
    a residual_std field for fam (residuals ARE present but keep to the confirmed diagnostic
    set; only add fields with clear grounded meaning). Cast every numeric. numpy only.
    Add a TestFofRegression (+ fam/gkam) class to tests/test_advisor_regression_v6.py building
    real fixtures via fdars.regression.fof_regression / fof_re_regression and
    fdars.scalar_on_function.fam / fregre_gkam / fregre_gsam (read those Rust signatures for
    arg shapes), with json.dumps + check_no_numpy + determinism + branch-flag assertions.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_regression_v6.py -x -q</automated>
    <fails_when>an fof/fam/gkam branch leaks a numpy scalar, uses a guessed key that KeyErrors, or an existing regression path regresses (suite red).</fails_when>
  </verify>
  <acceptance_criteria>
    - regression.py has has_fof_regression / has_fof_re_regression / has_fam / has_fregre_gkam branches using CONFIRMED keys
    - existing regression tests still pass (no regression in fregre_lm/pls/fosr/glm/concurrent)
    - new fixtures assert branch flags + json.dumps + check_no_numpy + determinism
  </acceptance_criteria>
  <reversibility rating="reversible">Additive branches at the bottom of an existing builder.</reversibility>
  <done>regression aspect covers fof/fof_re/fam/gkam/gsam with grounded native scalars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: classification.py — shapelet_classifier opaque-handle branch + __init__.py coercion guard</name>
  <files>python/fdars/advisor/aspects/classification.py, python/fdars/advisor/__init__.py, tests/test_advisor_group_b.py</files>
  <read_first>
    - src/shapelet_mod.rs:116-146 (PyShapeletClassifierFit #[pymethods]: .n_shapelets (usize), .train_accuracy (f64), .classes (PyArray1 i64), .n_classes (usize))
    - python/fdars/advisor/__init__.py:159-171 (the coercion block — the opaque handle lacks __array__ and data, so dict(raw) WOULD be attempted and FAIL; a guard must be added BEFORE dict(raw))
    - python/fdars/advisor/aspects/classification.py:142-188 (has_elastic_multinomial branch pattern to mirror)
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §2D (Pitfall 1 — opaque handle) + §Section 3
  </read_first>
  <behavior>
    - build_diagnostics(shapelet_classifier_fit(...), method="classification") does NOT raise TypeError (handle is converted, not dict()-coerced)
    - result sets has_shapelet_classifier True, shapelet_n_shapelets int > 0, shapelet_train_accuracy float in [0,1], shapelet_n_classes int
    - existing elastic_multinomial and point-estimate/CV classification paths unaffected
    - json.dumps succeeds; no np.generic; deterministic
  </behavior>
  <action>
    In __init__.py: add a coercion guard in the block at lines 159-171, BEFORE the `dict(raw)`
    call, that converts the shapelet opaque handle to a dict. Detect it by
    `hasattr(raw, "train_accuracy") and hasattr(raw, "n_shapelets")` and build:
    `raw = {"train_accuracy": float(raw.train_accuracy), "n_shapelets": int(raw.n_shapelets),
    "n_classes": int(raw.n_classes) if hasattr(raw, "n_classes") else None}`. This must run
    only for the classification path — place it so it also does not interfere with other
    aspects (a handle with these attributes is unique to shapelet_classifier_fit). Extract
    the attributes here (the pymethods getters are fdars-computed values — NOT synthetic).
    In classification.py: add a has_shapelet_classifier branch at the bottom (before return),
    mirroring has_elastic_multinomial. Discriminator on the dict path:
    `has_shapelet_classifier = "n_shapelets" in raw` (unique — no other classification/regression
    result produces this key). Fields: shapelet_n_shapelets=int(raw["n_shapelets"]);
    shapelet_train_accuracy=float(raw["train_accuracy"]); shapelet_n_classes=int(raw["n_classes"])
    if raw.get("n_classes") is not None else None. Stable None fallback on the else branch.
    Add a TestShapeletClassifier class to tests/test_advisor_group_b.py building a real
    shapelet_classifier_fit fixture (read src/shapelet_mod.rs for the fit signature / a prior
    shapelet test for input shape), asserting the handle is accepted, branch flags, json.dumps,
    check_no_numpy, determinism.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_group_b.py -x -q</automated>
    <fails_when>the shapelet opaque handle raises TypeError from dict(raw) (guard missing), a numpy scalar leaks, or an existing classification path regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - __init__.py converts the shapelet handle to a dict before dict(raw) is attempted (no TypeError)
    - classification.py has_shapelet_classifier branch uses CONFIRMED handle attributes
    - existing elastic_multinomial / point-estimate / CV tests still pass
  </acceptance_criteria>
  <reversibility rating="reversible">Additive branch + a narrowly-scoped coercion guard.</reversibility>
  <done>classification aspect accepts the shapelet opaque handle and produces grounded diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: spm.py — mfpca dict branch + spe_multivariate array branch</name>
  <files>python/fdars/advisor/aspects/spm.py, tests/test_advisor_spm_v11.py</files>
  <read_first>
    - src/spm_mod.rs:882-945 (mfpca keys — CONFIRMED: scores (n,ncomp), eigenfunctions (list of P arrays), eigenvalues, means (list), scales, grid_sizes), :977+ (spe_multivariate returns a naked (n,) numpy array)
    - python/fdars/advisor/aspects/spm.py (the builder; add branches near the top for the array path, and before `return diag` for mfpca — spm_phase1 dict path must stay intact)
    - python/fdars/advisor/aspects/_utils.py (_eigenvalues_to_variance_cumulative — reuse, do NOT hand-roll)
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §2E + §Code-Examples (spm mfpca/spe_multivariate skeleton)
  </read_first>
  <behavior>
    - build_diagnostics(spm_phase1_result, method="spm") — existing behavior unchanged (t2/spe/eigenvalue fields still produced)
    - build_diagnostics(mfpca_result, method="spm") → has_mfpca True; mfpca_ncomp==int(len(eigenvalues)); mfpca_n_obs int; mfpca_n_variables==int(len(eigenfunctions)); mfpca_eigenvalues list[float]; mfpca_variance_explained_cumulative via _eigenvalues_to_variance_cumulative
    - build_diagnostics(spe_multivariate_array, method="spm") → has_spe_multivariate True; spe_mv_n_obs int; spe_mv_max/mean float; spe_mv_all_nonneg bool
    - json.dumps succeeds; no np.generic; deterministic
  </behavior>
  <action>
    Extend `_build_spm_diagnostics` per RESEARCH §Code-Examples. Add near the TOP a
    `has_spe_multivariate = not isinstance(raw, dict) and hasattr(raw, "__array__")` branch
    for the naked spe_multivariate array (spe_mv_n_obs=int(len(a)), spe_mv_max=float(np.max(a)),
    spe_mv_mean=float(np.mean(a)), spe_mv_all_nonneg=bool(float(np.min(a)) >= 0.0)); when this
    fires, set the mfpca fields to None and return early (or set flags and skip dict access).
    CRITICAL: the existing spm_phase1 dict-path key accesses (t2/spe/eigenvalues) must be
    reachable ONLY when raw is a dict — guard the array path first so `raw.get(...)` is never
    called on an array. Add a has_mfpca branch (discriminator
    `isinstance(raw, dict) and "eigenfunctions" in raw and "scales" in raw`, unique vs spm_phase1)
    computing the fields above; import `_eigenvalues_to_variance_cumulative` from _utils (already
    imported at top of spm.py). Stable None fallbacks so every new key is always present.
    Cast every numeric. numpy only; the ONE existing live spe_moment_match_diagnostic call is
    guarded and stays — do NOT add any new live call for mfpca/spe_multivariate.
    Create tests/test_advisor_spm_v11.py (new) with TestMfpca + TestSpeMultivariate classes,
    real fixtures via fdars.spm.mfpca (list of 2D arrays input — read src/spm_mod.rs / a prior
    mfpca test for shape) and fdars.spm.spe_multivariate, asserting branch flags, json.dumps,
    check_no_numpy, determinism, and spe_mv_all_nonneg True. Also assert spm_phase1 path still
    works (regression guard).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_spm_v11.py -x -q</automated>
    <fails_when>the spe_multivariate array path calls raw.get() on an array (crash), a numpy scalar leaks, mfpca uses a guessed key that KeyErrors, or the spm_phase1 dict path regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - spm.py has has_spe_multivariate (array) and has_mfpca (dict) branches using CONFIRMED keys
    - the array path is guarded FIRST so no dict access runs on an array; spm_phase1 dict path intact
    - mfpca uses _eigenvalues_to_variance_cumulative from _utils (not hand-rolled)
    - new tests pass offline; spm_phase1 regression guard passes
  </acceptance_criteria>
  <reversibility rating="reversible">Additive branches in an existing builder.</reversibility>
  <done>spm aspect covers mfpca (dict) and spe_multivariate (naked array) with grounded native scalars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fdars result → build_diagnostics | result is dict, naked array (spe_multivariate), OR opaque handle (shapelet) — all untrusted |
| __init__.py dict(raw) coercion | opaque handle must be converted before dict(raw) is attempted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-07 | Spoofing | shapelet opaque handle vs dict | high | mitigate | hasattr(train_accuracy) coercion guard in __init__.py before dict(raw) (Pitfall 1) |
| T-72-08 | Spoofing | spe_multivariate array vs dict | high | mitigate | array path guarded first in spm builder before any dict access (Pitfall 5) |
| T-72-09 | Tampering | guessed mfpca/fof/fam keys → KeyError | medium | mitigate | Use CONFIRMED keys from Rust binding reads; guard every optional key |
| T-72-10 | Tampering | numpy scalar into json.dumps | high | mitigate | float()/int()/bool() cast on every value; check_no_numpy in tests |
| T-72-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (pure Python; fdars already built) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor_regression_v6.py tests/test_advisor_group_b.py tests/test_advisor_spm_v11.py -x -q` green
- `regression`/`classification`/`spm` unchanged in guard-sync sets (pre-existing; no drift introduced)
</verification>

<success_criteria>
- regression/classification/spm extended for fof/fof_re/fam/gkam/gsam/shapelet/mfpca/spe_multivariate (ADV-01)
- opaque handle + naked array inputs handled without crash (ADV-01)
- all diagnostics native-scalar grounded, json.dumps deterministic (ADV-02)
</success_criteria>

<output>
Create `.planning/phases/72-advisor-extension/72-03-SUMMARY.md` when done
</output>
---
phase: 72-advisor-extension
plan: 04
type: execute
wave: 4
depends_on: [72-01, 72-02, 72-03]
files_modified:
  - tests/test_advisor_grounding.py
autonomous: true
requirements: [ADV-01, ADV-02]
estimate:
  tokens: 30000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The full guard-sync + all new/extended advisor serialization tests pass together in one run (ADV-02)"
    - "A test asserts the build_diagnostics number path for the new aspects imports no anthropic/provider module — provably LLM-free (ADV-02)"
    - "test_advisor_grounding.py covers at least one new aspect (fts or frechet) proving native-scalar grounding via the existing grounding harness (ADV-01)"
    - "frechet and fts remain absent from both _RUNNABLE_METHODS frozensets (SC3)"
  artifacts:
    - tests/test_advisor_grounding.py
  key_links:
    - "guard-sync test + per-aspect serialization tests + grounding test form the phase gate"
---

<objective>
Consolidate the phase into a single passing gate: extend the grounding test for the new
aspects, add an explicit LLM-free assertion for the number path, and prove the whole
advisor + guard-sync surface is green together. No new aspect logic — this plan is the
verification lock that ADV-01 and ADV-02 are fully satisfied.

Purpose: turn the three ROADMAP success criteria into an executable gate.
Output: extended tests/test_advisor_grounding.py + a green full-suite advisor/MCP run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/72-advisor-extension/72-RESEARCH.md
@tests/test_advisor_grounding.py
@python/fdars/advisor/aspects/fts.py
@python/fdars/advisor/aspects/frechet.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: grounding coverage + LLM-free assertion for new aspects; full advisor/MCP gate green</name>
  <files>tests/test_advisor_grounding.py</files>
  <read_first>
    - tests/test_advisor_grounding.py (existing grounding harness — _check_grounding / GroundingViolationError usage; mirror its structure to add fts/frechet cases)
    - .planning/phases/72-advisor-extension/72-RESEARCH.md §Section 5 (LLM-free proof — build_diagnostics never imports anthropic; the deferred import lives in advise()) and §Section 4 (grounding check pattern)
    - python/fdars/advisor/aspects/fts.py, python/fdars/advisor/aspects/frechet.py (the shipped builders)
    - python/fdars/advisor/__init__.py:227-232 (dispatch tail — confirm fts/frechet branches shipped in 72-01)
  </read_first>
  <behavior>
    - test_advisor_grounding.py has at least one new case building diagnostics for fts (or frechet) and asserting native-scalar grounding passes (no GroundingViolationError on real values) — mirrors the existing regression/spm grounding cases
    - a test asserts that calling build_diagnostics(fts_result, method="fts") does NOT import "anthropic" (assert "anthropic" not in sys.modules before/after, in a subprocess or a clean check) — proving the number path is LLM-free
    - a subprocess/module-graph check confirms importing fdars.advisor.aspects.fts and .frechet pulls in no anthropic/provider module
    - running the combined gate passes: guard-sync + fts + frechet + regression_v6 + group_b + spm_v11 + grounding
  </behavior>
  <action>
    Extend tests/test_advisor_grounding.py: add a grounding case for fts (and/or frechet)
    mirroring the existing per-aspect grounding cases — build a real fts result, call
    build_diagnostics(result, method="fts"), and assert the grounding harness accepts the
    native-scalar dict (no GroundingViolationError). Add an explicit LLM-free assertion: in a
    fresh subprocess (subprocess.run([sys.executable, "-c", ...])) import fdars.advisor, call
    build_diagnostics on an fts result, and assert "anthropic" not in sys.modules afterward —
    proving no provider import is triggered on the number path. Keep everything offline (no
    ANTHROPIC_API_KEY). Do NOT modify any aspect or guard-set here — this is verification only.
    If the grounding harness needs a fabricated-value negative case (to prove it would REJECT a
    synthetic number), add one mirroring the existing pattern: inject a non-fdars value and
    assert GroundingViolationError.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_grounding.py tests/test_guard_sync_version_independent.py tests/test_advisor_fts.py tests/test_advisor_frechet.py tests/test_advisor_regression_v6.py tests/test_advisor_group_b.py tests/test_advisor_spm_v11.py -q</automated>
    <fails_when>any new/extended aspect leaks a numpy or synthetic value (grounding fails), the guard-sync sets drift, or build_diagnostics triggers an anthropic import on the number path.</fails_when>
  </verify>
  <acceptance_criteria>
    - test_advisor_grounding.py covers at least one new aspect with a passing native-scalar grounding case
    - an explicit LLM-free assertion proves build_diagnostics on the new aspects imports no anthropic module
    - the combined advisor + guard-sync gate passes green
    - fts and frechet still absent from both _RUNNABLE_METHODS frozensets (verified indirectly by guard-sync + SC3)
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions.</reversibility>
  <done>ADV-01 and ADV-02 are proven by a single green gate: grounding, guard-sync, per-aspect serialization, and LLM-free number path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_diagnostics number path → advise() | LLM boundary; number path must not import any provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-11 | Elevation | provider import leaking into number path | high | mitigate | Subprocess assertion: "anthropic" not in sys.modules after build_diagnostics (ADV-02 LLM-free) |
| T-72-12 | Tampering | synthetic value bypassing grounding | high | mitigate | Grounding harness negative case rejects fabricated values |
| T-72-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (pure Python; fdars already built) |
</threat_model>

<verification>
- Combined gate green: `pytest tests/test_advisor_grounding.py tests/test_guard_sync_version_independent.py tests/test_advisor_fts.py tests/test_advisor_frechet.py tests/test_advisor_regression_v6.py tests/test_advisor_group_b.py tests/test_advisor_spm_v11.py -q`
- Optional full-suite confidence run: `pytest tests/ -q` stays green (no regressions)
</verification>

<success_criteria>
- Grounding proven for the new aspects; guard-sync consistent; number path LLM-free (ADV-01, ADV-02)
- frechet + fts diagnostics-only (SC3)
</success_criteria>

<output>
Create `.planning/phases/72-advisor-extension/72-04-SUMMARY.md` when done
</output>
---
phase: 86-single-source-of-truth-wiring-ci-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/code/assert_coverage.py
  - paper/code/gen_refs_bib.py
  - paper/coverage_counts.tex
  - paper/refs.bib
  - paper/paper.tex
  - Makefile
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "assert_coverage.py derives all six coverage macros from python/fdars/_capability_map.json + _references_map.json — no coverage integer is hardcoded in any .tex file (PIPE-03)"
    - "assert_coverage.py --check exits non-zero when committed paper/coverage_counts.tex differs from a fresh regeneration (drift tripwire, PIPE-03)"
    - "gen_refs_bib.py generates paper/refs.bib with exactly 47 @misc entries, skipping the 10 _uncurated_* placeholder keys (PIPE-04)"
    - "paper/paper.tex consumes coverage macros via \\input{coverage_counts} and consumes refs.bib via \\bibliography{refs} — the manuscript never sees a literal integer or an uncurated cite-key"
    - "make paper regenerates figures + coverage_counts.tex + refs.bib in one command and leaves an empty git diff on re-run"
  artifacts:
    - paper/code/assert_coverage.py
    - paper/code/gen_refs_bib.py
    - paper/coverage_counts.tex
    - paper/refs.bib
    - paper/paper.tex
    - Makefile
  key_links:
    - "assert_coverage.py counting logic mirrors scripts/generate_capability_dataset.py:_coverage_counts() (denominator = sum(len(v) for v in cap.values()) = 437, numerator via curated papers = 28)"
    - "gen_refs_bib.py cite-key IS the paper-key (no transformation); paper.tex \\cite{} calls resolve against paper/refs.bib in the same directory"
    - "coverage_counts.tex + refs.bib are committed so tectonic (and the Phase 90 offline pdflatex+bibtex gate) find them without running the generators"
---

<objective>
Wire the two committed JSON maps (`python/fdars/_capability_map.json`, `python/fdars/_references_map.json`) to the LaTeX manuscript by writing two stdlib-only generator scripts under `paper/code/`, committing their generated outputs (`paper/coverage_counts.tex`, `paper/refs.bib`), consuming those outputs from `paper/paper.tex`, and extending the `make paper` pipeline to regenerate everything in one command.

Purpose: Establish the single-source-of-truth drift tripwires (PIPE-03, PIPE-04) so that every number and every citation in the manuscript is machine-derived and cannot silently drift from the maps. This plan produces the artifacts; Plan 86-02 wires them into a CI gate.

Output: `assert_coverage.py` (generate + `--check` modes), `gen_refs_bib.py`, committed `coverage_counts.tex` (6 `\newcommand` macros), regenerated `refs.bib` (47 `@misc` entries), a `\input{coverage_counts}` line in `paper.tex`, and extended Makefile targets.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md
@paper/paper.tex
@Makefile
@scripts/generate_capability_dataset.py
</context>

<artifacts_this_phase_produces>
This plan (86-01) produces:
- `paper/code/assert_coverage.py` — coverage-macro generator + `--check` drift gate (PIPE-03)
- `paper/code/gen_refs_bib.py` — BibTeX generator from `_references_map.json` (PIPE-04)
- `paper/coverage_counts.tex` — 6 generated `\newcommand` macros (committed)
- `paper/refs.bib` — regenerated from the stub, 47 `@misc` entries (committed)
- `paper/paper.tex` — modified to add `\input{coverage_counts}`
- `Makefile` — extended `paper` target additions

Macro command names emitted into `coverage_counts.tex` (values verified live this session):
`\nsubmodules{30}`, `\ncallables{437}`, `\npubliccallables{409}`, `\nfdata{27}`, `\ncoverage{28}`, `\ndocpapers{47}`.

Plan 86-02 produces `.github/workflows/paper.yml` and consumes the committed `coverage_counts.tex` + `refs.bib` from this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: assert_coverage.py — derive macros, write coverage_counts.tex, --check drift gate (end-to-end)</name>
  <files>paper/code/assert_coverage.py, paper/coverage_counts.tex</files>
  <read_first>
    - scripts/generate_capability_dataset.py:507-528 (canonical `_coverage_counts()` — mirror the denominator `sum(len(v) for v in cap.values())` and the curated-numerator loop exactly)
    - .planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md (Pattern 1: full reference implementation of assert_coverage.py; the "Macro set for coverage_counts.tex" table with the six macro names and values)
    - paper/code/paper_utils.py (repo-root resolution convention: `Path(__file__).resolve().parent.parent.parent`)
  </read_first>
  <behavior>
    This is the tracer: it proves the whole "JSON map → derived integers → generated .tex → drift detection" path end-to-end on one script before the second generator is written.
    - Generate mode (no `--check`): writes paper/coverage_counts.tex containing exactly six `\newcommand` macros derived from the maps.
    - Derived values (verified live against the current maps this session): nsubmodules=30, ncallables=437, npubliccallables=409, nfdata=27, ncoverage=28, ndocpapers=47.
    - `--check` mode: regenerate content in memory, compare byte-for-byte against committed paper/coverage_counts.tex; exit 0 on match (print an OK line), exit 1 on mismatch OR when the file is absent (print a DRIFT line to stderr).
    - Counting semantics MUST mirror scripts/generate_capability_dataset.py:_coverage_counts(): denominator = sum(len(v) for v in cap.values()) (this INCLUDES the `_Fdata` key — do NOT subtract `__init__`); numerator = count of callable_index entries with at least one curated paper.
  </behavior>
  <action>
    Create paper/code/assert_coverage.py as a stdlib-only script (json, sys, pathlib; `from __future__ import annotations`; snake_case; NumPy-style module docstring per project conventions). Resolve the repo root as `Path(__file__).resolve().parent.parent.parent` and the two maps under `python/fdars/`; write output to `paper/coverage_counts.tex`.

    Implement `_derive_counts()` returning a dict with the six macro keys. Mirror the canonical counting from generate_capability_dataset.py:_coverage_counts() — denominator via `sum(len(v) for v in cap.values())`, and the curated-numerator via iterating `_references_map.json["callable_index"]` counting entries where any cite-key is in the frozenset of papers with `curated is True`. Compute nsubmodules from non-underscore top-level cap keys, npubliccallables as the summed lengths of those keys, nfdata from non-dunder methods of the `_Fdata` key, ndocpapers as papers whose key does not start with `_uncurated`.

    Implement `_render(counts)` emitting a leading provenance comment (two `%` lines naming the generator script and the source JSON files) followed by one `\newcommand{\<macro>}{<value>}` line per macro in a STABLE deterministic order (sort the dict keys) so re-runs are byte-identical. End the file with a single trailing newline.

    Implement `main()` with a `--check` branch (in `sys.argv`): in check mode, if the output file is absent OR its text differs from the freshly rendered content, print a `DRIFT:` message to stderr and `sys.exit(1)`; otherwise print an OK line and exit 0. In generate mode, write the file and print a confirmation. Guard with `if __name__ == "__main__": main()`.

    Then run the script in generate mode to write paper/coverage_counts.tex and commit that generated file in the same task so the `--check` gate has a committed baseline to compare against.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python paper/code/assert_coverage.py && grep -qE '\\newcommand\{\\ncallables\}\{437\}' paper/coverage_counts.tex && grep -qE '\\newcommand\{\\npubliccallables\}\{409\}' paper/coverage_counts.tex && grep -qE '\\newcommand\{\\nsubmodules\}\{30\}' paper/coverage_counts.tex && grep -qE '\\newcommand\{\\nfdata\}\{27\}' paper/coverage_counts.tex && grep -qE '\\newcommand\{\\ncoverage\}\{28\}' paper/coverage_counts.tex && grep -qE '\\newcommand\{\\ndocpapers\}\{47\}' paper/coverage_counts.tex && ! grep -vE '^\s*%' paper/paper.tex paper/sections/*.tex | grep -qE '\b(437|409|28|47)\b' && .venv/bin/python paper/code/assert_coverage.py --check && echo GEN_OK</automated>
    <fails_when>The script errors, any of the six macros is missing or has the wrong integer, a raw coverage integer (437/409/28/47) appears in an uncommented line of paper.tex or a section stub, OR `--check` exits non-zero against the just-generated committed file — GEN_OK is not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && cp paper/coverage_counts.tex /tmp/cc_backup.tex && printf '\n%% drift\n' >> paper/coverage_counts.tex && ( .venv/bin/python paper/code/assert_coverage.py --check ; test $? -ne 0 && echo DRIFT_DETECTED ) ; cp /tmp/cc_backup.tex paper/coverage_counts.tex && rm -f /tmp/cc_backup.tex</automated>
    <fails_when>`--check` returns exit 0 after coverage_counts.tex is deliberately mutated — DRIFT_DETECTED is not printed, meaning the tripwire does not fire (PIPE-03 negative test).</fails_when>
  </verify>
  <done>paper/code/assert_coverage.py exists; generate mode writes six correct `\newcommand` macros to paper/coverage_counts.tex; no raw coverage integer appears in any uncommented .tex line; `--check` exits 0 against the committed file and exits non-zero after a deliberate mutation. The generated coverage_counts.tex is committed.</done>
  <reversibility rating="reversible">Pure generator script + generated artifact; regenerable from the maps at any time.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gen_refs_bib.py — regenerate paper/refs.bib as 47 @misc entries, skipping uncurated</name>
  <files>paper/code/gen_refs_bib.py, paper/refs.bib</files>
  <read_first>
    - .planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md (Pattern 2: full reference implementation of gen_refs_bib.py; the @misc template; Pitfalls 3 and 4 — title double-bracing and empty-string DOI/URL handling)
    - python/fdars/_references_map.json (papers object: each paper has title, authors list of "Last, F." strings, year, type, doi (may be absent or ""), url (may be ""); 10 keys start with `_uncurated`)
    - paper/refs.bib (current stub — this task overwrites it)
  </read_first>
  <behavior>
    - Iterate the `papers` object of _references_map.json in sorted key order; skip any key starting with `_uncurated` (10 skipped); emit one `@misc` entry per remaining paper (47 emitted).
    - Cite-key IS the paper-key verbatim (no name-parsing transformation).
    - author = " and ".join(paper["authors"]); title double-braced (`title = {{...}}`) to preserve case under `\bibliographystyle{plain}`.
    - doi and url lines emitted only when the value is truthy (normalize with `paper.get("doi") or ""`); absent/empty values omit the line entirely (avoids the empty-field BibTeX warning).
    - A `note = {Type: <type>}` line carries the original type for human readers.
    - Leading provenance comment header naming the generator and source JSON.
  </behavior>
  <action>
    Create paper/code/gen_refs_bib.py as a stdlib-only script (json, sys, pathlib; `from __future__ import annotations`; snake_case; NumPy-style module docstring). Resolve the repo root and `python/fdars/_references_map.json` the same way as assert_coverage.py; write output to `paper/refs.bib`.

    Implement `_entry(key, paper)` producing a `@misc{<key>, ...}` block per the research Pattern 2 template: author joined with " and ", double-braced title, year, conditional doi line, conditional url line, and a `note = {Type: <type>}` line. Escape any literal braces in the title value before double-bracing so BibTeX stays valid.

    Implement `main()` that loads the map, iterates `sorted(papers)`, skips keys starting with `_uncurated`, collects `_entry(...)` blocks, prepends the provenance header, joins entries with a blank line, ends with a single trailing newline, and writes paper/refs.bib. Print a confirmation with the emitted-count and skipped-count. Guard with `if __name__ == "__main__": main()`.

    Run the script to overwrite the Phase 85 stub refs.bib and commit the regenerated file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python paper/code/gen_refs_bib.py && test "$(grep -cE '^@misc\{' paper/refs.bib)" -eq 47 && ! grep -qE '@misc\{_uncurated' paper/refs.bib && ! grep -qE '@(article|book|inproceedings|incollection)\{' paper/refs.bib && grep -qE 'title\s*=\s*\{\{' paper/refs.bib && ! grep -qE '^\s*doi\s*=\s*\{\},' paper/refs.bib && ! grep -qE '^\s*url\s*=\s*\{\},' paper/refs.bib && echo REFS_OK</automated>
    <fails_when>refs.bib does not contain exactly 47 `@misc` entries, contains an `_uncurated` cite-key, contains any non-@misc entry type, has an unbraced (single-brace) title, OR emits an empty `doi = {},`/`url = {},` line — REFS_OK is not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python paper/code/gen_refs_bib.py && git diff --exit-code paper/refs.bib && echo REFS_DETERMINISTIC</automated>
    <fails_when>A second run of gen_refs_bib.py produces a non-empty git diff against the committed refs.bib — the generator is non-deterministic (REFS_DETERMINISTIC not printed). (Run after committing refs.bib.)</fails_when>
  </verify>
  <done>paper/code/gen_refs_bib.py exists; paper/refs.bib contains exactly 47 `@misc` entries with double-braced titles and no `_uncurated` keys, no empty doi/url lines, and no non-@misc types; a re-run leaves an empty git diff. The regenerated refs.bib is committed.</done>
  <reversibility rating="reversible">Generator + generated bib artifact; regenerable from the map.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Consume macros in paper.tex + extend make paper to regenerate everything</name>
  <files>paper/paper.tex, Makefile</files>
  <read_first>
    - paper/paper.tex:1-30 (preamble ends at `\begin{document}` line 23; `\maketitle` at line 24; the seven `\input{sections/...}` block starts line 30 — add `\input{coverage_counts}` in the preamble before `\begin{document}`)
    - Makefile:35-45 (existing `paper` / `paper-figures` targets, `PAPER_PYTHONPATH := scripts:paper/code`)
    - .planning/phases/86-single-source-of-truth-wiring-ci-gate/86-RESEARCH.md (Pitfall 2: paper.tex must `\input{coverage_counts}` or macros are undefined; Pitfall 5: tectonic resolves `\input`/`\bibliography` relative to paper/ so both generated files must live in paper/)
  </read_first>
  <action>
    Edit paper/paper.tex to add `\input{coverage_counts}` in the preamble, after the `\usepackage{...}` block and before `\begin{document}` (so the `\newcommand` macros are defined document-wide). Do NOT add any literal coverage integer — only the `\input` line.

    Extend the Makefile so the one-command `make paper` pipeline regenerates ALL outputs. Add two phony targets — `paper-coverage` (runs `PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/assert_coverage.py`, i.e. generate mode) and `paper-refs` (runs `PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/gen_refs_bib.py`) — add both to the existing `.PHONY` line, and make the `paper` target depend on `paper-figures paper-coverage paper-refs`. Keep the plain `python` convention (user activates .venv first). Do NOT add any tectonic/pdflatex invocation — PDF compile stays CI-only (GATE-01, Plan 86-02).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qE '\\input\{coverage_counts\}' paper/paper.tex && grep -nE '\\input\{coverage_counts\}|\\begin\{document\}' paper/paper.tex | awk -F: 'NR==1{a=$1} /begin\{document\}/{b=$1} END{exit !(a<b)}' && grep -vE '^\s*#' Makefile | grep -qE '^\.PHONY:.*\bpaper-coverage\b' && grep -vE '^\s*#' Makefile | grep -qE '^\.PHONY:.*\bpaper-refs\b' && grep -vE '^\s*#' Makefile | grep -qE '^paper:.*paper-figures.*paper-coverage.*paper-refs' && grep -vE '^\s*#' Makefile | grep -qE 'assert_coverage\.py' && grep -vE '^\s*#' Makefile | grep -qE 'gen_refs_bib\.py' && ! grep -vE '^\s*#' Makefile | grep -qiE 'tectonic|pdflatex' && echo WIRED_OK</automated>
    <fails_when>`\input{coverage_counts}` is missing or placed after `\begin{document}`, the Makefile `paper` target does not depend on all three regeneration targets, `paper-coverage`/`paper-refs` are not phony, the script invocations are absent, OR a tectonic/pdflatex call leaked into the Makefile — WIRED_OK is not printed.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts:paper/code .venv/bin/python paper/code/gen_figures.py && .venv/bin/python paper/code/assert_coverage.py && .venv/bin/python paper/code/gen_refs_bib.py && git diff --exit-code paper/coverage_counts.tex paper/refs.bib paper/figures/ && echo PIPELINE_DETERMINISTIC</automated>
    <fails_when>Re-running the full pipeline (figures + coverage + refs) after the outputs are committed produces a non-empty git diff on any generated artifact — PIPELINE_DETERMINISTIC is not printed. (Run this check after all three artifacts are committed.)</fails_when>
  </verify>
  <done>paper/paper.tex contains `\input{coverage_counts}` in the preamble before `\begin{document}` and no literal coverage integer; `make paper` depends on `paper-figures paper-coverage paper-refs` with the two new phony targets invoking the generators and no tectonic/pdflatex call; a full pipeline re-run leaves an empty git diff on all generated artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON maps → generator scripts | The scripts read `python/fdars/_capability_map.json` and `_references_map.json`; these are committed repo artifacts with a known schema, not user-supplied input. |
| generated .tex/.bib → manuscript | `coverage_counts.tex` and `refs.bib` are consumed by `paper.tex`; a malformed generated file would surface as a tectonic compile error, not a silent corruption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-01 | Tampering | hardcoded coverage integer in a .tex file | high | mitigate | assert_coverage.py `--check` gate + verify negative-greps raw coverage integers out of paper.tex/section stubs; drift fails CI (Plan 86-02) |
| T-86-02 | Tampering | `_uncurated_*` placeholder leaking into refs.bib | medium | mitigate | gen_refs_bib.py skips `_uncurated`-prefixed keys; verify asserts exactly 47 @misc entries and no `_uncurated` cite-key |
| T-86-03 | Information Disclosure | empty `doi = {},`/`url = {},` BibTeX warnings revealing malformed entries | low | mitigate | conditional emission (`paper.get("doi") or ""`); verify negative-greps empty doi/url lines |
| T-86-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — both scripts are stdlib-only (json, sys, pathlib); no legitimacy gate required |
</threat_model>

<verification>
- All three tasks' `<automated>` gates pass (GEN_OK, DRIFT_DETECTED, REFS_OK, REFS_DETERMINISTIC, WIRED_OK, PIPELINE_DETERMINISTIC).
- No coverage integer (437/409/28/47/30/27) appears in any uncommented line of paper.tex or any section stub.
- `assert_coverage.py --check` exits 0 against committed output and non-zero after a deliberate mutation.
- `paper/refs.bib` has exactly 47 `@misc` entries, zero `_uncurated` keys.
- `make paper` regenerates figures + coverage + refs deterministically (empty git diff on re-run).
</verification>

<success_criteria>
- PIPE-03: coverage counts machine-derived into `coverage_counts.tex`, consumed via `\input`, `--check` drift gate proven by negative test — no hardcoded integer in any .tex.
- PIPE-04: `refs.bib` machine-generated from `_references_map.json` (47 @misc, cite-key = paper-key, uncurated skipped).
- One-command `make paper` regenerates every pipeline output.
</success_criteria>

<output>
Create `.planning/phases/86-single-source-of-truth-wiring-ci-gate/86-01-SUMMARY.md` when done
</output>
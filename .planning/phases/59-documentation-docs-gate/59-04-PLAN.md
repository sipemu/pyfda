---
phase: 59-documentation-docs-gate
plan: 04
type: execute
wave: 4
depends_on: [59-01, 59-02, 59-03]
files_modified:
  - Cargo.toml
  - pyproject.toml
  - README.md
autonomous: false
requirements: [DOCS-02, DOCS-03, REL-01]
estimate:
  tokens: 45000
  raw_tokens: 24000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "Whole-site `.venv/bin/mkdocs build --strict` runs GREEN offline — the phase docs gate (DOCS-02)."
    - "A human validated the rendered data-flow diagram on the built site for method accuracy before close (DOCS-03)."
    - "Package version is bumped 0.8.0 → 0.9.0 in Cargo.toml + pyproject.toml and the [sklearn] extra is documented (REL-01)."
  artifacts:
    - "Cargo.toml (version = 0.9.0)"
    - "pyproject.toml (version = 0.9.0)"
  key_links:
    - "whole-site strict build ↔ all Plan 01–03 pages (index, 3 reference pages, coverage, gridsearch example, diagram embed) resolve with zero broken links"
    - "human diagram approval gate ↔ docs/assets/diagrams/sklearn-pipeline-dataflow.svg rendered on the built site"
---

<objective>
Close the phase: run the whole-site `--strict` build as the docs gate (one slow run near the end), halt for a BLOCKING human diagram method-accuracy review, then bump the package version 0.8.0 → 0.9.0 and document the `[sklearn]` extra.

Purpose: Prove the entire site (all new + existing pages) builds green offline with no broken links or fence failures, get human sign-off on the rendered diagram (the standing method-accuracy rule), and perform the one-way-ish version bump under a reversibility gate at the very end (DOCS-02, DOCS-03, REL-01).
Output: green whole-site strict build, human diagram approval, version bump, documented extra.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

DOCS phase, SEQUENTIAL on `main`, no worktrees. The whole-site build with executed fences is ~19–25 min — budget for it; run it ONCE here, not per page.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
@docs/assets/diagrams/STYLE_SPEC.md   # confirm final SVG conformance criteria for the human reviewer's checklist
@Cargo.toml                           # version = 0.8.0 (line ~3) → 0.9.0
@pyproject.toml                       # version = 0.8.0 (line ~7); [project.optional-dependencies] sklearn = [...] (line ~53)
@README.md                            # document the [sklearn] extra alongside existing extras (advisor/mcp)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-site --strict build gate (offline)</name>
  <files>(no source files — validation gate)</files>
  <precondition>All Plan 01–03 pages exist and their nav entries are wired; the main-tree `.venv` has fdars[sklearn] importable. Run from repo root on `main` (not a worktree — fences hardcode the main-tree `.venv/bin/mkdocs`).</precondition>
  <action>
Run the full offline whole-site strict build (NO `DOCS_FAST` — execute every fence) and confirm it exits 0 with no broken internal links and every fence (including the new Pipeline + GridSearchCV fences) executing. This is the slow gate (~19–25 min); run it exactly once here. If it fails, fix the offending page/fence/link (e.g. the index.md forward link to coverage.md now resolves since Plan 02 added the page) and re-run. Capture the build log to the scratchpad.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/mkdocs build --strict --site-dir /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-full 2>&1 | tee /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/build59-full.log; ec=${PIPESTATUS[0]}; test "$ec" -eq 0 && grep -q "FDARS_FENCE_OK" /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-full/sklearn/index.html && grep -q "FDARS_FENCE_OK" /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-full/sklearn/gridsearch-example/index.html && echo WHOLE_SITE_GREEN</automated>
  </verify>
  <done>`.venv/bin/mkdocs build --strict` exits 0 offline over the whole site; both new fences render FDARS_FENCE_OK; no broken internal links.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING human diagram method-accuracy review</name>
  <what-built>
The whole-site strict build is green and the hand-authored `docs/assets/diagrams/sklearn-pipeline-dataflow.svg` is rendered on the built site (embedded on `docs/sklearn/index.md`). Before this checkpoint, generate a viewable rendering: `rsvg-convert docs/assets/diagrams/sklearn-pipeline-dataflow.svg -o /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/dataflow.png` (or serve the built site) so the reviewer sees the RENDERED diagram, not raw XML.
  </what-built>
  <how-to-verify>
Review the rendered data-flow diagram against this method-accuracy checklist (this is the standing method-accuracy gate — the phase does not close without sign-off):
  1. Data flow is METHOD-ACCURATE: `(n_obs, n_points)` ndarray → transformer(s) → `(n_obs, n_components)` FPC scores → predictor, where the predictor consumes the SCORE matrix (not raw curves), inside a sklearn Pipeline frame.
  2. No invented/incorrect method behavior; every label matches what `fdars.sklearn` actually does.
  3. STYLE_SPEC conformance is visually correct: title/subtitle placement, palette, neutral (`#f8f9fa`/`#ced4da`) vs orange method panel (`#fff4ea`/`#fd7e14`) usage, arrow weights.
  4. The diagram reads clearly at rendered size.
On requested changes, revise the SVG (re-run the Plan 03 SVGO idempotence check) and re-present. Do NOT proceed until approved.
  </how-to-verify>
  <resume-signal>Type "approved" to close the diagram gate, or describe the required diagram changes.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Confirm the one-way version bump to 0.9.0</name>
  <decision>Proceed with bumping the package version 0.8.0 → 0.9.0 in Cargo.toml + pyproject.toml?</decision>
  <context>
The version bump is effectively one-way: it is the release version for v9.0, and a later semver `vX.Y.Z` tag (the user's action at ship) triggers the PyPI publish workflow. This decision gate confirms the docs gate is green and the diagram is human-approved before walking through the one-way door. No tag is created by this phase.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the 0.9.0 bump</name>
      <pros>Docs gate green + diagram approved; the milestone's docs deliverables are complete and the package is ready to version.</pros>
      <cons>Version string is the release identity; correcting it post-tag would require a new patch release.</cons>
    </option>
    <option id="hold">
      <name>Hold the bump</name>
      <pros>Defer if any docs/diagram concern remains open.</pros>
      <cons>Phase does not close; REL-01 remains unmet.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Version bump 0.8.0 → 0.9.0</name>
  <files>Cargo.toml, pyproject.toml</files>
  <reversibility rating="one-way">Version bump + subsequent tag drives PyPI publish; bumping is effectively one-way once released, so it is gated behind the Task 3 decision checkpoint and the human diagram approval, and runs last.</reversibility>
  <action>
Bump the package version from `0.8.0` to `0.9.0` in both `Cargo.toml` (`version = "0.8.0"` near line 3) and `pyproject.toml` (`version = "0.8.0"` near line 7) using scoped Edits — change ONLY the package version line in each (do not touch dependency version pins like `scikit-learn>=1.3`). Do NOT create a git tag — tagging (which triggers the PyPI publish workflow) is the user's action at ship, out of scope for this phase.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import re,sys; c=open('Cargo.toml').read(); p=open('pyproject.toml').read(); cv=re.search(r'^version\s*=\s*\"([^\"]+)\"', c, re.M).group(1); pv=re.search(r'^version\s*=\s*\"([^\"]+)\"', p, re.M).group(1); sys.exit(0 if cv=='0.9.0' and pv=='0.9.0' else (print('Cargo',cv,'pyproject',pv) or 1))"</automated>
  </verify>
  <done>Cargo.toml and pyproject.toml both declare version 0.9.0; no dependency pins changed; no git tag created.</done>
</task>

<task type="auto">
  <name>Task 5: Document the [sklearn] extra in packaging</name>
  <files>README.md</files>
  <action>
Document the `[sklearn]` optional extra in `README.md` alongside the existing extras (advisor/mcp), matching the existing install-extras prose style: `pip install "fdars[sklearn]"` installs scikit-learn (`>=1.3,<1.7` on Python 3.9, `>=1.3` on 3.10+) and enables the `fdars.sklearn` estimator layer; the base package imports with zero sklearn installed. Add a one-line pointer to the new docs section (`docs/sklearn/index.md`). Keep it factual — the `[sklearn]` extra already exists in `pyproject.toml`; this only documents it.
  </action>
  <verify>
    <automated>grep -q 'fdars\[sklearn\]' README.md && echo SKLEARN_EXTRA_DOCUMENTED</automated>
  </verify>
  <done>README.md documents the `[sklearn]` extra with its install command and a pointer to the sklearn docs section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version bump → PyPI publish | a later semver tag (user's action) triggers the publish workflow; this phase only edits the version string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-04 | Tampering | version-string edit | low | mitigate | Scoped Edit to the package version line only (not dependency pins); no tag created here (publish is user-gated at ship); reversibility-gated behind human diagram approval. |
| T-59-SC | Tampering | npm/pip installs | low | accept | No package installs (docs/packaging edits only over the existing `.venv`); no legitimacy gate needed. |
</threat_model>

<verification>
- Whole-site `.venv/bin/mkdocs build --strict` exits 0 offline; both new fences render FDARS_FENCE_OK.
- Human explicitly approves the rendered data-flow diagram as method-accurate.
- Cargo.toml and pyproject.toml both read version 0.9.0 (no dependency pin changes).
- README.md documents the `[sklearn]` extra.
</verification>

<success_criteria>
The whole-site strict build is green offline, a human has approved the rendered diagram as method-accurate, the package version is 0.9.0 in both manifests, and the [sklearn] extra is documented.
</success_criteria>

<output>
Create `.planning/phases/59-documentation-docs-gate/59-04-SUMMARY.md` when done.
</output>

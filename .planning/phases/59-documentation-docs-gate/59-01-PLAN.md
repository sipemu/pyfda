---
phase: 59-documentation-docs-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sklearn/index.md
  - mkdocs.yml
autonomous: true
requirements: [DOCS-01, DOCS-02]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The docs site has a top-level 'scikit-learn API' nav section whose first page is docs/sklearn/index.md (DOCS-01)."
    - "docs/sklearn/index.md builds green with an offline live fence emitting FDARS_FENCE_OK (DOCS-02)."
  artifacts:
    - "docs/sklearn/index.md"
    - "mkdocs.yml (nav extended with the scikit-learn API section)"
  key_links:
    - "mkdocs.yml nav entry 'scikit-learn API' → docs/sklearn/index.md (page reachable in the built nav)"
    - "docs/sklearn/index.md live fence → scripts/docs_fig + fdars.sklearn._skeletons (executes offline against the main-tree .venv)"
---

<objective>
Establish the "scikit-learn API" docs section: create the concept/overview page `docs/sklearn/index.md`, wire it into `mkdocs.yml` nav as a new top-level section, and prove the docs "tracer" — the index page plus ONE offline `FDARS_FENCE_OK` Pipeline worked fence — builds green under a scoped `mkdocs build --strict`.

Purpose: Prove the end-to-end docs path (nav → new page → offline live fence → strict build) on the thinnest possible slice before authoring the remaining reference pages, examples, and diagram. If the fence mechanism or nav wiring is wrong, catch it here on one page, not after authoring the whole section (DOCS-01, DOCS-02).
Output: `docs/sklearn/index.md` (concept + one Pipeline fence), extended `mkdocs.yml` nav.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

This is a DOCS phase running SEQUENTIALLY on `main`, NOT in worktrees (standing v6.0 rule: doc-build fences hardcode the main-tree `.venv/bin/mkdocs` path; `use_worktrees: false`). Run all build commands from the repo root against the main-tree `.venv`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
@docs/advisor/index.md                     # structural precedent: concept/overview page shape + fdars-section-hero div idiom
@docs/represent/interpolation.md           # FDARS_FENCE_OK fence idiom: exec="1" html="1", from docs_fig import fig,render, print("... FDARS_FENCE_OK")
@tests/sklearn/test_predictive_pipeline.py # small-dataset Pipeline([Imputer, BSplineSmoother, FPCATransformer, FPCLDAClassifier]) pattern to mirror in the fence
@mkdocs.yml                                 # nav to extend (add section after "AI Advisor", before "Examples")
@python/fdars/sklearn/_coverage.py         # TRIAGE_VERDICTS (28 estimators) — names to reference in the overview
</read_first>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Concept/overview page docs/sklearn/index.md with one offline Pipeline fence</name>
  <files>docs/sklearn/index.md</files>
  <precondition>The main-tree `.venv` has `fdars` importable with the `[sklearn]` extra (scikit-learn 1.8) installed — verify with `.venv/bin/python -c "import sklearn, fdars.sklearn._skeletons"` before authoring the fence.</precondition>
  <action>
Create `docs/sklearn/index.md` as the concept/overview page for the fdars scikit-learn API layer, mirroring the shape of `docs/advisor/index.md` (open with a `<div class="fdars-section-hero" markdown>` intro paragraph). Cover, in prose:
  - What the layer is: `fdars.sklearn` wraps functional-data methods as standard scikit-learn estimators (fit/transform/predict) so they compose in `Pipeline`/`GridSearchCV`/`cross_val_score`.
  - The plain-ndarray contract: estimators take `(n_obs, n_points)` ndarrays with `argvals` as a constructor param (default `np.arange(n_features)`); they call `fdars._native.*` directly and NEVER construct an `Fdata` internally.
  - The `[sklearn]` install: `pip install "fdars[sklearn]"` (scikit-learn optional extra; base package imports with zero sklearn installed; `fdars.sklearn` gates in its own `__init__.py` like `advisor`/`mcp`). Use an `!!! info "Optional extra"` admonition matching the advisor page style.
  - The full-`check_estimator`-compliance guarantee: every wrapped estimator passes the full battery with ZERO exemptions; methods that cannot pass are EXCLUDED (stay in the functional API), never exempted. Cross-reference the coverage page (forward link `[coverage / EXCLUDE list](coverage.md)` — the page arrives in Plan 02; the link may 404 in this plan's scoped build and is resolved by the whole-site gate in Plan 04).
  - The five families at a glance (transformers, regressors, classifiers, clusterers, outlier detectors) with a one-line "how it fits into a Pipeline" statement.
Then add ONE offline worked fence (the tracer) using the interpolation.md idiom: a python fence with `exec="1" html="1" source="above"`, importing `from docs_fig import fig, render` and building a SMALL synthetic dataset inline (reuse the shape from `tests/sklearn/test_predictive_pipeline.py`: ~40 obs × 20 points, two Gaussian-shifted classes, fixed `np.random.default_rng(7)`). Build `Pipeline([("imputer", Imputer()), ("smoother", BSplineSmoother()), ("fpca", FPCATransformer()), ("clf", FPCLDAClassifier())])` from `fdars.sklearn._skeletons`, `fit` then `predict`, plot something illustrative (e.g. the FPC scores colored by predicted class via an intermediate `FPCATransformer().fit_transform`), and end the fence body with a line printing the sentinel: `print("Predicted labels:", set(y_pred.tolist()), " FDARS_FENCE_OK")`. Keep the dataset small and the fence fully OFFLINE (no network, no LLM). Do NOT put a bare ```python``` fence for the sentinel string anywhere in prose that would be scanned — the sentinel appears only inside the executable fence's `print(...)`.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict --site-dir /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-01 2>&1 | tee /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/build59-01.log; grep -q "FDARS_FENCE_OK" /tmp/claude-1000/-home-simonm-projects-rust-pyfda/scratchpad/site59-01/sklearn/index.html && echo TRACER_OK</automated>
  </verify>
  <done>docs/sklearn/index.md renders the Pipeline fence to HTML containing FDARS_FENCE_OK; the scoped strict build exits 0 (note: a forward link to coverage.md may warn until Plan 02 adds that page and Plan 04 runs the whole-site gate).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the scikit-learn API section into mkdocs.yml nav</name>
  <files>mkdocs.yml</files>
  <action>
Edit the `nav:` block in `mkdocs.yml` to add a new top-level section named `scikit-learn API`, inserted after the `AI Advisor` section and before the `Examples` section (mirroring the advisor nav idiom). Add only the index entry in this plan:
```
  - scikit-learn API:
    - sklearn/index.md
```
(Per-family reference pages and the coverage page are added to this same section in Plan 02; the GridSearchCV example page in Plan 03. Leave placeholders out — nav entries are added when their pages are authored so `--strict` never references a missing file.)
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml,sys; nav=yaml.safe_load(open('mkdocs.yml'))['nav']; sec=[s for s in nav if isinstance(s,dict) and 'scikit-learn API' in s]; sys.exit(0 if sec and any('sklearn/index.md'==v for v in sec[0]['scikit-learn API']) else 1)"</automated>
  </verify>
  <done>mkdocs.yml nav contains a 'scikit-learn API' section listing sklearn/index.md; the YAML parses and the section resolves to the new page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → live fence execution | markdown-exec runs fence Python at build time against the main-tree `.venv` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-01 | Tampering | live-fence code executed at build | low | accept | Fences are offline, no network/LLM, run only trusted first-party code against local `.venv`; no external input crosses the boundary. Docs site has no runtime attack surface. |
| T-59-SC | Tampering | npm/pip installs | low | accept | No new package installs in this phase (docs-only over the existing `.venv`); no legitimacy gate needed. |
</threat_model>

<verification>
- Scoped strict build (`DOCS_FAST=1`, one page) exits 0 and the rendered `sklearn/index.html` contains `FDARS_FENCE_OK`.
- `mkdocs.yml` parses as YAML and exposes the `scikit-learn API` section → `sklearn/index.md`.
- The fence runs OFFLINE (no network access during build).
</verification>

<success_criteria>
docs/sklearn/index.md exists, is in mkdocs.yml nav under a 'scikit-learn API' section, and its Pipeline fence executes offline emitting FDARS_FENCE_OK under a strict build.
</success_criteria>

<output>
Create `.planning/phases/59-documentation-docs-gate/59-01-SUMMARY.md` when done.
</output>

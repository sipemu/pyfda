---
phase: 59-documentation-docs-gate
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - docs/sklearn/transformers.md
  - docs/sklearn/regressors-classifiers.md
  - docs/sklearn/clusterers-outliers.md
  - docs/sklearn/coverage.md
  - mkdocs.yml
autonomous: true
requirements: [DOCS-01]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Per-family reference pages document all 28 wrapped estimators with their fdars source + key params (DOCS-01)."
    - "A coverage/EXCLUDE page lists all 28 PASS estimators and every EXCLUDED_METHODS entry with reason + 'still available in the functional API', derived from _coverage.py so it cannot drift (DOCS-01)."
    - "The outlier-detector docs honestly state MagnitudeShapeDetector is MS-plot-faithful while the other five rank by a subset-invariant modified-band-depth surrogate (method-accuracy honesty)."
  artifacts:
    - "docs/sklearn/transformers.md"
    - "docs/sklearn/regressors-classifiers.md"
    - "docs/sklearn/clusterers-outliers.md"
    - "docs/sklearn/coverage.md"
    - "mkdocs.yml (nav extended with the four reference/coverage pages)"
  key_links:
    - "docs/sklearn/coverage.md content ↔ python/fdars/sklearn/_coverage.py (TRIAGE_VERDICTS 28×PASS + EXCLUDED_METHODS) — every listed estimator/exclusion traces to the registry"
    - "mkdocs.yml nav 'scikit-learn API' section → the four new pages"
---

<objective>
Author the per-family reference pages (transformers; regressors & classifiers; clusterers & outlier detectors) and the coverage/EXCLUDE page, and wire all four into the `scikit-learn API` nav section. The coverage page is derived from `python/fdars/sklearn/_coverage.py` (TRIAGE_VERDICTS + EXCLUDED_METHODS) so it stays truthful. The outlier section states the method-accuracy honesty caveat.

Purpose: Deliver the DOCS-01 reference surface — the wrapped-estimator catalog + the published coverage/EXCLUDE list — building on the section skeleton proven green in Plan 01.
Output: four new pages under `docs/sklearn/`, extended `mkdocs.yml` nav.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

DOCS phase, SEQUENTIAL on `main`, no worktrees. Build from repo root against the main-tree `.venv`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
@python/fdars/sklearn/_coverage.py   # SOURCE OF TRUTH: TRIAGE_VERDICTS (28 estimators, all PASS) + EXCLUDED_METHODS (reason-coded); reason-code definitions in the module docstring
@.planning/phases/59-documentation-docs-gate/59-CONTEXT.md   # method-accuracy honesty section (outlier surrogate; classifier/regressor reconstruction notes)
@docs/advisor/aspects.md             # precedent: per-topic reference page with tables (family, source, params)
@docs/sklearn/index.md               # the concept page from Plan 01 — match tone/structure; resolve its forward link to coverage.md
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-family reference pages (transformers; regressors & classifiers; clusterers & outliers)</name>
  <files>docs/sklearn/transformers.md, docs/sklearn/regressors-classifiers.md, docs/sklearn/clusterers-outliers.md</files>
  <action>
Author three reference pages, each opening with a one-paragraph intro and a table of the wrapped estimators for that family (columns: estimator class, sklearn mixin, fdars source, key constructor params). Draw the exact estimator names and family groupings from `TRIAGE_VERDICTS` in `_coverage.py` (the comment banners group them by family):
  - `docs/sklearn/transformers.md` — the 8 transformers: FPCATransformer, BSplineSmoother, LocalPolynomialSmoother, BasisRepresentation, Imputer, SplineInterpolator, DepthTransformer, NormTransformer (all `TransformerMixin`). State FPCATransformer is the hub: `(n_obs, n_points) → (n_obs, n_components)` scores with SVD sign canonicalization (idempotent fit).
  - `docs/sklearn/regressors-classifiers.md` — 5 regressors (FPCRegressor, PLSRegressor, RobustFPCRegressor, GLMRegressor, NonparametricRegressor; `RegressorMixin`) + 7 classifiers (FPCLDAClassifier, FPCQDAClassifier, FPCKNNClassifier, DDClassifier, LogisticFPCClassifier, ElasticMultinomialClassifier; `ClassifierMixin`). Add the reconstruction notes from 59-CONTEXT.md where relevant: LDA/QDA/KNN/DD/ElasticMultinomial predict via stored FPC scores; GLMRegressor = Gaussian FPC-OLS (not beta_t trapezoidal). Do NOT overclaim exactness.
  - `docs/sklearn/clusterers-outliers.md` — 3 clusterers (FunctionalKMeans, FuzzyFunctionalCMeans, FunctionalGMM; `ClusterMixin`) + 6 outlier detectors (LRTOutlierDetector, OutliergramDetector, MagnitudeShapeDetector, TVDMSSDetector, MUODDetector, DepthgramDetector; `OutlierMixin`). Include the METHOD-ACCURACY HONESTY caveat as an `!!! warning` admonition: MagnitudeShapeDetector uses a method-faithful MS-plot score (magnitude/shape outlyingness); the other five (LRT, outliergram, TVDMSS, MUOD, depthgram) rank by a subset-invariant modified-band-depth SURROGATE in the sklearn layer, with their true batch-relative methods remaining available in `fdars.outliers`. Do not overclaim.
Cross-link each page to the coverage page (`[coverage / EXCLUDE list](coverage.md)`). No live fences required on these pages (reference tables + prose only) to keep the whole-site build fast — worked fences live on index.md (Plan 01) and the GridSearchCV example (Plan 03).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import re,sys; names=set(); [names.update(re.findall(r'\`?([A-Z][A-Za-z]+(?:Transformer|Smoother|Representation|Imputer|Interpolator|Regressor|Classifier|KMeans|CMeans|GMM|Detector))\`?', open(p).read())) for p in ['docs/sklearn/transformers.md','docs/sklearn/regressors-classifiers.md','docs/sklearn/clusterers-outliers.md']]; from fdars.sklearn._coverage import TRIAGE_VERDICTS; missing=set(TRIAGE_VERDICTS)-names; sys.exit(0 if not missing else (print('MISSING',missing) or 1))"</automated>
  </verify>
  <done>All 28 TRIAGE_VERDICTS estimator names appear across the three reference pages; the clusterers-outliers page contains the method-accuracy honesty warning distinguishing MagnitudeShapeDetector from the five surrogate-scored detectors.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage/EXCLUDE page derived from _coverage.py</name>
  <files>docs/sklearn/coverage.md</files>
  <action>
Author `docs/sklearn/coverage.md` as the published coverage/EXCLUDE list. Two tables, both traceable to `_coverage.py`:
  1. WRAPPED (28 estimators): one row per `TRIAGE_VERDICTS` key — columns: estimator class, family, sklearn mixin, fdars source, verdict (all PASS). State up front: "all 28 wrapped estimators pass the full check_estimator battery with zero exemptions."
  2. EXCLUDED: one row per `EXCLUDED_METHODS` key — columns: fdars method, reason code (ORDER_SENSITIVE / IRREGULAR_INPUT / RESPONSE_DOMAIN / NON_STANDARD_INPUT / NON_STANDARD_OUTPUT / HYPERPARAMETER_SEARCH / NOT_AN_ESTIMATOR / SEQUENTIAL_STREAMING), plain-language reason (paraphrase the reason-code docstring definitions), and "still available in the functional API" pointing at the `functional_api` field (e.g. `fdars.spm.spm_monitor`). Cover every EXCLUDED_METHODS entry: alignment (elastic_align_pair, karcher_mean), pace_fpca, non-Gaussian GLM (binomial, poisson), concurrent_regression, fosr, cluster_optim, the four inference tests, spm_monitor.
Add a note that this list is derived from `python/fdars/sklearn/_coverage.py` and cannot drift from the shipped registry. Do NOT hardcode counts that could go stale beyond "28 wrapped" — describe the exclusion families by name.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys; from fdars.sklearn._coverage import EXCLUDED_METHODS; txt=open('docs/sklearn/coverage.md').read(); miss=[m for m in EXCLUDED_METHODS if EXCLUDED_METHODS[m]['functional_api'] not in txt]; sys.exit(0 if not miss else (print('MISSING functional_api refs:',miss) or 1))"</automated>
  </verify>
  <done>docs/sklearn/coverage.md lists all 28 wrapped estimators and every EXCLUDED_METHODS entry with its reason code and its functional_api pointer; every excluded method's functional-API path appears verbatim on the page.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the four pages into the scikit-learn API nav section</name>
  <files>mkdocs.yml</files>
  <action>
Extend the `scikit-learn API` nav section (created in Plan 01) to add the four new pages after `sklearn/index.md`, in reading order:
```
  - scikit-learn API:
    - sklearn/index.md
    - Transformers: sklearn/transformers.md
    - Regressors & Classifiers: sklearn/regressors-classifiers.md
    - Clusterers & Outlier Detectors: sklearn/clusterers-outliers.md
    - Coverage & Exclusions: sklearn/coverage.md
```
(The GridSearchCV example page is added to this section in Plan 03.) Use a scoped Edit on the existing section — do not rewrite the whole nav.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml,sys; nav=yaml.safe_load(open('mkdocs.yml'))['nav']; sec=[s for s in nav if isinstance(s,dict) and 'scikit-learn API' in s][0]['scikit-learn API']; got=set(v for d in sec for v in ([d.values()] if not isinstance(d,str) else [[d]]) for v in v) if False else set(); import itertools; flat=[list(d.values())[0] if isinstance(d,dict) else d for d in sec]; need={'sklearn/index.md','sklearn/transformers.md','sklearn/regressors-classifiers.md','sklearn/clusterers-outliers.md','sklearn/coverage.md'}; sys.exit(0 if need.issubset(set(flat)) else (print('nav flat',flat) or 1))"</automated>
  </verify>
  <done>The 'scikit-learn API' nav section lists index, the three reference pages, and coverage; YAML parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs source → published site | static reference prose/tables; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-02 | Information Disclosure | coverage/EXCLUDE page | low | mitigate | Derive content from `_coverage.py` so the published list matches the shipped registry (no drift); no secrets or credentials in docs. |
| T-59-SC | Tampering | npm/pip installs | low | accept | No package installs (docs-only); no legitimacy gate needed. |
</threat_model>

<verification>
- All 28 TRIAGE_VERDICTS names appear across the reference pages (automated).
- Every EXCLUDED_METHODS `functional_api` pointer appears on coverage.md (automated).
- Method-accuracy honesty warning present on the clusterers-outliers page.
- `mkdocs.yml` nav exposes all four pages under the scikit-learn API section.
</verification>

<success_criteria>
The scikit-learn API section documents all 28 wrapped estimators across three family reference pages plus a coverage/EXCLUDE page derived from _coverage.py, all wired into nav, with the outlier method-accuracy honesty caveat stated.
</success_criteria>

<output>
Create `.planning/phases/59-documentation-docs-gate/59-02-SUMMARY.md` when done.
</output>

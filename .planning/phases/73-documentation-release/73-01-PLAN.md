---
phase: 73-documentation-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/analyze/functional-time-series.md
  - docs/assets/diagrams/functional-time-series.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-01, DOCS-02]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The Functional Time Series page renders with a runnable offline fence printing FDARS_FENCE_OK (DOCS-01)."
    - "The fts concept diagram is STYLE_SPEC-conformant and SVGO-idempotent (DOCS-02)."
    - "The page is reachable from the Analyze nav section and DOCS_FAST build is green."
  artifacts:
    - docs/analyze/functional-time-series.md
    - docs/assets/diagrams/functional-time-series.svg
    - mkdocs.yml
  key_links:
    - "mkdocs.yml Analyze nav entry → docs/analyze/functional-time-series.md (broken link fails --strict)."
    - "Page image ref ../assets/diagrams/functional-time-series.svg → the committed SVG."
    - "Fence import fdars.fts.{ftsm,ftsm_forecast,stationarity_test} → shipped Phase 67 bindings."
---

<objective>
TRACER: author the Functional Time Series family page end-to-end — page + offline
FDARS_FENCE_OK fence (DOCS_FAST build green) + its STYLE_SPEC-conformant SVGO-idempotent
diagram + Analyze nav wiring — proving the whole page+fence+diagram+nav loop before the
remaining 6 families fan out.

Purpose: De-risk the docs authoring pipeline (fence execution env, SVG style/idempotence,
nav insertion) on ONE complete family before scaling. If this loop is wrong, catch it now.
Output: One documented capability family (fts), one diagram, one nav entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Author docs/analyze/functional-time-series.md with offline FDARS_FENCE_OK fence</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 1 fence mechanism + copy-pasteable page skeleton; Section 2 Family A fts template + fixture note)
    - docs/represent/depth-functions.md (worked-example page template — mirror its fence + structure exactly)
    - .planning/phases/67-functional-time-series-fts (SUMMARY files for the shipped fts API: ftsm/ftsm_forecast/stationarity_test signatures)
  </read_first>
  <action>
    Create docs/analyze/functional-time-series.md following the RESEARCH Section 1 page skeleton and the depth-functions.md template. Include: a 2-3 sentence intro; the diagram image reference `![Functional Time Series — concept diagram](../assets/diagrams/functional-time-series.svg){ .fdars-diagram }`; a method-concept section; and ONE `python exec="1" source="above"` fence using the RESEARCH Section 2 Family A minimal example verbatim as the basis (imports fdars.fts.{ftsm, ftsm_forecast, stationarity_test}, rng=default_rng(42), NON-SQUARE n=20 m=30 per the transposition-guard convention, n_perm=19 to keep runtime under 2s). The fence's final print MUST end with the FDARS_FENCE_OK marker. Add an API Reference section with a parameter table and a key table for the ftsm/forecast dict keys (mean, rotation, scores, fitted, weights, ncomp; forecast). Add a References section. Do NOT set DOCS_FAST-only behavior in the fence. Keep the fixture small (STATE build-time blocker: small fences).
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/fts_build.log; python scripts/check_docs_figures.py site</automated>
    <fails_when>The fence raises (traceback embedded in built HTML → check_docs_figures.py exits non-zero), OR the page is not yet in nav (this task creates the file; nav wiring is Task 3 — expect a mkdocs "not in nav" WARNING here, not an error, since DOCS_FAST build is not --strict).</fails_when>
  </verify>
  <acceptance_criteria>
    - docs/analyze/functional-time-series.md exists with a single exec fence importing shipped fdars.fts functions.
    - The rendered page in site/ contains the success marker string and no Python traceback.
    - check_docs_figures.py site exits 0 (no exec-error, no traceback-in-HTML).
    - Fixture is non-square (n != m).
  </acceptance_criteria>
  <done>The fts page builds under DOCS_FAST with the fence executing cleanly and emitting its success marker; check_docs_figures.py passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/assets/diagrams/functional-time-series.svg (hand-authored, STYLE_SPEC-conformant, SVGO-idempotent)</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (canonical style block, colour palette, stroke weights, viewBox rules, accessibility pattern, panel patterns)
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 3 STYLE_SPEC diagram rules + SVGO idempotence protocol; Section 6 diagram authoring workflow)
    - docs/assets/diagrams/clustering.svg (concrete 720×480 example to mirror structure)
  </read_first>
  <action>
    Hand-author docs/assets/diagrams/functional-time-series.svg depicting the fts method accurately: a functional-time-series forecast concept — a panel of observed functional curves (time-indexed), the FTSM decomposition (mean + basis/rotation), and the forecast horizon extrapolation. Use viewBox 720x300 or 720x480 as the layout needs. Copy the canonical five-class `<style>` block verbatim. Include role="img", a title-matching aria-label, and `<title>`/`<desc>` wired via aria-labelledby (ids prefixed fts-). Use the FDARS data-curve palette and structural colours/stroke weights from STYLE_SPEC. The diagram must be method-accurate: fts forecast = FTSM mean + basis extrapolation into the forecast horizon (not a generic "trend line"). Do NOT inline the SVG into the .md — it is a separate file referenced by Task 1's image tag.
  </action>
  <verify>
    <automated>FIRST=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/functional-time-series.svg --output -); SECOND=$(printf '%s' "$FIRST" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$FIRST") <(printf '%s' "$SECOND") && echo IDEMPOTENT_OK</automated>
    <fails_when>The second svgo pass differs from the first (diff non-empty) — the diagram is not stable under svgo.config.mjs and would be transformed further, OR the config flag/version pin is omitted so inlineStyles corrupts the CSS classes.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SVG has viewBox width 720, the canonical five-class style block, role="img", aria-label, and title/desc/aria-labelledby.
    - Two-pass svgo@3.3.4 with --config svgo.config.mjs produces zero diff (idempotent).
    - The diagram depicts FTSM forecast (mean + basis extrapolation), method-accurate.
  </acceptance_criteria>
  <done>The fts SVG is committed, STYLE_SPEC-conformant, and SVGO-idempotent (two-pass diff empty).</done>
</task>

<task type="auto">
  <name>Task 3: Wire the fts page into the Analyze nav and verify DOCS_FAST build is green</name>
  <read_first>
    - mkdocs.yml (Analyze nav section, lines ~138-150 — insert a new entry)
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Common Pitfall 8: create the .md file BEFORE the nav entry)
  </read_first>
  <action>
    Add a nav entry under the Analyze section in mkdocs.yml (slot into the existing section per the user's locked decision — NO new top-level groups): `- Functional Time Series: analyze/functional-time-series.md`. Place it near the other analyze entries (after analyze/index.md is fine). Do NOT create any new nav group.
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/fts_nav_build.log; python scripts/check_docs_figures.py site; grep -q 'functional-time-series.md' mkdocs.yml && echo NAV_WIRED_OK</automated>
    <fails_when>mkdocs reports the page is not in nav (WARNING gone means wired), OR the nav entry points at a nonexistent path, OR check_docs_figures.py finds a traceback. NAV_WIRED_OK absent means the grep failed.</fails_when>
  </verify>
  <acceptance_criteria>
    - mkdocs.yml Analyze section contains `analyze/functional-time-series.md`.
    - DOCS_FAST build no longer emits a "not in nav" warning for this page.
    - check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>The fts page is reachable from the Analyze nav; the DOCS_FAST build is green and the fence still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time fence → docs Python process | Fence code executes shipped fdars bindings at mkdocs build time; hardcoded fixtures only (no external input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering | fts fence output | low | mitigate | check_docs_figures.py scans built HTML for tracebacks; fixture is deterministic (seed=42) |
| T-73-02 | Denial of Service | --strict build time | low | accept | Small non-square fixture (n=20,m=30,n_perm=19) keeps fence < 2s per STATE build-time blocker |
</threat_model>

<verification>
- DOCS_FAST build green; check_docs_figures.py site exits 0.
- fts SVG passes two-pass svgo@3.3.4 idempotence (empty diff).
- mkdocs.yml Analyze nav references analyze/functional-time-series.md.
</verification>

<success_criteria>
The fts family is fully documented (page + fence + diagram + nav), proving the docs authoring
loop end-to-end for the remaining 6 families.
</success_criteria>

<output>
Create `.planning/phases/73-documentation-release/73-01-SUMMARY.md` when done
</output>

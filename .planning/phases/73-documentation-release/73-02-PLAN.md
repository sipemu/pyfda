---
phase: 73-documentation-release
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - docs/regression/function-on-function.md
  - docs/regression/additive-sof.md
  - docs/regression/frechet-regression.md
  - docs/assets/diagrams/function-on-function.svg
  - docs/assets/diagrams/additive-sof.svg
  - docs/assets/diagrams/frechet-regression.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-01, DOCS-02]
estimate:
  tokens: 72000
  raw_tokens: 40000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Three Regression pages (Function-on-Function, Additive/Generalized SoF, Fréchet Regression) each render with a runnable offline fence printing FDARS_FENCE_OK (DOCS-01)."
    - "Three STYLE_SPEC-conformant, SVGO-idempotent concept diagrams exist (DOCS-02)."
    - "All three pages are wired into the Regression nav section and DOCS_FAST build is green."
  artifacts:
    - docs/regression/function-on-function.md
    - docs/regression/additive-sof.md
    - docs/regression/frechet-regression.md
    - docs/assets/diagrams/function-on-function.svg
    - docs/assets/diagrams/additive-sof.svg
    - docs/assets/diagrams/frechet-regression.svg
  key_links:
    - "mkdocs.yml Regression nav entries → the three new regression/*.md pages."
    - "FoF fence → fdars.regression.fof_regression; SoF fence → fdars.scalar_on_function.fam; frechet fence → fdars.frechet.frechet_mean (naked array return, NOT a dict)."
---

<objective>
Author the three remaining Regression-section family pages (Function-on-Function, Additive/
Generalized Scalar-on-Function, Fréchet Regression), each with an offline FDARS_FENCE_OK
fence and a STYLE_SPEC-conformant SVGO-idempotent diagram, wired into the Regression nav.

Purpose: Complete DOCS-01/DOCS-02 coverage for the Regression-section families, reusing the
loop proven by the fts tracer (73-01).
Output: 3 pages, 3 diagrams, 3 nav entries under Regression.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/regression/function-on-function.md (FoF + FoF-RE) with offline fence</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 2 Family B FoF/SoF templates + fixture notes; note fof_regression EXCLUDES fpca_x/fpca_y keys; use non-square mx=20,my=15)
    - docs/represent/depth-functions.md (fence + page template to mirror)
    - .planning/phases/68-function-on-function-scalar-on-function-regression (SUMMARY files for fof_regression / fof_re_regression signatures and PyDict keys)
  </read_first>
  <action>
    Create docs/regression/function-on-function.md covering Function-on-Function regression (fof_regression + predict_fof) and, briefly, the random-effects variant (fof_re_regression). Follow the RESEARCH page skeleton. Include the diagram ref `![Function-on-Function Regression — concept diagram](../assets/diagrams/function-on-function.svg){ .fdars-diagram }`. Use the RESEARCH Section 2 Family B minimal FoF fence verbatim as the basis: imports fdars.regression.fof_regression, rng=default_rng(42), NON-SQUARE predictor/response grids (n=25, mx=20, my=15), ncomp_x=3 ncomp_y=3, final print ends with the FDARS_FENCE_OK marker. Add an API Reference table documenting the fof_regression dict keys (intercept, beta_surface, fitted, residuals, r_squared_t, r_squared, ncomp_x, ncomp_y, coef_matrix) and note fpca_x/fpca_y are intentionally excluded (per Phase 68-01 decision). Mention fof_re_regression + predict_fof_re in the API table (no separate live fence needed). Keep fixtures small.
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/fof_build.log; python scripts/check_docs_figures.py site</automated>
    <fails_when>The FoF fence raises (KeyError on an excluded key, or ImportError) → traceback in HTML → check_docs_figures.py exits non-zero. A "not in nav" WARNING is expected until Task 3 wires nav.</fails_when>
  </verify>
  <acceptance_criteria>
    - docs/regression/function-on-function.md exists with a single exec fence importing fdars.regression.fof_regression.
    - The fence uses non-square grids (mx != my) and prints the success marker.
    - check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>The FoF page builds under DOCS_FAST with the fence executing cleanly and emitting its success marker.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/regression/additive-sof.md and docs/regression/frechet-regression.md with offline fences</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 2 Family B SoF functions fam/fregre_gkam/fregre_gsam/variable_selection/model_selection_ncomp; Family C frechet — frechet_mean returns a NAKED array for SPD space, NOT a dict; SPD fixture must be genuinely positive-definite via A@A.T + I)
    - docs/represent/depth-functions.md (fence + page template)
    - .planning/phases/68-function-on-function-scalar-on-function-regression (SoF SUMMARYs) and .planning/phases/69-frechet-regression-density-fda (frechet SUMMARYs)
  </read_first>
  <action>
    Create TWO pages.
    (a) docs/regression/additive-sof.md — Additive/Generalized Scalar-on-Function regression. Diagram ref `![Additive Scalar-on-Function Regression — concept diagram](../assets/diagrams/additive-sof.svg){ .fdars-diagram }`. Fence imports fdars.scalar_on_function.fam (and mention fregre_gkam/fregre_gsam/variable_selection/model_selection_ncomp in the API table), small non-square fixture, final print ends with the FDARS_FENCE_OK marker. Document the fam 7-key result dict.
    (b) docs/regression/frechet-regression.md — Fréchet Regression. Diagram ref `![Fréchet Regression — concept diagram](../assets/diagrams/frechet-regression.svg){ .fdars-diagram }`. Fence uses the RESEARCH Family C SPD example verbatim as the basis: imports fdars.frechet.frechet_mean, builds a small list of genuinely-SPD (d,d) matrices via A@A.T + I, wraps the result in np.asarray (frechet_mean returns a NAKED array for SPD, not a dict — Pitfall 7), final print ends with the FDARS_FENCE_OK marker. Mention frechet_global_reg/frechet_local_reg/frechet_anova (dict returns) in the API table. Keep fixtures small.
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/sof_frechet_build.log; python scripts/check_docs_figures.py site</automated>
    <fails_when>Either fence raises — e.g. treating frechet_mean's return as a dict (Pitfall 7), or a non-SPD fixture failing the positive-diagonal/symmetry validation — producing a traceback in HTML that check_docs_figures.py catches.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both additive-sof.md and frechet-regression.md exist, each with one passing exec fence printing the success marker.
    - The frechet fence treats frechet_mean's return as a naked array (np.asarray), not a dict.
    - The SoF fence uses a non-square fixture.
    - check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>Both regression pages build under DOCS_FAST with fences executing cleanly and emitting their success markers.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 3 regression diagrams and wire all 3 pages into the Regression nav</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (canonical style block, palette, stroke weights, viewBox, accessibility, panel patterns)
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 3 STYLE_SPEC rules + SVGO idempotence protocol; method-accuracy notes: frechet = metric-space barycenter)
    - docs/assets/diagrams/clustering.svg (structural example); mkdocs.yml (Regression nav ~lines 114-129)
  </read_first>
  <action>
    Hand-author three method-accurate SVG diagrams in docs/assets/diagrams/: function-on-function.svg (FoF = coefficient beta-SURFACE mapping predictor curve → response curve, a 2D surface, not a scalar coefficient), additive-sof.svg (additive SoF = sum of smooth partial-effect functions of FPC scores → scalar response), frechet-regression.svg (Fréchet = conditional metric-space barycenter / mean object in a non-Euclidean space, e.g. SPD manifold). Each: viewBox width 720 (300/480 as needed), canonical five-class style block, role="img", title-matching aria-label, title/desc/aria-labelledby (ids prefixed fof-/asof-/frechet-), FDARS palette + STYLE_SPEC stroke weights. Then add three nav entries under the Regression section of mkdocs.yml (slot into the existing section per the locked decision — NO new top-level groups): `- Function-on-Function: regression/function-on-function.md`, `- Additive Scalar-on-Function: regression/additive-sof.md`, `- Fréchet Regression: regression/frechet-regression.md`.
  </action>
  <verify>
    <automated>for s in function-on-function additive-sof frechet-regression; do F=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/$s.svg --output -); S=$(printf '%s' "$F" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$F") <(printf '%s' "$S") || { echo "NOT_IDEMPOTENT: $s"; exit 1; }; done; echo ALL_IDEMPOTENT_OK; DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/reg_nav_build.log; python scripts/check_docs_figures.py site; grep -c 'function-on-function.md\|additive-sof.md\|frechet-regression.md' mkdocs.yml</automated>
    <fails_when>Any of the three diagrams is not svgo-idempotent (second pass differs), OR a nav entry points at a missing/misspelled path, OR check_docs_figures.py finds a traceback. The grep count must be 3.</fails_when>
  </verify>
  <acceptance_criteria>
    - Three SVGs exist, each STYLE_SPEC-conformant and two-pass svgo-idempotent.
    - mkdocs.yml Regression section references all three new pages (grep count 3).
    - DOCS_FAST build no longer warns "not in nav" for these pages; check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>All three regression diagrams are idempotent and all three pages are reachable from the Regression nav; DOCS_FAST build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time fence → docs Python process | Fences run shipped fdars bindings with hardcoded fixtures at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-03 | Tampering | frechet_mean return handling | low | mitigate | Fence treats return as naked array (Pitfall 7); check_docs_figures.py catches a wrong-type traceback |
| T-73-04 | Denial of Service | --strict build time | low | accept | Small non-square fixtures keep each fence fast per STATE build-time blocker |
</threat_model>

<verification>
- DOCS_FAST build green; check_docs_figures.py site exits 0 for all three pages.
- All three regression SVGs pass two-pass svgo@3.3.4 idempotence.
- mkdocs.yml Regression nav references the three new pages.
</verification>

<success_criteria>
The three Regression-section families are fully documented (pages + fences + diagrams + nav),
completing DOCS-01/02 for the Regression section.
</success_criteria>

<output>
Create `.planning/phases/73-documentation-release/73-02-SUMMARY.md` when done
</output>

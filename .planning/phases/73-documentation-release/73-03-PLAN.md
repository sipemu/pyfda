---
phase: 73-documentation-release
plan: 03
type: execute
wave: 3
depends_on: ["73-02"]
files_modified:
  - docs/analyze/density-fda.md
  - docs/analyze/advanced-clustering.md
  - docs/analyze/multi-domain.md
  - docs/analyze/shapelets.md
  - docs/assets/diagrams/density-fda.svg
  - docs/assets/diagrams/advanced-clustering.svg
  - docs/assets/diagrams/multi-domain.svg
  - docs/assets/diagrams/shapelets.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-01, DOCS-02]
estimate:
  tokens: 82000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Four Analyze pages (Density FDA, Advanced Clustering, Multi-Domain/FAMM, Shapelets) each render with a runnable offline fence printing FDARS_FENCE_OK (DOCS-01)."
    - "Four STYLE_SPEC-conformant, SVGO-idempotent concept diagrams exist (DOCS-02)."
    - "All four pages are wired into the Analyze nav section; GAK folds into the Shapelets page; DOCS_FAST build is green."
  artifacts:
    - docs/analyze/density-fda.md
    - docs/analyze/advanced-clustering.md
    - docs/analyze/multi-domain.md
    - docs/analyze/shapelets.md
    - docs/assets/diagrams/density-fda.svg
    - docs/assets/diagrams/advanced-clustering.svg
    - docs/assets/diagrams/multi-domain.svg
    - docs/assets/diagrams/shapelets.svg
  key_links:
    - "density fence → fdars.density_fda (normalize_density/lqd_transform naked arrays; lqd_fpca dict)."
    - "clustering fence → fdars.clustering.{dbscan_fd,kcfc_cluster} (dbscan noise = -1)."
    - "multi-domain fence → fdars.spm.mfpca (takes a LIST of 2D arrays, not a stacked 3D array)."
    - "shapelets fence → fdars.shapelet + fdars.metric.gak_gram_matrix/sigma_gak (GAK folds here)."
---

<objective>
Author the four remaining Analyze-section family pages (Density FDA, Advanced Clustering,
Multi-Domain/FAMM, Shapelets — GAK folds into Shapelets), each with an offline FDARS_FENCE_OK
fence and a STYLE_SPEC-conformant SVGO-idempotent diagram, wired into the Analyze nav.

Purpose: Complete DOCS-01/DOCS-02 coverage for the Analyze-section families, reusing the loop
proven by the fts tracer (73-01).
Output: 4 pages, 4 diagrams, 4 nav entries under Analyze.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author density-fda.md and advanced-clustering.md with offline fences</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 2 Family D density — normalize_density/lqd_transform/inverse_lqd return NAKED 1D arrays, lqd_fpca returns a dict with n_comp; Family F clustering — dbscan_fd noise=-1, kcfc_cluster labels key; Pitfall 9)
    - docs/represent/depth-functions.md (fence + page template)
    - .planning/phases/69-frechet-regression-density-fda (density SUMMARYs) and .planning/phases/70-multi-domain-data-famm-advanced-clustering (clustering SUMMARYs)
  </read_first>
  <action>
    Create TWO pages.
    (a) docs/analyze/density-fda.md — Density FDA. Diagram ref `![Density FDA — concept diagram](../assets/diagrams/density-fda.svg){ .fdars-diagram }`. Fence uses RESEARCH Family D minimal example verbatim as the basis: imports fdars.density_fda.{normalize_density, lqd_transform, lqd_fpca}, builds strictly-positive density-like functions, normalize_density/lqd_transform return NAKED 1D arrays, lqd_fpca returns a dict (print n_comp), final print ends with the FDARS_FENCE_OK marker. Document the lqd_fpca 6-key dict (scores, loadings, mean_lqd, singular_values, explained_variance, n_comp) and note the naked-array transform convention.
    (b) docs/analyze/advanced-clustering.md — Advanced Clustering. Diagram ref `![Advanced Clustering — concept diagram](../assets/diagrams/advanced-clustering.svg){ .fdars-diagram }`. Fence uses RESEARCH Family F example: imports fdars.clustering.{dbscan_fd, kcfc_cluster}, two-cluster non-square fixture (n=25, m=40), eps=0.5 min_points=3, k=2, final print ends with the FDARS_FENCE_OK marker. Document dbscan_fd dict (cluster with -1 noise, n_clusters, n_noise, distances) and mention funfem_cluster/align_cluster_fd in the API table. Keep fixtures small.
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/dens_clust_build.log; python scripts/check_docs_figures.py site</automated>
    <fails_when>Either fence raises — e.g. indexing a naked-array density return as a dict, or a non-positive density value failing lqd validation — producing a traceback in HTML that check_docs_figures.py catches. "not in nav" WARNINGs are expected until Task 3.</fails_when>
  </verify>
  <acceptance_criteria>
    - density-fda.md and advanced-clustering.md exist, each with one passing exec fence printing the success marker.
    - The density fence treats normalize_density/lqd_transform returns as naked arrays; lqd_fpca as a dict.
    - The clustering fence uses a non-square fixture.
    - check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>Both pages build under DOCS_FAST with fences executing cleanly and emitting their success markers.</done>
</task>

<task type="auto">
  <name>Task 2: Author multi-domain.md (mfpca) and shapelets.md (shapelet + GAK) with offline fences</name>
  <read_first>
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 2 Family E multi-domain — mfpca takes a LIST of 2D arrays not a stack (Pitfall 9); result keys scores/eigenfunctions/eigenvalues/means/scales/grid_sizes, NO n_comp key; PyMultiFunData is opaque; Family G shapelets — shapelet_transform_fit returns PyShapeletFit handle, shapelet_classifier_fit takes raw data+labels, quality "info_gain", classifier "knn"; GAK gak_gram_matrix/sigma_gak)
    - docs/represent/depth-functions.md (fence + page template)
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering (mfpca SUMMARYs) and .planning/phases/71-shapelets-gak-metric (shapelet + GAK SUMMARYs)
  </read_first>
  <action>
    Create TWO pages.
    (a) docs/analyze/multi-domain.md — Multi-Domain / FAMM. Diagram ref `![Multi-Domain FDA — concept diagram](../assets/diagrams/multi-domain.svg){ .fdars-diagram }`. Fence uses RESEARCH Family E mfpca example verbatim as the basis: imports fdars.spm.mfpca, passes a LIST [V1, V2] of two NON-SQUARE 2D arrays (m1=30, m2=25) with ncomp=2, prints the scores shape and eigenvalues, final print ends with the FDARS_FENCE_OK marker. Derive component count from len(eigenvalues) (there is NO n_comp key in the mfpca result). Document mfpca keys (scores, eigenfunctions, eigenvalues, means, scales, grid_sizes) and mention multi_fdata_from_components / dense_flmm / multi_famm in the API table WITHOUT a live fence (they need a complex fixture).
    (b) docs/analyze/shapelets.md — Shapelets (GAK folds into this page per the locked decision). Diagram ref `![Shapelets — concept diagram](../assets/diagrams/shapelets.svg){ .fdars-diagram }`. Fence uses RESEARCH Family G example verbatim as the basis: two-class fixture, shapelet_transform_fit(...quality="info_gain") returns a PyShapeletFit handle, then shapelet_transform, then sigma_gak + gak_gram_matrix, then shapelet_classifier_fit(...classifier="knn", k=3) with clf.train_accuracy, final print ends with the FDARS_FENCE_OK marker. Add a GAK subsection documenting gak/gak_gram_matrix/gak_gram_train/gak_gram_predict/sigma_gak and note the Gram output is usable as a precomputed kernel. Add a one-line note pointing to the GAK content for readers arriving from the Distance-Metrics context. Keep fixtures small.
  </action>
  <verify>
    <automated>DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/multi_shape_build.log; python scripts/check_docs_figures.py site</automated>
    <fails_when>Either fence raises — e.g. passing a stacked 3D array to mfpca (Pitfall 9), accessing a nonexistent mfpca n_comp key, using an invalid shapelet quality/classifier string (raises ValueError), or mishandling the PyShapeletFit handle — producing a traceback in HTML that check_docs_figures.py catches.</fails_when>
  </verify>
  <acceptance_criteria>
    - multi-domain.md and shapelets.md exist, each with one passing exec fence printing the success marker.
    - The mfpca fence passes a list of 2D arrays (not a stack) and derives ncomp from len(eigenvalues).
    - The shapelets fence uses valid enum strings and handles the PyShapeletFit handle; GAK content is on this page.
    - check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>Both pages build under DOCS_FAST with fences executing cleanly and emitting their success markers; GAK is folded into the Shapelets page.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 4 analyze diagrams and wire all 4 pages into the Analyze nav</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (canonical style block, palette, stroke weights, viewBox, accessibility, panel patterns)
    - .planning/phases/73-documentation-release/73-RESEARCH.md (Section 3 STYLE_SPEC + SVGO idempotence; method-accuracy notes: shapelet = discriminative subsequence)
    - docs/assets/diagrams/clustering.svg (structural example); mkdocs.yml (Analyze nav ~lines 138-150)
  </read_first>
  <action>
    Hand-author four method-accurate SVG diagrams in docs/assets/diagrams/: density-fda.svg (LQD transform: density to log-quantile-density space to FPCA, and Wasserstein barycenter as a geometric mean of distributions), advanced-clustering.svg (density-based DBSCAN plus k-centres-functional clustering: clusters of curves with noise points labelled -1), multi-domain.svg (MFPCA over multiple simultaneous functional domains/variables sharing subject scores), shapelets.svg (a discriminative subsequence/shapelet extracted from curves separating two classes by best-match distance — NOT a generic clustering picture). Each: viewBox width 720 (300/480 as needed), canonical five-class style block, role="img", title-matching aria-label, title/desc/aria-labelledby (ids prefixed dens-/clust-/multi-/shap-), FDARS palette plus STYLE_SPEC stroke weights. Then add four nav entries under the Analyze section of mkdocs.yml (slot into the existing section — NO new top-level groups): `- Density FDA: analyze/density-fda.md`, `- Advanced Clustering: analyze/advanced-clustering.md`, `- Multi-Domain FDA: analyze/multi-domain.md`, `- Shapelets: analyze/shapelets.md`.
  </action>
  <verify>
    <automated>for s in density-fda advanced-clustering multi-domain shapelets; do F=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/$s.svg --output -); S=$(printf '%s' "$F" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$F") <(printf '%s' "$S") || { echo "NOT_IDEMPOTENT: $s"; exit 1; }; done; echo ALL_IDEMPOTENT_OK; DOCS_FAST=1 PYTHONPATH=scripts .venv/bin/mkdocs build 2>&1 | tee /tmp/ana_nav_build.log; python scripts/check_docs_figures.py site; grep -c 'analyze/density-fda.md\|analyze/advanced-clustering.md\|analyze/multi-domain.md\|analyze/shapelets.md' mkdocs.yml</automated>
    <fails_when>Any of the four diagrams is not svgo-idempotent (second pass differs), OR a nav entry points at a missing/misspelled path, OR check_docs_figures.py finds a traceback. The grep count must be 4.</fails_when>
  </verify>
  <acceptance_criteria>
    - Four SVGs exist, each STYLE_SPEC-conformant and two-pass svgo-idempotent.
    - mkdocs.yml Analyze section references all four new pages (grep count 4).
    - DOCS_FAST build no longer warns "not in nav" for these pages; check_docs_figures.py site exits 0.
  </acceptance_criteria>
  <done>All four analyze diagrams are idempotent and all four pages are reachable from the Analyze nav; DOCS_FAST build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time fence → docs Python process | Fences run shipped fdars bindings with hardcoded fixtures at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-05 | Tampering | mfpca list-vs-stack input | low | mitigate | Fence passes a list of 2D arrays (Pitfall 9); check_docs_figures.py catches a wrong-shape traceback |
| T-73-06 | Denial of Service | --strict build time | low | accept | Small non-square fixtures per family keep fences fast per STATE build-time blocker |
</threat_model>

<verification>
- DOCS_FAST build green; check_docs_figures.py site exits 0 for all four pages.
- All four analyze SVGs pass two-pass svgo@3.3.4 idempotence.
- mkdocs.yml Analyze nav references the four new pages (grep count 4).
</verification>

<success_criteria>
The four Analyze-section families are fully documented (pages + fences + diagrams + nav),
completing DOCS-01/02 for the Analyze section. All 7 diagrams now exist.
</success_criteria>

<output>
Create `.planning/phases/73-documentation-release/73-03-SUMMARY.md` when done
</output>

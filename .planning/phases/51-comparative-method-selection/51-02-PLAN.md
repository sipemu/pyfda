---
phase: 51-comparative-method-selection
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - python/fdars/advisor/_prompts.py
  - python/fdars/advisor/_compare_methods.py
  - tests/test_compare_methods_advise.py
autonomous: true
requirements: [COMPARE-01, COMPARE-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A 'comparison' advise task family narrates the fdars-computed ranking; adding it does not change any existing task family's behavior (COMPARE-02)."
    - "The winner is set from the deterministic fdars sort BEFORE the LLM call and validated afterward so the LLM narration cannot override it; it is surfaced as a distinct output field (COMPARE-01 winner authority)."
    - "Diagnostics reach the LLM as a list of {label, diagnostics} blocks — never a flat-merged dict — so grounding is attributable per candidate and the guard runs per candidate (COMPARE-02)."
    - "Every cited number in the narration is grounded against the correct candidate's diagnostics; a value present only in a different candidate is not accepted as grounding for a claim about this candidate."
  artifacts:
    - "'comparison' clause in _system_prompt() supported-task set (python/fdars/advisor/_prompts.py)"
    - "compare_methods(run_llm=True) path in python/fdars/advisor/_compare_methods.py returning the ranking + winner + LLM narration with the fdars winner preserved"
    - "tests/test_compare_methods_advise.py — offline (mock/injected provider) + env-gated live coverage"
  key_links:
    - "deterministic winner (Plan 01 sort) -> set on the result -> validated after LLM -> distinct output field"
    - "labeled candidate blocks -> per-candidate grounding check -> attributable citations"
    - "_system_prompt('comparison') -> comparison task clause instructing rank-narration only, no winner selection"
---

<objective>
Add the "comparison" advise task family (the 4th) that narrates the deterministic
ranking from Plan 01. The narration explains WHY the fdars-chosen winner ranks
where it does, citing each candidate's grounded diagnostics with correct
per-candidate provenance. The winner itself is authoritative from the Plan-01
deterministic sort — set before the LLM call, validated after, and surfaced as a
distinct field the LLM cannot override.

Purpose: COMPARE-02 (comparison task family with per-candidate provenance) and the
winner-authority half of COMPARE-01. The grounding guard flat-scans, so passing a
flat-merged multi-candidate dict would collapse provenance (RESEARCH PITFALLS 7 &
13) — candidates MUST stay as labeled blocks.

Output: "comparison" task clause in `_prompts.py`; the `run_llm=True` path of
`compare_methods`; offline + env-gated-live tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-comparative-method-selection/51-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/51-comparative-method-selection/51-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02 subset)

- Task family key: `"comparison"` added to `_supported_tasks` and the task-clause branch in `_system_prompt()` (python/fdars/advisor/_prompts.py).
- `compare_methods(run_llm=True)` returns a result carrying: `winner` (fdars-chosen, distinct field), `ranking` (Plan-01 order), and the LLM narration (an `Advice` object or a `narration` field) — the winner field is NOT derived from the LLM.
- Per-candidate provenance payload: a list of `{"label", "method", "diagnostics"}` blocks passed to the LLM; grounding validated per candidate.
- Test names: `tests/test_compare_methods_advise.py::test_comparison_task_prompt_added`, `::test_existing_tasks_unchanged`, `::test_winner_set_before_llm_and_preserved`, `::test_llm_cannot_override_winner`, `::test_provenance_is_per_candidate_not_flat_merged`, `::test_grounding_runs_per_candidate`, plus env-gated `::test_live_comparison_narration`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "comparison" task clause in _system_prompt (COMPARE-02)</name>
  <read_first>
    - python/fdars/advisor/_prompts.py (_system_prompt structure, _supported_tasks set, the existing 'interpretation'/'parameter'/'method' task clauses, _GROUNDING_INVARIANT, _ASPECT_PRIMERS reuse)
    - .planning/research/PITFALLS.md (Pitfall 11 primer wording — no invented thresholds)
    - .planning/phases/51-comparative-method-selection/51-CONTEXT.md (New comparison task family narrates the ranking; the LLM never chooses the winner)
  </read_first>
  <files>python/fdars/advisor/_prompts.py, tests/test_compare_methods_advise.py</files>
  <behavior>
    - _system_prompt("comparison", aspect="clustering") returns a prompt containing the grounding invariant, the per-aspect primer, and a comparison task clause.
    - _system_prompt("interpretation"), ("parameter"), ("method") return byte-for-byte the same string as before this change.
    - _system_prompt("bogus") still raises ValueError.
  </behavior>
  <action>
    Add `"comparison"` to `_supported_tasks` in `_system_prompt()` and add a task-clause branch for it. The comparison clause must instruct the LLM to: narrate the ALREADY-DECIDED ranking (the ranking and winner are supplied and fdars-computed — the model explains it, it does NOT select it); reference each candidate by its explicit label; cite each candidate's own diagnostic values when explaining its rank; and never claim a candidate is the winner other than the one supplied. Reuse the existing `_ASPECT_PRIMERS.get(aspect)` mechanism unchanged so per-aspect primers apply to each candidate. End the clause with the standard "only cite values present in the diagnostics; do not supply thresholds not present" prohibition (mirror the depth/fpca/inference primer wording, Pitfall 11). Do NOT place any acceptance-criterion literal negative-grep string verbatim in the clause text beyond what is required for the prompt.

    Keep the change additive: do not alter the base prompt, the FDA primer, `_GROUNDING_INVARIANT`, or the three existing task clauses.

    Add tests: `test_comparison_task_prompt_added` (prompt contains a comparison-specific instruction and the grounding invariant), `test_existing_tasks_unchanged` (interpretation/parameter/method prompts equal a captured baseline string), `test_comparison_rejects_bogus_task` (unsupported task still raises).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_compare_methods_advise.py -q -k "prompt or unchanged or bogus"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_compare_methods_advise.py -k "prompt or unchanged or bogus"` exits 0.
    - `_system_prompt("comparison")` succeeds; the three pre-existing task prompts are unchanged (equality assertion).
    - The comparison clause instructs narration of a supplied ranking and forbids the model choosing the winner.
  </acceptance_criteria>
  <done>The "comparison" task family exists in the prompt builder without altering existing task behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Winner authority + per-candidate provenance in the LLM path (COMPARE-01/02)</name>
  <read_first>
    - python/fdars/advisor/_compare_methods.py (the Plan-01 offline core + the run_llm=True branch to fill in)
    - python/fdars/advisor/__init__.py (advise() signature: task, domain_context, model, provider, aspect; how it calls resolve_provider + _check_grounding)
    - python/fdars/advisor/providers/_validate.py (_check_grounding flat-scans diagnostics; _flatten_diagnostics_numbers — proves why per-candidate blocks are required)
  </read_first>
  <files>python/fdars/advisor/_compare_methods.py, tests/test_compare_methods_advise.py</files>
  <behavior>
    - compare_methods(run_llm=True) computes the deterministic ranking + winner FIRST, then calls the LLM to narrate, then returns a result whose winner equals the fdars-sort winner regardless of the LLM narration content.
    - If a mock provider returns narration naming a different candidate as "winner", the returned winner field is still the fdars-sort winner (LLM cannot override).
    - Grounding is checked against EACH candidate's own diagnostics block; a citation valid only for candidate B is not accepted as grounding for a claim about candidate A.
  </behavior>
  <action>
    Implement the `run_llm=True` branch of `compare_methods`. Order of operations (per 51-CONTEXT "Winner authority"): (1) normalize + guard + deterministic sort exactly as the offline path (reuse the Plan-01 helpers — do not duplicate ranking logic); (2) capture `winner` and `ranking` from the fdars sort; (3) build a per-candidate provenance payload — a list of `{"label", "method", "diagnostics"}` blocks (NEVER `{**a, **b}`); (4) call the LLM to narrate via the "comparison" task. Prefer routing through the existing `advise()` by passing the labeled-blocks structure as the diagnostics argument with `task="comparison"` and `aspect=<family>`, OR call `resolve_provider` + `complete_structured` directly if the labeled-blocks shape needs a bespoke user message — in either case run grounding PER candidate block (call `_check_grounding` once per `{label, diagnostics}` block against that block's diagnostics, not against a merged dict) so provenance is attributable (PITFALLS 7 & 13).

    After the LLM returns, set the result's `winner` field from the captured fdars-sort winner (step 2), NOT from the narration. Validate that if the narration references a winner it matches the fdars winner; if it contradicts, keep the fdars winner authoritative (the phase invariant) and record the LLM's narration as-is under a separate field (e.g. `narration`/`advice`). Return `{"method", "metric", "ranking", "winner", "advice"|"narration"}` — `winner` is the fdars-chosen value, surfaced distinctly from the narration (51-CONTEXT "Winner surfacing").

    Add tests using an injected mock provider (no API key): `test_winner_set_before_llm_and_preserved` (mock narration mentions a loser as best; returned winner still equals the fdars winner), `test_llm_cannot_override_winner` (assert winner field == deterministic sort winner), `test_provenance_is_per_candidate_not_flat_merged` (construct two candidates whose diagnostics share a coincidental numeric value; assert the payload sent to the provider is a list of labeled blocks, and that a citation grounded only in the other candidate raises via the per-candidate grounding check), `test_grounding_runs_per_candidate` (a mock narration citing candidate A's value in a claim about candidate B triggers GroundingViolationError). Follow the mock-provider injection pattern already used in the advisor test suite.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_compare_methods_advise.py -q -k "winner or provenance or per_candidate"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_compare_methods_advise.py -k "winner or provenance or per_candidate"` exits 0 with no API key.
    - The returned `winner` equals the Plan-01 deterministic-sort winner even when the mock narration names a different candidate (LLM cannot override).
    - The provider receives a list of `{label, diagnostics}` blocks, never a flat-merged dict; grounding is evaluated per candidate block.
  </acceptance_criteria>
  <done>The LLM narrates the ranking with attributable per-candidate provenance while the fdars-computed winner remains authoritative and distinctly surfaced.</done>
  <reversibility rating="reversible">Internal wiring within compare_methods; the public return shape was fixed in Plan 01.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Env-gated live comparison narration smoke test</name>
  <read_first>
    - tests/test_advisor_live_integration.py (env-gated live pattern: pytest.mark.skipif on ANTHROPIC_API_KEY)
    - python/fdars/advisor/_compare_methods.py (run_llm=True path from Task 2)
    - tests/test_compare_methods_advise.py (offline tests to sit alongside)
  </read_first>
  <files>tests/test_compare_methods_advise.py</files>
  <action>
    Add `test_live_comparison_narration`, skipped unless `ANTHROPIC_API_KEY` is set (mirror `tests/test_advisor_live_integration.py`'s module or per-test skipif). It runs `compare_methods(run_llm=True)` over two real same-family candidates (e.g. two clustering configs' diagnostics), asserts the returned `winner` equals the offline deterministic winner for the same inputs, asserts the narration is non-empty, and asserts grounding passes (no GroundingViolationError). No network runs in CI (test skips without the key). Keep the candidate data small so any manual live run is cheap.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_compare_methods_advise.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_compare_methods_advise.py` exits 0 (the live test is skipped without ANTHROPIC_API_KEY; no FAILED/ERROR).
    - `test_live_comparison_narration` is collected and skipped in a keyless environment.
    - When run with a key (manual), the live winner equals the offline deterministic winner.
  </acceptance_criteria>
  <done>Env-gated live coverage exists and skips cleanly offline; CI stays network-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compare_methods -> LLM provider | fdars-computed labeled diagnostics blocks + prompt cross to the model; model output (narration) is untrusted and must not set the winner |
| LLM narration -> result | Untrusted text; grounding-checked per candidate; winner overwritten from fdars sort |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-05 | Tampering | LLM overriding the winner | critical | mitigate | Winner set from fdars sort before the call and re-asserted after; test_llm_cannot_override_winner + test_winner_set_before_llm_and_preserved |
| T-51-06 | Spoofing (provenance) | flat-merged diagnostics | high | mitigate | Per-candidate labeled blocks; per-candidate grounding check; test_provenance_is_per_candidate_not_flat_merged |
| T-51-07 | Information disclosure | fabricated citations | high | mitigate | _check_grounding run per candidate block; GroundingViolationError on wrong-run citation |
| T-51-08 | Repudiation | non-deterministic CI | medium | mitigate | Offline tests use an injected mock provider; live test env-gated; CI network-free |
| T-51-SC | Tampering | npm/pip/cargo installs | high | accept | No new package installs (pure-Python on existing deps) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_compare_methods_advise.py -q` passes offline; live test skips without a key.
- Existing task-family prompts unchanged (equality assertion).
- Winner authority and per-candidate provenance enforced and tested.
- No new `build_diagnostics` method slot; `_DIAGNOSTICS_METHODS` untouched (guard-sync no-op).
</verification>

<success_criteria>
- COMPARE-02 (comparison task family narrates ranking with correct per-candidate provenance) implemented and tested.
- COMPARE-01 winner-authority half (fdars winner set before LLM, validated, distinctly surfaced) enforced.
- CI stays LLM-free; live coverage is env-gated.
</success_criteria>

<output>
Create `.planning/phases/51-comparative-method-selection/51-02-SUMMARY.md` when done
</output>

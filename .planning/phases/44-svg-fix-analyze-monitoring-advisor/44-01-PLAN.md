---
phase: 44-svg-fix-analyze-monitoring-advisor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/outlier-detection.svg
  - docs/assets/diagrams/scoring-metrics.svg
  - docs/assets/diagrams/elastic-clustering.svg
  - docs/assets/diagrams/clustering.svg
  - docs/assets/diagrams/gmm-clustering.svg
  - docs/assets/diagrams/seasonal-analysis.svg
autonomous: true
requirements: [SVGFIX-01, SVGFIX-02, SVGFIX-03, SVGFIX-04]
estimate:
  tokens: 95000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every flagged analyze/ diagram renders on the built site (rsvg-convert PNG) with no overlapping labels and no text overflowing its container rect — consistent spacing/alignment/sizing (SC1, SVGFIX-01)."
    - "Every diagram in this batch conforms to STYLE_SPEC.md — 720-width viewBox, the .ttl/.sub/.lab/.sm/.mono CSS classes, system-ui fonts, role=\"img\" + aria-label (SC2, SVGFIX-02)."
    - "Every diagram in this batch is method-accurate against the shipped fdars bindings — outlier-detection.svg's taxonomy and its three detection-method labels name only real fdars bindings; no diagram misdepicts what its method does (SC3, SVGFIX-04)."
    - "Each changed diagram passes SVGO idempotence: svgo@3.3.4 --config svgo.config.mjs second pass byte-identical (SC4, SVGFIX-03)."
    - "The 11 OK diagrams in the analyze/monitoring/advisor batch are byte-unchanged — git diff --name-only lists only files within the 6 flagged analyze/ paths (SC4, no-churn decision)."
    - "The analyze/ section passed a built-site PNG review before its commit landed; monitoring/ and advisor/ have no flagged diagrams so need no diagram edit (SC5)."
  artifacts:
    - "docs/assets/diagrams/outlier-detection.svg (redundant overrides stripped + bottom-row overflow fixed + method-accuracy: taxonomy confirmed, conformal_prediction_band() label corrected to a real binding)"
    - "docs/assets/diagrams/scoring-metrics.svg (cramped right-panel labels re-spaced)"
    - "docs/assets/diagrams/elastic-clustering.svg (redundant overrides stripped; modest conservative enrichment only if warranted, surfaced for Phase 49)"
    - "docs/assets/diagrams/clustering.svg (only the 2 redundant .lab font-size=\"13\" overrides stripped)"
    - "docs/assets/diagrams/gmm-clustering.svg (redundant overrides stripped IF any; likely byte-unchanged — all inline sizes differ from class)"
    - "docs/assets/diagrams/seasonal-analysis.svg (redundant overrides stripped IF any; likely byte-unchanged — all inline sizes differ from class)"
  key_links:
    - "STYLE_SPEC.md class sizes (.ttl=17 .sub=12 .lab=13 .sm=11 .mono=12) are the reference that decides which inline font-size= is REDUNDANT (strip) vs INTENTIONAL (keep) — the confirmed Phase 43 lesson."
    - "The shipped bindings (src/outliers_mod.rs, src/conformal_mod.rs, src/depth_mod.rs) + docs/analyze/outlier-detection.md resolve the outlier-detection.svg method-accuracy FLAGs."
    - "svgo.config.mjs + pinned svgo@3.3.4 is the per-diagram determinism gate; .venv + rsvg-convert is the analyze/ section visual gate."
---

<objective>
Correct all flagged concept diagrams in the analyze/ section (the Phase 44 bucket of 42-AUDIT.md §2) on all four fix axes — visual/layout, STYLE_SPEC conformance, XML source formatting, and method-accuracy against the shipped fdars bindings — and verify each on the built site via rendered PNG. The monitoring/ and advisor/ sections have NO flagged diagrams (all 11 batch diagrams outside analyze/ are OK), so this phase touches only 6 analyze/ SVGs. The 11 OK diagrams stay byte-unchanged.

Purpose: This is the second of three section-batched SVG-fix phases; it reuses the correction + per-diagram-gate + per-section-review pattern established in Phase 43 (per SVGFIX-01..04).
Output: Up to 6 corrected `docs/assets/diagrams/*.svg` files (no new files, no docs `.md` edits), committed as a single analyze/ section commit (monitoring/ and advisor/ have nothing to commit).

Scope boundaries (from 44-CONTEXT.md — non-negotiable):
- Edit ONLY the 6 flagged analyze/ SVGs listed in `files_modified`. Do NOT touch the 11 OK diagrams (tolerance-bands, functional-outliers, functional-boxplot, equivalence-testing, covariance-functions, functional-statistics, spm, advanced-spm, profile-partial-monitoring, advisor-loop, advisor-grounding-invariant), any docs `.md` page, or create new diagrams.
- Do NOT touch depth-functions.svg — it was resolved in Phase 43 and belongs to represent/; its appearance in 42-AUDIT.md §2 prose is the known audit editorial nit (§1 scoring table is authoritative over §2 prose).
- Do NOT run a whole-site `mkdocs build --strict` — deferred to Phase 49 (GATE-01). The per-diagram SVGO idempotence + analyze/ rsvg-convert PNG render IS the correct per-phase proof.
- PNGs are written to the scratchpad only and are NEVER committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-svg-fix-analyze-monitoring-advisor/44-CONTEXT.md
@.planning/phases/42-diagram-audit/42-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
</context>

<pre_flight>
The redundant-vs-intentional override classification and the method-accuracy FLAGs were resolved against the class sizes and shipped bindings during planning. Executors: re-confirm per element, do not re-guess.

**Class-size reference (STYLE_SPEC.md §Canonical style Block):** `.ttl`=17px, `.sub`=12px, `.lab`=13px, `.sm`=11px, `.mono`=12px. An inline `font-size=` (and `style="fill:..."`) is REDUNDANT (strip it — the class already supplies it) ONLY when its value equals the element's class value. When the inline value DIFFERS from the class value it is an INTENTIONAL per-element override → KEEP it byte-for-byte (the confirmed Phase 43 lesson). Verify per element before removing.

**Per-file override survey done at plan time (re-verify, then act):**
- `clustering.svg`: `.lab font-size="13"` (2 elements) are REDUNDANT (=.lab 13) → strip. Everything else differs (`.ttl`=20, `.sub`=14, `.sm`=12, `.lab`=15) → KEEP. Expect exactly 2 override strips.
- `gmm-clustering.svg`: every inline size differs from its class (`.ttl`=20, `.sub`=14, `.sm`=12, `.lab`=14/15) → likely ZERO redundant overrides → leave byte-unchanged and record the confirmation in the SUMMARY. Only strip an override if a genuine class-size duplicate is found on re-inspection.
- `seasonal-analysis.svg`: same as gmm — every inline size differs from its class → likely ZERO redundant → likely byte-unchanged; only strip a genuine duplicate.
- `outlier-detection.svg`: text carries BOTH class and inline `font-size=`/`style="fill:..."`; several inline sizes are near-class but distinct (17.5 vs .ttl 17, 12.5 vs .lab 13, 10 vs .sm 11). These are per-element size tweaks (INTENTIONAL) — treat conservatively: strip only exact class-size duplicates; where an inline value differs (even by 0.5px) KEEP it. The primary outlier-detection.svg fixes are the bottom-row overflow (visual) and the method-accuracy FLAGs below, NOT aggressive override stripping.
- `elastic-clustering.svg`: text carries `class` + inline `font-size=`/`style="fill:..."`; strip only exact class-size duplicates (verify), keep distinct values.

**outlier-detection.svg method-accuracy FLAGs — RESOLVED at plan time against shipped sources:**
1. Taxonomy "Magnitude / Shape / **Amplitude**" is CANONICAL and CORRECT — do NOT relabel "Amplitude". Evidence: `docs/analyze/outlier-detection.md:7-9` defines exactly this three-type taxonomy; the shipped bindings confirm an amplitude class — `src/outliers_mod.rs:257,265` (`tvdmss` returns `amplitude_outliers`/`amplitude_index`) and `src/outliers_mod.rs:271` (`MUOD` "detects shape, magnitude, and amplitude outliers"). The Phase 44 CONTEXT hypothesis that "Amplitude" might be non-canonical is REJECTED by the sources.
2. Bottom-row detection-method label `conformal_prediction_band()` is NOT a real fdars binding — `src/conformal_mod.rs` exports `conformal_fregre_lm`, `conformal_fregre_np`, `conformal_classif`, `conformal_elastic_regression`, `conformal_elastic_pcr`, `conformal_logistic`, `conformal_elastic_logistic` (NO `conformal_prediction_band`). This is a genuine SVGFIX-04 defect: the label names a function that does not exist. The other two labels are real: `depth()` (via `fdars.depth`) and `detect_outliers_lrt()` (`src/outliers_mod.rs:31`). Fix: replace the non-existent `conformal_prediction_band()` label with a real distribution-free / likelihood detection binding the page actually teaches (e.g. `magnitude_shape()` / `outliergram()` / `tvdmss()` — all real, per `src/outliers_mod.rs:66,93,213`, and taught in `docs/analyze/outlier-detection.md`), OR generalise the third box to a real detector consistent with the page. Choose the label that keeps the three-box "depth / likelihood / plane-based" story accurate; confirm the chosen name greps in `src/*_mod.rs` before committing.

**Tooling confirmed present at plan time:** `svgo.config.mjs`, `svgo@3.3.4` (via `npx`, fnm shell), `/usr/bin/rsvg-convert`. All 6 target files already have `viewBox="0 0 720 {300|480}"`, `role="img"`, `aria-label`, and the five canonical CSS classes (the STYLE_SPEC.md §Legacy table listing elastic-clustering/outlier-detection at non-720 widths is STALE — both are already 720). So NO viewBox migration is needed this phase.
</pre_flight>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Fix outlier-detection.svg end-to-end through the full per-diagram gate — all three axes (XML + visual overflow + method-accuracy)</name>
  <read_first>
    - docs/assets/diagrams/outlier-detection.svg (title/subtitle lines 14-15 carry inline `font-size="17.5"`/`style="fill:#333"` + `12.5`/`#888`; the three taxonomy panels lines 17-71; the bottom-row Detection Methods rects lines 78-85 — the three `width="170"` rects at x=20/215/410 with 10px labels `depth() → low depth = outlier`, `detect_outliers_lrt() → likelihood test`, `conformal_prediction_band() → dist-free` that overflow ~170px)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical style Block — the five class sizes decide which inline `font-size=` is redundant; §viewBox; §Accessibility — the file already conforms, do not regress it)
    - docs/analyze/outlier-detection.md lines 7-9 and 20-22 (the canonical Magnitude/Shape/Amplitude taxonomy + the real detector table naming detect_outliers_lrt, outliergram, magnitude_shape)
    - src/outliers_mod.rs lines 31, 66, 93, 213, 257-265, 271 (real bindings: detect_outliers_lrt, outliergram, magnitude_shape, tvdmss with amplitude_outliers, MUOD) and src/conformal_mod.rs lines 32-440 (confirms NO conformal_prediction_band)
    - 42-AUDIT.md §1 scoring row + §2 Phase 44 entry for outlier-detection.svg (XML overrides + bottom-row overflow + taxonomy FLAG)
  </read_first>
  <files>docs/assets/diagrams/outlier-detection.svg</files>
  <action>
    This is the tracer: prove the full edit -> SVGO-idempotence -> rsvg-PNG -> analyze/ section-review pipeline on ONE diagram that exercises ALL THREE fix axes before scaling to the rest.

    Axis A (method-accuracy, SVGFIX-04): Do NOT relabel the "Amplitude" taxonomy panel — it is canonical (verified against docs/analyze/outlier-detection.md:9 + the amplitude bindings in src/outliers_mod.rs). Instead, correct the bottom-row third detection-method label: `conformal_prediction_band()` names a function that does NOT exist in the shipped bindings. Replace it with a real fdars outlier-detection binding that keeps the three-box story accurate (depth-based / likelihood-based / plane-or-depth-based). Prefer a real name the page teaches — e.g. `magnitude_shape()`, `outliergram()`, or `tvdmss()` (all real per src/outliers_mod.rs). Before committing, confirm the chosen label greps as a real `pub fn` in src/*_mod.rs. Keep the arrow-and-caption format of the box.

    Axis B (visual/layout, SVGFIX-01): The three bottom-row rects are 170px wide; the labels at 10px overflow (the longest, the corrected third label and `detect_outliers_lrt() → likelihood test`, exceed the rect). Fix the overflow WITHOUT moving the taxonomy panels: shorten each caption to fit, and/or widen/re-space the three rects within the 720 canvas (the group is translate(60,3); the rects currently sit at x=20/215/410 width=170, right edge ~580 inside the 600-wide inner area — you have head-room to the 660 inner-right). Ensure every bottom-row label sits fully inside its rect on the PNG (no clipping, no crossing the rect border).

    Axis C (XML, SVGFIX-02/03): Strip only inline `font-size=`/`style="fill:..."` overrides whose value EXACTLY duplicates the element's class size/fill; KEEP the deliberate distinct tweaks (17.5 vs .ttl 17, 12.5, 10 vs .sm 11) byte-for-byte. This is conservative — the point is the two visual/method fixes above, not an aggressive override purge. Do not change viewBox, role, aria-label, the five classes, or any path/color/position beyond the bottom-row rects.

    Exact re-spacing geometry and the chosen replacement binding name are at your discretion so long as: the taxonomy stays Magnitude/Shape/Amplitude, the third detection label names a real binding, no bottom-row text overflows its rect, and SVGO idempotence holds.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && sv=docs/assets/diagrams/outlier-detection.svg && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/od.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/od.p1.svg" --output "$SC/od.p2.svg" && cmp "$SC/od.p1.svg" "$SC/od.p2.svg" && rsvg-convert "$sv" -o "$SC/outlier-detection.png" && test -s "$SC/outlier-detection.png" && grep -q "viewBox=\"0 0 720" "$sv" && grep -q "role=\"img\"" "$sv" && grep -q "aria-label" "$sv" && ! grep -q "conformal_prediction_band" "$sv" && grep -qE "Magnitude Outlier" "$sv" && grep -qE "Amplitude Outlier" "$sv" && echo GATE_OK'</automated>
    <human-check>Open $SC/outlier-detection.png: the three taxonomy panels still read Magnitude / Shape / Amplitude; the three bottom-row detection-method labels each sit fully inside their rect with no overflow/clipping; the third label names a real fdars binding (not conformal_prediction_band); no other panel changed. This is the analyze/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - outlier-detection.svg taxonomy panels still read "Magnitude" / "Shape" / "Amplitude" (grep-confirmable) — "Amplitude" NOT relabelled.
    - The string `conformal_prediction_band` no longer appears in the file; the third detection-method label names a binding that greps as a real `pub fn` in src/*_mod.rs (verify with `grep -R "pub fn <chosen_name>" src`).
    - Every bottom-row detection-method label fits within its rect on the PNG (no text overflow / clipping past the rect edge).
    - viewBox `0 0 720 300`, `role="img"`, `aria-label`, and all five CSS classes still present (grep-confirmable; unchanged).
    - Any inline `font-size=` that remains differs from its element's class size (no remaining exact class-size duplicate); intentional distinct tweaks preserved.
    - SVGO idempotence: 2nd pass byte-identical (`cmp` exits 0); non-empty PNG in scratchpad (never committed).
    - `git diff --name-only` for this task lists only docs/assets/diagrams/outlier-detection.svg.
  </acceptance_criteria>
  <reversibility rating="reversible">All edits are hand-authored SVG changes tracked in git; the diagram can be reverted with `git checkout`.</reversibility>
  <done>outlier-detection.svg is corrected on all three axes (taxonomy confirmed, non-existent binding label replaced with a real one, bottom-row overflow fixed, redundant-only overrides stripped), passes SVGO idempotence + renders cleanly, and the analyze/ section PNG review passed. It lands in the single analyze/ section commit together with Tasks 2-3.</done>
</task>

<task type="auto">
  <name>Task 2: Fix scoring-metrics.svg (label re-spacing) + elastic-clustering.svg (redundant overrides + conservative enrichment)</name>
  <read_first>
    - docs/assets/diagrams/scoring-metrics.svg (left panel group translate(40,98): `|ε(t)|` label at x=172 y=72 line 46 and the `∫|ε(t)| dt (shaded)` legend at x=198 y=13 line 54; right orange panel group translate(452,112): the `⚠ MAPE: rejects |y_true| ≈ 0` warning at x=0 y=185 line 96, near the panel's y=278 bottom edge; the `Δ MAPE`/MAPE rows lines 85-96) — this file has NO inline font-size and NO STYLE_SPEC issue; the fix is purely visual re-spacing of the cramped right-panel labels
    - docs/assets/diagrams/elastic-clustering.svg (4 flow boxes lines 18-45; each text carries `class` + inline `font-size=`/`style="fill:..."`; the COMPUTATION/RESULTS section labels lines 44-45)
    - docs/analyze/elastic-clustering.md lines 1-5, 12-14, 63-81 (the method: elastic/amplitude distance matrix via `elastic_self_distance_matrix`, then distance-based clustering via `hierarchical_cut`; note the page teaches that plain L² k-means FAILS here — the diagram's "K-Means / Hierarchical" box wording is a possible over-simplification)
    - docs/analyze/scoring-metrics.md (confirms the metric definitions the labels annotate)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical style Block — class sizes for the redundant-override check)
  </read_first>
  <files>docs/assets/diagrams/scoring-metrics.svg, docs/assets/diagrams/elastic-clustering.svg</files>
  <action>
    scoring-metrics.svg (visual only, SVGFIX-01): re-space/enlarge the cramped right-panel labels so they read clearly. The `⚠ MAPE: rejects |y_true| ≈ 0` warning sits at y=185 inside the translate(452,112) group (absolute ~y=297), crowding the panel's y=278 bottom edge — nudge it up / give it breathing room within the orange panel. The left-panel `|ε(t)|` integral label and its `∫|ε(t)| dt (shaded)` legend are cramped near the shaded region — re-space slightly so they do not touch the curve or each other. Do NOT change the metric definitions, the panel rects' outer geometry, or any text content beyond spacing/position (and, if strictly needed for fit, minimal wording). There is no XML/STYLE_SPEC issue in this file — do not introduce inline overrides.
    elastic-clustering.svg (XML + conservative enrichment): (a) strip only inline `font-size=`/`style="fill:..."` overrides that EXACTLY duplicate the element's class size/fill; KEEP any distinct value. (b) Enrichment is a DESIGN JUDGMENT, not a factual fix — the diagram is sparse (4 bare flow boxes) vs analyze/ peers, but the referencing page is rich. Keep any enrichment MODEST and conservative: at most tighten the pipeline wording to match the page (the distance is the **elastic/amplitude (phase-invariant) distance** via `elastic_self_distance_matrix`, and clustering is **distance-based** — the page teaches that plain L² k-means fails, so if you touch the "K-Means / Hierarchical" box prefer wording that does not imply plain L² k-means is the recommended path). Do NOT redesign the panel or add new boxes speculatively. SURFACE the enrichment extent (what you changed vs left) in the SUMMARY explicitly for the Phase 49 blocking human diagram review. If on inspection the boxes are already method-accurate and only redundant overrides exist, keep the change to the override strip alone and note that enrichment was deliberately declined pending Phase 49.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && ok=1; for f in scoring-metrics elastic-clustering; do sv="docs/assets/diagrams/$f.svg"; git diff --quiet -- "$sv" && continue; npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/$f.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/$f.p1.svg" --output "$SC/$f.p2.svg" && cmp "$SC/$f.p1.svg" "$SC/$f.p2.svg" && rsvg-convert "$sv" -o "$SC/$f.png" && test -s "$SC/$f.png" && grep -q "viewBox=\"0 0 720" "$sv" && grep -q "role=\"img\"" "$sv" && grep -q "aria-label" "$sv" || { echo "FAIL $f"; ok=0; }; done; [ $ok -eq 1 ] && echo GATE_OK'</automated>
    <human-check>Open the scratchpad PNGs for every changed file: scoring-metrics.svg right-panel `⚠ MAPE` warning and left-panel `|ε(t)|`/`∫` labels are no longer cramped (clear spacing, nothing clipped at the panel bottom); elastic-clustering.svg renders cleanly with any wording tightening legible and method-accurate. Part of the analyze/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - scoring-metrics.svg: the `⚠ MAPE: rejects |y_true| ≈ 0` warning and the `|ε(t)|` / `∫|ε(t)| dt (shaded)` labels are re-spaced so none is clipped or crowding the panel edge on the PNG; metric definitions and outer panel geometry unchanged; no inline font-size introduced.
    - elastic-clustering.svg: any inline `font-size=`/`style=fill` that remains differs from its class value (no remaining exact class-size duplicate); any enrichment is modest, method-accurate against docs/analyze/elastic-clustering.md, and explicitly surfaced in the SUMMARY for Phase 49.
    - For each CHANGED file: SVGO idempotence 2nd pass byte-identical (`cmp` exits 0) + non-empty scratchpad PNG; viewBox 720 / role / aria / five classes preserved (grep-confirmable).
    - `git diff --name-only` for this task lists only scoring-metrics.svg and elastic-clustering.svg (no OK diagram, no .md).
  </acceptance_criteria>
  <reversibility rating="reversible">All edits are hand-authored SVG changes tracked in git; either diagram can be reverted with `git checkout`.</reversibility>
  <done>scoring-metrics.svg labels are re-spaced and elastic-clustering.svg's redundant overrides are stripped (with any enrichment kept conservative and surfaced for Phase 49); both changed files pass SVGO idempotence + render cleanly; they land in the single analyze/ section commit with Tasks 1 and 3.</done>
</task>

<task type="auto">
  <name>Task 3: Strip only REDUNDANT inline font-size overrides from clustering.svg / gmm-clustering.svg / seasonal-analysis.svg (expect near-zero churn) + commit the analyze/ section</name>
  <read_first>
    - docs/assets/diagrams/clustering.svg (locate the 2 elements carrying `class="lab" font-size="13"` — these are the ONLY redundant overrides, =.lab 13; everything else — .ttl 20, .sub 14, .sm 12, .lab 15 — is a distinct INTENTIONAL value to KEEP)
    - docs/assets/diagrams/gmm-clustering.svg (survey inline `font-size=`: at plan time every value differs from its class — .ttl 20, .sub 14, .sm 12, .lab 14/15 — so expect ZERO redundant; confirm per element)
    - docs/assets/diagrams/seasonal-analysis.svg (same survey: at plan time every inline value differs from its class → expect ZERO redundant; confirm per element)
    - docs/assets/diagrams/STYLE_SPEC.md (§Canonical style Block — .ttl=17 .sub=12 .lab=13 .sm=11 .mono=12: the exact sizes that make an override redundant-vs-intentional)
    - 42-AUDIT.md §1 rows for clustering / gmm-clustering / seasonal-analysis (minor XML flag only; layouts clean)
  </read_first>
  <files>docs/assets/diagrams/clustering.svg, docs/assets/diagrams/gmm-clustering.svg, docs/assets/diagrams/seasonal-analysis.svg</files>
  <action>
    For each file, for each text element carrying BOTH a CSS class and an inline `font-size=` (and/or `style="fill:..."`): compare the inline value to the element's class value (.ttl=17, .sub=12, .lab=13, .sm=11, .mono=12). Strip the inline attribute ONLY when it EXACTLY duplicates the class value (redundant cruft the class already supplies). KEEP every inline value that DIFFERS from the class — those are intentional per-element size/color tweaks (the confirmed Phase 43 lesson). Do NOT touch positions, colors, paths, layout, or text content. Do NOT change viewBox, role, aria-label, or the five classes.
    Expected outcome from the plan-time survey: clustering.svg = exactly 2 redundant `.lab font-size="13"` strips; gmm-clustering.svg and seasonal-analysis.svg = ZERO redundant overrides → leave them BYTE-UNCHANGED and record the confirmation in the SUMMARY (this is a correct outcome, not a miss — mirrors the Phase 43 "5 files left byte-unchanged" precedent). Only strip in gmm/seasonal if re-inspection finds a genuine exact class-size duplicate.
    After all changed files across Tasks 1-3 pass their gates and the analyze/ PNG review, commit the whole analyze/ batch as ONE section commit (monitoring/ and advisor/ have no flagged diagrams so contribute nothing): `fix(diagrams): analyze/ — outlier-detection method-accuracy + overflow, scoring-metrics re-space, redundant-override cleanup`.
  </action>
  <verify>
    <automated>bash -c 'cd /home/simonm/projects/rust/pyfda && SC=/tmp/claude-1000/-home-simonm-projects-rust-pyfda/81d3a2ad-9c69-4845-a299-2219a3a880f5/scratchpad && mkdir -p "$SC" && ok=1; for f in clustering gmm-clustering seasonal-analysis; do sv="docs/assets/diagrams/$f.svg"; git diff --quiet -- "$sv" && { echo "unchanged $f (ok)"; continue; }; npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$sv" --output "$SC/$f.p1.svg" && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$SC/$f.p1.svg" --output "$SC/$f.p2.svg" && cmp "$SC/$f.p1.svg" "$SC/$f.p2.svg" && rsvg-convert "$sv" -o "$SC/$f.png" && test -s "$SC/$f.png" && grep -q "viewBox=\"0 0 720" "$sv" && grep -q "role=\"img\"" "$sv" || { echo "FAIL $f"; ok=0; }; done; changed=$(git diff --name-only -- docs/assets/diagrams | grep -vE "outlier-detection|scoring-metrics|elastic-clustering|clustering|gmm-clustering|seasonal-analysis" | grep -c .svg || true); [ "$changed" = "0" ] && [ $ok -eq 1 ] && echo GATE_OK'</automated>
    <human-check>Open any changed PNG (clustering.svg if the 2 strips landed): renders pixel-identical to before the redundant-override removal — no visible change, which is the intent. gmm-clustering.svg and seasonal-analysis.svg confirmed byte-unchanged unless a genuine duplicate was found. Final analyze/ section review (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    - clustering.svg: the 2 redundant `.lab font-size="13"` overrides are removed; no other override touched; renders visually identical.
    - gmm-clustering.svg and seasonal-analysis.svg: byte-unchanged (git diff --quiet passes) UNLESS a genuine exact class-size duplicate was found and stripped; the confirmation is noted in the SUMMARY.
    - For every CHANGED file: SVGO idempotence 2nd pass byte-identical + non-empty scratchpad PNG; viewBox 720 / role / aria preserved.
    - `git diff --name-only -- docs/assets/diagrams` across the whole phase lists ONLY files within the 6 flagged analyze/ paths — the 11 OK diagrams (incl. depth-functions.svg, spm.svg, advisor-*) are byte-unchanged.
    - The analyze/ batch is committed as a single section commit.
  </acceptance_criteria>
  <reversibility rating="reversible">All edits are hand-authored SVG changes tracked in git; any diagram can be reverted with `git checkout`.</reversibility>
  <done>clustering.svg's 2 redundant overrides are stripped, gmm-clustering.svg + seasonal-analysis.svg are confirmed byte-unchanged (or minimally corrected if a real duplicate surfaced), all changed files pass SVGO idempotence + render cleanly, the 11 OK diagrams are byte-unchanged, and the full analyze/ batch is committed as one section commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed SVG | Hand-edited SVG source is committed and later rendered by MkDocs into the public site. |
| npx → svgo@3.3.4 | The idempotence gate shells out to an npm package by pinned version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Information Disclosure | outlier-detection.svg names a non-existent binding (`conformal_prediction_band()`), teaching a misleading API | high | mitigate | Task 1 replaces it with a binding confirmed to grep as a real `pub fn` in src/*_mod.rs; acceptance criteria negative-grep `conformal_prediction_band` and positive-grep the chosen real name. |
| T-44-02 | Tampering | Over-aggressive override stripping deletes an INTENTIONAL distinct inline size, changing rendered geometry | medium | mitigate | Per-element compare against the class-size reference (only exact duplicates stripped); gmm/seasonal expected byte-unchanged; SVGO idempotence + rsvg PNG catch geometry regressions. |
| T-44-03 | Tampering | Editing an SVG regresses STYLE_SPEC conformance (drops role/aria/viewBox/a class) | medium | mitigate | Every task's automated gate grep-asserts viewBox 720 + role="img" + aria-label on each changed file; outlier-detection.svg tracer proves the check before scaling. |
| T-44-04 | Tampering | Accidental edit/churn to one of the 11 OK diagrams (incl. depth-functions.svg, spm.svg) breaks determinism | medium | mitigate | Task 3 gate greps `git diff --name-only -- docs/assets/diagrams` and fails if any non-flagged .svg changed; OK diagrams must stay byte-unchanged. |
| T-44-SC | Tampering | npm/npx install of svgo@3.3.4 | low | accept | svgo@3.3.4 is the project-pinned, CI-established gate tool (STYLE_SPEC.md §SVGO Invocation) in use since Phase 1; version pinned, not `latest`. No new package introduced. |
</threat_model>

<verification>
Per changed diagram (all tasks): `npx svgo@3.3.4 --config svgo.config.mjs` run twice → 2nd pass byte-identical (`cmp` exit 0), and `rsvg-convert` → non-empty PNG in the scratchpad (never committed). STYLE_SPEC conformance grep on every changed file: viewBox 720, `role="img"`, `aria-label` (the five classes are already present and untouched).
Method-accuracy: outlier-detection.svg — `conformal_prediction_band` negative-grepped out; the replacement label positive-grepped as a real `pub fn` in src/*_mod.rs; the Magnitude/Shape/Amplitude taxonomy preserved (taxonomy confirmed canonical against docs/analyze/outlier-detection.md + src/outliers_mod.rs at plan time).
No-churn guard: `git diff --name-only -- docs/assets/diagrams` lists ONLY files within the 6 flagged analyze/ paths — the 11 OK diagrams (tolerance-bands, functional-outliers, functional-boxplot, equivalence-testing, covariance-functions, functional-statistics, spm, advanced-spm, profile-partial-monitoring, advisor-loop, advisor-grounding-invariant) and depth-functions.svg are byte-unchanged.
Explicitly NOT run this phase: whole-site `mkdocs build --strict` (deferred to Phase 49 / GATE-01).
</verification>

<success_criteria>
- SC1: Every flagged analyze/ diagram renders cleanly on the PNG check — no overlapping labels, no text overflowing its container rect (outlier-detection bottom row + scoring-metrics right panel), consistent spacing/alignment/sizing.
- SC2: Every diagram in this batch conforms to STYLE_SPEC.md (720 viewBox, five CSS classes, system-ui, role="img"+aria-label) — preserved on every changed file.
- SC3: Every diagram in this batch is method-accurate against the shipped fdars bindings — outlier-detection.svg taxonomy confirmed canonical and its third detection label corrected to a real binding.
- SC4: Each changed diagram passes SVGO idempotence; the 11 OK diagrams (and depth-functions.svg) are byte-unchanged.
- SC5: The analyze/ section passed a built-site PNG review before its single section commit landed (monitoring/ and advisor/ have no flagged diagrams).
- Any conservative pedagogical/design judgment (elastic-clustering.svg enrichment extent) is surfaced in the SUMMARY for the Phase 49 blocking human diagram review.
</success_criteria>

<output>
Create `.planning/phases/44-svg-fix-analyze-monitoring-advisor/44-01-SUMMARY.md` when done.
Include: per-diagram outcome (changed / confirmed-byte-unchanged); the outlier-detection.svg method-accuracy FLAG resolutions (Amplitude taxonomy confirmed canonical → kept; `conformal_prediction_band()` confirmed non-existent → replaced with <chosen real binding>); the redundant-vs-intentional override tally per file (clustering 2 strips; gmm-clustering + seasonal-analysis byte-unchanged unless a real duplicate surfaced); and an explicit callout of the elastic-clustering.svg enrichment extent (what was tightened vs deliberately deferred) for the Phase 49 human diagram review.
</output>

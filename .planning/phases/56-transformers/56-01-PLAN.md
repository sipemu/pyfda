---
phase: 56-transformers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_transformers_compliance.py
autonomous: true
requirements: [XFORM-01, XFORM-02, XFORM-03, XFORM-05]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Imputer passes the full parametrize_with_checks battery on installed sklearn (XFORM-03)"
    - "The 5 already-PASS transformers (FPCATransformer, BSplineSmoother, LocalPolynomialSmoother, DepthTransformer, NormTransformer) still pass the full battery — regression-guarded (XFORM-01, XFORM-02, XFORM-05)"
    - "A per-transformer compliance test runs the battery per-estimator (fast subset), not the whole 28-estimator triage"
    - "Every transformer under test calls fdars._native.* directly and never constructs an Fdata"
  artifacts:
    - tests/sklearn/test_transformers_compliance.py
    - python/fdars/sklearn/_skeletons.py
    - python/fdars/sklearn/_coverage.py
  key_links:
    - "Imputer.fit/transform ensure_all_finite compat shim -> native.represent.impute_missing_values"
    - "test_transformers_compliance parametrize_with_checks -> each transformer instance"
---

<objective>
Establish the transformer-compliance pattern end-to-end by promoting ONE PASS-WITH-FIXES transformer (Imputer) to full `parametrize_with_checks`-green, and lock in the 5 already-PASS transformers with a regression test. This is the tracer slice: it wires a per-transformer compliance harness through the estimator fixes and the `_coverage.py` verdict update, proving the exact pattern Plan 02 will repeat for the remaining two.

Purpose: The Imputer has the richest fix (cross-version `ensure_all_finite`/`force_all_finite` shim + `accept_sparse=False`), so proving it green first de-risks the whole family and produces the reusable compliance-test scaffold. The 5-PASS regression guard prevents silent breakage while Plan 02 edits the same file.
Output: Fixed `Imputer` in `_skeletons.py`, `Imputer` verdict flipped to `PASS` in `_coverage.py`, and a new fast per-transformer compliance test covering all 6 transformers touched in this wave.

Artifacts this phase produces (cumulative across plans): green `TransformerMixin` estimators for FPCATransformer, BSplineSmoother, LocalPolynomialSmoother, BasisRepresentation, Imputer, SplineInterpolator, DepthTransformer, NormTransformer; per-transformer compliance tests; a `Pipeline([smoother, fpca])` round-trip test; `_coverage.py` verdicts all `PASS`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-transformers/56-CONTEXT.md

# Existing implementation + contract (read before editing)
@python/fdars/sklearn/_base.py
@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_coverage.py
@tests/sklearn/conftest.py
@tests/sklearn/test_triage.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Promote Imputer to full check_estimator-green (XFORM-03)</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <read_first>
python/fdars/sklearn/_skeletons.py (Imputer class, lines ~476-572 — existing fit/transform with the try/except ensure_all_finite shim and the _HAS_TAGS_DATACLASS-guarded tag overrides);
python/fdars/sklearn/_base.py (_validate shim — note it forwards **kw to sklearn validate_data; the compat is between ensure_all_finite (1.6+) and force_all_finite (1.3-1.5));
_coverage.py Imputer verdict note (4 failures: check_dtype_object + 3 sparse checks; root cause = incomplete finite-shim NaN path when dtype=object/sparse, fix = complete the shim + accept_sparse=False).
  </read_first>
  <action>
Fix the Imputer per the exact Phase-55 triage verdict for XFORM-03. Two changes, both inside the Imputer class only:
(1) Add accept_sparse=False to BOTH `_validate` calls (fit reset=True and transform reset=False) so the sparse-input checks raise the sklearn-convention TypeError rather than reaching the native call. This resolves check_estimator_sparse_tag / check_estimator_sparse_array / check_estimator_sparse_matrix.
(2) Complete the finite-value cross-version shim so the dtype=object NaN path is handled: keep the existing try(ensure_all_finite="allow-nan") / except TypeError(force_all_finite="allow-nan") structure, and ensure accept_sparse=False rides alongside it in both branches. This resolves check_dtype_object.
Keep the existing _HAS_TAGS_DATACLASS-guarded __sklearn_tags__ (input_tags.allow_nan=True) / _more_tags path UNCHANGED — CR-01 was already applied in Phase 55. Do NOT construct an Fdata; the native call stays impute_missing_values(X, argvals_, method, constant_value). Do NOT touch any other transformer in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest "tests/sklearn/test_transformers_compliance.py::test_imputer_compliance" -q</automated>
  </verify>
  <done>Imputer instance passes every parametrize_with_checks case (check_dtype_object + all three sparse checks now green); no Fdata is constructed anywhere in the Imputer class.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-transformer compliance test for Imputer + the 5 already-PASS transformers</name>
  <files>tests/sklearn/test_transformers_compliance.py</files>
  <read_first>
tests/sklearn/test_triage.py (parametrize_with_checks usage pattern + minimal-hyperparameter construction, e.g. FPCATransformer(n_components=1), BasisRepresentation(n_basis=3));
tests/sklearn/conftest.py (importorskip("sklearn") gate is already inherited by every file in this directory);
python/fdars/sklearn/_skeletons.py (import path for the transformer classes).
  </read_first>
  <action>
Create tests/sklearn/test_transformers_compliance.py. Import from fdars.sklearn._skeletons. Define ONE `@parametrize_with_checks([...])`-decorated test function PER transformer so each transformer's full battery runs as an independent, individually-selectable test — this keeps the suite fast (per-transformer subset) and never re-runs the whole 28-estimator triage. Provide these test functions in this plan's scope: test_imputer_compliance (Imputer()), test_fpca_compliance (FPCATransformer(n_components=1)), test_bspline_smoother_compliance (BSplineSmoother()), test_local_poly_smoother_compliance (LocalPolynomialSmoother()), test_depth_transformer_compliance (DepthTransformer()), test_norm_transformer_compliance (NormTransformer()). Each decorated function body is just `check(estimator)`. Construct each estimator with the minimal valid hyperparameters used in test_triage.py. Leave clearly-named placeholders/imports so Plan 02 can add test_basis_representation_compliance and test_spline_interpolator_compliance without restructuring.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_transformers_compliance.py -q -k "fpca or bspline or local_poly or depth or norm or imputer"</automated>
  </verify>
  <done>All six per-transformer compliance tests exist and pass; each uses parametrize_with_checks so individual checks are independently reported.</done>
</task>

<task type="auto">
  <name>Task 3: Flip Imputer verdict to PASS in _coverage.py (XFORM-03)</name>
  <files>python/fdars/sklearn/_coverage.py</files>
  <read_first>
python/fdars/sklearn/_coverage.py (TRIAGE_VERDICTS dict — the Imputer entry currently reads "PASS-WITH-FIXES: complete ensure_all_finite compat shim + accept_sparse=False"; and the header tally comment "PASS: 6 ... PASS-WITH-FIXES: 22");
tests/sklearn/test_coverage.py (TestVerdictDomain asserts each value starts with PASS/PASS-WITH-FIXES/EXCLUDE — a bare "PASS" is valid).
  </read_first>
  <action>
Update the Imputer entry in TRIAGE_VERDICTS from its PASS-WITH-FIXES string to "PASS" now that XFORM-03's Imputer fix is green. Update the running tally comment at the top of the TRIAGE_VERDICTS block to reflect one more PASS and one fewer PASS-WITH-FIXES (7 PASS + 21 PASS-WITH-FIXES). Do NOT touch BasisRepresentation or SplineInterpolator verdicts — those are Plan 02. Leave EXCLUDED_METHODS unchanged.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_coverage.py -q && .venv/bin/python -c "from fdars.sklearn._coverage import TRIAGE_VERDICTS as v; assert v['Imputer']=='PASS', v['Imputer']"</automated>
  </verify>
  <done>TRIAGE_VERDICTS["Imputer"] == "PASS"; test_coverage.py still green; tally comment updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller -> sklearn estimator | Arbitrary ndarrays (incl. NaN, object dtype, sparse) supplied by the check_estimator battery cross into the estimator |
| estimator -> fdars._native | Validated float64 dense arrays cross into the Rust binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | Imputer.fit/transform input validation | low | mitigate | accept_sparse=False + finite-shim reject non-conforming input at the Python layer with sklearn-convention errors before the native call |
| T-56-02 | Denial of Service | native impute on unvalidated shapes | low | accept | validate_data enforces 2D numeric shape upstream; no unbounded allocation path in a pure-Python wrapper |
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_transformers_compliance.py -q` — all Wave-1 transformer batteries green.
- `.venv/bin/pytest tests/sklearn/test_coverage.py tests/sklearn/test_triage.py -q` — registry + triage harness still green.
- `.venv/bin/python -c "import ast,inspect; from fdars.sklearn._skeletons import Imputer; assert 'Fdata(' not in inspect.getsource(Imputer)"` — Imputer constructs no Fdata.
</verification>

<success_criteria>
Imputer is full-battery green (XFORM-03); the 5 already-PASS transformers are regression-guarded green (XFORM-01/02/05); per-transformer fast compliance harness exists; Imputer verdict is PASS in _coverage.py.
</success_criteria>

<output>
Create `.planning/phases/56-transformers/56-01-SUMMARY.md` when done.
</output>

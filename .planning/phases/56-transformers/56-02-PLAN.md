---
phase: 56-transformers
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_transformers_compliance.py
autonomous: true
requirements: [XFORM-03, XFORM-04]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "BasisRepresentation passes the full parametrize_with_checks battery, rejecting 1-feature input with a sklearn-convention message (XFORM-04)"
    - "SplineInterpolator passes the full battery: transform is idempotent/subset-invariant, fit accepts y=None, spline order is guarded to native [1,3) (XFORM-03)"
    - "Both promoted transformers have a per-transformer compliance test in the shared fast harness"
    - "Both transformers call fdars._native.* directly and never construct an Fdata"
  artifacts:
    - python/fdars/sklearn/_skeletons.py
    - python/fdars/sklearn/_coverage.py
    - tests/sklearn/test_transformers_compliance.py
  key_links:
    - "SplineInterpolator output grid constructor param -> output_argvals_ resolved in fit -> idempotent transform"
    - "BasisRepresentation 1-feature guard -> sklearn-convention ValueError before native.basis.fdata_to_basis_1d"
---

<objective>
Expand the proven Plan-01 pattern to the remaining two PASS-WITH-FIXES transformers: BasisRepresentation (XFORM-04) and SplineInterpolator (XFORM-03). Apply the exact Phase-55 triage fixes, add their per-transformer compliance tests to the shared harness, and flip both verdicts to PASS.

Purpose: These two carry the heavier structural fixes (SplineInterpolator has 13 failing checks driven by fit-time output grid + missing y=None + order guard). Doing them after the Imputer tracer means the compliance-test scaffold and the fix idiom are already established.
Output: Fixed BasisRepresentation and SplineInterpolator in `_skeletons.py`, two new compliance tests, and both verdicts set to `PASS` in `_coverage.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-transformers/56-CONTEXT.md

@python/fdars/sklearn/_base.py
@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_coverage.py
@tests/sklearn/test_transformers_compliance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote BasisRepresentation to full green with a 1-feature guard (XFORM-04)</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <read_first>
python/fdars/sklearn/_skeletons.py (BasisRepresentation class, lines ~398-473 — fit computes actual_n_basis via native.basis.fdata_to_basis_1d; transform projects+reconstructs);
python/fdars/sklearn/_base.py (the "1 sample" guard convention documented in the base-class docstring — mirror that style for the feature-count guard);
_coverage.py BasisRepresentation verdict note (1 failure: check_fit2d_1feature; native emits wrong message; fix = Python-layer 1-feature guard emitting the sklearn-convention substring).
  </read_first>
  <action>
Add a 1-feature guard to BasisRepresentation.fit per the XFORM-04 verdict. After `_validate` sets n_features_in_ and after the 1-sample guard, when n_pts (number of evaluation points / columns) is below the minimum the basis projection needs, raise a ValueError whose message contains the sklearn-convention feature-count substring the battery matches on (the verdict specifies the "1 feature(s)" / "n_features=1" convention). Raise BEFORE the native.basis.fdata_to_basis_1d call. Keep verbatim constructor storage and argvals_ resolution unchanged; do NOT construct an Fdata. Touch only the BasisRepresentation class.
  </action>
  <verify>
    <automated>.venv/bin/pytest "tests/sklearn/test_transformers_compliance.py::test_basis_representation_compliance" -q</automated>
  </verify>
  <done>BasisRepresentation passes every parametrize_with_checks case incl. check_fit2d_1feature; the 1-feature guard fires before any native call and no Fdata is constructed.</done>
</task>

<task type="auto">
  <name>Task 2: Promote SplineInterpolator to full green — idempotent transform, y=None, order guard (XFORM-03)</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <read_first>
python/fdars/sklearn/_skeletons.py (SplineInterpolator class, lines ~575-659 — output_argvals is ALREADY a constructor param stored verbatim and resolved to output_argvals_ in fit; fit ALREADY has y=None; transform calls native.represent.spline_interpolate(X, argvals_, output_argvals_, order));
src/represent_mod.rs (spline_interpolate signature — order is passed straight through to fdars_core::spline_interpolate; native accepts order in [1,3));
_coverage.py SplineInterpolator verdict note (13 failures; root causes: no y=None on fit, output grid set at fit time breaking idempotency, order out of native [1,3) range; fix = output grid constructor param + y=None + guard order to [1,3) with sklearn-convention message).
  </read_first>
  <action>
Bring SplineInterpolator to full green per the XFORM-03 verdict. The output-grid-as-constructor-param and y=None on fit are already present in the skeleton — VERIFY they are correct (output_argvals stored verbatim in __init__, resolved to output_argvals_ ONLY in fit; fit signature is `fit(self, X, y=None)`) and fix if the skeleton drifted. The remaining fix is the spline-order guard: in fit, after argvals_ resolution, validate that self.order is within the native-supported range [1, 3) and raise a ValueError with a sklearn-convention message when it is not — BEFORE storing/using it. Note the default order=4 in the current skeleton is OUTSIDE native [1,3); change the constructor default to a native-valid order (order=3, cubic-equivalent upper bound is exclusive so use the highest valid integer the native range permits) and update the docstring accordingly, so the default estimator constructs and transforms without tripping its own guard. Keep transform idempotent (same output grid across repeated transforms). Do NOT construct an Fdata; touch only the SplineInterpolator class.
  </action>
  <verify>
    <automated>.venv/bin/pytest "tests/sklearn/test_transformers_compliance.py::test_spline_interpolator_compliance" -q</automated>
  </verify>
  <done>SplineInterpolator passes every parametrize_with_checks case: check_fit_score_takes_y (y=None), check_methods_subset_invariance + check_transformer_general (idempotent output grid), and the order guard rejects out-of-[1,3) orders with a sklearn-convention message; default constructor is native-valid; no Fdata constructed.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two compliance tests and flip both verdicts to PASS</name>
  <files>tests/sklearn/test_transformers_compliance.py, python/fdars/sklearn/_coverage.py</files>
  <read_first>
tests/sklearn/test_transformers_compliance.py (the per-transformer parametrize_with_checks pattern and the Plan-01 placeholders for basis + spline);
python/fdars/sklearn/_coverage.py (TRIAGE_VERDICTS BasisRepresentation and SplineInterpolator entries + the running tally comment updated by Plan 01 to 7 PASS / 21 PASS-WITH-FIXES);
tests/sklearn/test_coverage.py (TestVerdictDomain — bare "PASS" is a valid prefix).
  </read_first>
  <action>
In test_transformers_compliance.py add the two per-transformer tests filling the Plan-01 placeholders: test_basis_representation_compliance (BasisRepresentation(n_basis=3)) and test_spline_interpolator_compliance (SplineInterpolator()), each a `@parametrize_with_checks([...])`-decorated function whose body is `check(estimator)`. In _coverage.py flip TRIAGE_VERDICTS["BasisRepresentation"] and TRIAGE_VERDICTS["SplineInterpolator"] to "PASS", and update the tally comment to 9 PASS / 19 PASS-WITH-FIXES (all 8 transformers now PASS). Do NOT change any non-transformer verdict or EXCLUDED_METHODS.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_transformers_compliance.py tests/sklearn/test_coverage.py -q && .venv/bin/python -c "from fdars.sklearn._coverage import TRIAGE_VERDICTS as v; assert v['BasisRepresentation']=='PASS' and v['SplineInterpolator']=='PASS', v"</automated>
  </verify>
  <done>Both new compliance tests pass; TRIAGE_VERDICTS["BasisRepresentation"] and ["SplineInterpolator"] are "PASS"; test_coverage.py green; tally comment shows all 8 transformers PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller -> sklearn estimator | Arbitrary ndarrays (incl. 1-feature, out-of-range spline order) supplied by the check_estimator battery |
| estimator -> fdars._native | Validated float64 dense arrays + a native-valid spline order cross into the Rust binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-03 | Tampering | BasisRepresentation.fit / SplineInterpolator.fit input+param validation | low | mitigate | Python-layer 1-feature guard and spline-order [1,3) guard reject non-conforming input at the wrapper with sklearn-convention errors before the native call |
| T-56-04 | Denial of Service | native spline_interpolate on out-of-range order | low | mitigate | order guard rejects orders outside native [1,3) before the native call, preventing an unhandled native error path |
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_transformers_compliance.py -q` — all 8 per-transformer batteries green.
- `.venv/bin/pytest tests/sklearn/test_coverage.py tests/sklearn/test_triage.py -q` — registry + triage harness still green.
- `.venv/bin/python -c "import inspect; from fdars.sklearn._skeletons import BasisRepresentation, SplineInterpolator; assert 'Fdata(' not in inspect.getsource(BasisRepresentation) and 'Fdata(' not in inspect.getsource(SplineInterpolator)"` — neither constructs an Fdata.
</verification>

<success_criteria>
BasisRepresentation (XFORM-04) and SplineInterpolator (XFORM-03) are full-battery green with the exact Phase-55 triage fixes; both have per-transformer compliance tests; both verdicts are PASS. All 8 transformers now PASS.
</success_criteria>

<output>
Create `.planning/phases/56-transformers/56-02-SUMMARY.md` when done.
</output>
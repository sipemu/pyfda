---
phase: 67-functional-time-series-fts
plan: 04
type: execute
wave: 4
depends_on: [67-03]
files_modified:
  - src/fts_mod.rs
  - tests/test_fts.py
autonomous: true
requirements: [FTS-03]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "fdars.fts.spectral_density returns {freqs (N,), re (list of N (m,m) arrays), im, m, n_curves, bandwidth} (FTS-03)"
    - "fdars.fts.dpca returns {filters (list), scores (N-2L, ncomp), eigenvalues (list), n_freqs, filter_lag, ncomp, valid_range (tuple)} (FTS-03)"
    - "fdars.fts.dpca_reconstruct returns the dpca keys merged with fitted_reconstruction (N-2L, m) and reconstruction_error (ncomp,) (FTS-03)"
  artifacts:
    - "src/fts_mod.rs — spectral_density, dpca, dpca_reconstruct #[pyfunction]s + register() lines"
    - "tests/test_fts.py — spectral/DR-family tests"
  key_links:
    - "SpectralDensityResult.re/im are Vec<Vec<f64>>, each inner Vec flat column-major (m,m) — reshape per frequency via FdMatrix::from_column_major, emit as a Python list of (m,m) arrays"
    - "dpca_reconstruct uses the COMBINED-FUNCTION pattern: fit dpca internally, then call dpca_reconstruct — no #[pyclass] handle"
    - "DpcaResult.valid_range is (usize,usize) → Python tuple; scores rows = N - 2*filter_lag (assert against result[filter_lag], don't hardcode)"
---

<objective>
EXPANSION (FTS-03 spectral / dimension-reduction): complete the fts submodule with the spectral family —
`spectral_density`, `dpca`, `dpca_reconstruct` — the last three of the 13 functions.

Purpose: These carry the two remaining structural gotchas from 67-RESEARCH.md: the
`SpectralDensityResult.re/im` `Vec<Vec<f64>>` (per-frequency flat column-major (m,m) matrices → list of
numpy 2D), and the `&DpcaResult` struct-ref input to `dpca_reconstruct` (handled by the same
COMBINED-FUNCTION pattern as the ftsm forecast family — fit `dpca` internally, then reconstruct).

Output: Three new `#[pyfunction]`s appended to `src/fts_mod.rs` and registered; spectral/DR tests
appended to `tests/test_fts.py`. After this plan all 13 fts functions are bound and FTS-03 is complete.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-functional-time-series-fts/67-RESEARCH.md
</context>

<artifacts_produced>
- `#[pyfunction] spectral_density` — `spectral_density(data, argvals, bandwidth=None)` → PyDict {freqs (N,), re (list of (m,m)), im (list of (m,m)), m, n_curves, bandwidth}
- `#[pyfunction] dpca` — `dpca(data, argvals, ncomp=3, bandwidth=None, filter_lag=None)` → PyDict {filters (list), scores (N-2L, ncomp), eigenvalues (list), n_freqs, filter_lag, ncomp, valid_range (tuple)}
- `#[pyfunction] dpca_reconstruct` — `dpca_reconstruct(data, argvals, ncomp=3, bandwidth=None, filter_lag=None)` → merged PyDict (dpca keys + fitted_reconstruction (N-2L, m), reconstruction_error (ncomp,))
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind spectral_density (Vec&lt;Vec&lt;f64&gt;&gt; per-frequency reshape)</name>
  <files>src/fts_mod.rs</files>
  <read_first>
    - .planning/phases/67-functional-time-series-fts/67-RESEARCH.md (§3 spectral_density signature; §4 SpectralDensityResult fields; §10 item 3 re/im per-frequency reshape; §11 spectral_density key table; Pitfall 3 bandwidth=Some(0) rejected)
    - src/fts_mod.rs (long_run_covariance from 67-03 for the FdMatrix::from_column_major reshape pattern)
    - src/convert.rs (fdmatrix_to_numpy2d, vec_to_numpy1d, to_pyerr, to_pyresult)
  </read_first>
  <behavior>
    - `spectral_density(data, argvals, bandwidth=None)` returns PyDict {"freqs" (N,), "re" (Python list of
      N numpy (m,m) arrays), "im" (same), "m" int, "n_curves" int, "bandwidth" int}.
    - On the non-square fixture (N=40, M=25): freqs shape (40,), re is a list of length 40, each element
      shape (25, 25), n_curves==40, m==25.
    - `bandwidth=0` raises ValueError (upstream rejects Some(0) for spectral_density).
  </behavior>
  <action>
Append the `spectral_density` `#[pyfunction]` to `src/fts_mod.rs`: signature `(data, argvals,
bandwidth=None)` with `bandwidth: Option<usize>`. Body: convert data+argvals,
`let result = to_pyresult(fdars_core::fts::spectral_density(&mat, &av, bandwidth))?;`.

Handle re/im per 67-RESEARCH.md §10 item 3: `let m = result.m;`, build a `PyList` for "re" by iterating
`&result.re` — for each flat column-major inner Vec, `let fd = fdars_core::matrix::FdMatrix::
from_column_major(freq.clone(), m, m).map_err(to_pyerr)?;` then `re_list.append(fdmatrix_to_numpy2d(py,
&fd))?;`. Repeat for "im". Set "freqs" (vec_to_numpy1d result.freqs), "m", "n_curves", "bandwidth". This
yields Python lists of (m,m) arrays that users can `np.stack(...)` to (N,m,m).

Add the `wrap_pyfunction!` line to `register()`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.fts as f; assert callable(f.spectral_density); print('spectral_density bound OK')"</automated>
    <fails_when>maturin non-zero (compile error in the per-frequency reshape), AttributeError on spectral_density, or the assert fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `spectral_density` is bound and registered; build is green.
    - The re/im per-frequency `Vec<Vec<f64>>` reshape emits Python lists of (m,m) arrays.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive function in a new module; independently removable.</reversibility>
  <done>`spectral_density` compiles, imports, and is callable with the per-frequency reshape handled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind dpca + dpca_reconstruct (combined-function) + spectral/DR tests</name>
  <files>src/fts_mod.rs, tests/test_fts.py</files>
  <read_first>
    - .planning/phases/67-functional-time-series-fts/67-RESEARCH.md (§3 dpca + dpca_reconstruct signatures; §6 COMBINED-FUNCTION for &DpcaResult; §4 DpcaResult + DpcaReconstruction fields; §10 items 4,5,6 filters/eigenvalues/valid_range conversions; §11 dpca + dpca_reconstruct tables; §12 test items 11,12,13; Pitfall 6 filter_lag default → scores rows = N-2L)
    - src/fts_mod.rs (spectral_density from Task 1; ftsm_forecast combined-function pattern from 67-02)
    - tests/test_fts.py (non-square fixture)
  </read_first>
  <behavior>
    - `dpca(data, argvals, ncomp=3, bandwidth=None, filter_lag=None)` returns {"filters" (Python list of
      ncomp numpy 2D), "scores" (N-2L, ncomp), "eigenvalues" (Python list of ncomp numpy 1D), "n_freqs"
      int, "filter_lag" int, "ncomp" int, "valid_range" (int,int) tuple}.
    - `dpca_reconstruct(data, argvals, ncomp=3, bandwidth=None, filter_lag=None)` returns the dpca keys
      merged with "fitted_reconstruction" (N-2L, m) and "reconstruction_error" (ncomp,).
    - On the non-square fixture: scores rows == N - 2*result["filter_lag"] (do NOT hardcode); filters list
      length == ncomp; valid_range is a 2-tuple; reconstruction_error length == ncomp and is
      monotone non-increasing; fitted_reconstruction shape (N-2L, 25).
  </behavior>
  <action>
Append two `#[pyfunction]`s to `src/fts_mod.rs`.

`dpca`: signature `(data, argvals, ncomp=3, bandwidth=None, filter_lag=None)` with `ncomp: usize`,
`bandwidth: Option<usize>`, `filter_lag: Option<usize>`. Body: convert data+argvals,
`let result = to_pyresult(fdars_core::fts::dpca(&mat, &av, ncomp, bandwidth, filter_lag))?;`. Build the
PyDict per 67-RESEARCH.md §10 items 4,5,6: "filters" = PyList of `fdmatrix_to_numpy2d(py, f)` over
`&result.filters`; "scores" = fdmatrix_to_numpy2d(&result.scores); "eigenvalues" = PyList of
`vec_to_numpy1d(py, ev)` over `result.eigenvalues`; "n_freqs", "filter_lag", "ncomp" ints;
"valid_range" = `(result.valid_range.0, result.valid_range.1)` (a Python 2-tuple). Consider a private
helper `fn dpca_result_to_dict(py, &DpcaResult) -> PyResult<Bound<PyDict>>` reused by both dpca and
dpca_reconstruct.

`dpca_reconstruct`: identical signature. Use the COMBINED-FUNCTION pattern (67-RESEARCH.md §6): fit
`let dp = to_pyresult(fdars_core::fts::dpca(&mat, &av, ncomp, bandwidth, filter_lag))?;` then
`let recon = to_pyresult(fdars_core::fts::dpca_reconstruct(&mat, &av, &dp))?;`. Build the dpca dict
(reuse the helper) and ADD "fitted_reconstruction" (fdmatrix_to_numpy2d &recon.fitted) and
"reconstruction_error" (vec_to_numpy1d recon.reconstruction_error). Do NOT deserialize a Python dict
into a DpcaResult and do NOT add a #[pyclass] handle.

Add both `wrap_pyfunction!` lines to `register()`.

Append spectral/DR tests to `tests/test_fts.py` reusing the non-square fixture, covering the behaviors
above. For scores rows assert `dp["scores"].shape[0] == N - 2*dp["filter_lag"]` (Pitfall 6 — never
hardcode the interior count). For reconstruction_error monotonicity use
`np.all(np.diff(err) <= 1e-12)`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -m pytest tests/test_fts.py -x -q 2>&1 | tail -20</automated>
    <fails_when>maturin non-zero (e.g. cannot convert PyDict to DpcaResult — combined-function pattern not used), any pytest failure/error, scores/fitted_reconstruction shape mismatch, filters/eigenvalues list length != ncomp, reconstruction_error not monotone non-increasing, or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `dpca` and `dpca_reconstruct` are bound, registered, build green; all 13 fts functions now registered.
    - dpca scores rows equal `N - 2*filter_lag` (computed, not hardcoded); valid_range is a tuple.
    - dpca_reconstruct returns the merged dict with fitted_reconstruction + monotone reconstruction_error.
    - `pytest tests/test_fts.py -x -q` exits 0.
  </acceptance_criteria>
  <done>The spectral/DR family completes the 13-function fts binding (FTS-03); the full suite is green on the non-square fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust (PyO3) | data numpy arrays cross into `numpy2d_to_fdmatrix`; nested Vec<Vec>/Vec<FdMatrix> reshaped back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-08 | Tampering | spectral_density re/im per-frequency col-major reshape | medium | mitigate | reshape each inner Vec via `FdMatrix::from_column_major(freq, m, m)`; shape assertions on the (m,m) list catch a wrong reshape |
| T-67-09 | Tampering | dpca/dpca_reconstruct interior-range and combined-fit | low | mitigate | combined-function pattern (fit dpca internally); scores-rows assertion computed from filter_lag catches an interior-count bug |
| T-67-10 | Denial of Service | bandwidth=Some(0) / invalid ncomp/filter_lag | low | mitigate | upstream validates (spectral_density rejects Some(0)) → ValueError via `to_pyresult` |
| T-67-SC | Tampering | npm/pip/cargo installs | low | accept | no new dependencies this phase; no install tasks |
</threat_model>

<verification>
- `maturin develop` green after each task.
- All 13 fts functions bound and registered.
- `pytest tests/test_fts.py -x -q` exits 0 with spectral/DR shape + monotonicity checks passing.
</verification>

<success_criteria>
- FTS-03: `fplsr` (67-02), `spectral_density`, `dpca`, `dpca_reconstruct` each return documented PyDicts; the full 13-function fts submodule is complete and green.
</success_criteria>

<output>
Create `.planning/phases/67-functional-time-series-fts/67-04-SUMMARY.md` when done.
</output>

---
phase: 67-functional-time-series-fts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fts_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_fts.py
autonomous: true
requirements: [FTS-01]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "import fdars.fts works and fdars.fts.ftsm is callable (FTS-01)"
    - "ftsm on non-square (n_obs=40, n_points=25) input returns a PyDict whose shapes prove no row/col transposition: mean (25,), rotation (25,3), scores (40,3), fitted (40,25) (FTS-01)"
    - "ar_models is a list of ncomp dicts each with keys order, phi, sigma2 (FTS-01)"
  artifacts:
    - "src/fts_mod.rs — new PyO3 submodule with register() and the ftsm #[pyfunction]"
    - "tests/test_fts.py — non-square fixture + ftsm shape assertions"
  key_links:
    - "src/lib.rs registers the fts submodule via register_submodule!(m, \"fts\", fts_mod::register)"
    - "python/fdars/__init__.py adds \"fts\" to _submodule_names so import fdars.fts resolves"
---

<objective>
TRACER SLICE (FTS-01): Stand up the new `fdars.fts` submodule end-to-end by binding ONE
representative function — `ftsm` — through every layer the phase will touch: new
`src/fts_mod.rs` → `register()` → `src/lib.rs` submodule registration → `python/fdars/__init__.py`
name list → a `tests/test_fts.py` non-square fixture that proves transposition-correctness.

Purpose: Prove the registration + `numpy2d_to_fdmatrix` conversion + PyDict assembly + test
harness on the agent's best early-context tokens, BEFORE fanning out to the remaining 12
functions in plans 67-02/03/04. If the module wiring or the FdMatrix→numpy shape mapping is
wrong, this catches it after one commit instead of after all 13 bindings.

Output: A compiled, importable `fdars.fts` submodule with `ftsm` working and its non-square
test green. `src/fts_mod.rs` becomes the skeleton the remaining bindings expand into.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-functional-time-series-fts/67-RESEARCH.md
</context>

<artifacts_produced>
This plan produces the following NEW symbols (the rest land in 67-02/03/04):
- Python submodule `fdars.fts` (registered, importable)
- `#[pyfunction] ftsm` (Python signature `ftsm(data, argvals, ncomp=3)`)
- `pub fn fts_mod::register(m: &Bound<'_, PyModule>) -> PyResult<()>`
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "import fdars.fts + ftsm" — one path through every layer</name>
  <files>src/fts_mod.rs, src/lib.rs, python/fdars/__init__.py</files>
  <read_first>
    - .planning/phases/67-functional-time-series-fts/67-RESEARCH.md (§3 ftsm signature; §4 FtsmResult + ArModelResult fields; §8 registration edits; §10 item 1 ar_models list-of-dicts; §11 ftsm key table; §9 module template)
    - src/regression_mod.rs (lines 1-46 — canonical 2D-input + required argvals + PyDict return analog for fpca)
    - src/pace_fpca_mod.rs (analog module: PyDict + PyList assembly patterns)
    - src/convert.rs (numpy2d_to_fdmatrix, fdmatrix_to_numpy2d, vec_to_numpy1d, numpy1d_to_vec, to_pyresult)
    - src/lib.rs (register_submodule! macro at lines 32-38; registration block lines 43-62)
    - python/fdars/__init__.py (_submodule_names tuple lines 34-55)
  </read_first>
  <action>
Create NEW file `src/fts_mod.rs` starting with a `//!` module doc comment, `use crate::convert::*;`,
`use numpy::{PyReadonlyArray1, PyReadonlyArray2};`, `use pyo3::prelude::*;`, and
`use pyo3::types::{PyDict, PyList};`.

Bind ONLY `ftsm` in this tracer. Signature per 67-RESEARCH.md §3 and §7: `#[pyfunction]` with
`#[pyo3(signature = (data, argvals, ncomp=3))]`, params `py: Python<'py>`,
`data: PyReadonlyArray2<'py, f64>`, `argvals: PyReadonlyArray1<'py, f64>`, `ncomp: usize`, returning
`PyResult<Bound<'py, PyDict>>`. Body: `let mat = numpy2d_to_fdmatrix(data)?;`,
`let av = numpy1d_to_vec(argvals);`, `let result = to_pyresult(fdars_core::fts::ftsm(&mat, ncomp, &av))?;`.
argvals is REQUIRED (do NOT use default_grid) — upstream validates argvals.len() == n_points.

Build the PyDict with exactly the keys from 67-RESEARCH.md §11 ftsm table: "mean" (vec_to_numpy1d
from result.mean), "rotation" (fdmatrix_to_numpy2d &result.rotation), "scores"
(fdmatrix_to_numpy2d &result.scores), "fitted" (fdmatrix_to_numpy2d &result.fitted), "weights"
(vec_to_numpy1d result.weights), "ncomp" (result.ncomp usize). For "ar_models" build a PyList where
each element is a PyDict with keys "order" (ar.order), "phi" (vec_to_numpy1d ar.phi.clone()), "sigma2"
(ar.sigma2), iterating `&result.ar_models` per 67-RESEARCH.md §10 item 1. Return `Ok(dict)`.

Add a `pub fn register(m: &Bound<'_, PyModule>) -> PyResult<()>` that registers only ftsm this tracer:
`m.add_function(wrap_pyfunction!(ftsm, m)?)?; Ok(())`. Plans 67-02/03/04 will append the remaining
wrap_pyfunction! lines here.

Edit `src/lib.rs`: add `mod fts_mod;` in the module-declaration block (alphabetical/after tolerance_mod
is fine), and add `register_submodule!(m, "fts", fts_mod::register);` after the pace_fpca line (lib.rs:62).

Edit `python/fdars/__init__.py`: add `"fts",` to the `_submodule_names` tuple after `"pace_fpca"`
(with a `# Phase 67 — Functional Time Series` comment), and add one bullet to the module docstring
mentioning functional time series (FTSM fit/forecast, ACF, stationarity, DPCA).

Do NOT add any pure-Python layer for fts (thin native wrappers only, per CONTEXT.md discretion).
Do NOT touch Cargo.toml/Cargo.lock (Cargo.lock is gitignored — never git add it).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -5 && .venv/bin/python -c "import fdars.fts; assert callable(fdars.fts.ftsm); print('ftsm bound OK')"</automated>
    <fails_when>maturin develop returns non-zero (Rust compile error), or ModuleNotFoundError on import fdars.fts, or AttributeError on fdars.fts.ftsm, or the assert fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/fts_mod.rs` exists with a `//!` doc comment, the `ftsm` `#[pyfunction]`, and `pub fn register`.
    - `maturin develop` builds green with no warnings-as-errors failures.
    - `import fdars.fts` succeeds and `fdars.fts.ftsm` is callable.
    - `src/lib.rs` has `mod fts_mod;` and the `register_submodule!(m, "fts", fts_mod::register)` line.
    - `python/fdars/__init__.py` `_submodule_names` contains `"fts"`.
  </acceptance_criteria>
  <reversibility rating="reversible">New additive module + registration lines; fully removable by deleting the file and reverting two edits.</reversibility>
  <done>`import fdars.fts` works and `ftsm` is a callable native function; the submodule is wired end-to-end and the build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-square ftsm transposition test — prove the shape mapping</name>
  <files>tests/test_fts.py</files>
  <read_first>
    - .planning/phases/67-functional-time-series-fts/67-RESEARCH.md (§12 Test Architecture — non-square fixture + ftsm shape assertions; §5 non-square rationale)
    - tests/test_advisor_group_b.py (existing test style/imports in this repo, for reference only)
  </read_first>
  <behavior>
    - `import fdars.fts as fts` and `from fdars.fts import ftsm` both succeed (import smoke).
    - Fixture is deliberately NON-SQUARE: N=40 observations, M=25 grid points, with `assert N != M`.
      Build AR(1)-driven curves on `argvals = np.linspace(0.0, 1.0, M)` seeded with numpy default_rng(42)
      per 67-RESEARCH.md §12; `data.shape == (40, 25)`.
    - `r = fts.ftsm(data, argvals, ncomp=3)` returns a dict with keys
      {"mean","rotation","scores","fitted","weights","ncomp","ar_models"}.
    - Shape assertions that FAIL if data/result is transposed: `r["mean"].shape == (25,)`,
      `r["rotation"].shape == (25, 3)`, `r["scores"].shape == (40, 3)`, `r["fitted"].shape == (40, 25)`,
      `r["weights"].shape == (25,)`.
    - `len(r["ar_models"]) == 3` and each element's keys equal `{"order","phi","sigma2"}`.
    - Error guard: `fts.ftsm(data, argvals, ncomp=0)` raises `ValueError` (upstream rejects ncomp < 1).
  </behavior>
  <action>
Create NEW file `tests/test_fts.py`. Put the non-square AR(1) fixture (N=40, M=25) into a module-level
helper or pytest fixture exactly as specified in 67-RESEARCH.md §12 (do NOT use a square N==M fixture —
square shapes hide row/col swap bugs). Write the ftsm test with the shape assertions and ar_models key
assertions above, plus the ncomp=0 `pytest.raises(ValueError)` guard. Keep this file focused on ftsm
only for the tracer; plans 67-02/03/04 will APPEND their own test functions to this same file. Use the
`assert N != M` guard line so a future accidental square edit fails loudly.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_fts.py -x -q 2>&1 | tail -15</automated>
    <fails_when>pytest reports any failure/error, or a shape assertion fails (indicating transposition), or the ncomp=0 guard does not raise ValueError, or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_fts.py` exists and contains an `assert N != M` non-square fixture (40 × 25).
    - The ftsm test passes with all shape assertions on non-square data.
    - The `ncomp=0` guard asserts `ValueError` is raised.
    - `pytest tests/test_fts.py -x -q` exits 0.
  </acceptance_criteria>
  <done>The ftsm binding is proven transposition-correct on non-square input, and the shared test file + fixture exist for the expansion plans to extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust (PyO3) | numpy arrays cross into `numpy2d_to_fdmatrix`; only shape/dtype trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-01 | Tampering | ftsm numpy→FdMatrix conversion | low | mitigate | route all 2D input through `numpy2d_to_fdmatrix`; non-square fixture proves no silent transposition |
| T-67-02 | Denial of Service | invalid ncomp/argvals | low | mitigate | upstream fdars-core validates (InvalidDimension/InvalidParameter) → propagated as ValueError via `to_pyresult`; guard test asserts ncomp=0 raises |
| T-67-SC | Tampering | npm/pip/cargo installs | low | accept | no new dependencies added this phase (RESEARCH §Standard Stack: none); no install tasks, no legitimacy gate required |
</threat_model>

<verification>
- `maturin develop` builds green.
- `import fdars.fts` and `fdars.fts.ftsm` callable.
- `pytest tests/test_fts.py -x -q` exits 0 with non-square shape assertions passing.
</verification>

<success_criteria>
- FTS-01 (partial — ftsm fit path): import works; ftsm returns a transposition-correct PyDict on non-square input.
- The `fts_mod.rs` skeleton + shared `tests/test_fts.py` fixture exist for 67-02/03/04 to expand.
</success_criteria>

<output>
Create `.planning/phases/67-functional-time-series-fts/67-01-SUMMARY.md` when done.
</output>

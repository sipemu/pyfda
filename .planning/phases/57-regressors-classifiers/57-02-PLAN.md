---
phase: 57-regressors-classifiers
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_regressors_compliance.py
  - tests/sklearn/test_classifiers_compliance.py
autonomous: true
requirements: [REG-02, CLF-01, CLF-02]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "RobustFPCRegressor, GLMRegressor (Gaussian), NonparametricRegressor are check_estimator-green RegressorMixin estimators (REG-02)"
    - "FPCLDAClassifier, FPCQDAClassifier, FPCKNNClassifier, LogisticFPCClassifier are check_estimator-green ClassifierMixin estimators using LabelEncoder in fit (CLF-01)"
    - "DDClassifier and ElasticMultinomialClassifier are check_estimator-green where triage passed (CLF-02)"
    - "Every classifier predict is stored-model / reconstructed-in-numpy — predict(X[mask]) == predict(X)[mask] (no vstack of train+test)"
  artifacts:
    - "tests/sklearn/test_classifiers_compliance.py (per-estimator parametrize_with_checks)"
    - "test_regressors_compliance.py extended with Robust/GLM/Nonparametric batteries"
    - "_coverage.py verdicts flipped to PASS for all 3 differentiator regressors + 6 classifiers"
  key_links:
    - "RobustFPCRegressor.predict -> predict_fregre_robust(X_fit_, y_fit_, X_new, ...) (subset-invariant native predict)"
    - "GLMRegressor/NonparametricRegressor predict reconstruct from stored representation WITHOUT vstacking train+test"
    - "LDA/QDA/KNN/DD classifier predict reconstruct labels in numpy from stored FPC scores / stored-distance native funcs (transductive-only natives)"
    - "LogisticFPCClassifier.predict -> predict_functional_logistic(X_fit_, y_fit_, X_new, ...) with LabelEncoder to {0,1}"
---

<objective>
Expand the stored-model-predict pattern proven in Plan 01 across the differentiator regressors and the full FPC classifier family, promoting each to `check_estimator`-green under a per-estimator `parametrize_with_checks` battery. Delivers REG-02, CLF-01, CLF-02.

Purpose: the native classification functions (`fclassif_lda/qda/knn/dd`) are transductive-only (fit+predict on the whole batch, no separate predict entrypoint), so the vstack anti-pattern in `_BaseFdarsClassifier` contaminates train with test and always predicts class 0 on subsets. This plan replaces it with genuine stored-model / numpy-reconstructed predict per the CONTEXT strategy.
Output: 3 differentiator regressors + 6 classifiers green; their `_coverage.py` verdicts flipped to PASS; compliance test files extended/created.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_base.py
@python/fdars/sklearn/_coverage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Differentiator regressors — Robust / GLM(Gaussian) / Nonparametric green (REG-02)</name>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 1062-1310 (RobustFPCRegressor, GLMRegressor, NonparametricRegressor — GLM and Nonparametric currently vstack train+test in predict; Robust already calls predict_fregre_robust on stored train);
    src/regression_mod.rs lines 558-582 (predict_fregre_robust: data, response, new_data — fits on train only);
    src/regression_mod.rs lines 190-216 (fregre_np: takes a precomputed distance matrix, no separate predict entrypoint);
    _coverage.py TRIAGE_VERDICTS lines 206-222 (failing checks per estimator).
  </read_first>
  <behavior>
    - parametrize_with_checks green for RobustFPCRegressor, GLMRegressor, NonparametricRegressor (no exemptions).
    - check_methods_subset_invariance: predict(X[mask]) == predict(X)[mask] for all three.
    - check_regressors_train: R2 > 0.5 on battery data.
    - check_non_transformer_estimators_n_iter: GLMRegressor exposes n_iter_ set in fit.
    - check_requires_y_none: all three raise the sklearn-convention required-y ValueError.
  </behavior>
  <action>
    RobustFPCRegressor: predict already uses `predict_fregre_robust(X_fit_, y_fit_, X_new, ...)` (stored-model, subset-invariant) — add the y=None guard (reuse Plan-01 helper) and confirm R2>0.5; do NOT vstack.

    GLMRegressor: replace the vstack-in-predict (currently `functional_glm(vstack([X_fit_, X]), concat([y_fit_, zeros]))` then slice) with a reconstructed linear predictor from the STORED fitted model. Gaussian GLM has an identity link, so predict = intercept_ + FPC-projection of centered X_new onto the stored beta representation. Store enough at fit time (intercept, beta_t / FPC eigenfunctions + coefficients, training mean) to compute the linear predictor on X_new alone. Add n_iter_ (from the native result's iteration count, or the configured max_iter if the native does not return it) and the y=None guard. Verify subset-invariance holds because predict now touches only stored fit state + X_new.

    NonparametricRegressor: keep the distance-matrix approach but make predict use ONLY the stored training data as neighbours — compute `_pairwise_l2(X_new, X_fit_)` (new-vs-train, NOT the augmented vstack matrix) and the stored fitted kernel/bandwidth to produce Nadaraya-Watson estimates for each new point independently. This is subset-invariant (each new point's prediction depends only on stored train, never on sibling test points). Add the y=None guard. Document the stored representation in the predict docstring.

    Do not put any verbatim negative-grep acceptance token in a comment.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py::test_robust_fpc_regressor_compliance tests/sklearn/test_regressors_compliance.py::test_glm_regressor_compliance tests/sklearn/test_regressors_compliance.py::test_nonparametric_regressor_compliance -q</automated>
  </verify>
  <done>All three differentiator regressors pass every parametrize_with_checks check; none vstack train+test in predict; GLMRegressor exposes n_iter_.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FPC classifiers — LDA / QDA / KNN / DD stored-model predict (CLF-01, CLF-02)</name>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 1318-1497 (_BaseFdarsClassifier vstack anti-pattern + FPCLDA/QDA/KNN/DD subclasses);
    src/classification_mod.rs lines 22-83 (fclassif_lda/qda/knn: transductive — data, labels, ncomp[, k] → predicted for ALL rows, no separate predict);
    src/classification_mod.rs lines 175-267 (fclassif_dd transductive; knn_classify_from_distances + kernel_classify_from_distances: predict from a precomputed distance matrix — the stored-distance path);
    FPCATransformer fit/transform lines 118-264 (FPC scores + sign-canonicalization to reuse for the stored FPC representation).
  </read_first>
  <behavior>
    - parametrize_with_checks green for FPCLDAClassifier, FPCQDAClassifier, FPCKNNClassifier, DDClassifier.
    - check_methods_subset_invariance: predict(X[mask]) == predict(X)[mask] (each test point classified independently of siblings).
    - check_classifiers_train: accuracy >= 0.83 on battery data.
    - check_classifiers_regression_target: FPCKNN raises on continuous-target y (label-type validation via check_type_of_target).
    - check_requires_y_none: all raise the sklearn-convention required-y ValueError.
    - LabelEncoder used in fit; classes_ stored; predict inverse-transforms to original labels.
  </behavior>
  <action>
    The native fclassif_lda/qda/knn/dd are transductive-only (no separate predict entrypoint), so the vstack-then-slice approach in _BaseFdarsClassifier is the root cause of the subset-invariance + always-class-0 failures. Replace predict with a genuine stored-model / numpy-reconstructed classifier per method:

    - FPCLDA / FPCQDA: at fit, compute the FPC scores of the training data (reuse FPCATransformer's fpca extraction + sign-canonicalization to get a stable eigenbasis + training scores). Store the eigenbasis, training mean, training FPC scores, and encoded labels. In predict, project X_new onto the stored eigenbasis to get test FPC scores, then classify each test point independently using LDA/QDA discriminant functions computed in numpy from the stored per-class means/covariances of the training FPC scores. This makes predict depend only on stored fit state + the single test point.

    - FPCKNN: store training FPC scores + encoded labels. In predict, project X_new onto the stored eigenbasis and use knn_classify_from_distances with a distance matrix of test-vs-train FPC-score distances (or a numpy kNN vote over stored training scores). Add label-type validation in fit (check_type_of_target rejecting continuous targets) + the y=None guard.

    - DDClassifier: store a depth-based representation of the training set per class; in predict, compute each test point's depth w.r.t. the stored per-class training samples and assign the deepest class — each test point independent of siblings. Reconstruct in numpy from stored per-class training data (no vstack of test into the reference set).

    Keep LabelEncoder in fit, store classes_ + label_encoder_, inverse_transform in predict. Preserve the ncomp capping `min(ncomp, n_obs-1, n_pts)`. Never construct Fdata.

    Restructure _BaseFdarsClassifier so its predict no longer vstacks; if the per-method stored representations diverge too much for a shared base, give each classifier its own fit/predict (Claude's discretion on the base-class shape).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_classifiers_compliance.py::test_fpc_lda_compliance tests/sklearn/test_classifiers_compliance.py::test_fpc_qda_compliance tests/sklearn/test_classifiers_compliance.py::test_fpc_knn_compliance tests/sklearn/test_classifiers_compliance.py::test_dd_compliance -q</automated>
  </verify>
  <done>LDA/QDA/KNN/DD classifiers pass every parametrize_with_checks check; predict is subset-invariant (no vstack of train+test); LabelEncoder in fit; FPCKNN validates label type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LogisticFPC + ElasticMultinomial classifiers + flip all verdicts to PASS (CLF-01, CLF-02)</name>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 1560-1659 (LogisticFPCClassifier — already uses predict_functional_logistic on stored train + LabelEncoder to float64 {0,1}; the 21-check cascade root cause is fit-returns-self / label domain);
    python/fdars/sklearn/_skeletons.py lines 1499-1557 (ElasticMultinomialClassifier — vstack predict + missing check_is_fitted + no 1-feature guard);
    src/regression_mod.rs lines 605-624 (predict_functional_logistic: stored train + new_data, subset-invariant);
    src/classification_mod.rs lines 297-360 (elastic_multinomial: transductive, labels non-negative 0-indexed; returns predicted_classes for all rows — no separate predict);
    _coverage.py TRIAGE_VERDICTS lines 242-256 (Logistic 21 failures; Elastic 10 failures).
  </read_first>
  <behavior>
    - parametrize_with_checks green for LogisticFPCClassifier (binary) and ElasticMultinomialClassifier.
    - LogisticFPC: fit returns self; LabelEncoder maps binary classes to {0,1} float64 for the native; check_estimators_fit_returns_self green (root of the cascade); multiclass documented as a limitation if native is binary-only.
    - Elastic: check_estimators_unfitted + check_fit_check_is_fitted (check_is_fitted before predict); check_fit2d_1feature (argvals>=2 guard with sklearn-convention message); check_methods_subset_invariance; n_iter_ exposed.
  </behavior>
  <action>
    LogisticFPCClassifier: it already predicts via `predict_functional_logistic(X_fit_, y_fit_, X_new, ...)` (stored-model). Confirm fit returns self (the cascade root) and that LabelEncoder maps the two classes to {0.0, 1.0} float64 before the native call. If the native is binary-only, add a fit-time guard that raises a clear ValueError on >2 classes and document the binary limitation in the class docstring (deferred multiclass, per CONTEXT — not a blocker).

    ElasticMultinomialClassifier: add check_is_fitted at the top of predict; add a 1-feature/argvals>=2 guard in fit raising a ValueError with the sklearn-convention n_features message before the native call; replace the vstack-then-slice predict with a stored-model reconstruction — store the fitted per-class OvR representation (or training FPC/basis scores + fitted coefficients) and classify each test point independently. The native elastic_multinomial is transductive (predicted_classes for all rows), so reconstruct the OvR logistic decision in numpy from the stored fit; expose n_iter_. Keep LabelEncoder → non-negative 0-indexed labels for the native.

    Then create tests/sklearn/test_classifiers_compliance.py (mirroring the transformer harness) with a per-estimator parametrize_with_checks function for LDA/QDA/KNN/DD (Task 2) + Logistic + Elastic. Extend test_regressors_compliance.py with the three differentiator-regressor batteries (Task 1). Flip _coverage.py TRIAGE_VERDICTS to "PASS" for RobustFPCRegressor, GLMRegressor, NonparametricRegressor, FPCLDAClassifier, FPCQDAClassifier, FPCKNNClassifier, DDClassifier, LogisticFPCClassifier, ElasticMultinomialClassifier, updating each comment to record the Phase-57 fix.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_classifiers_compliance.py -q && .venv/bin/python -c "from fdars.sklearn._coverage import TRIAGE_VERDICTS as v; want=['RobustFPCRegressor','GLMRegressor','NonparametricRegressor','FPCLDAClassifier','FPCQDAClassifier','FPCKNNClassifier','DDClassifier','LogisticFPCClassifier','ElasticMultinomialClassifier']; assert all(v[k]=='PASS' for k in want), {k:v[k] for k in want}"</automated>
  </verify>
  <done>Logistic + Elastic classifiers pass every parametrize_with_checks check; all 3 differentiator regressors + 6 classifiers read PASS in _coverage.py; compliance test files exist for regressors and classifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/battery -> estimator | sklearn's check_estimator supplies arbitrary numeric arrays + arbitrary integer/continuous labels; estimators validate, LabelEncode, and never construct Fdata |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-03 | Tampering | arbitrary label domains into native classifiers | low | mitigate | LabelEncoder in fit maps to native 0-indexed/{0,1} domain; classes_ stored; inverse_transform in predict |
| T-57-04 | Tampering | continuous target passed to a classifier | low | mitigate | FPCKNN label-type validation (check_type_of_target) rejects continuous targets |
| T-57-05 | Denial of Service | 1-feature / tiny-sample battery data | low | mitigate | argvals>=2 + _min_samples guards raising sklearn-convention ValueErrors before native calls |

No package installs in this plan — no supply-chain threat.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_regressors_compliance.py tests/sklearn/test_classifiers_compliance.py -q` green.
- No vstack(train,test) in any regressor or classifier predict.
- All 9 estimators (3 regressors + 6 classifiers) read PASS in _coverage.py.
</verification>

<success_criteria>
Differentiator regressors green (REG-02, criterion 2); FPC classifiers green with LabelEncoder + stored-model predict (CLF-01, criterion 3); DD + elastic-multinomial green where triage passed (CLF-02, criterion 3).
</success_criteria>

<output>
Create `.planning/phases/57-regressors-classifiers/57-02-SUMMARY.md` when done.
</output>
---
phase: 57-regressors-classifiers
plan: 03
type: execute
wave: 3
depends_on: [57-02]
files_modified:
  - tests/sklearn/test_predictive_pipeline.py
autonomous: true
requirements: [PRED-01]
estimate:
  tokens: 40000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PRED-01: a Pipeline([imputer, smoother, fpca, classifier]) wrapped in GridSearchCV fits and predicts end-to-end on held-out data"
    - "GridSearchCV searches over pipeline-stage hyperparameters (stage__param keys) and selects a best_estimator_ that predicts on held-out data"
    - "A regressor Pipeline([imputer, smoother, fpca, FPCRegressor]) fits, predicts a finite float array, and scores a finite value"
    - "The full sklearn compliance suite (regressors + classifiers + transformers) is green together — no cross-plan regression"
  artifacts:
    - "tests/sklearn/test_predictive_pipeline.py"
  key_links:
    - "Pipeline stage names imputer->smoother->fpca->classifier; GridSearchCV param_grid uses stage__param keys (e.g. fpca__n_components, clf__ncomp)"
    - "FPCATransformer maps (n_obs,n_points) curves to (n_obs,n_components) scores; the downstream classifier consumes those scores as its (n_obs, n_features) input and resolves argvals_ from that width in fit"
---

<objective>
Prove the full predictive story: a functional-data `Pipeline([imputer, smoother, fpca, classifier])` driven by `GridSearchCV` fits and predicts end-to-end. Delivers PRED-01 (success criterion 4).

Purpose: this is the phase capstone — it demonstrates the regressors/classifiers built in Plans 01-02 compose with the Phase-56 transformers under sklearn's hyperparameter search, which is the milestone's core value ("plug natively into Pipeline/GridSearchCV").
Output: a fast, offline test that constructs the pipeline, runs GridSearchCV over stage hyperparameters, and asserts fit + predict succeed on held-out data; plus a regressor-pipeline smoke and a whole-suite regression guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@python/fdars/sklearn/_skeletons.py
@tests/sklearn/test_transformer_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline([imputer, smoother, fpca, classifier]) under GridSearchCV (PRED-01)</name>
  <files>tests/sklearn/test_predictive_pipeline.py</files>
  <read_first>
    tests/sklearn/test_transformer_pipeline.py (the Phase-56 Pipeline round-trip test pattern: _make_X deterministic synthetic data, Pipeline construction, fit/transform assertions);
    python/fdars/sklearn/_skeletons.py: Imputer (line 532), BSplineSmoother (line 283), FPCATransformer (line 118), and a Plan-02-promoted classifier — use FPCLDAClassifier (line 1411, ncomp constructor param) which is green and inductive after Plan 02.
  </read_first>
  <action>
    Create tests/sklearn/test_predictive_pipeline.py. Build a synthetic two-class functional dataset (e.g. ~40 curves over ~20 grid points, two separable classes offset by a mean shift, plus a sparse injected-NaN pattern like `X[::5, ::7] = np.nan` so the Imputer stage is exercised) with a fixed RNG seed — keep it small and fully offline (no network, no docs build). Hold out a test split (e.g. train_test_split with a fixed random_state, or a manual slice) so predict runs on unseen rows.

    Construct `Pipeline([("imputer", Imputer()), ("smoother", BSplineSmoother()), ("fpca", FPCATransformer()), ("clf", FPCLDAClassifier())])`. Wrap it in `GridSearchCV` with a `param_grid` over at least two stage hyperparameters using the `stage__param` convention (e.g. `{"fpca__n_components": [2, 3], "clf__ncomp": [2, 3]}`), a small `cv` (e.g. cv=3), and `refit=True`. Keep the grid small.

    Assert: `grid.fit(X_train, y_train)` succeeds; `grid.best_estimator_` is set; `grid.predict(X_test)` returns labels of the right length whose values are all within the training class set (`set(y_train)`); `grid.best_params_` contains the searched keys. Keep the whole test well under a minute.

    Note the score flow: FPCATransformer maps (n_obs,n_points) curves to (n_obs,n_components) scores, and FPCLDAClassifier consumes those scores as its (n_obs, n_features) input — its `fit` resolves `argvals_` from the score-matrix width, and `ncomp` is capped by `min(ncomp, n_obs-1, n_features)`. Choose grid values so the capped ncomp stays >= 1 for the score-width the fpca stage produces.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_predictive_pipeline.py::test_gridsearch_predictive_pipeline -q</automated>
  </verify>
  <acceptance_criteria>
    - GridSearchCV over the 4-stage pipeline fits without error and sets best_estimator_.
    - grid.predict(X_test) returns len(X_test) labels, all in set(y_train).
    - grid.best_params_ contains both searched stage__param keys.
    - Test completes in under a minute, fully offline.
  </acceptance_criteria>
  <done>GridSearchCV over Pipeline([imputer, smoother, fpca, classifier]) fits, selects best_estimator_, and predicts on held-out data — test green.</done>
</task>

<task type="auto">
  <name>Task 2: Regressor pipeline smoke + whole-suite regression guard (PRED-01)</name>
  <files>tests/sklearn/test_predictive_pipeline.py</files>
  <read_first>
    tests/sklearn/ (existing suite: test_regressors_compliance.py, test_classifiers_compliance.py, test_transformers_compliance.py, test_predictive_pipeline.py, test_coverage.py, test_triage.py);
    python/fdars/sklearn/_skeletons.py FPCRegressor (line 918, promoted in Plan 01).
  </read_first>
  <action>
    Add a second, lighter test to test_predictive_pipeline.py: `Pipeline([("imputer", Imputer()), ("smoother", BSplineSmoother()), ("fpca", FPCATransformer()), ("reg", FPCRegressor())])` fit+predict on a small regression dataset (scalar continuous y over ~40 curves x ~20 points), asserting `pipe.predict(X)` returns a finite float array of length n_obs and `pipe.score(X, y)` returns a finite value — showing regressors compose in a Pipeline too.

    Then run the sklearn compliance suites together to confirm Plans 01-02 promotions plus these pipelines hold with no regression. test_triage.py may still be red for estimators outside this phase (clusterers/outliers, Phase 58) — that is expected and out of scope; do NOT reconcile the whole-suite triage gate here (Phase 58). The phase-57 estimators must be green.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_predictive_pipeline.py tests/sklearn/test_regressors_compliance.py tests/sklearn/test_classifiers_compliance.py tests/sklearn/test_transformers_compliance.py tests/sklearn/test_coverage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Regressor pipeline test: predict returns a finite float array of length n_obs; score(X, y) is finite.
    - test_predictive_pipeline.py + regressor + classifier + transformer compliance + test_coverage all green together.
    - No cross-plan regression across the phase's estimators.
  </acceptance_criteria>
  <done>Both pipeline tests green; regressor + classifier + transformer compliance + coverage suites all green together (no cross-plan regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline stage -> stage | each stage's output (imputed curves, smoothed curves, FPCA scores) is the next stage's validated input; no Fdata construction anywhere in the chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-06 | Tampering | NaN + dtype flowing through the pipeline | low | mitigate | Imputer allow-nan validate + downstream float64 upcast per stage (established base-class contract) |
| T-57-07 | Denial of Service | GridSearchCV cross-product blows up build/test time | low | mitigate | small param_grid + small cv + small synthetic data; offline only, well under a minute |

No package installs in this plan — no supply-chain threat.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_predictive_pipeline.py -q` green.
- GridSearchCV over the 4-stage classifier pipeline fits + predicts; regressor pipeline smoke green.
- Regressor + classifier + transformer + coverage suites green together.
</verification>

<success_criteria>
A `Pipeline([imputer, smoother, fpca, classifier])` wrapped in `GridSearchCV` fits and predicts end-to-end (PRED-01, success criterion 4). Regressors also compose in a Pipeline. No cross-plan regression across the phase's estimators.
</success_criteria>

<output>
Create `.planning/phases/57-regressors-classifiers/57-03-SUMMARY.md` when done.
</output>

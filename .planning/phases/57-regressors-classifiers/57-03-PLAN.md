---
phase: 57-regressors-classifiers
plan: 03
type: execute
wave: 3
depends_on: [57-02]
files_modified:
  - tests/sklearn/test_predictive_pipeline.py
autonomous: true
requirements: [PRED-01]
estimate:
  tokens: 40000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A Pipeline([imputer, smoother, fpca, classifier]) wrapped in GridSearchCV fits and predicts end-to-end (PRED-01)"
    - "GridSearchCV searches over pipeline-stage hyperparameters and selects a best_estimator_ that predicts on held-out data"
  artifacts:
    - "tests/sklearn/test_predictive_pipeline.py"
  key_links:
    - "Pipeline stage names map imputer->smoother->fpca->classifier; GridSearchCV param_grid uses stage__param keys"
    - "classifier consumes FPCATransformer (n_obs,n_components) scores as its (n_obs,n_points) input"
---

<objective>
Prove the full predictive story: a functional-data `Pipeline([imputer, smoother, fpca, classifier])` driven by `GridSearchCV` fits and predicts end-to-end. Delivers PRED-01 (success criterion 4).

Purpose: this is the phase capstone — it demonstrates the regressors/classifiers built in Plans 01-02 compose with the Phase-56 transformers under sklearn's hyperparameter search, which is the milestone's core value ("plug natively into Pipeline/GridSearchCV").
Output: a fast, offline test that constructs the pipeline, runs GridSearchCV over stage hyperparameters, and asserts fit + predict succeed on held-out data.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@python/fdars/sklearn/_skeletons.py
@tests/sklearn/test_transformer_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline([imputer, smoother, fpca, classifier]) under GridSearchCV (PRED-01)</name>
  <read_first>
    tests/sklearn/test_transformer_pipeline.py (the Phase-56 Pipeline round-trip test pattern — how transformers are chained and data is generated);
    python/fdars/sklearn/_skeletons.py Imputer (532), BSplineSmoother (283) or LocalPolynomialSmoother (359), FPCATransformer (118), and a classifier promoted in Plan 02 (e.g. FPCLDAClassifier 1411).
  </read_first>
  <action>
    Create tests/sklearn/test_predictive_pipeline.py. Build a synthetic two-class functional dataset (e.g. a few dozen curves over ~20 grid points, two separable classes, some injected NaN so the imputer stage is exercised) — keep it small and fully offline (no network, no docs build).

    Construct Pipeline([("imputer", Imputer()), ("smoother", BSplineSmoother()), ("fpca", FPCATransformer()), ("clf", <a Plan-02 classifier, e.g. FPCLDAClassifier>)]). Wrap it in GridSearchCV with a param_grid over at least two stage hyperparameters using the stage__param convention (e.g. fpca__n_components and clf__ncomp, or smoother__bandwidth), a small cv (e.g. cv=3), and refit=True.

    Assert: grid.fit(X, y) succeeds; grid.best_estimator_ is set; grid.predict(X_test) returns labels of the right shape whose values are within the training class set; grid.best_params_ contains the searched keys. Keep the whole test well under a minute.

    Note: FPCATransformer maps (n_obs,n_points) curves to (n_obs,n_components) scores, and the downstream classifier consumes those scores as its input matrix — confirm argvals defaults flow through (each stage resolves argvals_ from its own input width in fit).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_predictive_pipeline.py::test_gridsearch_predictive_pipeline -q</automated>
  </verify>
  <done>GridSearchCV over Pipeline([imputer, smoother, fpca, classifier]) fits, selects best_estimator_, and predicts on held-out data — test green.</done>
</task>

<task type="auto">
  <name>Task 2: Regressor pipeline smoke + whole-suite regression guard</name>
  <read_first>
    tests/sklearn/ (full existing suite — test_regressors_compliance.py, test_classifiers_compliance.py, test_transformers_compliance.py, test_predictive_pipeline.py, test_triage.py).
  </read_first>
  <action>
    Add a second, lighter test to test_predictive_pipeline.py: Pipeline([imputer, smoother, fpca, FPCRegressor]) (or PLSRegressor) fit+predict on a small regression dataset (scalar y), asserting predict returns a float array of the right length and pipeline.score(X, y) returns a finite value — showing regressors compose in a Pipeline too.

    Then run the whole sklearn test suite to confirm Plans 01-02 promotions plus this pipeline hold together and nothing regressed. test_triage.py may still be red for estimators outside this phase's scope (clusterers/outliers, Phase 58) — that is expected; the phase-57 estimators it covers must be green. Do not reconcile the whole-suite triage gate here (that lands in Phase 58).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_predictive_pipeline.py tests/sklearn/test_regressors_compliance.py tests/sklearn/test_classifiers_compliance.py tests/sklearn/test_transformers_compliance.py -q</automated>
  </verify>
  <done>Both pipeline tests green; regressor + classifier + transformer compliance suites all green together (no cross-plan regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline stage -> stage | each stage's output (imputed curves, smoothed curves, FPCA scores) is the next stage's validated input; no Fdata construction anywhere in the chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-06 | Tampering | NaN + dtype flowing through the pipeline | low | mitigate | Imputer allow-nan validate + downstream float64 upcast per stage (established base-class contract) |
| T-57-07 | Denial of Service | GridSearchCV cross-product blows up build/test time | low | mitigate | small param_grid + small cv + small synthetic data; offline only, well under a minute |

No package installs in this plan — no supply-chain threat.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_predictive_pipeline.py -q` green.
- GridSearchCV over the 4-stage classifier pipeline fits + predicts; regressor pipeline smoke green.
- Regressor + classifier + transformer compliance suites green together.
</verification>

<success_criteria>
A `Pipeline([imputer, smoother, fpca, classifier])` wrapped in `GridSearchCV` fits and predicts end-to-end (PRED-01, success criterion 4). Regressors also compose in a Pipeline. No cross-plan regression across the phase's estimators.
</success_criteria>

<output>
Create `.planning/phases/57-regressors-classifiers/57-03-SUMMARY.md` when done.
</output>

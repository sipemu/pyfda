---
phase: 57-regressors-classifiers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_regressors_compliance.py
autonomous: true
requirements: [REG-01]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "FPCRegressor passes the full parametrize_with_checks battery (REG-01) — check_regressors_train R2>0.5 + check_methods_subset_invariance both green"
    - "FPCRegressor.score() works via RegressorMixin (r2_score on stored-model predict)"
    - "PLSRegressor passes the full parametrize_with_checks battery with a y=None guard (REG-01)"
    - "predict() is stored-model (fit-on-train-only) — predict(X[mask]) == predict(X)[mask]"
  artifacts:
    - "tests/sklearn/test_regressors_compliance.py (per-estimator parametrize_with_checks)"
    - "python/fdars/sklearn/_coverage.py TRIAGE_VERDICTS FPCRegressor + PLSRegressor flipped to PASS"
  key_links:
    - "FPCRegressor.predict -> _native.regression.predict_fregre_lm(X_fit_, y_fit_, X_new, n_comp) (subset-invariant native predict)"
    - "PLSRegressor.predict -> _native.regression.predict_fregre_pls(X_fit_, argvals_, y_fit_, X_new, n_comp)"
---

<objective>
Tracer for Phase 57: promote FPCRegressor to a fully `check_estimator`-green `RegressorMixin` end-to-end via the stored-model-predict pattern, proving the recipe every remaining predictor reuses. Bring PLSRegressor along (it is one guard away) so REG-01 is fully satisfied in this plan.

Purpose: prove the stored-model-predict architecture on the agent's best early-context tokens before expanding to the differentiator regressors and the classifier family in Plan 02. REG-01 requires FPC-based + PLS regression to be `RegressorMixin` estimators exposing a working `score()`.
Output: FPCRegressor + PLSRegressor green under per-estimator `parametrize_with_checks`; their `_coverage.py` verdicts flipped to PASS; a fast compliance test file mirroring the Phase-56 transformer harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@python/fdars/sklearn/_skeletons.py
@python/fdars/sklearn/_base.py
@python/fdars/sklearn/_coverage.py
@tests/sklearn/test_transformers_compliance.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: FPCRegressor stored-model predict + full parametrize_with_checks green (REG-01)</name>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 918-989 (current FPCRegressor);
    src/regression_mod.rs lines 476-493 (predict_fregre_lm signature: data_fit, response, new_data, n_comp — fits on train ONLY, predicts new_data);
    _coverage.py TRIAGE_VERDICTS["FPCRegressor"] (4 failures: check_regressors_train x3 + check_requires_y_none).
  </read_first>
  <behavior>
    - parametrize_with_checks([FPCRegressor()]) runs every check green (no exemptions).
    - check_regressors_train: R2 > 0.5 on the sklearn battery data. score() (from RegressorMixin) computes r2_score(y, predict(X)).
    - check_methods_subset_invariance: predict(X[mask]) equals predict(X)[mask] — predict fits on stored X_fit_/y_fit_ only, so each test point is independent of the others in the batch.
    - check_requires_y_none: fit(X, y=None) raises a ValueError whose message follows sklearn convention for a required y.
  </behavior>
  <action>
    Fix FPCRegressor so predict is genuinely fit-once/predict-many. The native `predict_fregre_lm(data_fit, response, new_data, n_comp)` already re-fits on the STORED train (data_fit, response) only and predicts new_data — this is subset-invariant by construction, so the existing predict wiring is architecturally correct; do NOT introduce any vstack of train+test.

    Root-cause the R2<=0.5 failure: the default `n_components=3` capped by `min(n_components, n_obs-1, n_pts)` under-fits sklearn's battery data. Raise the effective component count for the battery-sized data by NOT capping below what the native fit needs, and confirm the fitted r_squared stored at fit time reflects a real fit. Keep score() delegated to RegressorMixin (do not override) so check_regressors_train uses r2_score on predict.

    Add a y=None guard at the TOP of fit (before validate) that raises ValueError with a required-y message following sklearn convention (mirror how sklearn regressors phrase "requires y to be passed, but the target y is None"). Do NOT put the verbatim negative-grep token from the acceptance criterion in any comment.

    Keep the contract: subclass _BaseFdarsEstimator, store constructor args verbatim, resolve argvals_ in fit, n_features_in_ via _validate then astype float64, call _native directly, never construct Fdata.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py::test_fpc_regressor_compliance -q</automated>
  </verify>
  <done>FPCRegressor passes every parametrize_with_checks check; predict is stored-model (no vstack); fit(X, y=None) raises the sklearn-convention ValueError.</done>
</task>

<task type="auto">
  <name>Task 2: PLSRegressor y=None guard + full compliance green (REG-01)</name>
  <read_first>
    python/fdars/sklearn/_skeletons.py lines 992-1059 (current PLSRegressor — already uses predict_fregre_pls with stored X_fit_/argvals_/y_fit_);
    src/regression_mod.rs lines 514-535 (predict_fregre_pls signature);
    _coverage.py TRIAGE_VERDICTS["PLSRegressor"] (1 failure: check_requires_y_none).
  </read_first>
  <action>
    PLSRegressor already predicts via `predict_fregre_pls(X_fit_, argvals_, y_fit_, X_new, n_comp)` (stored-model, subset-invariant). Its sole triage failure is check_requires_y_none. Add the same y=None guard used for FPCRegressor at the top of fit (raise ValueError with the sklearn-convention required-y message). Confirm check_regressors_train R2>0.5 holds; if it under-fits on battery data, apply the same n_components handling as FPCRegressor. Do not override score() (RegressorMixin provides it).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py::test_pls_regressor_compliance -q</automated>
  </verify>
  <done>PLSRegressor passes every parametrize_with_checks check including check_requires_y_none and check_regressors_train.</done>
</task>

<task type="auto">
  <name>Task 3: Per-estimator compliance harness + flip verdicts to PASS (REG-01)</name>
  <read_first>
    tests/sklearn/test_transformers_compliance.py (the per-estimator parametrize_with_checks pattern to mirror — one decorated function per estimator, check(estimator) body);
    _coverage.py lines 194-222 (regressor TRIAGE_VERDICTS entries).
  </read_first>
  <action>
    Create tests/sklearn/test_regressors_compliance.py mirroring the transformer harness: import FPCRegressor + PLSRegressor from fdars.sklearn._skeletons; define test_fpc_regressor_compliance and test_pls_regressor_compliance, each decorated with @parametrize_with_checks([Estimator()]) and a check(estimator) body. Keep it a per-estimator battery (NOT the 28-estimator triage) so it stays fast.

    In _coverage.py, flip TRIAGE_VERDICTS["FPCRegressor"] and TRIAGE_VERDICTS["PLSRegressor"] to "PASS" and update their explanatory comments to record the Phase-57 stored-model-predict + y=None fix. Leave RobustFPCRegressor/GLMRegressor/NonparametricRegressor and all classifiers untouched (Plan 02).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sklearn/test_regressors_compliance.py -q && .venv/bin/python -c "from fdars.sklearn._coverage import TRIAGE_VERDICTS as v; assert v['FPCRegressor']=='PASS' and v['PLSRegressor']=='PASS', v"</automated>
  </verify>
  <done>test_regressors_compliance.py green for both estimators; _coverage.py verdicts for FPCRegressor + PLSRegressor read PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/battery -> estimator | sklearn's check_estimator supplies arbitrary numeric arrays; estimator must validate and never construct Fdata |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01 | Tampering | float32/dtype inputs to native predict | low | mitigate | _validate then explicit astype(float64) before every _native call (base-class contract) |
| T-57-02 | Denial of Service | tiny-sample battery data (1 sample/1 feature) | low | mitigate | _min_samples guard raising ValueError with "1 sample" substring before native call |

No package installs in this plan (pure-Python over existing 0.23.0 bindings) — no supply-chain threat.
</threat_model>

<verification>
- `.venv/bin/pytest tests/sklearn/test_regressors_compliance.py -q` green.
- FPCRegressor + PLSRegressor verdicts PASS in _coverage.py.
- No vstack(train,test) in FPCRegressor/PLSRegressor predict.
</verification>

<success_criteria>
FPC-based + PLS regression are `check_estimator`-green `RegressorMixin` estimators with a working `score()` (REG-01, success criterion 1). Stored-model-predict pattern proven for the rest of the phase.
</success_criteria>

<output>
Create `.planning/phases/57-regressors-classifiers/57-01-SUMMARY.md` when done.
</output>

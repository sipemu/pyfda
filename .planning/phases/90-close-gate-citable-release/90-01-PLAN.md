---
phase: 90-close-gate-citable-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - pyproject.toml
  - python/fdars/__init__.py
  - paper/sections/comparison_table.tex
  - CITATION.cff
  - paper/snippets/
  - paper/figures/
  - paper/coverage_counts.tex
  - paper/refs.bib
autonomous: true
requirements: [GATE-02, REL-01]
estimate:
  tokens: 55000
  raw_tokens: 42000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "import fdars reports __version__ == '0.13.0' after rebuild (REL-01)"
    - "Cargo.toml, pyproject.toml, python/fdars/__init__.py, CITATION.cff, and \\FdarsVersion all read 0.13.0 with no residual 0.12.0 or TODO(REL-01) (REL-01)"
    - "GATE-02 correctness gate stays green at 0.13.0: all --check gates pass, git diff paper/figures/ is empty, and 0 dangling citation keys (GATE-02)"
    - "CITATION.cff keeps the arXiv URL PLACEHOLDER marked (no fabricated DOI/arXiv ID) (REL-01)"
  artifacts:
    - Cargo.toml
    - pyproject.toml
    - python/fdars/__init__.py
    - paper/sections/comparison_table.tex
    - CITATION.cff
  key_links:
    - "maturin develop --release rebuild connects the bumped source version to the runtime fdars.__version__ that snippet/coverage generation reads"
    - "make paper regenerates every version-bearing paper artifact so the GATE-02 drift gates stay consistent at 0.13.0"
---

<objective>
Prepare the citable 0.13.0 release locally and prove the GATE-02 correctness gate
stays green at the new version. Bump every version reference 0.12.0 -> 0.13.0,
rebuild fdars at 0.13.0, regenerate all paper artifacts so version-bearing outputs
are consistent, finalize CITATION.cff (arXiv DOI stays a marked placeholder), and
confirm the correctness gate is green.

Purpose: A citable release must carry one consistent version across the crate, the
Python package, the manuscript, and the citation file — and the paper's drift/determinism
gates must still pass against the rebuilt package. This is the mechanical, fully
delegatable half of the close phase.

Output: Bumped source files, a rebuilt 0.13.0 fdars, regenerated deterministic paper
artifacts, a finalized CITATION.cff, and a single atomic commit — with GATE-02 proven green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-close-gate-citable-release/90-CONTEXT.md

@Cargo.toml
@pyproject.toml
@python/fdars/__init__.py
@paper/sections/comparison_table.tex
@CITATION.cff
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump every 0.12.0 version reference to 0.13.0</name>
  <files>Cargo.toml, pyproject.toml, python/fdars/__init__.py, paper/sections/comparison_table.tex, CITATION.cff</files>
  <read_first>
    - Cargo.toml line 3: `version = "0.12.0"`
    - pyproject.toml line 7: `version = "0.12.0"`
    - python/fdars/__init__.py line 35: `__version__ = "0.12.0"`
    - paper/sections/comparison_table.tex line 1: `\newcommand{\FdarsVersion}{0.12.0}` and line 4 `TODO(REL-01)` comment
    - CITATION.cff line 5: `version: "0.12.0"`; line 6: `date-released`; lines 34/37 arXiv PLACEHOLDER TODOs
  </read_first>
  <action>
    Implements REL-01. Bump the five source version references from 0.12.0 to 0.13.0:
    (1) Cargo.toml line 3 `version = "0.13.0"`.
    (2) pyproject.toml line 7 `version = "0.13.0"`.
    (3) python/fdars/__init__.py line 35 `__version__ = "0.13.0"`.
    (4) paper/sections/comparison_table.tex line 1 `\newcommand{\FdarsVersion}{0.13.0}`, and REMOVE the
        line-4 `TODO(REL-01): bump \FdarsVersion...` comment now that it is done.
    (5) CITATION.cff line 5 `version: "0.13.0"` and set `date-released: "2026-09-09"`.
    Finalize CITATION.cff DOI/arXiv wiring per REL-01 WITHOUT fabricating an ID: the
    `preferred-citation` arXiv URL stays the literal `https://arxiv.org/abs/PLACEHOLDER`
    value and both its TODO markers (journal venue TODO, arXiv-ID TODO) REMAIN, because the
    real arXiv ID is only assigned at submission (a deferred idea). Do NOT invent a DOI or arXiv number.
    Do not touch any other file in this task; the rebuild + regeneration happen in Task 2.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qxF 'version = "0.13.0"' <(sed -n '3p' Cargo.toml) && grep -qxF 'version = "0.13.0"' <(sed -n '7p' pyproject.toml) && grep -qxF '__version__ = "0.13.0"' <(sed -n '35p' python/fdars/__init__.py) && grep -qF '\newcommand{\FdarsVersion}{0.13.0}' paper/sections/comparison_table.tex && grep -qxF 'version: "0.13.0"' <(sed -n '5p' CITATION.cff) && ! grep -qF 'TODO(REL-01)' paper/sections/comparison_table.tex && grep -qF 'arxiv.org/abs/PLACEHOLDER' CITATION.cff && ! grep -rnF '0.12.0' Cargo.toml pyproject.toml python/fdars/__init__.py paper/sections/comparison_table.tex CITATION.cff && echo BUMP_OK</automated>
    <fails_when>Any of the five files still reads 0.12.0, the \FdarsVersion TODO(REL-01) comment survives, the arXiv PLACEHOLDER was replaced with a fabricated ID (grep for PLACEHOLDER fails), or a version line does not read exactly 0.13.0.</fails_when>
  </verify>
  <done>All five source references read 0.13.0; the \FdarsVersion TODO is gone; the arXiv URL is still the marked PLACEHOLDER (no fabricated DOI); no residual 0.12.0 in these files.</done>
  <reversibility rating="reversible">Version-string edits are pure text and trivially revertible before the commit; the one-way-door actions (tag/publish) are in plan 90-02, not here.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Rebuild fdars at 0.13.0 and regenerate all paper artifacts</name>
  <files>python/fdars/_native (built), paper/snippets/, paper/figures/, paper/coverage_counts.tex, paper/refs.bib</files>
  <read_first>
    - Makefile lines 41-63: `paper-figures`, `paper-coverage`, `paper-refs`, `paper-snippets`, and the aggregate `paper` target
    - Makefile line 37: `PAPER_PYTHONPATH := scripts:paper/code`
    - .venv exists at repo root (activate with `. .venv/bin/activate`)
  </read_first>
  <action>
    Implements REL-01 + GATE-02. Rebuild the extension at the new version and regenerate every
    version-bearing paper output so the runtime version and captured artifacts agree:
    (1) Activate the venv and run `maturin develop --release` so the compiled `fdars._native`
        reflects the 0.13.0 Cargo/pyproject bump and `import fdars; fdars.__version__` reports 0.13.0.
    (2) Run the full one-command pipeline `make paper` (PAPER_PYTHONPATH is set inside the Makefile;
        it runs paper-figures + paper-coverage + paper-refs + paper-snippets). This regenerates
        snippets, coverage macros, refs.bib, and figures against the rebuilt 0.13.0 package.
    The exact commands (run from repo root): `. .venv/bin/activate && maturin develop --release && make paper`.
    Commit nothing in this task — the atomic commit is Task 3 after the gate proves green. If `make paper`
    mutates any committed artifact, that is expected (regeneration); if it mutates paper/figures/ (a
    determinism drift), Task 3's gate will catch it — do NOT hand-edit figures to force stability.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate && maturin develop --release && make paper && python -c "import fdars; assert fdars.__version__ == '0.13.0', fdars.__version__" && echo REBUILD_REGEN_OK</automated>
    <fails_when>maturin develop fails to compile, `make paper` errors on any generator, or `import fdars` reports a version other than 0.13.0 (rebuild did not take effect).</fails_when>
  </verify>
  <done>fdars is rebuilt at 0.13.0 (import reports 0.13.0), and all paper artifacts (snippets, coverage_counts.tex, refs.bib, figures) are regenerated against the 0.13.0 package.</done>
  <precondition>maturin (1.13.1) and cargo (1.97.0) are installed and .venv exists at repo root (user_setup: verified this session).</precondition>
  <reversibility rating="reversible">A rebuild + artifact regeneration is idempotent and revertible via git checkout before the commit.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Prove GATE-02 green at 0.13.0, then commit atomically</name>
  <files>Cargo.toml, pyproject.toml, python/fdars/__init__.py, paper/sections/comparison_table.tex, CITATION.cff, paper/snippets/, paper/figures/, paper/coverage_counts.tex, paper/refs.bib</files>
  <read_first>
    - Makefile lines 53-61: `paper-check` (four --check gates) and `paper-verify` (figure byte-stability)
    - .github/workflows/paper.yml lines 44-72: the exact gate order CI runs
    - paper/refs.bib and paper/refs_manual.bib: bib entries backing every \cite key
  </read_first>
  <action>
    Implements GATE-02. Prove the correctness gate is green at 0.13.0, then commit everything atomically.
    Run the local GATE-02 checks in this order:
    (1) `make paper-check` — runs assert_coverage.py --check, gen_refs_bib.py --check, check_comparison.py,
        and gen_snippets.py --check (all must exit 0 at 0.13.0).
    (2) `make paper-verify` — regenerates figures and asserts `git diff --exit-code paper/figures/` is empty
        (byte-identical determinism).
    (3) Dangling-citation check — every \cite key used in paper/paper.tex + paper/sections/*.tex must
        resolve to an entry in paper/refs.bib OR paper/refs_manual.bib (0 dangling).
    (4) `import fdars` reports 0.13.0.
    If any gate is red, STOP and report — do NOT commit a red gate and do NOT hand-edit generated artifacts
    to force a pass. Once all four are green, commit atomically with message
    `chore(90): prepare citable 0.13.0 release (version bump + regenerated paper artifacts)` staging exactly:
    Cargo.toml, pyproject.toml, python/fdars/__init__.py, paper/sections/comparison_table.tex, CITATION.cff,
    and the regenerated paper/snippets/ paper/figures/ paper/coverage_counts.tex paper/refs.bib.
    Do NOT run `git push`, do NOT create or push any tag — those are plan 90-02 (orchestrator/user) steps.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate && make paper-check && make paper-verify && python -c "import fdars; assert fdars.__version__ == '0.13.0'" && MISS=0; for k in $(grep -rhoE '\\cite[pt]?\{[^}]+\}' paper/paper.tex paper/sections/*.tex | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sed 's/ //g' | sort -u); do grep -qhE "@[a-zA-Z]+\{$k," paper/refs.bib paper/refs_manual.bib || { echo "DANGLING $k"; MISS=1; }; done; test "$MISS" -eq 0 && echo GATE02_GREEN</automated>
    <fails_when>Any --check gate exits non-zero, `git diff paper/figures/` is non-empty (determinism drift), any \cite key has no matching bib entry (DANGLING printed), or fdars.__version__ != 0.13.0.</fails_when>
  </verify>
  <done>make paper-check and make paper-verify both pass at 0.13.0, 0 dangling citation keys, import reports 0.13.0, and one atomic release-prep commit exists on branch main (no tag, no push).</done>
  <reversibility rating="reversible">The commit is local-only (no push/tag); it can be reset before plan 90-02 pushes. The irreversible push/tag/publish are deferred to 90-02.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source version -> built artifact | A stale/mismatched version bump could ship a package whose runtime version disagrees with its metadata |
| generated paper artifacts -> committed manuscript | Non-deterministic regeneration could commit drifting figures/snippets that break the arXiv self-containment claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-90-01 | Tampering | version bump across 5 files | medium | mitigate | Task 1 verify greps all five files read exactly 0.13.0 with zero residual 0.12.0 |
| T-90-02 | Information Disclosure | fabricated arXiv DOI/ID | high | mitigate | Task 1 asserts the arXiv URL stays literal PLACEHOLDER; no DOI invented (honesty constraint) |
| T-90-03 | Tampering | non-deterministic figure regeneration | high | mitigate | Task 3 `make paper-verify` asserts empty `git diff paper/figures/` |
| T-90-04 | Repudiation | committing a red GATE-02 | high | mitigate | Task 3 gates all four checks before the atomic commit; STOP-on-red instruction |
| T-90-SC | Tampering | no npm/pip/cargo installs added | low | accept | No new package installs; maturin/cargo/deps already present (verified this session) |
</threat_model>

<verification>
- All five version references read 0.13.0; \FdarsVersion TODO removed; arXiv PLACEHOLDER intact.
- `import fdars; fdars.__version__ == "0.13.0"` after rebuild.
- `make paper-check` and `make paper-verify` both green.
- 0 dangling citation keys.
- One atomic commit staged with the version bumps + regenerated artifacts; no tag, no push.
</verification>

<success_criteria>
GATE-02 correctness gate is green at 0.13.0 and REL-01 release prep is complete and committed
locally, ready for plan 90-02 to push (orchestrator) and hand the tag/publish to the user.
</success_criteria>

<output>
Create `.planning/phases/90-close-gate-citable-release/90-01-SUMMARY.md` when done.
</output>

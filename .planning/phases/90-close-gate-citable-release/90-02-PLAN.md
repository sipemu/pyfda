---
phase: 90-close-gate-citable-release
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified: []
autonomous: false
requirements: [GATE-03, GATE-04, REL-01]
estimate:
  tokens: 45000
  raw_tokens: 34000
  tasks: 5
  confidence: med
must_haves:
  truths:
    - "The unpushed main commits (including the 90-01 release-prep commit) reach origin/main (GATE-03 trigger)"
    - "paper.yml CI compiles the arXiv PDF end-to-end (tectonic) — arXiv self-containment proven in CI, not locally (GATE-03)"
    - "A blocking human read-through of the compiled PDF (prose, comparison table, citations) is approved before close (GATE-04)"
    - "The v0.13.0 tag command + PyPI publish steps are prepared and handed to the user (not auto-executed) (REL-01)"
  artifacts: []
  key_links:
    - "push to origin/main is the ONLY trigger for paper.yml — GATE-03 cannot run until the push lands"
    - "the CI-compiled PDF artifact is the exact document the human reads for GATE-04 approval"
    - "GATE-04 approval is the gate on tag/publish — no tag or publish before the human approves"
---

<objective>
Close the milestone: push the release-prep work to origin/main so paper.yml compiles the
arXiv PDF (GATE-03), fetch that PDF for a BLOCKING human read-through (GATE-04), and — only
on approval — hand the user the prepared v0.13.0 tag + PyPI publish commands (REL-01).

Purpose: The final gates are inherently orchestrator/human actions: only a push triggers the
CI PDF compile, only a human can approve the read-through, and (by user decision) only the user
performs the tag push + PyPI publish. This plan documents those steps for the orchestrator to run.

Output: A pushed origin/main, a green paper.yml run with a fetched PDF artifact, a recorded
human GATE-04 approval, and the exact tag/publish commands handed to the user.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-close-gate-citable-release/90-CONTEXT.md

@.github/workflows/paper.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize push to origin/main (GATE-03 trigger)</name>
  <decision>Authorize pushing local main (~112 unpushed commits, including the 90-01 release-prep commit) to origin/main to trigger the paper.yml CI PDF compile</decision>
  <context>
    Implements GATE-03 (trigger). Branch main is ~112 commits ahead of origin and has NEVER been
    pushed; paper.yml (`on: push: branches: [main]`, lines 3-12) has never run. Pushing publishes
    all those commits to the public default branch — a one-way door — and is the ONLY trigger for the
    CI PDF compile that proves arXiv self-containment. The user already confirmed "push to main" and
    "push to trigger CI" in 90-CONTEXT.md (2026-09-09), so this is pre-authorized; the checkpoint
    exists so a human confirms the moment is right (90-01 release-prep commit present) before the
    irreversible publish. Only the branch is pushed here — the v0.13.0 tag is deferred to Task 4.
  </context>
  <options>
    <option id="push">
      <name>Push origin main now</name>
      <pros>Triggers paper.yml → GATE-03 CI PDF compile; matches how prior milestones shipped; user pre-authorized</pros>
      <cons>One-way door: publishes ~112 previously-local commits to the public default branch</cons>
    </option>
    <option id="hold">
      <name>Hold — do not push yet</name>
      <pros>Keeps the work local if a last-minute concern surfaces before publishing</pros>
      <cons>GATE-03 cannot run; the milestone cannot close without the CI PDF compile</cons>
    </option>
  </options>
  <resume-signal>Select "push" to authorize `git push origin main`, or "hold" to pause. On "push", the executor runs the push and CI monitor in the next tasks — it does NOT push any tag.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push origin/main and confirm it matches local (GATE-03 trigger)</name>
  <files>(none — git push; no repo file changes)</files>
  <read_first>
    - .github/workflows/paper.yml lines 3-12: `on: push: branches: [main]` is the CI trigger
    - Task 1 decision must be "push" before running this
  </read_first>
  <action>
    Implements GATE-03 (trigger). After the Task-1 "push" authorization, run `git push origin main`
    from the repo root. This is an automatable orchestrator action (gh/git available) — do NOT ask the
    user to run it. Confirm origin/main now equals local main HEAD and contains the 90-01 release-prep
    commit. This push is what triggers the paper.yml run. Do NOT push any tag (the v0.13.0 tag is the
    user's action in Task 5) — push the branch only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && git push origin main && test "$(git rev-parse origin/main)" = "$(git rev-parse main)" && echo PUSH_OK</automated>
    <fails_when>The push is rejected (auth/permission — surface as an auth gate), or origin/main HEAD does not equal local main HEAD after the push.</fails_when>
  </verify>
  <done>origin/main matches local main (all commits including the release-prep commit are pushed); a paper.yml run has been triggered.</done>
  <reversibility rating="one-way">Pushing to origin/main publishes ~112 previously-local commits; the Task-1 decision checkpoint gates this one-way action, which the user pre-authorized.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Monitor paper.yml CI to green and fetch the compiled PDF (GATE-03)</name>
  <files>(none — CI monitor + artifact fetch)</files>
  <read_first>
    - .github/workflows/paper.yml lines 44-83: figure-determinism gate, four --check drift gates, then the tectonic "Compile PDF (GATE-01)" step
    - gh 2.97.0 is available (verified this session)
  </read_first>
  <action>
    Implements GATE-03. After the Task-2 push, monitor the paper.yml run to green — this is an
    automatable action (gh available), do NOT ask the user to do it. Steps:
    (1) Find the run: `gh run list --workflow=paper.yml --branch main --limit 1` (capture the run-id).
    (2) Watch to completion: `gh run watch <run-id> --exit-status` — this exits non-zero if any step
        (figure byte-stability, the four --check drift gates, or the final tectonic "Compile PDF" step) fails.
    (3) On green, fetch the compiled PDF if paper.yml uploads it as an artifact: `gh run download <run-id>`.
        If no artifact is uploaded, surface (in the summary + for Task 4) that the PDF is reproduced by
        the green tectonic step and note where the read-through PDF is obtained.
    If the run is RED, STOP: the milestone cannot close. Report the failing step; the fix loops back
    through plan 90-01 (regenerate/commit), then re-push (Task 2) and re-monitor. Do not proceed to the
    GATE-04 read-through until CI is green and the PDF is in hand.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && RID=$(gh run list --workflow=paper.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId') && test -n "$RID" && gh run watch "$RID" --exit-status && echo "CI_GREEN run=$RID"</automated>
    <fails_when>No paper.yml run is found for main, or `gh run watch --exit-status` returns non-zero because any step (determinism gate, a --check drift gate, or the tectonic PDF compile) failed.</fails_when>
  </verify>
  <done>paper.yml is green end-to-end (tectonic PDF compile succeeds), proving arXiv self-containment in CI; the compiled PDF is located/fetched for the human read-through.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: BLOCKING human read-through of the compiled manuscript (GATE-04)</name>
  <what-built>
    Implements GATE-04. The paper.yml CI run is green (Task 3) and its tectonic-compiled PDF —
    the exact arXiv submission document at fdars 0.13.0 — is available for review (fetched via
    `gh run download`, or reproduced by the green tectonic step). This is a BLOCKING read-through
    that gates milestone close; it is never auto-approved.
  </what-built>
  <how-to-verify>
    Open the CI-compiled PDF and confirm all three:
    1. Prose accuracy — the manuscript is method-accurate and free of overclaims (no benchmarks, no
       fabricated numbers).
    2. Comparison table — the capability-dimension × peer-package table reads honestly (fdars column
       grounded, competitor cells defensible); \FdarsVersion shows 0.13.0.
    3. Citations — citations resolve and are appropriate in context (0 dangling keys was proven in 90-01).
    If any is wrong, describe the changes; the fix loops back through 90-01 regeneration + re-push +
    re-CI before this gate is re-presented. Do NOT let the release hand-off (Task 5) proceed until you
    explicitly approve.
  </how-to-verify>
  <resume-signal>Type "approved" once prose, comparison table, and citations are all confirmed correct for arXiv submission, or describe the required changes.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 5: User pushes the v0.13.0 tag + PyPI publish (REL-01 hand-off)</name>
  <action>Run the prepared v0.13.0 tag push (which fires the PyPI publish) — the orchestrator prepares the commands; you execute them</action>
  <instructions>
    Implements REL-01 (hand-off only). By your decision (90-CONTEXT.md "Release scope -> Prepare, you
    publish"), the orchestrator does NOT push the tag or publish to PyPI — YOU do. Everything
    automatable is already done: the version bumps + regenerated artifacts are committed and on
    origin/main (Task 2), and GATE-04 is approved (Task 4). Run these two commands to cut the citable
    release (pushing the semver tag is what fires the PyPI publish workflow, per the release-versioning
    memory):

      git tag -a v0.13.0 -m "fdars 0.13.0 — citable release paired with the arXiv software paper"
      git push origin v0.13.0

    Note: the arXiv/Zenodo DOI in CITATION.cff stays a marked PLACEHOLDER until the arXiv ID is
    assigned at submission — that substitution is a deferred, post-close step, not part of this release.
  </instructions>
  <verification>After you push the tag, the orchestrator can confirm it exists on origin (`git ls-remote --tags origin v0.13.0`) and that the PyPI publish workflow was triggered — the orchestrator itself does not create or push the tag.</verification>
  <resume-signal>Type "done" once you have pushed the v0.13.0 tag (and, if you choose, confirmed the PyPI publish started).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main -> origin/main (public) | Pushing publishes ~112 previously-local commits; one-way-door |
| orchestrator -> PyPI/tag (release) | The tag push fires an irreversible PyPI publish; reserved to the user by decision |
| CI-compiled PDF -> human approval | The document the human approves must be the exact CI-compiled artifact, not a stale local copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-90-05 | Elevation of Privilege | orchestrator pushing the release tag | high | mitigate | Task 5 is a human-action gate: the user runs the tag push/publish; the orchestrator explicitly does NOT create or push the tag |
| T-90-06 | Tampering | approving a stale/local PDF instead of the CI artifact | high | mitigate | Task 4 reviews the CI-compiled PDF fetched in Task 3, tied to the green paper.yml run |
| T-90-07 | Repudiation | closing before human GATE-04 approval | high | mitigate | Task 4 is a blocking-human gate; Task 5 is explicitly ordered after approval |
| T-90-08 | Denial of Service | CI red (drift/tectonic) blocks close | medium | accept | Documented STOP-and-loop path back through 90-01 on any red step |
</threat_model>

<verification>
- origin/main matches local main after the authorized push (GATE-03 trigger).
- paper.yml is green end-to-end including the tectonic PDF compile (GATE-03).
- The user explicitly approves the read-through (prose, table, citations) (GATE-04).
- The v0.13.0 tag + publish commands are handed to the user; orchestrator did not tag/publish (REL-01).
</verification>

<success_criteria>
GATE-03 (CI PDF compile) is green, GATE-04 (human read-through) is approved, and the REL-01
tag/publish hand-off is complete — the milestone is cleared to close and the citable 0.13.0
release is in the user's hands to publish.
</success_criteria>

<output>
Create `.planning/phases/90-close-gate-citable-release/90-02-SUMMARY.md` when done.
</output>

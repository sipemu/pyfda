---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - conftest.py
  - docs/requirements.txt
  - .github/workflows/docs.yml
autonomous: false
requirements: [FND-05]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 32000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "conftest.py (repo root) defines pytest_markdown_docs_globals() returning {np, plt, fdars}, and sets matplotlib.use('Agg') before importing pyplot (FND-05, D-06)"
    - "pytest --co -q discovers example code fences via pytest-markdown-docs with --markdown-docs-syntax=superfences (FND-05, ROADMAP SC #5)"
    - "pytest --markdown-docs --markdown-docs-syntax=superfences docs/examples/canadian-weather.md passes with PYTHONPATH=scripts and compiled fdars (FND-05, smoke-test per D-04/D-05)"
    - "The doc-test CI gate (Gate B) blocks on the smoke-test page ONLY, not on all example pages (D-11)"
    - "pytest-markdown-docs is added to docs/requirements.txt (FND-05)"
    - { statement: "A fence that raises an exception fails the doc-test gate, while a fence that only prints HTML output (print(render(f))) passes — pytest-markdown-docs fails only on exceptions, not on printed output (FND-05 raise-vs-print edge, RESEARCH Open Q2 / A1)", verification: backstop }
    - { statement: "If the smoke-test reveals cross-fence state does NOT carry (var defined in one fence used in the next), the fallback is chosen at execution time per Claude's discretion: (a) a custom conftest fence-exec harness with a shared namespace, or (b) a consolidate-fences convention — decided by exactly how pytest-markdown-docs fails (D-04)", verification: backstop }
  prohibitions:
    - "The doc-test CI gate MUST NOT be made blocking on all example pages now — only the one smoke-test page is gated; the gated set expands page-by-page as Phase 9 fixes each example (D-11, avoids pulling Phase 9 work forward)"
  artifacts:
    - conftest.py
    - docs/requirements.txt
    - .github/workflows/docs.yml
  key_links:
    - "conftest.py matplotlib.use('Agg') must precede import matplotlib.pyplot as plt (CI has no display)"
    - "PYTHONPATH=scripts → fences resolve docs_fig / docs_data imports (globals hook does not inject them)"
    - "--markdown-docs-syntax=superfences → exec=\"1\" html=\"1\" fences are recognized as Python (else silently skipped, RESEARCH Pitfall 5 / A1)"
    - "Gate B smoke-test page list → the single canadian-weather.md now; grows in Phase 9 (D-11)"
---

<objective>
Stand up the pytest-markdown-docs doc-test harness end-to-end: a conftest.py globals hook, the pinned dependency, a smoke-test on a real fdars-importing page, and a CI gate (Gate B) that blocks on that one page only (D-11). This is the last vertical guardrail slice.

Purpose: FND-05 makes example fences runnable as tests so Phase 9 can catch API drift; scoping the CI gate to one verified page keeps CI green through phases 2–8 while proving the harness works. The D-04 smoke-test settles whether pytest-markdown-docs is THE harness or a fallback is needed.
Output: conftest.py, pytest-markdown-docs in docs/requirements.txt, and docs.yml Gate B (one-page smoke).
</objective>

## Phase Goal (user story)

**As a** Phase-9 author fixing example pages, **I want to** run example code fences as pytest tests with np/plt/fdars pre-injected and a CI gate that grows page-by-page, **so that** API drift in examples is caught mechanically without forcing all pages green at once.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — pytest-markdown-docs@0.9.2 (SUS verdict) before install</name>
  <what-built>Nothing installed yet. Blocking legitimacy gate required before adding pytest-markdown-docs to docs/requirements.txt. RESEARCH.md's Package Legitimacy Audit marks pytest-markdown-docs@0.9.2 SUS solely due to low download-count visibility on PyPI; it is backed by Modal Labs, source repo github.com/modal-labs/pytest-markdown-docs, confirmed on PyPI (0.9.2 released 2026-03-23).</what-built>
  <how-to-verify>
    Confirm pytest-markdown-docs@0.9.2 legitimacy before adding it to docs/requirements.txt:
    1. Open https://pypi.org/project/pytest-markdown-docs/0.9.2/ — confirm it is the Modal Labs project, source repo github.com/modal-labs/pytest-markdown-docs.
    2. Confirm you are pinning 0.9.2 (the researched version).
    3. Confirm the package is a pytest plugin (dev/test tool), not a runtime dependency of the shipped fdars wheel.
    Approve to allow Task 3 to add pytest-markdown-docs to docs/requirements.txt.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe concerns (e.g., pin a different version, or prefer the custom-harness fallback outright).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create conftest.py with the pytest_markdown_docs_globals hook (FND-05, D-06)</name>
  <files>conftest.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md — the "conftest.py (test-config, repo root)" block (verbatim conftest; the matplotlib.use("Agg")-before-pyplot constraint; the tests/test_basic.py import-convention analog)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 5 (execution model; the conftest.py source; the globals hook signature returns {np, plt, fdars})
    - .planning/phases/01-foundation/01-CONTEXT.md — D-06 (globals hook exposes np, plt, fdars)
    - scripts/docs_fig.py — lines 23–26, the established matplotlib.use("Agg") before pyplot pattern to mirror
  </read_first>
  <action>
    Create conftest.py at the repo root exactly per PATTERNS.md: set matplotlib.use("Agg") BEFORE import matplotlib.pyplot as plt (CI has no display; same pattern as docs_fig.py:23–26), import numpy as np and import fdars, and define pytest_markdown_docs_globals() returning {"np": np, "plt": plt, "fdars": fdars} (D-06). Include the module docstring explaining that exec blocks still import explicitly; globals are a fallback. Do NOT inject docs_fig/docs_data here — those resolve via PYTHONPATH=scripts (Task 4 sets that in CI; local runs export it).
  </action>
  <verify>
    <automated>PYTHONPATH=scripts python -c "import conftest; g=conftest.pytest_markdown_docs_globals(); assert set(g)=={'np','plt','fdars'}, set(g); import matplotlib; assert matplotlib.get_backend().lower()=='agg', matplotlib.get_backend(); print('CONFTEST_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pytest_markdown_docs_globals() returns exactly the keys np, plt, fdars
    - matplotlib backend is Agg after importing conftest (use('Agg') precedes the pyplot import)
    - import fdars succeeds (requires compiled fdars in the environment; if absent, run this check in CI/phase verification)
  </acceptance_criteria>
  <done>Repo-root conftest.py exposes np/plt/fdars to markdown fences with the Agg backend forced.</done>
</task>

<task type="auto">
  <name>Task 3: Add pytest-markdown-docs to docs/requirements.txt and run the smoke-test (D-04, D-05)</name>
  <files>docs/requirements.txt</files>
  <read_first>
    - docs/requirements.txt — the existing docs build deps (mkdocs-material, markdown-exec, matplotlib, numpy, pandas, scipy, scikit-learn); pytest-markdown-docs is absent
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 5 (smoke-test recommendation canadian-weather.md — 8 fences importing fdars.regression; the critical finding that current fences are already self-contained so cross-fence state is a non-issue; the pytest invocation; the --markdown-docs-syntax=superfences requirement) and Open Q2/A1 (html="1" print(render(f)) prints HTML, does not raise, so passes)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-04 (smoke-test before locking in; fallback is Claude's discretion), D-05 (page with genuine cross-fence state dependency; planner picked canadian-weather.md — the most meaningful real-fdars page even though RESEARCH found its fences self-contained)
    - conftest.py — the globals hook from Task 2
  </read_first>
  <action>
    Add pytest-markdown-docs (pinned to the checkpoint-approved version, e.g. pytest-markdown-docs==0.9.2) to docs/requirements.txt. Run the smoke-test: PYTHONPATH=scripts pytest --markdown-docs --markdown-docs-syntax=superfences docs/examples/canadian-weather.md -v (requires compiled fdars; run locally or in CI). Assess the D-04 outcome: RESEARCH found every canadian-weather.md fence is self-contained (re-imports and re-declares), so the smoke-test is expected to PASS without needing the continuation feature. If it passes → pytest-markdown-docs is locked in as THE harness (no fallback needed). If cross-fence state unexpectedly fails, decide the fallback at execution time per D-04 Claude's-discretion: (a) a custom conftest fence-exec harness that execs a page's fences in one shared namespace, or (b) a consolidate-fences convention — picking based on exactly how it failed. Record the D-04 verdict (locked-in, or which fallback + why) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q 'pytest-markdown-docs' docs/requirements.txt && PYTHONPATH=scripts pytest --co -q --markdown-docs --markdown-docs-syntax=superfences docs/examples/canadian-weather.md 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/requirements.txt contains a pinned pytest-markdown-docs entry
    - pytest --co -q --markdown-docs --markdown-docs-syntax=superfences discovers fences on canadian-weather.md (non-empty collection — proves the syntax flag recognizes exec="1" fences, RESEARCH Pitfall 5)
    - The full smoke-test run passes on canadian-weather.md (run in CI/phase verification if local env lacks compiled fdars); a fence that only prints HTML passes, a raising fence fails (Open Q2/A1)
    - The D-04 verdict (locked-in vs fallback + rationale) is recorded for the SUMMARY
  </acceptance_criteria>
  <reversibility rating="reversible">Adding a dev/test dependency + a conftest hook; removable with no data or API surface to unwind (D-04 explicitly allows swapping to the fallback harness).</reversibility>
  <done>pytest-markdown-docs is pinned in docs/requirements.txt; the smoke-test on canadian-weather.md passes (or the fallback is chosen and recorded per D-04).</done>
</task>

<task type="auto">
  <name>Task 4: Wire the doc-test smoke gate into CI (Gate B) — smoke-test page only (D-09, D-11)</name>
  <files>.github/workflows/docs.yml</files>
  <read_first>
    - .github/workflows/docs.yml — current steps; Gate B inserts AFTER "Build and install fdars (maturin develop)" and BEFORE "Build and gate on figure errors"; the venv-activation + PYTHONPATH: scripts patterns
    - .planning/phases/01-foundation/01-PATTERNS.md — the ".github/workflows/docs.yml — two step additions" block (Gate B YAML verbatim; step ordering; the source .venv/bin/activate requirement)
    - .planning/phases/01-foundation/01-RESEARCH.md — CI Extension Gate B (YAML; placement; D-11 implementation note that only the one page is gated and the list grows in Phase 9)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-11 (doc-test gate blocks on smoke-test page ONLY; NOT all pages)
  </read_first>
  <action>
    Add a "Doc-test smoke (canadian-weather.md)" step to .github/workflows/docs.yml, inserted AFTER "Build and install fdars" (needs compiled fdars) and BEFORE "Build and gate on figure errors". Use the Gate B YAML from PATTERNS.md/RESEARCH: env PYTHONPATH: scripts, source .venv/bin/activate, then pytest --markdown-docs --markdown-docs-syntax=superfences docs/examples/canadian-weather.md -v. Hardcode the single page (D-11) and add a comment stating the gated set expands page-by-page in Phase 9 — do NOT gate docs/examples/ wholesale. If Task 3 selected the fallback harness instead of pytest-markdown-docs, wire that fallback's smoke command here instead, still scoped to the one page. This step coexists with Plan 01's Gate A already in docs.yml (both edit docs.yml; Plan 01 is an earlier wave so its edit lands first).
  </action>
  <verify>
    <automated>grep -q 'Doc-test smoke' .github/workflows/docs.yml && grep -Fq 'canadian-weather.md' .github/workflows/docs.yml && python -c "import yaml; yaml.safe_load(open('.github/workflows/docs.yml')); print('YAML_OK')" && test $(grep -c 'docs/examples/' .github/workflows/docs.yml) -le 2</automated>
  </verify>
  <acceptance_criteria>
    - docs.yml has a "Doc-test smoke (canadian-weather.md)" step after maturin develop, before the mkdocs-build gate
    - The step runs pytest --markdown-docs --markdown-docs-syntax=superfences on the SINGLE page docs/examples/canadian-weather.md (not docs/examples/ wholesale — D-11)
    - A comment records that the gated page set grows in Phase 9
    - docs.yml parses as valid YAML and still contains Plan 01's Gate A step (both gates present)
  </acceptance_criteria>
  <reversibility rating="reversible">Additive CI step scoped to one page; removable cleanly.</reversibility>
  <done>docs.yml runs a doc-test smoke gate on canadian-weather.md only, growing in Phase 9 (D-11) — the doc-test guardrail is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → CI/local pip install | pytest-markdown-docs is fetched into the docs build environment |
| example .md fences → pytest exec | fence Python runs under pytest with injected globals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | pip install pytest-markdown-docs | high | mitigate | slopcheck + blocking-human legitimacy checkpoint (Task 1) before adding to docs/requirements.txt; pin ==0.9.2; Modal-Labs-backed, source repo confirmed |
| T-01-04b | Tampering | example fence code executed by pytest | low | mitigate | Fences are committed docs content authored in-repo; no runtime/user input; same trust level as the existing markdown-exec build (RESEARCH Security Domain) |
| T-01-04c | Denial of service | fences silently skipped (missing syntax flag) | low | mitigate | --markdown-docs-syntax=superfences required; Task 3 --co check asserts non-empty collection (RESEARCH Pitfall 5) |

The supply-chain threat T-01-04 is High and mitigated by the mandated blocking-human checkpoint (Task 1); the block-on-high threshold is satisfied. All other threats Low at ASVS L1.
</threat_model>

<verification>
- conftest.py exposes np/plt/fdars with the Agg backend forced.
- pytest --co discovers fences via the superfences syntax flag; the smoke-test passes on canadian-weather.md (or the fallback is chosen and recorded).
- docs.yml Gate B blocks on the one smoke-test page only (D-11).
</verification>

<success_criteria>
- pytest --co -q discovers example code fences via pytest-markdown-docs, and a conftest.py globals hook exposes np, plt, fdars to fence execution (ROADMAP SC #5, FND-05).
- The doc-test CI gate blocks on the smoke-test page only, keeping CI green through phases 2–8 (D-11).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>

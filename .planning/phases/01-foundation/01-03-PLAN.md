---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - mkdocs.yml
  - docs/includes/load-canadian-weather.md
  - docs/includes/load-canadian-weather-precip.md
  - docs/includes/load-tecator.md
  - docs/includes/load-growth.md
  - docs/includes/load-phoneme.md
autonomous: true
requirements: [FND-04]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 26000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "pymdownx.snippets is enabled in mkdocs.yml with base_path: [docs] so --8<-- \"includes/...\" resolves under docs/ (FND-04)"
    - "docs/includes/ contains one snippet fragment per dataset loader (canadian-weather, canadian-weather-precip, tecator, growth, phoneme), each holding only plain Python preamble lines with no fence delimiters and no exec attributes (FND-04, RESEARCH Pitfall 4)"
    - "At least one example page fence uses the --8<-- include instead of an inline CSV-loading preamble, and mkdocs build --strict still succeeds (FND-04, ROADMAP SC #4)"
    - { statement: "A snippet include file that is empty or references a missing loader is caught by mkdocs build --strict rather than silently producing a blank preamble (FND-04 empty/missing-include edge)", verification: backstop }
  prohibitions:
    - "docs/includes/ snippet files MUST NOT contain ```python fence delimiters or exec=\"1\" attributes — those belong on the consuming fence, else the include runs at include-time not exec-time (RESEARCH Pitfall 4)"
  artifacts:
    - mkdocs.yml
    - docs/includes/load-canadian-weather.md
    - docs/includes/load-canadian-weather-precip.md
    - docs/includes/load-tecator.md
    - docs/includes/load-growth.md
    - docs/includes/load-phoneme.md
  key_links:
    - "mkdocs.yml pymdownx.snippets base_path: [docs] → --8<-- \"includes/NAME.md\" resolves to docs/includes/NAME.md"
    - "snippets substitution runs before markdown-exec → included preamble is part of the exec'd fence body"
---

<objective>
Enable pymdownx.snippets and factor the repeated dataset-loading preambles into docs/includes/ so example pages stop duplicating the CSV-loading block (FND-04). Prove it end-to-end by converting one real fence to use an include and confirming mkdocs build --strict still passes.

Purpose: 114 `from docs_data import` occurrences repeat across 17 example pages; centralizing the four+one loaders into includes removes that duplication and gives Phase 9's example sweep a single edit point per dataset.
Output: mkdocs.yml with pymdownx.snippets; five docs/includes/ fragments; one converted example fence proving the wiring.
</objective>

## Phase Goal (user story)

**As an** author maintaining the example pages, **I want to** reference a shared dataset-loading snippet with --8<-- instead of pasting the CSV-loading preamble into every fence, **so that** a loader change is a one-file edit and pages stay consistent.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable pymdownx.snippets in mkdocs.yml (FND-04)</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml — the existing markdown_extensions: block (around lines 49–67; pymdownx.superfences, pymdownx.tabbed, etc. already present); pymdownx.snippets is confirmed absent
    - .planning/phases/01-foundation/01-PATTERNS.md — the "mkdocs.yml — one block addition" block (exact YAML: pymdownx.snippets with base_path: [docs], insertion point after toc:)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 4 (mkdocs.yml change; base_path resolution semantics)
  </read_first>
  <action>
    Add a pymdownx.snippets entry to the markdown_extensions: list in mkdocs.yml with base_path: [docs], inserted after the toc: block per PATTERNS.md. base_path: [docs] makes --8<-- "includes/load-canadian-weather.md" resolve to docs/includes/load-canadian-weather.md. Add a one-line comment tagging FND-04. Do NOT touch other extensions or the plugins: block.
  </action>
  <verify>
    <automated>grep -q 'pymdownx.snippets' mkdocs.yml && python -c "import yaml; yaml.safe_load(open('mkdocs.yml')); print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'pymdownx.snippets' mkdocs.yml hits
    - The snippets entry declares base_path with docs
    - mkdocs.yml parses as valid YAML (yaml.safe_load succeeds)
    - No existing markdown_extensions entry removed (git diff shows only additions)
  </acceptance_criteria>
  <done>pymdownx.snippets is enabled with base_path: [docs] in mkdocs.yml.</done>
</task>

<task type="auto">
  <name>Task 2: Create the five docs/includes/ dataset-preamble snippets (FND-04)</name>
  <files>docs/includes/load-canadian-weather.md, docs/includes/load-canadian-weather-precip.md, docs/includes/load-tecator.md, docs/includes/load-growth.md, docs/includes/load-phoneme.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md — the "docs/includes/load-*.md" block (the four+one include files; the plain-Python-lines-only format; the target preamble pattern)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 4 (include file structure: raw Python lines only, no fence delimiters, no exec attributes; the four loaders map to include files; the precipitation 5th include)
    - docs/examples/canadian-weather.md — the actual repeated preamble to extract (the import numpy / from docs_fig import / from docs_data import load_canadian_weather / day, X, meta = load_canadian_weather("temperature") pattern)
    - python/fdars/datasets.py or docs' docs_data module — confirm the exact loader signatures (load_canadian_weather, load_tecator, load_growth, load_phoneme) before hardcoding them
  </read_first>
  <action>
    Create five docs/includes/*.md snippet fragments, each containing ONLY plain Python preamble lines (no ```python fence, no exec attributes — RESEARCH Pitfall 4): the common imports (import numpy as np; from docs_fig import fig, render; from docs_data import <loader>) plus the loader call. Map: load-canadian-weather.md → load_canadian_weather("temperature"); load-canadian-weather-precip.md → load_canadian_weather("precipitation"); load-tecator.md → load_tecator(); load-growth.md → load_growth(); load-phoneme.md → load_phoneme(). Confirm each loader signature against the actual docs_data/datasets source before writing (do not guess return tuples — copy the exact unpacking used in the existing example pages). A leading HTML comment naming the file (per PATTERNS.md) is fine; a fence delimiter is NOT.
  </action>
  <verify>
    <automated>bash -c 'for f in load-canadian-weather load-canadian-weather-precip load-tecator load-growth load-phoneme; do test -f "docs/includes/$f.md" || { echo "missing $f"; exit 1; }; grep -q "^\`\`\`" "docs/includes/$f.md" && { echo "$f has a fence delimiter forbidden"; exit 1; }; grep -q "exec=" "docs/includes/$f.md" && { echo "$f has exec attr forbidden"; exit 1; }; done; echo INCLUDES_OK'</automated>
  </verify>
  <acceptance_criteria>
    - All five docs/includes/*.md files exist
    - No include file contains a ``` fence delimiter (grep for ^``` finds nothing in each)
    - No include file contains an exec= attribute
    - Each include imports its loader and calls it with the mapped argument (canadian-weather → temperature; precip → precipitation)
    - Loader signatures match the existing example pages' usage (spot-checked against docs/examples/*.md)
  </acceptance_criteria>
  <done>Five plain-Python preamble snippets exist in docs/includes/, fence-free and exec-attribute-free.</done>
</task>

<task type="auto">
  <name>Task 3: Convert one example fence to use the include and prove mkdocs build --strict passes (FND-04)</name>
  <files>docs/includes/load-canadian-weather.md</files>
  <read_first>
    - docs/examples/canadian-weather.md — an early self-contained fence whose preamble matches load-canadian-weather.md (the conversion target); confirm the fence uses exec="1" html="1" and the day, X, meta unpacking
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 4 "Usage in an example page fence" (the --8<-- "includes/..." goes inside the exec fence, replacing the inline preamble) and the "Important caveat" (snippets substitute before markdown-exec)
    - docs/includes/load-canadian-weather.md — the snippet created in Task 2 (must exactly reproduce the preamble being replaced so the fence still runs)
  </read_first>
  <action>
    In ONE example page fence in docs/examples/canadian-weather.md whose inline preamble exactly matches load-canadian-weather.md, replace the inline import/load lines with a single --8<-- "includes/load-canadian-weather.md" line inside the same exec="1" html="1" fence (keep the page-specific code and print(render(f)) below it). This proves the snippets→markdown-exec ordering works end-to-end. Then run mkdocs build --strict (requires compiled fdars + PYTHONPATH=scripts) and confirm it succeeds and the figure still renders. Note: this is the ONLY page-content edit this phase makes to an example — the broader example sweep (converting all 17 pages, fixing API drift) is Phase 9, NOT this phase (per CONTEXT deferred). If the compiled fdars package is unavailable in the execution environment, record that mkdocs build must be run in CI/the phase verification and assert the include-substitution textually instead (grep the built site or the page for the resolved preamble).
  </action>
  <verify>
    <automated>grep -Fq '8<-- "includes/load-canadian-weather.md"' docs/examples/canadian-weather.md && echo INCLUDE_WIRED_OK</automated>
  </verify>
  <acceptance_criteria>
    - At least one exec fence in docs/examples/canadian-weather.md contains the --8<-- "includes/load-canadian-weather.md" line and no longer duplicates that preamble inline
    - The include line sits inside an exec="1" fence (the fence delimiters and print(render(f)) remain on the page, not in the include)
    - mkdocs build --strict succeeds with the converted fence (run in CI/phase verification if the local env lacks compiled fdars); the figure renders (no traceback in the built page)
  </acceptance_criteria>
  <done>One example fence sources its preamble via --8<-- and the strict build passes — the snippets pipeline is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/includes/ committed fragments → markdown-exec | included Python is textually substituted into exec fences at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03a | Tampering | markdown-exec code injection via snippets | low | mitigate | docs/includes/ is committed to git; no runtime/user content; only authors edit includes (RESEARCH Security Domain) |
| T-01-03b | Denial of service | empty/malformed include silently blanks a preamble | low | mitigate | mkdocs build --strict fails on the resulting NameError/render error rather than shipping a blank figure (Task 3) |

No networked service, auth, input handling, or cryptography. All threats Low at ASVS L1; block-on-high not tripped. This plan installs no packages (pymdownx is already bundled with mkdocs-material — no supply-chain checkpoint needed).
</threat_model>

<verification>
- pymdownx.snippets enabled with base_path: [docs].
- Five fence-free, exec-attribute-free include fragments exist in docs/includes/.
- One example fence uses the include and mkdocs build --strict passes.
</verification>

<success_criteria>
- Dataset-loading preambles are factored into docs/includes/ snippets and pymdownx.snippets is enabled, so example pages can stop repeating the CSV-loading block inline (ROADMAP SC #4, FND-04).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>

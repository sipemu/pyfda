---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/STYLE_SPEC.md
  - svgo.config.mjs
  - .github/workflows/docs.yml
autonomous: false
requirements: [FND-01, FND-02]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 34000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "docs/assets/diagrams/STYLE_SPEC.md exists and documents the palette, the five CSS classes (.ttl .sub .lab .sm .mono), stroke weights, viewBox width 720, allowed heights {300,480,520}, and contains a copy-paste <style> block (FND-01)"
    - "svgo.config.mjs (repo root) exists and disables inlineStyles, minifyStyles, cleanupIds, removeDesc, removeUnknownsAndDefaults, removeViewBox (FND-02, D-03)"
    - "Running npx svgo@3.3.4 --config svgo.config.mjs against a conforming diagram in stdout mode leaves the <style> block, IDs, <desc>, viewBox, and role/aria-label intact (FND-02)"
    - "The SVGO gate is check-only: it NEVER rewrites committed hand-authored SVGs, only diffs stdout against source (D-02)"
    - ".github/workflows/docs.yml runs the SVGO lint gate on all diagrams before mkdocs build, blocking on any diff (D-09, D-10)"
    - { statement: "The svgo diff gate produces zero diff on all 43 current diagrams under the config; any legacy diagram that fails is moved to a documented exclusion list rather than rewritten (RESEARCH Open Q1 / A2)", verification: backstop }
    - { statement: "For a malformed SVG or a diagram with no <style> block (the 8 legacy diagrams), the gate's behavior is decided during task 3 — pass if svgo would not touch it, else exclusion-list; the gate never edits the file (FND-02 empty/malformed edge)", verification: backstop }
  prohibitions:
    - "The SVGO gate MUST NOT rewrite, reformat, or optimize any committed hand-authored SVG in docs/assets/diagrams/ (D-02: hand-authored markup is the source of truth)"
    - "STYLE_SPEC conformance (viewBox width 720, correct class names, required accessibility attrs) MUST NOT be machine-enforced by the svgo gate this phase — it stays a human review-gate concern (D-03)"
  artifacts:
    - docs/assets/diagrams/STYLE_SPEC.md
    - svgo.config.mjs
    - .github/workflows/docs.yml
  key_links:
    - "svgo.config.mjs plugin overrides → the five preserved constructs (<style>, IDs, <desc>, viewBox, role/aria-label)"
    - "CI Gate A diff loop → docs/assets/diagrams/*.svg (must run before mkdocs build)"
    - "npx version pin svgo@3.3.4 → reproducible gate output (v4 has a different API)"
---

<objective>
Establish the SVG authoring contract (STYLE_SPEC.md) and prove the SVGO check-only lint gate end-to-end through CI on the real 43-diagram corpus. This is the phase TRACER: the thinnest path that touches every layer the phase's enforcement story depends on — spec baseline → svgo config → local diff gate → CI wiring — verified working before any expansion (determinism, snippets, doc-tests) is built.

Purpose: Every subsequent diagram-sweep phase (3–8) depends on a written spec to conform to and a machine gate that blocks non-conforming/unsafe SVG structure. Proving the gate green in CI on day one catches an architectural dead-end (e.g., legacy diagrams failing the gate) after one commit instead of after ten.
Output: STYLE_SPEC.md, svgo.config.mjs, and an extended docs.yml CI job with a blocking SVGO gate.
</objective>

## Phase Goal (user story)

**As an** author of the later diagram-sweep phases (3–8), **I want to** have a written SVG style spec and a machine lint gate that blocks non-conforming SVG structure in CI, **so that** every diagram I hand-author is checked against one shared contract before it ships.

(Framed per MVP_MODE from the ROADMAP Phase 1 Goal "The tooling and guardrails that every subsequent diagram sweep depends on are in place and verified working" — the ROADMAP Goal line is not in As-a/I-want/so-that form; this story is the planner's author-of-later-phases framing, not an invented scope change.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end SVGO gate — one diagram, wired through spec → config → local diff</name>
  <files>svgo.config.mjs, docs/assets/diagrams/STYLE_SPEC.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 1 (SVGO Check-Only Gate, the exact svgo.config.mjs, the live-test findings on elastic-alignment.svg) and Pattern 2 (canonical <style> block, viewBox conventions, palette, stroke weights)
    - .planning/phases/01-foundation/01-PATTERNS.md — the "svgo.config.mjs (config, no analog)" block (copy verbatim) and the "STYLE_SPEC.md" analog block
    - .planning/phases/01-foundation/01-CONTEXT.md — D-01, D-02, D-03 (zero-install pinned npx; check-only; optimization-safety scope only)
    - docs/assets/diagrams/elastic-alignment.svg — the canonical conforming diagram (lines 1–8 hold the <style> block and root role/aria-label); this is the tracer's proof diagram
  </read_first>
  <action>
    Create svgo.config.mjs at repo root exactly as in RESEARCH.md Pattern 1 / PATTERNS.md: a preset-default export with overrides disabling inlineStyles, minifyStyles, cleanupIds, removeDesc, removeUnknownsAndDefaults, removeViewBox — implementing D-03's optimization-safety-only scope (preserve <style>, IDs, <desc>, viewBox, role/aria-label). Create a first-pass STYLE_SPEC.md at docs/assets/diagrams/STYLE_SPEC.md that (per D-01) documents the pinned invocation using version svgo@3.3.4 and the mandatory --config flag, and embeds the canonical copy-paste <style> block verbatim from elastic-alignment.svg lines 2–8 (the five classes .ttl .sub .lab .sm .mono). Prove the tracer: run npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/elastic-alignment.svg --output - and diff against the source — a zero diff is the end-to-end proof that the config preserves all five constructs on a real diagram. Do NOT write STYLE_SPEC's full palette/stroke/viewBox tables yet (Task 2 completes those); this task proves the config-to-diagram path only.
  </action>
  <verify>
    <automated>test -f svgo.config.mjs && diff <(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/elastic-alignment.svg --output -) docs/assets/diagrams/elastic-alignment.svg && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - test -f svgo.config.mjs exits 0
    - The svgo config file body contains all six disabled plugins: inlineStyles, minifyStyles, cleanupIds, removeDesc, removeUnknownsAndDefaults, removeViewBox
    - diff of svgo stdout output against docs/assets/diagrams/elastic-alignment.svg exits 0 (zero diff — config is lossless on a conforming diagram)
    - docs/assets/diagrams/elastic-alignment.svg is byte-unchanged after the run (git status shows no modification to it)
    - STYLE_SPEC.md contains the literal five class selectors and a <style> block
  </acceptance_criteria>
  <reversibility rating="reversible">Zero-install npx + a check-only gate; swapping to a committed package.json or a different optimizer later requires no data migration (D-01).</reversibility>
  <done>svgo.config.mjs exists and produces zero diff on the canonical conforming diagram without modifying it; STYLE_SPEC.md exists with the canonical style block.</done>
</task>

<task type="auto">
  <name>Task 2: Complete STYLE_SPEC.md — palette, classes, stroke weights, viewBox spec (FND-01)</name>
  <files>docs/assets/diagrams/STYLE_SPEC.md</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — the first-pass file from Task 1 (extend it)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 2 (class semantics, ViewBox conventions with the 34/4/1 tally, Palette hex list with roles, Stroke weights, Accessibility pattern) and "SVG Baseline Summary"
    - .planning/phases/01-foundation/01-PATTERNS.md — the STYLE_SPEC analog block (palette from docs_fig.py lines 29–37, panel fill/border patterns, viewBox conventions)
    - scripts/docs_fig.py — FDARS_COLORS data-curve palette (lines 29–37)
  </read_first>
  <action>
    Extend STYLE_SPEC.md to fully satisfy FND-01. Document: (a) the palette — the seven structural hex values with their roles (near-black #1a1a2e title-only, muted grey #6c757d, mid-grey #495057, light grey #ced4da borders, near-white #f8f9fa panel fill, orange accent #fd7e14 with #fff4ea fill, pale orange #f8d7b8) plus the FDARS_COLORS data-curve palette; (b) the five typography classes with their exact font shorthand and semantics; (c) stroke weights (panel borders 1.5, axis/reference 1.2, primary curves 2–2.8, secondary curves 1.4–1.6, arrows 2); (d) the fixed viewBox width 720 and the three allowed heights {300, 480, 520}, noting the 4 legacy non-conforming viewBoxes as migration targets for later phases (NOT this phase); (e) the role="img" + aria-label accessibility pattern; (f) a callout that svgo MUST always be invoked with --config svgo.config.mjs and pinned svgo@3.3.4 (Pitfall 1, Pitfall 6). Keep the canonical <style> block from Task 1. Do NOT invent a new look — formalize the existing 35-diagram baseline (D per CONTEXT §specifics).
  </action>
  <verify>
    <automated>grep -q '\.ttl' docs/assets/diagrams/STYLE_SPEC.md && grep -q '\.mono' docs/assets/diagrams/STYLE_SPEC.md && grep -q '720' docs/assets/diagrams/STYLE_SPEC.md && grep -q '#1a1a2e' docs/assets/diagrams/STYLE_SPEC.md && grep -q 'aria-label' docs/assets/diagrams/STYLE_SPEC.md && grep -Fq 'svgo@3.3.4' docs/assets/diagrams/STYLE_SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - grep finds all five class names (.ttl .sub .lab .sm .mono) in STYLE_SPEC.md
    - grep finds the token 720 (fixed viewBox width) and the three heights 300, 480, 520
    - grep finds each of the seven structural palette hex values (#1a1a2e #6c757d #495057 #ced4da #f8f9fa #fd7e14 #f8d7b8)
    - grep finds aria-label and role="img" (accessibility pattern)
    - grep finds the literal svgo@3.3.4 pin and the --config requirement
    - The file names stroke weights for panel borders, axis lines, primary and secondary data curves
  </acceptance_criteria>
  <done>STYLE_SPEC.md documents the palette, five classes, stroke weights, viewBox width 720 + allowed heights, accessibility pattern, the copy-paste <style> block, and the pinned svgo invocation — the full FND-01 checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Run the diff gate across all 43 diagrams; build the exclusion list if any legacy diagram fails</name>
  <files>svgo.config.mjs, docs/assets/diagrams/STYLE_SPEC.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 1 "Important" paragraph (the 8 no-<style> legacy diagrams) and Open Question 1 / Assumption A2 (whether all 43 pass; add an exclusion list if a legacy diagram fails, do NOT rewrite it)
    - .planning/phases/01-foundation/01-RESEARCH.md — "SVG Baseline Summary" (the 4 non-conforming-viewBox diagrams: elastic-clustering.svg, outlier-detection.svg, covariance-functions.svg, ex-sonar-tsrvf.svg)
    - svgo.config.mjs — the config being validated
  </read_first>
  <action>
    Run the check-only diff loop over every file in docs/assets/diagrams/*.svg using npx svgo@3.3.4 --config svgo.config.mjs --quiet --input <svg> --output - diffed against the source. Record which diagrams (if any) produce a non-zero diff. Per RESEARCH Open Q1 / A2 and D-02, resolve any failure by ADDING the offending file to a documented exclusion list (a shell glob or explicit skip list consumed by the CI gate in Task 4) — NEVER by rewriting the hand-authored SVG. If all 43 pass, record that fact in STYLE_SPEC.md (or an adjacent note the CI step reads) and use no exclusions. Document the resolution (all-pass, or the exclusion list with the reason each file is excluded and which later DIA phase migrates it) in STYLE_SPEC.md under a short "SVGO gate coverage" heading so the CI step in Task 4 and future phases inherit the decision. This task resolves the FND-02 empty/no-<style> edge for the 8 legacy diagrams.
  </action>
  <verify>
    <automated>bash -c 'FAILED=0; FAILS=""; for svg in docs/assets/diagrams/*.svg; do diff <(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$svg" --output -) "$svg" >/dev/null 2>&1 || { FAILS="$FAILS $svg"; FAILED=1; }; done; echo "failing:$FAILS"; git diff --quiet docs/assets/diagrams/ && echo "NO_SVG_REWRITES" || { echo "ERROR: gate rewrote a committed SVG"; exit 1; }'</automated>
  </verify>
  <acceptance_criteria>
    - The diff loop runs against all 43 diagrams and the failing set is recorded (empty set OR a named exclusion list)
    - git diff --quiet docs/assets/diagrams/ exits 0 (NO committed SVG was modified by the gate — proves check-only, D-02)
    - STYLE_SPEC.md contains an "SVGO gate coverage" note stating either all-43-pass or the explicit exclusion list with a reason + owning later phase per excluded file
    - No SVG file appears in git status as modified
  </acceptance_criteria>
  <done>The gate's coverage over all 43 diagrams is established and documented; any legacy failure is excluded (not rewritten); no committed SVG was altered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4a: Package legitimacy checkpoint — svgo@3.3.4 (SUS verdict) before CI wiring</name>
  <what-built>Nothing installed yet. This is a blocking legitimacy gate required before the CI workflow invokes npx svgo@3.3.4. RESEARCH.md's Package Legitimacy Audit marks svgo@3.3.4 as SUS (the automated seam flagged the too-new v4.0.2 latest tag; v3.3.4 is the long-established stable v3 dist-tag with 36M weekly downloads and confirmed source repo github.com/svg/svgo, no postinstall script).</what-built>
  <how-to-verify>
    Confirm svgo@3.3.4 legitimacy before it is wired into CI:
    1. Open https://www.npmjs.com/package/svgo/v/3.3.4 — confirm it is the svg/svgo project, ~36M weekly downloads, source repo github.com/svg/svgo.
    2. Confirm you are pinning 3.3.4 (NOT latest / v4), because v4 has a different CLI/config API (Pitfall 6).
    3. Confirm no surprising postinstall behavior (the audit reports postinstall: null).
    Approve to allow Task 4b to wire npx svgo@3.3.4 into the CI workflow.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with CI wiring, or describe concerns (e.g., pin a different version).</resume-signal>
</task>

<task type="auto">
  <name>Task 4b: Wire the SVGO lint gate into CI (Gate A) — blocking on all diagrams (D-09, D-10)</name>
  <files>.github/workflows/docs.yml</files>
  <read_first>
    - .github/workflows/docs.yml — the existing single "deploy" job; the "Build and gate on figure errors" step (Gate A inserts BEFORE it) and the venv-activation pattern
    - .planning/phases/01-foundation/01-PATTERNS.md — the ".github/workflows/docs.yml — two step additions" block (Gate A YAML verbatim, step ordering)
    - .planning/phases/01-foundation/01-RESEARCH.md — "CI Extension" section (Gate A YAML, placement before mkdocs build)
    - docs/assets/diagrams/STYLE_SPEC.md — the "SVGO gate coverage" note from Task 3 (whether to iterate all diagrams or apply an exclusion list)
  </read_first>
  <action>
    Add a "Lint SVG diagrams (SVGO)" step to .github/workflows/docs.yml, inserted BEFORE the existing "Build and gate on figure errors" step (per D-10: the gate must run and block before mkdocs build). Use the diff loop from RESEARCH.md CI Extension Gate A: iterate docs/assets/diagrams/*.svg, diff npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$svg" --output - against the source, set FAILED=1 on any diff, and exit 1 if FAILED. If Task 3 produced an exclusion list, apply it (skip those globs) with an inline comment naming the reason. This step needs no venv activation (npx is node, not python) — but keep it consistent with surrounding step style. Add a comment pinning svgo@3.3.4 and explaining check-only semantics (D-02).
  </action>
  <verify>
    <automated>grep -Fq 'svgo@3.3.4' .github/workflows/docs.yml && grep -Fq -- '--config svgo.config.mjs' .github/workflows/docs.yml && grep -q 'Lint SVG diagrams' .github/workflows/docs.yml && python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/docs.yml')); print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs.yml contains a step named "Lint SVG diagrams (SVGO)" that runs before "Build and gate on figure errors"
    - The step invokes npx svgo@3.3.4 --config svgo.config.mjs in stdout mode and exits non-zero on any diff (blocking)
    - The YAML parses cleanly (python yaml.safe_load succeeds)
    - The step reflects the Task-3 coverage decision (all-diagrams loop, or exclusion list applied with a comment)
    - No committed SVG is rewritten by CI (the gate is diff-only)
  </acceptance_criteria>
  <reversibility rating="reversible">CI step is additive; removing it restores prior behavior with no artifacts to unwind.</reversibility>
  <done>docs.yml runs a blocking SVGO diff gate over the diagram corpus before the build step; the end-to-end tracer (spec → config → diff → CI) is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → CI runner | `npx svgo@3.3.4` fetches a package into the CI environment |
| committed SVG files → svgo process | hand-authored diagrams are read (never written) by the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `npx svgo@3.3.4` postinstall | low | mitigate | Pin exact version `svgo@3.3.4` (never `latest`/v4); audit confirms `postinstall: null`; legitimacy checkpoint (Task 4a) verifies before CI wiring |
| T-01-02 | Tampering | svgo `--input` path handling | low | accept | Input paths are hardcoded `docs/assets/diagrams/*.svg` globs in CI, never user-supplied — no practical path-traversal surface (ASVS L1) |
| T-01-03 | Tampering | committed hand-authored SVGs | low | mitigate | Gate runs stdout `--output -` + diff only; never `-o <file>`; Task 3/4b verify `git diff --quiet` (D-02: source of truth is never rewritten) |
| T-01-SC | Tampering | npm install of svgo | high | mitigate | slopcheck + blocking-human legitimacy checkpoint (Task 4a) before install/CI wiring for the SUS-flagged `svgo@3.3.4` |

All threats are Low at ASVS L1 except the mandated supply-chain checkpoint (T-01-SC), which is handled by the blocking-human gate; the block-on-high threshold is satisfied by that checkpoint.
</threat_model>

<verification>
- STYLE_SPEC.md satisfies the full FND-01 checklist (classes, palette, stroke weights, viewBox 720 + heights, <style> block, accessibility, pinned svgo invocation).
- svgo.config.mjs preserves all five constructs; zero diff on the canonical conforming diagram.
- The diff gate's coverage over all 43 diagrams is established and documented (all-pass or exclusion list); no committed SVG is rewritten.
- docs.yml runs the SVGO gate before mkdocs build, blocking on any diff.
</verification>

<success_criteria>
- Running `svgo --config svgo.config.mjs` against any conforming diagram produces no errors and leaves `<style>`, IDs, `<desc>`, `viewBox`, `role`/`aria-label` intact (ROADMAP SC #2).
- STYLE_SPEC.md exists and documents the full spec (ROADMAP SC #1).
- The gate is wired into CI and blocks (D-09, D-10).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>

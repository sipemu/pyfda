---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/docs_fig.py
autonomous: true
requirements: [FND-03, FND-06]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "scripts/docs_fig.py sets plt.rcParams['svg.hashsalt'] = 'fdars-docs' so matplotlib SVG element IDs are deterministic across builds (FND-03)"
    - "Two consecutive full mkdocs builds produce byte-identical SVG output from docs_fig.py exec blocks (FND-03, ROADMAP SC #3)"
    - "scripts/docs_fig.py exposes a fast(full, fast_value) helper that reads DOCS_FAST once and returns fast_value when DOCS_FAST is set, else full (FND-06, D-08)"
    - "Setting DOCS_FAST=1 causes the docs build to complete materially faster than the full build by lowering expensive iteration counts (FND-06, ROADMAP SC #6)"
    - "With DOCS_FAST unset the full build is the source of truth and is the only mode where the byte-identical determinism guarantee holds — determinism is NOT required in fast mode (D-07)"
    - { statement: "A build with zero stochastic figures is still byte-identical across two runs (hashsalt covers non-random ID nondeterminism); stochastic blocks additionally require an RNG seed, a convention documented here and audited per-section in phases 3-8 (FND-03 RNG-seeding edge, RESEARCH Pitfall 3)", verification: backstop }
  prohibitions:
    - "DOCS_FAST output figures MUST NOT be committed as publishable figures and MUST NOT be treated as the determinism source of truth — the full build (DOCS_FAST unset) is the only source of truth (D-07)"
    - "The DOCS_FAST switch MUST NOT be implemented as per-block scattered os.environ checks — it MUST route through the single fast() helper (D-08, DRY)"
  artifacts:
    - scripts/docs_fig.py
  key_links:
    - "plt.rcParams['svg.hashsalt'] must be set before any figure is rendered (module-import time in docs_fig.py) → deterministic IDs"
    - "fast() helper reads os.environ['DOCS_FAST'] once → later phases call fast(full, fast_value) at figure call sites"
---

<objective>
Add the two determinism-and-speed guardrails to the central figure helper: a fixed svg.hashsalt for byte-identical SVG IDs (FND-03) and a fast() helper that lets DOCS_FAST lower expensive iteration counts (FND-06). Both are single-file additions to scripts/docs_fig.py, the established central render entrypoint.

Purpose: FND-03 guarantees two consecutive builds diff clean (a precondition for meaningful diagram review across phases); FND-06 gives later-phase authors a fast local build loop. Wiring both into the one helper keeps every future figure block DRY.
Output: scripts/docs_fig.py with svg.hashsalt set and a fast(full, fast_value) helper.
</objective>

## Phase Goal (user story)

**As an** author rendering build-time figures in later phases, **I want to** get byte-identical SVG output on repeated full builds and a DOCS_FAST fast-build path, **so that** I can trust diffs during review and iterate locally without slow rebuilds.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set svg.hashsalt for deterministic SVG IDs (FND-03)</name>
  <files>scripts/docs_fig.py</files>
  <read_first>
    - scripts/docs_fig.py — the existing plt.rcParams.update({...}) dict (lines 39–58); add the new key here so it is set at module-import time, before any figure renders
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 3 (FND-03 addition; svg.hashsalt confirmed present in matplotlib 3.10.8, defaults to None) and Pitfall 3 (hashsalt does not seed RNGs)
    - .planning/phases/01-foundation/01-PATTERNS.md — the "scripts/docs_fig.py — additions only" block (exact rcParams key to add)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-07 (determinism required only in full build)
  </read_first>
  <action>
    Add the key "svg.hashsalt": "fdars-docs" to the existing plt.rcParams.update({...}) dict in scripts/docs_fig.py (after the existing keys, per PATTERNS.md). This fixes matplotlib element IDs (default None uses uuid4 → IDs differ every run). Note in a short comment that this alone does not seed RNGs — stochastic exec blocks must seed their own RNG (rng = np.random.default_rng(42)); the per-block seed audit is a phases-3–8 concern (RESEARCH Pitfall 3), and STYLE_SPEC.md / this comment records the convention. Do NOT restructure the rcParams dict or change other keys.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts python -c "import docs_fig, matplotlib.pyplot as plt; assert plt.rcParams['svg.hashsalt']=='fdars-docs', plt.rcParams['svg.hashsalt']; print('HASHSALT_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Importing docs_fig sets plt.rcParams['svg.hashsalt'] to the literal fdars-docs
    - The key lives inside the existing rcParams.update dict (grep 'svg.hashsalt' scripts/docs_fig.py hits inside that block)
    - No other rcParams key value changed (git diff shows only the added line + comment)
  </acceptance_criteria>
  <done>docs_fig.py sets svg.hashsalt='fdars-docs' at import time; matplotlib SVG IDs are now deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Add the fast(full, fast_value) DOCS_FAST helper (FND-06, D-08)</name>
  <files>scripts/docs_fig.py</files>
  <read_first>
    - scripts/docs_fig.py — end of file (after render(), line 85); the module docstring Usage block (lines 7–17) and the import style (line 19, from __future__ import annotations)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 3 (FND-06 addition: exact fast() signature and docstring; the confirmed expensive params n_perm→50, max_iter→5, nb→100, oversampling→2)
    - .planning/phases/01-foundation/01-PATTERNS.md — the "New fast() helper" block (verbatim; the import os as _os leading-underscore-alias convention)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-07 (speed-only; fast figures may look rougher; NOT for committed figures), D-08 (central helper reads env once)
  </read_first>
  <action>
    Add def fast(full, fast_value) after render() exactly per RESEARCH Pattern 3 / PATTERNS.md: it returns fast_value if os.environ.get("DOCS_FAST") is truthy, else full — reading the env var once per call (D-08's DRY central-helper intent; later-phase blocks call fast(500, 50) etc. instead of inlining os.environ checks). Add import os as _os using the leading-underscore-alias convention (keeps it out of help(docs_fig)). Update the module docstring Usage block to mention fast(). Include the docstring's usage examples (n_perm=fast(500, 50), max_iter=fast(20, 5), nb=fast(800, 100)). Per D-07, the docstring states fast mode is speed-only and NOT the determinism/publish source of truth.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts python -c "import os,importlib,docs_fig; os.environ.pop('DOCS_FAST',None); importlib.reload(docs_fig); assert docs_fig.fast(500,50)==500; os.environ['DOCS_FAST']='1'; importlib.reload(docs_fig); assert docs_fig.fast(500,50)==50; print('FAST_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs_fig.fast(500, 50) returns 500 when DOCS_FAST is unset
    - docs_fig.fast(500, 50) returns 50 when DOCS_FAST=1
    - The helper reads os.environ (not a hardcoded per-call constant) and is the single DOCS_FAST switch (grep shows one os.environ.get('DOCS_FAST') in docs_fig.py)
    - The module docstring Usage block references fast() and states it is speed-only (D-07)
  </acceptance_criteria>
  <done>docs_fig.py exposes fast(full, fast_value) driven by DOCS_FAST, DRY per D-08; determinism-only-in-full-mode documented per D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOCS_FAST env var → build behavior | a developer-set env var toggles iteration counts at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02a | Tampering | DOCS_FAST-produced figures mistaken for publishable output | low | mitigate | Helper docstring + D-07 state fast mode is speed-only and never the source of truth; full build (DOCS_FAST unset) is the only determinism/publish mode |
| T-01-02b | Information disclosure | svg.hashsalt static string | low | accept | hashsalt is a build-determinism salt for element IDs, not a secret; a fixed value is the intended, documented behavior (matplotlib rcParam) |

No networked service, auth, input handling, or cryptography in this plan. All threats Low at ASVS L1; block-on-high threshold not tripped. This plan installs no packages (no supply-chain checkpoint needed).
</threat_model>

<verification>
- svg.hashsalt='fdars-docs' set at docs_fig import time (FND-03 mechanism in place).
- fast() returns full when DOCS_FAST unset and fast_value when set (FND-06).
- The full-build byte-identical determinism guarantee is exercised end-to-end by the plan's success criteria (two consecutive mkdocs builds diff clean on docs_fig SVG output) — note this requires the compiled fdars package + mkdocs, run in the phase verification/CI, not in the unit checks above.
</verification>

<success_criteria>
- Two consecutive full `mkdocs build` runs produce byte-identical SVG output from docs_fig.py exec blocks (ROADMAP SC #3, FND-03).
- `DOCS_FAST=1 mkdocs build` completes materially faster than the full build (ROADMAP SC #6, FND-06).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>

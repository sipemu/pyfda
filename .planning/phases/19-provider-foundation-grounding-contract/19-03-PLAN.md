---
phase: 19-provider-foundation-grounding-contract
plan: 03
type: tdd
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - tests/test_advisor_providers.py
autonomous: true
requirements:
  - PROV-01
  - PROV-06
  - GROUND-01
  - GROUND-02
  - GROUND-03
  - GROUND-04
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Test-only fake providers (native, fallback, refusal) satisfy the Provider protocol via isinstance (PROV-01)"
    - "Native path returns a validated Advice; fallback path validates raw dict, retries once on bad JSON, and raises after the 2-retry cap (GROUND-01, GROUND-02)"
    - "A refusal provider raises rather than returning a vacuous Advice (GROUND-04)"
    - "_check_grounding passes grounded/qualitative evidence and raises GroundingViolationError on a fabricated number, on every provider path (GROUND-03)"
    - "resolve_provider precedence (default anthropic, instance passthrough, env provider/model, unknown-raises) is verified offline (PROV-06)"
    - "All new tests run network-free with no ANTHROPIC_API_KEY; tests/test_advisor.py remains untouched"
  artifacts:
    - tests/test_advisor_providers.py
  key_links:
    - "Fake providers exercise ValidateAndRetry native + fallback paths with no network"
    - "advise(provider=<fake>) drives the centralized _check_grounding path"
---

<objective>
Add a new offline test suite tests/test_advisor_providers.py that exercises the provider/grounding machinery built in 19-01/19-02 using test-only in-memory fake providers (no network, no keys). Explicitly covers the paths the guardrail suite cannot reach: native vs fallback selection, retry-cap raise, refusal raise, grounding rejection, and env/param precedence.

Purpose: The existing tests/test_advisor.py is the pure-refactor guardrail and must stay byte-identical; all NEW behavior gets its own file so a failure localizes cleanly (RESEARCH.md "Test Organization Principle"). These tests turn the seven Phase 19 requirements into named, automated assertions.

Output: tests/test_advisor_providers.py with the TestProviderProtocol / TestValidateAndRetry / TestCheckGrounding / TestResolveProvider / TestAdviseIntegration classes from RESEARCH.md "Test Strategy", all green and offline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-provider-foundation-grounding-contract/19-RESEARCH.md
@python/fdars/advisor/providers/__init__.py
@tests/test_advisor.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Fake providers + protocol conformance + ValidateAndRetry path tests</name>
  <files>tests/test_advisor_providers.py</files>
  <behavior>
    - FakeNativeProvider (supports_native_structured_output=True, complete_structured returns schema.model_validate(dict)), FakeFallbackProvider (=False, returns a raw dict), FakeRefusalProvider (raises ValueError) each satisfy isinstance(_, Provider).
    - test_native_path_returns_advice_directly: ValidateAndRetry(FakeNativeProvider(valid dict)).complete_structured(...) returns an Advice instance.
    - test_fallback_path_validates_raw_dict: ValidateAndRetry(FakeFallbackProvider(valid dict)) returns an Advice.
    - test_fallback_retry_on_bad_json: fallback provider returns malformed dict first then valid — an Advice is returned (one retry consumed).
    - test_fallback_raises_after_max_retries: fallback provider always malformed — ValueError raised (not an Advice, no fabrication).
    - test_refusal_raises: ValidateAndRetry(FakeRefusalProvider) raises ValueError.
  </behavior>
  <action>Write RED-first: define the three fake providers (from RESEARCH.md "Test-Only Fake Provider"), then TestProviderProtocol (isinstance against fdars.advisor.providers.Provider) and TestValidateAndRetry covering native return, fallback validate, single-retry recovery, retry-cap raise, and refusal raise. Import Provider, ValidateAndRetry from fdars.advisor.providers and Advice from fdars.advisor. Build valid Advice-shaped dicts by inspecting the Advice/Recommendation schema fields in fdars.advisor._schema. All tests must be network-free and require no ANTHROPIC_API_KEY. Do NOT modify tests/test_advisor.py.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && pytest tests/test_advisor_providers.py::TestProviderProtocol tests/test_advisor_providers.py::TestValidateAndRetry -x -q</automated>
  </verify>
  <done>Fake providers satisfy the protocol; native path returns Advice; fallback validates and retries once; retry-cap and refusal both raise ValueError — all offline.</done>
</task>

<task type="tdd">
  <name>Task 2: _check_grounding tests (grounded pass, fabricated reject, qualitative pass, native-path coverage)</name>
  <files>tests/test_advisor_providers.py</files>
  <behavior>
    - test_grounding_passes_when_all_numbers_in_diagnostics: Advice whose evidence cites only values present in the diagnostics dict → no exception.
    - test_grounding_rejects_fabricated_number: evidence citing k=7 when diagnostics has k=4 → GroundingViolationError.
    - test_grounding_passes_qualitative_evidence: evidence with no numbers ("Alignment converged quickly") → no exception.
    - test_grounding_runs_on_native_path: advise(diag, task=..., domain_context=..., provider=FakeNativeProvider(fabricated Advice)) raises GroundingViolationError — proving grounding runs on the provider path, centralized in advise().
  </behavior>
  <action>Add TestCheckGrounding to tests/test_advisor_providers.py. Import _check_grounding and GroundingViolationError from fdars.advisor.providers, advise and Advice from fdars.advisor. For the direct-call tests, construct Advice/Recommendation instances (or model_validate dicts) with controlled evidence strings and a small diagnostics dict. For test_grounding_runs_on_native_path, pass a FakeNativeProvider pre-baked with a fabricated numeric citation as advise(..., provider=fake) and assert GroundingViolationError — this proves GROUND-03 centralization (grounding runs even when the provider is not Anthropic). No network, no key.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && pytest tests/test_advisor_providers.py::TestCheckGrounding -x -q</automated>
  </verify>
  <done>Grounded and qualitative evidence pass; fabricated numeric citations raise GroundingViolationError both via direct _check_grounding and via advise(provider=fake); all offline.</done>
</task>

<task type="tdd">
  <name>Task 3: resolve_provider precedence + advise() integration with fake provider</name>
  <files>tests/test_advisor_providers.py</files>
  <behavior>
    - test_default_returns_anthropic_wrapped: resolve_provider() returns a ValidateAndRetry whose name == "anthropic" (with anthropic import guarded/patched so no network).
    - test_explicit_provider_instance_passthrough: resolve_provider(provider=FakeNativeProvider(...)) wraps the instance in ValidateAndRetry.
    - test_env_var_provider_selection: FDARS_ADVISOR_PROVIDER="anthropic" selects anthropic.
    - test_env_var_model_override: FDARS_ADVISOR_MODEL="claude-haiku-4-5" is used as the resolved model.
    - test_unknown_provider_raises: resolve_provider(provider="ollama") raises ValueError with a message naming that only anthropic is supported this phase.
    - test_advise_with_fake_provider_returns_advice: advise(diag, task="interpretation", domain_context="x", provider=fake) returns an Advice.
  </behavior>
  <action>Add TestResolveProvider and TestAdviseIntegration to tests/test_advisor_providers.py. Use monkeypatch for FDARS_ADVISOR_PROVIDER / FDARS_ADVISOR_MODEL env vars and to avoid constructing a real Anthropic client where the assertion only needs name/model (e.g. patch AnthropicProvider.__init__ or the anthropic import so no network/key is required). Assert precedence params > env > default (PROV-06). For the advise() integration, pass a FakeNativeProvider carrying a grounded Advice dict and assert isinstance(result, Advice). Confirm the whole new file plus the untouched guardrail file both pass together.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && pytest tests/test_advisor_providers.py -q && pytest tests/test_advisor.py -q</automated>
  </verify>
  <done>resolve_provider precedence (default/instance/env-provider/env-model/unknown-raise) verified; advise(provider=fake) returns Advice; the full new suite and the untouched guardrail suite both pass offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fake -> ValidateAndRetry/advise | In-memory only; no network; asserts the grounding/retry contract deterministically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-06 | Tampering | fabricated-number leak | high | mitigate | test_grounding_rejects_fabricated_number + native-path grounding test assert GroundingViolationError |
| T-19-07 | Repudiation | silent fabrication after retries | high | mitigate | test_fallback_raises_after_max_retries asserts deterministic raise, not a fabricated Advice |
</threat_model>

<verification>
- pytest tests/test_advisor_providers.py -q  (all new offline tests pass)
- pytest tests/test_advisor.py -q  (guardrail still green; file untouched)
- Confirm no test requires ANTHROPIC_API_KEY: run the suite with the key unset
</verification>

<success_criteria>
tests/test_advisor_providers.py covers PROV-01, PROV-06, and GROUND-01–04 with in-memory fake providers, runs network-free, and passes alongside the unchanged tests/test_advisor.py guardrail.
</success_criteria>

<output>
Create `.planning/phases/19-provider-foundation-grounding-contract/19-03-SUMMARY.md` when done
</output>

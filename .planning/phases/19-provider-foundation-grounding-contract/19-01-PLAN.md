---
phase: 19-provider-foundation-grounding-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/providers/__init__.py
  - python/fdars/advisor/providers/_protocol.py
  - python/fdars/advisor/providers/_validate.py
  - python/fdars/advisor/providers/_factory.py
  - python/fdars/advisor/providers/anthropic.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-06
  - GROUND-01
  - GROUND-02
  - GROUND-03
  - GROUND-04
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "advise() runs end-to-end through resolve_provider() -> AnthropicProvider.complete_structured() and returns the same Advice for provider=None (PROV-02)"
    - "A Provider protocol exposes complete_structured(schema, messages, system), name, model, supports_native_structured_output (PROV-01)"
    - "resolve_provider() honors params > env (FDARS_ADVISOR_PROVIDER/_MODEL/_BASE_URL + keys) > anthropic default (PROV-06)"
    - "Native path returns a schema-validated Advice; non-native path validates-and-retries with max_retries=2 and raises deterministically after the cap (GROUND-01, GROUND-02)"
    - "_check_grounding runs in advise() after complete_structured() on every provider path and rejects fabricated numbers (GROUND-03)"
    - "A provider refusal or empty response raises a clear error, never a vacuously-valid Advice (GROUND-04)"
    - "pytest tests/test_advisor.py passes unchanged (pure-refactor gate)"
  artifacts:
    - python/fdars/advisor/__init__.py
    - python/fdars/advisor/providers/_protocol.py
    - python/fdars/advisor/providers/_validate.py
    - python/fdars/advisor/providers/_factory.py
    - python/fdars/advisor/providers/anthropic.py
  key_links:
    - "python/fdars/__init__.py:72 sys.modules injection resolves the new advisor/ package unchanged"
    - "advise() -> resolve_provider() -> AnthropicProvider (deferred anthropic import via _require_anthropic inside a function body)"
    - "advise() calls _check_grounding(advice, diagnostics) after complete_structured() returns"
---

<objective>
TRACER: convert python/fdars/advisor.py into a package python/fdars/advisor/ and wire ONE complete behavioral path end-to-end — advise() -> resolve_provider() -> AnthropicProvider.complete_structured() -> ValidateAndRetry -> _check_grounding -> return Advice — proving pytest tests/test_advisor.py stays green. This is a PURE REFACTOR: existing Anthropic behavior and outputs are unchanged.

Purpose: Prove the provider architecture end-to-end on a single path before any mechanical file-splitting, so an architectural dead-end surfaces after one commit rather than ten. All seven Phase 19 requirements are behaviorally exercised by this one wired path; plans 19-02 (mechanical split) and 19-03 (explicit unit tests) build out from this proven slice.

Output: A working advisor/ package whose advise() runs through the Provider protocol with the AnthropicProvider adapter, grounding centralized in advise(), and the existing guardrail suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-provider-foundation-grounding-contract/19-RESEARCH.md
@python/fdars/advisor.py
@python/fdars/__init__.py
@tests/test_advisor.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Convert advisor.py to advisor/ package (whole-module move, zero behavior change)</name>
  <files>python/fdars/advisor/__init__.py, python/fdars/advisor.py</files>
  <precondition>python/fdars/__init__.py:64 does `from fdars import advisor` and line 72 does `_sys.modules["fdars.advisor"] = advisor` — verify these two lines are present and unchanged before starting (RESEARCH.md "sys.modules Injection").</precondition>
  <action>Create the directory python/fdars/advisor/ and move the entire current contents of python/fdars/advisor.py verbatim into python/fdars/advisor/__init__.py, then delete python/fdars/advisor.py. Do NOT rename, split, or alter any function, constant, class, or import at this step — this is a pure file-to-directory move so that `from fdars import advisor` resolves to advisor/__init__.py. Do NOT touch python/fdars/__init__.py: per RESEARCH.md the `from fdars import advisor` (line 64) and `_sys.modules["fdars.advisor"] = advisor` (line 72) injection resolve the package automatically. Preserve __all__ exactly (build_diagnostics, advise, describe_cluster_differences, Advice, Recommendation). This delivers PROV-02's zero-public-API-change guarantee at the package boundary.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python -c "from fdars import advisor; print(advisor.__file__)" | grep -q "advisor/__init__.py" && pytest tests/test_advisor.py -x -q</automated>
  </verify>
  <done>python/fdars/advisor.py no longer exists; python/fdars/advisor/__init__.py exists; `from fdars import advisor` resolves to the package __init__; all of tests/test_advisor.py passes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add providers/ layer — Provider protocol, AnthropicProvider, ValidateAndRetry, _check_grounding, resolve_provider</name>
  <files>python/fdars/advisor/providers/__init__.py, python/fdars/advisor/providers/_protocol.py, python/fdars/advisor/providers/anthropic.py, python/fdars/advisor/providers/_validate.py, python/fdars/advisor/providers/_factory.py</files>
  <read_first>python/fdars/advisor/__init__.py (the moved module — lines around the advise() body extracted at old advisor.py:978-1007, and the _require_anthropic/_require_pydantic guards)</read_first>
  <action>Create the providers/ subpackage implementing the surfaces specified verbatim in RESEARCH.md sections "Provider Protocol Design", "Grounding + Retry Contract", and "Selection / Precedence":
  - _protocol.py: a @runtime_checkable typing.Protocol named Provider with attributes name (str), model (str), method complete_structured(schema, messages, system), and a supports_native_structured_output capability flag (PROV-01). Import only typing; no fdars/anthropic imports.
  - anthropic.py: AnthropicProvider with name="anthropic", supports_native_structured_output=True. Its __init__ constructs the client and calls the deferred anthropic import; per RESEARCH.md the guard MUST do a bare `import anthropic` inside a function body (reuse the existing _require_anthropic from advisor/__init__.py) so the test_advise_raises_importerror_without_anthropic monkeypatch on sys.modules["anthropic"] still fires. complete_structured() runs client.messages.parse(model=self.model, max_tokens=16000, thinking={"type": "adaptive"}, system=system, output_format=schema, messages=messages) — identical parameters to old advise() at old advisor.py:991-998 — and raises ValueError when response.parsed_output is None, carrying the stop_reason (GROUND-04, GROUND-01).
  - _validate.py: ValidateAndRetry(provider) proxying name/model/supports_native_structured_output; complete_structured() returns the native result directly when supports_native_structured_output is true (GROUND-01), else runs _fallback_with_retry with MAX_RETRIES=2, pydantic model_validate, a repair message that re-appends the ORIGINAL messages (full diagnostics re-included), and a deterministic raise after the cap — never fabricating (GROUND-02). Also define GroundingViolationError(ValueError) and _check_grounding(advice, diagnostics) using the numeric-substring heuristic from RESEARCH.md (_flatten_diagnostics_text adds str, .3f and .4f float reprs; _extract_numbers uses the decimal-token regex); grounding failure raises immediately and never triggers a repair retry (GROUND-03).
  - _factory.py: resolve_provider(provider=None, model=None, api_key=None, base_url=None, **kw) returning a ValidateAndRetry-wrapped adapter with precedence params > FDARS_ADVISOR_PROVIDER/_MODEL/_BASE_URL + per-provider key env > anthropic default; if provider is already a Provider instance, wrap and return it; unknown provider names raise ValueError naming that only "anthropic" is supported this phase (PROV-06). Read FDARS_ADVISOR_BASE_URL into the resolved base_url even though only anthropic consumes it this phase.
  Import the providers layer LAZILY (inside resolve_provider / adapter constructors) so `import fdars` never touches an LLM SDK. providers/__init__.py exports Provider, AnthropicProvider, ValidateAndRetry, resolve_provider, _check_grounding, GroundingViolationError.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python -c "import fdars; from fdars.advisor.providers import Provider, AnthropicProvider, ValidateAndRetry, resolve_provider, _check_grounding; print('ok')" && python -c "import sys; sys.modules['anthropic']=None; import fdars.advisor.providers._factory as f" </automated>
  </verify>
  <done>providers/ package imports cleanly; Provider, AnthropicProvider, ValidateAndRetry, resolve_provider, _check_grounding, GroundingViolationError are importable; importing the providers layer does not import the anthropic SDK at module load.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire advise() through resolve_provider() + centralized _check_grounding (extract old inline call)</name>
  <files>python/fdars/advisor/__init__.py</files>
  <behavior>
    - With provider=None and pydantic present, advise() builds the same system prompt and user_content as today, calls resolve_provider(provider=None, model=model), passes Advice + messages + system to complete_structured(), then calls _check_grounding(advice, diagnostics) and returns the Advice.
    - With sys.modules["anthropic"] monkeypatched to None, advise() raises ImportError matching "pip install fdars\\[advisor\\]" (guardrail test_advise_raises_importerror_without_anthropic).
    - A fabricated numeric citation in the returned Advice's evidence raises GroundingViolationError (behavior exercised fully in plan 19-03; here confirm the call site exists and runs).
  </behavior>
  <action>Extract the inline Anthropic call block (old advisor.py:978-1007) out of advise(). Add the parameter provider: "str | Provider | None" = None to advise() AFTER model (keyword-only), keeping model="claude-opus-4-8" default so every existing call site is unchanged (RESEARCH.md "advise() Current Signature"). The new advise() body: call _require_pydantic(); p = resolve_provider(provider=provider, model=model) (import resolve_provider lazily from advisor.providers inside the function body); build system = _system_prompt(task) and the user_content exactly as today; messages = [{"role": "user", "content": user_content}]; advice = p.complete_structured(Advice, messages, system); _check_grounding(advice, diagnostics) (import lazily from advisor.providers); return advice. Do NOT call _check_grounding inside AnthropicProvider — it must live in advise() so it runs on every provider (GROUND-03). Update describe_cluster_differences() to accept provider=None and forward it to advise() (RESEARCH.md section 6), and add provider= to its docstring Parameters. Leave _require_anthropic/_require_pydantic/_system_prompt/build_diagnostics and all builders in place (their final split is plan 19-02).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && pytest tests/test_advisor.py -x -q && grep -rn "reason only from" python/fdars/advisor/ | wc -l | grep -qx 1</automated>
  </verify>
  <done>advise() no longer contains the inline client.messages.parse block; it routes through resolve_provider() and calls _check_grounding() after completion; the grounding-invariant sentence appears exactly once under advisor/; all of tests/test_advisor.py passes unchanged including the ImportError monkeypatch test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisor -> Anthropic API | Outbound HTTPS; diagnostics serialized via json.dumps; no user input executed as code |
| LLM response -> Advice | Untrusted model output crosses into the returned object; grounding check is the guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | LLM response (fabricated numbers) | high | mitigate | _check_grounding rejects Advice citing values absent from diagnostics (GROUND-03) |
| T-19-02 | Information Disclosure | ANTHROPIC_API_KEY | low | accept | Key read from env only, never stored in code (unchanged from v2.0) |
| T-19-03 | Denial of Service | retry loop | low | mitigate | max_retries=2 hardcoded; deterministic raise after cap (GROUND-02) |
</threat_model>

<verification>
- pytest tests/test_advisor.py -x  (guardrail: pure-refactor gate, must be byte-identical pass)
- python -c "from fdars import advisor; print(advisor.__file__)" prints .../fdars/advisor/__init__.py
- grep -rn "reason only from" python/fdars/advisor/ returns exactly one location
- python -c "import fdars" succeeds with anthropic absent (offline import guarantee)
</verification>

<success_criteria>
advise() runs end-to-end through the Provider protocol + AnthropicProvider adapter with grounding centralized in advise(); provider=None reproduces today's behavior; tests/test_advisor.py passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/19-provider-foundation-grounding-contract/19-01-SUMMARY.md` when done
</output>

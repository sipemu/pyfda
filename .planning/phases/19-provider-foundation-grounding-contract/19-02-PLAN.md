---
phase: 19-provider-foundation-grounding-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_schema.py
  - python/fdars/advisor/_prompts.py
  - python/fdars/advisor/aspects/__init__.py
  - python/fdars/advisor/aspects/alignment.py
  - python/fdars/advisor/aspects/fpca.py
  - python/fdars/advisor/aspects/basis.py
  - python/fdars/advisor/aspects/smoothing.py
  - python/fdars/advisor/aspects/clustering.py
autonomous: true
requirements:
  - PROV-02
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Advice/Recommendation schema lives in advisor/_schema.py, re-exported from advisor/__init__.py (PROV-02)"
    - "_system_prompt and the single _GROUNDING_INVARIANT constant live in advisor/_prompts.py; the invariant sentence appears exactly once (PROV-02)"
    - "All five _build_*_diagnostics builders live under advisor/aspects/ and are imported lazily by build_diagnostics (PROV-02)"
    - "pytest tests/test_advisor.py passes unchanged (pure-refactor gate)"
  artifacts:
    - python/fdars/advisor/_schema.py
    - python/fdars/advisor/_prompts.py
    - python/fdars/advisor/aspects/alignment.py
    - python/fdars/advisor/aspects/fpca.py
    - python/fdars/advisor/aspects/basis.py
    - python/fdars/advisor/aspects/smoothing.py
    - python/fdars/advisor/aspects/clustering.py
  key_links:
    - "advisor/__init__.py imports Advice/Recommendation from _schema and _system_prompt from _prompts"
    - "build_diagnostics() dispatcher imports each aspect builder lazily inside its branch"
    - "_schema.py and _prompts.py import only pydantic/typing — no advisor imports (no circular import)"
---

<objective>
MECHANICAL SPLIT (pure refactor): move the schema, system prompt, and the five offline diagnostics builders out of advisor/__init__.py into their final submodule locations (_schema.py, _prompts.py, aspects/*.py) with zero behavior change. The tracer path proven in plan 19-01 stays green throughout.

Purpose: Land the file structure RESEARCH.md prescribes so Phase 20 (providers/openai.py etc.) and Phase 21 (aspects/depth.py etc.) drop in cleanly. Doing this AFTER the behavioral wire means any failing test here points at a file-move mistake, not at wiring.

Output: The final advisor/ package layout with schema, prompts, and aspects separated; build_diagnostics dispatches to aspects/ lazily; tests/test_advisor.py still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-provider-foundation-grounding-contract/19-RESEARCH.md
@python/fdars/advisor/__init__.py
@tests/test_advisor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract _schema.py (Advice/Recommendation + fallback stubs) and _prompts.py (_system_prompt + _GROUNDING_INVARIANT)</name>
  <files>python/fdars/advisor/_schema.py, python/fdars/advisor/_prompts.py, python/fdars/advisor/__init__.py</files>
  <read_first>python/fdars/advisor/__init__.py (Advice/Recommendation block at old advisor.py:67-181; _system_prompt at old advisor.py:809-933 incl. the grounding invariant sentence)</read_first>
  <action>Move the Advice and Recommendation Pydantic models AND both branches of the try/except ImportError fallback stubs verbatim into advisor/_schema.py (old advisor.py:67-181). _schema.py imports only pydantic (inside try/except) and typing — no advisor imports (avoids the circular import in RESEARCH.md Risk 2). Move _system_prompt into advisor/_prompts.py (old advisor.py:809-933); _prompts.py imports only typing. Per RESEARCH.md section 4, extract the grounding-invariant sentence into a module-level constant _GROUNDING_INVARIANT in _prompts.py and build _system_prompt's text from it so grep finds exactly ONE copy of "reason only from" under advisor/; add an unused-for-now parameter aspect: str = "" to _system_prompt (Phase 21 will use it) without changing today's output. In advisor/__init__.py, replace the moved code with `from fdars.advisor._schema import Advice, Recommendation` and `from fdars.advisor._prompts import _system_prompt`, keeping __all__ unchanged. Do NOT change the schema fields, the fallback-stub behavior, or the prompt text.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python -c "from fdars.advisor._schema import Advice, Recommendation; print('ok')" && python -c "from fdars.advisor._prompts import _system_prompt, _GROUNDING_INVARIANT; print('ok')" && grep -rn "reason only from" python/fdars/advisor/ | wc -l | grep -qx 1 && pytest tests/test_advisor.py -x -q</automated>
  </verify>
  <done>Advice/Recommendation importable from advisor._schema; _system_prompt and _GROUNDING_INVARIANT importable from advisor._prompts; the invariant sentence appears exactly once under advisor/; advise()/describe_cluster_differences still work; tests/test_advisor.py passes.</done>
</task>

<task type="auto">
  <name>Task 2: Move the five diagnostics builders into advisor/aspects/</name>
  <files>python/fdars/advisor/aspects/__init__.py, python/fdars/advisor/aspects/alignment.py, python/fdars/advisor/aspects/fpca.py, python/fdars/advisor/aspects/basis.py, python/fdars/advisor/aspects/smoothing.py, python/fdars/advisor/aspects/clustering.py</files>
  <read_first>python/fdars/advisor/__init__.py (builders at old advisor.py:258-342, 349-419, 426-516, 519-625, 632-740)</read_first>
  <action>Create advisor/aspects/ (with an empty aspects/__init__.py) and move each _build_*_diagnostics function plus its private helpers verbatim into its own module, per the RESEARCH.md "Diagnostics Builders" table: _build_alignment_diagnostics -> aspects/alignment.py, _build_fpca_diagnostics -> aspects/fpca.py, _build_basis_diagnostics -> aspects/basis.py, _build_smoothing_diagnostics -> aspects/smoothing.py, _build_clustering_diagnostics -> aspects/clustering.py. Carry along whatever numpy/typing imports and existing lazy `from fdars.alignment import ...` / `from fdars.basis import ...` calls each builder uses (those stay lazy, inside the function bodies, as today). Do NOT alter any numeric logic — outputs must stay byte-identical (this is the determinism guardrail in test_build_diagnostics_deterministic). Keep _selfcheck_alignment_diagnostics in advisor/__init__.py for now (RESEARCH.md Open Question 2 — safer, not externally imported).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python -c "from fdars.advisor.aspects.alignment import _build_alignment_diagnostics; from fdars.advisor.aspects.fpca import _build_fpca_diagnostics; from fdars.advisor.aspects.basis import _build_basis_diagnostics; from fdars.advisor.aspects.smoothing import _build_smoothing_diagnostics; from fdars.advisor.aspects.clustering import _build_clustering_diagnostics; print('ok')"</automated>
  </verify>
  <done>All five builders (and helpers) live under advisor/aspects/ and import individually; no numeric logic changed.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire build_diagnostics() dispatcher to import aspect builders lazily; confirm green gate</name>
  <files>python/fdars/advisor/__init__.py</files>
  <read_first>python/fdars/advisor/__init__.py (build_diagnostics dispatcher at old advisor.py:188-255; the _supported set at old advisor.py:226)</read_first>
  <action>Update the build_diagnostics() dispatcher so each method branch imports its builder lazily from advisor.aspects, e.g. inside the alignment branch: `from fdars.advisor.aspects.alignment import _build_alignment_diagnostics; return _build_alignment_diagnostics(raw, argvals=argvals)`. Keep the _supported set exactly {"alignment", "fpca", "basis", "smoothing", "clustering"} — do NOT add new method strings (that is Phase 21). Keep the dispatcher, describe_cluster_differences, and _selfcheck_alignment_diagnostics in advisor/__init__.py. Run the full guardrail suite as the pure-refactor gate.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && pytest tests/test_advisor.py -x -q</automated>
  </verify>
  <done>build_diagnostics dispatches to advisor/aspects/ builders lazily; _supported set unchanged; all of tests/test_advisor.py passes unchanged (including the deterministic and real-dataset clustering tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) module boundaries | Pure structural move; no new external boundary introduced this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Tampering | grounding-invariant duplication | medium | mitigate | _GROUNDING_INVARIANT single constant; grep asserts exactly one "reason only from" |
| T-19-05 | Denial of Service | circular import at load | low | mitigate | _schema/_prompts import only pydantic/typing; aspects imported lazily |
</threat_model>

<verification>
- pytest tests/test_advisor.py -x  (pure-refactor gate)
- grep -rn "reason only from" python/fdars/advisor/ returns exactly one location
- python -c "from fdars.advisor._schema import Advice" and _prompts / aspects imports all succeed
- python -c "import fdars" still succeeds with anthropic absent
</verification>

<success_criteria>
Schema, prompt, and the five diagnostics builders live in their final submodule locations; build_diagnostics dispatches lazily to aspects/; the grounding invariant appears exactly once; tests/test_advisor.py passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/19-provider-foundation-grounding-contract/19-02-SUMMARY.md` when done
</output>

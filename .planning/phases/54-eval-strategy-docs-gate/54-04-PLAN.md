---
phase: 54-eval-strategy-docs-gate
plan: "04"
type: execute
wave: 3
depends_on: ["54-01", "54-02", "54-03"]
files_modified: []
autonomous: false
requirements: [DOCS-03]
estimate:
  tokens: 30000
  raw_tokens: 22000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The whole-site mkdocs build --strict is green offline (no network), with every new page's fence emitting FDARS_FENCE_OK."
    - "The three new SVGs pass the SVGO idempotence + build-determinism gate."
    - "A blocking human diagram method-accuracy review passes: the new SVGs are rendered to PNG and the user approves method-accuracy before the phase/milestone is closed."
  artifacts: []
  key_links:
    - "PYTHONPATH=scripts .venv/bin/mkdocs build --strict exits 0 offline"
    - "svgo(svgo(svg)) == svgo(svg) for all three new SVGs"
    - "rsvg-convert renders the three new SVGs to PNG; human checkpoint approves accuracy (autonomous:false)"
---

<objective>
The final milestone-closing gate: prove the whole-site `mkdocs build --strict` is green offline (all new fences emit `FDARS_FENCE_OK`), the three new SVGs pass the SVGO idempotence gate, and then PAUSE for a BLOCKING human diagram method-accuracy review — render the three new SVGs to PNG (via rsvg-convert) and require the user's explicit approval before the phase/milestone is closed. This runs sequentially on main (fences hardcode the main-tree `.venv/bin/mkdocs` path); it is the LAST wave and depends on Plans 01–03.

Purpose: DOCS-03 — a green strict build and a human accuracy review are the milestone's proven quality bar. The v6.0 lesson is explicit: a misdepicted method slipped past executors + the verifier and was caught only by human review, so self-review is NOT sufficient here.
Output: a green whole-site strict build, SVGO-passing new diagrams, and a recorded human approval of diagram method-accuracy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Build recipe + gate conventions
@docs/assets/diagrams/STYLE_SPEC.md
@scripts/docs_fig.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SVGO idempotence gate on the three new diagrams</name>
  <files>docs/assets/diagrams/advisor-comparative-selection.svg, docs/assets/diagrams/advisor-pipeline-report.svg, docs/assets/diagrams/advisor-auto-tuning.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — the mandatory SVGO invocation (`npx svgo@3.3.4 --config svgo.config.mjs --quiet`), the idempotence-not-diff gate approach, and the never-rewrite rule (check-only, `--output -`).
  </read_first>
  <action>Run the SVGO idempotence gate against each of the three new SVGs from Plan 02: for each file, produce pass1 with `npx svgo@3.3.4 --config svgo.config.mjs --quiet --input <file> --output -`, feed pass1 back via `--input -` to produce pass2, and assert pass1 == pass2 (empty diff). Do NOT write the optimised output back to the file (check-only). If any diagram fails idempotence, report the specific file and the offending construct so Plan 02 can be revised; do not attempt to auto-rewrite committed hand-authored SVGs.</action>
  <verify>
    <automated>for f in advisor-comparative-selection advisor-pipeline-report advisor-auto-tuning; do P1=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/$f.svg --output -) && P2=$(printf '%s' "$P1" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && [ "$P1" = "$P2" ] || { echo "SVGO idempotence FAILED: $f"; exit 1; }; done; echo "all three SVGs idempotent"</automated>
  </verify>
  <acceptance_criteria>
    - All three new SVGs pass the SVGO idempotence check (pass1 == pass2) under `svgo.config.mjs`.
    - No committed SVG is rewritten (check-only; `--output -`).
  </acceptance_criteria>
  <done>The three new diagrams pass the SVGO idempotence + build-determinism gate.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-site mkdocs build --strict green offline</name>
  <files>docs/advisor/comparative-selection.md, docs/advisor/pipeline-report.md, docs/advisor/auto-tuning.md, docs/advisor/aspects.md, mkdocs.yml</files>
  <read_first>
    - scripts/docs_fig.py — the `fast()` / DOCS_FAST mechanism; the full build (DOCS_FAST unset) is the source of truth and the only mode whose output is publishable.
    - docs/assets/diagrams/STYLE_SPEC.md — build recipe context (venv + PYTHONPATH=scripts).
  </read_first>
  <precondition>The main-tree `.venv` has fdars + mkdocs installed and `rsvg-convert` is available; docs run sequentially on main (not in a worktree).</precondition>
  <action>Run the whole-site strict build against the real main-tree venv: `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` with no network. This live-executes every fence (~19–25 min); confirm it exits 0 and that each new advisor page's fence emitted `FDARS_FENCE_OK` in the built output (grep the generated site HTML for the sentinel on the three new pages). The build is the source of truth — run WITHOUT DOCS_FAST for the final gate (a DOCS_FAST pre-pass is acceptable to catch errors fast, but the recorded pass must be the full strict build). If the strict build fails on a new page (broken fence, missing nav target, dead link, network access), report the exact failing page/fence for Plan 01/03 revision.</action>
  <verify>
    <automated>PYTHONPATH=scripts .venv/bin/mkdocs build --strict 2>&1 | tail -5 && grep -rq 'FDARS_FENCE_OK' site/advisor/comparative-selection/ site/advisor/pipeline-report/ site/advisor/auto-tuning/ && echo "strict build green + sentinels present"</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` exits 0 offline (no network access).
    - The generated site HTML for all three new advisor pages contains `FDARS_FENCE_OK` (each fence executed).
    - No `--strict` warnings/errors from the new pages or nav entries.
  </acceptance_criteria>
  <done>The whole-site strict build is green offline and every new page's fence executed (FDARS_FENCE_OK present).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING human diagram method-accuracy review</name>
  <what-built>The three new v8.0 advisor diagrams — advisor-comparative-selection.svg, advisor-pipeline-report.svg, advisor-auto-tuning.svg — authored against the shipped 50–53 behavior, embedded in their pages, and passing the strict build + SVGO gate. Before the checkpoint, render each SVG to PNG with `rsvg-convert -w 1440 -h 600 docs/assets/diagrams/<file>.svg -o <file>.png` (one per diagram) so the user can inspect them. This is a REQUIRED blocking review (autonomous:false; ignores auto_advance) — self-review is not sufficient (v6.0 hypograph/epigraph lesson: a misdepicted method slipped past executors + verifier).</what-built>
  <how-to-verify>
    View the three rendered PNGs and confirm each diagram faithfully depicts the shipped behavior:
    1. advisor-comparative-selection.png — the winner is chosen by the fdars-computed deterministic sort; the LLM narrates only and does NOT choose the winner; candidates are labeled per-candidate blocks (never flat-merged). Cross-check against 51-02-SUMMARY.md.
    2. advisor-pipeline-report.png — the four stages (represent → smooth → cluster/regress → monitor) are per-stage (never flat-merged); caveats are Python-computed cross-stage (e.g. high imputed fraction → FPCA caveat); the LLM narrates only and cannot invent caveats. Cross-check against 52-02-SUMMARY.md.
    3. advisor-auto-tuning.png — the bounded loop propose (schema-validated numeric parameter_delta, the ONLY LLM numeric contribution) → apply/clamp → re-run fdars → compare target vs budget → iterate; bounded termination (budget/converged/oscillation/guard_stop/parse_failure) and a Goodhart guard after the re-run. Cross-check against 53-01/53-02-SUMMARY.md.
    If any diagram is method-inaccurate, describe the specific inaccuracy — it routes back to Plan 02 for the SVG fix (no silent close).
  </how-to-verify>
  <resume-signal>Type "approved" once all three diagrams are confirmed method-accurate, or describe the specific inaccuracy to route back to Plan 02.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → network | The whole-site strict build must run offline; any fence reaching the network fails the gate. |
| diagram → reader (method-accuracy) | A misdepicted method is the primary residual risk after executors + verifier; only human review catches it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54D-01 | Tampering | diagram method-accuracy | high | mitigate | Blocking human review (autonomous:false, ignores auto_advance) renders PNGs and requires explicit approval — the v6.0 hypograph/epigraph lesson. |
| T-54D-02 | Denial of Service | whole-site strict build | high | mitigate | Full `--strict` build run offline; FDARS_FENCE_OK sentinels grepped in built HTML; small fence data keeps the ~19–25 min build tractable. |
| T-54D-SC | Tampering | npx svgo / rsvg-convert | low | accept | Both are pinned/existing gate tools already used across v6.0–v7.0 docs phases; no new project package. |
</threat_model>

<verification>
- All three new SVGs pass the SVGO idempotence gate.
- `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` exits 0 offline; FDARS_FENCE_OK present in the built HTML for all three new pages.
- The user has explicitly approved diagram method-accuracy after viewing rendered PNGs (blocking checkpoint).
</verification>

<success_criteria>
Whole-site strict build green offline, three new SVGs SVGO-idempotent, and a passing blocking human diagram method-accuracy review — DOCS-03 satisfied and the milestone ready to close.
</success_criteria>

<output>
Create `.planning/phases/54-eval-strategy-docs-gate/54-04-SUMMARY.md` when done.
</output>

---
phase: 54-eval-strategy-docs-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_advisor_eval.py
autonomous: true
requirements: [EVAL-01, EVAL-02]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A deterministic comparative eval asserts the fdars-computed compare_methods winner equals the known-best method on a constructed dataset — no LLM chooses the winner."
    - "A deterministic auto-tune eval asserts the target metric moves in the improving direction and the loop terminates with a bounded stop_reason — fully offline via injected seams + FakeProvider, no API key."
    - "Both eval families assert diagnostic-improvement AND grounding-pass; there is no LLM-as-judge anywhere in the CI path."
    - "Any live-LLM eval is env-gated and skips cleanly without ANTHROPIC_API_KEY; the suite runs network-free."
  artifacts:
    - tests/test_advisor_eval.py
  key_links:
    - "compare_methods(run_llm=False).winner == known-best method label (fdars-computed sort authority)"
    - "auto_tune(..., _run_method, _build_diagnostics, provider=FakeProvider).trace target_after moves toward target and stop_reason in the bounded set"
    - "live eval guarded by pytest.mark.skipif(not os.environ.get('ANTHROPIC_API_KEY'))"
---

<objective>
Deliver the deterministic, offline eval strategy that measures "good advice" for the two agentic capabilities — comparative method-selection and closed-loop auto-tuning — in a new `tests/test_advisor_eval.py`. The correct answer is KNOWN from constructed synthetic fixtures: a dataset with a known-best method (comparative) and a known improving convergence direction (auto-tune). Assert diagnostic-improvement + grounding-pass. Offline-only; live-LLM eval env-gated; NO LLM-as-judge in CI; no network.

Purpose: Close the milestone on a proven quality bar for the "good advice" capabilities (EVAL-01, EVAL-02). Aspects + pipeline stay covered by their existing grounding/offline tests (scope decision in 54-CONTEXT.md).
Output: `tests/test_advisor_eval.py` — deterministic comparative + auto-tune eval fixtures, plus one env-gated live smoke test that skips without a key.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Method-accuracy ground truth for the two "good advice" capabilities
@python/fdars/advisor/_compare_methods.py
@python/fdars/advisor/__init__.py
@python/fdars/advisor/_tuning.py

# Analog offline test conventions to mirror (FakeProvider, seams, run_llm=False, env-gated live)
@tests/test_compare_methods.py
@tests/test_advisor_tuning_llm.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic comparative-selection eval fixture (known-best winner)</name>
  <files>tests/test_advisor_eval.py</files>
  <read_first>
    - python/fdars/advisor/_compare_methods.py — `compare_methods()` signature (candidates dict/list, method, metric, run_llm), `_METRIC_REGISTRY`, `_DEFAULT_METRIC_BY_FAMILY`, and the deterministic fdars sort that fixes `result["winner"]` BEFORE any LLM call (COMPARE-01).
    - tests/test_compare_methods.py — the offline `compare_methods(candidates, run_llm=False)` invocation pattern and how labeled candidates are constructed.
    - .planning/phases/51-comparative-method-selection/51-02-SUMMARY.md — winner authority: `result["winner"]` is ALWAYS the fdars-sort winner; the LLM narrates only.
  </read_first>
  <behavior>
    - Construct a deterministic candidate set (pre-built diagnostics dicts, one family, shared metric) where ONE candidate is unambiguously best on the ranking metric by construction.
    - Test 1: `compare_methods(candidates, run_llm=False)["winner"]` equals the constructed known-best label — asserts the fdars-computed sort selects the right method.
    - Test 2: the ranking is fully deterministic — two calls with identical inputs return identical `winner` and ordering.
    - Test 3 (grounding-pass): run the comparison narration path with an injected offline FakeProvider whose evidence cites only real per-candidate diagnostic values; assert it completes without a GroundingViolationError and `result["winner"]` is unchanged by narration (LLM cannot override the winner).
    - Edge case: incommensurable candidates (mixed families / missing metric) raise ValueError before any ranking.
  </behavior>
  <action>Create `tests/test_advisor_eval.py` with a `TestComparativeEval` class. Build a synthetic candidate set of pre-built diagnostics dicts for a single task family with a shared ranking metric, engineered so exactly one candidate is best (the known-best label). Assert `compare_methods(candidates, run_llm=False)["winner"]` equals that label, and that repeated calls are byte-identical in winner + order. Add a grounding-pass test using an offline FakeProvider (mirror the FakeProvider shape from tests/test_advisor_tuning_llm.py: `name`, `model`, `supports_native_structured_output` attributes + a callable returning an Advice with evidence citing ONLY real candidate diagnostic values) and assert no GroundingViolationError and winner unchanged post-narration. Add an incommensurable-inputs test asserting ValueError. Keep all fixture arrays synthetic and n small (n <= 20). Reference the winner-authority invariant per COMPARE-01. NO network, NO API key.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor_eval.py -k "Comparative" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_advisor_eval.py -k "Comparative" -x -q` exits 0.
    - The known-best assertion uses `compare_methods(..., run_llm=False)["winner"]` (fdars-computed, not LLM-chosen).
    - The grounding-pass test uses an injected offline FakeProvider (no ANTHROPIC_API_KEY read).
    - Determinism test passes (two identical calls → identical winner + order).
  </acceptance_criteria>
  <done>The comparative eval asserts the fdars-computed winner equals the known-best label on a constructed dataset, is deterministic, and passes grounding offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic auto-tune eval fixture (improving direction + bounded termination)</name>
  <files>tests/test_advisor_eval.py</files>
  <read_first>
    - python/fdars/advisor/__init__.py — `auto_tune(dataset_id, method, *, target_metric, max_steps, provider, guard, _run_method, _build_diagnostics, **initial_params) -> TuneResult`; the `_run_method`/`_build_diagnostics` test seams and the grounding-invariant numeric boundary.
    - python/fdars/advisor/_tuning.py — `run_tuning_loop`, `_PARAM_REGISTRY` (tuneable methods smoothing/basis/fpca/clustering; target metrics + directions), termination precedence (budget > parse_failure > oscillation > guard_stop > convergence), `_is_improvement` (direction from `_METRIC_REGISTRY`).
    - tests/test_advisor_tuning_llm.py — the exact FakeProvider + `_fake_run_method` + `_make_fake_build_diagnostics` offline seam pattern (no API key, no network).
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-02-SUMMARY.md — TuneResult.trace fields (`param_before`, `param_after`, `target_before`, `target_after`, `accepted`, `stop_reason`, `guard_violations`) and `TuneResult.improved` semantics.
  </read_first>
  <behavior>
    - Construct a deterministic fake `_build_diagnostics` whose target metric monotonically improves as the tuned param moves in a known direction (the known improving direction from the data), driven by an offline FakeProvider proposing that direction.
    - Test 1 (improving direction): `auto_tune(...).trace` shows `target_after` moving toward the metric's optimising direction across accepted steps; final `improved` is True.
    - Test 2 (bounded termination): the loop terminates with a `stop_reason` in the bounded set (budget / converged / oscillation / guard_stop / parse_failure) and never exceeds `max_steps`.
    - Test 3 (offline, no key): the whole eval runs with `provider=FakeProvider(...)` + injected `_run_method`/`_build_diagnostics` and no ANTHROPIC_API_KEY.
    - Test 4 (grounding-pass): FakeProvider evidence cites only qualitative direction text (no fabricated numeric tokens), so the existing `_check_grounding` on each `advise()` call never fires.
  </behavior>
  <action>Add a `TestAutoTuneEval` class to `tests/test_advisor_eval.py`. Build a synthetic `_make_fake_build_diagnostics` (mirror tests/test_advisor_tuning_llm.py) whose target metric improves monotonically as the tuned param moves in the known-best direction, and a FakeProvider whose parameter_delta drives the param that way with qualitative-only evidence. Assert (a) the TuneResult.trace target_after moves toward the metric's optimising direction and `improved` is True; (b) `stop_reason` is in the bounded set and `len(trace steps) <= max_steps`; (c) it all runs offline with no ANTHROPIC_API_KEY. Use `max_steps` small (e.g. 5) and n <= 20. Reference the grounding-invariant numeric boundary per TUNE-03. NO network, NO API key.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor_eval.py -k "AutoTune" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_advisor_eval.py -k "AutoTune" -x -q` exits 0.
    - The eval uses `auto_tune(..., provider=FakeProvider, _run_method=..., _build_diagnostics=...)` — fully offline, no ANTHROPIC_API_KEY read.
    - Asserts target metric moves in the improving direction (`improved is True`) AND `stop_reason` is bounded with step count `<= max_steps`.
  </acceptance_criteria>
  <done>The auto-tune eval asserts the target metric moves in the known improving direction and the loop terminates boundedly, fully offline.</done>
</task>

<task type="auto">
  <name>Task 3: Env-gated live eval smoke test + CI-policy guard (no LLM-judge, network-free)</name>
  <files>tests/test_advisor_eval.py</files>
  <read_first>
    - tests/test_advisor_tuning_llm.py + tests/test_compare_methods_advise.py — the existing `@pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"), ...)` env-gating convention for live tests (test_live_* naming preserved elsewhere; here use a distinct name so QUAL-02's exact-3 test_live_* contract is unaffected).
    - .planning/phases/54-eval-strategy-docs-gate/54-CONTEXT.md — CI policy: offline deterministic checks only; live-LLM eval env-gated (skips without a key); CI stays network-free; NO LLM-as-judge in CI.
  </read_first>
  <action>Add a single env-gated live eval to `tests/test_advisor_eval.py`, guarded by `@pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"), reason="live eval needs ANTHROPIC_API_KEY")`, that runs one real comparative-selection narration and asserts only that it completes and the fdars-computed winner is preserved (NOT an LLM-as-judge quality score). Name it distinctly (e.g. `test_eval_live_comparison_smoke`) so it does NOT match the `test_live_*` prefix that the QUAL-02 contract counts. Add a module docstring stating the CI policy: offline-only in CI, no LLM-as-judge, live is env-gated, no network. Confirm collection-only that the live test is skipped when the key is absent.</action>
  <verify>
    <automated>env -u ANTHROPIC_API_KEY .venv/bin/python -m pytest tests/test_advisor_eval.py -q -rs 2>&1 | grep -qE "skipped|passed" && env -u ANTHROPIC_API_KEY .venv/bin/python -m pytest tests/test_advisor_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `env -u ANTHROPIC_API_KEY .venv/bin/python -m pytest tests/test_advisor_eval.py -q` exits 0 with the live test reported skipped (the deterministic tests all pass; no network access).
    - The live test asserts winner-preservation only — there is no LLM-as-judge quality scoring anywhere in the file.
    - The live test name does NOT start with `test_live_` (QUAL-02 exact-3 contract preserved).
  </acceptance_criteria>
  <done>The full `tests/test_advisor_eval.py` passes offline with the single live test skipped without a key; no LLM-as-judge, no network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → LLM provider | Env-gated live test may reach the network only when ANTHROPIC_API_KEY is set; CI has no key so the boundary is never crossed in CI. |
| eval fixture → grounding scanner | Fabricated numeric tokens in fake evidence could bypass the grounding check if not authored carefully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54A-01 | Information Disclosure | env-gated live test | low | mitigate | `skipif(not ANTHROPIC_API_KEY)` guard; CI runs network-free; live test asserts winner-preservation only (no LLM-as-judge). |
| T-54A-02 | Tampering | grounding-pass fixture | medium | mitigate | FakeProvider evidence cites only real per-candidate/current-step diagnostic values (comparative) or qualitative-only text (auto-tune) so `_check_grounding` behaves as in production. |
| T-54A-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan (test-only, existing deps); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor_eval.py -q` exits 0 offline (no ANTHROPIC_API_KEY), live test skipped.
- Comparative eval: fdars-computed winner == known-best; deterministic; grounding-pass offline.
- Auto-tune eval: target moves in improving direction, `improved is True`, bounded `stop_reason`, `<= max_steps`, offline.
- No LLM-as-judge anywhere; no network in CI; live eval env-gated.
</verification>

<success_criteria>
`tests/test_advisor_eval.py` exists and passes offline; the two deterministic eval families assert diagnostic-improvement + grounding-pass with a known-from-data correct answer; the single live test skips without a key; EVAL-01 and EVAL-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/54-eval-strategy-docs-gate/54-01-SUMMARY.md` when done.
</output>

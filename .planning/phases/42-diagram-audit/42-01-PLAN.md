---
phase: 42-diagram-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-diagram-audit/42-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "42-AUDIT.md inventories every one of the 61 top-level concept SVGs in docs/assets/diagrams/ (cards/ and thumb/ excluded), one row per file, and states the reconciled true count of 61 (not the framing's 68) with the discrepancy explained (AUDIT-01)."
    - "Each inventoried diagram is scored on all four axes — visual/layout quality, STYLE_SPEC conformance, XML source formatting, method-accuracy — on the OK / Minor / Major severity scale, with a one-line note on every non-OK cell (AUDIT-01)."
    - "The visual/layout axis verdict for each diagram is backed by an actual rsvg-convert render (PNG generated and eyeballed), not judged from source alone (per 42-CONTEXT.md Assessment Method)."
    - "A ranked, per-section fix list partitions all 61 diagrams across the three downstream fix-phase buckets — 43 (learn/represent/align), 44 (analyze/monitoring/advisor), 45 (regression/inference) — with none dropped or duplicated, ordered Major-first then by section (AUDIT-01)."
    - "The report confirms which docs/examples/*.md pages and which of the 5 advisor surface pages (python-api, mcp, providers, agent-skill, aspects) lack a concept SVG — the DIACOV coverage gap for Phases 46/47 (AUDIT-01)."
    - "The report confirms the thin-page extension list judged by section-structure completeness (~200 lines as a soft signal), covering/expanding the 8-page seed list, for DEPTH in Phase 48 (AUDIT-01)."
  artifacts:
    - .planning/phases/42-diagram-audit/42-AUDIT.md
  key_links:
    - "42-AUDIT.md ranked fix list -> consumed by Phases 43/44/45 plans as their evidence-backed worklist"
    - "42-AUDIT.md coverage-gap list -> consumed by Phases 46 (examples) / 47 (advisor) plans"
    - "42-AUDIT.md thin-page list -> consumed by Phase 48 (page depth) plan"
---

<objective>
Produce `.planning/phases/42-diagram-audit/42-AUDIT.md` — the single evidence document that gates the entire v7.0 milestone. It inventories every concept diagram in `docs/assets/diagrams/` (61 top-level SVGs; `cards/`/`thumb/` excluded), scores each on the four fix axes (visual/layout, STYLE_SPEC conformance, XML formatting, method-accuracy) with an OK/Minor/Major verdict + note per non-OK cell, then derives a ranked per-section fix list chunked to Phases 43/44/45, a diagram-coverage gap list for Phases 46/47, and a thin-page extension list for Phase 48.

Purpose: downstream fix/coverage/depth phases execute against evidence rather than guesswork; this is the AUDIT-01 deliverable and depends on nothing.
Output: `.planning/phases/42-diagram-audit/42-AUDIT.md`.

READ-ONLY ANALYSIS PHASE: no diagram is edited and no docs page is rewritten. PNGs rendered for visual inspection are scratch artifacts (write them under the scratchpad or a gitignored temp dir; do NOT commit them and do NOT modify any committed SVG). SVGO is invoked check-only (idempotence), never with rewrite output onto a source file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-diagram-audit/42-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@.planning/milestones/v1.0-phases/02-audit/02-AUDIT.md
@.planning/codebase/CONVENTIONS.md

Section->diagram map already established during planning (each SVG's owning docs section, from `grep -rl <svg> docs --include='*.md'`, preferring the non-index method page). The three fix-phase buckets partition cleanly:
- 43 learn/represent/align = 24 diagrams (learn 6, represent 10, align 8)
- 44 analyze/monitoring/advisor = 17 diagrams (analyze 12, monitoring 3, advisor 2)
- 45 regression/inference = 19 diagrams (regression 15, inference 4)
- edge case: `ex-sonar-tsrvf.svg` is referenced only from `examples/sonar-tsrvf.md` (the 61st file) — a tsrvf/align-family concept diagram; the audit must explicitly assign it to a fix bucket (recommend 43 alongside `tsrvf.svg`) so all 61 land in exactly one bucket.

Tooling confirmed during planning: `rsvg-convert` 2.62.3 at /usr/bin/rsvg-convert; `.venv/bin/mkdocs` present; `svgo.config.mjs` present. STYLE_SPEC markers are grep-reproducible: viewBox width 720 with height 300/480/520; five CSS classes `.ttl .sub .lab .sm .mono`; `system-ui` font stack; `role="img"` + `aria-label` on root `<svg>`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end audit slice — inventory + render + 4-axis score for one diagram, wired into 42-AUDIT.md skeleton</name>
  <files>.planning/phases/42-diagram-audit/42-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/02-audit/02-AUDIT.md (report-format precedent: per-diagram x per-axis table with evidence-bearing cell notes, then ranked lists)
    - docs/assets/diagrams/STYLE_SPEC.md (conformance rubric: viewBox 720x{300|480|520}, five CSS classes, system-ui, role=img + aria-label, SVGO idempotence gate)
    - 42-CONTEXT.md (OK/Minor/Major scale with one-line note per non-OK cell; visual axis judged by rsvg render; method-accuracy FLAGS suspected issues, does not re-derive)
    - docs/assets/diagrams/smoothing.svg (the exemplar to score end-to-end — the v1.0 audit found a real defect: Panel 3 ghost polyline reuses Panel 1 coordinates verbatim, so it exercises every axis)
  </read_first>
  <action>Prove the full audit pipeline on ONE diagram before scaling out. (1) Create the 42-AUDIT.md skeleton: a Purpose section, a scoring-legend section defining OK/Minor/Major and the four axes, one section-grouped scoring table with columns (Diagram | Section | Fix bucket | Visual/layout | STYLE_SPEC | XML formatting | Method-accuracy | Notes), plus placeholder headers for the ranked fix list, coverage-gap list, thin-page list, and count reconciliation. (2) For smoothing.svg: render it to PNG with rsvg-convert into the scratchpad (NOT committed) and inspect for the visual verdict; read the source and grep the STYLE_SPEC markers for the conformance verdict; assess XML-formatting cleanliness; state a method-accuracy verdict FLAGGING the known ghost-polyline issue for the fix phase. (3) Fill smoothing.svg's row with all four OK/Minor/Major cells, each non-OK cell carrying a one-line evidence note citing file:line where relevant. Assign it to the 43 (learn) bucket. Do NOT modify smoothing.svg and do NOT commit any PNG.</action>
  <verify>
    <automated>test -f .planning/phases/42-diagram-audit/42-AUDIT.md; grep -q 'smoothing.svg' .planning/phases/42-diagram-audit/42-AUDIT.md; grep -Eq 'Major|Minor' .planning/phases/42-diagram-audit/42-AUDIT.md</automated>
  </verify>
  <done>42-AUDIT.md exists with the scoring legend and section-table structure; smoothing.svg has a complete four-axis scored row with evidence notes and a fix-bucket assignment; the render+read+grep pipeline is proven; no committed SVG or PNG was changed/added.</done>
</task>

<task type="auto">
  <name>Task 2: Score all 61 diagrams on four axes; reconcile the count</name>
  <files>.planning/phases/42-diagram-audit/42-AUDIT.md</files>
  <read_first>
    - .planning/phases/42-diagram-audit/42-AUDIT.md (the skeleton + smoothing.svg exemplar row from Task 1 — the shape every other row copies)
    - docs/assets/diagrams/STYLE_SPEC.md (conformance rubric, re-read only if needed)
    - the referencing docs page(s) for each diagram (docs/<section>/<page>.md) — read the page prose to judge the method-accuracy axis against what the shipped fdars method does; FLAG suspected mismatches, do not re-derive
  </read_first>
  <action>Scale the proven pipeline across the remaining 60 top-level SVGs (glob docs/assets/diagrams/*.svg, maxdepth 1; cards/ and thumb/ out of scope). For EACH diagram produce all four axis verdicts: (a) Visual/layout — render to PNG with rsvg-convert into the scratchpad and inspect for overlapping labels, cramped spacing, misalignment, inconsistent sizing; (b) STYLE_SPEC conformance — grep the source for viewBox-720 width, the five CSS classes, system-ui, role=img, aria-label, recording each failing marker by name (may cross-check with the pinned SVGO idempotence invocation run twice and compared, check-only — NEVER write output back onto the source); (c) XML source formatting — assess hand-editability/cleanliness; (d) Method-accuracy — read the referencing page prose and FLAG any suspected mismatch for verification in the fix phase (do not fully re-derive). Score each cell OK/Minor/Major with a one-line note on every non-OK cell (cite file:line where the evidence lives). Group rows by docs section (learn, represent, align, analyze, monitoring, advisor, regression, inference, examples). Then reconcile the count: state the authoritative true count is 61 top-level concept SVGs (cards/ and thumb/ excluded), explain the framing's 68 figure as stale, and note the ex-sonar-tsrvf.svg examples-section edge case. Every one of the 61 files must have exactly one fully-scored row.</action>
  <verify>
    <automated>for f in $(find docs/assets/diagrams -maxdepth 1 -name '*.svg' -exec basename {} \;); do grep -q "$f" .planning/phases/42-diagram-audit/42-AUDIT.md || { echo "MISSING $f"; exit 1; }; done; echo ok</automated>
  </verify>
  <done>All 61 top-level SVGs have a fully-scored four-axis row grouped by section; every non-OK cell has a one-line evidence note; the count is reconciled to 61 with the 68 discrepancy explained; ex-sonar-tsrvf.svg is placed and bucket-assigned.</done>
</task>

<task type="auto">
  <name>Task 3: Ranked per-section fix list + coverage-gap list + thin-page list</name>
  <files>.planning/phases/42-diagram-audit/42-AUDIT.md</files>
  <read_first>
    - .planning/phases/42-diagram-audit/42-AUDIT.md (the completed 61-row scoring table from Task 2 — the source the three downstream lists are derived from)
    - 42-CONTEXT.md (ranked list ordered Major-first then by section; coverage gap = examples pages + 5 advisor pages lacking a concept SVG; thin-page list by section-structure completeness with the 8-page seed list, ~200 lines soft signal)
    - docs/examples/ (the ~21 worked-example pages — confirm which reference a concept SVG; sonar-tsrvf.md already has one, the rest do not) and docs/advisor/{python-api,mcp,providers,agent-skill,aspects}.md (all 5 currently lack a concept SVG)
  </read_first>
  <action>From the completed scoring table, derive the three downstream lists. (1) Ranked per-section fix list: partition ALL 61 diagrams into the three fix-phase buckets — 43 (learn/represent/align, 24 + ex-sonar-tsrvf.svg = 25), 44 (analyze/monitoring/advisor, 17), 45 (regression/inference, 19) — such that every diagram lands in exactly one bucket with none dropped or duplicated (25+17+19 = 61); within each bucket order by severity (Major rows first, then Minor, then OK) then by section, so each fix phase reads a prioritized worklist. Carry each diagram's worst-axis severity and its non-OK note(s) into the list. (2) Coverage-gap list: enumerate which docs/examples/*.md pages lack a concept SVG (all except sonar-tsrvf.md — ~20 pages, excluding index.md if it warrants none) for DIACOV-01/Phase 46, and confirm all 5 advisor surface pages (python-api, mcp, providers, agent-skill, aspects) lack one for DIACOV-02/Phase 47; state for each page whether a diagram is warranted. (3) Thin-page extension list: judge each seed page (regression/concurrent-regression, regression/functional-glm, represent/pace-fpca, inference/interval-inference, represent/interpolation, represent/imputation, analyze/scoring-metrics, analyze/functional-statistics) plus any other sub-mature method page by section-structure completeness (missing intro / method explanation / worked example / parameters / caveats), using ~200 lines only as a soft signal; confirm or expand the list for DEPTH/Phase 48. Write all three lists into 42-AUDIT.md under their headers.</action>
  <verify>
    <automated>grep -qi 'ranked' .planning/phases/42-diagram-audit/42-AUDIT.md; grep -qi 'coverage' .planning/phases/42-diagram-audit/42-AUDIT.md; grep -qi 'thin' .planning/phases/42-diagram-audit/42-AUDIT.md; grep -q 'python-api' .planning/phases/42-diagram-audit/42-AUDIT.md; grep -q 'concurrent-regression' .planning/phases/42-diagram-audit/42-AUDIT.md</automated>
  </verify>
  <done>42-AUDIT.md contains: a ranked fix list partitioning all 61 diagrams across the 43/44/45 buckets (25/17/19, none dropped or duplicated, Major-first); a confirmed coverage-gap list naming the examples pages and all 5 advisor pages lacking a concept SVG; and a thin-page extension list covering the 8-page seed set plus any others surfaced. The three lists are consumable as-is by the Phase 43-48 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx svgo@3.3.4 download | pinned package pulled from npm during the check-only conformance cross-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | 42-AUDIT.md scoring cells | low | accept | Read-only analysis; every non-OK cell carries a file:line evidence note, so claims are reproducible against source. No code path or runtime is affected. |
| T-42-SC | Tampering | npx svgo@3.3.4 install | low | accept | svgo is version-pinned (3.3.4) and already the established, in-repo SVGO gate dependency (svgo.config.mjs); it is invoked check-only (idempotence), never with rewrite output onto a source file. No new package is introduced by this phase. |
</threat_model>

<verification>
- 42-AUDIT.md exists and every one of the 61 top-level SVGs in docs/assets/diagrams/ has exactly one four-axis-scored row.
- Every non-OK cell has a one-line note; visual-axis verdicts are backed by rsvg-convert renders.
- The ranked fix list partitions all 61 diagrams across the 43/44/45 buckets (25/17/19) with none dropped or duplicated.
- Coverage-gap list names the examples pages and all 5 advisor pages lacking a concept SVG; thin-page list covers the 8-page seed set plus any others.
- The count is reconciled to 61 with the framing's 68 discrepancy explained.
- No committed docs/assets/diagrams/*.svg or docs/**/*.md was modified; no scratch PNG was committed.
</verification>

<success_criteria>
AUDIT-01 satisfied: a per-diagram x 4-axis scored inventory of all 61 concept diagrams, a ranked per-section fix list chunked to Phases 43/44/45, a confirmed diagram-coverage gap list (examples + 5 advisor pages), and a confirmed thin-page extension list — all in 42-AUDIT.md, ready to gate the downstream v7.0 phases.
</success_criteria>

<output>
Create `.planning/phases/42-diagram-audit/42-01-SUMMARY.md` when done.
</output>
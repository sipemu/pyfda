---
phase: 29-docs-diagrams-worked-examples
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - docs/analyze/functional-statistics.md
  - docs/analyze/scoring-metrics.md
  - docs/assets/diagrams/functional-statistics.svg
  - docs/assets/diagrams/scoring-metrics.svg
autonomous: true
requirements: [DOCS-01, DOCS-02]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The functional-statistics page shows a method-accurate SVG where the depth-based median is an OBSERVED curve (a data row), visually distinct from the cross-sectional mean and from geometric_median (DOCS-01)"
    - "The scoring-metrics page shows a method-accurate SVG of predicted-vs-true curves with the residual the metric integrates, noting MAPE rejects near-zero truths and MSLE rejects values ≤ −1 (DOCS-01)"
    - "Both pages run a real offline worked example against docs/data emitting FDARS_FENCE_OK against fdars.fdata.* / fdars.scoring.* (DOCS-02)"
  artifacts:
    - docs/analyze/functional-statistics.md
    - docs/analyze/scoring-metrics.md
    - docs/assets/diagrams/functional-statistics.svg
    - docs/assets/diagrams/scoring-metrics.svg
  key_links:
    - "functional-statistics fence → fdars.fdata.functional_variance/std/covariance/depth_based_median/trim_mean (data-only signatures)"
    - "scoring-metrics fence → fdars.scoring.functional_mae/mse/mape/msle/explained_variance (y_true, y_pred, argvals)"
    - "each page → its committed SVG via ![](../assets/diagrams/NAME.svg){ .fdars-diagram }"
---

<objective>
Author the analyze-section docs for the new functional statistics and scoring capabilities. docs/analyze/functional-statistics.md covers functional_variance/std/covariance, depth_based_median (+ trim_mean) and the fd.var/std/cov/median methods; docs/analyze/scoring-metrics.md covers the five fdars.scoring prediction-scoring metrics. Each page gets a new method-accurate hand-authored inline SVG and an executed offline FDARS_FENCE_OK worked example against the real shipped bindings.

Purpose: Documents the STAT-01/02/03 capabilities to the project's method-accurate standard. Reuses the toolchain proven by the Plan 01 tracer.
Output: 2 new analyze pages, 2 new SVGs. (Nav wiring + strict-build gate are centralized in Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Method definitions (authoritative) — Group 2 rows: functional_variance/std/covariance,
# depth_based_median, trim_mean, and the five scoring metrics with their domain restrictions:
@.planning/research/FEATURES.md

# The SVG style contract:
@docs/assets/diagrams/STYLE_SPEC.md

# A conforming SVG to match structurally:
@docs/assets/diagrams/landmark-registration.svg

# Prose + fence exemplar:
@docs/represent/basis-representation.md

# The executed-fence + FDARS_FENCE_OK pattern:
@docs/advisor/python-api.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: functional-statistics.md page + functional-statistics.svg + executed fence</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (canonical &lt;style&gt;, viewBox 0 0 720 300, palette, role/aria-label)
    - docs/assets/diagrams/landmark-registration.svg (structural exemplar)
    - .planning/research/FEATURES.md (functional_variance/std/covariance + depth_based_median + trim_mean rows — note the KEY DISTINCTION that depth_based_median returns an OBSERVED sample curve, unlike geometric_median which returns a new curve)
    - docs/advisor/python-api.md lines 17-30 (fence + FDARS_FENCE_OK pattern)
  </read_first>
  <files>docs/analyze/functional-statistics.md, docs/assets/diagrams/functional-statistics.svg</files>
  <action>
    (a) Write docs/assets/diagrams/functional-statistics.svg (hand-authored inline SVG, STYLE_SPEC-conforming: viewBox 0 0 720 300 — or 0 0 720 480 if a two-row layout reads better, both are allowed by the spec; canonical &lt;style&gt;; role/aria-label matching the title). Depict: (1) overlaid curves with the cross-sectional mean as a bold line and a pointwise ±1 std shaded band that is visibly WIDEST where curves diverge most; (2) the depth-based median. METHOD-ACCURACY (critical trap): the depth-based median MUST be drawn as one of the ACTUAL observed curves (a data row) — highlighted/bold — NOT an averaged or synthetic line; make it visually distinct from the mean (dashed red) and, if shown, from a geometric_median (a separate synthetic dotted line) so the reader sees depth-median ≠ mean ≠ geometric-median. Optionally include a small covariance-surface heatmap thumbnail (m×m, diagonal = variance).

    (b) Write docs/analyze/functional-statistics.md: intro prose on pointwise Bessel-corrected functional_variance/functional_std (functions of t, not scalars; std² == var exactly; require n≥2), the m×m functional_covariance surface (diagonal equals variance; column-major layout guarded by the round-trip in STAT-01), depth_based_median (returns the deepest OBSERVED curve — contrast explicitly with geometric_median), and trim_mean (depth-trimmed robust mean; alpha=0 equals the plain mean). Embed the SVG. Add ONE executed fence ```python exec="1" html="1" source="above"``` that loads canadian_weather (via `from docs_data import load_canadian_weather`), calls the REAL data-only bindings `fdars.fdata.functional_variance(X)`, `fdars.fdata.functional_std(X)`, `fdars.fdata.functional_covariance(X)`, `fdars.fdata.depth_based_median(X)` (returns the median curve as a 1-D array), and `fdars.fdata.trim_mean(X, alpha=0.1)`; assert std**2 ≈ var and that the covariance diagonal ≈ variance (layout guard); print a line containing FDARS_FENCE_OK. Deterministic + offline. Note in prose the fd.var()/std()/cov()/median() convenience methods.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build 2>&amp;1 | tail -3; grep -rq "FDARS_FENCE_OK" site/analyze/functional-statistics/index.html &amp;&amp; echo STATS_FENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The built page site/analyze/functional-statistics/index.html contains FDARS_FENCE_OK (executed offline fence ran).
    - SVGO idempotence holds on docs/assets/diagrams/functional-statistics.svg: `first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/functional-statistics.svg --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$first") <(printf '%s' "$second")` is empty.
    - `grep -c 'role="img"' docs/assets/diagrams/functional-statistics.svg` returns 1; `grep -Ec 'viewBox="0 0 720 (300|480)"' docs/assets/diagrams/functional-statistics.svg` returns 1.
    - The page references the SVG: `grep -c 'functional-statistics.svg' docs/analyze/functional-statistics.md` returns 1.
  </acceptance_criteria>
  <done>functional-statistics.md renders with an executed FDARS_FENCE_OK fence against the real fdars.fdata bindings, and its SVG passes SVGO idempotence, conforms to STYLE_SPEC, and depicts the depth-median as an observed curve distinct from the mean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: scoring-metrics.md page + scoring-metrics.svg + executed fence</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (conformance contract)
    - docs/analyze/functional-statistics.md (the sibling analyze page from Task 1 — match structure)
    - .planning/research/FEATURES.md (the five functional_* scoring metrics row — exact formulas and the domain restrictions: MAPE raises if any |y_true| < eps; MSLE raises if any value ≤ −1)
    - docs/advisor/python-api.md lines 17-30 (fence + FDARS_FENCE_OK pattern)
  </read_first>
  <files>docs/analyze/scoring-metrics.md, docs/assets/diagrams/scoring-metrics.svg</files>
  <action>
    (a) Write docs/assets/diagrams/scoring-metrics.svg (hand-authored inline SVG, STYLE_SPEC-conforming). Depict a pair of curves — y_true (bold) and y_pred (dashed) — with the absolute error |y_true − y_pred| SHADED as the region between them, and a second panel showing the scalar scores collapsing that integrated region to single numbers. METHOD-ACCURACY: annotate that these are DOMAIN-INTEGRATED scalars (Simpson's rule), not per-grid-point vectors — a genuine functional extension, not a column-wise average.

    (b) Write docs/analyze/scoring-metrics.md: intro prose on the five metrics (functional_mae, functional_mse, functional_mape, functional_msle, functional_explained_variance), all Simpson-integrated curve-averaged scalars with the uniform signature (y_true, y_pred, argvals). METHOD-ACCURACY prose (mandatory): functional_mape RAISES ValueError when any |y_true| is near zero (no epsilon fallback — recommend MSE/MSLE for zero-crossing data); functional_msle RAISES ValueError when any value ≤ −1; functional_explained_variance ranges (−∞, 1]. Embed the SVG. Add ONE executed fence ```python exec="1" html="1" source="above"``` that builds a deterministic predict-vs-true setup on a docs/data dataset (e.g. tecator or growth: smooth or FPCA-reconstruct X to make y_pred, keep X as y_true — fixed seed, offline), calls the REAL bindings `fdars.scoring.functional_mae(y_true, y_pred, argvals)`, `functional_mse(...)`, and `functional_explained_variance(...)`, prints the scalar scores, and ends with a line containing FDARS_FENCE_OK. Ensure y_true has no near-zero values so MAPE would be valid (or simply demonstrate MAE/MSE/explained_variance and describe MAPE/MSLE domains in prose without triggering the raise in the executed fence). Deterministic + offline.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build 2>&amp;1 | tail -3; grep -rq "FDARS_FENCE_OK" site/analyze/scoring-metrics/index.html &amp;&amp; echo SCORING_FENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - site/analyze/scoring-metrics/index.html contains FDARS_FENCE_OK.
    - SVGO idempotence holds on docs/assets/diagrams/scoring-metrics.svg (twice-run diff empty, same svgo command as Task 1 with this filename).
    - `grep -c 'role="img"' docs/assets/diagrams/scoring-metrics.svg` returns 1; `grep -Ec 'viewBox="0 0 720 (300|480)"' docs/assets/diagrams/scoring-metrics.svg` returns 1.
    - The page documents the domain restrictions: `grep -c 'MAPE' docs/analyze/scoring-metrics.md` returns ≥ 1 and `grep -c 'MSLE' docs/analyze/scoring-metrics.md` returns ≥ 1.
    - `grep -c 'scoring-metrics.svg' docs/analyze/scoring-metrics.md` returns 1.
  </acceptance_criteria>
  <done>scoring-metrics.md renders with an executed FDARS_FENCE_OK fence against the real fdars.scoring bindings, documents the MAPE/MSLE domain restrictions, and its SVG passes SVGO idempotence and STYLE_SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → executed fence | markdown-exec runs Python at build time; must stay offline/deterministic |
| hand-authored SVG → SVGO gate | committed markup is source of truth; gate is check-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04 | Information Disclosure | executed scoring/stats fences | medium | mitigate | network-free, base extras only, fixed seeds; no API key referenced |
| T-29-05 | Tampering | committed hand-authored SVGs | low | mitigate | SVGO gate stdout-only idempotence check, never rewrites committed files |
| T-29-06 | Denial of Service | non-deterministic fence output | medium | mitigate | fixed seeds + svg.hashsalt keep the strict build reproducible |
</threat_model>

<verification>
- Both analyze pages build (full `mkdocs build`) with executed fences containing FDARS_FENCE_OK.
- Both new SVGs pass SVGO idempotence and carry role="img" + a permitted 720-width viewBox.
- The depth-median trap and the MAPE/MSLE domain restrictions are visibly honored in the SVG and page prose respectively.
- Nav wiring + full strict build are handled in Plan 04.
</verification>

<success_criteria>
Two analyze pages (functional-statistics, scoring-metrics) render with method-accurate STYLE_SPEC-conforming SVGs (SVGO-idempotent) and executed offline FDARS_FENCE_OK worked examples against the real shipped fdars.fdata and fdars.scoring bindings.
</success_criteria>

<output>
Create `.planning/phases/29-docs-diagrams-worked-examples/29-02-SUMMARY.md` when done.
</output>

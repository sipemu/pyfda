---
phase: 29-docs-diagrams-worked-examples
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - docs/align/shift-registration.md
  - docs/align/banded-alignment.md
  - docs/assets/diagrams/shift-registration.svg
  - docs/assets/diagrams/banded-alignment.svg
  - docs/advisor/aspects.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]
user_setup: []

estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The shift-registration page shows a method-accurate SVG of a RIGID HORIZONTAL translation to the cross-sectional mean — visually distinct from an elastic warp (DOCS-01)"
    - "The banded-alignment page shows a method-accurate SVG of the Sakoe–Chiba diagonal corridor over the DP grid (DOCS-01)"
    - "Both align pages run a real offline worked example against docs/data emitting FDARS_FENCE_OK against fdars.alignment.* (DOCS-02)"
    - "docs/advisor/aspects.md is updated for scoring / imputation-quality / registration-quality diagnostics (DOCS-03), advise() LLM fences kept illustrative/non-executed"
  artifacts:
    - docs/align/shift-registration.md
    - docs/align/banded-alignment.md
    - docs/assets/diagrams/shift-registration.svg
    - docs/assets/diagrams/banded-alignment.svg
    - docs/advisor/aspects.md
  key_links:
    - "shift-registration fence → fdars.alignment.least_squares_shift_registration (returns dict {registered_data, shifts}) + the 3 quality scores"
    - "banded-alignment fence → fdars.alignment.karcher_mean_with_band(..., band_frac=...) + elastic_*_distance_matrix_with_band"
    - "aspects.md represent/alignment sections + new scoring coverage → the ADV-01/ADV-02 diagnostics shipped in Phase 28"
---

<objective>
Author the align-section docs for the new registration capabilities and update the advisor coverage page. docs/align/shift-registration.md covers least_squares_shift_registration (+ fd.shift_register()) and the three registration-quality scores; docs/align/banded-alignment.md covers karcher_mean_with_band + the banded distance matrices (Sakoe–Chiba corridor). docs/advisor/aspects.md is extended for the new scoring aspect + imputation-quality (represent) + registration-quality (alignment) diagnostics shipped in Phase 28. Each new page gets a method-accurate hand-authored inline SVG and an executed offline FDARS_FENCE_OK worked example.

Purpose: Documents ALGN-01/02/03 and the advisor extension (ADV-01/02) to the project's method-accurate standard, closing DOCS coverage for the alignment + advisor surfaces. Reuses the toolchain proven by the Plan 01 tracer.
Output: 2 new align pages, 2 new SVGs, an updated advisor aspects.md. (Nav wiring + strict-build gate are centralized in Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Method definitions (authoritative) — Group 3 rows: least_squares_shift_registration (contrast
# with karcher_mean), the three quality scores, karcher_mean_with_band + Sakoe–Chiba band:
@.planning/research/FEATURES.md

# The SVG style contract:
@docs/assets/diagrams/STYLE_SPEC.md

# A conforming SVG + the align page prose exemplar (shows the alignment tone + cross-linking):
@docs/assets/diagrams/landmark-registration.svg
@docs/align/landmark-registration.md

# The executed-fence + FDARS_FENCE_OK pattern; the advisor page to EXTEND (do not replace):
@docs/advisor/python-api.md
@docs/advisor/aspects.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: shift-registration.md page + shift-registration.svg + executed fence</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (canonical &lt;style&gt;, viewBox, palette, role/aria-label)
    - docs/assets/diagrams/landmark-registration.svg (structural exemplar) + docs/align/landmark-registration.md (align prose/fence exemplar)
    - .planning/research/FEATURES.md (least_squares_shift_registration row — the CONTRAST-with-karcher_mean note: shift is a constant translation δ, NOT an arbitrary monotone warp; and the three quality-score rows)
    - docs/advisor/python-api.md lines 17-30 (fence + FDARS_FENCE_OK pattern)
  </read_first>
  <files>docs/align/shift-registration.md, docs/assets/diagrams/shift-registration.svg</files>
  <action>
    (a) Write docs/assets/diagrams/shift-registration.svg (hand-authored inline SVG, STYLE_SPEC-conforming: viewBox 0 0 720 300, canonical &lt;style&gt;, role/aria-label). Depict: (left) misaligned curves with peaks at different t positions; (centre) the horizontal shift δ as a STRAIGHT HORIZONTAL arrow on a single curve with the L2-to-mean objective shaded; (right) registered curves with peaks aligned and the mean sharpened. METHOD-ACCURACY (critical trap — the whole point of this diagram): the shift MUST be depicted as a RIGID HORIZONTAL TRANSLATION (the curve slides left/right unchanged in shape) — it MUST NOT look like an elastic warp (no stretching/compression of the time axis, no curved warp arrow). To reinforce the distinction, include a small contrasting glyph: a STRAIGHT arrow labelled "shift (rigid)" vs a CURVED arrow labelled "elastic warp" marked as the different method.

    (b) Write docs/align/shift-registration.md: intro prose positioning shift registration as the entry-level, rigid alignment baseline BEFORE elastic-alignment.md — it finds one scalar shift δᵢ ∈ [−max_shift, max_shift] per curve minimising Simpson-weighted L2 to the cross-sectional mean (golden-section search), then re-evaluates on the original grid. Contrast explicitly with karcher_mean (arbitrary monotone warps, Fisher-Rao/SRVF). Cross-link to elastic-alignment.md. Embed the SVG. Add ONE executed fence ```python exec="1" html="1" source="above"``` that loads growth or canadian_weather, calls the REAL binding `res = fdars.alignment.least_squares_shift_registration(X, argvals, max_shift)` (returns a dict with keys "registered_data" and "shifts"; recommend max_shift = 0.25*(argvals[-1]-argvals[0])), then evaluates the three quality scores `fdars.alignment.least_squares_score(res["registered_data"], argvals)`, `pairwise_correlation_score(...)`, and `sobolev_least_squares_score(res["registered_data"], argvals, lambda_=...)` (Sobolev needs a uniform grid), prints the mean shift + scores, and ends with a line containing FDARS_FENCE_OK. Deterministic + offline. Note the fd.shift_register(max_shift) convenience method (returns (Fdata, shifts)).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build 2>&amp;1 | tail -3; grep -rq "FDARS_FENCE_OK" site/align/shift-registration/index.html &amp;&amp; echo SHIFT_FENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - site/align/shift-registration/index.html contains FDARS_FENCE_OK.
    - SVGO idempotence holds on docs/assets/diagrams/shift-registration.svg: `first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/shift-registration.svg --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$first") <(printf '%s' "$second")` is empty.
    - `grep -c 'role="img"' docs/assets/diagrams/shift-registration.svg` returns 1; `grep -c 'viewBox="0 0 720 300"' docs/assets/diagrams/shift-registration.svg` returns 1.
    - The page contrasts rigid vs elastic: `grep -ci 'rigid' docs/align/shift-registration.md` returns ≥ 1 and `grep -ci 'elastic' docs/align/shift-registration.md` returns ≥ 1.
    - `grep -c 'shift-registration.svg' docs/align/shift-registration.md` returns 1.
  </acceptance_criteria>
  <done>shift-registration.md renders with an executed FDARS_FENCE_OK fence against the real least_squares_shift_registration + quality-score bindings, and its SVG passes SVGO idempotence, conforms to STYLE_SPEC, and depicts a rigid horizontal translation visually distinct from an elastic warp.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: banded-alignment.md page + banded-alignment.svg + executed fence</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (conformance contract)
    - docs/align/shift-registration.md (sibling align page from Task 1) + docs/align/elastic-alignment.md (the page this cross-links to)
    - .planning/research/FEATURES.md (karcher_mean_with_band + Sakoe–Chiba band row — band_frac ∈ (0,1], None = unbanded/exact; band confines DP cells to |i−j| ≤ B; 4–6× speedup)
    - docs/advisor/python-api.md lines 17-30 (fence + FDARS_FENCE_OK pattern)
  </read_first>
  <files>docs/align/banded-alignment.md, docs/assets/diagrams/banded-alignment.svg</files>
  <action>
    (a) Write docs/assets/diagrams/banded-alignment.svg (hand-authored inline SVG, STYLE_SPEC-conforming). Depict the DP table as a grid with the Sakoe–Chiba band highlighted as a DIAGONAL STRIPE (cells inside the band colour-coded, cells outside shaded grey / "not evaluated"), and a small secondary panel of two curves being aligned where the warp path can only travel within the stripe. Label band_frac as the stripe half-width parameter. METHOD-ACCURACY: the band is a DIAGONAL corridor around the main diagonal of the (i,j) DP grid (|i−j| ≤ B); band_frac=None means the whole grid is evaluated (exact/unbanded).

    (b) Write docs/align/banded-alignment.md: intro prose on karcher_mean_with_band (same Fisher-Rao Karcher mean as karcher_mean, but the DP alignment step is restricted to a Sakoe–Chiba band of half-width band_frac × domain_length, giving 4–6× speedup; band_frac=None reproduces the exact unbanded result identically) and the banded distance-matrix variants elastic_self_distance_matrix_with_band / elastic_cross_distance_matrix_with_band. Note the speed/accuracy tradeoff (start at band_frac≈0.2, widen if quality degrades) and that automatic band selection is intentionally NOT provided. Embed the SVG. Add ONE executed fence ```python exec="1" html="1" source="above"``` that loads a docs/data dataset (canadian_weather or a phoneme subset), calls the REAL binding `fdars.alignment.karcher_mean_with_band(X, argvals, band_frac=0.2)` (returns a dict with keys including "mean", "gammas", "n_iter", "converged"), optionally also `elastic_self_distance_matrix_with_band(X, argvals, band_frac=0.2)`, prints e.g. n_iter/converged and a shape, and ends with a line containing FDARS_FENCE_OK. Deterministic + offline (keep the dataset small enough to build fast; may subsample with a fixed seed). Cross-link to elastic-alignment.md.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build 2>&amp;1 | tail -3; grep -rq "FDARS_FENCE_OK" site/align/banded-alignment/index.html &amp;&amp; echo BAND_FENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - site/align/banded-alignment/index.html contains FDARS_FENCE_OK.
    - SVGO idempotence holds on docs/assets/diagrams/banded-alignment.svg (twice-run diff empty, same svgo command as Task 1 with this filename).
    - `grep -c 'role="img"' docs/assets/diagrams/banded-alignment.svg` returns 1; `grep -Ec 'viewBox="0 0 720 (300|480)"' docs/assets/diagrams/banded-alignment.svg` returns 1.
    - The page names the band parameter: `grep -c 'band_frac' docs/align/banded-alignment.md` returns ≥ 1 and `grep -ci 'sakoe' docs/align/banded-alignment.md` returns ≥ 1.
    - `grep -c 'banded-alignment.svg' docs/align/banded-alignment.md` returns 1.
  </acceptance_criteria>
  <done>banded-alignment.md renders with an executed FDARS_FENCE_OK fence against the real karcher_mean_with_band binding, and its SVG passes SVGO idempotence, conforms to STYLE_SPEC, and depicts the Sakoe–Chiba diagonal corridor.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update docs/advisor/aspects.md for scoring / imputation-quality / registration-quality coverage</name>
  <read_first>
    - docs/advisor/aspects.md (the FULL page — the Coverage Table near the top, the ## represent section ~line 179, the ## alignment section ~line 83, and an existing executed-fence example ~line 172/247 to mirror)
    - .planning/research/FEATURES.md (advisor-relevance notes for scoring, imputation, registration-quality — the diagnostic field names)
    - STATE.md decisions (Phase 28 ADV-01/ADV-02: scoring is a diagnostics-only method; imputation-quality extends represent; registration-quality extends alignment; new keys default None when inputs absent; guard-sync test stays green)
  </read_first>
  <files>docs/advisor/aspects.md</files>
  <action>
    EXTEND (do not replace) docs/advisor/aspects.md for the Phase-28 advisor additions:
    (a) Add a `## scoring` aspect section (matching the existing per-aspect section format: **fdars source**, a key table, **Task families**) documenting the scoring diagnostics method wired in ADV-01 (the five functional_* metrics as diagnostics). Add a `scoring` row to the Coverage Table near the top.
    (b) In the existing `## represent` section, add the imputation-quality diagnostic keys (e.g. nan_frac / has_boundary_nans / imputation residual per ADV-02) as new table rows, noting they default None when no NaN inputs are present.
    (c) In the existing `## alignment` section, add the registration-quality diagnostic keys (ls_score / pairwise_corr_score / sobolev_score) per ADV-02, noting they default None when registration inputs are absent.
    Keep any advisor advise() (LLM) fence ILLUSTRATIVE / non-executed or env-gated so the build stays offline — do NOT add an executed fence that requires an API key. If you add an executed fence, it must be build_diagnostics-only (offline, deterministic) and emit FDARS_FENCE_OK, mirroring the existing offline diagnostics fences already on this page (e.g. around line 172). Preserve every existing FDARS_FENCE_OK sentinel already in the file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts DOCS_FAST=1 mkdocs build 2>&amp;1 | tail -3; grep -c "FDARS_FENCE_OK" site/advisor/aspects/index.html</automated>
  </verify>
  <acceptance_criteria>
    - The build succeeds and site/advisor/aspects/index.html still contains every pre-existing FDARS_FENCE_OK sentinel (count is ≥ the pre-edit count — no executed diagnostics fence was broken).
    - `grep -c '## scoring' docs/advisor/aspects.md` returns 1 (new scoring aspect section added).
    - The represent and alignment sections gained the new diagnostic rows: `grep -ci 'imputation\|nan_frac\|has_boundary_nans' docs/advisor/aspects.md` returns ≥ 1 and `grep -ci 'pairwise_corr\|ls_score\|sobolev' docs/advisor/aspects.md` returns ≥ 1.
    - No advise() executed fence requiring an API key was added: `grep -c 'ANTHROPIC_API_KEY' docs/advisor/aspects.md` returns 0.
  </acceptance_criteria>
  <done>aspects.md documents the scoring aspect + imputation-quality + registration-quality diagnostics, keeps the build offline (no API-key fence), and preserves all existing FDARS_FENCE_OK sentinels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → executed fence | markdown-exec runs Python at build time; must stay offline/deterministic |
| advisor advise() fence → LLM API | advise() would require a network key; MUST stay illustrative/non-executed to keep the build offline |
| hand-authored SVG → SVGO gate | committed markup is source of truth; gate is check-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-07 | Information Disclosure | advisor advise() (LLM) fence | high | mitigate | advise() fences kept illustrative/non-executed or env-gated; only build_diagnostics (offline, no key) may be executed |
| T-29-08 | Tampering | committed hand-authored SVGs | low | mitigate | SVGO gate stdout-only idempotence check, never rewrites committed files |
| T-29-09 | Denial of Service | non-deterministic / slow fence (large phoneme DP) | medium | mitigate | fixed seeds + small/subsampled datasets keep the banded-alignment fence fast and reproducible |
</threat_model>

<verification>
- Both align pages build (full `mkdocs build`) with executed fences containing FDARS_FENCE_OK.
- Both new align SVGs pass SVGO idempotence and carry role="img" + a permitted 720-width viewBox.
- The shift-registration SVG depicts a rigid horizontal translation (not a warp); the banded SVG depicts the Sakoe–Chiba corridor.
- aspects.md gains scoring / imputation-quality / registration-quality coverage without any API-key fence and preserves existing sentinels.
- Nav wiring + full strict build are handled in Plan 04.
</verification>

<success_criteria>
Two align pages (shift-registration, banded-alignment) render with method-accurate STYLE_SPEC-conforming SVGs (SVGO-idempotent) and executed offline FDARS_FENCE_OK worked examples against the real shipped fdars.alignment bindings, and docs/advisor/aspects.md is extended for the new scoring / imputation-quality / registration-quality diagnostics while staying offline.
</success_criteria>

<output>
Create `.planning/phases/29-docs-diagrams-worked-examples/29-03-SUMMARY.md` when done.
</output>

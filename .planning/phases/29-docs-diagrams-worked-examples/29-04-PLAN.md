---
phase: 29-docs-diagrams-worked-examples
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - mkdocs.yml
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All six new capability pages are wired into the mkdocs.yml nav (represent + analyze + align) (DOCS-03)"
    - "Full `mkdocs build --strict` exits 0 OFFLINE against the whole site (not just new pages), with the real shipped bindings (DOCS-03)"
    - "Every one of the ~6 new SVGs passes the SVGO idempotence lint + build-determinism gate (DOCS-01)"
    - "Every executed fence across all new pages emits FDARS_FENCE_OK (DOCS-02)"
    - "A human has reviewed the ~6 new diagrams on the built site for method-accuracy and approved (DOCS-01)"
  artifacts:
    - mkdocs.yml
  key_links:
    - "mkdocs.yml Analyze nav → functional-statistics.md + scoring-metrics.md"
    - "mkdocs.yml Align nav → shift-registration.md + banded-alignment.md"
    - "the full strict build → every new page + every executed fence + every new SVG"
---

<objective>
Finalize Phase 29: centralize the remaining nav wiring (the four analyze + align pages from Plans 02/03; represent's two were wired by the Plan 01 tracer), then run the phase-end gates — full offline `mkdocs build --strict` over the whole site, the SVGO idempotence + build-determinism gate over all six new SVGs, and a confirmation that every new executed fence emits FDARS_FENCE_OK — and finish with a blocking human PNG/method-accuracy review of the six new diagrams on the built site.

Purpose: This plan serializes LAST (it is the only Wave-3 plan) so the whole-site strict build and the cross-cutting gates run once against everything Plans 01–03 produced, and so the human review sees the complete diagram set together (per the v1.0/v2.1 review-gate precedent and the project constraint that diagram correctness is validated by section review, not assumed).
Output: complete nav, a green whole-site strict build, all-SVG gate pass, and human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Nav file — add the Analyze + Align entries (represent entries already added by Plan 01):
@mkdocs.yml

# The SVG style contract + the SVGO gate mechanism reference:
@docs/assets/diagrams/STYLE_SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the four analyze + align pages into mkdocs.yml nav</name>
  <read_first>
    - mkdocs.yml lines 82-145 (the nav block — Represent, Align, Analyze, AI Advisor sections)
    - the four page paths produced by Plans 02/03: analyze/functional-statistics.md, analyze/scoring-metrics.md, align/shift-registration.md, align/banded-alignment.md
  </read_first>
  <files>mkdocs.yml</files>
  <action>
    Edit mkdocs.yml (scoped Edit calls, not a rewrite — this file also carries the represent entries added by Plan 01 in an earlier wave; do NOT disturb them):
    - Under the Align section, add `- Shift Registration: align/shift-registration.md` positioned BEFORE Elastic Alignment (it is the entry-level baseline) and `- Banded Elastic Alignment: align/banded-alignment.md` after the Advanced Elastic Alignment entry.
    - Under the Analyze section, add `- Functional Statistics: analyze/functional-statistics.md` and `- Scoring Metrics: analyze/scoring-metrics.md` (near Covariance Functions is a natural slot).
    Verify no page under docs/ is left un-navigated (mkdocs --strict fails on orphan pages).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; for p in analyze/functional-statistics.md analyze/scoring-metrics.md align/shift-registration.md align/banded-alignment.md represent/interpolation.md represent/imputation.md; do grep -q "$p" mkdocs.yml || { echo "MISSING NAV: $p"; exit 1; }; done &amp;&amp; echo ALL_NAV_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints ALL_NAV_WIRED (all six new pages, including the two represent pages from Plan 01, are present in mkdocs.yml).
    - `grep -c 'align/shift-registration.md' mkdocs.yml` returns 1 and `grep -c 'align/banded-alignment.md' mkdocs.yml` returns 1.
    - `grep -c 'analyze/functional-statistics.md' mkdocs.yml` returns 1 and `grep -c 'analyze/scoring-metrics.md' mkdocs.yml` returns 1.
  </acceptance_criteria>
  <done>All six new capability pages are wired into the mkdocs.yml nav; no docs/ page is orphaned.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-end gates — whole-site strict build (offline) + SVGO idempotence/determinism over all 6 new SVGs + FDARS_FENCE_OK presence</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO invocation: `npx svgo@3.3.4 --config svgo.config.mjs --quiet --input <file> --output -`, idempotence check pattern)
    - Makefile (the `docs` target: `mkdocs build --strict` then `python scripts/check_docs_figures.py site`)
  </read_first>
  <files>mkdocs.yml</files>
  <action>
    Run the phase-end gates against the WHOLE site (not just new pages), using the memory build recipe (venv + PYTHONPATH=scripts). This task authors no new content — it is the gate. If any gate fails, fix the offending page/SVG (or flag back to the relevant plan) until all pass. This must be the FULL build (DOCS_FAST unset — the source of truth), run OFFLINE (no API key in env). The gate steps:
    1. Full strict build: `PYTHONPATH=scripts mkdocs build --strict` must exit 0.
    2. Figure-error gate: `python scripts/check_docs_figures.py site` must pass (no errored figure blocks).
    3. SVGO idempotence over all six new SVGs (interpolation-policy, imputation, functional-statistics, scoring-metrics, shift-registration, banded-alignment): for each, run svgo@3.3.4 twice and diff pass 2 against pass 1 — must be empty for every file (the exact gate the CI docs.yml runs corpus-wide).
    4. FDARS_FENCE_OK presence: confirm each new page's built HTML contains the sentinel.
    Do NOT let svgo rewrite any committed SVG (stdout mode only, --output -).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts mkdocs build --strict 2>&amp;1 | tail -3 &amp;&amp; python scripts/check_docs_figures.py site &amp;&amp; ok=1; for s in interpolation-policy imputation functional-statistics scoring-metrics shift-registration banded-alignment; do f=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/$s.svg --output -); s2=$(printf '%s' "$f" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); if [ "$f" != "$s2" ]; then echo "SVGO NOT IDEMPOTENT: $s"; ok=0; fi; done; for p in represent/interpolation represent/imputation analyze/functional-statistics analyze/scoring-metrics align/shift-registration align/banded-alignment; do grep -rq FDARS_FENCE_OK site/$p/index.html || { echo "MISSING FENCE: $p"; ok=0; }; done; [ "$ok" = 1 ] &amp;&amp; echo ALL_GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=scripts mkdocs build --strict` exits 0 (full build, DOCS_FAST unset, offline) — the verify command prints ALL_GATES_GREEN.
    - `python scripts/check_docs_figures.py site` reports no errored figure blocks.
    - All six new SVGs are SVGO-idempotent (no "SVGO NOT IDEMPOTENT" line printed).
    - Each of the six new pages' built HTML contains FDARS_FENCE_OK (no "MISSING FENCE" line printed).
    - No API key is present in the build env (the build ran offline): the advisor advise() fences did not execute.
  </acceptance_criteria>
  <done>The whole-site strict build passes offline, every new SVG is SVGO-idempotent under svgo.config.mjs, and every new executed fence emits FDARS_FENCE_OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking human PNG/method-accuracy review of all six new diagrams</name>
  <what-built>Six new hand-authored inline SVG concept diagrams for the v4.0 capabilities — interpolation+extrapolation policy, missing-value imputation, functional statistics, scoring metrics, shift registration, banded elastic alignment — rendered on a fully-built, strict-clean site with executed offline worked examples and complete nav.</what-built>
  <how-to-verify>
    Build the full site and PNG-review each of the six new diagrams for METHOD-ACCURACY (this is the phase-final review gate — the project constraint is that diagram correctness is validated by section review, not assumed):
    1. Build (source of truth, no DOCS_FAST): `cd /home/simonm/projects/rust/pyfda &amp;&amp; source .venv/bin/activate; PYTHONPATH=scripts mkdocs build`.
    2. Render all six to PNG: `for s in interpolation-policy imputation functional-statistics scoring-metrics shift-registration banded-alignment; do rsvg-convert docs/assets/diagrams/$s.svg -o /tmp/$s.png; done` and open /tmp/*.png.
    3. Method-accuracy checklist — confirm EACH:
       - interpolation-policy: Exception shows a STOP/error marker at the boundary and does NOT draw an extrapolated curve; Fill drops to a constant; Periodic wraps; Boundary clamps flat.
       - imputation: Linear ramps across the interior gap and extends FLAT at the boundary gap; Mean/Constant are horizontal fills; non-NaN values preserved.
       - functional-statistics: the depth-based median is one of the ACTUAL observed curves (not an averaged/synthetic line), visually distinct from the mean (and from geometric_median if shown); the ±1 std band is widest where curves diverge.
       - scoring-metrics: |y_true − y_pred| residual is shaded and the scores are shown as domain-integrated scalars.
       - shift-registration: a RIGID HORIZONTAL translation (curve slides unchanged) — visually distinct from an elastic warp (straight shift arrow vs curved warp arrow).
       - banded-alignment: a Sakoe–Chiba DIAGONAL corridor over the DP grid; cells outside the band shaded "not evaluated".
    4. Optionally `mkdocs serve` and read the six pages at http://127.0.0.1:8000, confirming each executed fence rendered output and the FDARS_FENCE_OK sentinel appears in the source-above block.
  </how-to-verify>
  <resume-signal>Type "approved" or list per-diagram corrections needed (which SVG, what is method-inaccurate)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full docs build → all executed fences | markdown-exec runs every page's Python; the whole build must stay offline/deterministic |
| advisor advise() fence → LLM API | must remain non-executed/illustrative so the strict build needs no network/key |
| committed SVGs → SVGO gate | check-only idempotence gate; never rewrites committed hand-authored markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-10 | Information Disclosure | full-site build executing all fences | high | mitigate | build runs offline (no API key in env); advise() fences illustrative; only build_diagnostics/data fences execute |
| T-29-11 | Denial of Service | non-deterministic whole-site build | medium | mitigate | svg.hashsalt=fdars-docs + fixed seeds + check_docs_figures.py gate keep the strict build reproducible and figure-error-free |
| T-29-12 | Tampering | committed hand-authored SVGs | low | mitigate | SVGO gate stdout-only (--output -), idempotence check, never rewrites committed files |
</threat_model>

<verification>
- All six new pages are nav-wired in mkdocs.yml (no orphan pages).
- Full `PYTHONPATH=scripts mkdocs build --strict` exits 0 offline over the whole site.
- `python scripts/check_docs_figures.py site` reports no figure errors.
- All six new SVGs are SVGO-idempotent under svgo.config.mjs.
- Every new page's built HTML contains FDARS_FENCE_OK.
- Human has PNG-reviewed and approved the six diagrams for method-accuracy.
</verification>

<success_criteria>
The complete v4.0 documentation ships: six nav-wired capability pages across represent/analyze/align plus the updated advisor coverage, each with a method-accurate SVGO-idempotent SVG and an executed offline FDARS_FENCE_OK worked example, on a green whole-site `mkdocs build --strict`, with human method-accuracy sign-off on all six new diagrams.
</success_criteria>

<output>
Create `.planning/phases/29-docs-diagrams-worked-examples/29-04-SUMMARY.md` when done.
</output>
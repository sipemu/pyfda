---
phase: 27-scoring-metrics-alignment-registration-bindings
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alignment_mod.rs
  - python/fdars/fdata_class.py
  - tests/test_alignment_registration.py
autonomous: true
requirements: [ALGN-01, ALGN-02, ALGN-03]
tags: [rust, pyo3, fdars-core, alignment, registration, banded]

estimate:
  tokens: 68000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "least_squares_shift_registration returns a dict with registered_data (n,m) ndarray and shifts (n,) ndarray (ALGN-01)"
    - "fd.shift_register() returns (registered Fdata, shifts) with same argvals (ALGN-01)"
    - "least_squares_score / pairwise_correlation_score / sobolev_least_squares_score return finite floats; sobolev on a NON-uniform grid raises a clear ValueError (ALGN-02)"
    - "karcher_mean_with_band(..., band_frac=None) equals unbanded karcher_mean to tolerance; band_frac defaults to None (ALGN-03)"
    - "banded distance matrices route through fdmatrix_to_numpy2d and pass a multi-curve DISTINCT-per-curve round-trip transposition test (ALGN-03, #33 class)"
    - "No .unwrap() on any fallible fdars-core Result in the new alignment wrappers"
  artifacts:
    - "src/alignment_mod.rs (+7 pyfunctions: least_squares_shift_registration, least_squares_score, pairwise_correlation_score, sobolev_least_squares_score, karcher_mean_with_band, elastic_self_distance_matrix_with_band, elastic_cross_distance_matrix_with_band)"
    - "python/fdars/fdata_class.py (fd.shift_register() method)"
    - "tests/test_alignment_registration.py (NEW)"
  key_links:
    - "least_squares_shift_registration → PyDict {registered_data, shifts} (mirrors karcher_mean dict pattern)"
    - "fd.shift_register() delegates to _native.alignment.least_squares_shift_registration, wraps registered_data into an Fdata with same argvals"
---

<objective>
Extend `fdars.alignment` with the fdars-core 0.17.0 shift-registration, registration-quality,
and banded-elastic API: `least_squares_shift_registration` (→ `ShiftRegistrationResult`
marshalled as a PyDict) plus the `fd.shift_register()` Fdata method; the 3 registration-quality
scores (`least_squares_score`, `pairwise_correlation_score`, `sobolev_least_squares_score`);
and the 3 banded functions (`karcher_mean_with_band`, `elastic_self_distance_matrix_with_band`,
`elastic_cross_distance_matrix_with_band`) with optional `band_frac` (`None` = unbanded). Every
fallible input surfaces as a clean `ValueError`. Implements ALGN-01, ALGN-02, ALGN-03 and the
CONTEXT.md locked decisions (bind `*_with_band` NOT `*_banded`; `band_frac: Option<f64>=None`;
dict result; `fd.shift_register()`).

Purpose: shift registration, quality scores, and banded elastic alignment belong with the
existing elastic/karcher family already in `alignment_mod.rs`.
Output: +7 pyfunctions in `src/alignment_mod.rs`, `fd.shift_register()`, `tests/test_alignment_registration.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scoring-metrics-alignment-registration-bindings/27-CONTEXT.md
@src/convert.rs
@python/fdars/fdata_class.py
</context>

<artifacts_this_phase_produces>
- `src/alignment_mod.rs` +7 pyfunctions: `least_squares_shift_registration`,
  `least_squares_score`, `pairwise_correlation_score`, `sobolev_least_squares_score`,
  `karcher_mean_with_band`, `elastic_self_distance_matrix_with_band`,
  `elastic_cross_distance_matrix_with_band`
- `least_squares_shift_registration` PyDict result `{registered_data: (n,m), shifts: (n,)}`
- `fd.shift_register()` Fdata method (returns registered Fdata + shifts)
- NEW `tests/test_alignment_registration.py`
</artifacts_this_phase_produces>

<confirmed_signatures>
Confirmed against installed fdars-core 0.17.0 source
(`~/.cargo/registry/src/*/fdars-core-0.17.0/src/alignment/{shift,quality,karcher,pairwise}.rs`
+ `src/lib.rs` re-exports under `fdars_core::alignment::`):

- `least_squares_shift_registration(data: &FdMatrix, argvals: &[f64], max_shift: f64) -> Result<ShiftRegistrationResult, FdarError>`
  - `ShiftRegistrationResult { registered_data: FdMatrix /* n×m */, shifts: Vec<f64> /* len n */ }`
    (CONFIRMED field names — `registered_data` and `shifts`, exactly two fields.)
- `least_squares_score(registered: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>`
- `pairwise_correlation_score(registered: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>`
- `sobolev_least_squares_score(registered: &FdMatrix, argvals: &[f64], lambda: f64) -> Result<f64, FdarError>`
  (requires a UNIFORM grid when lambda > 0 — 5-point stencil derivative; non-uniform surfaces as FdarError → ValueError)
- `karcher_mean_with_band(data, argvals, max_iter: usize, tol: f64, lambda: f64, band_frac: Option<f64>) -> KarcherMeanResult`
  (INFALLIBLE — returns the struct directly, same fields as existing `karcher_mean`: mean, mean_srsf, aligned_data (n×m FdMatrix), gammas (n×m FdMatrix), n_iter, converged)
- `elastic_self_distance_matrix_with_band(data, argvals, lambda: f64, band_frac: Option<f64>) -> FdMatrix` (INFALLIBLE)
- `elastic_cross_distance_matrix_with_band(data1, data2, argvals, lambda: f64, band_frac: Option<f64>) -> FdMatrix` (INFALLIBLE)

IMPORTANT: bind the `_with_band` variants (band_frac: Option<f64>, None disables the band), NOT
the coexisting `_banded` variants (band_frac: f64, where 0.0 does NOT disable). Mirror the
existing `karcher_mean` (dict, infallible) and `elastic_self/cross_distance_matrix` (matrix via
fdmatrix_to_numpy2d, infallible) wrappers already in `src/alignment_mod.rs`.
</confirmed_signatures>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — least_squares_shift_registration end-to-end (dict result + identity-shift assertion)</name>
  <files>src/alignment_mod.rs, tests/test_alignment_registration.py</files>
  <read_first>
    - src/alignment_mod.rs lines ~68-93 — the existing `karcher_mean` wrapper: `#[pyo3(signature=(...))]`, `numpy2d_to_fdmatrix`/`numpy1d_to_vec`, the `PyDict::new(py)` construction, `dict.set_item("aligned_data", fdmatrix_to_numpy2d(py, &result.aligned_data))?`, `dict.set_item("shifts"...)` style with `vec_to_numpy1d`, and `Ok(dict)`. This is the direct analog for the shift-registration dict.
    - src/alignment_mod.rs lines ~2100-2137 — the tail of the existing `register()` fn where new `m.add_function(wrap_pyfunction!(...))` lines are appended.
    - src/convert.rs — `fdmatrix_to_numpy2d` (for registered_data), `vec_to_numpy1d` (for shifts), `to_pyresult` (fallible call), `numpy2d_to_fdmatrix`, `numpy1d_to_vec`.
    - The `<confirmed_signatures>` block for the exact `least_squares_shift_registration` signature and the two `ShiftRegistrationResult` field names (`registered_data`, `shifts`).
  </read_first>
  <action>
In `src/alignment_mod.rs`, add `least_squares_shift_registration` as a `#[pyfunction]` returning
`PyResult<Bound<'py, pyo3::types::PyDict>>`, mirroring the existing `karcher_mean` dict wrapper.
Signature `#[pyo3(signature = (data, argvals, max_shift))]` taking
`(py, data: PyReadonlyArray2<f64>, argvals: PyReadonlyArray1<f64>, max_shift: f64)`. Convert data
via `numpy2d_to_fdmatrix(...)?`, argvals via `numpy1d_to_vec`, call
`to_pyresult(fdars_core::alignment::least_squares_shift_registration(&mat, &av, max_shift))?`.
Build a PyDict: `dict.set_item("registered_data", fdmatrix_to_numpy2d(py, &result.registered_data))?`
(routes through the #33 transposition guard) and
`dict.set_item("shifts", vec_to_numpy1d(py, result.shifts))?`; `Ok(dict)`. The fallible core call
must propagate through `to_pyresult()` — never panic-unwrap a Result. Register
it by appending `m.add_function(wrap_pyfunction!(least_squares_shift_registration, m)?)?;` to the
existing `register()` fn. Rebuild with `.venv/bin/maturin develop`. Create
`tests/test_alignment_registration.py` with: a dict-shape test — for n curves on an m-grid,
`result["registered_data"].shape == (n, m)`, `result["shifts"].shape == (n,)`,
`result["shifts"].dtype == np.float64`; and an identity-shift assertion — for data already aligned
(all curves identical, or a single shape at zero offset) `np.allclose(result["shifts"], 0.0, atol=1e-6)`.
Verify green before Task 2.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_alignment_registration.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import fdars, numpy as np; r=fdars.alignment.least_squares_shift_registration(np.ones((3,20)), np.linspace(0,1,20), 0.2); print(sorted(r.keys()))"` prints `['registered_data', 'shifts']`.
    - `tests/test_alignment_registration.py` passes: dict shapes correct; identity-shift ≈ 0.
    - `grep -v '^\s*//' src/alignment_mod.rs | grep -c 'least_squares_shift_registration.*unwrap()'` returns `0` (no unwrap on the new wrapper).
  </acceptance_criteria>
  <done>least_squares_shift_registration bound end-to-end returning the {registered_data, shifts} dict; rebuilt; shape + identity-shift tests pass; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand — 3 registration-quality scores + fd.shift_register() method</name>
  <files>src/alignment_mod.rs, python/fdars/fdata_class.py, tests/test_alignment_registration.py</files>
  <read_first>
    - src/alignment_mod.rs — the Task-1 `least_squares_shift_registration` wrapper and the scalar-returning wrappers already in the module (e.g. `elastic_self_distance_matrix` returns `PyResult<Bound<PyArray2>>`; the quality scores return `PyResult<f64>` — simpler). Append to the same `register()` fn.
    - python/fdars/fdata_class.py lines ~628-724 — the `interpolate()` and `impute()` Fdata methods: how they call `_native.<submodule>.<fn>(self.data, self.argvals, ...)` and construct a new `Fdata(data=..., argvals=..., rangeval=..., names=self.names.copy(), id=self.id.copy(), metadata=self.metadata)`. `fd.shift_register()` mirrors this but also returns the shifts array.
    - python/fdars/fdata_class.py lines ~602-626 — the `median()` method for the return-a-derived-Fdata convention.
    - The `<confirmed_signatures>` block for the three quality-score signatures (note `sobolev_least_squares_score` takes an extra `lambda` and needs a uniform grid).
  </read_first>
  <behavior>
    - least_squares_score(registered, argvals) returns a finite float ≥ 0.
    - pairwise_correlation_score(registered, argvals) returns a float in [-1, 1]; for near-identical registered curves it is close to 1.0.
    - sobolev_least_squares_score(registered, argvals, lambda) with lambda=0 equals least_squares_score to tolerance.
    - sobolev_least_squares_score with lambda>0 on a NON-uniform argvals grid raises ValueError (clear message, not a silent wrong answer).
    - fd.shift_register(max_shift) returns a 2-tuple (Fdata, shifts ndarray); the Fdata has the same argvals/rangeval and n_obs as self; shifts has shape (n_obs,).
  </behavior>
  <action>
In `src/alignment_mod.rs`, add `least_squares_score` and `pairwise_correlation_score` as
`#[pyfunction]`s returning `PyResult<f64>` — each takes `(py?, registered: PyReadonlyArray2<f64>,
argvals: PyReadonlyArray1<f64>)`, converts via `numpy2d_to_fdmatrix(...)?`/`numpy1d_to_vec`, and
returns `to_pyresult(fdars_core::alignment::<fn>(&mat, &av))?` (no `py` handle needed for a scalar
return; follow the module's scalar-return convention). Add `sobolev_least_squares_score` with
`#[pyo3(signature = (registered, argvals, lambda_=0.0))]` passing `lambda_` through to
`fdars_core::alignment::sobolev_least_squares_score(&mat, &av, lambda_)` via `to_pyresult`. Give
each a NumPy-style docstring; for sobolev note the uniform-grid requirement when lambda>0. Register
all three. Every fallible core call must propagate through `to_pyresult()` — never panic-unwrap a
Result. In `python/fdars/fdata_class.py`, add `fd.shift_register(max_shift)`
mirroring `interpolate()`: delegate to `_native.alignment.least_squares_shift_registration(
self.data, self.argvals, max_shift)`, wrap `result["registered_data"]` into a new `Fdata` (same
argvals, rangeval, names.copy(), id.copy(), metadata), and return the 2-tuple `(registered_fdata,
result["shifts"])`. Rebuild with `.venv/bin/maturin develop`. Extend
`tests/test_alignment_registration.py`: the three scores return finite floats in-range;
`sobolev_least_squares_score(reg, uniform_grid, 0.0)` ≈ `least_squares_score(reg, uniform_grid)`;
`pytest.raises(ValueError)` for `sobolev_least_squares_score(reg, non_uniform_grid, lambda>0)`;
`fd.shift_register(max_shift)` returns `(Fdata, ndarray)` with correct n_obs, argvals unchanged, and
shifts shape `(n_obs,)`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_alignment_registration.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import fdars; [getattr(fdars.alignment, f) for f in ('least_squares_score','pairwise_correlation_score','sobolev_least_squares_score')]; print('ok')"` prints `ok`.
    - `tests/test_alignment_registration.py` passes: sobolev(lambda=0)≈least_squares_score; `pytest.raises(ValueError)` on sobolev with lambda>0 + non-uniform grid; `fd.shift_register()` returns `(Fdata, ndarray)` with shifts shape `(n_obs,)`.
    - `grep -n "def shift_register" python/fdars/fdata_class.py` returns a match.
  </acceptance_criteria>
  <done>Three quality scores bound with the sobolev non-uniform-grid ValueError proven; fd.shift_register() returns (Fdata, shifts); rebuilt; tests pass; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand — 3 banded *_with_band functions + multi-curve transposition test</name>
  <files>src/alignment_mod.rs, tests/test_alignment_registration.py</files>
  <read_first>
    - src/alignment_mod.rs lines ~68-93 — `karcher_mean` dict wrapper (the `karcher_mean_with_band` analog: same dict keys, plus the extra `band_frac` param; INFALLIBLE so no `to_pyresult`).
    - src/alignment_mod.rs lines ~214-243 — `elastic_self_distance_matrix` and `elastic_cross_distance_matrix`: `PyReadonlyArray2` inputs, `fdars_core::alignment::elastic_*_distance_matrix(...)`, `Ok(fdmatrix_to_numpy2d(py, &result))`. The banded variants add `band_frac: Option<f64>` and are also INFALLIBLE.
    - src/convert.rs — `fdmatrix_to_numpy2d` (mandatory for both banded matrix returns — the #33 guard).
    - The `<confirmed_signatures>` block for the three `_with_band` signatures and their param orders (note `karcher_mean_with_band` takes `max_iter, tol, lambda, band_frac`; the distance matrices take `lambda, band_frac`).
    - tests/test_alignment_registration.py from Tasks 1-2 (extend).
  </read_first>
  <behavior>
    - karcher_mean_with_band(data, argvals, band_frac=None) returns a dict with the same keys as karcher_mean (mean, mean_srsf, aligned_data (n,m), gammas (n,m), n_iter, converged), and equals unbanded karcher_mean to tolerance when band_frac=None.
    - elastic_self_distance_matrix_with_band(data, argvals, band_frac=None) returns an (n,n) symmetric matrix with a zero diagonal.
    - elastic_cross_distance_matrix_with_band(data1, data2, argvals, band_frac=None) returns a (n1,n2) matrix.
    - Multi-curve transposition guard: with 3 DISTINCT-shaped curves, the self-distance matrix computed from the (n,m) input matches the matrix computed from each pair independently — a row/col swap (transposition bug) changes at least one off-diagonal entry.
    - band_frac defaults to None (unbanded); passing a float in (0,1) restricts the Sakoe–Chiba band.
  </behavior>
  <action>
In `src/alignment_mod.rs`, add `karcher_mean_with_band` as a `#[pyfunction]` returning
`PyResult<Bound<'py, pyo3::types::PyDict>>` mirroring `karcher_mean` but with
`#[pyo3(signature = (data, argvals, lambda_=0.0, max_iter=20, tol=1e-4, band_frac=None))]` where
`band_frac: Option<f64>`; call `fdars_core::alignment::karcher_mean_with_band(&mat, &av, max_iter,
tol, lambda_, band_frac)` (INFALLIBLE — no `to_pyresult`) and set the same six dict keys as
`karcher_mean` (`mean`, `mean_srsf`, `aligned_data`, `gammas` via `fdmatrix_to_numpy2d`; `n_iter`,
`converged`). Add `elastic_self_distance_matrix_with_band` with
`#[pyo3(signature = (data, argvals, lambda_=0.0, band_frac=None))]` calling
`fdars_core::alignment::elastic_self_distance_matrix_with_band(&mat, &av, lambda_, band_frac)` and
returning `Ok(fdmatrix_to_numpy2d(py, &result))`. Add `elastic_cross_distance_matrix_with_band`
with `#[pyo3(signature = (data1, data2, argvals, lambda_=0.0, band_frac=None))]` calling the cross
variant on two converted matrices and returning `fdmatrix_to_numpy2d`. NEVER call
`PyArray2::from_vec2` directly — always `fdmatrix_to_numpy2d`. Register all three. Rebuild with
`.venv/bin/maturin develop`. Extend `tests/test_alignment_registration.py`: (a) `band_frac=None`
equivalence — `karcher_mean_with_band(X, av, band_frac=None)["mean"]` ≈ `karcher_mean(X, av)["mean"]`
to tolerance; (b) MULTI-CURVE transposition round-trip — build 3 curves with DISTINCT per-curve
shapes (e.g. sin(k·π·t) for k=1,2,3), compute `elastic_self_distance_matrix_with_band`, assert the
matrix is symmetric with a ~0 diagonal AND that a chosen off-diagonal entry d[0,2] matches the
independently-computed pairwise elastic distance between curve 0 and curve 2 (a transposition
scrambles this); (c) shape check on `elastic_cross_distance_matrix_with_band` → `(n1, n2)`. Then run
the FULL suite and confirm no regressions vs the 328-passed baseline.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_alignment_registration.py -q 2>&1 | tail -15 && .venv/bin/pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import fdars; [getattr(fdars.alignment, f) for f in ('karcher_mean_with_band','elastic_self_distance_matrix_with_band','elastic_cross_distance_matrix_with_band')]; print('ok')"` prints `ok`.
    - `tests/test_alignment_registration.py` passes: `band_frac=None` matches unbanded karcher_mean; multi-curve self-distance matrix is symmetric with ~0 diagonal and the off-diagonal matches the independent pairwise distance (transposition guard); cross matrix shape `(n1,n2)`.
    - `grep -c 'karcher_mean_banded\|elastic_self_distance_matrix_banded\|elastic_cross_distance_matrix_banded' src/alignment_mod.rs` returns `0` (bound the `_with_band` variants, NOT `_banded`).
    - Full suite: `.venv/bin/pytest tests/ -q` shows no regressions vs the 328-passed / 4-skipped baseline.
  </acceptance_criteria>
  <done>Three banded `_with_band` functions bound with band_frac=None default; band_frac=None equivalence and multi-curve transposition round-trip proven; no `_banded` variant bound; full suite green; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 wrapper | Untrusted numpy arrays (non-uniform grids, degenerate shapes, empty data) cross into Rust here |
| PyO3 wrapper → fdars-core | Converted FdMatrix/argvals/band_frac cross into the compute engine |
| FdMatrix (column-major) → numpy (row-major) | Every matrix return (registered_data, gammas, aligned_data, banded distance matrices) crosses the layout boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | Multi-curve transposition (#33 class) on banded distance matrices → silently scrambled values | high | mitigate | Route every matrix return through `fdmatrix_to_numpy2d`; DISTINCT-per-curve round-trip test asserting an off-diagonal matches the independent pairwise distance |
| T-27-02-02 | Denial of Service | `.unwrap()` panic on fallible Result (shift registration, quality scores, sobolev non-uniform grid) | high | mitigate | `to_pyresult()` on all fallible calls; `pytest.raises(ValueError)` for sobolev non-uniform grid |
| T-27-02-03 | Tampering | Binding the wrong banded API (`_banded` where band_frac=0.0 does NOT disable) → wrong semantics | high | mitigate | Bind `_with_band` (band_frac: Option<f64>=None); grep gate asserts no `_banded` variant is bound; band_frac=None equals unbanded karcher_mean test |
| T-27-02-04 | Tampering | ShiftRegistrationResult exposing only registered_data, dropping shifts | medium | mitigate | Dict-shape test asserts both `registered_data` and `shifts` keys with correct shapes/dtype |

No npm/pip/cargo installs introduced in this plan (fdars-core 0.17.0 already pinned in Phase 25); package-legitimacy gate not applicable.
</threat_model>

<verification>
- `least_squares_shift_registration` returns `{registered_data (n,m), shifts (n,)}`; `fd.shift_register()` returns `(Fdata, shifts)`.
- Three quality scores return finite in-range floats; sobolev(lambda=0)≈least_squares_score; sobolev non-uniform grid + lambda>0 raises ValueError.
- `karcher_mean_with_band(band_frac=None)` ≈ unbanded `karcher_mean`; band_frac defaults to None.
- Banded distance matrices route through `fdmatrix_to_numpy2d`; multi-curve DISTINCT-per-curve transposition round-trip passes.
- No `_banded` variant bound (grep gate == 0); no `.unwrap()` on fallible results.
- Full suite green, no regressions vs 328-passed baseline.
</verification>

<success_criteria>
ALGN-01: user runs `least_squares_shift_registration` and receives registered curves + per-curve
shifts as a dict, plus `fd.shift_register()`. ALGN-02: user scores registration quality with the
three scores; the Sobolev uniform-grid requirement surfaces as a clear ValueError. ALGN-03: user
runs banded elastic alignment (three `_with_band` fns) with optional `band_frac` where `None` means
unbanded, and the banded distance matrices are proven layout-correct by a multi-curve round-trip test.
</success_criteria>

<output>
Create `.planning/phases/27-scoring-metrics-alignment-registration-bindings/27-02-SUMMARY.md` when done.
</output>

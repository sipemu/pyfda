---
phase: 81-curation-paper-registry
plan: 02
type: execute
wave: 2
depends_on: [81-01]
files_modified:
  - python/fdars/_references_map.json
  - tests/test_guard_sync_version_independent.py
autonomous: true
requirements: [CURATE-01, CURATE-02, CURATE-03]
estimate:
  tokens: 60000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per-sub-method depth callables (FM, band/MBD, modal, random-projection, random-Tukey, boxplot, epigraph) carry distinct paper attributions — never the dispatcher, never a shared over-broad pointer (CURATE-01, CURATE-02)"
    - "Dense FPCA + PACE (Yao-Müller-Wang 2005) + KL-simulation callables are curated with author-verified entries (CURATE-01)"
    - "Outlier-detection callables (functional boxplot, outliergram, MS-plot, LRT) map to their specific paper roots with honest curated:false where the root is unconfirmed (CURATE-02)"
    - "cross_language pointers populated per covered paper (fdapace, fdaoutlier, scikit-fda) with confidence flags (CURATE-03)"
  artifacts:
    - python/fdars/_references_map.json
    - tests/test_guard_sync_version_independent.py
  key_links:
    - "depth.functional_depth dispatcher stays OUT of callable_index (curated:false family — wired in 81-05), while depth sub-methods ARE curated"
    - "callable_index ↔ papers[*].callables ↔ _capability_map.json (GATE-05 A/B)"
    - "curated:false on functional_spatial depth until fdars-core source confirms the root (81-RESEARCH §3.3 A11)"
---

<objective>
Curate the depth sub-method family, the functional boxplot, dense FPCA + PACE, and outlier detection — the highest-density families that share few foundational papers.

Purpose: These families exercise the sub-method-attribution discipline hardest (six distinct depth papers vs one dispatcher; boxplot ≠ dispatcher; FPCA book vs PACE journal). Getting them right early sets the accuracy bar.
Output: `_references_map.json` grown with the depth, boxplot, FPCA/PACE, simulation, and outlier families; GATE-05 green; coverage fraction increased.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-curation-paper-registry/81-CONTEXT.md
@.planning/phases/81-curation-paper-registry/81-RESEARCH.md
@docs/authoring/references-schema.md
@python/fdars/_references_map.json
@python/fdars/_capability_map.json
@tests/test_guard_sync_version_independent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate depth sub-methods (modal, random-projection, spatial, epigraph) + functional boxplot</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.3 (depth sub-methods: cuevas_febrero_fraiman_2007 modal+random-proj+random-Tukey; claeskens_hubert_slaets_2014 random_projection_deriv; dabo_gijbels_2010 spatial — [ASSUMED], curated:false pending source; narisetty_nair_2016 epigraph) and §3.4 (sun_genton_2011 functional boxplot — [VERIFIED])
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md Pitfall 3 (functional_depth dispatcher vs per-method callables — dispatcher stays UNcurated here)
    - python/fdars/_capability_map.json (module `depth` — 18 callables listed; confirm modal_1d/2d, random_projection_1d/2d/deriv_1d, random_tukey_1d/2d, functional_spatial_*, kernel_functional_spatial_*, modified_epigraph_index_1d, functional_boxplot)
    - python/fdars/_references_map.json (FM 2001 + LP&R 2009 already seeded — do not duplicate)
  </read_first>
  <action>
    Per 81-RESEARCH §3.3–3.4, add depth sub-method papers. AUTHOR-VERIFY each against its DOI landing page; curated:false on any mismatch/unreachable page.

    1. `cuevas_febrero_fraiman_2007` (DOI `10.1007/s00180-007-0053-0`, [CITED]) → `depth.modal_1d`, `depth.modal_2d`, `depth.random_projection_1d`, `depth.random_projection_2d`, `depth.random_tukey_1d`, `depth.random_tukey_2d`.
    2. `claeskens_hubert_slaets_2014` (DOI `10.1080/01621459.2013.856795`, [ASSUMED]) → `depth.random_projection_deriv_1d`. Verify; curated:false if unconfirmed.
    3. Functional spatial depth (`depth.functional_spatial_1d/2d`, `depth.kernel_functional_spatial_1d/2d`): per §3.3 the root is CONTESTED/unconfirmed (A11). Check fdars-core source/docstrings for the cited paper. If confirmed → `dabo_gijbels_2010` curated:true; if not → add them with `curated:false` and a note "spatial depth root unconfirmed against fdars-core source". Per CONTEXT (unclear → curated:false), do NOT force-cite.
    4. `narisetty_nair_2016` (DOI `10.1080/01621459.2015.1110033`, [CITED]) → `depth.modified_epigraph_index_1d`. Verify the fdars-core source cites this before curated:true.
    5. `sun_genton_2011` (DOI `10.1198/jcgs.2011.09224`, [VERIFIED]) → `depth.functional_boxplot`. This is NOT the `functional_depth` dispatcher — it is a specific method. Add R cross_language `fdaoutlier::functional_boxplot` per §4.
    6. Do NOT add `depth.functional_depth` — the category dispatcher is the 81-05 curated:false family.

    Edit `papers[*].callables` and `callable_index` together (GATE-05 A).
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map"</automated>
    <fails_when>A depth callable cited does not resolve in _capability_map.json, `depth.functional_depth` was added to callable_index, or a curated DOI/URL fails the structural gate.</fails_when>
  </verify>
  <done>Depth sub-methods + functional boxplot curated per §3.3–3.4; functional_spatial marked per fdars-core source check; dispatcher excluded; GATE-05 green.</done>
</task>

<task type="auto">
  <name>Task 2: Curate dense FPCA, PACE, and KL simulation</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.5 (FPCA: ramsay_silverman_2005 extend to regression.fpca; yao_muller_wang_2005 PACE — [VERIFIED]; KL simulation callables)
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §4 (cross_language for yao_muller_wang_2005: R fdapace FPCA, Python scikit-fda FPCA, Matlab PACE confidence:low)
    - python/fdars/_capability_map.json (modules `pace_fpca`, `regression`, `simulation` — confirm pace_fpca/irreg_fdata_from_lists/PyIrregFdata, regression.fpca, simulation.sim_kl/simulate/eigenfunctions/eigenvalues)
    - python/fdars/_references_map.json (ramsay_silverman_2005 added in 81-01 — EXTEND it here, do not create a second entry)
  </read_first>
  <action>
    Per 81-RESEARCH §3.5, add the FPCA/PACE/simulation family. AUTHOR-VERIFY before curated:true.

    1. EXTEND the `ramsay_silverman_2005` entry (created in 81-01) to add `regression.fpca` and the KL-simulation callables `simulation.sim_kl`, `simulation.simulate`, `simulation.eigenfunctions`, `simulation.eigenvalues` (all resolve-checked in _capability_map.json). Update both `papers` and `callable_index`.
    2. Add `yao_muller_wang_2005` (DOI `10.1198/016214504000001745`, [VERIFIED]) → `pace_fpca.pace_fpca`, `pace_fpca.irreg_fdata_from_lists`, `pace_fpca.PyIrregFdata`. Add R/Python/Matlab cross_language per §4 (Matlab PACE confidence:low, `www.stat.ucdavis.edu` domain now allowlisted).
    3. Leave `simulation.gaussian_process`, `simulation.covariance_matrix` UNCURATED (curated:false family tail in 81-05) — standard GP theory, no FDA root.
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map"</automated>
    <fails_when>ramsay_silverman_2005 becomes a duplicate second entry, a PACE callable does not resolve, or cross_language URLs use a non-allowlisted domain.</fails_when>
  </verify>
  <done>FPCA/PACE/KL-simulation curated per §3.5; ramsay_silverman_2005 extended (not duplicated); yao_muller_wang_2005 added with cross_language; GATE-05 green.</done>
</task>

<task type="auto">
  <name>Task 3: Curate outlier detection (boxplot-linked, MS-plot, LRT)</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.19 (outliers: sun_genton_2011 outliergram; dai_genton_2019 magnitude_shape+muod; febrero_galeano_gonzalez_2008 LRT; depthgram/tvdmss/sequential_transform → curated:false pending verification)
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §4 (cross_language: febrero fda.usc foutliers, sun_genton fdaoutlier functional_boxplot)
    - python/fdars/_capability_map.json (module `outliers` — 8 callables; confirm outliergram, magnitude_shape, muod, detect_outliers_lrt, detect_outliers_lrt_with_dist, depthgram, tvdmss, sequential_transform_outliers)
    - python/fdars/_references_map.json (sun_genton_2011 added in Task 1 — EXTEND for outliergram)
  </read_first>
  <action>
    Per 81-RESEARCH §3.19, add the outlier-detection family. AUTHOR-VERIFY before curated:true.

    1. EXTEND `sun_genton_2011` (added in Task 1) to add `outliers.outliergram`.
    2. Add `dai_genton_2019` (DOI `10.1016/j.csda.2018.03.017`, [CITED]) → `outliers.magnitude_shape`, `outliers.muod`. Add R cross_language per §4 where available.
    3. Add `febrero_galeano_gonzalez_2008` (DOI `10.1002/env.878`, [CITED]) → `outliers.detect_outliers_lrt`, `outliers.detect_outliers_lrt_with_dist`. Add R `fda.usc::foutliers` cross_language.
    4. Leave `outliers.depthgram` ([ASSUMED] Dai & Genton 2018 — different paper), `outliers.tvdmss` ([ASSUMED] Huang & Sun 2019), `outliers.sequential_transform_outliers` UNCURATED (curated:false family tail in 81-05) unless the executor confirms the specific root against fdars-core source — if confirmed, curate with the verified paper.

    Edit both `papers` and `callable_index` (GATE-05 A). Record the emitted coverage fraction in the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map" && python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); print('papers=%d idx=%d' % (len(d['papers']), len(d['callable_index'])))"</automated>
    <fails_when>An outlier callable does not resolve, sun_genton_2011 is duplicated, or a curated entry fails the structural gate.</fails_when>
  </verify>
  <done>Outlier family curated per §3.19 with honest curated:false on unconfirmed roots; GATE-05 green; coverage fraction grown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator → committed JSON | Sub-method attribution accuracy (dispatcher vs method, boxplot vs depth category) is correctness-critical |
| fdars-core source → attribution | Contested roots (spatial depth, epigraph) must be checked against the actual implementation, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-04 | Tampering | depth sub-method attribution | high | mitigate | Sub-method-keyed attribution; dispatcher excluded; functional_spatial curated:false until fdars-core source confirms the root; blocking human review at Phase 84 |
| T-81-05 | Spoofing | assumed DOIs (claeskens, dai_genton) | medium | mitigate | Author-verify against landing page before curated:true; curated:false on unverifiable |
| T-81-SC | Tampering | package installs | low | accept | No installs — data + test edits only |
</threat_model>

<verification>
- `pytest tests/test_guard_sync_version_independent.py -q` — Guard Group 3 green.
- `depth.functional_depth` absent from `callable_index` (verified in 81-05 sentinel wiring; must not appear here).
- Coverage fraction increased vs 81-01 baseline.
</verification>

<success_criteria>
- Depth sub-methods, functional boxplot, FPCA/PACE, KL simulation, and outlier detection curated with author-verified flags.
- Contested/unconfirmed roots (spatial depth, epigraph, depthgram, tvdmss) marked curated:false rather than force-cited.
- GATE-05 A/B/C green; coverage fraction grown.
</success_criteria>

<output>
Create `.planning/phases/81-curation-paper-registry/81-02-SUMMARY.md` when done. Record papers added, any curated:false rulings with the fdars-core source-check outcome, and the emitted N/437 fraction.
</output>

---
phase: 81-curation-paper-registry
plan: 05
type: execute
wave: 5
depends_on: [81-04]
files_modified:
  - python/fdars/_references_map.json
  - tests/test_guard_sync_version_independent.py
autonomous: true
requirements: [CURATE-04, CURATE-05]
estimate:
  tokens: 48000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The six anti-feature families (explain, spm-minus-mfpca, scoring+metrics, conformal, seasonal, depth.functional_depth dispatcher) are absent from callable_index at the module-category level — the tool returns the uncurated sentinel, not a forced citation (CURATE-04)"
    - "depth.functional_depth (dispatcher) is absent from callable_index while depth sub-methods remain curated (CURATE-04)"
    - "The final coverage fraction N/437 is recorded honestly (derived from the live _capability_map.json) and emitted by the guard test; no LLM-synthesized placeholder ships as curated (CURATE-05)"
  artifacts:
    - python/fdars/_references_map.json
    - tests/test_guard_sync_version_independent.py
  key_links:
    - "anti-feature module names ↔ absence from callable_index (verified by an assertion in the guard test)"
    - "coverage numerator ↔ curated:true entries only (curated:false does not count toward coverage)"
    - "denominator ↔ live _capability_map.json (437)"
---

<objective>
Finalize the phase: wire the six anti-feature families to the curated:false sentinel path (absence from callable_index) and lock in the honest coverage reporting for CURATE-05.

Purpose: This is the anti-scope-inflation plan. It asserts (in the guard test) that the anti-feature families are NOT force-cited, and that the coverage numerator counts only author-verified curated:true entries — so the reported N/437 is truthful.
Output: An anti-feature-absence guard assertion; the final coverage fraction recorded; GATE-05 green; the phase closes at the honest ~32% coverage.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-curation-paper-registry/81-CONTEXT.md
@.planning/phases/81-curation-paper-registry/81-RESEARCH.md
@docs/authoring/references-schema.md
@python/fdars/_references_map.json
@python/fdars/_capability_map.json
@tests/test_guard_sync_version_independent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the six anti-feature families to the curated:false sentinel path + guard the absence</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §2 (Anti-Feature Family Map — the six families, exact modules, and the treatment: absent from callable_index at module-category level; spm.mfpca is the sole spm exception) and Pitfall 3 (dispatcher vs sub-methods) and Pitfall 7 (spm.mfpca IS curated)
    - .planning/phases/81-curation-paper-registry/81-CONTEXT.md (six anti-feature families listed; per-sub-method depth papers stay curated)
    - docs/authoring/references-schema.md ("The curated:false Tail Convention" — absence is the sentinel; a curated:false entry only needed if a callable must appear in callable_index for completeness)
    - python/fdars/_capability_map.json (modules `explain` 46, `spm` 23, `metrics` 5, `scoring` 5, `conformal` 7, `seasonal` 18, `depth.functional_depth`)
    - tests/test_guard_sync_version_independent.py (Guard Group 3 — where to add the anti-feature-absence assertion)
  </read_first>
  <action>
    Per 81-RESEARCH §2, wire the six anti-feature families to the sentinel path. The sentinel path is ABSENCE from `callable_index` — the Phase-82 MCP tool returns `{"curated": false, "sentinel": "NO_CURATED_ENTRY"}` for any callable not in the index. So the correct treatment is: do NOT add these callables to `callable_index` at all (no forced module-level citation, no placeholder paper).

    1. Confirm (and, if any slipped in during 81-01..81-04, REMOVE) that NONE of the following appear as keys in `callable_index`:
       - every `explain.*` callable (46)
       - every `spm.*` callable EXCEPT `spm.mfpca` (which IS curated — Happ & Greven 2018, added in 81-04)
       - every `metrics.*` and `scoring.*` callable (10 total)
       - every `conformal.*` callable (7)
       - every `seasonal.*` callable (18)
       - `depth.functional_depth` (the category dispatcher — its sub-methods FM/band/MBD/modal/random-projection remain curated)
    2. Add a NEW guard assertion (extend an existing Group-3 test or add `test_references_map_anti_feature_families_absent`) that loads `_references_map.json` and asserts: for each anti-feature module in a hardcoded set `{"explain","metrics","scoring","conformal","seasonal"}`, NO `callable_index` key starts with `"<module>."`; for `spm`, the ONLY allowed `spm.*` key is `spm.mfpca`; and `"depth.functional_depth"` is NOT a key. On failure the message must name the offending key and say "anti-feature families must be absent from callable_index (curated:false sentinel path) — see CURATE-04 / 81-RESEARCH §2".
    3. Do NOT create a curated:false placeholder paper spanning these families — absence is the sentinel, and a placeholder would inflate the paper count without provenance. (The schema's `_uncurated_batch` convention is available only if a callable MUST appear in the index for a downstream reason; none applies here.)

    Edit both `papers` and `callable_index` if any removal is needed (GATE-05 A).
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map or anti_feature" && python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); keys=set(d['callable_index']); bad=[k for k in keys if k.split('.',1)[0] in {'explain','metrics','scoring','conformal','seasonal'} or k=='depth.functional_depth' or (k.startswith('spm.') and k!='spm.mfpca')]; assert not bad, bad; print('anti-feature families absent OK; spm.mfpca present:', 'spm.mfpca' in keys)"</automated>
    <fails_when>Any anti-feature-family callable appears in callable_index, depth.functional_depth is a key, an spm.* key other than spm.mfpca is present, or a placeholder paper was created.</fails_when>
  </verify>
  <done>The six anti-feature families are absent from callable_index (sentinel path); spm.mfpca is the sole curated spm callable; a guard assertion enforces the absence; GATE-05 green.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize CURATE-05 honest coverage reporting (N/437, curated:true only)</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §1 (denominator 437, derived from _capability_map.json; 409 was the stale estimate) and §5 (coverage estimate ~139/437 ≈ 32%; curated:false and anti-features do NOT count toward coverage)
    - .planning/phases/81-curation-paper-registry/81-CONTEXT.md (coverage partial-but-honest; only author-verified entries carry provenance; no LLM-synthesized placeholder ships as curated; coverage floor deferred to REF-FUT-01)
    - tests/test_guard_sync_version_independent.py (the `test_references_map_coverage_fraction` added in 81-01 — finalize its numerator semantics here)
    - docs/authoring/references-schema.md ("What the Guards Enforce" — the coverage emission is informational, not a hard floor this milestone)
  </read_first>
  <action>
    Finalize the coverage-emission mechanism established in 81-01 so it reports the phase's honest final figure per CURATE-05.

    1. Confirm the numerator in `test_references_map_coverage_fraction` counts ONLY callables whose `callable_index` value includes at least one paper_key with `papers[pk]["curated"] is True` (a curated:false-only callable does NOT count toward coverage; anti-feature families are absent so they cannot count). If the 81-01 numerator logic already matches, leave it; otherwise tighten it to this rule and add an inline comment tying it to CURATE-05.
    2. Ensure the denominator remains `sum(len(v) for v in cap_data.values())` computed from the live `_capability_map.json` (437) — never hardcoded, and update the authoring-time drift-tripwire constant if the capability map legitimately grew (message must instruct the executor accordingly).
    3. Add a lightweight in-JSON coverage-metadata note per Claude's Discretion (CONTEXT): a top-level `"_meta"` object is NOT part of the locked two-key schema, so instead record the achieved fraction and the "no LLM-synthesized placeholder ships as curated" honesty statement in the 81-05 SUMMARY and rely on the guard test as the machine-readable emitter. Do NOT add a third top-level key to `_references_map.json` (it would break GATE-05 A's papers/callable_index-only shape expectations) unless the guard tests explicitly tolerate it — verify first; if unsupported, keep coverage purely test-emitted.
    4. Run the coverage test with `-s` and record the emitted `COVERAGE: N/437` line in the SUMMARY as the phase's honest final figure (expected ~139/437 ≈ 32%, but report the ACTUAL number after author-verification downgrades some [ASSUMED] entries to curated:false).
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -s -q -k "coverage_fraction" && pytest tests/test_guard_sync_version_independent.py -q</automated>
    <fails_when>The coverage numerator counts curated:false or anti-feature callables, the denominator is hardcoded instead of derived from _capability_map.json, or any GATE-05 test regresses.</fails_when>
  </verify>
  <done>Coverage reported honestly as N/437 (curated:true only, derived denominator); the emitted fraction is recorded in the SUMMARY; only author-verified entries carry provenance; full Guard Group 3 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator → coverage claim | Overstating coverage (counting curated:false as covered) misrepresents the honesty guarantee |
| anti-feature family → callable_index | A forced citation on an anti-feature family violates the LLM-free sentinel contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-12 | Information Disclosure | overstated coverage | high | mitigate | Numerator counts curated:true only; denominator derived from live capability map; guard test emits the honest N/437 |
| T-81-13 | Tampering | anti-feature forced citation | high | mitigate | Guard assertion enforces absence of the six families from callable_index (spm.mfpca sole exception) |
| T-81-14 | Repudiation | LLM-synthesized placeholder as curated | high | mitigate | No placeholder paper created; only author-verified entries carry provenance; blocking human review at Phase 84 |
| T-81-SC | Tampering | package installs | low | accept | No installs — data + test edits only |
</threat_model>

<verification>
- `pytest tests/test_guard_sync_version_independent.py -q` — full Guard Group 3 (A/B/C + coverage + anti-feature-absence) green.
- `pytest tests/test_guard_sync_version_independent.py -s -k coverage_fraction` — prints the honest final `COVERAGE: N/437`.
- No anti-feature-family callable (except spm.mfpca) in callable_index.
- No placeholder paper inflating the paper count.
</verification>

<success_criteria>
- The six anti-feature families are wired to the curated:false sentinel path (absent from callable_index; spm.mfpca the sole exception), enforced by a guard assertion.
- `depth.functional_depth` dispatcher absent while its sub-methods stay curated.
- Coverage reported honestly as N/437 (curated:true only, derived denominator), emitted by the guard test and recorded in the SUMMARY.
- No LLM-synthesized placeholder ships as curated.
- GATE-05 A/B/C green.
</success_criteria>

<output>
Create `.planning/phases/81-curation-paper-registry/81-05-SUMMARY.md` when done. Record: the final emitted N/437 coverage fraction, confirmation the six anti-feature families are absent, and a list of any [ASSUMED] entries downgraded to curated:false during author-verification across the phase.
</output>
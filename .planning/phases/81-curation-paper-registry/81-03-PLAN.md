---
phase: 81-curation-paper-registry
plan: 03
type: execute
wave: 3
depends_on: [81-02]
files_modified:
  - python/fdars/_references_map.json
  - tests/test_guard_sync_version_independent.py
autonomous: true
requirements: [CURATE-01, CURATE-02, CURATE-03]
estimate:
  tokens: 62000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Scalar-on-function/FLM, generalized FLM, nonparametric/PLS regression, Fréchet regression, and density/LQD FDA callables are curated per their specific paper roots (CURATE-01, CURATE-02)"
    - "Inference callables (ITP, SCB, permutation ANOVA, FLM F-test) map to their specific papers; the contested oneway_anova_vstat is flagged in-JSON with co-primary candidates, never force-picked (CURATE-02)"
    - "Robust regression and FoF/FoS callables without a clean single root are marked curated:false, not force-cited (CURATE-02)"
    - "cross_language pointers populated (fda.usc, refund, fdatest) with confidence flags (CURATE-03)"
  artifacts:
    - python/fdars/_references_map.json
    - tests/test_guard_sync_version_independent.py
  key_links:
    - "oneway_anova_vstat contested-flag ↔ fdars-core source V-statistic formula (81-RESEARCH §3.15)"
    - "callable_index ↔ papers[*].callables ↔ _capability_map.json (GATE-05 A/B)"
    - "inference.flm_f_test root verified vs fdars-core source (Shen-Faraway vs Müller-Stadtmüller)"
---

<objective>
Curate the regression, Fréchet, density/LQD, and inference families — the families with the most contested attributions and the most curated:false rulings.

Purpose: This plan carries the contested-attribution discipline: `oneway_anova_vstat` is genuinely split (Cuesta-Albertos & Febrero-Bande 2010 vs Górecki & Smaga 2015) and MUST be flagged in-JSON, not force-picked. Robust/FoF regression callables with no clean root become curated:false.
Output: `_references_map.json` grown with regression/FLM/Fréchet/density/inference families; contested cases flagged; GATE-05 green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-curation-paper-registry/81-CONTEXT.md
@.planning/phases/81-curation-paper-registry/81-RESEARCH.md
@docs/authoring/references-schema.md
@python/fdars/_references_map.json
@python/fdars/_capability_map.json
@tests/test_guard_sync_version_independent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate scalar-on-function / FLM / generalized-FLM / nonparametric / PLS regression</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.6 (regression: ramsay_dalzell_1991 extend to fregre_lm/predict/cv; muller_stadtmuller_2005 GLM [VERIFIED]; ferraty_vieu_2006 nonparametric [CITED]; preda_saporta_2005 PLS [ASSUMED]; robust + FoF + FoS rulings)
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §4 (cross_language: muller_stadtmuller fda.usc fregre.glm)
    - python/fdars/_capability_map.json (module `regression` — 29 callables; confirm fregre_lm/predict_fregre_lm/fregre_cv, functional_logistic/functional_glm/predict_functional_logistic, fregre_np/fregre_np_cv/fregre_np_mixed, fpls/fregre_pls/predict_fregre_pls, concurrent_regression, fregre_l1/fregre_huber/predict_fregre_robust, fof_*, fosr*)
    - python/fdars/_references_map.json (ramsay_dalzell_1991 seeded for _Fdata.to_pc; ferraty_vieu_2006 not yet present)
  </read_first>
  <action>
    Per 81-RESEARCH §3.6, curate the regression family. AUTHOR-VERIFY before curated:true; curated:false on unverifiable.

    1. EXTEND `ramsay_dalzell_1991` to add `regression.fregre_lm`, `regression.predict_fregre_lm`, `regression.fregre_cv` (scalar-on-function FLM). Verify the seeded DOI still resolves.
    2. Add `muller_stadtmuller_2005` (DOI `10.1214/009053604000001156`, [VERIFIED]) → `regression.functional_logistic`, `regression.functional_glm`, `regression.predict_functional_logistic`. Add R `fda.usc::fregre.glm` cross_language.
    3. Add `ferraty_vieu_2006` (book, DOI `10.1007/0-387-36620-2`, [CITED]) → `regression.fregre_np`, `regression.fregre_np_cv`, `regression.fregre_np_mixed`.
    4. Add `preda_saporta_2005` (DOI `10.1016/j.csda.2004.07.002`, [ASSUMED] A6) → `regression.fpls`, `regression.fregre_pls`, `regression.predict_fregre_pls`. Verify; curated:false if unconfirmed. (Note: `fts.fplsr` is wired in 81-04 to the same key.)
    5. EXTEND `ramsay_silverman_2005` (from 81-01) to add `regression.concurrent_regression`, `regression.fosr`, `regression.fosr_fpc`, `regression.predict_fosr`.
    6. Mark curated:false (do NOT force-cite): `regression.fregre_l1`, `regression.fregre_huber`, `regression.predict_fregre_robust` (§3.6 robust — no clean FDA root); `regression.fof_regression`, `regression.fof_cv`, `regression.predict_fof` (§3.6 FoF — [ASSUMED] Yao-Müller 2010, verify vs fdars-core source; if confirmed add `yao_muller_2010` DOI `10.1093/biomet/asq044`, else curated:false). These curated:false callables are carried into the 81-05 family tail unless curated here.

    Edit both `papers` and `callable_index` (GATE-05 A).
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map"</automated>
    <fails_when>A regression callable does not resolve in _capability_map.json, a seed entry is duplicated, or a curated DOI/URL fails the structural gate.</fails_when>
  </verify>
  <done>Regression family curated per §3.6; robust/FoF marked curated:false or verified-and-curated; GATE-05 green.</done>
</task>

<task type="auto">
  <name>Task 2: Curate Fréchet regression + density/LQD FDA</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.7 (petersen_muller_2019 Fréchet global+local+mean+anova [VERIFIED]) and §3.8 (petersen_muller_2016 LQD [ASSUMED]; agueh_carlier_2011 Wasserstein barycenter [ASSUMED])
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §4 (petersen_muller_2019 — no direct R/Python impl found; record honest gaps)
    - python/fdars/_capability_map.json (modules `frechet`, `density_fda` — confirm frechet_global_reg/local_reg/mean/anova, lqd_transform/inverse_lqd/lqd_fpca/normalize_density/wasserstein_barycenter)
  </read_first>
  <action>
    Per 81-RESEARCH §3.7–3.8, curate Fréchet + density families. AUTHOR-VERIFY before curated:true.

    1. Add `petersen_muller_2019` (DOI `10.1214/17-AOS1624`, [VERIFIED]) → `frechet.frechet_global_reg`, `frechet.frechet_local_reg`, `frechet.frechet_mean`, `frechet.frechet_anova`. Per §4 no direct R/Python implementation was found — record honest gaps by simply omitting the cross_language keys (do NOT create empty-string placeholders, per schema).
    2. Add `petersen_muller_2016` (DOI `10.1214/15-AOS1363`, [ASSUMED] A5) → `density_fda.lqd_transform`, `density_fda.inverse_lqd`, `density_fda.lqd_fpca`, `density_fda.normalize_density`. Verify the AoS landing page; curated:false if the DOI suffix cannot be confirmed.
    3. Add `agueh_carlier_2011` (DOI `10.1137/100805741`, [ASSUMED]) → `density_fda.wasserstein_barycenter`. The `epubs.siam.org` domain is allowlisted (81-01). Verify; curated:false if unconfirmed.

    Edit both `papers` and `callable_index` (GATE-05 A).
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map"</automated>
    <fails_when>A frechet/density callable does not resolve, an empty-string cross_language placeholder was created, or a curated DOI/URL fails the structural gate.</fails_when>
  </verify>
  <done>Fréchet + density/LQD families curated per §3.7–3.8; honest cross_language gaps recorded by omission; GATE-05 green.</done>
</task>

<task type="auto">
  <name>Task 3: Curate inference (ITP, SCB, permutation ANOVA, FLM F-test) + flag contested oneway_anova_vstat</name>
  <files>python/fdars/_references_map.json, tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.14 (inference: pini_vantini_2016 ITP [CITED]; degras_2011 SCB [CITED]; shen_faraway_2004 FLM F-test [CITED], verify vs fdars-core; cuevas_febrero_fraiman_2004 permutation ANOVA [CITED])
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §3.15 (CONTESTED oneway_anova_vstat — Cuesta-Albertos & Febrero-Bande 2010 vs Górecki & Smaga 2015; flag in-JSON, curated:false pending fdars-core source check; verbatim `notes` string provided)
    - .planning/phases/81-curation-paper-registry/81-RESEARCH.md §4 (cross_language: pini fdatest ITP1bspline; cuevas fda.usc fanova.onefactor; degras — no CRAN package found)
    - python/fdars/_capability_map.json (modules `inference`, `tolerance` — confirm itp_flm/itp_one_pop/itp_two_pop, mean_scb/scb_two_sample_test, tolerance.scb_mean_degras, flm_f_test/flm_gof_test, f_perm_test/t_perm_test/two_sample_mean_test, oneway_anova_vstat)
    - docs/authoring/references-schema.md ("Contested or multi-primary attributions" — list co-primaries or flag in notes, defer definitive call to Phase 84)
  </read_first>
  <action>
    Per 81-RESEARCH §3.14–3.15, curate the inference family. AUTHOR-VERIFY before curated:true.

    1. Add `pini_vantini_2016` (DOI `10.1111/biom.12476`, [CITED]) → `inference.itp_flm`, `inference.itp_one_pop`, `inference.itp_two_pop`. Add R `fdatest` cross_language (§4). Note the year is 2016 (Biometrics), not the 2017 companion.
    2. Add `degras_2011` (DOI `10.5705/ss.2009.207`, [CITED]) → `inference.mean_scb`, `inference.scb_two_sample_test`, `tolerance.scb_mean_degras`. URL on `www3.stat.sinica.edu.tw` (allowlisted 81-01). Per §4 no standalone CRAN package found — omit R cross_language.
    3. Add `cuevas_febrero_fraiman_2004` (DOI `10.1016/j.csda.2003.10.021`, [CITED]) → `inference.f_perm_test`, `inference.t_perm_test`, `inference.two_sample_mean_test`. Add R `fda.usc::fanova.onefactor` cross_language.
    4. `inference.flm_f_test`, `inference.flm_gof_test`: per §3.14 the candidate root is `shen_faraway_2004` (Statistica Sinica, no DOI → `type:"journal"`, omit `doi`, url on `www3.stat.sinica.edu.tw`) BUT verify against fdars-core source — the alternative is Müller-Stadtmüller 2005. If fdars-core source is ambiguous, list BOTH as co-primaries in the callable_index value array and explain in `notes`; if unconfirmed, curated:false.
    5. CONTESTED `inference.oneway_anova_vstat`: check fdars-core source for the specific V-statistic formula. Add `cuesta_albertos_febrero_2010` (DOI `10.1007/s11749-010-0185-3`, [CITED]) AND reference Górecki & Smaga 2015 in `notes` using the verbatim contested-note string from §3.15. Wire `curated:false` pending the source check UNLESS fdars-core source unambiguously identifies one primary — then curate that one with the other listed as co-primary. NEVER force-pick silently.

    Edit both `papers` and `callable_index` (GATE-05 A). Record the emitted coverage fraction in the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/test_guard_sync_version_independent.py -q -k "references_map" && python -c "import json,importlib.resources as r; d=json.loads((r.files('fdars')/'_references_map.json').read_text()); pks=d['callable_index'].get('inference.oneway_anova_vstat',[]); assert not pks or any('contest' in d['papers'][pk].get('notes','').lower() for pk in pks), 'contested case must carry a contested note'; print('OK papers=%d idx=%d' % (len(d['papers']), len(d['callable_index'])))"</automated>
    <fails_when>oneway_anova_vstat is force-picked without a contested note, an inference callable does not resolve, or a curated entry fails the structural gate.</fails_when>
  </verify>
  <done>Inference family curated per §3.14; oneway_anova_vstat and flm_f_test resolved against fdars-core source (co-primary or contested-flagged, never force-picked); GATE-05 green; coverage fraction grown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator → committed JSON | Contested attributions (ANOVA V-stat, FLM F-test) are the highest correctness risk in the phase |
| fdars-core source → attribution | The tie-breaker for contested cases is the actual implementation, not the literature at large |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-06 | Tampering | contested oneway_anova_vstat attribution | high | mitigate | Flag in-JSON with co-primary candidates + verbatim contested note; curated:false pending fdars-core source; never force-picked; blocking human review at Phase 84 |
| T-81-07 | Tampering | flm_f_test root ambiguity | medium | mitigate | Verify vs fdars-core source; co-primary or curated:false if ambiguous |
| T-81-08 | Spoofing | assumed DOIs (preda_saporta, petersen_muller_2016, agueh_carlier) | medium | mitigate | Author-verify landing page before curated:true; curated:false on unverifiable |
| T-81-SC | Tampering | package installs | low | accept | No installs — data + test edits only |
</threat_model>

<verification>
- `pytest tests/test_guard_sync_version_independent.py -q` — Guard Group 3 green.
- `oneway_anova_vstat` (if in callable_index) carries a contested note referencing both candidate papers.
- Coverage fraction increased vs 81-02 baseline.
</verification>

<success_criteria>
- Regression, Fréchet, density/LQD, and inference families curated with author-verified flags.
- Contested `oneway_anova_vstat` flagged in-JSON (co-primary or curated:false), never force-picked; `flm_f_test` root resolved vs fdars-core source.
- Robust/FoF regression without a clean root marked curated:false.
- GATE-05 A/B/C green; coverage fraction grown.
</success_criteria>

<output>
Create `.planning/phases/81-curation-paper-registry/81-03-SUMMARY.md` when done. Record the contested-case resolutions (fdars-core source-check outcome for oneway_anova_vstat and flm_f_test), any curated:false rulings, and the emitted N/437 fraction.
</output>
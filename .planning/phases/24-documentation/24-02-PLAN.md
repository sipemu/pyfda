---
phase: 24-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/advisor/aspects.md
autonomous: true
requirements: [DOCS-02]
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "docs/advisor/aspects.md documents all 12 aspects (clustering, smoothing, alignment, basis, fpca, represent, depth, outliers, classification, regression, regression_cv, spm) with their build_diagnostics keys and the 3 task families (interpretation / parameter / method) (per DOCS-02)."
    - "Every documented diagnostics-key set is derived from the actual aspects/*.py builder, not from FEATURES.md (which had 8 discrepancies vs the real bindings) (per DOCS-02)."
    - "aspects.md contains at least one EXECUTED OFFLINE fence (python exec=1 html=1 source=above) that calls build_diagnostics(...) and prints FDARS_FENCE_OK, key-free and offline — never calling advise() (per DOCS-02)."
  artifacts:
    - "docs/advisor/aspects.md"
  key_links:
    - "Each aspect's key list in aspects.md matches the keys emitted by python/fdars/advisor/aspects/<aspect>.py."
    - "The depth executed fence passes method_name= and a depth score array (fdars.depth returns arrays, not dicts); the fpca fence passes an fpca result dict."
---

<objective>
Create `docs/advisor/aspects.md` — the per-aspect advisor reference — documenting,
for every one of the 12 fdars aspects, the diagnostics `build_diagnostics`
emits and the three grounded task families, plus a coverage table and one or
two executed OFFLINE fences that run against the real shipped builders. Satisfies
DOCS-02.

Purpose: Users need to know exactly which diagnostics each aspect produces and
what kinds of grounded advice (interpretation / parameter / method) they can
request — accurate to the shipped code.
Output: A new `docs/advisor/aspects.md` page whose diagnostics tables are derived
from the actual `aspects/*.py` builders, with executed offline `build_diagnostics`
fences for depth and fpca (representative aspects) emitting `FDARS_FENCE_OK`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-documentation/24-CONTEXT.md

# SOURCE OF TRUTH for the diagnostics keys — read each builder and transcribe its emitted keys:
@python/fdars/advisor/aspects/clustering.py
@python/fdars/advisor/aspects/smoothing.py
@python/fdars/advisor/aspects/alignment.py
@python/fdars/advisor/aspects/basis.py
@python/fdars/advisor/aspects/fpca.py
@python/fdars/advisor/aspects/represent.py
@python/fdars/advisor/aspects/depth.py
@python/fdars/advisor/aspects/outliers.py
@python/fdars/advisor/aspects/classification.py
@python/fdars/advisor/aspects/regression.py
@python/fdars/advisor/aspects/regression_cv.py
@python/fdars/advisor/aspects/spm.py
# advise() aspect param + task families; build_diagnostics dispatch + n_classes:
@python/fdars/advisor/__init__.py
# Executed-fence convention to mirror exactly (exec=1 html=1 source=above + FDARS_FENCE_OK):
@docs/advisor/python-api.md
# Offline dataset loaders available to the executed fences:
@scripts/docs_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author aspects.md skeleton + coverage table + per-aspect key sections (from the builders)</name>
  <files>docs/advisor/aspects.md</files>
  <action>
    Create `docs/advisor/aspects.md` titled "Per-Aspect Coverage"
    (H1 `# Per-Aspect Coverage`), following existing advisor-page tone and
    STYLE_SPEC. Per DOCS-02 it must cover ALL 12 aspects. Structure:

    1. Intro: every fdars analysis aspect gets the same two-stage treatment as
       clustering — an offline deterministic `build_diagnostics(result, method=…)`
       report plus grounded `advise(…, aspect=…)` task families. The aspect is
       ALWAYS caller-specified (never auto-detected from result keys). The three
       task families for every aspect are `"interpretation"`, `"parameter"`, and
       `"method"` (the `task=` argument to `advise`).

    2. A coverage table with one row per aspect: columns
       Aspect (method=) | fdars source(s) | Key diagnostics | Offline fence —
       listing all 12 method strings: clustering, smoothing, alignment, basis,
       fpca, represent, depth, outliers, classification, regression,
       regression_cv, spm. Mark depth and fpca as having an executed offline
       fence on this page (Task 2/3); the rest are documented by key table only.

    3. One second-level subsection per aspect. For EACH aspect, transcribe the
       emitted diagnostics keys DIRECTLY from its builder file (read it — do NOT
       use FEATURES.md, which had 8 discrepancies). Use these authoritative key
       sets (each dict also always carries a constant `method` key):

       - clustering: k, cluster_means, cluster_sizes,
         pairwise_amplitude_distance, pairwise_phase_distance,
         mean_amplitude_separation, mean_phase_separation
       - smoothing: lambda_values, gcv_curve, edf, gcv_aic_approx,
         gcv_bic_approx, optimal_lambda, optimal_gcv, optimal_edf
         (note the single-fit pspline_fit_gcv branch fills optimal_gcv/optimal_edf
         and leaves the curve keys None)
       - alignment: mean_length, mean_min, mean_max, mean_avg, mean_curve,
         n_obs, amplitude_distances, phase_distances, amplitude_mean,
         amplitude_max, phase_mean, phase_max, converged, n_iter
       - basis: n_basis_values, gcv_curve, edf, gcv_aic_approx, gcv_bic_approx,
         optimal_n_basis, optimal_gcv, optimal_edf
       - fpca: n_components, n_obs, eigenvalues, explained_variance_ratio,
         cumulative_variance_explained, total_variance,
         phase_leakage_indicator, phase_leakage_flagged
       - represent: n_obs, n_points, argvals_min, argvals_max,
         argvals_spacing_mean, argvals_spacing_std, is_uniform_grid,
         data_range_min, data_range_max, data_range_mean
       - depth: method_name, n_obs, depth_min, depth_max, depth_mean,
         depth_median, depth_q10, depth_q90, depth_histogram
       - outliers: n_obs, n_outliers, outlier_fraction, threshold,
         has_magnitude_shape, magnitude_range, shape_range, has_outliergram,
         mei_range, mbd_range
       - classification: n_obs, n_classes, accuracy, error_rate, cv_error_rate,
         fold_error_std, best_ncomp (note: n_classes is a caller-supplied
         build_diagnostics(..., n_classes=K) arg, not inferrable from the result)
       - regression: n_obs, r_squared, residual_mean, residual_std,
         residual_max_abs, residual_skew, beta_t_range, has_fosr
       - regression_cv: optimal_k, min_cv_error, cv_curve, k_values,
         cv_curve_range, elbow_present
       - spm: n_obs, ncomp, t2_limit, spe_limit, t2_max, t2_mean,
         t2_exceedance_rate, spe_max, spe_mean, spe_exceedance_rate,
         eigenvalues, variance_explained_cumulative, spe_kurtosis_excess,
         spe_moment_match_adequate

       For each aspect, present the keys as a small table or definition list with
       a one-line meaning per key, and note the fdars source function(s) that
       produce the input result (e.g. depth ← fdars.depth.* score arrays;
       spm ← fdars.spm.spm_phase1; regression ← fregre_lm/pls/l1/huber/np/fosr;
       regression_cv ← fregre_cv / model_selection_ncomp; clustering ← kmeans_fd;
       fpca ← fdars.regression.fpca; represent ← raw data + argvals). Before
       writing each key table, open the corresponding aspects/<aspect>.py and
       confirm the key list matches the builder exactly.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && test -f docs/advisor/aspects.md && for m in clustering smoothing alignment basis fpca represent depth outliers classification regression regression_cv spm; do grep -q "$m" docs/advisor/aspects.md || { echo "MISSING aspect: $m"; exit 1; }; done && grep -q "phase_leakage_indicator" docs/advisor/aspects.md && grep -q "spe_moment_match_adequate" docs/advisor/aspects.md && grep -q "cv_error_rate" docs/advisor/aspects.md && echo OK</automated>
  </verify>
  <done>aspects.md exists, has a subsection for each of the 12 aspects with a key table transcribed from the builder, documents the 3 task families, and includes the coverage table. Distinctive keys (phase_leakage_indicator, spe_moment_match_adequate, cv_error_rate) are present, confirming key sets came from the real builders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add executed OFFLINE depth fence (build_diagnostics on a depth score array)</name>
  <files>docs/advisor/aspects.md</files>
  <behavior>
    - The depth fence runs in the docs build (exec="1") with no API key and no network.
    - It loads a bundled dataset via docs_data, computes a depth score ARRAY (fdars.depth.* returns an ndarray, not a dict), and calls build_diagnostics(scores, method="depth", method_name="fraiman_muniz").
    - It prints at least one diagnostic value plus the sentinel FDARS_FENCE_OK.
  </behavior>
  <action>
    In the depth subsection of aspects.md, add an EXECUTED offline fence using the
    exact opening ```python exec="1" html="1" source="above``` convention from
    python-api.md. The fence MUST (verified to run offline in planning):
      - `from docs_data import load_canadian_weather`
      - `from fdars import depth`
      - `from fdars.advisor import build_diagnostics`
      - load `day, X, meta = load_canadian_weather("temperature")`
      - `scores = depth.fraiman_muniz_1d(X, X)` (returns an ndarray)
      - `diag = build_diagnostics(scores, method="depth", method_name="fraiman_muniz")`
      - print `diag["n_obs"]`, `diag["depth_mean"]`, and end a printed line with
        the sentinel token FDARS_FENCE_OK.
    Do NOT call advise() and do NOT reference any API key — this fence is offline
    and key-free. Precede it with one sentence noting it runs live in the build.
    (CRITICAL: depth builder consumes the raw score array directly and requires
    the method_name kwarg — passing a dict or omitting method_name is wrong.)
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && python - <<'PY'
import re, io
src = open("docs/advisor/aspects.md").read()
# find fenced exec blocks
blocks = re.findall(r'```python exec="1"[^\n]*\n(.*?)```', src, re.DOTALL)
depth = [b for b in blocks if 'method="depth"' in b]
assert depth, "no executed depth fence found"
assert 'FDARS_FENCE_OK' in depth[0], "depth fence missing sentinel"
assert 'advise(' not in depth[0], "depth fence must not call advise()"
print("depth fence OK")
PY</automated>
  </verify>
  <done>aspects.md has an executed offline depth fence that calls build_diagnostics(scores, method="depth", method_name=...) and prints FDARS_FENCE_OK, with no advise() call and no API key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add executed OFFLINE fpca fence + final offline strict-fence check</name>
  <files>docs/advisor/aspects.md</files>
  <behavior>
    - The fpca fence runs in the docs build (exec="1") offline, key-free.
    - It computes an fpca result dict and calls build_diagnostics(result, method="fpca").
    - It prints a diagnostic value plus FDARS_FENCE_OK; it never calls advise().
  </behavior>
  <action>
    In the fpca subsection of aspects.md, add a second EXECUTED offline fence
    (same ```python exec="1" html="1" source="above``` opener). The fence MUST
    (verified to run offline in planning):
      - `from docs_data import load_canadian_weather`
      - `from fdars import regression`
      - `from fdars.advisor import build_diagnostics`
      - `day, X, meta = load_canadian_weather("temperature")`
      - `fp = regression.fpca(X, day, n_comp=4)`
      - `diag = build_diagnostics(fp, method="fpca")`
      - print `diag["n_components"]`, `diag["cumulative_variance_explained"][0]`,
        and a line ending with the sentinel FDARS_FENCE_OK.
    No advise() call, no API key. This proves a second representative aspect runs
    against the real shipped builder.

    Then confirm every executed fence on the page runs offline by rendering the
    page's fences with the project venv (the same mechanism the strict build
    uses). All executed fences must emit FDARS_FENCE_OK with no network/key.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts .venv/bin/python - <<'PY'
import re
src = open("docs/advisor/aspects.md").read()
blocks = re.findall(r'```python exec="1"[^\n]*\n(.*?)```', src, re.DOTALL)
assert any('method="fpca"' in b for b in blocks), "no executed fpca fence"
import sys; sys.path.insert(0, "scripts")
ok = 0
for b in blocks:
    assert 'advise(' not in b, "an executed fence calls advise() — forbidden"
    ns = {}
    exec(b, ns)  # must run offline, key-free
    ok += 1
assert ok >= 2, f"expected >=2 executed offline fences, ran {ok}"
print(f"{ok} offline fences ran; FDARS_FENCE_OK")
PY</automated>
  </verify>
  <done>aspects.md has an executed offline fpca fence; every executed fence on the page runs offline and key-free, calls only build_diagnostics (never advise()), and emits FDARS_FENCE_OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → executed fences | Only build_diagnostics fences execute; they must stay offline/key-free (no advise(), no network). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03 | Tampering | per-aspect key tables drift from real builders | medium | mitigate | Key sets transcribed directly from aspects/*.py (verified), never from FEATURES.md; distinctive-key greps in verify guard against drift. |
| T-24-04 | Denial of Service | executed fence needs a key/network and breaks the build | high | mitigate | Fences call only build_diagnostics on bundled datasets; verify runs each exec block offline and asserts no advise() call. |

No npm/pip/cargo install tasks — package-legitimacy gate not applicable.
</threat_model>

<verification>
- aspects.md documents all 12 aspects with builder-accurate key tables + 3 task families.
- Coverage table present; depth and fpca marked as executed-fence aspects.
- Executed depth and fpca fences run offline (key-free), call only build_diagnostics, emit FDARS_FENCE_OK.
</verification>

<success_criteria>
DOCS-02 satisfied: per-aspect page documents diagnostics + task families for
every fdars aspect, with builder-derived key sets and executed offline fences
for representative aspects.
</success_criteria>

<output>
Create `.planning/phases/24-documentation/24-02-SUMMARY.md` when done.
</output>
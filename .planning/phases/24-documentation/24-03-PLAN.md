---
phase: 24-documentation
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - docs/advisor/index.md
  - docs/advisor/python-api.md
  - mkdocs.yml
autonomous: true
requirements: [DOCS-03]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "docs/advisor/index.md reflects provider-agnostic operation (all four backends, not Anthropic-only) and full-library coverage (all 12 aspects, not just clustering/smoothing/FPCA/alignment/basis) (per DOCS-03)."
    - "docs/advisor/python-api.md documents the provider, model, and aspect params on advise() and lists the full 12-aspect method set for build_diagnostics (per DOCS-03)."
    - "mkdocs.yml AI Advisor nav includes the new Provider Setup and Per-Aspect Coverage pages (per DOCS-03)."
    - "PYTHONPATH=scripts .venv/bin/mkdocs build --strict exits 0 offline with all executed fences emitting FDARS_FENCE_OK (per DOCS-03)."
  artifacts:
    - "docs/advisor/index.md"
    - "docs/advisor/python-api.md"
    - "mkdocs.yml"
  key_links:
    - "New nav entries point at advisor/providers.md and advisor/aspects.md, which exist from plans 24-01/24-02."
    - "The strict build renders every executed fence across the whole advisor section with no network/key."
---

<objective>
Update the AI Advisor overview and Python API pages for provider-agnostic
operation and full-library coverage, wire the two new pages (Provider Setup,
Per-Aspect Coverage) into the mkdocs nav, and prove the whole docs site builds
offline under `mkdocs build --strict`. Satisfies DOCS-03 and closes Phase 24
(the final phase of v3.0).

Purpose: The published overview and API pages currently describe an
Anthropic-only, five-method advisor; they must reflect the shipped
provider-agnostic, 12-aspect reality, and the new pages must be reachable and
build-clean.
Output: Updated `docs/advisor/index.md` and `docs/advisor/python-api.md`,
updated `mkdocs.yml` nav, and a green strict offline build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-documentation/24-CONTEXT.md

# Pages to edit (read current content before editing):
@docs/advisor/index.md
@docs/advisor/python-api.md
# advise() provider/model/aspect params + build_diagnostics 12-method set:
@python/fdars/advisor/__init__.py
# Env-var / provider wording to cross-link to (from plan 24-01):
@.claude/skills/fdars-advisor/SKILL.md
# New sibling pages produced by 24-01 and 24-02 that this plan links + navs:
@docs/advisor/providers.md
@docs/advisor/aspects.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.md — provider-agnostic + full-library coverage</name>
  <files>docs/advisor/index.md</files>
  <action>
    Edit `docs/advisor/index.md` (scoped edits, keep the existing structure and
    the two SVG diagram references intact). Per DOCS-03, make two corrections:

    1. Provider-agnostic framing. The current text implies an Anthropic-only
       advisor ("passes those diagnostics to Claude", "requires ANTHROPIC_API_KEY").
       Reword the "What the Advisor Does" / "Installation" / offline-vs-env-gated
       sections so they describe advise() routing through a uniform Provider
       protocol backed by any of Anthropic (default), OpenAI/OpenAI-compatible,
       Google Gemini, or local Ollama. Keep the grounding invariant text
       unchanged (it is provider-independent). Add a one-line pointer to the new
       Provider Setup page (advisor/providers.md) for keys/base_url/precedence.
       Update the Installation section to mention the provider extras
       ([openai]/[gemini]/[ollama]/[all-providers]) alongside [advisor]/[mcp],
       and soften the "requires ANTHROPIC_API_KEY" note to "requires the selected
       provider's credential (none for local Ollama)".

    2. Full-library coverage. The overview and the MCP `fdars_run_method`
       bullet currently name only five methods (alignment, FPCA, basis,
       smoothing, clustering). Update the prose so the advisor is described as
       covering ALL 12 aspects (clustering, smoothing, alignment, basis, fpca,
       represent, depth, outliers, classification, regression, regression_cv,
       spm), and add a pointer to the new Per-Aspect Coverage page
       (advisor/aspects.md). Do NOT overstate `fdars_run_method`'s runnable set
       if it differs from the diagnostics set — describe build_diagnostics as
       covering all 12 aspects and keep run/compare wording accurate to mcp.md.

    Do not introduce any executed fence in index.md; keep it prose + the two
    existing diagrams.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q "providers.md" docs/advisor/index.md && grep -q "aspects.md" docs/advisor/index.md && grep -qiE "ollama" docs/advisor/index.md && grep -qiE "gemini" docs/advisor/index.md && grep -q "represent" docs/advisor/index.md && grep -qE "spm|monitoring" docs/advisor/index.md && echo OK</automated>
  </verify>
  <done>index.md describes provider-agnostic operation (all four backends incl. Ollama/Gemini), full 12-aspect coverage, and links to both new pages; grounding-invariant text and existing diagrams are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update python-api.md — provider/model/aspect params + full 12-aspect method set</name>
  <files>docs/advisor/python-api.md</files>
  <action>
    Edit `docs/advisor/python-api.md` (scoped edits; keep the existing executed
    clustering fence and the illustrative advise() warning intact). Per DOCS-03:

    1. build_diagnostics reference. The `method` parameter row currently lists
       only 5 methods ("alignment", "fpca", "basis", "smoothing", "clustering").
       Update it to the full shipped set of 12: alignment, fpca, basis,
       smoothing, clustering, depth, outliers, classification, represent,
       regression, regression_cv, spm. Also add the `n_classes` keyword param
       (int, optional — caller-supplied ground-truth class count for the
       classification aspect; ignored by other methods) matching the shipped
       signature in advisor/__init__.py. Update the "Keys depend on method"
       list to point readers to the Per-Aspect Coverage page (aspects.md) for
       the full per-aspect key sets rather than duplicating all 12 here.

    2. advise() reference. The current signature/params show only
       task/domain_context/model. Update the signature block and the Parameters
       table to include the shipped `provider` (str | Provider | None, default
       None → Anthropic) and `aspect` (str, default "" — selects the per-aspect
       FDA primer clause) parameters, matching advisor/__init__.py. Note the
       three task families (interpretation / parameter / method) apply to every
       aspect. Add a one-line cross-link to the Provider Setup page
       (providers.md) for provider/model selection and precedence.

    Keep the existing executed clustering fence (it already prints FDARS_FENCE_OK)
    and the illustrative advise() fence + warning unchanged. Do not add a new
    executed fence that calls advise().
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q '"regression_cv"' docs/advisor/python-api.md && grep -q '"spm"' docs/advisor/python-api.md && grep -q '"represent"' docs/advisor/python-api.md && grep -q "n_classes" docs/advisor/python-api.md && grep -q "provider" docs/advisor/python-api.md && grep -q "aspect" docs/advisor/python-api.md && grep -q "providers.md" docs/advisor/python-api.md && grep -q "aspects.md" docs/advisor/python-api.md && echo OK</automated>
  </verify>
  <done>python-api.md documents the full 12-method build_diagnostics set + n_classes, and the advise() provider/model/aspect params with the 3 task families, cross-linking to providers.md and aspects.md; existing executed fence and illustrative warning are preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Wire new pages into mkdocs nav + strict offline build gate</name>
  <files>mkdocs.yml</files>
  <action>
    1. Edit `mkdocs.yml` (scoped edit) to add the two new pages under the
       existing `AI Advisor:` nav section. Place them in a sensible order —
       Provider Setup after the overview and Per-Aspect Coverage after the
       Python API — so the section reads: index → Python API → Provider Setup →
       Per-Aspect Coverage → MCP Server → Agent Skill. Use the labels
       "Provider Setup: advisor/providers.md" and
       "Per-Aspect Coverage: advisor/aspects.md". Do not disturb other nav
       entries.

    2. Run the strict OFFLINE build gate (this is the DOCS-03 build-verify and
       the final gate of Phase 24). It MUST run with the project venv, with
       PYTHONPATH=scripts so the executed fences resolve `docs_data`, with no
       network and no API key set, and exit 0. All executed fences (the existing
       clustering fence plus the new depth/fpca aspect fences) must render and
       emit FDARS_FENCE_OK. The verify command runs the build key-free and then
       greps the built site for the sentinel to confirm the aspect fences
       executed.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q "advisor/providers.md" mkdocs.yml && grep -q "advisor/aspects.md" mkdocs.yml && env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY -u GOOGLE_API_KEY PYTHONPATH=scripts .venv/bin/mkdocs build --strict && grep -rq "FDARS_FENCE_OK" site/advisor/aspects/index.html && echo STRICT_BUILD_OK</automated>
  </verify>
  <done>mkdocs.yml AI Advisor nav includes Provider Setup and Per-Aspect Coverage; `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` exits 0 offline with no key set, and the built aspects page contains FDARS_FENCE_OK (executed fences ran).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → executed fences | The strict build renders all executed fences; they must remain offline/key-free so CI (and this gate) pass without credentials. |
| nav config → page files | New nav entries must resolve to existing files (providers.md, aspects.md) or `--strict` fails. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-05 | Denial of Service | strict build fails on missing nav target or broken fence | high | mitigate | depends_on 24-01/24-02 guarantees the two pages exist before nav wiring; strict build gate runs key-free and greps the built sentinel. |
| T-24-06 | Information Disclosure | build accidentally requires a provider key | medium | mitigate | Gate runs with provider key env vars unset (env -u ...); only build_diagnostics fences execute; advise() fences stay illustrative. |

No npm/pip/cargo install tasks — package-legitimacy gate not applicable.
</threat_model>

<verification>
- index.md and python-api.md updated for provider-agnostic + 12-aspect reality with cross-links.
- mkdocs.yml AI Advisor nav includes Provider Setup + Per-Aspect Coverage.
- `PYTHONPATH=scripts .venv/bin/mkdocs build --strict` exits 0 offline, key-free; built aspects page contains FDARS_FENCE_OK.
</verification>

<success_criteria>
DOCS-03 satisfied and Phase 24 complete: overview + Python API reflect
provider-agnostic operation and full-library coverage, the new pages are in the
nav, and the whole site builds green under `mkdocs build --strict` offline with
executed fences running against the shipped implementation.
</success_criteria>

<output>
Create `.planning/phases/24-documentation/24-03-SUMMARY.md` when done.
</output>

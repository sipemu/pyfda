---
phase: 70-multi-domain-data-famm-advanced-clustering
plan: 02
type: execute
wave: 2
depends_on: [70-01]
files_modified:
  - src/famm_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_famm.py
autonomous: true
requirements: [MULTI-02]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "import fdars.famm works; dense_flmm, fast_fmm, multi_famm are callable and return documented PyDicts (SC2)"
    - "dense_flmm returns a 14-key dict; fast_fmm returns a 6-key dict (p=0 gives (0,m)-shaped arrays); multi_famm returns a 4-key dict with a components list of D per-dimension dicts (SC2)"
    - "None of the three FAMM bindings accept PyMultiFunData — all take plain 2D numpy arrays + subject_ids, per the 0.33 signatures (SC2)"
  artifacts:
    - src/famm_mod.rs
    - tests/test_famm.py
  key_links:
    - "lib.rs registers famm submodule via register_submodule! after multi_fdata"
    - "__init__.py _submodule_names includes 'famm'"
---

<objective>
Bind the three Functional Additive Mixed-Model functions — `dense_flmm`, `fast_fmm`, `multi_famm` —
as the new `fdars.famm` submodule (MULTI-02, ROADMAP SC2). Sequenced AFTER 70-01 to satisfy the
phase ordering constraint (SC2/SC3 "consume PyMultiFunData where required" / "after"), and because it
shares `lib.rs` + `__init__.py` with 70-01.

Purpose: expose mixed-model FAMM fitting to Python with documented PyDict returns.

Output: `src/famm_mod.rs` (new), `lib.rs` + `__init__.py` registrations, `tests/test_famm.py`.

CRITICAL — read before writing: NONE of `dense_flmm` / `fast_fmm` / `multi_famm` consume
`MultiFunData` (research grep-confirmed 0 references in famm.rs). They take plain `&FdMatrix`
(or `&[FdMatrix]` for multi_famm) + `&[usize]` subject_ids + `Option<&FdMatrix>` covariates + config.
Do NOT accept `PyRef<PyMultiFunData>` anywhere in this module. MULTI-02's "consume PyMultiFunData where
required" is vacuously satisfied — no FAMM function requires it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md
@src/convert.rs
@src/pace_fpca_mod.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — create src/famm_mod.rs, register fdars.famm, bind dense_flmm end-to-end (14-key PyDict)</name>
  <files>src/famm_mod.rs, src/lib.rs, python/fdars/__init__.py</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 3.1 (dense_flmm signature; DenseFlmmConfig fields ncomp=3/max_iter=50/tol=1e-10/random_slopes; DenseFlmmResult 14 fields; exact PyDict key list; input contract: data via numpy2d_to_fdmatrix, subject_ids as PyReadonlyArray1<i64> → numpy1d_to_usize_vec, covariates Option) and section 5.2/5.3 (lib.rs + __init__.py edit points) and section 8 (FAMM subject-id fixture)
    - src/convert.rs lines 30-115 (numpy2d_to_fdmatrix, numpy1d_to_usize_vec, fdmatrix_to_numpy2d, vec_to_numpy1d, to_pyresult)
    - src/pace_fpca_mod.rs (register fn shape + PyDict result-converter helper style)
  </read_first>
  <action>
    Create `src/famm_mod.rs`. Module `//!` doc comment records, per fdars-core 0.33, that FAMM functions
    take plain FdMatrix inputs and do not accept the multi-domain container handle.

    Bind `#[pyfunction] dense_flmm` with `#[pyo3(signature = (data, subject_ids, covariates=None,
    ncomp=3, max_iter=50, tol=1e-10))]`. Params: `data: PyReadonlyArray2<f64>`,
    `subject_ids: PyReadonlyArray1<i64>`, `covariates: Option<PyReadonlyArray2<f64>>`, then the three
    config scalars. Body: convert data via numpy2d_to_fdmatrix; subject_ids via numpy1d_to_usize_vec;
    covariates via `.map(numpy2d_to_fdmatrix).transpose()?` to get Option<FdMatrix>. Build
    `DenseFlmmConfig` (NOT #[non_exhaustive] — struct-literal or Default+mutation both fine; use
    Default::default() + field mutation for consistency, leaving random_slopes=false). Call
    `to_pyresult(fdars_core::famm::dense_flmm(&mat, &sids, cov.as_ref(), &config))?`.

    Write a private helper `dense_flmm_result_to_pydict(py, r: DenseFlmmResult) -> PyResult<Bound<PyDict>>`
    emitting all 14 keys from research 3.1: mean_function (vec_to_numpy1d), beta_functions/random_effects/
    fitted/residuals (fdmatrix_to_numpy2d), random_variance/sigma2_u/sigma2_slope/eigenvalues
    (vec_to_numpy1d), sigma2_eps (f64), ncomp/n_subjects/n_iter (int via set_item), converged (bool).
    Factor this helper so multi_famm (Task 3) can reuse it for each per-dimension component dict.

    Add `pub fn register(m) -> PyResult<()>` adding dense_flmm (Tasks 2/3 will extend it).

    In src/lib.rs: add `mod famm_mod;` after `mod multi_fdata_mod;`, and
    `register_submodule!(m, "famm", famm_mod::register);` after the multi_fdata register_submodule! call.
    In python/fdars/__init__.py: append a `"famm"` entry (Phase-70 comment) after `"multi_fdata"`.

    Explicit imports only (-D warnings).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qE 'register_submodule!\(m, "famm"' src/lib.rs && grep -q '"famm"' python/fdars/__init__.py && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.famm as f; assert callable(f.dense_flmm); print('fdars.famm + dense_flmm wired OK')"</automated>
    <fails_when>famm is not registered in lib.rs or __init__.py, the crate fails to build, or dense_flmm is absent from the imported module.</fails_when>
  </verify>
  <acceptance_criteria>
    - src/famm_mod.rs exists; fdars.famm registered in lib.rs and __init__.py
    - dense_flmm is a #[pyfunction] taking plain 2D data + i64 subject_ids + optional covariates + config scalars (NOT PyMultiFunData)
    - dense_flmm returns a PyDict with all 14 keys from research 3.1
    - A reusable dense_flmm_result_to_pydict helper exists
    - maturin develop builds green; import + attribute check passes
  </acceptance_criteria>
  <done>fdars.famm imports; dense_flmm present and returns a 14-key dict; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Bind fast_fmm (6-key PyDict; p=0 gives (0,m)-shaped arrays)</name>
  <files>src/famm_mod.rs</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 3.2 (fast_fmm signature; FastFmmConfig fields smooth_window=3/max_iter=30/tol=1e-8/compute_inference=true; FastFmmResult 6 fields; PyDict keys; the p==0 → (0,m) zero-sized-array note)
    - src/famm_mod.rs (dense_flmm binding + register fn just added — same input-marshalling pattern)
  </read_first>
  <action>
    Add `#[pyfunction] fast_fmm` to src/famm_mod.rs with `#[pyo3(signature = (data, subject_ids,
    covariates=None, smooth_window=3, max_iter=30, tol=1e-8, compute_inference=true))]`. Same input
    marshalling as dense_flmm (data → numpy2d_to_fdmatrix; subject_ids → numpy1d_to_usize_vec; covariates
    → Option<FdMatrix>). Build FastFmmConfig (Default + mutation). Call
    `to_pyresult(fdars_core::famm::fast_fmm(&mat, &sids, cov.as_ref(), &config))?`.

    Emit the 6-key PyDict from research 3.2: beta_matrix/t_stats/p_values (fdmatrix_to_numpy2d — these are
    (p,m), and (0,m) when p==0, which fdmatrix_to_numpy2d handles as a shape-(0,m) array), sigma2_eps/
    sigma2_u (vec_to_numpy1d, length m), n_grid (int). Add fast_fmm to register.

    Explicit imports only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.famm as f; assert callable(f.fast_fmm); print('fast_fmm bound OK')"</automated>
    <fails_when>The crate fails to build or fast_fmm is absent from fdars.famm.</fails_when>
  </verify>
  <acceptance_criteria>
    - fast_fmm is a #[pyfunction] with the config defaults from research 3.2, added to register
    - Returns a 6-key PyDict (beta_matrix, t_stats, p_values, sigma2_eps, sigma2_u, n_grid)
    - maturin develop builds green; import + attribute check passes
  </acceptance_criteria>
  <done>fast_fmm present in fdars.famm and callable; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Bind multi_famm (list of 2D arrays → Vec<FdMatrix>; 4-key PyDict with components list) + tests/test_famm.py</name>
  <files>src/famm_mod.rs, tests/test_famm.py</files>
  <read_first>
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 3.3 (multi_famm signature: data: &[FdMatrix]; MultiFammConfig ncomp=3/max_iter=50/tol=1e-10; MultiFammResult 4 fields; PyDict keys incl. components = list of per-dimension dense_flmm dicts) and section 8 (subject-id + multi-variable fixtures; non-square 20x30)
    - src/famm_mod.rs (dense_flmm_result_to_pydict helper from Task 1 — reuse for each component; PyList building pattern)
    - .planning/phases/70-multi-domain-data-famm-advanced-clustering/70-RESEARCH.md section 4 (Vec<FdMatrix> from a PyList of 2D arrays — same collect pattern used for mfpca)
  </read_first>
  <action>
    Add `#[pyfunction] multi_famm<'py>(py, data_list: &Bound<'py, PyList>, subject_ids:
    PyReadonlyArray1<i64>, covariates=None, ncomp=3, max_iter=50, tol=1e-10)`. Build
    `let mats: Vec<FdMatrix> = data_list.iter().map(|item| { let arr =
    item.downcast::<PyReadonlyArray2<f64>>...; numpy2d_to_fdmatrix(arr) }).collect::<PyResult<Vec<_>>>()?;`
    then pass `mats.as_slice()` (a `&[FdMatrix]`). Convert subject_ids/covariates as in dense_flmm. Build
    MultiFammConfig (Default + mutation). Call
    `to_pyresult(fdars_core::famm::multi_famm(&mats, &sids, cov.as_ref(), &config))?`.

    Emit the 4-key PyDict from research 3.3: n_dims (int), stacked_fitted/stacked_residuals
    (fdmatrix_to_numpy2d), components (build a PyList by mapping each DenseFlmmResult in result.components
    through the reused dense_flmm_result_to_pydict helper). Add multi_famm to register.

    Then create `tests/test_famm.py` (NON-SQUARE fixtures per research section 8): n_subjects=5, n_visits=4,
    n_total=20, subject_ids = np.repeat(arange(5),4).astype(int64), data (20,30). Tests:
    - test_dense_flmm: result is a dict with the 14 expected keys; mean_function shape (30,); fitted shape (20,30).
    - test_fast_fmm: result dict has the 6 keys; with no covariates, beta_matrix.shape == (0, 30).
    - test_multi_famm: pass [data1(20,30), data2(20,30)] (share ncols=30); result has 4 keys; len(result["components"]) == 2; each component is a 14-key dict.
    Import fdars.famm; use pytest style consistent with the repo.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_famm.py -x -q 2>&1 | tail -20</automated>
    <fails_when>The crate fails to build, multi_famm is absent, key-sets/shapes differ from research 3.1-3.3, or the components list length is not D.</fails_when>
  </verify>
  <acceptance_criteria>
    - multi_famm is a #[pyfunction] taking a list of 2D arrays (built into Vec<FdMatrix>), added to register
    - Returns a 4-key PyDict; components is a list of D per-dimension 14-key dicts (via the reused helper)
    - tests/test_famm.py exists with the three tests on NON-SQUARE fixtures and passes
    - fast_fmm p=0 case asserts (0,m)-shaped beta_matrix
  </acceptance_criteria>
  <done>tests/test_famm.py passes: dense_flmm/fast_fmm/multi_famm return the documented dicts on non-square fixtures.</done>
</task>

</tasks>

<verification>
- `maturin develop` builds green (no -D warnings failures).
- `import fdars.famm` works; dense_flmm, fast_fmm, multi_famm callable.
- `pytest tests/test_famm.py -x -q` passes.
- Full suite green (additive submodule): `.venv/bin/pytest tests/ -q 2>&1 | tail -5`.
</verification>

<success_criteria>
MULTI-02 satisfied: `dense_flmm`, `fast_fmm`, `multi_famm` callable via `fdars.famm`, each returning
its documented PyDict (14 / 6 / 4 keys), all taking plain 2D inputs (none consuming PyMultiFunData —
vacuously satisfying "where required"). (ROADMAP SC2.)
</success_criteria>

<output>
Create `.planning/phases/70-multi-domain-data-famm-advanced-clustering/70-02-SUMMARY.md` when done.
</output>

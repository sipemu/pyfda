---
phase: 50-deferred-advisor-aspects-compat-pre-flight
plan: 03
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - tests/test_aspect_provider_matrix.py
  - tests/test_advisor_live_integration.py
autonomous: true
requirements: [ASPECT-05]
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "advise() returns grounded interpretation for each new aspect (PACE-FPCA, elastic-multinomial, ITP), verified offline across the native and fallback provider machinery via the aspect x provider grounding matrix (ASPECT-05)"
    - "Every emitted scalar for the three new aspects survives _check_grounding when cited by a provider (ASPECT-05)"
    - "A live-LLM path for the three new aspects is env-gated and network-free in CI (skips without a key) (ASPECT-05)"
  artifacts:
    - "PACE-FPCA, elastic-multinomial, ITP fixtures added to tests/test_aspect_provider_matrix.py _ASPECT_FIXTURES"
    - "env-gated live coverage for the three new aspects in tests/test_advisor_live_integration.py"
  key_links:
    - "new aspect diagnostics (50-02) -> matrix fixtures -> _build_grounded_advice_dict -> advise() -> _check_grounding across native+fallback providers"
---

<objective>
Verify that `advise()` returns grounded interpretation for each of the three newly-filled deferred aspects across providers — offline via the aspect×provider grounding matrix (network-free, deterministic) plus an env-gated live path. This closes ASPECT-05 and proves that every scalar emitted in Plan 50-02 is citable and survives `_check_grounding` through both the native-structured-output and fallback provider machinery.

Purpose: The grounding invariant is the milestone's hard constraint. Adding scalars is not enough — they must be provably citable by a provider without triggering a grounding violation, across the same native/fallback matrix that guards the 12 existing aspects.
Output: PACE-FPCA / elastic-multinomial / ITP fixtures in the matrix test; env-gated live coverage for the three.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-CONTEXT.md
@.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PACE-FPCA, elastic-multinomial, ITP fixtures to the offline aspect x provider grounding matrix (ASPECT-05)</name>
  <files>tests/test_aspect_provider_matrix.py</files>
  <read_first>
    - tests/test_aspect_provider_matrix.py lines 79-234 (the `_ASPECT_FIXTURES` list of `(aspect_id, result, build_kwargs)` tuples; `_build_grounded_advice_dict` which scans the diagnostics for the first top-level numeric value and cites it; the `test_aspect_provider_matrix` parametrization over `_PROVIDER_KINDS = ["native","fallback"]`; note the aspect-specific build_kwargs branch at lines 266-274 where `classification` passes `n_classes` and `alignment` passes `argvals`)
    - python/fdars/advisor/aspects/inference.py (the ITP branch key names from 50-02 — the ITP fixture is a plain dict with `adjusted_pvalues`/`raw_pvalues` lists + `n_basis`/`n_perm`)
    - python/fdars/advisor/aspects/fpca.py (the pace branch — a PACE fixture needs `eigenvalues` (list), `ncomp` (int), `sigma2` (float), and `fitted_lower`/`fitted_upper` (2-D lists) so the new band-width scalar is populated)
    - python/fdars/advisor/aspects/classification.py (elastic fixture needs `train_accuracy`, `n_classes`; the matrix build_kwargs must pass `holdout_accuracy` so `overfitting_gap` is populated and citable)
  </read_first>
  <action>Extend `_ASPECT_FIXTURES` in tests/test_aspect_provider_matrix.py with three NEW tuples per ASPECT-05, using distinct fixture ids so parametrize labels stay unique: (1) `("fpca_pace", _PACE_FPCA_RESULT, {})` where `_PACE_FPCA_RESULT` is a synthetic dict with `eigenvalues` (list of floats, e.g. [3.0,1.5,0.5]), `ncomp` (int, e.g. 2 so truncated_rank_flagged is True), `sigma2` (float, e.g. 0.05), and `fitted_lower`/`fitted_upper` (small 2-D lists so the band-width scalar is a real float) — routed via method `"fpca"`; (2) `("classification_elastic", _ELASTIC_RESULT, {"holdout_accuracy": 0.72})` where `_ELASTIC_RESULT` has `train_accuracy` (float, e.g. 0.95), `n_classes` (int, e.g. 3), `predicted_classes`/`train_probabilities` optional — routed via method `"classification"` so `overfitting_gap` is populated and citable; (3) `("inference_itp", _ITP_RESULT, {})` where `_ITP_RESULT` has `adjusted_pvalues` (list, e.g. [0.02,0.8,0.9,0.85,0.6]), `raw_pvalues` (list), `n_basis` (int 5), `n_perm` (int 99) — routed via method `"inference"`. IMPORTANT: the fixture id is NOT the method — extend the aspect-routing branch in `test_aspect_provider_matrix` (the `if aspect == ...` block) so `fpca_pace`->method `"fpca"`, `classification_elastic`->method `"classification"` (and forward `holdout_accuracy` from build_kwargs), `inference_itp`->method `"inference"`. `_build_grounded_advice_dict` already cites the first top-level numeric value, which for these fixtures will be one of the new scalars — assert (via the existing matrix body) that `advise()` returns an Advice that passes `_check_grounding` for BOTH `native` and `fallback` provider kinds. Reference ASPECT-05. Keep all fixtures synthetic and offline (no fdars call, no network, no RNG).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_aspect_provider_matrix.py -q -k "pace or elastic or itp" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_aspect_provider_matrix.py -k "pace or elastic or itp"` exits 0 with 6 passed (3 aspects x 2 provider kinds)
    - The matrix run cites a NEW scalar for each aspect and passes `_check_grounding` (no GroundingViolationError) for both native and fallback providers
    - `grep -c 'fpca_pace\|classification_elastic\|inference_itp' tests/test_aspect_provider_matrix.py` returns >= 3
    - The full matrix still passes: `pytest tests/test_aspect_provider_matrix.py -q` exits 0 (existing 12 aspects unaffected)
  </acceptance_criteria>
  <done>The three new aspects are in the offline aspect x provider grounding matrix and pass _check_grounding across native and fallback providers.</done>
</task>

<task type="auto">
  <name>Task 2: Env-gated live-LLM coverage for the three new aspects, network-free in CI (ASPECT-05)</name>
  <files>tests/test_advisor_live_integration.py</files>
  <read_first>
    - tests/test_advisor_live_integration.py (the existing env-gate pattern: `pytest.mark.skipif` on `FDARS_INTEGRATION` / `ANTHROPIC_API_KEY`; how a real `advise()` is invoked against a live provider and how grounding is asserted on the response)
    - python/fdars/advisor/aspects/inference.py, fpca.py, classification.py (the new scalar names to assert are present in the built diagnostics before the live call)
  </read_first>
  <action>Extend tests/test_advisor_live_integration.py with env-gated live-LLM tests for the three new aspects per ASPECT-05, MIRRORING the existing skipif env-gate exactly (skip cleanly when `FDARS_INTEGRATION` is unset or no `ANTHROPIC_API_KEY` — CI stays network-free). For each of PACE-FPCA (method `"fpca"`), elastic-multinomial (method `"classification"` with a `holdout_accuracy`), and ITP (method `"inference"`): build diagnostics from a small synthetic fixture (reuse the fixture shapes from Task 1), call `advise(diag, task="interpretation", aspect=<aspect>)` against the live provider, and assert the returned Advice passes grounding (no GroundingViolationError raised; interpretation non-empty). Do NOT add any network dependency that runs unconditionally — the tests MUST be skipped by default. Reference ASPECT-05.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_live_integration.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_advisor_live_integration.py` reports the new tests as SKIPPED (not FAILED, not ERROR) when `FDARS_INTEGRATION` is unset — CI stays network-free
    - `grep -c 'skipif' tests/test_advisor_live_integration.py` increased (each new live test carries the env-gate)
    - The new live tests reference all three methods (`fpca`, `classification`, `inference`) with the new-aspect fixtures
  </acceptance_criteria>
  <done>Live-LLM coverage exists for the three new aspects and is env-gated so CI runs it network-free (skips without a key).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new aspect diagnostics -> provider machinery | the new scalars must be citable through both native-structured-output and fallback providers without a grounding violation |
| CI env -> live-LLM test | a live test that is not env-gated introduces a network dependency and API-key requirement into CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50C-01 | Denial of Service | un-gated live test in CI | high | mitigate | mirror the existing FDARS_INTEGRATION skipif; assert SKIPPED-by-default (Task 2) |
| T-50C-02 | Spoofing | new scalar fails grounding across a provider kind | medium | mitigate | matrix covers both native and fallback kinds; asserts _check_grounding passes (Task 1) |
| T-50C-SC | Tampering | package installs | low | accept | no package install this plan (test-only edits); RESEARCH Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `pytest tests/test_aspect_provider_matrix.py -q` passes (12 existing + 3 new aspects x 2 provider kinds)
- `pytest tests/test_advisor_live_integration.py -q` reports the new tests SKIPPED with no key set (network-free CI)
- Each new aspect passes `_check_grounding` for both native and fallback providers
</verification>

<success_criteria>
- advise() returns grounded interpretation for PACE-FPCA, elastic-multinomial, ITP across the native+fallback matrix
- Every new scalar is citable and survives _check_grounding
- Live-LLM coverage exists and is env-gated (CI network-free)
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Files modified:**
- `tests/test_aspect_provider_matrix.py` (3 new fixtures + routing: `fpca_pace`, `classification_elastic`, `inference_itp`)
- `tests/test_advisor_live_integration.py` (env-gated live coverage for the three new aspects)

**New test fixtures:**
- `_PACE_FPCA_RESULT`, `_ELASTIC_RESULT`, `_ITP_RESULT` (synthetic, offline)

**New parametrized matrix cases:**
- `fpca_pace`, `classification_elastic`, `inference_itp` × {native, fallback}

**No production symbols added; no new scalar keys (verifies the keys added in 50-02); guard-sync no-op.**
</artifacts>

<output>
Create `.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-03-SUMMARY.md` when done
</output>

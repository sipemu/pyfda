---
phase: 50-deferred-advisor-aspects-compat-pre-flight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_mcp_import_smoke.py
  - tests/test_guard_sync_version_independent.py
autonomous: true
requirements: [COMPAT-01, COMPAT-02, COMPAT-03]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The advisor extra pins anthropic below 1.0 so the advisor imports on Python 3.9 (COMPAT-01)"
    - "The MCP server and its 3 existing tools import and load over stdio via the mcp v2 MCPServer path (COMPAT-02)"
    - "The guard-sync assertion (_DIAGNOSTICS_METHODS mirrors build_diagnostics._supported) runs on Python 3.9 — a pure dict-key comparison that does NOT import mcp (COMPAT-03)"
  artifacts:
    - "pyproject.toml with anthropic>=0.72.0,<1.0 in the [advisor] extra"
    - "tests/test_mcp_import_smoke.py"
    - "tests/test_guard_sync_version_independent.py"
  key_links:
    - "build_diagnostics._supported set <-> _DIAGNOSTICS_METHODS frozenset (drift lock, now version-independent)"
---

<objective>
Land the blocking compatibility fixes on the EXISTING advisor/MCP surface as an isolated first commit, before any deferred-aspect work. Three fixes: pin `anthropic` below 1.0 (keeps the advisor importable on Python 3.9); add a regression test that the mcp v2 `MCPServer` path and its 3 existing tools load over stdio (verify-only — the import is already correct); and split the guard-sync assertion into a version-independent test that runs on the Python 3.9 CI baseline.

Purpose: The v8.0 milestone adds new advisor aspects on top of this surface. If the advisor stops importing on 3.9 or the guard-sync test only runs on 3.10+, aspect regressions ship undetected. These fixes must land first (per CONTEXT decision: isolated pre-flight commit).
Output: `pyproject.toml` pin, `tests/test_mcp_import_smoke.py`, `tests/test_guard_sync_version_independent.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: COMPAT-02 tracer — MCP server + 3 tools import/load over stdio end-to-end</name>
  <files>tests/test_mcp_import_smoke.py</files>
  <read_first>
    - python/fdars/mcp/server.py (confirm line 35 `from mcp.server import MCPServer`, line 41 `mcp = MCPServer("fdars-advisor")`, and the 3 `@mcp.tool()` handlers `fdars_build_diagnostics`, `fdars_run_method`, `fdars_compare_run`)
    - tests/test_mcp_server.py (lines 40-50: the module-level `pytestmark = pytest.mark.skipif(sys.version_info < (3, 10), ...)` skip pattern to MIRROR — this new file also imports `mcp` so it too must skip on 3.9)
  </read_first>
  <action>Create a new regression test module `tests/test_mcp_import_smoke.py` per COMPAT-02 (verify-only: NO import change to server.py). Mirror the module-level skip from tests/test_mcp_server.py: set `pytestmark = pytest.mark.skipif(sys.version_info < (3, 10), reason="mcp requires Python 3.10+")` because importing `fdars.mcp.server` imports `mcp`. Write one end-to-end test that imports `mcp` (the `MCPServer` instance) from `fdars.mcp.server`, asserts it is not None, and asserts all three existing tool handler names — `fdars_build_diagnostics`, `fdars_run_method`, `fdars_compare_run` — are registered/loadable on the server (introspect the server's registered tools, or import each handler symbol from `fdars.mcp.server` and assert it is callable). Assert the underlying import path is the mcp v2 `MCPServer` (assert `MCPServer` is importable from `mcp.server`). This is the thinnest end-to-end slice proving the stdio server surface still loads under the pinned dependency set. Reference COMPAT-02.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_mcp_import_smoke.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mcp_import_smoke.py` exits 0 on Python 3.10+ (or reports SKIPPED, not FAILED, on 3.9)
    - The test asserts `MCPServer` is importable from `mcp.server` (not the legacy `mcp` v1 path)
    - The test asserts all three tool handler names load: `fdars_build_diagnostics`, `fdars_run_method`, `fdars_compare_run`
    - `grep -c 'from mcp.server import MCPServer' python/fdars/mcp/server.py` returns 1 (import unchanged — verify-only)
  </acceptance_criteria>
  <done>A version-gated regression test proves the mcp v2 server and its 3 existing tools load; no production import was changed.</done>
</task>

<task type="auto">
  <name>Task 2: COMPAT-01 — pin anthropic below 1.0 in the [advisor] extra</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (line 42: `advisor = ["anthropic>=0.72.0", "pydantic>=2.0"]` — the exact string to edit)
  </read_first>
  <action>In `pyproject.toml`, change the `[advisor]` extra so the anthropic requirement becomes exactly `anthropic>=0.72.0,<1.0` (add the upper bound `<1.0`; keep `pydantic>=2.0` unchanged). This defers the full anthropic 1.x migration (which drops Python 3.9) to its own milestone, per COMPAT-01 and the CONTEXT decision. Do NOT touch the `[mcp]`, `[dev]`, or provider extras. Reference COMPAT-01.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -c 'anthropic>=0.72.0,<1.0' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'anthropic>=0.72.0,<1.0' pyproject.toml` matches the `[advisor]` extra line
    - `grep -c 'anthropic>=0.72.0"' pyproject.toml` returns 0 (the un-capped form is gone)
    - The `pydantic>=2.0` entry in the `[advisor]` extra is preserved
  </acceptance_criteria>
  <done>The advisor extra caps anthropic below 1.0; the un-capped requirement no longer appears.</done>
</task>

<task type="auto">
  <name>Task 3: COMPAT-03 — version-independent guard-sync test (pure dict-key comparison, no mcp import)</name>
  <files>tests/test_guard_sync_version_independent.py</files>
  <read_first>
    - tests/test_mcp_server.py (line 503 `test_diagnostics_methods_match_advisor_supported`: the CURRENT guard-sync test that is skipped on Py<3.10 because the module imports `mcp`; understand it provokes a ValueError from `build_diagnostics` to recover the supported set)
    - python/fdars/advisor/__init__.py (lines 125-141: the `_supported` set literal inside `build_diagnostics`, and the `ValueError` message format `"build_diagnostics: unsupported method {method!r}. Supported: {sorted(_supported)!r}."`)
    - python/fdars/mcp/server.py (lines 63-83: the `_DIAGNOSTICS_METHODS` frozenset — the set to compare against)
  </read_first>
  <action>Create `tests/test_guard_sync_version_independent.py` — a NEW test that runs on ALL supported Python versions including 3.9 (COMPAT-03). It MUST NOT import `mcp` and MUST NOT import `fdars.mcp.server` (importing that module pulls in `mcp`, which is unavailable on 3.9). Instead, recover BOTH sets without importing mcp: (1) the advisor supported set by calling `build_diagnostics({}, "__sentinel_not_a_method__")`, catching the `ValueError`, and parsing the sorted list embedded after `Supported: ` in the message (use `ast.literal_eval` on the bracketed list substring); (2) the MCP diagnostics set as a plain module-level literal duplicated INTO this test file as `_EXPECTED_DIAGNOSTICS_METHODS` — a hard-coded frozenset of the 14 aspect strings (`alignment, fpca, basis, smoothing, clustering, depth, outliers, classification, represent, regression, regression_cv, spm, scoring, inference`) — with a comment stating it must equal `fdars.mcp.server._DIAGNOSTICS_METHODS` and that both must equal the advisor `_supported` set. Assert the parsed advisor set equals `_EXPECTED_DIAGNOSTICS_METHODS`. Do NOT add a module-level 3.10 skip. Add a SECOND test, guarded internally by `if sys.version_info >= (3, 10)` (import inside the test body, skip via `pytest.importorskip("mcp")` at the top of that test only), that imports `fdars.mcp.server._DIAGNOSTICS_METHODS` and asserts it equals `_EXPECTED_DIAGNOSTICS_METHODS` — this keeps the hard-coded literal honest on 3.10+ without blocking the 3.9 run of the primary test. Reference COMPAT-03.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_guard_sync_version_independent.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_guard_sync_version_independent.py` exits 0
    - `grep -c 'import mcp' tests/test_guard_sync_version_independent.py` returns 0 (no top-level mcp import; only an in-test `importorskip` on the 3.10-guarded test)
    - `grep -c 'pytestmark' tests/test_guard_sync_version_independent.py` returns 0 (no module-level version skip — the primary test runs on 3.9)
    - The primary test recovers the advisor `_supported` set by parsing the `build_diagnostics` ValueError and asserts equality with the hard-coded 14-aspect expected set
  </acceptance_criteria>
  <done>A guard-sync assertion runs on Python 3.9 without importing mcp; a companion 3.10-guarded test keeps the hard-coded expected set in sync with `_DIAGNOSTICS_METHODS`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package resolver -> installed env | pyproject dependency spec controls which anthropic version is resolved; a too-loose pin admits an API-breaking 1.x on 3.9 |
| CI Python 3.9 baseline -> guard-sync test | the guard-sync assertion must execute on the 3.9 runner (where mcp is absent) or drift ships undetected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50A-01 | Tampering | pyproject anthropic pin | high | mitigate | cap `anthropic>=0.72.0,<1.0`; verify grep gate (Task 2) |
| T-50A-02 | Denial of Service | guard-sync test coverage | medium | mitigate | version-independent test with no mcp import runs on 3.9 (Task 3) |
| T-50A-SC | Tampering | anthropic dependency (already-pinned) | low | accept | no NEW package install this plan — only tightening an existing bound; RESEARCH.md Package Legitimacy Gate not triggered (no new package added) |
</threat_model>

<verification>
- `pytest tests/test_mcp_import_smoke.py tests/test_guard_sync_version_independent.py -q` passes (import smoke SKIPs on 3.9, guard-sync runs)
- `grep 'anthropic>=0.72.0,<1.0' pyproject.toml` matches
- `git diff HEAD --stat` shows ONLY pyproject.toml + the two new test files (isolated pre-flight commit — no aspect files touched)
</verification>

<success_criteria>
- anthropic capped below 1.0 in [advisor]
- MCP v2 server + 3 tools proven to load (version-gated regression test)
- Guard-sync assertion runs on Python 3.9 without importing mcp
- Commit contains only the three compat files (isolated first commit)
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Files created:**
- `tests/test_mcp_import_smoke.py`
- `tests/test_guard_sync_version_independent.py`

**Files modified:**
- `pyproject.toml` (anthropic pin `>=0.72.0,<1.0`)

**New test names:**
- `test_mcp_import_smoke.py::*` (mcp v2 server + 3-tool load, version-gated)
- `test_guard_sync_version_independent.py::*` (pure dict-key guard-sync, runs on 3.9; + 3.10-guarded companion)

**New symbols:**
- `_EXPECTED_DIAGNOSTICS_METHODS` (test-local frozenset mirror of `_DIAGNOSTICS_METHODS`)

**No production symbols added; no new scalar keys; guard-sync is a no-op (`_DIAGNOSTICS_METHODS` unchanged).**
</artifacts>

<output>
Create `.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-01-SUMMARY.md` when done
</output>

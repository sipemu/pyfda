---
phase: 50-deferred-advisor-aspects-compat-pre-flight
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - python/fdars/advisor/aspects/inference.py
  - python/fdars/advisor/aspects/fpca.py
  - python/fdars/advisor/aspects/classification.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor_itp.py
  - tests/test_advisor_group_b.py
autonomous: true
requirements: [ASPECT-01, ASPECT-02, ASPECT-03, ASPECT-04]
estimate:
  tokens: 92000
  raw_tokens: 46000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "The ITP aspect reduces the vector-valued adjusted-p-curve to grounded DETECTION scalars (min adjusted p-value; detected-at-0.05) AND LOCALISATION scalars (count + proportion of significant intervals; first significant basis) together — never a lone global scalar (ASPECT-03)"
    - "The PACE-FPCA aspect emits grounded sigma2 noise/signal scalar, a truncated-rank flag, and a mean prediction-band width, all native float/int (ASPECT-01)"
    - "The elastic-multinomial aspect emits a grounded overfitting gap (train minus caller-supplied holdout/CV accuracy) and a class-count flag, native float/int (ASPECT-02)"
    - "Every emitted scalar is a native Python float/int (no numpy scalars) and the diagnostics dict is json.dumps-serialisable"
    - "_ASPECT_PRIMERS is EXTENDED (not replaced) for fpca/classification/inference; the ITP primer explicitly teaches the whether-vs-where (detection vs localisation) distinction (ASPECT-04)"
    - "No new _DIAGNOSTICS_METHODS key and no new build_diagnostics._supported key are added — guard-sync stays a no-op (ASPECT-04)"
  artifacts:
    - "ITP branch in python/fdars/advisor/aspects/inference.py"
    - "PACE prediction-band + truncated-rank scalars in python/fdars/advisor/aspects/fpca.py"
    - "overfitting-gap + class-count scalars in python/fdars/advisor/aspects/classification.py"
    - "extended _ASPECT_PRIMERS fpca/classification/inference entries in python/fdars/advisor/_prompts.py"
    - "tests/test_advisor_itp.py"
  key_links:
    - "src/inference_mod.rs itp_result_to_pydict (adjusted_pvalues array) -> inference.py ITP branch scalar reduction"
    - "pace_fpca fitted_lower/fitted_upper arrays -> fpca.py mean prediction-band width scalar"
    - "every new scalar -> _check_grounding _flatten_diagnostics_numbers (must be a top-level native number)"
---

<objective>
Fill the three deferred advisor aspects with grounded, fdars-computed scalars and extend their primers, WITHOUT adding any new `build_diagnostics` method slot (guard-sync stays a no-op). The tracer proves the highest-risk, most-novel path end-to-end first: the ITP vector→scalar reduction, from the raw `adjusted_pvalues` array all the way through `advise()` surviving `_check_grounding`. The remaining tasks expand out to PACE-FPCA and elastic-multinomial scalars and the primer edits.

Purpose: These aspects feed every later LLM call in v8.0 (comparison, pipeline, auto-tuning). ITP is the riskiest because a naive `min(adjusted_pvalues)` reduction misleads the LLM into treating local significance as global (PITFALLS #8) — the tracer forces the detection+localisation contract to be proven before the other aspects are built.
Output: ITP branch in inference.py; PACE + elastic scalars in fpca.py/classification.py; extended primers in _prompts.py; offline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — ITP vector-to-scalar reduction, end-to-end through _check_grounding (ASPECT-03)</name>
  <files>python/fdars/advisor/aspects/inference.py, tests/test_advisor_itp.py</files>
  <read_first>
    - python/fdars/advisor/aspects/inference.py (the WHOLE builder: the shape-detection block at lines 123-136 raises ValueError when neither TestResult nor ToleranceBand keys are present — the ITP result has `adjusted_pvalues`/`raw_pvalues` arrays and NO `p_value`/`statistic`, so it currently hits that ValueError; the ITP branch must be added as a new detected shape)
    - src/inference_mod.rs lines 583-594 (`itp_result_to_pydict`: the ITP result dict keys are exactly `adjusted_pvalues` (array m,), `raw_pvalues` (array m,), `basis_type` (str), `n_basis` (int), `n_perm` (int))
    - tests/test_inference.py lines 724-790 (how a real `itp_one_pop` result is constructed: `inf.itp_one_pop(data, argvals, n_perm=99)` returns the dict; `adjusted_pvalues.shape == (n_basis,)`)
    - python/fdars/advisor/providers/_validate.py lines 173-210 (`_flatten_diagnostics_numbers`: only top-level and nested numeric values are collected; a numpy array value would break json serialisation and must NOT be stored — store only native scalars)
  </read_first>
  <behavior>
    - Test: build_diagnostics(itp_result, "inference") on a synthetic ITP dict with `adjusted_pvalues=[0.02, 0.80, 0.90, 0.85, 0.60]` (1 of 5 significant at 0.05) returns `method=="inference"`, `itp_result_present is True`.
    - Test (detection): `itp_min_adjusted_pvalue == 0.02` (native float), `itp_detected_at_0.05 is True`.
    - Test (localisation): `itp_n_significant_0.05 == 1` (native int), `itp_fraction_significant_0.05 == 0.2` (native float), `itp_first_significant_basis == 0` (native int, the index of the first basis with adjusted p < 0.05).
    - Test (localisation, none-significant): with all adjusted p > 0.05, `itp_n_significant_0.05 == 0`, `itp_fraction_significant_0.05 == 0.0`, `itp_detected_at_0.05 is False`, `itp_first_significant_basis is None`.
    - Test (grounding survival): json.dumps(diag) succeeds (no numpy array stored); every itp_* scalar is a native float/int/bool/None; a hand-built Advice citing `itp_min_adjusted_pvalue` and `itp_n_significant_0.05` passes `_check_grounding` against the diag dict.
    - Test (no-vector-stored): "adjusted_pvalues" is NOT a key in the returned diag (the raw array is reduced, never echoed).
  </behavior>
  <action>Add an ITP branch to `_build_inference_diagnostics` in inference.py. Detect the ITP shape by presence of the `adjusted_pvalues` key (unique to the ITP result; TestResult uses `p_value`/`statistic`, ToleranceBand uses `half_width`/`center`). Add this shape to the accepted-shapes guard so the ITP dict no longer raises the "neither TestResult nor ToleranceBand" ValueError. When detected: convert `adjusted_pvalues` to a plain Python list of floats (iterate, cast each with `float()` — do NOT depend on numpy being importable here beyond the array-iteration already available); with alpha fixed at 0.05, compute and emit these grounded scalars, ALL native types: `itp_result_present` (bool True); DETECTION — `itp_min_adjusted_pvalue` (float, the minimum adjusted p), `itp_detected_at_0.05` (bool, min adjusted p < 0.05); LOCALISATION — `itp_n_significant_0.05` (int count of basis intervals with adjusted p < 0.05), `itp_fraction_significant_0.05` (float count/n_basis, 0.0 when n_basis is 0), `itp_first_significant_basis` (int index of the first basis with adjusted p < 0.05, or None when none significant); plus `itp_n_basis` (int) and `itp_n_perm` (int from the raw `n_perm`). Do NOT store the `adjusted_pvalues` or `raw_pvalues` arrays in the returned dict (breaks json + grounding). On the existing TestResult and ToleranceBand paths, set all `itp_*` fields to None (so the key set is stable across shapes) and `itp_result_present` to False. Reference ASPECT-03 and PITFALLS #8 (emit detection AND localisation together; never a lone scalar). Create `tests/test_advisor_itp.py` implementing the behaviors above with a synthetic ITP dict fixture (no fdars call needed — a plain dict with an `adjusted_pvalues` list is sufficient and offline-deterministic).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_itp.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_advisor_itp.py` exits 0
    - `python -c "import json,fdars.advisor as a; print(json.dumps(a.build_diagnostics({'adjusted_pvalues':[0.02,0.8,0.9],'raw_pvalues':[0.01,0.7,0.8],'n_basis':3,'n_perm':99},'inference')))"` prints valid JSON containing `itp_min_adjusted_pvalue`, `itp_n_significant_0.05`, `itp_first_significant_basis` and does NOT contain `adjusted_pvalues`
    - The diagnostics dict emits BOTH `itp_min_adjusted_pvalue` (detection) AND `itp_n_significant_0.05` + `itp_fraction_significant_0.05` (localisation) — a test asserts all three present and non-None on a significant fixture
    - No numpy scalar leaks: a test asserts `type(diag["itp_fraction_significant_0.05"]) is float` and `type(diag["itp_n_significant_0.05"]) is int`
    - `grep -c 'adjusted_pvalues' python/fdars/advisor/aspects/inference.py` shows the key is READ (detection) but a test confirms it is not a returned diag key
  </acceptance_criteria>
  <done>An ITP result flows build_diagnostics -> reduced detection+localisation scalars -> a grounded Advice surviving _check_grounding, end-to-end, with no numpy array stored and json.dumps clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PACE-FPCA grounded scalars — sigma2 ratio, truncated-rank flag, mean prediction-band width (ASPECT-01)</name>
  <files>python/fdars/advisor/aspects/fpca.py, tests/test_advisor_group_b.py</files>
  <read_first>
    - python/fdars/advisor/aspects/fpca.py (the existing `has_pace_fpca` branch at lines 88-108: it already reads `pace_ncomp`, `pace_sigma2`, `pace_variance_explained_cumulative/first` from the raw dict; EXTEND this branch, do not rewrite the standard-FPCA branch above it)
    - src/pace_fpca_mod.rs lines 169-186 (the pace_fpca result keys: `mean`, `eigenvalues`, `eigenfunctions`, `scores`, `fitted`, `fitted_lower`, `fitted_upper`, `argvals`, `sigma2` (float), `ncomp` (int) — `fitted_lower`/`fitted_upper` are 2-D (n,m) prediction-band arrays; `sigma2` is the noise variance)
    - tests/test_pace_fpca.py lines 125-180 (the real result-key set and how a pace_fpca result is built; `fitted_lower`/`fitted_upper` shapes are (n,m))
    - tests/test_advisor_group_b.py (the existing pace/elastic test structure to extend — class-per-aspect, real fdars results)
  </read_first>
  <behavior>
    - Test: build_diagnostics(pace_result, "fpca") with `has_pace_fpca True` emits `pace_sigma2` (float, already present) and a NEW `pace_noise_signal_ratio` (float): sigma2 divided by the total signal variance (sum of eigenvalues); native float, 0.0-safe when total variance is 0.
    - Test (truncated-rank flag): `pace_truncated_rank_flagged` (bool): True when `ncomp` is at/below a truncation heuristic relative to available eigenvalues (e.g. ncomp < len(eigenvalues)); derived only from fdars-computed ncomp/eigenvalue counts — no invented threshold.
    - Test (mean prediction-band width): `pace_mean_prediction_band_width` (float): the mean over all (n,m) cells of `fitted_upper - fitted_lower`, cast to a native float; None when the band arrays are absent.
    - Test (native types + json): all three new scalars are native float/bool; json.dumps(diag) succeeds; no numpy scalar.
    - Test (non-pace path unaffected): a standard fpca result (singular_values path) leaves all `pace_*` fields None, including the three new ones.
  </behavior>
  <action>Extend the existing `has_pace_fpca` branch in fpca.py to emit three additional grounded scalars per ASPECT-01, all native float/int (no numpy scalars): `pace_noise_signal_ratio` (float) = `pace_sigma2 / total_signal_variance` where total_signal_variance is the sum of the pace `eigenvalues` (guard divide-by-zero -> emit None or 0.0 deterministically); `pace_truncated_rank_flagged` (bool) computed from the fdars-computed `ncomp` vs the number of available eigenvalues (True when `ncomp < len(eigenvalues)`, i.e. the decomposition was truncated below full rank) — derive ONLY from counts already in the result, invent no numeric threshold; `pace_mean_prediction_band_width` (float) = mean over all cells of (`fitted_upper` - `fitted_lower`) reduced to one native float via a plain arithmetic mean (`fitted_upper`/`fitted_lower` are 2-D arrays — reduce to a scalar; None when either band key is absent). In the else (non-pace) branch, add the three new fields set to None so the key set is stable. Do NOT store any array in the diag. Extend the pace test class in tests/test_advisor_group_b.py with tests for the three new scalars using a real `pace_fpca` result. Reference ASPECT-01.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_group_b.py -q -k "pace or fpca" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_advisor_group_b.py -k "pace"` exits 0
    - A test asserts `type(diag["pace_noise_signal_ratio"]) is float`, `type(diag["pace_truncated_rank_flagged"]) is bool`, `type(diag["pace_mean_prediction_band_width"]) is float`
    - `json.dumps(build_diagnostics(pace_result, "fpca"))` succeeds (no numpy scalar, no array stored)
    - A test asserts a standard (non-pace) fpca result leaves `pace_noise_signal_ratio`, `pace_truncated_rank_flagged`, `pace_mean_prediction_band_width` all None
  </acceptance_criteria>
  <done>PACE-FPCA emits sigma2 noise/signal ratio, a truncated-rank flag, and a mean prediction-band width, all fdars-computed native scalars; standard FPCA is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: elastic-multinomial grounded scalars — overfitting gap + class-count flag (ASPECT-02)</name>
  <files>python/fdars/advisor/aspects/classification.py, tests/test_advisor_group_b.py</files>
  <read_first>
    - python/fdars/advisor/aspects/classification.py (the existing `has_elastic_multinomial` branch at lines 133-149: it reads `train_accuracy` and `n_classes` from raw; EXTEND this branch. Note the elastic_multinomial RESULT dict carries ONLY `train_accuracy` — there is NO holdout/CV accuracy in the result, confirmed against src/classification_mod.rs)
    - src/classification_mod.rs (elastic_multinomial_result_to_pydict: keys are `n_classes`, `classes`, `train_probabilities`, `predicted_classes`, `train_accuracy` — NO holdout/cv accuracy key exists in the result)
    - tests/test_advisor_group_b.py lines 42-107 (the elastic_multinomial test class; how `n_classes` is a caller/raw-supplied value — the precedent for a caller-supplied `holdout_accuracy` follows the same n_classes pattern)
  </read_first>
  <behavior>
    - Test (overfitting gap, caller-supplied holdout): build_diagnostics(elastic_result, "classification", holdout_accuracy=0.72) with train_accuracy=0.95 emits `overfitting_gap == 0.23` (float, = train_accuracy - holdout_accuracy); `overfitting_gap_holdout_source == "holdout_accuracy"` (str provenance) — mirrors the n_classes caller-supplied pattern.
    - Test (no holdout supplied): when `holdout_accuracy` is None (default), `overfitting_gap is None` (cannot fabricate a holdout — grounding invariant) and `overfitting_gap_holdout_source is None`.
    - Test (class-count flag): `n_classes_flagged` (bool) True when the fdars-computed `n_classes` >= a small-multiclass boundary derived from the count itself (e.g. n_classes > 2, i.e. multiclass rather than binary) — derived only from the fdars-computed count, no invented threshold beyond the binary/multiclass structural split.
    - Test (native types + json): overfitting_gap is native float or None; n_classes_flagged is native bool; json.dumps succeeds.
    - Test (point-estimate path unaffected): a plain fclassif result (accuracy path) leaves overfitting_gap None and does not raise.
  </behavior>
  <action>Extend the `has_elastic_multinomial` branch in classification.py to emit an overfitting gap and a class-count flag per ASPECT-02, all native types. Add a `holdout_accuracy: float | None = None` keyword parameter to `_build_classification_diagnostics` (threaded through from build_diagnostics **kwargs, exactly as `n_classes` is threaded) — this is CALLER-SUPPLIED because the elastic_multinomial result has no holdout/CV accuracy of its own (grounding invariant: the gap can only be computed against a real fdars/caller-measured holdout, never a fabricated one). Emit `overfitting_gap` (float) = `train_accuracy - holdout_accuracy` when `holdout_accuracy` is not None, else None; emit `overfitting_gap_holdout_source` (str "holdout_accuracy" when supplied, else None) for provenance. Emit `n_classes_flagged` (bool) derived from the fdars-computed `n_classes` (True when n_classes > 2 — the binary-vs-multiclass structural split; do not invent a larger threshold). In the non-elastic branch add these fields set to None. Reference ASPECT-02. Extend the elastic test class in tests/test_advisor_group_b.py with the behaviors above.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_group_b.py -q -k "multinomial or elastic or classification" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_advisor_group_b.py -k "multinomial"` exits 0
    - A test asserts `overfitting_gap == train_accuracy - holdout_accuracy` for a caller-supplied holdout, and `overfitting_gap is None` when no holdout supplied (no fabricated holdout)
    - A test asserts `type(diag["overfitting_gap"]) is float` (when supplied) and `type(diag["n_classes_flagged"]) is bool`
    - A plain fclassif (accuracy) result yields `overfitting_gap is None` and does not raise
  </acceptance_criteria>
  <done>elastic-multinomial emits a grounded overfitting gap (train minus caller-supplied holdout) and a class-count flag; the gap is None rather than fabricated when no holdout is provided.</done>
</task>

<task type="auto">
  <name>Task 4: Extend _ASPECT_PRIMERS for fpca/classification/inference; guard-sync no-op check (ASPECT-04)</name>
  <files>python/fdars/advisor/_prompts.py, tests/test_advisor_itp.py</files>
  <read_first>
    - python/fdars/advisor/_prompts.py lines 45-169 (`_ASPECT_PRIMERS`: the existing `"fpca"` entry has PACE `sigma2`/`pace_ncomp`/`pace_variance_explained_first` language; the `"classification"` entry has elastic_multinomial `train_accuracy`/`train_error_rate`/`n_classes` language; the `"inference"` entry ends with the "do not claim significance beyond the p_value and alpha levels already provided" prohibition — the pattern to reuse per PITFALLS #11)
    - python/fdars/advisor/aspects/inference.py (the itp_* scalar names emitted in Task 1 — the primer must name them)
    - .planning/research/PITFALLS.md #11 (primer wording must not invent thresholds; end with a prohibition on inventing reference values)
  </read_first>
  <action>EXTEND (append to, never replace) three `_ASPECT_PRIMERS` entries per ASPECT-04 — NO new dictionary keys are added (fpca/classification/inference already exist, so guard-sync stays a no-op and `_DIAGNOSTICS_METHODS`/`_supported` are untouched). (1) `"fpca"`: add a clause naming `pace_noise_signal_ratio` (higher = more noise relative to signal), `pace_truncated_rank_flagged` (the decomposition was truncated below full rank), and `pace_mean_prediction_band_width` (mean PACE prediction-band width — wider = more predictive uncertainty). (2) `"classification"`: add a clause naming `overfitting_gap` (train minus holdout accuracy; larger positive = more overfitting) and `n_classes_flagged` (multiclass rather than binary). (3) `"inference"`: add an ITP clause that EXPLICITLY teaches the whether-vs-where distinction — state that `itp_min_adjusted_pvalue` and `itp_detected_at_0.05` answer DETECTION (whether ANY interval is significant) while `itp_n_significant_0.05`, `itp_fraction_significant_0.05`, and `itp_first_significant_basis` answer LOCALISATION (where and how much); instruct the model to NOT infer global significance from the minimum alone and to cite the count/fraction alongside the minimum. End each new clause with the existing-pattern prohibition: only cite values present in the diagnostics; do not supply thresholds or reference values not present. The ITP clause MUST contain the substring "localis" (localisation) so the whether-vs-where teaching is verifiable. Add one test to tests/test_advisor_itp.py that imports `_ASPECT_PRIMERS` and asserts the `"inference"` entry contains "localis" and names `itp_n_significant_0.05`. Reference ASPECT-04.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_advisor_itp.py -q -k "primer" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_advisor_itp.py -k primer` exits 0
    - `python -c "from fdars.advisor._prompts import _ASPECT_PRIMERS as p; assert 'localis' in p['inference'] and 'itp_n_significant_0.05' in p['inference'] and 'overfitting_gap' in p['classification'] and 'pace_noise_signal_ratio' in p['fpca']"` exits 0
    - No new key added to `_ASPECT_PRIMERS`: `python -c "from fdars.advisor._prompts import _ASPECT_PRIMERS as p; assert set(p) >= {'fpca','classification','inference'}; assert len(p) <= 14"` exits 0 (the three entries are EXTENDED, not added)
    - `git diff HEAD python/fdars/advisor/__init__.py python/fdars/mcp/server.py` is empty (guard-sync no-op — neither `_supported` nor `_DIAGNOSTICS_METHODS` changed)
  </acceptance_criteria>
  <done>The fpca/classification/inference primers name the new scalars; the ITP primer teaches whether-vs-where (contains "localis"); no new primer key and no guard-sync change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fdars result dict -> aspect builder | untrusted-shape dict crosses here; a numpy array must be reduced to a native scalar before storage |
| aspect diagnostics -> LLM (advise) | grounded scalars cross to the LLM; a primer that implies a threshold lets the LLM fabricate a citation |
| LLM response -> _check_grounding | every cited number must exist in the diagnostics dict as a top-level native number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50B-01 | Information Disclosure | ITP scalar reduction | high | mitigate | emit detection AND localisation together; never store the raw p-curve array (PITFALLS #8; Task 1) |
| T-50B-02 | Tampering | numpy scalar leak into diagnostics | high | mitigate | cast every emitted value to native float/int; json.dumps gate in Tasks 1-3 |
| T-50B-03 | Spoofing | fabricated overfitting-gap holdout | high | mitigate | overfitting_gap is None (not fabricated) when no caller holdout supplied (Task 3) |
| T-50B-04 | Elevation of Privilege | primer implies invented threshold | medium | mitigate | each new primer clause ends with the "only cite values present" prohibition (Task 4; PITFALLS #11) |
| T-50B-05 | Tampering | guard-sync drift | medium | mitigate | no new `_supported`/`_DIAGNOSTICS_METHODS` key; git-diff-empty gate (Task 4) |
| T-50B-SC | Tampering | package installs | low | accept | no package install this plan (pure Python edits + tests); RESEARCH Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `pytest tests/test_advisor_itp.py tests/test_advisor_group_b.py -q` passes
- `json.dumps(build_diagnostics(itp_result,"inference"))` and `json.dumps(build_diagnostics(pace_result,"fpca"))` both succeed (no numpy scalar, no array stored)
- ITP diag carries BOTH detection (`itp_min_adjusted_pvalue`, `itp_detected_at_0.05`) AND localisation (`itp_n_significant_0.05`, `itp_fraction_significant_0.05`, `itp_first_significant_basis`)
- `git diff HEAD python/fdars/advisor/__init__.py python/fdars/mcp/server.py` empty (guard-sync no-op)
- All aspect + primer edits committed atomically in one commit (guard-sync atomicity rule)
</verification>

<success_criteria>
- ITP reduced to detection + localisation grounded scalars, never a lone global scalar
- PACE-FPCA emits sigma2 noise/signal ratio, truncated-rank flag, mean prediction-band width
- elastic-multinomial emits overfitting gap (train minus caller-supplied holdout) + class-count flag
- All emitted scalars native float/int; diagnostics json-serialisable; survive _check_grounding
- Primers extended (no new key); ITP primer teaches whether-vs-where
- Guard-sync unchanged (no new method slot)
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Files created:**
- `tests/test_advisor_itp.py` (ITP detection+localisation scalar tests + primer whether-vs-where test)

**Files modified:**
- `python/fdars/advisor/aspects/inference.py` (ITP branch)
- `python/fdars/advisor/aspects/fpca.py` (PACE extra scalars)
- `python/fdars/advisor/aspects/classification.py` (overfitting gap + class-count flag; new `holdout_accuracy` kwarg)
- `python/fdars/advisor/_prompts.py` (extended fpca/classification/inference primers)
- `tests/test_advisor_group_b.py` (extended pace + elastic tests)

**New scalar keys (all native float/int/bool/None):**
- ITP (inference aspect): `itp_result_present`, `itp_min_adjusted_pvalue`, `itp_detected_at_0.05`, `itp_n_significant_0.05`, `itp_fraction_significant_0.05`, `itp_first_significant_basis`, `itp_n_basis`, `itp_n_perm`
- PACE (fpca aspect): `pace_noise_signal_ratio`, `pace_truncated_rank_flagged`, `pace_mean_prediction_band_width`
- elastic-multinomial (classification aspect): `overfitting_gap`, `overfitting_gap_holdout_source`, `n_classes_flagged`

**New function parameter:**
- `_build_classification_diagnostics(..., holdout_accuracy=None)` (threaded from `build_diagnostics` **kwargs)

**Guard-sync: NO-OP** — no new `_DIAGNOSTICS_METHODS` key, no new `build_diagnostics._supported` key, no new `_ASPECT_PRIMERS` key.
</artifacts>

<output>
Create `.planning/phases/50-deferred-advisor-aspects-compat-pre-flight/50-02-SUMMARY.md` when done
</output>
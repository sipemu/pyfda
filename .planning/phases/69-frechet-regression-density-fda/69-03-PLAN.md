---
phase: 69-frechet-regression-density-fda
plan: 03
type: execute
wave: 3
depends_on: [69-02]
files_modified:
  - src/frechet_mod.rs
  - tests/test_frechet.py
autonomous: true
requirements: [FRE-01]
estimate:
  tokens: 58000
  raw_tokens: 36000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "frechet_mean(objects, space, d, weights=None) dispatches on space via a monomorphized match with an Err wildcard arm"
    - "space='spd' and space='correlation' return a (d,d) numpy 2D array; space='spherical' returns a (d,) unit vector"
    - "Per-space input validation runs in the binding: SPD symmetric + positive diagonal; spherical unit-norm; correlation unit-diagonal"
    - "An invalid space string raises ValueError listing the valid names 'spd', 'spherical', 'correlation'"
  artifacts:
    - src/frechet_mod.rs
    - tests/test_frechet.py
  key_links:
    - "match space { \"spd\" => frechet_mean::<SpdMatrixSpace>, \"spherical\" => ::<SphericalSpace>, \"correlation\" => ::<CorrelationMatrixSpace>, _ => Err }"
    - "SpdMatrixSpace::new(d, SpdMetric::Frobenius) — Frobenius only (Power/LogCholesky deferred)"
---

<objective>
FRE-01 (part 2 of 2): Append the generic `frechet_mean` binding to `src/frechet_mod.rs` — the
ONLY generic Fréchet function — using a MONOMORPHIZED string-dispatch match over the three
statistically-common metric spaces (SPD, spherical, correlation), each with its own numpy input
contract, in-binding validation, and result marshalling. The wildcard arm returns a `ValueError`
listing the valid space names (mandatory per the locked STATE enum-arm decision).

Purpose: Complete FRE-01 by exposing Fréchet mean over metric spaces. This is the subtle part:
per-space marshalling (col-major flattening, unit-norm/symmetry/unit-diagonal checks) and a
monomorphized (NOT trait-object) dispatch.
Output: `fdars.frechet.frechet_mean` supporting `space` in {"spd","spherical","correlation"}.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/scalar_on_function_mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind frechet_mean with monomorphized 3-space string dispatch + per-space marshalling and validation</name>
  <files>src/frechet_mod.rs</files>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§3 frechet_mean generic signature + "Better binding signature"; §4 per-space return types; §5 FULL per-space marshalling: spd_object_from_numpy, spherical_object_from_numpy, corr_object_from_numpy, the space_from_str match pattern, result reshape; §9 frechet_mean return table; Pitfall 3 & 5)
    - src/frechet_mod.rs (the file from plan 69-02 — append frechet_mean + its private helpers; add any imports now genuinely used: PyArray2, PyReadonlyArray1, PyReadonlyArray2, PyList)
    - src/scalar_on_function_mod.rs (analog: penalty_from_str Err-wildcard-arm dispatch pattern)
  </read_first>
  <action>
Append to `src/frechet_mod.rs`. Add private per-space marshalling helpers verbatim-per-§5:
`spd_object_from_numpy(arr: PyReadonlyArray2<f64>, d, i)` — validate shape (d,d), flatten
row-major numpy element (r,c) → column-major index `r + c*d`, return `Vec<f64>` length d*d;
`spherical_object_from_numpy(arr: PyReadonlyArray1<f64>, d, i)` — validate length d, return the
vec; `corr_object_from_numpy` — identical flatten to SPD (shape (d,d), col-major).

Bind `frechet_mean` with `#[pyo3(signature = (objects, space, d, weights=None))]` where
`objects: &Bound<'py, PyList>` (Python list of per-object numpy arrays), `space: &str`,
`d: usize`, `weights: Option<PyReadonlyArray1<'py, f64>>`. Convert `weights` to
`Option<Vec<f64>>` via `.map(numpy1d_to_vec)` and take `.as_deref()` for the `Option<&[f64]>`
upstream argument. Implement the dispatch as a MONOMORPHIZED `match space { ... }` exactly per
§5 "space_from_str match pattern":
- `"spd"`: collect objects via `spd_object_from_numpy`, construct
  `SpdMatrixSpace::new(d, SpdMetric::Frobenius)` (Frobenius ONLY — Power/LogCholesky deferred;
  do not expose a metric param), validate symmetric + positive diagonal per §5 (|M[i,j]-M[j,i]|
  < 1e-8; flat[i+i*d] > 0), call `frechet_mean::<SpdMatrixSpace>(&space, &objects, weights_ref)`,
  reshape the flat col-major d*d result to a (d,d) numpy 2D via `PyArray2::from_vec2` per §4/§5.
- `"spherical"`: collect via `spherical_object_from_numpy`, validate unit-norm per §5
  (|‖v‖ - 1| < 1e-6, raising a per-object ValueError), construct `SphericalSpace::new(d)`, call
  the monomorphization, return `vec_to_numpy1d(py, mean)` as a (d,) numpy 1D.
- `"correlation"`: collect via `corr_object_from_numpy`, validate symmetric + unit diagonal per
  §5 (|flat[i+i*d]-1.0| < 1e-8), construct `CorrelationMatrixSpace::new(d)`, call the
  monomorphization, reshape to (d,d) numpy 2D.
- `_` wildcard: `Err(PyValueError::new_err(format!("space must be 'spd', 'spherical', or
  'correlation', got '{space}'")))` — lists all valid names (mandatory).
Wrap each `SpaceType::new(...)` and each `frechet_mean(...)` call in `to_pyresult`. The return
type is `PyResult<Bound<'py, PyAny>>` (branches yield PyArray2 or PyArray1 → `.into_any()`).
Access all upstream types via `fdars_core::frechet::{SpdMatrixSpace, SpdMetric, SphericalSpace,
CorrelationMatrixSpace, frechet_mean}` (confirm exact re-export paths against the crate; §5 uses
`fdars_core::frechet::SpdMatrixSpace` etc.). Add `frechet_mean` to `register()`.

Ensure `-D warnings` stays green: every import added is now used (PyArray2, PyReadonlyArray1/2,
PyList). If plan 69-02 left `extract_ragged_vecs` imported-but-unused, either use it here or
drop it — this plan takes numpy arrays per-object, not ragged vecs, so `extract_ragged_vecs` is
likely NOT needed in frechet_mod at all; confirm and remove any dangling import.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.frechet as f; assert callable(f.frechet_mean); print('frechet_mean bound OK')"</automated>
    <fails_when>the crate fails to build (unused import under -D warnings, or a wrong upstream type path), OR frechet_mean is not callable after rebuild.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frechet_mean(objects, space, d, weights=None)` is callable and dispatches via a monomorphized `match` (concrete `frechet_mean::<S>` per arm, not trait objects).
    - SPD uses `SpdMetric::Frobenius` only; no metric parameter is exposed.
    - The wildcard arm raises `ValueError` naming all three valid spaces.
    - Build green under `-D warnings` with no dangling imports.
  </acceptance_criteria>
  <done>frechet_mean is bound with three-space monomorphized dispatch and rebuilds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Per-space frechet_mean tests (spd, spherical, correlation) + invalid-space negative case</name>
  <files>tests/test_frechet.py</files>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§10 SPD/spherical/correlation fixtures with make_spd/make_unit_vec/make_corr; §9 frechet_mean return table)
    - tests/test_frechet.py (append after plan 69-02's density-default tests)
  </read_first>
  <action>
Append per-space tests to `tests/test_frechet.py` using the §10 fixtures:
- `test_mean_spd`: build 5 (3,3) SPD matrices via `make_spd`, call
  `fdars.frechet.frechet_mean(objects_spd, space="spd", d=3)`, assert `result.shape == (3,3)`
  and the result is symmetric (`np.allclose(result, result.T)`).
- `test_mean_spherical`: build 6 (4,) unit vectors via `make_unit_vec`, call with
  `space="spherical", d=4`, assert `result.shape == (4,)` and
  `abs(np.linalg.norm(result) - 1.0) < 1e-4` (result is unit-norm).
- `test_mean_correlation`: build 4 (3,3) correlation matrices via `make_corr`, call with
  `space="correlation", d=3`, assert `result.shape == (3,3)` and unit diagonal
  (`all(abs(result[i,i]-1.0) < 1e-6 for i in range(3))`).
- `test_mean_invalid_space`: call `frechet_mean(objects_spd, space="banana", d=3)` and assert
  `ValueError` whose message contains `spd`, `spherical`, and `correlation`.
- `test_mean_bad_norm`: pass a non-unit-norm spherical object and assert `ValueError` (validates
  the in-binding unit-norm check).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_frechet.py -x -q 2>&1 | tail -25</automated>
    <fails_when>any per-space result has the wrong shape, OR the SPD/correlation result is not symmetric / not unit-diagonal, OR the spherical result is not unit-norm, OR an invalid space string does not raise a ValueError naming the three valid spaces, OR a non-unit-norm spherical object is accepted.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frechet_mean` returns (d,d) for spd/correlation and (d,) unit-norm for spherical.
    - SPD/correlation results satisfy their structural invariants (symmetry; unit diagonal for correlation).
    - Invalid space raises `ValueError` listing all three valid names; non-unit-norm spherical input raises `ValueError`.
    - Full `tests/test_frechet.py` passes (density-default + all three spaces).
  </acceptance_criteria>
  <done>All three metric spaces plus the invalid-space and bad-norm negative paths are covered and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust | Per-object numpy matrices/vectors + a user-controlled space string cross into frechet_mean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-06 | Tampering | Non-unit-norm spherical / non-symmetric SPD / non-unit-diagonal correlation objects | medium | mitigate | Binding validates each object per space (norm/symmetry/diagonal) and raises `ValueError` before the upstream call |
| T-69-07 | Tampering | Wrong object shape (not (d,d) or (d,)) | medium | mitigate | Per-space `*_object_from_numpy` helpers shape-check against `d` and raise `ValueError` |
| T-69-08 | Tampering | Unknown space string | low | mitigate | Monomorphized-match wildcard arm returns a `ValueError` naming valid spaces (no silent fallthrough) |
</threat_model>

<verification>
- `import fdars.frechet` + `frechet_mean` callable across all three spaces.
- `pytest tests/test_frechet.py -x` passes (density-default from 69-02 + three-space + negatives).
- Build green under `-D warnings`; no dangling `extract_ragged_vecs` import in frechet_mod.
</verification>

<success_criteria>
`frechet_mean` supports SPD, spherical, and correlation spaces via monomorphized string dispatch
with a valid-names `Err` wildcard arm, correct per-space return shapes, and in-binding validation
of each object's structural invariants.
</success_criteria>

<output>
Create `.planning/phases/69-frechet-regression-density-fda/69-03-SUMMARY.md` when done.
</output>

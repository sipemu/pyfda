---
phase: 69-frechet-regression-density-fda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/convert.rs
  - src/pace_fpca_mod.rs
  - tests/test_convert_ragged.py
autonomous: true
requirements: [FRE-03]
estimate:
  tokens: 42000
  raw_tokens: 30000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "convert::extract_ragged_vecs exists as pub fn, taking (list, caller_name) and returning Vec<Vec<f64>>"
    - "pace_fpca_mod.rs no longer defines a private extract_list_of_vecs; both call sites use crate::convert::extract_ragged_vecs"
    - "Existing pace_fpca behavior is unchanged — tests/test_pace_fpca.py stays green after the move"
    - "Non-uniform (ragged) per-observation lengths are accepted by the helper (a behavior test proves it)"
  artifacts:
    - src/convert.rs
    - src/pace_fpca_mod.rs
    - tests/test_convert_ragged.py
  key_links:
    - "pace_fpca_mod.rs call sites (lines 127-128) → crate::convert::extract_ragged_vecs(list, \"irreg_fdata_from_lists\")"
    - "PyList/PyTuple imports move from pace_fpca_mod.rs into convert.rs"
---

<objective>
FRE-03: Relocate + rename the private ragged-list conversion helper `extract_list_of_vecs`
(currently in `src/pace_fpca_mod.rs:33-65`) into `src/convert.rs` as a public
`extract_ragged_vecs(list, caller_name: &str)`, add a `caller_name` parameter so error
messages are context-specific, and switch the two pace_fpca call sites to the relocated
helper. Single source of truth — no re-export shim. This is the prerequisite the Fréchet and
density_fda input marshalling (plans 69-02/03/04) depend on.

Purpose: Both new submodules consume this ragged helper; factoring it out first avoids
duplication and lets 69-02..04 import a shared, tested utility.
Output: `convert::extract_ragged_vecs` (public); pace_fpca refactored with no behavior change;
a ragged-input behavior test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Relocate extract_list_of_vecs → convert::extract_ragged_vecs and rewire pace_fpca (end-to-end, one path proven)</name>
  <files>src/convert.rs, src/pace_fpca_mod.rs</files>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§2 — full FRE-03 refactor spec: verbatim relocated function, signature change, call-site + import edits)
    - src/pace_fpca_mod.rs (the private helper at lines 33-65; call sites at 127-128; import line 14)
    - src/convert.rs (destination file; existing imports lines 1-6; existing helpers to sit beside)
  </read_first>
  <action>
Add `use pyo3::types::{PyList, PyTuple};` to `src/convert.rs` (after the existing `use` lines).
`PyValueError` is reachable via `pyo3::prelude::*`; reference it as `pyo3::exceptions::PyValueError`
in the function body (matching existing `to_pyerr`).

In `src/convert.rs`, add `pub fn extract_ragged_vecs(list: &Bound<'_, PyList>, caller_name: &str)
-> PyResult<Vec<Vec<f64>>>`. The body is the verbatim logic from 69-RESEARCH.md §2 "Relocated +
renamed form": iterate `list.iter().enumerate()`, accept each element as a 1-D numpy f64 array
(`item.extract::<numpy::PyReadonlyArray1<f64>>()` → `.as_array().to_vec()`), else a `PyList`
of floats, else a `PyTuple` of floats, else build a `PyValueError` whose message interpolates
`caller_name` and the element index `i` and the offending Python type name. The ONLY change from
the original is the `caller_name` parameter replacing the hard-coded `"irreg_fdata_from_lists"`
string in the error message. Write a doc comment (`///`) per §2 documenting the three accepted
element types and that per-caller length-uniformity validation is the CALLER's responsibility
(the helper intentionally does NOT reject ragged lengths — that is the point).

In `src/pace_fpca_mod.rs`: DELETE the private `fn extract_list_of_vecs(...)` definition
(lines 33-65) entirely. Update BOTH call sites (currently lines 127-128) from
`extract_list_of_vecs(av_list)?` / `extract_list_of_vecs(vl_list)?` to
`crate::convert::extract_ragged_vecs(av_list, "irreg_fdata_from_lists")?` and
`crate::convert::extract_ragged_vecs(vl_list, "irreg_fdata_from_lists")?` respectively — the
`caller_name` literal preserves the pre-refactor error message text.

Fix imports in `pace_fpca_mod.rs`: the import at line 14 is
`use pyo3::types::{PyDict, PyList, PyTuple};`. After the move, `PyTuple` is no longer referenced
in this file; `PyList` IS still referenced (the call sites take `&Bound<PyList>` and the
downcast at lines 119-124 uses `PyList`). Keep `PyDict` and `PyList`; drop `PyTuple`. CI runs
`-D warnings`, so an unused `PyTuple` import fails the build — verify by grepping the file for
`PyTuple` after the edit and confirming zero remaining uses.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -v '^//' src/pace_fpca_mod.rs | grep -c 'fn extract_list_of_vecs' | grep -qx 0 && grep -q 'pub fn extract_ragged_vecs' src/convert.rs && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.pace_fpca as p; assert callable(p.irreg_fdata_from_lists); print('pace_fpca rewired OK')"</automated>
    <fails_when>the old private `extract_list_of_vecs` still exists in pace_fpca_mod.rs, OR `extract_ragged_vecs` is not public in convert.rs, OR the crate fails to build (e.g. unused PyTuple import under -D warnings), OR the fdars.pace_fpca submodule no longer imports.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/convert.rs` defines `pub fn extract_ragged_vecs(list: &Bound<'_, PyList>, caller_name: &str) -> PyResult<Vec<Vec<f64>>>`.
    - `src/pace_fpca_mod.rs` contains no `fn extract_list_of_vecs` definition and both call sites route through `crate::convert::extract_ragged_vecs(..., "irreg_fdata_from_lists")`.
    - `maturin develop` builds green under `-D warnings` (no unused imports).
    - `fdars.pace_fpca.irreg_fdata_from_lists` is importable and callable.
  </acceptance_criteria>
  <done>The helper lives in convert.rs as a public function, pace_fpca uses it, and the extension rebuilds and imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Prove no pace_fpca regression + add a ragged-input behavior test</name>
  <files>tests/test_convert_ragged.py</files>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§2 "Non-uniform-length test to ADD"; §10 "extract_ragged_vecs refactor test" — the irreg_fdata_from_lists ragged path)
    - tests/test_pace_fpca.py (existing suite that must stay green — the behavioral proof of no-regression)
  </read_first>
  <action>
The primary no-regression proof is that the EXISTING `tests/test_pace_fpca.py` still passes
against the refactored build (no source edits to that file).

Additionally create `tests/test_convert_ragged.py` — a Python-level behavior test that exercises
`extract_ragged_vecs` INDIRECTLY through its only public consumer,
`fdars.pace_fpca.irreg_fdata_from_lists`, on NON-UNIFORM (ragged) per-curve lengths (the helper
is not itself exposed to Python; the Rust `#[test]` in §2 is optional and NOT required by this
plan). Build two lists per 69-RESEARCH.md §10: `av_list = [np.array([0.0,0.5,1.0]),
np.array([0.0,0.25,0.5,0.75,1.0])]` and a matching-length `vl_list`, call
`irreg_fdata_from_lists(av_list, vl_list)`, and assert it returns without error (a ragged input
is accepted, proving the relocated helper preserves ragged acceptance). Add a second case mixing
a plain Python list of floats and a tuple as elements (both accepted element types) to cover the
non-numpy branches. Add a negative case: pass an element of an unsupported type (e.g. a string)
and assert `ValueError` is raised whose message contains `irreg_fdata_from_lists` (proving the
`caller_name` threads into the error text).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_pace_fpca.py tests/test_convert_ragged.py -x -q 2>&1 | tail -15</automated>
    <fails_when>the existing pace_fpca tests regress after the refactor, OR the new ragged test fails because ragged input is rejected, OR the negative-case ValueError message no longer contains the caller_name `irreg_fdata_from_lists`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_pace_fpca.py` passes unchanged (no-regression proof for FRE-03).
    - `tests/test_convert_ragged.py` proves ragged (non-uniform-length) input is accepted and that an unsupported element type raises `ValueError` mentioning `irreg_fdata_from_lists`.
    - Both files pass under `pytest -x`.
  </acceptance_criteria>
  <done>Existing pace_fpca tests stay green and a new ragged-input behavior test passes, confirming the relocation is behavior-preserving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust (numpy/list marshalling) | Untrusted ragged list/array input crosses into `extract_ragged_vecs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Tampering | `extract_ragged_vecs` element extraction | medium | mitigate | Unsupported element type returns a `PyValueError` (context-named via `caller_name`), not a panic — preserved from the original helper |
| T-69-02 | Denial of Service | Behavior change breaking pace_fpca | low | mitigate | Existing `tests/test_pace_fpca.py` re-run unchanged gates the refactor as behavior-preserving |
</threat_model>

<verification>
- `maturin develop` builds green under `-D warnings` (no unused `PyTuple` import).
- `pytest tests/test_pace_fpca.py tests/test_convert_ragged.py -x` passes.
- `grep 'fn extract_list_of_vecs' src/pace_fpca_mod.rs` returns nothing (excluding comments); `grep 'pub fn extract_ragged_vecs' src/convert.rs` matches.
</verification>

<success_criteria>
`convert::extract_ragged_vecs` is the single public source of the ragged-list helper, pace_fpca
consumes it with no behavior change (existing tests green), and ragged input acceptance is
covered by a behavior test.
</success_criteria>

<output>
Create `.planning/phases/69-frechet-regression-density-fda/69-01-SUMMARY.md` when done.
</output>

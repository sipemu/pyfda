---
phase: 69-frechet-regression-density-fda
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - src/frechet_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_frechet.py
autonomous: true
requirements: [FRE-01]
estimate:
  tokens: 55000
  raw_tokens: 34000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "import fdars.frechet works — the submodule is registered in lib.rs and __init__.py"
    - "frechet_anova(responses, argvals, group_labels) returns a 9-key PyDict with a permutation p-value in [0,1]"
    - "frechet_global_reg / frechet_local_reg return a PyDict whose 'predicted' is numpy 2D (n_out, m), transposition-correct on non-square input"
    - "Group labels that are not contiguous 0..k raise ValueError"
  artifacts:
    - src/frechet_mod.rs
    - tests/test_frechet.py
  key_links:
    - "lib.rs → register_submodule!(m, \"frechet\", frechet_mod::register)"
    - "__init__.py _submodule_names → \"frechet\""
    - "numpy2d_to_fdmatrix / numpy1d_to_vec / numpy1d_to_usize_vec (convert.rs) feed the density-default inputs"
---

<objective>
FRE-01 (part 1 of 2): Create the new `src/frechet_mod.rs` submodule, register it end-to-end
(`fdars.frechet` importable), and bind the three DENSITY-DEFAULT Fréchet functions
(`frechet_anova`, `frechet_global_reg`, `frechet_local_reg`) — the non-generic
density/distribution-response variants with clean numpy-2D I/O. `frechet_mean` (the generic
3-space dispatch) is deferred to plan 69-03 which appends to this same file.

Purpose: Establish the frechet submodule skeleton on a proven end-to-end path (tracer:
frechet_anova) before fanning out to the two regression functions, catching any registration /
marshalling dead-end after one commit.
Output: `fdars.frechet` submodule with 3 density-default functions, each returning a documented
PyDict per 69-RESEARCH.md §9.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/scalar_on_function_mod.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — register fdars.frechet + bind frechet_anova end-to-end (density-default, one path through every layer)</name>
  <files>src/frechet_mod.rs, src/lib.rs, python/fdars/__init__.py, tests/test_frechet.py</files>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§3 frechet_anova signature+validation; §4 FrechetAnovaResult 9 fields; §8 frechet_mod.rs skeleton + lib.rs/__init__.py edits; §9 frechet_anova PyDict key table; §10 density-default fixtures)
    - src/lib.rs (mod list + register_submodule! block — where the 2 new lines go)
    - python/fdars/__init__.py (_submodule_names tuple lines 36-59 + module docstring line 20-21)
    - src/scalar_on_function_mod.rs (analog: PyDict assembly, convert::* imports, register() pattern)
    - src/convert.rs (numpy2d_to_fdmatrix, numpy1d_to_vec, numpy1d_to_usize_vec, usize_vec_to_numpy1d, vec_to_numpy1d, to_pyresult)
  </read_first>
  <action>
Create `src/frechet_mod.rs` with the module doc comment and imports from 69-RESEARCH.md §8
skeleton (bring in `extract_ragged_vecs`, `fdmatrix_to_numpy2d`, `numpy1d_to_vec`,
`numpy2d_to_fdmatrix`, `to_pyresult`, `usize_vec_to_numpy1d`, `vec_to_numpy1d` from
`crate::convert`; numpy array types; `PyDict`, `PyList`; `PyValueError`). Only import what THIS
plan uses to keep `-D warnings` green — `PyArray2`/`PyList` and `extract_ragged_vecs` are used by
plan 69-03's frechet_mean, so if they are unused after this plan, gate them behind that plan (add
them when 69-03 lands, OR add `#[allow(unused_imports)]`-free by importing per-function). Prefer:
import in this plan only what tasks 1-3 reference; 69-03 adds its own imports.

Register the submodule end-to-end:
- `src/lib.rs`: add `mod frechet_mod;` after `mod scalar_on_function_mod;` (line ~30), and
  `register_submodule!(m, "frechet", frechet_mod::register);` after the scalar_on_function
  registration (line ~66).
- `python/fdars/__init__.py`: add `"frechet",  # Phase 69 — Fréchet regression & mean over metric spaces`
  to the `_submodule_names` tuple after the `scalar_on_function` entry (line ~58), and add the
  Fréchet bullet to the module docstring per §8.

Bind `frechet_anova` per §3 signature: Python signature
`frechet_anova(responses, argvals, group_labels, n_perm=999, seed=42)`. Marshalling:
`responses` (PyReadonlyArray2 f64) → `numpy2d_to_fdmatrix`; `argvals` (PyReadonlyArray1 f64) →
`numpy1d_to_vec`; `group_labels` (PyReadonlyArray1 i64) → `numpy1d_to_usize_vec`. Use
`#[pyo3(signature = (responses, argvals, group_labels, n_perm=999, seed=42))]`. PRE-VALIDATE
(per Pitfall 4): if the distinct group labels are not contiguous 0..k, raise `PyValueError`
with a message clarifying that labels must be contiguous integers starting at 0 (do this before
the upstream call so the message is actionable). Call
`fdars_core::frechet::frechet_anova(&responses, &argvals, &group_labels, n_perm, seed)` wrapped
in `to_pyresult`. Build the 9-key PyDict EXACTLY per §9 frechet_anova table: `statistic`,
`p_value_asymptotic`, `p_value_permutation` (all f64); `n_perm` (`result.n_perm as i64`);
`group_frechet_variances` (`vec_to_numpy1d`); `pooled_frechet_variance`, `fn_statistic`,
`un_statistic` (f64); `group_labels` (`usize_vec_to_numpy1d(py, result.group_labels)`). Access
result fields by name only — `FrechetAnovaResult` is `#[non_exhaustive]` (no struct literals).

Add `register(m)` per §8 skeleton wiring only `frechet_anova` for now (tasks 2-3 append the
other two — keep register() growing as functions land, OR wire all three names now and let the
build fail until tasks 2-3 add the fns; prefer adding each function to register() in the task
that creates it).

Write the tracer test in `tests/test_frechet.py`: build the density-default + 3-group fixtures
from §10 (N=40, M=50, N_OUT=10, N_PRED=2; contiguous group_labels 0,1,2), call `frechet_anova`,
assert the returned dict has the 9 keys, that `0.0 <= result["p_value_permutation"] <= 1.0`, and
that `result["group_frechet_variances"].shape == (3,)`. Add a NEGATIVE case: non-contiguous
labels (e.g. [0,1,3]) raise `ValueError`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.frechet as f; assert callable(f.frechet_anova); print('fdars.frechet + frechet_anova wired OK')" && .venv/bin/pytest tests/test_frechet.py -x -q 2>&1 | tail -15</automated>
    <fails_when>the frechet submodule is not registered (import fails), OR frechet_anova is not callable, OR the returned dict is missing any of the 9 keys, OR the permutation p-value is outside [0,1], OR non-contiguous group labels do not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - `import fdars.frechet` succeeds and `frechet_anova` is callable.
    - `lib.rs` has the `mod frechet_mod;` + `register_submodule!(..., "frechet", ...)` lines; `__init__.py` `_submodule_names` includes `"frechet"`.
    - `frechet_anova` returns the exact 9-key dict from §9 with a permutation p-value in [0,1].
    - Non-contiguous group labels raise `ValueError`.
    - Build green under `-D warnings`.
  </acceptance_criteria>
  <done>The fdars.frechet submodule is registered and one density-default function (frechet_anova) works end-to-end with a passing non-square test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind frechet_global_reg (density-default, 3-key PyDict, transposition-correct)</name>
  <files>src/frechet_mod.rs, tests/test_frechet.py</files>
  <behavior>
    - frechet_global_reg(predictors(N,p), responses(N,M), argvals(M,), xout(N_OUT,p)) returns dict with keys predicted, xout, x_bar.
    - result["predicted"].shape == (N_OUT, M) — NOT (M, N_OUT) (Pitfall 2).
    - result["xout"].shape == (N_OUT, p); result["x_bar"].shape == (p,).
    - Mismatched argvals length (argvals.len() != responses.ncols()) raises ValueError (upstream validation via to_pyresult).
  </behavior>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§3 frechet_global_reg signature+validation+KEY CORRECTNESS NOTE; §4 FrechetGlobalRegResult 3 fields; §9 global_reg PyDict table; §10 fixtures; Pitfall 1 & 2)
    - src/frechet_mod.rs (the file created in Task 1 — append here)
  </read_first>
  <action>
Bind `frechet_global_reg(predictors, responses, argvals, xout)` per §3: all three 2D args
(`predictors`, `responses`, `xout`) via `numpy2d_to_fdmatrix`; `argvals` via `numpy1d_to_vec`.
Call `fdars_core::frechet::frechet_global_reg(&predictors, &responses, &argvals, &xout)` wrapped
in `to_pyresult`. Build the 3-key PyDict per §9: `predicted` (`fdmatrix_to_numpy2d`, shape
(n_out, m)), `xout` (`fdmatrix_to_numpy2d`, shape (n_out, p)), `x_bar` (`vec_to_numpy1d`, shape
(p,)). Fields by name only (`#[non_exhaustive]`). In the docstring, note per Pitfall 1 that this
uses signed-weight global linear weights (NOT a Wasserstein barycenter) — do not reimplement;
just call upstream. Add `frechet_global_reg` to `register()`.

Append a test to `tests/test_frechet.py` reusing the §10 density-default fixtures: assert the
3 keys, `result["predicted"].shape == (N_OUT, M)`, `result["xout"].shape == (N_OUT, N_PRED)`,
`result["x_bar"].shape == (N_PRED,)`. Add a negative case: pass an `argvals` whose length !=
responses.shape[1] and assert `ValueError`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_frechet.py -x -q 2>&1 | tail -15</automated>
    <fails_when>frechet_global_reg is missing a dict key, OR result["predicted"] has shape (M, N_OUT) instead of (N_OUT, M), OR a mismatched-argvals input does not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frechet_global_reg` returns the exact 3-key dict {predicted, xout, x_bar} from §9.
    - `predicted` is (N_OUT, M), `xout` is (N_OUT, p), `x_bar` is (p,).
    - Mismatched `argvals` length raises `ValueError` via upstream validation.
  </acceptance_criteria>
  <done>frechet_global_reg is bound, transposition-correct, and covered by a passing non-square test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind frechet_local_reg (density-default, bandwidth positional, 3-key PyDict)</name>
  <files>src/frechet_mod.rs, tests/test_frechet.py</files>
  <behavior>
    - frechet_local_reg(predictors, responses, argvals, xout, bandwidth) returns dict {predicted, xout, bandwidth}.
    - result["predicted"].shape == (N_OUT, M); result["bandwidth"] == the bandwidth passed.
    - bandwidth <= 0.0 raises ValueError (upstream validation).
  </behavior>
  <read_first>
    - .planning/phases/69-frechet-regression-density-fda/69-RESEARCH.md (§3 frechet_local_reg signature+validation; §4 FrechetLocalRegResult 3 fields; §9 local_reg PyDict table; §10 fixtures)
    - src/frechet_mod.rs (append after Task 2)
  </read_first>
  <action>
Bind `frechet_local_reg(predictors, responses, argvals, xout, bandwidth)` per §3: 2D args via
`numpy2d_to_fdmatrix`; `argvals` via `numpy1d_to_vec`; `bandwidth: f64` REQUIRED positional (no
default — no sensible default exists for the user's data scale). Call
`fdars_core::frechet::frechet_local_reg(&predictors, &responses, &argvals, &xout, bandwidth)`
wrapped in `to_pyresult`. Build the 3-key PyDict per §9: `predicted` (`fdmatrix_to_numpy2d`),
`xout` (`fdmatrix_to_numpy2d`), `bandwidth` (`result.bandwidth` as f64). Fields by name only.
Add `frechet_local_reg` to `register()`.

Append a test to `tests/test_frechet.py` reusing the §10 fixtures with a concrete `bandwidth`
(e.g. 0.5): assert the 3 keys, `result["predicted"].shape == (N_OUT, M)`, and
`result["bandwidth"] == 0.5`. Add a negative case: `bandwidth=-1.0` (or 0.0) raises `ValueError`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_frechet.py -x -q 2>&1 | tail -20</automated>
    <fails_when>frechet_local_reg is missing a dict key, OR predicted is not (N_OUT, M), OR the echoed bandwidth differs from the input, OR a non-positive bandwidth does not raise ValueError.</fails_when>
  </verify>
  <acceptance_criteria>
    - `frechet_local_reg` returns {predicted, xout, bandwidth}; predicted is (N_OUT, M); bandwidth echoes the input.
    - Non-positive bandwidth raises `ValueError`.
    - All of `tests/test_frechet.py` passes (anova + global + local).
  </acceptance_criteria>
  <done>frechet_local_reg is bound with required-positional bandwidth, and the full density-default frechet test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → Rust | numpy 2D density/predictor matrices + group labels cross into the frechet bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-03 | Tampering | Invalid numpy shapes / non-monotone argvals | medium | mitigate | Upstream `validate_reg_input` returns `FdarError` → `PyValueError` via `to_pyresult` (no panic) |
| T-69-04 | Tampering | Non-contiguous frechet_anova group labels | medium | mitigate | Binding pre-validates contiguous 0..k and raises an actionable `ValueError` before the upstream call |
| T-69-05 | Information Disclosure | Wrong transposition leaking mislabeled results | low | mitigate | Non-square (N≠M≠N_OUT) fixtures assert exact `(n_out, m)` shapes |
</threat_model>

<verification>
- `import fdars.frechet` works; `frechet_anova`, `frechet_global_reg`, `frechet_local_reg` callable.
- `pytest tests/test_frechet.py -x` passes with non-square shape assertions.
- Build green under `-D warnings` (no unused imports left for plan 69-03's frechet_mean).
</verification>

<success_criteria>
The `fdars.frechet` submodule is registered and the three density-default functions each return
their exact documented PyDict, transposition-correct on non-square input, with negative-path
validation raising `ValueError`.
</success_criteria>

<output>
Create `.planning/phases/69-frechet-regression-density-fda/69-02-SUMMARY.md` when done.
</output>

---
phase: 21-per-aspect-advisor-coverage
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - python/fdars/advisor/aspects/_utils.py
  - python/fdars/advisor/aspects/represent.py
  - python/fdars/advisor/aspects/fpca.py
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor.py
autonomous: true
requirements: [ASPECT-01, ASPECT-06]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A single shared _eigenvalues_to_variance_cumulative helper exists in advisor/aspects/_utils.py, reused (not copied) by fpca and later spm (ASPECT-01 supporting)"
    - "build_diagnostics(fdata_dict_or_Fdata, method='represent') returns deterministic data-quality diagnostics; represent is a NEW method string, not an extension of basis/fpca (ASPECT-01)"
    - "_system_prompt('interpretation','represent') carries a represent-specific clause (ASPECT-06)"
  artifacts:
    - python/fdars/advisor/aspects/_utils.py
    - python/fdars/advisor/aspects/represent.py
  key_links:
    - "_utils._eigenvalues_to_variance_cumulative <- imported by fpca.py (refactor) and later spm.py"
    - "dispatcher _supported gains 'represent' -> lazy builder import"
---

<objective>
Introduce the shared `advisor/aspects/_utils.py` scaffold with the single canonical `_eigenvalues_to_variance_cumulative` helper (per the CONTEXT locked decision: do NOT duplicate the eigenvalue→variance code across files), refactor `fpca.py` to use it (a safe, pure-logic extract that MUST preserve fpca's existing output byte-for-byte), and add the `represent` aspect (ASPECT-01) as its own NEW method string — a pre-analysis data-quality check over the raw data matrix + evaluation grid, per 21-RESEARCH §4.

Decision applied (21-RESEARCH §4): `represent` is a NEW method string, NOT an extension of `basis`/`fpca` — it operates on INPUT data (data + argvals), while basis/fpca operate on method OUTPUTS. `represent` itself does NOT use the eigenvalue helper (RESEARCH §1d) — only fpca and spm do — but this plan establishes `_utils.py` here so plan 05 (spm) can import it.

Purpose: land the shared helper + first output-preserving refactor + the represent aspect.
Output: `_utils.py`, `represent.py`, refactored `fpca.py`, represent prompt clause, determinism tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-aspect-advisor-coverage/21-RESEARCH.md

@python/fdars/advisor/aspects/fpca.py
@python/fdars/advisor/aspects/depth.py
@python/fdars/advisor/__init__.py
@python/fdars/advisor/_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _utils.py shared helper + output-preserving fpca refactor</name>
  <files>python/fdars/advisor/aspects/_utils.py, python/fdars/advisor/aspects/fpca.py, tests/test_advisor.py</files>
  <behavior>
    - _eigenvalues_to_variance_cumulative(np.array([2.1,0.8,0.3])) returns a list of 3 native floats, monotonically non-decreasing, last≈1.0.
    - Zero-sum input returns [0.0]*len (no divide-by-zero).
    - After the fpca refactor, build_diagnostics(fpca_fixture, "fpca") returns the SAME dict as before the refactor (regression guard).
  </behavior>
  <read_first>python/fdars/advisor/aspects/fpca.py lines 36-51 (eigenvalue to variance logic), 21-RESEARCH.md §3 (shared helper spec)</read_first>
  <action>Create `python/fdars/advisor/aspects/_utils.py` with `from __future__ import annotations`, `import numpy as np`, and `_eigenvalues_to_variance_cumulative(eigenvalues) -> list` per 21-RESEARCH §3: cast to a float ndarray, compute total=float(ev.sum()); if total<=0 return [0.0]*len(ev); else evr=ev/total and return [float(v) for v in np.cumsum(evr)]. The helper takes ALREADY-SCALED eigenvalues (no sv-squared/(n-1) step). Then refactor `fpca.py`: fpca still computes eigenvalues from singular values (sv**2/denom) FIRST, then feeds those eigenvalues into the shared helper to obtain cumulative_variance_explained — replacing only the local np.cumsum(evr) line while leaving explained_variance_ratio and every other fpca field exactly as-is. The refactor MUST NOT change fpca's output dict. Add a `test_utils_eigenvalues_variance` unit test and a `test_fpca_output_unchanged_after_refactor` regression test (capture the expected dict inline from a fixed fpca fixture and assert equality) to `tests/test_advisor.py`.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor.py -k "utils_eigenvalues or fpca" -x -q</automated>
  </verify>
  <done>Shared helper exists and is byte-stable; fpca output is unchanged after the refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: represent builder (new method string) + dispatch + prompt clause</name>
  <files>python/fdars/advisor/aspects/represent.py, python/fdars/advisor/__init__.py, python/fdars/advisor/_prompts.py</files>
  <behavior>
    - build_diagnostics({"data": np.ones((20,50)), "argvals": np.linspace(0,1,50)}, "represent") -> n_obs=20, n_points=50, is_uniform_grid==True, argvals_min≈0.0, argvals_max≈1.0.
    - An Fdata-like object exposing .data and .argvals attributes yields the same fields (transparent handling).
    - All values native Python types; json.dumps(sort_keys=True) succeeds.
  </behavior>
  <read_first>python/fdars/advisor/aspects/depth.py (builder shape), 21-RESEARCH.md §1d and §4 (represent field list + input API)</read_first>
  <action>Create `python/fdars/advisor/aspects/represent.py` with `_build_represent_diagnostics(raw, **kwargs) -> dict` per 21-RESEARCH §1d. Accept either a dict with keys "data" (n×m) and "argvals" (m,) OR an Fdata object with .data/.argvals attributes: resolve via `data = getattr(raw, "data", None); if data is None and isinstance(raw, dict): data = raw.get("data")` (same for argvals) — per RESEARCH open-question #2. Compute the §1d fields: n_obs, n_points, argvals_min/max, argvals_spacing_mean/std (via np.diff; None when len<2), is_uniform_grid (spacing_std/spacing_mean < 0.01 when spacing_mean>0 else True), data_range_min/max/mean. NO fdars call, NO eigenvalue helper (represent is a data-quality check, not a decomposition). Guard missing keys -> emit None. Add "represent" to `_supported` + dispatch branch in `__init__.py`. Add a "represent" primer to `_ASPECT_PRIMERS` per 21-RESEARCH §5 (n_points as grid resolution, is_uniform_grid, sparse/irregular grids may need pre-smoothing — the clause MUST contain the token is_uniform_grid for the test).</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json; from fdars.advisor import build_diagnostics as b; d=b({'data':np.ones((20,50)),'argvals':np.linspace(0,1,50)},'represent'); assert d['n_obs']==20 and d['n_points']==50 and d['is_uniform_grid'] is True and abs(d['argvals_max']-1.0)<1e-9; json.dumps(d,sort_keys=True); from fdars.advisor._prompts import _system_prompt; assert 'is_uniform_grid' in _system_prompt('interpretation','represent'); print('OK')"</automated>
  </verify>
  <done>represent works as a new method string over data+argvals input (dict or Fdata); dispatch + prompt clause wired; ASPECT-01 covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: represent determinism test</name>
  <files>tests/test_advisor.py</files>
  <behavior>
    - test_represent_deterministic: the np.ones((20,50)) + linspace fixture (no RNG) -> equal dicts, byte-identical json.dumps(sort_keys=True), no numpy scalars.
    - TestPrompts assertion: represent clause appears only for aspect='represent'.
  </behavior>
  <read_first>tests/test_advisor.py (TestBuildDiagnosticsOffline + check_no_numpy walker), 21-RESEARCH.md §8 (represent fixture — prefer np.ones to avoid RNG)</read_first>
  <action>Add `test_represent_deterministic` to `TestBuildDiagnosticsOffline` using the deterministic np.ones((20,50)) + np.linspace(0,1,50) fixture from 21-RESEARCH §8 (avoid the RNG fixture to keep the test byte-stable). Reuse the recursive check_no_numpy walker: two build_diagnostics calls -> assert equal, assert byte-identical json.dumps(sort_keys=True), assert no numpy scalar leaks. Extend TestPrompts to assert the represent clause is present only when aspect='represent'.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_represent_deterministic -x -q</automated>
  </verify>
  <done>represent determinism test passes; full advisor offline suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller data/argvals -> represent builder | Untrusted data matrix + grid crosses into the offline builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering | represent builder | low | mitigate | Guard data/argvals resolution for both dict and Fdata inputs; emit None on missing; validate array shapes before NumPy ops (ASVS V5) |
| T-21-05 | Denial of Service | fpca refactor | low | mitigate | Output-equality regression test prevents a silent behavior change in a shipped aspect |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor.py -q` passes (fpca unchanged; represent added).
- No new package installs.
</verification>

<success_criteria>
- Single shared eigenvalue helper in _utils.py; fpca refactored with identical output.
- ASPECT-01 covered by represent as a new method string.
</success_criteria>

<output>
Create `.planning/phases/21-per-aspect-advisor-coverage/21-03-SUMMARY.md` when done.
</output>

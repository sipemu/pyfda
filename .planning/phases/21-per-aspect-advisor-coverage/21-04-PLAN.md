---
phase: 21-per-aspect-advisor-coverage
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - python/fdars/advisor/aspects/regression.py
  - python/fdars/advisor/aspects/regression_cv.py
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor.py
autonomous: true
requirements: [ASPECT-04, ASPECT-06]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "build_diagnostics(regr_result, method='regression') handles fregre_lm/pls/l1/huber/np/fosr shapes with guarded r_squared and 1-D-residual checks (ASPECT-04)"
    - "build_diagnostics(cv_result, method='regression_cv') handles both fregre_cv and model_selection_ncomp shapes with elbow detection (ASPECT-04)"
    - "_system_prompt('interpretation','regression') and (…, 'regression_cv') each carry an aspect-specific clause (ASPECT-06)"
  artifacts:
    - python/fdars/advisor/aspects/regression.py
    - python/fdars/advisor/aspects/regression_cv.py
  key_links:
    - "dispatcher _supported gains 'regression','regression_cv' -> lazy builder imports"
    - "_ASPECT_PRIMERS gains regression + regression_cv entries"
---

<objective>
Add the two MEDIUM-complexity aspects: `regression` and `regression_cv` (ASPECT-04). These test the optional-key-guarding discipline hardest, because RESEARCH found four regression discrepancies (corrections #3, #4, #5, #7): `fregre_l1`/`fregre_huber` have NO `r_squared`; `fosr`/`fosr_fpc` return 2-D `residuals` and use key `"fitted"` not `"fitted_values"`; `fregre_cv` returns numpy arrays (not lists) for `cv_errors`/`k_values`; and `model_selection_ncomp` returns `criteria` tuples instead of top-level cv arrays.

Purpose: build out both regression aspects with pure-NumPy skewness and elbow detection (no scipy, no signal library — per Don't-Hand-Roll).
Output: regression + regression_cv builders, prompt clauses, determinism tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-aspect-advisor-coverage/21-RESEARCH.md

@python/fdars/advisor/aspects/depth.py
@python/fdars/advisor/__init__.py
@python/fdars/advisor/_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: regression builder + dispatch + prompt clause (corrections #3/#4/#5)</name>
  <files>python/fdars/advisor/aspects/regression.py, python/fdars/advisor/__init__.py, python/fdars/advisor/_prompts.py</files>
  <behavior>
    - build_diagnostics(fregre_lm_result, "regression") -> r_squared=0.82, residual_mean/std/max_abs/skew present, beta_t_range present, has_fosr==False.
    - build_diagnostics(fregre_l1_result, "regression") (NO r_squared key) -> r_squared is None, no KeyError.
    - A 2-D-residuals (fosr) fixture -> residual stats are None (guarded on ndim==1), has_fosr==True.
    - All native types; json.dumps(sort_keys=True) succeeds.
  </behavior>
  <read_first>python/fdars/advisor/aspects/depth.py, 21-RESEARCH.md §1e (six regression shapes + skewness formula) and §2 (corrections #3/#4/#5)</read_first>
  <action>Create `python/fdars/advisor/aspects/regression.py` with `_build_regression_diagnostics(raw, **kwargs) -> dict` per 21-RESEARCH §1e. Emit: n_obs (from fitted_values shape[0], else fitted shape[0] for fosr), r_squared guarded via `float(raw["r_squared"]) if "r_squared" in raw else None` (correction #3 — l1/huber lack it). Residual stats (residual_mean/std/max_abs/skew) computed ONLY when residuals are present AND 1-D: `res = np.asarray(raw.get("residuals", [])); if res.ndim == 1 and res.size:` (correction #4 — fosr residuals are 2-D). Skewness pure-NumPy (Don't-Hand-Roll): m3=np.mean((r-r.mean())**3); m2=np.var(r); skew=m3/m2**1.5 if m2>0 else 0.0. beta_t_range=[min,max] when "beta_t" in raw. has_fosr = bool("fitted" in raw and np.asarray(raw["fitted"]).ndim==2) (correction #5 — fosr uses key "fitted"). Guard every key. Add "regression" to `_supported` + dispatch branch. Add a "regression" primer to `_ASPECT_PRIMERS` per 21-RESEARCH §5 (r_squared goodness-of-fit, residual_skew, beta_t functional coefficient — the clause MUST contain the token r_squared for the test).</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json; from fdars.advisor import build_diagnostics as b; lm=b({'fitted_values':np.array([1.1,2.,3.2,.9,2.5]),'residuals':np.array([.1,-.2,.3,-.1,.2]),'beta_t':np.linspace(-.5,.5,30),'r_squared':0.82},'regression'); assert abs(lm['r_squared']-0.82)<1e-9 and lm['has_fosr'] is False and lm['residual_skew'] is not None; l1=b({'fitted_values':np.array([1.,2.1,3.,1.,2.4]),'residuals':np.array([.2,-.3,.5,-.2,.3]),'beta_t':np.linspace(-.3,.3,30)},'regression'); assert l1['r_squared'] is None; fo=b({'fitted':np.ones((4,6)),'residuals':np.zeros((4,6)),'r_squared':0.5},'regression'); assert fo['has_fosr'] is True and fo['residual_mean'] is None; json.dumps(lm,sort_keys=True); from fdars.advisor._prompts import _system_prompt; assert 'r_squared' in _system_prompt('interpretation','regression'); print('OK')"</automated>
  </verify>
  <done>regression builder handles all six shapes with guarded r_squared + 1-D residual guard + fosr detection; dispatch + prompt clause wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: regression_cv builder + dispatch + prompt clause (corrections #7)</name>
  <files>python/fdars/advisor/aspects/regression_cv.py, python/fdars/advisor/__init__.py, python/fdars/advisor/_prompts.py</files>
  <behavior>
    - build_diagnostics(fregre_cv_result, "regression_cv") -> optimal_k=3, min_cv_error=0.045, cv_curve list of native floats, k_values list of native ints, elbow_present==True (min at interior index).
    - build_diagnostics(model_selection_ncomp_result, "regression_cv") -> optimal_k from best_ncomp, cv_curve extracted from criteria tuples (GCV = tuple index 3).
    - All native types; json.dumps(sort_keys=True) succeeds.
  </behavior>
  <read_first>python/fdars/advisor/aspects/depth.py, 21-RESEARCH.md §1f (fregre_cv + model_selection_ncomp keys) and Don't-Hand-Roll (elbow rule)</read_first>
  <action>Create `python/fdars/advisor/aspects/regression_cv.py` with `_build_regression_cv_diagnostics(raw, **kwargs) -> dict` per 21-RESEARCH §1f. Handle BOTH source functions. For `fregre_cv`: optimal_k=int(raw["optimal_k"]), min_cv_error=float(raw["min_cv_error"]), cv_curve=[float(v) for v in raw["cv_errors"]] (correction #7 — cast numpy array to list), k_values=[int(v) for v in raw["k_values"]]. For `model_selection_ncomp` (no top-level cv arrays): optimal_k=int(raw["best_ncomp"]), and extract cv_curve/k_values from the `criteria` list of (ncomp, aic, bic, gcv) tuples — k_values=[c[0] for c in criteria], cv_curve=[c[3] for c in criteria] (GCV). Guard which keys exist. cv_curve_range=[min,max] when cv_curve non-empty. elbow_present (Don't-Hand-Roll): True when the curve has an interior local minimum (a strict minimum NOT at index 0 or -1). Add "regression_cv" to `_supported` + dispatch branch. Add a "regression_cv" primer to `_ASPECT_PRIMERS` per 21-RESEARCH §5 (optimal_k minimising CV error, elbow_present, boundary-optimal_k warning — the clause MUST contain the token optimal_k for the test).</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json; from fdars.advisor import build_diagnostics as b; cv=b({'optimal_k':3,'min_cv_error':0.045,'k_values':np.array([1,2,3,4,5]),'cv_errors':np.array([.12,.07,.045,.046,.048]),'fold_errors':np.array([.04,.05,.04,.05,.04])},'regression_cv'); assert cv['optimal_k']==3 and abs(cv['min_cv_error']-0.045)<1e-9 and cv['k_values']==[1,2,3,4,5] and cv['elbow_present'] is True; ms=b({'best_ncomp':2,'criteria':[(1,10.,11.,0.5),(2,9.,10.,0.3),(3,9.5,10.5,0.35)]},'regression_cv'); assert ms['optimal_k']==2 and ms['k_values']==[1,2,3] and abs(ms['cv_curve'][1]-0.3)<1e-9; json.dumps(cv,sort_keys=True); from fdars.advisor._prompts import _system_prompt; assert 'optimal_k' in _system_prompt('interpretation','regression_cv'); print('OK')"</automated>
  </verify>
  <done>regression_cv builder handles fregre_cv + model_selection_ncomp with array-to-list casts + elbow detection; dispatch + prompt clause wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: determinism tests for regression + regression_cv</name>
  <files>tests/test_advisor.py</files>
  <behavior>
    - test_regression_deterministic: fregre_lm fixture -> equal dicts, byte-identical json.dumps(sort_keys=True), no numpy scalars; second assertion on l1 fixture confirms r_squared=None path.
    - test_regression_cv_deterministic: fregre_cv fixture -> same guarantees; elbow_present asserted.
    - TestPrompts: regression + regression_cv clauses appear only for their aspect.
  </behavior>
  <read_first>tests/test_advisor.py (TestBuildDiagnosticsOffline + check_no_numpy walker), 21-RESEARCH.md §8 (regression + regression_cv fixtures)</read_first>
  <action>Add `test_regression_deterministic` and `test_regression_cv_deterministic` to `TestBuildDiagnosticsOffline`, using the synthetic fixtures from 21-RESEARCH §8 (fregre_lm + fregre_l1 for regression; fregre_cv for regression_cv). Reuse check_no_numpy: two calls -> equal, byte-identical json.dumps(sort_keys=True), no numpy scalars. Include the l1 (no-r_squared) assertion and the elbow_present assertion. Extend TestPrompts with regression/regression_cv clause assertions.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_regression_deterministic tests/test_advisor.py::TestBuildDiagnosticsOffline::test_regression_cv_deterministic -x -q</automated>
  </verify>
  <done>Both determinism tests pass; full advisor offline suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller regression result dict -> builders | Variably-shaped result dict (six regression variants) crosses into offline builders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering | regression builders | low | mitigate | Guard r_squared, residual ndim, fitted-vs-fitted_values, cv array casts; emit None on missing keys (ASVS V5) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor.py -q` passes.
- No new package installs; skewness + elbow are pure NumPy (no scipy).
</verification>

<success_criteria>
- ASPECT-04 covered by regression + regression_cv.
- Corrections #3/#4/#5/#7 applied: guarded r_squared, 1-D residual guard, fosr "fitted" detection, cv array-to-list casts, criteria-tuple handling.
</success_criteria>

<output>
Create `.planning/phases/21-per-aspect-advisor-coverage/21-04-SUMMARY.md` when done.
</output>

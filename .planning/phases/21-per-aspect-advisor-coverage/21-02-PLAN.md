---
phase: 21-per-aspect-advisor-coverage
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - python/fdars/advisor/aspects/outliers.py
  - python/fdars/advisor/aspects/classification.py
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor.py
autonomous: true
requirements: [ASPECT-02, ASPECT-03, ASPECT-06]
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "build_diagnostics(lrt_result, method='outliers') returns deterministic diagnostics; handles all four outlier shapes (LRT, outliergram, magnitude_shape) via key-presence guards (ASPECT-02)"
    - "build_diagnostics(clf_result, method='classification') handles both point-estimate (accuracy) and CV (error_rate) result shapes (ASPECT-03)"
    - "n_classes is an explicit, documented parameter on build_diagnostics() forwarded only to the classification branch; other aspects' signatures are unchanged (ASPECT-03)"
    - "_system_prompt('interpretation','outliers') and (…, 'classification') each carry an aspect-specific clause (ASPECT-06)"
  artifacts:
    - python/fdars/advisor/aspects/outliers.py
    - python/fdars/advisor/aspects/classification.py
  key_links:
    - "dispatcher _supported set gains 'outliers','classification' -> lazy builder imports"
    - "_ASPECT_PRIMERS gains outliers + classification entries"
---

<objective>
Expand the proven tracer pattern to two more LOW-complexity aspects: outliers (ASPECT-02) and classification (ASPECT-03). Each is a new builder file mirroring depth, a new `_supported` entry + dispatch branch, a new `_ASPECT_PRIMERS` clause, and a determinism test. These consume dict-shaped results (the normal pattern), unlike depth's raw array.

This plan closes ASPECT-02 (depth from 21-01 + outliers here) and ASPECT-03. It applies RESEARCH corrections #2 and #6: classification CV uses key `"error_rate"` (NOT `cv_error_rate`), and `fclassif_cv` has NO `accuracy` key — both accesses are guarded.

Purpose: build out the horizontal coverage for the two dict-input LOW aspects.
Output: outliers + classification diagnostics with their prompt clauses and determinism tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-aspect-advisor-coverage/21-RESEARCH.md

@python/fdars/advisor/aspects/clustering.py
@python/fdars/advisor/aspects/depth.py
@python/fdars/advisor/__init__.py
@python/fdars/advisor/_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: outliers builder + dispatch + prompt clause (ASPECT-02)</name>
  <files>python/fdars/advisor/aspects/outliers.py, python/fdars/advisor/__init__.py, python/fdars/advisor/_prompts.py</files>
  <behavior>
    - build_diagnostics({"outliers": bool_arr, "threshold": 2.47}, "outliers") -> n_obs, n_outliers=int(sum), outlier_fraction, threshold=2.47.
    - build_diagnostics({"magnitude":[...], "shape":[...]}, "outliers") -> has_magnitude_shape==True, magnitude_range/shape_range present, n_outliers absent-or-None (no "outliers" key).
    - build_diagnostics({"mei":[...], "mbd":[...], "outliers":[...]}, "outliers") -> has_outliergram==True, mei_range/mbd_range present.
    - All values native Python types; json.dumps(sort_keys=True) succeeds.
  </behavior>
  <read_first>python/fdars/advisor/aspects/depth.py (builder shape from 21-01), 21-RESEARCH.md §1b (four outlier shapes + guard rules)</read_first>
  <action>Create `python/fdars/advisor/aspects/outliers.py` with `_build_outliers_diagnostics(raw, **kwargs) -> dict` computing the fields in 21-RESEARCH §1b. The `outliers` method string covers four fdars functions with DIFFERENT keys (detect_outliers_lrt -> outliers+threshold; outliergram -> mei+mbd+outliers; magnitude_shape -> magnitude+shape only). Guard EVERY key access (per ASVS V5 / RESEARCH pitfall): infer n_obs from whichever array is present; emit n_outliers/outlier_fraction only when "outliers" in raw; emit threshold only when "threshold" in raw; set has_magnitude_shape / has_outliergram booleans from key presence and emit the corresponding *_range pairs only when present. CRITICAL (RESEARCH pitfall): magnitude_shape returns NO "outliers" key, so never assume n_outliers is computable from it. Add "outliers" to `_supported` and a lazy dispatch branch in `__init__.py`. Add an "outliers" entry to `_ASPECT_PRIMERS` in `_prompts.py` per 21-RESEARCH §5 (explain outlier_fraction, LRT threshold vs outliergram geometric criterion, magnitude vs shape outlyingness — the clause MUST contain the token outlier_fraction for the test). No fenced code in this action.</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json; from fdars.advisor import build_diagnostics as b; d=b({'outliers':np.array([False,False,True,False,False]),'threshold':2.47},'outliers'); assert d['n_outliers']==1 and abs(d['threshold']-2.47)<1e-9; m=b({'magnitude':np.array([0.1,0.3,2.5]),'shape':np.array([0.05,0.1,0.8])},'outliers'); assert m['has_magnitude_shape'] and m.get('n_outliers') is None; json.dumps(d,sort_keys=True); json.dumps(m,sort_keys=True); from fdars.advisor._prompts import _system_prompt; assert 'outlier_fraction' in _system_prompt('interpretation','outliers'); print('OK')"</automated>
  </verify>
  <done>outliers builder handles all four result shapes with guarded key access; dispatch + prompt clause wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: classification builder + dispatch + prompt clause (ASPECT-03)</name>
  <files>python/fdars/advisor/aspects/classification.py, python/fdars/advisor/__init__.py, python/fdars/advisor/_prompts.py</files>
  <behavior>
    - build_diagnostics({"predicted":[0,0,1,1,2,2], "accuracy":0.8333}, "classification") -> n_obs=6, accuracy≈0.8333, error_rate≈0.1667.
    - build_diagnostics({"error_rate":0.18, "fold_errors":[...], "best_ncomp":4}, "classification") -> cv_error_rate=0.18, fold_error_std present, best_ncomp=4, accuracy derived as 1-error_rate (or None per chosen guard).
    - n_classes is an EXPLICIT param on build_diagnostics(): build_diagnostics(result, "classification", n_classes=3) -> diag["n_classes"]==3; omitting it -> diag["n_classes"] is None. inspect.signature(build_diagnostics) contains "n_classes".
    - All native types; json.dumps(sort_keys=True) succeeds.
  </behavior>
  <read_first>python/fdars/advisor/aspects/depth.py, 21-RESEARCH.md §1c (two classification shapes + corrections #2/#6)</read_first>
  <action>Create `python/fdars/advisor/aspects/classification.py` with `_build_classification_diagnostics(raw, *, n_classes=None, **kwargs) -> dict` per 21-RESEARCH §1c. Handle TWO shapes: point-estimate functions return "predicted"+"accuracy"; `fclassif_cv` returns "error_rate"+"fold_errors"+"best_ncomp" and NO "accuracy". Apply RESEARCH correction #2: read the CV rate from raw key "error_rate" (fclassif_cv's actual key) and emit it in diagnostics as cv_error_rate to distinguish from point-estimate accuracy. Apply correction #6: guard `accuracy` with `if "accuracy" in raw`; when only CV error_rate is present, derive accuracy = 1.0 - error_rate. Emit: n_obs (from len(predicted) if present else from fold_errors), accuracy, error_rate (1-accuracy when accuracy present), cv_error_rate, fold_error_std (np.std of fold_errors when present), best_ncomp, n_classes (int(n_classes) when the param is not None, else None — it CANNOT be inferred from the result dict, which contains only predicted labels; RESEARCH §1c).

BLOCKER #5 — n_classes sourcing (CHOSEN MECHANISM: explicit parameter, not bare **kwargs). Make `n_classes` an EXPLICIT, DOCUMENTED parameter on the public `build_diagnostics()` and forward it to the classification branch. Concretely, in `python/fdars/advisor/__init__.py`: (a) add `n_classes: int | None = None` to the `build_diagnostics()` signature in the keyword-only block (currently `def build_diagnostics(result, method, *, argvals=None, **kwargs)` — insert `n_classes: int | None = None,` after `argvals=None,`); (b) document it in the `build_diagnostics` docstring (one line under Parameters: "n_classes : int, optional — ground-truth class count for the classification aspect; cannot be inferred from a result dict, so the caller supplies it. Ignored by other methods."); (c) in the classification dispatch branch, forward it explicitly: `return _build_classification_diagnostics(raw, n_classes=n_classes, **kwargs)`. Do NOT thread `n_classes` into any other branch's call — the other builders keep their current signatures untouched (depth/outliers/represent/regression/regression_cv/spm are unaffected; their dispatch branches are NOT modified). This keeps `n_classes` explicit + discoverable via `inspect.signature(build_diagnostics)` and documented, while leaving every other aspect's contract byte-identical. Add "classification" to `_supported` + the dispatch branch. Add "classification" primer to `_ASPECT_PRIMERS` per 21-RESEARCH §5 (accuracy, error_rate = 1-accuracy, fold_error_std as CV instability, best_ncomp — clause MUST contain the token error_rate for the test).</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json, inspect; from fdars.advisor import build_diagnostics as b; assert 'n_classes' in inspect.signature(b).parameters; p=b({'predicted':np.array([0,0,1,1,2,2]),'accuracy':0.8333},'classification',n_classes=3); assert p['n_obs']==6 and abs(p['accuracy']-0.8333)<1e-4 and p['n_classes']==3; nn=b({'predicted':np.array([0,0,1,1,2,2]),'accuracy':0.8333},'classification'); assert nn['n_classes'] is None; c=b({'error_rate':0.18,'fold_errors':np.array([0.15,0.2,0.17,0.22,0.16]),'best_ncomp':4},'classification'); assert abs(c['cv_error_rate']-0.18)<1e-9 and c['best_ncomp']==4 and ('accuracy' not in c or c['accuracy'] is None or abs(c['accuracy']-0.82)<1e-9); json.dumps(p,sort_keys=True); json.dumps(c,sort_keys=True); from fdars.advisor._prompts import _system_prompt; assert 'error_rate' in _system_prompt('interpretation','classification'); print('OK')"</automated>
  </verify>
  <done>classification builder handles point-estimate + CV shapes with corrected keys (error_rate not cv_error_rate; guarded accuracy); dispatch + prompt clause wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: determinism tests for outliers + classification</name>
  <files>tests/test_advisor.py</files>
  <behavior>
    - test_outliers_deterministic: LRT-shape fixture -> equal dicts, byte-identical json.dumps(sort_keys=True), no numpy scalars.
    - test_classification_deterministic: point-estimate fixture -> same guarantees; a second assertion on the CV fixture confirms cv_error_rate path.
  </behavior>
  <read_first>tests/test_advisor.py (TestBuildDiagnosticsOffline + the check_no_numpy walker added in 21-01)</read_first>
  <action>Add `test_outliers_deterministic` and `test_classification_deterministic` to `TestBuildDiagnosticsOffline`, using the synthetic fixtures from 21-RESEARCH §8 (outliers LRT + magnitude_shape; classification point-estimate + CV). Reuse the recursive `check_no_numpy` walker introduced in 21-01 (import it or re-declare a small local copy). Each test: two build_diagnostics calls -> assert equal, assert byte-identical json.dumps(sort_keys=True), assert no numpy scalar types leak. Add a TestPrompts assertion (extend the class from 21-01) that the outliers and classification clauses appear only for their aspect and not for aspect="".</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_outliers_deterministic tests/test_advisor.py::TestBuildDiagnosticsOffline::test_classification_deterministic -x -q</automated>
  </verify>
  <done>Both determinism tests pass; full advisor offline suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller result dict -> builder | Untrusted, variably-shaped result dict crosses into offline builders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering | outliers/classification builders | low | mitigate | Guard every raw.get() key access; emit None on missing keys rather than raising KeyError (ASVS V5) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor.py -q` passes.
- No new package installs.
</verification>

<success_criteria>
- ASPECT-02 fully covered (depth + outliers); ASPECT-03 covered (classification).
- Corrected keys applied: error_rate (not cv_error_rate), guarded accuracy, guarded magnitude_shape.
</success_criteria>

<output>
Create `.planning/phases/21-per-aspect-advisor-coverage/21-02-SUMMARY.md` when done.
</output>

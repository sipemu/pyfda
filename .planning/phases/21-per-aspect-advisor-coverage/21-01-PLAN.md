---
phase: 21-per-aspect-advisor-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/aspects/depth.py
  - python/fdars/advisor/__init__.py
  - python/fdars/advisor/_prompts.py
  - tests/test_advisor.py
autonomous: true
requirements: [ASPECT-02, ASPECT-06, ASPECT-07]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "build_diagnostics(scores_array, method='depth', method_name=...) returns a deterministic plain-Python dict (ASPECT-02)"
    - "advise() accepts an additive aspect='' param threaded into _system_prompt(task, aspect); aspect='' reproduces current behavior (ASPECT-06)"
    - "_system_prompt('interpretation', 'depth') contains a depth-specific clause; existing (aspect='') prompt output is unchanged (ASPECT-06)"
    - "An unsupported method raises ValueError; aspect is never auto-detected from result keys (ASPECT-07)"
  artifacts:
    - python/fdars/advisor/aspects/depth.py
    - python/fdars/advisor/_prompts.py
    - tests/test_advisor.py
  key_links:
    - "build_diagnostics dispatcher (__init__.py) -> _build_depth_diagnostics (depth.py) via _supported set + lazy import"
    - "advise() -> _system_prompt(task, aspect) -> _ASPECT_PRIMERS[aspect]"
---

<objective>
TRACER: prove the entire per-aspect vertical end-to-end on ONE low-complexity aspect (depth). This slice touches every layer the phase will modify — a new `advisor/aspects/*.py` builder file, the `_supported` set + lazy dispatch branch in `advisor/__init__.py`, a per-aspect prompt clause in `_prompts.py`, the additive `aspect` parameter on `advise()`, and the offline determinism + no-auto-detection tests — so any architectural dead-end surfaces after one commit, not ten.

Depth is chosen as the tracer because it reveals the ONE non-obvious input contract (per 21-RESEARCH §1a / discrepancy #1): depth functions return a raw `ndarray (n,)`, NOT a result dict, so the builder must accept the score array directly. Getting this right in the tracer de-risks every later aspect.

Purpose: establish the reusable pattern (builder file shape, dispatcher extension, prompt-primer mapping, aspect threading, determinism test) that plans 02-05 replicate mechanically.
Output: working `depth` aspect (ASPECT-02 partial), the shared `aspect` prompt-threading machinery (ASPECT-06), and the no-auto-detection guarantee (ASPECT-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-aspect-advisor-coverage/21-RESEARCH.md

@python/fdars/advisor/__init__.py
@python/fdars/advisor/_prompts.py
@python/fdars/advisor/aspects/clustering.py
@python/fdars/advisor/aspects/fpca.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end depth aspect — builder + dispatcher, one path only</name>
  <files>python/fdars/advisor/aspects/depth.py, python/fdars/advisor/__init__.py</files>
  <behavior>
    - Given a fixed score array [0.05, 0.2, 0.5, 0.8, 0.95, 0.3, 0.45, 0.6, 0.15, 0.7], build_diagnostics(scores, method="depth") returns a dict with method=="depth" and n_obs==10.
    - depth_min==0.05, depth_max==0.95 (native Python float, not np.float64).
    - depth_histogram is a list of 10 native ints summing to 10.
    - Every value in the returned dict is a native Python type (float/int/str/list) — json.dumps(diag, sort_keys=True) succeeds.
    - build_diagnostics(scores, method="depth", method_name="fraiman_muniz") sets diag["method_name"]=="fraiman_muniz".
  </behavior>
  <read_first>python/fdars/advisor/aspects/clustering.py (builder file shape + native-type casting), python/fdars/advisor/__init__.py lines 115-149 (dispatcher _supported set + lazy-import branch pattern)</read_first>
  <action>FIX THE ARRAY-COERCION BUG FIRST, then add the builder. (A) `__init__.py` array-input fix (BLOCKER #2): the dispatcher today runs `raw = getattr(result, "raw", result)` (currently `__init__.py:124`) immediately followed by an unconditional coercion `if not isinstance(raw, dict): raw = dict(raw)` (currently `__init__.py:125-126`). For a 1-D ndarray, `dict(ndarray)` raises TypeError — so depth (which needs the raw score ARRAY, not a dict) would crash before its branch ever runs. Replace ONLY line 125's guard condition so array-likes AND Fdata-like objects pass through untouched: change `if not isinstance(raw, dict):` to `if not isinstance(raw, dict) and not hasattr(raw, "__array__") and not hasattr(raw, "data"):` (body `raw = dict(raw)` unchanged). Rationale: (a) numpy ndarrays expose `__array__` (depth's score array); (b) an `Fdata` object exposes a `.data` attribute (the represent aspect in plan 03 accepts an Fdata directly — see 21-RESEARCH §4 and blocker #4). Both must reach their builder WITHOUT `dict(raw)` being attempted (an Fdata has no `keys()`, so `dict(Fdata)` would raise). This leaves the dict-coercion path byte-identical for the current five aspects (all dict/wrapper inputs, none of which expose `.data`) while letting depth's array and represent's Fdata flow through. Do NOT reorder the dispatch branches; the guard change alone is sufficient. Note: a plain result dict does NOT hit this branch (it is already a dict), so the dict form of represent input is also unaffected. (B) Create `python/fdars/advisor/aspects/depth.py` with `from __future__ import annotations` and `_build_depth_diagnostics(raw, *, method_name="unknown", **kwargs) -> dict`, mirroring the clustering builder's native-type-casting discipline (every scalar wrapped in float()/int()/bool(), every array via `[float(v) for v in arr]`). Per 21-RESEARCH §1a: `raw` is the depth SCORE ARRAY itself (an ndarray/array-like), NOT a dict — do `scores = np.asarray(raw, dtype=float)`. Compute the fields from 21-RESEARCH §1a: method (constant "depth"), method_name, n_obs=int(len(scores)), depth_min/depth_max/depth_mean/depth_median (float), depth_q10/depth_q90 via np.percentile, depth_histogram = [int(v) for v in np.histogram(scores, bins=10)[0]]. Pure NumPy — NO fdars call. (C) Extend `advisor/__init__.py`: add "depth" to the `_supported` set (per ASPECT-02), and add a lazy-import dispatch branch `if method_lc == "depth": from fdars.advisor.aspects.depth import _build_depth_diagnostics; return _build_depth_diagnostics(raw, **kwargs)` placed alongside the other branches (after the array-guard fix, so `raw` is still the ndarray). The verify below constructs `build_diagnostics(np.array([...]), method="depth", ...)` and MUST NOT raise TypeError. Keep the existing dict-coercion behavior for the current five aspects unchanged. No fenced code in this action.</action>
  <verify>
    <automated>.venv/bin/python -c "import numpy as np, json; from fdars.advisor import build_diagnostics; d=build_diagnostics(np.array([0.05,0.2,0.5,0.8,0.95,0.3,0.45,0.6,0.15,0.7]), method='depth', method_name='fraiman_muniz'); assert d['method']=='depth' and d['n_obs']==10 and d['method_name']=='fraiman_muniz' and abs(d['depth_min']-0.05)<1e-9 and sum(d['depth_histogram'])==10; json.dumps(d, sort_keys=True); print('OK')"</automated>
  </verify>
  <done>build_diagnostics(scores, method="depth") returns a JSON-serialisable native-Python dict with the documented fields; the single depth path works end-to-end and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Thread aspect through advise() + add depth prompt primer</name>
  <files>python/fdars/advisor/_prompts.py, python/fdars/advisor/__init__.py</files>
  <action>Two additive, backward-compatible changes. Note the CURRENT state (read the files): `_prompts.py:40` ALREADY declares `def _system_prompt(task: str, aspect: str = "") -> str:` but the `aspect` param is UNUSED (docstring says "Reserved for Phase 21 ... accepted so callers can pass it without error"). `advise()` (`__init__.py:311-318`) does NOT yet have an `aspect` param, and `__init__.py:368` calls `system = _system_prompt(task)` (no aspect forwarded). (1) `_prompts.py` — define `_ASPECT_PRIMERS` AT MODULE LEVEL (top-level, immediately after the `_GROUNDING_INVARIANT` constant block, before `def _system_prompt`) as a dict mapping aspect name -> a short FDA-primer clause string, seeded with the "depth" entry (per 21-RESEARCH §5: explain functional depth, central vs peripheral curves, and that depth_q10 is the 10th percentile flagging peripheral curves — the clause text MUST contain the token depth_q10 so the test can assert it). Then WIRE the existing `aspect` param inside `_system_prompt`: after the `base = (...)` FDA-primer string is assembled and before the `# -- Task-family clause --` section, add `aspect_primer = _ASPECT_PRIMERS.get(aspect.lower(), "")` and concatenate it onto `base` (e.g. `base = base + aspect_primer`) so the aspect clause lands after the existing FDA-primer lines and before the task clause. Because `_ASPECT_PRIMERS.get("", "")` returns "" (there is no "" key), `_system_prompt('interpretation','')` and `_system_prompt('interpretation')` remain byte-identical to today's output — this preserves ASPECT-06 backward-compat. Update the `aspect` param docstring in `_system_prompt` to state it selects the per-aspect FDA primer from `_ASPECT_PRIMERS`; default "" reproduces prior behavior. (2) `advise()` (`__init__.py`) — add an `aspect: str = ""` keyword-only parameter placed AFTER `provider` in the signature (currently `__init__.py:317` is `provider: "str | object | None" = None,`; add `aspect: str = "",` on the next line, still inside the keyword-only `*,` block that begins at `__init__.py:313`). Change the `system = _system_prompt(task)` call at `__init__.py:368` to `system = _system_prompt(task, aspect)`. Document the new param in the advise() docstring (one line: aspect selects the per-aspect FDA primer clause; default "" reproduces prior behavior, per ASPECT-06). Confirm backward compatibility: with `aspect=""` (the default), `advise()` produces the exact same system prompt as before — no caller is required to pass aspect. Do NOT add a new schema, a new prompt function, or a per-aspect Advice class — one shared builder, one schema.</action>
  <verify>
    <automated>.venv/bin/python -c "from fdars.advisor._prompts import _system_prompt; base=_system_prompt('interpretation'); dep=_system_prompt('interpretation','depth'); assert base==_system_prompt('interpretation',''), 'aspect=\"\" must reproduce base'; assert 'depth_q10' in dep and 'depth_q10' not in base; import inspect; from fdars.advisor import advise; assert 'aspect' in inspect.signature(advise).parameters; print('OK')"</automated>
  </verify>
  <done>advise() exposes an additive aspect='' param threaded to _system_prompt; _system_prompt('interpretation','depth') carries a depth clause; aspect='' output is unchanged from before this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Determinism + no-auto-detection tests (Nyquist scaffold)</name>
  <files>tests/test_advisor.py</files>
  <behavior>
    - test_depth_deterministic: build_diagnostics(scores, "depth") twice -> equal dicts AND byte-identical json.dumps(sort_keys=True); a recursive check asserts no value isinstance np.generic.
    - test_no_auto_detection: build_diagnostics({...}, method="not_a_real_method") raises ValueError matching "unsupported method".
    - test_aspect_caller_specified: an array passed with method="depth" runs the depth branch (diag["method"]=="depth"); a wrong/unknown method never silently re-routes based on key/shape.
    - test_prompt_aspect_backward_compatible: _system_prompt('interpretation') == _system_prompt('interpretation','').
  </behavior>
  <read_first>tests/test_advisor.py (TestBuildDiagnosticsOffline class + existing determinism test pattern at test_build_diagnostics_deterministic)</read_first>
  <action>Extend `tests/test_advisor.py`. Add depth methods to the existing `TestBuildDiagnosticsOffline` class: `test_depth_deterministic` (per 21-RESEARCH §6 determinism-test pattern — two build_diagnostics calls on the fixed 10-element score array, assert equality and byte-identical json.dumps(sort_keys=True), then a recursive `check_no_numpy` walker asserting no value is a numpy scalar), and `test_aspect_caller_specified` (per 21-RESEARCH §6 — assert a depth array with method="depth" yields diag["method"]=="depth"; this locks ASPECT-07's caller-specified contract). Add `test_no_auto_detection` asserting `build_diagnostics(some_dict, method="not_a_real_method")` raises ValueError (pytest.raises with match on the unsupported-method message). Add a new `TestPrompts` class with `test_prompt_aspect_backward_compatible` asserting `_system_prompt('interpretation') == _system_prompt('interpretation','')` and that the depth clause appears only when aspect='depth'. These are the Wave-0 test scaffolds the RESEARCH validation map lists as "No — Wave 0"; later plans add their aspect's method into this same class. Do NOT put the literal unsupported-method message string in any non-test action prose.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_advisor.py::TestBuildDiagnosticsOffline::test_depth_deterministic tests/test_advisor.py::TestBuildDiagnosticsOffline::test_no_auto_detection tests/test_advisor.py::TestBuildDiagnosticsOffline::test_aspect_caller_specified tests/test_advisor.py::TestPrompts::test_prompt_aspect_backward_compatible -x -q</automated>
  </verify>
  <done>All four tracer tests pass; the full existing advisor offline suite still passes (regression-free).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller result -> build_diagnostics | Untrusted result payload (dict or array) crosses into the offline builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | build_diagnostics depth branch | low | mitigate | Treat `raw` as untrusted; `np.asarray(raw, dtype=float)` validates array-likeness; guard prevents dict-unwrap corruption of array input (ASVS V5) |
| T-21-02 | Information Disclosure | advise() aspect param -> prompt | low | accept | aspect only selects a static primer string; no user data path changes; default "" preserves behavior |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_advisor.py -q` passes (no regression in existing offline/integration-guarded tests).
- No package installs introduced (pure NumPy + existing fdars); no legitimacy gate needed.
</verification>

<success_criteria>
- depth aspect works end-to-end through build_diagnostics with a deterministic, JSON-serialisable dict.
- advise() carries an additive aspect param; aspect="" is byte-identical to prior behavior.
- ValueError on unsupported method; aspect never auto-detected.
</success_criteria>

<output>
Create `.planning/phases/21-per-aspect-advisor-coverage/21-01-SUMMARY.md` when done.
</output>

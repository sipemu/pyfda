---
phase: 02-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-audit/02-AUDIT.md
autonomous: false
requirements: [AUD-01, AUD-02, AUD-03]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "02-AUDIT.md exists at .planning/phases/02-audit/02-AUDIT.md with all three top-level sections present as headings: coverage table, R-era grep report, ranked gap/example list (per D-05)."
    - "The coverage table defines the full column set: page path, diagram file, style axis, accuracy axis, rollup label, warrants-a-diagram (yes/no + reason), needs-method-verification flag (per D-01/D-02/D-04)."
    - "Every learn/ content page (6: introduction, custom-plotting, simulation, smoothing, derivatives, irregular-sampling) appears as exactly one row in the coverage table."
    - "For each learn/ diagram the style axis records which STYLE_SPEC markers pass/fail — viewBox width 720, the five CSS classes (.ttl/.sub/.lab/.sm/.mono), system-ui fonts, role=img + aria-label (grep-checkable, per D-03)."
    - "The rollup label for each learn/ row follows the D-02 rule: missing if no diagram; accurate only if BOTH axes clean (conforms + accurate); else inconsistent."
    - "The smoothing.svg finding (smoothed panel reuses noisy coordinates) is confirmed with a file:line evidence pointer OR explicitly recorded as not-found (per specifics)."
    - "A stable ID scheme is defined and instantiated: GAP-#### for coverage gaps, EX-#### for new-example candidates; the R-era report uses file:line format."
  artifacts:
    - ".planning/phases/02-audit/02-AUDIT.md"
  key_links:
    - "Coverage table row count for learn/ == count of learn/ content pages under nav: in mkdocs.yml (excluding index.md unless warranted)."
    - "Style-axis marker verdicts are reproducible by grep against docs/assets/diagrams/*.svg, not eyeballed."
---

<objective>
Establish the `02-AUDIT.md` master document and prove the full audit pipeline end-to-end on ONE method section (learn/) — carrying that section through all three deliverables: coverage rows (both axes + rollup), any R-era hits, and any gap/example candidates it surfaces. This is the tracer slice: it fixes the document skeleton, column set, rollup rule, style-marker grep method, and the GAP-####/EX-#### ID schemes that Plans 02 and 03 expand into.

Purpose: De-risk the audit structure on real data (learn/ carries the known smoothing coordinate-bug finding) before scaling to the remaining five sections and the reference-API sweep. If the taxonomy or the grep-checkable style method is wrong, catch it after one section, not six.
Output: `.planning/phases/02-audit/02-AUDIT.md` with all three sections scaffolded and the learn/ section fully populated.
</objective>

## User Story

**As a** documentation maintainer scoping Phases 3–9,
**I want to** see one method section fully audited across coverage, style/accuracy classification, R-era hits, and gap candidates in a single git-diffable document,
**so that** I can trust the audit method before it is applied to the whole nav.

## Artifacts this phase produces

This phase produces exactly ONE file: `.planning/phases/02-audit/02-AUDIT.md`. No source code symbols are created (this is a read-only documentation audit — no `fdars`/`fdars-core` code, no docs prose, and no SVGs are modified). Named sections/schemes created here:
- `## 1. Page→Diagram Coverage Table` (columns fixed by this plan)
- `## 2. R-era Grep Report` (file:line entries, grouped by section)
- `## 3. Ranked Gap + New-Example List` (GAP-#### and EX-#### ID schemes, Selection column)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audit/02-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@mkdocs.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold 02-AUDIT.md + audit learn/ section end-to-end (all three deliverables)</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-CONTEXT.md (locked decisions D-01..D-08; the primary contract)
    - .planning/ROADMAP.md §"Phase 2: Audit" (AUD-01/02/03 success criteria) and §"Phase 3: learn/ Diagrams" (the smoothing coordinate-bug finding)
    - docs/assets/diagrams/STYLE_SPEC.md (viewBox-720, five CSS classes, system-ui fonts, role/aria markers — the style-axis yardstick)
    - mkdocs.yml §nav: → Learn: block (the 6 learn/ content pages + index.md)
    - docs/learn/*.md (the six content pages; grep each for the `assets/diagrams/NAME.svg` reference to build the page→diagram join)
    - docs/assets/diagrams/smoothing.svg, introduction.svg, custom-plotting.svg, simulation.svg, derivatives.svg, irregular-sampling.svg (the learn/ diagram population to classify)
  </read_first>
  <action>
    Create .planning/phases/02-audit/02-AUDIT.md with a short preamble (phase, date, purpose, the D-02 rollup rule stated explicitly, and the D-06 selection-gate note that the user marks the Selection column before Phase 3), then three top-level Markdown sections.

    Section "## 1. Page→Diagram Coverage Table" — a Markdown table with EXACTLY these columns in order: `Page` (path relative to docs/), `Diagram` (svg filename or —), `Style axis` (`conforms` | `legacy-outlier` + failing markers), `Accuracy axis` (`accurate` | `inaccurate/misleading`), `Rollup` (`accurate` | `inconsistent` | `missing`), `Warrants diagram?` (`yes`/`no` + one-line reason), `Needs method-verification` (flag text or —). Populate one row per learn/ content page: introduction, custom-plotting, simulation, smoothing, derivatives, irregular-sampling. Judge index.md per-page (warrants a diagram only if an overview clearly helps; record the reason). Build the page→diagram join by grepping each learn page for its `assets/diagrams/NAME.svg` reference.

    Style axis (grep-checkable per D-03): for each learn diagram check the STYLE_SPEC markers — viewBox width 720 (`viewBox="0 0 720`), the five CSS classes (.ttl/.sub/.lab/.sm/.mono), system-ui font stack, and `role="img"` + aria-label. Record `conforms` only if all present; else `legacy-outlier` and NAME which markers fail. Do not eyeball — the verdict must be reproducible by grep.

    Accuracy axis (inspect-and-flag per D-04): expert-inspect each learn SVG against method knowledge. For smoothing.svg specifically, confirm-with-evidence the known finding that the "smoothed" panel reuses the noisy coordinates — record a file:line evidence pointer into smoothing.svg, or explicitly write "(finding not reproduced — coordinates differ)" if inspection contradicts it. Where accuracy cannot be settled by eye, set the Needs-method-verification flag naming exactly what to check and defer to the section's Phase-3 sweep. Do NOT run a full verification pass; a cheap decisive fdars check is permitted only where a ~5-min check prevents mis-scoping (D-04 discretion boundary).

    Rollup (D-02): `missing` if no diagram; else `accurate` only if BOTH axes clean; else `inconsistent`.

    Section "## 2. R-era Grep Report" — add the section heading, a one-line scope statement (covers docs/assets/diagrams/*.svg AND docs/**/*.md incl. reference/ and examples/ per D-08), and a `### learn/` subsection listing every R-era hit in learn/ pages and their diagrams as `file:line — matched text`. Note where a hit is legitimate prose (e.g. custom-plotting.md discusses ggplot2 as narrative comparison) vs an actual R-era leftover to remove; the full cross-section report is Plan 03's job — here only learn/ is filled and the scope/format is fixed. If learn/ has zero hits, write "(no R-era hits in learn/)".

    Section "## 3. Ranked Gap + New-Example List" — add the section heading and define the ID schemes: `GAP-####` for coverage/style/accuracy gaps, `EX-####` for new-example candidates, and a `Selection` column the user marks before Phase 3 (D-06). Pre-seed the five LOCKED baseline examples (Phase 9) as EX-0001..EX-0005 with their names (conformal coverage guarantee, function-on-scalar regression, outlier-detection workflow, tolerance-bands vs conformal comparison, functional depth centrality ordering) and a Selection column defaulting to `[baseline-locked]`. Add any learn/-derived GAP rows the coverage table surfaced (e.g. an inaccurate/missing learn diagram → a GAP-#### with priority signals: zero accurate diagram, method centrality, authoring effort). The reference-API sweep and full ranking are Plan 03.

    Do NOT modify any file under docs/. This plan only writes 02-AUDIT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-audit/02-AUDIT.md && grep -q "## 1. Page→Diagram Coverage Table" .planning/phases/02-audit/02-AUDIT.md && grep -q "## 2. R-era Grep Report" .planning/phases/02-audit/02-AUDIT.md && grep -q "## 3. Ranked Gap + New-Example List" .planning/phases/02-audit/02-AUDIT.md && for p in introduction custom-plotting simulation smoothing derivatives irregular-sampling; do grep -q "learn/$p" .planning/phases/02-audit/02-AUDIT.md || { echo "MISSING learn row: $p"; exit 1; }; done && grep -qE "EX-0001|EX-0005" .planning/phases/02-audit/02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 02-AUDIT.md exists and contains all three section headings (coverage table, R-era grep report, ranked gap/example list).
    - The coverage table header row contains all seven columns: Page, Diagram, Style axis, Accuracy axis, Rollup, Warrants diagram?, Needs method-verification.
    - All 6 learn/ content pages (introduction, custom-plotting, simulation, smoothing, derivatives, irregular-sampling) each have exactly one coverage row (grep finds `learn/<page>` for each).
    - Each learn/ row with a diagram records a Style-axis verdict naming which STYLE_SPEC markers pass/fail; the verdict is reproducible by grep against the SVG.
    - Each learn/ row's Rollup label is consistent with the D-02 rule given its two axes.
    - The smoothing.svg coordinate-reuse finding is recorded with a file:line pointer OR an explicit not-reproduced note.
    - The R-era report has a `### learn/` subsection with file:line entries or an explicit "(no R-era hits in learn/)".
    - The ranked list defines GAP-#### and EX-#### schemes, a Selection column, and pre-seeds EX-0001..EX-0005 with the five locked baseline example names.
    - No file under docs/ was modified (git status shows only 02-AUDIT.md added).
  </acceptance_criteria>
  <done>02-AUDIT.md exists with all three sections scaffolded and the learn/ section fully populated across coverage (both axes + rollup), R-era grep, and gap/example candidates. ID schemes and column set are fixed for Plans 02/03 to expand.</done>
  <reversibility rating="reversible">The audit doc is a planning artifact; taxonomy/columns can be revised freely before Phases 3–9 consume it.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm audit structure before scaling to remaining sections</name>
  <what-built>02-AUDIT.md skeleton (three sections, seven-column coverage table, D-02 rollup rule, grep-checkable style-marker method, GAP-####/EX-#### ID schemes) fully populated for the learn/ section as a proof of the audit pipeline.</what-built>
  <how-to-verify>
    Open .planning/phases/02-audit/02-AUDIT.md. Confirm: (1) the seven coverage columns match what you want each sweep to read; (2) the two-axis style/accuracy split correctly separates a restyle (legacy-outlier but accurate) from a redraw (inaccurate); (3) the smoothing finding is captured with evidence in the way you expect; (4) the EX-#### baseline seeding and Selection column are the shape you will mark before Phase 3. If the structure is right, the remaining five sections + R-era report + reference-API sweep expand into this same shape (Plans 02–03).
  </how-to-verify>
  <resume-signal>Type "approved" to expand to the remaining sections, or describe structural changes to make first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is read-only analysis producing a single Markdown planning artifact. It reads files already committed in the repo (docs/, mkdocs.yml, STYLE_SPEC.md, planning files) and writes one file under `.planning/`. No new external input is ingested, no untrusted data crosses a boundary, no code execution path is introduced, and no `fdars`/`fdars-core` code or docs prose is modified. The optional cheap `fdars` sanity check (D-04) runs local, already-installed library code against local data.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | 02-AUDIT.md content accuracy | low | accept | Audit verdicts are human-reviewed at the Task 2 checkpoint and again at the Phase 3 selection gate; no automated action is taken on unreviewed audit content. |
| T-02-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this phase (read-only audit); no supply-chain surface. |
</threat_model>

<verification>
- 02-AUDIT.md exists with all three sections.
- learn/ coverage rows complete (6/6 content pages) with both axes + rollup.
- Style-axis verdicts are grep-reproducible against the learn/ SVGs.
- Human checkpoint approves the structure before Plans 02/03 expand it.
</verification>

<success_criteria>
The audit document skeleton and learn/ section prove the full three-deliverable pipeline end-to-end, and the structure is human-approved for scaling.
</success_criteria>

<output>
Create `.planning/phases/02-audit/02-01-SUMMARY.md` when done.
</output>

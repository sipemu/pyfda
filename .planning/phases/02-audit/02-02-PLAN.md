---
phase: 02-audit
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/phases/02-audit/02-AUDIT.md
autonomous: true
requirements: [AUD-01]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every content page in the remaining five method sections (represent, align, analyze, regression, monitoring) appears as exactly one row in the coverage table — no page omitted (AUD-01 completeness invariant)."
    - "The total coverage-table row count for the six method sections equals the count of method-section content pages under nav: in mkdocs.yml (learn 6 + represent 7 + align 6 + analyze 8 + regression 12 + monitoring 3 = 42 content pages, plus any index.md rows the audit judged warrant an overview diagram)."
    - "Every new coverage row records all three of: style axis (with failing STYLE_SPEC markers named), accuracy axis, and the derived rollup label following the D-02 rule."
    - "Style-axis verdicts for every classified diagram are reproducible by grep against docs/assets/diagrams/*.svg (viewBox-720, five CSS classes, system-ui fonts, role/aria — per D-03)."
    - "The six named preliminary findings are each confirmed with a file:line evidence pointer OR explicitly recorded as not-found: smoothing coordinate reuse (Plan 01), basis-representation.svg R-era, spm.svg R-era, elastic-alignment phase-vs-amplitude split absent, conformal-prediction.svg scalar-not-band, scalar-on-function β(t) absent."
    - "Diagrams accuracy-flaggable only against fdars method semantics (β(t), conformal functional band, SPM Phase I/II) carry a Needs-method-verification flag naming exactly what to check, deferred to that section's sweep (D-04)."
  artifacts:
    - ".planning/phases/02-audit/02-AUDIT.md"
  key_links:
    - "Coverage-table method-section row count == mkdocs.yml nav method-section content-page count (completeness join)."
    - "Every diagram filename referenced by a coverage row exists in docs/assets/diagrams/ (join integrity)."
---

<objective>
Expand the coverage table from the learn/ tracer to the full six method sections: append coverage rows for every content page in represent/, align/, analyze/, regression/, and monitoring/, each with style axis (grep-checkable markers), accuracy axis (inspect-and-flag), rollup (D-02), warrants-a-diagram judgement, and needs-method-verification flags. This closes the AUD-01 completeness invariant: every nav method-section content page is classified, no page omitted.

Purpose: Deliver the complete page→diagram coverage map that Phases 3–8 each read for their section's scope, cleanly separating restyle targets (legacy-outlier + accurate) from redraw targets (inaccurate).
Output: `02-AUDIT.md` §1 coverage table extended to all six method sections.
</objective>

## Artifacts this phase produces

Extends the single file `.planning/phases/02-audit/02-AUDIT.md` (§1 coverage table). No source code symbols created; no docs/ files modified.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-audit/02-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@mkdocs.yml
@.planning/phases/02-audit/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coverage rows for represent/ and align/ (13 content pages)</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-AUDIT.md (the established §1 column set and rollup rule from Plan 01)
    - .planning/phases/02-audit/02-CONTEXT.md (D-01/D-02/D-03/D-04 taxonomy; the phase-vs-amplitude split and basis-representation R-era findings)
    - .planning/ROADMAP.md §"Phase 4: represent/" and §"Phase 5: align/" (the pre-identified findings per section)
    - docs/assets/diagrams/STYLE_SPEC.md §"Legacy Non-Conforming viewBoxes" and §"Accessibility Pattern" (named migration targets: covariance-functions, elastic-clustering; the 9 diagrams missing role=img)
    - mkdocs.yml §nav: → Represent: (7 content pages: fpca, elastic-fpca, basis-representation, andrews-transformation, depth-functions, streaming-depth, distance-metrics) and Align: (6 content pages: elastic-alignment, advanced-alignment, landmark-registration, tsrvf, alignment-comparison, shape-analysis)
    - docs/represent/*.md and docs/align/*.md (grep each for its assets/diagrams/NAME.svg reference)
    - the corresponding SVGs in docs/assets/diagrams/ (basis-representation, fpca, elastic-fpca, andrews-transformation, depth-functions, streaming-depth, distance-metrics, elastic-alignment, advanced-alignment, landmark-registration, tsrvf, alignment-comparison, shape-analysis)
  </read_first>
  <action>
    Append one coverage row per represent/ and align/ content page to the §1 table in 02-AUDIT.md, using the exact column set fixed in Plan 01. Grep each page for its `assets/diagrams/NAME.svg` reference to build the page→diagram join; a page with no diagram reference gets Diagram = — and a warrants-a-diagram judgement with reason.

    Style axis per diagram (D-03, grep-checkable): check viewBox width 720 (`viewBox="0 0 720`), the five CSS classes (.ttl/.sub/.lab/.sm/.mono), system-ui font stack, `role="img"` + aria-label. Record `legacy-outlier` + the failing markers, or `conforms`. Note: streaming-depth and shape-analysis may lack a dedicated diagram — record accurately. covariance-functions and elastic-clustering are named legacy viewBox targets in STYLE_SPEC (they belong to analyze/, handled in Task 2).

    Accuracy axis (D-04 inspect-and-flag): for basis-representation.svg confirm-with-evidence the known R-era content — record a file:line pointer (also feeds Plan 03's grep report). For elastic-alignment.svg confirm-with-evidence whether it lacks a clear phase-vs-amplitude split — record file:line evidence or a Needs-method-verification flag naming exactly what to check ("confirm diagram visually separates phase variation from amplitude variation"). Where accuracy needs fdars semantics to settle, set the flag and defer to the section sweep; do not run a full verification pass.

    Rollup per row per D-02. Judge each section's index.md per-page for warrants-a-diagram (record reason).
  </action>
  <verify>
    <automated>for p in fpca elastic-fpca basis-representation andrews-transformation depth-functions streaming-depth distance-metrics; do grep -q "represent/$p" .planning/phases/02-audit/02-AUDIT.md || { echo "MISSING represent row: $p"; exit 1; }; done && for p in elastic-alignment advanced-alignment landmark-registration tsrvf alignment-comparison shape-analysis; do grep -q "align/$p" .planning/phases/02-audit/02-AUDIT.md || { echo "MISSING align row: $p"; exit 1; }; done && grep -q "basis-representation.svg" .planning/phases/02-audit/02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - All 7 represent/ content pages and all 6 align/ content pages each have exactly one coverage row (grep finds `represent/<page>` and `align/<page>` for each).
    - Each row records style axis, accuracy axis, rollup, warrants-diagram, and needs-method-verification per the Plan 01 column set.
    - basis-representation.svg accuracy verdict cites a file:line for its R-era content (or explicit not-found).
    - elastic-alignment phase-vs-amplitude finding is recorded as file:line evidence or a named Needs-method-verification flag.
    - Every referenced diagram filename exists in docs/assets/diagrams/.
  </acceptance_criteria>
  <done>represent/ (7) and align/ (6) content pages are fully classified in the coverage table with both axes, rollup, and the two named findings evidenced.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage rows for analyze/ (8 content pages)</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-AUDIT.md (§1 column set)
    - .planning/ROADMAP.md §"Phase 6: analyze/" (the legacy-outlier list: clustering, depth-functions, gmm-clustering, outlier-detection, seasonal-analysis, covariance-functions, elastic-clustering)
    - docs/assets/diagrams/STYLE_SPEC.md §"Legacy Non-Conforming viewBoxes" (elastic-clustering 700×250, outlier-detection 600×350, covariance-functions 600×425 — named non-720 targets)
    - mkdocs.yml §nav: → Analyze: (8 content pages: tolerance-bands, clustering, gmm-clustering, elastic-clustering, outlier-detection, seasonal-analysis, equivalence-testing, covariance-functions)
    - docs/analyze/*.md (grep each for its assets/diagrams/NAME.svg reference)
    - the analyze SVGs (tolerance-bands, clustering, gmm-clustering, elastic-clustering, outlier-detection, seasonal-analysis, equivalence-testing, covariance-functions)
  </read_first>
  <action>
    Append one coverage row per analyze/ content page to §1, same column set. Grep each page for its diagram reference.

    Style axis (D-03): flag the three named non-720 viewBox outliers (elastic-clustering, outlier-detection, covariance-functions) with their actual viewBox and any missing CSS classes / role-aria markers; classify the rest by grep. These are legacy-outlier-but-possibly-accurate → restyle targets, which the two-axis split must make visible.

    Accuracy axis (D-04 inspect-and-flag): most analyze/ diagrams are expected legacy-outlier on style but accurate on method — record accordingly so they read as restyle (not redraw). Where a diagram's method depiction cannot be settled by eye, set a Needs-method-verification flag. equivalence-testing may warrant a diagram but have none — record warrants-diagram = yes + reason if so.

    Rollup per D-02. Judge analyze/index.md per-page for warrants-a-diagram.
  </action>
  <verify>
    <automated>for p in tolerance-bands clustering gmm-clustering elastic-clustering outlier-detection seasonal-analysis equivalence-testing covariance-functions; do grep -q "analyze/$p" .planning/phases/02-audit/02-AUDIT.md || { echo "MISSING analyze row: $p"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All 8 analyze/ content pages each have exactly one coverage row.
    - The three named non-720 viewBox diagrams (elastic-clustering, outlier-detection, covariance-functions) are flagged legacy-outlier on the style axis with their actual viewBox recorded.
    - Diagrams that are style-outlier but method-accurate carry rollup = inconsistent (restyle), not missing — verifying the two-axis restyle/redraw split works.
    - Each row records both axes + rollup + warrants-diagram + needs-method-verification.
  </acceptance_criteria>
  <done>analyze/ (8) content pages fully classified; restyle vs redraw distinction visible via the two axes.</done>
</task>

<task type="auto">
  <name>Task 3: Coverage rows for regression/ (12) and monitoring/ (3) content pages</name>
  <files>.planning/phases/02-audit/02-AUDIT.md</files>
  <read_first>
    - .planning/phases/02-audit/02-AUDIT.md (§1 column set)
    - .planning/ROADMAP.md §"Phase 7: regression/" (conformal-prediction scalar-not-band; scalar-on-function β(t)) and §"Phase 8: monitoring/" (spm R-era; SPM Phase I/II limits)
    - .planning/STATE.md §Blockers/Concerns (regression/ + monitoring/ need method-semantic verification against fdars — β(t), conformal functional bands, SPM Phase I/II)
    - .planning/phases/02-audit/02-CONTEXT.md §Method-Accuracy Verification Depth (D-04 flag examples verbatim)
    - mkdocs.yml §nav: → Regression: (12 content pages: scalar-on-function, function-on-scalar, classification, elastic-regression, scalar-on-shape, cross-validation, regression-diagnostics, uncertainty-quantification, explainability, conformal-prediction, conformal-classification, robust-regression) and Monitoring: (3 content pages: spm, advanced-spm, profile-partial-monitoring)
    - docs/regression/*.md and docs/monitoring/*.md (grep each for its assets/diagrams/NAME.svg reference)
    - the regression + monitoring SVGs (scalar-on-function, function-on-scalar, classification, conformal-prediction, conformal-classification, robust-regression, regression-diagnostics, cross-validation, elastic-regression, scalar-on-shape, uncertainty-quantification, explainability, spm, advanced-spm, profile-partial-monitoring)
  </read_first>
  <action>
    Append one coverage row per regression/ and monitoring/ content page to §1, same column set. Grep each page for its diagram reference; several regression pages (scalar-on-shape, uncertainty-quantification, elastic-regression) may have no dedicated diagram — record Diagram = — with a warrants-diagram judgement.

    Style axis (D-03, grep-checkable) for each diagram as before.

    Accuracy axis (D-04 inspect-and-flag) — this is the method-semantic-heavy section. For conformal-prediction.svg confirm-with-evidence whether it shows a scalar constant interval instead of a time-varying band ŷ(t)±q(t): record file:line evidence AND set the Needs-method-verification flag "confirm conformal band is time-varying ŷ(t)±q(t) against fdars.conformal" (resolved in Phase 7). For scalar-on-function.svg confirm whether β(t) is foregrounded; set flag "confirm scalar-on-function shows β(t) coefficient curve". For spm.svg confirm-with-evidence its R-era content (file:line, feeds Plan 03) and set flag "confirm SPM shows distinct Phase I / Phase II limits". Do NOT run full method verification here — record inspect-and-flag verdicts and defer resolution to Phases 7–8 sweeps per D-04 and the STATE.md research flag.

    Rollup per D-02. Judge each section index.md per-page for warrants-a-diagram.
  </action>
  <verify>
    <automated>for p in scalar-on-function function-on-scalar classification elastic-regression scalar-on-shape cross-validation regression-diagnostics uncertainty-quantification explainability conformal-prediction conformal-classification robust-regression; do grep -q "regression/$p" .planning/phases/02-audit/02-AUDIT.md || { echo "MISSING regression row: $p"; exit 1; }; done && for p in spm advanced-spm profile-partial-monitoring; do grep -q "monitoring/$p" .planning/phases/02-audit/02-AUDIT.md || { echo "MISSING monitoring row: $p"; exit 1; }; done && grep -q "ŷ(t)" .planning/phases/02-audit/02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - All 12 regression/ content pages and all 3 monitoring/ content pages each have exactly one coverage row.
    - conformal-prediction row records the scalar-not-band finding (file:line + Needs-method-verification flag naming the ŷ(t)±q(t) check).
    - scalar-on-function row carries the β(t) Needs-method-verification flag.
    - spm row records R-era content (file:line) + the Phase I/II Needs-method-verification flag.
    - Method-semantic accuracy is flagged for later verification, not resolved now (no full verification pass performed).
  </acceptance_criteria>
  <done>regression/ (12) and monitoring/ (3) content pages fully classified; the three named method-semantic findings each carry evidence + a named verification flag deferred to Phases 7–8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Read-only analysis appending to one Markdown planning artifact. Reads committed docs/, SVGs, mkdocs.yml, STYLE_SPEC.md; writes only `.planning/phases/02-audit/02-AUDIT.md`. No new external input, no code execution path introduced, no docs/ or fdars code modified. No new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | coverage-table completeness/accuracy | low | accept | Row-count join against mkdocs.yml nav is grep-verifiable; per-section verdicts reviewed at the Phase 3 selection gate before any fix work. |
| T-02-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this phase (read-only audit). |
</threat_model>

<verification>
- Coverage table has one row per content page across all six method sections (42 content pages + any warranted index rows).
- Every row carries style axis + accuracy axis + rollup.
- The six named preliminary findings are each evidenced (file:line) or flagged.
</verification>

<success_criteria>
The AUD-01 completeness invariant holds: every nav method-section content page is classified in the coverage table with the two-axis taxonomy and rollup, no page omitted.
</success_criteria>

<output>
Create `.planning/phases/02-audit/02-02-SUMMARY.md` when done.
</output>

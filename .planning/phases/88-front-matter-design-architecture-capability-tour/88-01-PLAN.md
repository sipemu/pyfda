---
phase: 88-front-matter-design-architecture-capability-tour
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/code/gen_snippets.py
  - paper/snippets/represent.tex
  - paper/code/assert_coverage.py
  - paper/coverage_counts.tex
  - paper/paper.tex
  - paper/sections/abstract.tex
  - paper/sections/advisor.tex
  - paper/sections/conclusion.tex
  - paper/sections/capabilities.tex
  - Makefile
  - .github/workflows/paper.yml
autonomous: true
requirements: [MANU-05]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Running gen_snippets.py (generate mode) executes real fdars code and writes paper/snippets/represent.tex containing the source AND its captured output"
    - "Re-running gen_snippets.py leaves git diff paper/snippets/ empty (byte-stable / deterministic)"
    - "gen_snippets.py --check exits non-zero when the committed represent.tex is stale or missing"
    - "assert_coverage.py emits a nsklearnestimators macro derived from fdars.sklearn.TRIAGE_VERDICTS (machine-derived, not hardcoded), distinct from ncoverage"
    - "paper.tex loads listings with fdarsinput/fdarsoutput styles and input{snippets/represent} resolves"
    - "make paper regenerates snippets; make paper-check runs gen_snippets.py --check; paper.yml builds fdars via maturin and runs the snippet drift gate before tectonic"
    - "paper.yml path filter includes python/fdars/** and src/** so an API change re-triggers the snippet gate"
  artifacts:
    - paper/code/gen_snippets.py
    - paper/snippets/represent.tex
    - paper/sections/abstract.tex
    - paper/sections/advisor.tex
    - paper/sections/conclusion.tex
  key_links:
    - "gen_snippets.py --check <-> make paper-check <-> paper.yml (the no-stale-snippets drift gate)"
    - "assert_coverage.py nsklearnestimators <-> coverage_counts.tex <-> paper.tex input (machine-derived sklearn count)"
    - "paper.tex usepackage{listings} + lstdefinestyle <-> input{snippets/*.tex} (PDF compiles)"
---

<objective>
Build the executed-snippet harness that makes the capability tour provably current, proven end-to-end on ONE family (represent), and lay all paper.tex structural scaffolding + the machine-derived sklearn-estimator macro that later Wave-2 plans build on. (MANU-05 foundation.)

Purpose: This is the make-or-break tracer for the milestone's HIGHEST-RISK concern ("stale API snippets"). Proving one snippet flows script -> executed -> generated .tex -> \input -> drift-gate -> CI de-risks the whole phase before the tour expands to all families.
Output: gen_snippets.py (generate + --check drift mode), one committed paper/snippets/represent.tex, the \nsklearnestimators macro, the listings preamble, new section stubs (abstract/advisor/conclusion), and full make/paper.yml wiring (maturin build + path-filter widening).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: gen_snippets.py harness proven on represent (script -> executed -> represent.tex -> drift gate)</name>
  <files>paper/code/gen_snippets.py, paper/snippets/represent.tex</files>
  <precondition>fdars 0.12.0 is importable in the active environment (maturin-built .venv); PYTHONPATH includes scripts:paper/code.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Harness Design reference implementation sketch, determinism rules, --check mode; SS Validated Snippets #1 Represent EXACT call shape; Pitfall 2 dtype + Pitfall 4 float-drift rules)
    - /home/simonm/projects/rust/pyfda/paper/code/assert_coverage.py (the proven generate/--check/_render/sys.exit(1) shape to mirror)
    - /home/simonm/projects/rust/pyfda/paper/code/paper_utils.py (data_path convention to inject into the snippet namespace)
  </read_first>
  <action>
    Create paper/code/gen_snippets.py mirroring the shape of assert_coverage.py: module docstring, from __future__ import annotations, a main() with a --check branch that byte-compares against the committed file and sys.exit(1) on drift, else writes files. Follow the RESEARCH SS Harness Design reference sketch: a _run(source) that execs snippet source in a fresh namespace under contextlib.redirect_stdout(io.StringIO()) and returns captured stdout; a _render(source, output) that emits the static banner "% AUTO-GENERATED by paper/code/gen_snippets.py -- do not edit." (NO date/timestamp), a lstlisting source block with style=fdarsinput, and (when output is non-empty) a lstlisting output block with style=fdarsoutput, ending with a single trailing newline (utf-8, LF) exactly like assert_coverage.py:_render.

    Inject data_path into the exec namespace (from paper_utils import data_path) so the reader-facing snippet body uses data_path("growth.csv") and is robust to cwd (per RESEARCH SS Harness Design "Note on data_path"). Any stochastic snippet seeds np.random.seed; represent is deterministic.

    Seed the SNIPPETS list with ONE entry ("represent", body) using the VERIFIED RESEARCH SS Validated Snippets #1 body: read growth via pd.read_csv(data_path("growth.csv"), index_col=0); ARG = growth.index.values.astype(float); X = growth.values.T.astype(np.float64) (materializes a contiguous float64 array per Pitfall 2); fd = Fdata(X, argvals=ARG, names={"x": "Age (years)", "y": "Height (cm)"}); print(repr(fd)); print("mean shape:", fd.mean().shape). Keep imports inside the snippet body so the fragment is self-contained and reader-copy-pasteable. The snippet prints repr + a shape only (never a raw float array, per Pitfall 4).

    Run generate mode to write paper/snippets/represent.tex, run it two more times to confirm byte-stability, then commit both gen_snippets.py and represent.tex. Do NOT hand-write represent.tex -- it must be emitted by the harness.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py && test -f paper/snippets/represent.tex && grep -qF 'AUTO-GENERATED by paper/code/gen_snippets.py' paper/snippets/represent.tex && grep -qF 'style=fdarsinput' paper/snippets/represent.tex && grep -qF 'style=fdarsoutput' paper/snippets/represent.tex && grep -qF '93 obs' paper/snippets/represent.tex && ! grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/snippets/represent.tex && cp paper/snippets/represent.tex /tmp/rep_88_01.tex && PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py && diff -q /tmp/rep_88_01.tex paper/snippets/represent.tex && rm -f /tmp/rep_88_01.tex && PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py --check && echo HARNESS_OK</automated>
    <fails_when>gen_snippets.py errors on the real fdars call; represent.tex is missing the banner/styles/captured "93 obs" output; a date leaks into the fragment; a second run produces a byte-different file (non-deterministic); or --check exits non-zero when the file is fresh.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; cp paper/snippets/represent.tex /tmp/rep_keep_88_01.tex && printf '%% drift\n' >> paper/snippets/represent.tex && ( PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py --check ; rc=$? ; cp /tmp/rep_keep_88_01.tex paper/snippets/represent.tex ; rm -f /tmp/rep_keep_88_01.tex ; test $rc -ne 0 && git diff --quiet paper/snippets/represent.tex && echo SNIPPET_DRIFT_GATE_FIRES )</automated>
    <fails_when>--check exits 0 after the committed fragment is mutated (the drift gate does not fire), or the file is not restored byte-for-byte.</fails_when>
  </verify>
  <done>gen_snippets.py runs represent against fdars, writes a deterministic represent.tex with source + captured output and no timestamp, is byte-stable on re-run, --check passes clean AND exits non-zero on a mutated fragment. Both files committed.</done>
  <acceptance_criteria>
    - paper/code/gen_snippets.py exists with a generate mode and a --check drift mode mirroring assert_coverage.py
    - paper/snippets/represent.tex is machine-generated, contains fdarsinput + fdarsoutput lstlisting blocks and the captured "93 obs x 31 points" output, and has no date/timestamp
    - Re-running the harness is byte-stable; --check exits 1 on a mutated fragment
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: \nsklearnestimators macro + listings preamble + section stubs + capabilities represent \input</name>
  <files>paper/code/assert_coverage.py, paper/coverage_counts.tex, paper/paper.tex, paper/sections/abstract.tex, paper/sections/advisor.tex, paper/sections/conclusion.tex, paper/sections/capabilities.tex</files>
  <precondition>fdars.sklearn is importable (TRIAGE_VERDICTS available) in the paper env.</precondition>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS Architecture Facts sklearn 28-estimator fact + Pitfall 6 conflating-the-28s; SS Harness Design lstdefinestyle fdarsinput/fdarsoutput blocks + Pitfall 1 listings-missing; SS Data Model Facts repr en-dash/x UTF-8 note)
    - /home/simonm/projects/rust/pyfda/paper/code/assert_coverage.py (_derive_counts / _render structure to extend)
    - /home/simonm/projects/rust/pyfda/paper/paper.tex (current preamble + \input structure)
    - /home/simonm/projects/rust/pyfda/python/fdars/sklearn/_coverage.py (TRIAGE_VERDICTS: all 28 entries are verdict "PASS" after the Phase 57-58 reclassification)
  </read_first>
  <action>
    In paper/code/assert_coverage.py, add an nsklearnestimators macro derived from fdars.sklearn.TRIAGE_VERDICTS: inside _derive_counts(), import TRIAGE_VERDICTS and compute sum(1 for v in TRIAGE_VERDICTS.values() if v == "PASS") (all 28 skeletoned estimators carry verdict "PASS" post-reclassification, so this equals 28). This value is machine-derived -- do NOT write the integer 28 anywhere in the .py or .tex. Keep it DISTINCT from ncoverage (curated-paper-backed callables, a coincidental collision at 28 per Pitfall 6). assert_coverage.py was intentionally stdlib-only and decoupled from a compiled fdars in Phase 86; the paper env is now maturin-built (Task 3 adds it to CI), so import fdars.sklearn at derive time and, if the import fails, raise a clear RuntimeError instructing to run under the built venv -- do NOT silently omit the macro. Add nsklearnestimators to the module docstring's macro list. Regenerate paper/coverage_counts.tex (now 7 macros, emitted in sorted key order) and commit it.

    In paper/paper.tex preamble: add \usepackage{listings} after \usepackage{adjustbox}. Define two styles once (planner's discretion on exact keys): \lstdefinestyle{fdarsinput} = language=Python, basicstyle=\ttfamily\small, columns=fullflexible, keepspaces=true, showstringspaces=false, breaklines=true, frame=single; \lstdefinestyle{fdarsoutput} = basicstyle=\ttfamily\small, columns=fullflexible, keepspaces=true, frame=none. Handle the repr's UTF-8 en-dash and x characters via a \lstset{literate=...} mapping (e.g. map the en-dash to -- and x to $\times$) so listings typesets the captured output cleanly under tectonic (SS Data Model Facts repr note).

    Also in paper.tex: (a) replace the placeholder text inside \begin{abstract}...\end{abstract} with \input{sections/abstract}; (b) add \input{sections/advisor} immediately after \input{sections/comparison}; (c) add \input{sections/conclusion} immediately after \input{sections/availability}. This gives every Wave-2 prose plan its own distinct section file with zero further paper.tex edits.

    Create three compileable stub section files (no \today / timestamps, no "Placeholder" language that a later gate might flag -- use a neutral single sentence): paper/sections/abstract.tex (a short one-sentence abstract stub, filled in 88-03), paper/sections/advisor.tex (\section{Grounded AI Advisor and Scientific Provenance} + one stub sentence, filled in 88-04), paper/sections/conclusion.tex (\section{Conclusion} + one stub sentence, filled in 88-05).

    In paper/sections/capabilities.tex: replace the placeholder with a one-paragraph section intro (cite \nsubmodules / \npubliccallables macros and Table~\ref{tab:comparison} for the honest breadth claim, no hardcoded integers) followed by \input{snippets/represent} so the tracer snippet renders. Add a LaTeX comment marking where Plan 88-02 will append the remaining family \input lines. Do NOT add any other family \input here.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/assert_coverage.py && grep -qE '\\newcommand\{\\nsklearnestimators\}\{28\}' paper/coverage_counts.tex && test "$(grep -c '\\newcommand' paper/coverage_counts.tex)" -eq 7 && PYTHONPATH=scripts:paper/code python paper/code/assert_coverage.py --check && ! grep -qE '\{28\}' <(grep 'ncoverage' paper/code/assert_coverage.py) && echo MACRO_OK</automated>
    <fails_when>the macro is missing, is not 28, the file no longer has exactly 7 macros, --check drifts, or the literal 28 was hardcoded in the derivation code.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qE '\\usepackage\{listings\}' paper/paper.tex && grep -qE 'lstdefinestyle\{fdarsinput\}' paper/paper.tex && grep -qE 'lstdefinestyle\{fdarsoutput\}' paper/paper.tex && awk '/usepackage\{adjustbox\}/{a=NR} /usepackage\{listings\}/{l=NR} /begin\{document\}/{d=NR} END{exit !(a>0 && l>a && l<d)}' paper/paper.tex && grep -qF '\input{sections/abstract}' paper/paper.tex && grep -qF '\input{sections/advisor}' paper/paper.tex && grep -qF '\input{sections/conclusion}' paper/paper.tex && test -f paper/sections/abstract.tex && test -f paper/sections/advisor.tex && test -f paper/sections/conclusion.tex && grep -qF '\input{snippets/represent}' paper/sections/capabilities.tex && ! grep -qi 'Placeholder' paper/sections/capabilities.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/abstract.tex paper/sections/advisor.tex paper/sections/conclusion.tex && echo SCAFFOLD_OK</automated>
    <fails_when>listings/lstdefinestyle missing or out of preamble order; any of the three new \input lines or stub files absent; capabilities.tex still has a placeholder or lacks the represent \input; or a timestamp leaked into a stub.</fails_when>
  </verify>
  <done>coverage_counts.tex has 7 macros incl. a machine-derived \nsklearnestimators=28 distinct from \ncoverage; paper.tex loads listings with both styles and the UTF-8 literate map, and \inputs abstract/advisor/conclusion; three compileable stubs exist; capabilities.tex has an intro citing the coverage macros + comparison table and \inputs the represent snippet.</done>
  <acceptance_criteria>
    - assert_coverage.py derives \nsklearnestimators from TRIAGE_VERDICTS (no hardcoded 28); coverage_counts.tex regenerated with 7 sorted macros and passes --check
    - paper.tex loads listings, defines fdarsinput/fdarsoutput styles + a UTF-8 literate map, and \inputs sections/abstract, sections/advisor, sections/conclusion
    - abstract.tex, advisor.tex, conclusion.tex stubs exist and compile; capabilities.tex \inputs snippets/represent and cites breadth via macros + Table~\ref{tab:comparison}
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire gen_snippets into make + paper.yml (maturin build, scikit-learn, path-filter widening, drift gate before tectonic)</name>
  <files>Makefile, .github/workflows/paper.yml</files>
  <read_first>
    - /home/simonm/projects/rust/pyfda/.planning/phases/88-front-matter-design-architecture-capability-tour/88-RESEARCH.md (SS paper.yml maturin/fdars install step -- the exact YAML to add, step-order rule, scikit-learn add, path-filter widening; Pitfall 5 path-filter misses API changes)
    - /home/simonm/projects/rust/pyfda/Makefile (paper / paper-check targets to extend)
    - /home/simonm/projects/rust/pyfda/.github/workflows/paper.yml (current step order + path filter)
    - /home/simonm/projects/rust/pyfda/.github/workflows/ci.yml (the maturin develop --release build pattern to mirror)
  </read_first>
  <action>
    Makefile: add a paper-snippets target (PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/gen_snippets.py) and add it to the paper: prerequisite list AFTER paper-coverage (so the sklearn macro exists first) -- e.g. paper: paper-figures paper-coverage paper-refs paper-snippets. Append the drift gate to paper-check: PYTHONPATH=$(PAPER_PYTHONPATH) python paper/code/gen_snippets.py --check (as the last check line). Add paper-snippets to the .PHONY list.

    paper.yml: (1) Widen BOTH the push and pull_request paths filters to add "python/fdars/**" and "src/**" (per Pitfall 5 -- an API change must re-trigger the snippet gate). (2) Add a Rust toolchain + build step mirroring ci.yml: uses dtolnay/rust-toolchain@stable, uses Swatinem/rust-cache@v2, then a "Create venv and build fdars (maturin develop)" step that creates .venv, activates it, pip installs maturin numpy pandas scipy scikit-learn matplotlib, and runs maturin develop --release. Add scikit-learn (the Pipeline snippet imports it) and pandas (all snippets read CSVs). (3) Add a snippet drift-gate step "Assert snippets not stale (MANU-05 / GATE-02 drift gate)" running (under the venv) PYTHONPATH=scripts:paper/code python paper/code/gen_snippets.py --check. (4) STEP ORDER (load-bearing, Phase 86 pattern): the fdars build + all offline drift gates (figures, assert_coverage --check, gen_refs_bib, check_comparison, gen_snippets --check) MUST precede Setup tectonic + the PDF compile so drift fails fast. Because the existing offline steps now need the built fdars for assert_coverage.py (which imports fdars.sklearn), run them under the same .venv -- update the existing "Install Python dependencies" / step activation so assert_coverage.py --check runs against the built package. Keep tectonic + Compile PDF last, unchanged.

    Validate the YAML parses. Do NOT reorder tectonic before the offline gates.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -vE '^\s*#' Makefile | grep -qE 'gen_snippets\.py' && awk '/^paper-check:/{p=1} p&&/gen_snippets\.py --check/{f=1} /^paper:/{p=0} END{exit !f}' Makefile && awk '/^paper:/{print}' Makefile | grep -qE 'paper-snippets' && python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/paper.yml'))" && grep -qE 'maturin develop' .github/workflows/paper.yml && grep -qE 'scikit-learn' .github/workflows/paper.yml && grep -qE 'python/fdars/\*\*' .github/workflows/paper.yml && grep -qE 'src/\*\*' .github/workflows/paper.yml && grep -qE 'gen_snippets\.py --check' .github/workflows/paper.yml && awk '/gen_snippets\.py --check/{g=NR} /[Ss]etup tectonic|setup-tectonic/{t=NR} END{exit !(g>0 && t>g)}' .github/workflows/paper.yml && awk '/maturin develop/{m=NR} /gen_snippets\.py --check/{g=NR} END{exit !(m>0 && g>m)}' .github/workflows/paper.yml && echo WIRING_OK</automated>
    <fails_when>make paper-check omits the snippet drift gate; paper: lacks paper-snippets; paper.yml YAML is invalid; maturin/scikit-learn missing; path filter lacks python/fdars/** or src/**; the snippet gate runs after tectonic; or the build runs after the snippet gate.</fails_when>
  </verify>
  <done>make paper regenerates snippets after coverage; make paper-check runs gen_snippets.py --check; paper.yml builds fdars via maturin (with scikit-learn+pandas), widens the path filter to python/fdars/** + src/**, and runs the snippet drift gate before tectonic. YAML parses.</done>
  <acceptance_criteria>
    - Makefile: paper-snippets target added, appended to paper: after paper-coverage, and gen_snippets.py --check appended to paper-check
    - paper.yml: maturin develop --release build step (scikit-learn + pandas installed), path filter widened to python/fdars/** + src/**, gen_snippets --check step present and ordered after the build and before Setup tectonic
    - The YAML parses via yaml.safe_load
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snippet source -> exec() | gen_snippets.py exec's author-written snippet source (not user input) in-process |
| CI runner -> maturin build | paper.yml compiles fdars from the checked-out tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-01 | Tampering | generated paper/snippets/*.tex go stale vs the live API | high | mitigate | gen_snippets.py --check byte-compare drift gate in make paper-check + paper.yml; path filter widened to python/fdars/** + src/** so API changes re-trigger |
| T-88-02 | Information Disclosure | advisor snippet could hit an LLM/leak a key | low | mitigate | tour uses LLM-free build_diagnostics only; advise() shown in prose, never executed (Pitfall 7) |
| T-88-03 | Tampering | non-deterministic float output causes false CI diffs | medium | mitigate | snippets print shapes/keys/rounded scalars only; determinism gate (byte-stable re-run) catches residual (Pitfall 4) |
| T-88-04 | Elevation | gen_snippets.py exec of arbitrary code | low | accept | source is author-authored, committed, reviewed; no network, no user input (RESEARCH SS Security Domain) |
</threat_model>

<verification>
- make paper regenerates figures, coverage (incl. \nsklearnestimators), refs, and snippets deterministically; a re-run leaves git diff paper/ empty.
- make paper-check runs every offline drift gate incl. gen_snippets.py --check and exits 1 on any stale artifact.
- paper.yml parses, builds fdars, runs the snippet gate before tectonic, and triggers on python/fdars/** + src/** changes.
- The tracer proves the full path for ONE family; Plan 88-02 expands the SNIPPETS list + capabilities.tex \input lines.
</verification>

<success_criteria>
- gen_snippets.py runs represent against fdars 0.12.0, emits a deterministic represent.tex (source + captured output, no timestamp), and its --check drift gate fires on mutation.
- \nsklearnestimators is machine-derived (=28) and distinct from \ncoverage; coverage_counts.tex passes --check with 7 macros.
- paper.tex loads listings + both styles + UTF-8 literate map, and \inputs abstract/advisor/conclusion; the three stubs compile; capabilities.tex \inputs the represent snippet.
- make + paper.yml wired: maturin build, scikit-learn/pandas, widened path filter, snippet drift gate before tectonic.
</success_criteria>

<output>
Create `.planning/phases/88-front-matter-design-architecture-capability-tour/88-01-SUMMARY.md` when done.
</output>

---
phase: 39-group-c-depth-outliers-interval-inference-bindings
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/outliers_mod.rs
  - src/depth_mod.rs
  - tests/test_outliers.py
autonomous: true
requirements: [OUTL-01, OUTL-02, OUTL-03, OUTL-04]
user_setup: []
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "fdars.outliers.tvdmss(data, ...) returns a dict {magnitude_outliers: list[int], shape_outliers: list[int], tvd: (n,) ndarray, mss: (n,) ndarray} from TvdMssOutliers via a TvdMssConfig struct literal (3 f64, no seed) — OUTL-01"
    - "fdars.outliers.muod(data, ...) returns a dict with 3 index sets (shape/magnitude/amplitude_outliers as list[int]) + 3 (n,) score vectors (shape/magnitude/amplitude_index) from MuodResult via a MuodConfig struct literal (1 f64 factor, no seed) — OUTL-02"
    - "fdars.outliers.sequential_transform_outliers(data, transforms=[...], depth_method=..., emp_factor=...) returns a dict {per_transform_outliers: list[dict], union_outliers: list[int]}; each per-transform dict has transform:str + outliers:list[int]; transforms string-dispatch an invalid token raises ValueError; depth_method reuses depth_method_from_str — OUTL-03"
    - "fdars.outliers.depthgram(data, ...) returns a 10-key dict (6 (n,) index vectors mbd_mei_d/mei_mbd_d/mbd_mei_t/mei_mbd_t/mbd_mei_t2/mei_mbd_t2 + shape_outliers/magnitude_outliers as list[int] + mbd/mei as (n,)) from DepthgramResult via a DepthgramConfig struct literal (2 f64, no seed) — OUTL-04"
    - "no config struct carries a seed field and no core fn takes argvals — no seed param and no forwarded argvals added; outlier index sets are Python list[int], scores are 1-D numpy, scalars are Python float; degenerate inputs (too few curves) raise ValueError via to_pyresult()"
    - "all 4 detectors registered in outliers_mod::register(); no .unwrap()/.expect(); cargo fmt --check + cargo clippy -- -D warnings clean; tests/test_outliers.py green"
  artifacts:
    - src/outliers_mod.rs
    - tests/test_outliers.py
  key_links:
    - "4 new #[pyfunction]s (tvdmss/muod/sequential_transform_outliers/depthgram) + 4 *_to_pydict converters + a seq_transform_from_str dispatcher + a seq_transform_variant_str helper added to src/outliers_mod.rs; all 4 appended to outliers_mod::register(); config structs (TvdMssConfig/MuodConfig/DepthgramConfig/SeqTransformConfig — none #[non_exhaustive]) built by struct literal; result structs (#[non_exhaustive]) accessed field-by-field; index sets via .into_iter().map(|x| x as i64).collect::<Vec<i64>>() (the boxplot_result_to_pydict pattern), scores via vec_to_numpy1d"
  key_links_extra:
    - "SeqTransformConfig.depth_method obtained from the existing depth_method_from_str in depth_mod.rs (import it) so the depth dispatcher is reused, not re-implemented"
---

<objective>
Expose fdars-core 0.23.0's four new functional-outlier detectors — `tvdmss`, `muod`, `sequential_transform_outliers`, `depthgram` — through `fdars.outliers` by extending `src/outliers_mod.rs` (per D — CONTEXT.md locked decisions OUTL-01..04). Each detector takes a config struct built by struct literal from flat Python kwargs, calls the deterministic core function on an `FdMatrix`, and returns a dict with outlier index sets as Python `list[int]` and score vectors as 1-D numpy arrays.

The first task is a tracer that wires ONE detector (`tvdmss` — the simplest, 3-field deterministic config, flat result) end-to-end (kwargs → `TvdMssConfig` struct literal → core `tvdmss(&mat, config)` → `#[non_exhaustive]` `TvdMssOutliers` field-by-field → dict with `list[int]` + `(n,)` arrays → registered + importable Python smoke) BEFORE expanding to the three remaining detectors, which each add one wrinkle: `muod` (3 index sets + 3 score vectors), `sequential_transform_outliers` (the `SeqTransform` string dispatcher + the `Vec<(SeqTransform, Vec<usize>)>` → `list[dict]` conversion + `DepthMethod` reuse), and `depthgram` (10-field result).

Purpose: Deliver OUTL-01 (tvdmss), OUTL-02 (muod), OUTL-03 (sequential_transform_outliers with SeqTransform string dispatch + invalid-transform ValueError), OUTL-04 (depthgram) — all deterministic (no config has a seed field; no core fn takes argvals), with index sets as `list[int]` and every fallible call through `to_pyresult()`.
Output: An extended `src/outliers_mod.rs` (4 new `#[pyfunction]`s + 4 `*_to_pydict` converters + a `seq_transform_from_str` dispatcher + a variant→string helper + 4 lines in `register()`) and a new `tests/test_outliers.py` (`TestTvdMss`, `TestMuod`, `TestSeqTransform`, `TestDepthgram`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-group-c-depth-outliers-interval-inference-bindings/39-CONTEXT.md
@.planning/phases/39-group-c-depth-outliers-interval-inference-bindings/39-RESEARCH.md
@.planning/phases/39-group-c-depth-outliers-interval-inference-bindings/39-VALIDATION.md
@src/outliers_mod.rs
@src/depth_mod.rs
@src/convert.rs
</context>

## Artifacts this phase produces

New symbols created by this plan:

| Artifact | Kind | Location |
|----------|------|----------|
| `fdars.outliers.tvdmss` | Python-callable `#[pyfunction]` | `src/outliers_mod.rs` |
| `fdars.outliers.muod` | Python-callable `#[pyfunction]` | `src/outliers_mod.rs` |
| `fdars.outliers.sequential_transform_outliers` | Python-callable `#[pyfunction]` | `src/outliers_mod.rs` |
| `fdars.outliers.depthgram` | Python-callable `#[pyfunction]` | `src/outliers_mod.rs` |
| `tvdmss_to_pydict` / `muod_to_pydict` / `seq_transform_to_pydict` / `depthgram_to_pydict` | private Rust converters | `src/outliers_mod.rs` |
| `seq_transform_from_str` | private Rust string→`SeqTransform` dispatcher (+ variant→string helper) | `src/outliers_mod.rs` |
| `tests/test_outliers.py` | new pytest file | `tests/` |

`outliers_mod::register()` gains four `m.add_function(...)` lines. No `lib.rs` change (the `outliers` submodule is already wired), no `__init__.py` change, no new external packages, datasets, or CI-matrix changes.

**Deferred (NOT in this plan, per D — CONTEXT.md):** advisor `outliers`-aspect extension (Phase 40, ADV-04); docs functional-outliers page + SVGs (Phase 41, DOCS-10).

**Plan-time spike (from ROADMAP) — RESOLVED by research:** the seed audit is done — NONE of the four config structs (`TvdMssConfig`, `MuodConfig`, `SeqTransformConfig`, `DepthgramConfig`) has a `seed` field; all four detectors are fully deterministic. Do NOT add a `seed` kwarg to any of them. Likewise none of the four core functions takes `argvals` — do NOT accept-and-forward a grid (omit `argvals` from the Python signatures per research Open Question 1).

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tvdmss tracer — prove the config-struct-literal → #[non_exhaustive] result → list[int]+ndarray dict path on one detector</name>
  <files>src/outliers_mod.rs, tests/test_outliers.py</files>
  <read_first>
    - src/outliers_mod.rs (whole file — the existing `outliergram`/`magnitude_shape` `#[pyfunction]` + `PyDict::new` + `dict.set_item` shape; the `register()` at lines 154-160; note `use crate::convert::*;` and `use numpy::PyReadonlyArray2;` at the top)
    - src/depth_mod.rs lines 442-461 (`boxplot_result_to_pydict` — the canonical `Vec<usize>` → `list[int]` conversion via `.into_iter().map(|x| x as i64).collect::<Vec<i64>>()`, and `vec_to_numpy1d` for score vectors)
    - src/convert.rs lines 29-45, 66-95 (`numpy2d_to_fdmatrix`, `vec_to_numpy1d`, `to_pyresult`)
    - 39-RESEARCH.md "Pattern 2: Outlier Config Struct Binding" (the tvdmss `#[pyfunction]` skeleton verbatim), "OUTL-01: TvdMss" (TvdMssConfig 3 f64 fields + defaults 1.5/1.5/0.5; TvdMssOutliers 4 fields #[non_exhaustive]; min n=3; no seed; no argvals), Pitfall 1 (do not forward argvals) and Pitfall 2 (never struct-literal the #[non_exhaustive] result)
  </read_first>
  <action>
    Prove the outlier-binding shape on the simplest detector before adding the other three. In `src/outliers_mod.rs`, add `use pyo3::exceptions::PyValueError;` and `use pyo3::types::PyDict;` if not already present (Tasks 3 needs PyValueError; add PyDict now for clarity), and add:

    A `#[pyfunction]` `tvdmss` with `#[pyo3(signature = (data, emp_factor_mss=1.5, emp_factor_tvd=1.5, central_region_tvd=0.5))]` (NO `argvals`, NO `seed` — verified: the core fn takes neither). Body: `let mat = numpy2d_to_fdmatrix(data)?;` then build the config by struct literal (`TvdMssConfig` is NOT `#[non_exhaustive]`): `let config = fdars_core::outliers::TvdMssConfig { emp_factor_mss, emp_factor_tvd, central_region_tvd };`. Call `let r = to_pyresult(fdars_core::outliers::tvdmss(&mat, config))?;` and return `tvdmss_to_pydict(py, r)`.

    A private `tvdmss_to_pydict(py, r)` that accesses the `#[non_exhaustive]` `TvdMssOutliers` field-by-field (never struct-literal / never exhaustive-match it) and emits 4 keys: `"magnitude_outliers"` and `"shape_outliers"` as `list[int]` via `r.magnitude_outliers.into_iter().map(|x| x as i64).collect::<Vec<i64>>()` (mirror `boxplot_result_to_pydict`); `"tvd"` and `"mss"` via `vec_to_numpy1d(py, r.tvd)` / `vec_to_numpy1d(py, r.mss)`.

    Append `m.add_function(wrap_pyfunction!(tvdmss, m)?)?;` to `outliers_mod::register()`.

    Create `tests/test_outliers.py` with a module docstring, `import numpy as np`, `import pytest`, `from fdars import outliers as outl`, and a `TestTvdMss` class with `test_tvdmss_smoke`: build `data = np.random.default_rng(1).standard_normal((15, 30))` then plant an outlier `data[0, :] += 10`; call `outl.tvdmss(data)`; assert the dict has exactly the 4 keys {magnitude_outliers, shape_outliers, tvd, mss}; assert `magnitude_outliers` and `shape_outliers` are Python `list` of `int` (`isinstance(v, list)` and all `isinstance(i, int)`); assert `tvd` and `mss` are numpy arrays of shape `(15,)`. Keep the fixture small and synthetic.

    Build with `maturin develop` FIRST; if `TvdMssConfig` field names or the `tvdmss` path don't resolve, confirm against fdars-core 0.23.0 `outliers.rs` (research verified them verbatim at lines 467-502) before proceeding.
  </action>
  <verify>
    <automated>.venv/bin/python -m maturin develop && .venv/bin/python -m pytest tests/test_outliers.py::TestTvdMss::test_tvdmss_smoke -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `maturin develop` builds green with the new `tvdmss` `#[pyfunction]`, the `tvdmss_to_pydict` converter, and the `register()` line.
    - `test_tvdmss_smoke` passes: `tvdmss(data)` returns a 4-key dict; index sets are `list[int]`; `tvd`/`mss` are `(15,)` numpy arrays.
    - Config built by struct literal; the `#[non_exhaustive]` result accessed field-by-field; the core call routes through `to_pyresult()`; no `.unwrap()`/`.expect()`; no `argvals`/`seed` in the signature.
  </acceptance_criteria>
  <done>One outlier detector works end-to-end (Python kwargs → TvdMssConfig struct literal → core tvdmss → #[non_exhaustive] result → dict with list[int] + ndarray → importable) and is committed; the outlier-binding shape is proven — OUTL-01 established.</done>
  <reversibility rating="reversible">A thin config-struct binding reusing the existing FdMatrix input path and the boxplot list[int] pattern; no new architecture, fully reversible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: muod binding — 3 index sets + 3 score vectors (MuodConfig 1 f64, deterministic)</name>
  <files>src/outliers_mod.rs, tests/test_outliers.py</files>
  <read_first>
    - src/outliers_mod.rs (the `tvdmss` + `tvdmss_to_pydict` added in Task 1 — mirror their shape)
    - 39-RESEARCH.md "OUTL-02: MUOD" (MuodConfig 1 f64 `factor` default 1.5, NOT #[non_exhaustive]; MuodResult 6 fields #[non_exhaustive]: shape_outliers/magnitude_outliers/amplitude_outliers as Vec<usize>, shape_index/magnitude_index/amplitude_index as Vec<f64>; min n=3, min m=2; no seed; no argvals) and the Python dict layout list
    - 39-VALIDATION.md OUTL-02 row
  </read_first>
  <action>
    Add a `#[pyfunction]` `muod` to `src/outliers_mod.rs` mirroring `tvdmss`. Signature: `#[pyo3(signature = (data, factor=1.5))]` (NO argvals, NO seed). Body: `numpy2d_to_fdmatrix(data)?`; build `let config = fdars_core::outliers::MuodConfig { factor };` (struct literal — not #[non_exhaustive]); `let r = to_pyresult(fdars_core::outliers::muod(&mat, config))?;`; return `muod_to_pydict(py, r)`.

    Add `muod_to_pydict(py, r)` accessing the `#[non_exhaustive]` `MuodResult` field-by-field, emitting 6 keys: `"shape_outliers"`, `"magnitude_outliers"`, `"amplitude_outliers"` as `list[int]` (the `.into_iter().map(|x| x as i64).collect::<Vec<i64>>()` pattern); `"shape_index"`, `"magnitude_index"`, `"amplitude_index"` via `vec_to_numpy1d`.

    Append `m.add_function(wrap_pyfunction!(muod, m)?)?;` to `register()`.

    Add a `TestMuod` class to `tests/test_outliers.py`:
    - `test_muod_smoke`: `data = np.random.default_rng(2).standard_normal((15, 30))`, `data[0, :] += 10`; call `outl.muod(data)`; assert the dict has exactly the 6 keys; assert the three `*_outliers` are `list[int]` and the three `*_index` are `(15,)` numpy arrays.
    - `test_muod_degenerate`: pass a too-small sample (e.g. `np.zeros((2, 30))`, below the n>=3 guard) and assert `pytest.raises(ValueError)` (surfaced from core via `to_pyresult()`).
  </action>
  <verify>
    <automated>.venv/bin/python -m maturin develop && .venv/bin/python -m pytest tests/test_outliers.py::TestMuod -q</automated>
  </verify>
  <acceptance_criteria>
    - `muod(data)` returns a 6-key dict: 3 index sets as `list[int]`, 3 score vectors as `(n,)` numpy arrays.
    - A below-min-n sample raises `ValueError` (not a panic).
    - Config built by struct literal; result accessed field-by-field; core call via `to_pyresult()`; no `.unwrap()`/`.expect()`; no argvals/seed.
  </acceptance_criteria>
  <done>muod returns the 6-key dict with 3 list[int] index sets + 3 (n,) score vectors and raises ValueError on degenerate input — OUTL-02 is complete.</done>
  <reversibility rating="reversible">Same config-struct pattern as tvdmss; dict key set adjustable; no architecture.</reversibility>
</task>

<task type="auto">
  <name>Task 3: sequential_transform_outliers — SeqTransform string dispatch + list[dict] per-transform + invalid-transform ValueError + DepthMethod reuse</name>
  <files>src/outliers_mod.rs, tests/test_outliers.py</files>
  <read_first>
    - src/outliers_mod.rs (the tvdmss/muod bindings + converters from Tasks 1-2)
    - src/depth_mod.rs lines 414-432 (`depth_method_from_str` — this is `pub(crate)`? confirm visibility; if it is a private `fn`, either make it `pub(crate)` in depth_mod.rs and `use crate::depth_mod::depth_method_from_str;` here, OR inline a minimal 4-arm+13-arm depth-token→DepthMethod match in outliers_mod.rs. Prefer reusing it: change `fn depth_method_from_str` to `pub(crate) fn` in src/depth_mod.rs — a one-word visibility change — and import it. Note SeqTransformConfig only needs the token→DepthMethod mapping; scale/nproj/seed default to true/50/None.)
    - 39-RESEARCH.md "OUTL-03: Sequential Transform Outliers" (SeqTransform 5 variants T0/T1/T2/D1/D2 #[non_exhaustive]; SeqTransformConfig NOT #[non_exhaustive], fields depth_method:DepthMethod default ModifiedBand + emp_factor:f64 default 1.5; SeqTransformOutliers #[non_exhaustive]: per_transform_outliers:Vec<(SeqTransform,Vec<usize>)> + union_outliers:Vec<usize>; signature `sequential_transform_outliers(data, sequence: &[SeqTransform], config)`; min n=2), "Pattern 3: SeqTransform String Dispatcher" (the seq_transform_from_str skeleton), "Pattern 2 note on SeqTransformConfig", Pitfall 5 (per_transform_outliers → list[dict] with transform:str + outliers:list[int]) and Open Question 3 / Assumption A3 (use LOWERCASE tokens "t0"/"t1"/"t2"/"d1"/"d2" for consistency with existing lowercase pyfda tokens)
    - 39-VALIDATION.md OUTL-03 row
  </read_first>
  <action>
    Add the `SeqTransform` string dispatcher and the binding to `src/outliers_mod.rs`.

    Add `fn seq_transform_from_str(s: &str) -> PyResult<fdars_core::outliers::SeqTransform>` matching LOWERCASE tokens (per research A3/OQ3, consistent with existing lowercase pyfda tokens): `"t0" => T0`, `"t1" => T1`, `"t2" => T2`, `"d1" => D1`, `"d2" => D2`, with a wildcard `other => Err(PyValueError::new_err(format!("transform must be one of 't0', 't1', 't2', 'd1', 'd2', got '{other}'")))`. Add a companion `fn seq_transform_variant_str(t: &fdars_core::outliers::SeqTransform) -> &'static str` matching each `#[non_exhaustive]` variant back to its lowercase token (with a wildcard `_ => "unknown"` arm required by #[non_exhaustive]).

    Reuse the depth dispatcher: make `depth_method_from_str` `pub(crate)` in `src/depth_mod.rs` (single-word visibility change) and `use crate::depth_mod::depth_method_from_str;` in outliers_mod.rs. (If a build issue makes cross-module reuse awkward, inline the 13-arm token→DepthMethod match here and record the deviation in the SUMMARY.)

    Add `#[pyfunction] sequential_transform_outliers` with `#[pyo3(signature = (data, transforms, depth_method="modified_band", emp_factor=1.5))]`, params `data: PyReadonlyArray2<'py, f64>`, `transforms: Vec<String>` (Python `list[str]` → owned iteration), `depth_method: &str`, `emp_factor: f64`. Body: `numpy2d_to_fdmatrix(data)?`; map `transforms` through `seq_transform_from_str` into a `Vec<SeqTransform>` (propagate the first `ValueError` via `?` — use a `.collect::<PyResult<Vec<_>>>()?`); build `let config = fdars_core::outliers::SeqTransformConfig { depth_method: depth_method_from_str(depth_method, true, 50, None)?, emp_factor };` (struct literal — not #[non_exhaustive]); call `let r = to_pyresult(fdars_core::outliers::sequential_transform_outliers(&mat, &seq, config))?;`; return `seq_transform_to_pydict(py, r)`.

    Add `seq_transform_to_pydict(py, r)` accessing the `#[non_exhaustive]` `SeqTransformOutliers` field-by-field: build `"per_transform_outliers"` as a Python `list` of dicts — iterate `for (t, idxs) in r.per_transform_outliers`, per entry make a sub-`PyDict` with `"transform"` = `seq_transform_variant_str(&t)` and `"outliers"` = `idxs.into_iter().map(|x| x as i64).collect::<Vec<i64>>()`, push to a `Vec` and set as the list value; `"union_outliers"` as `list[int]`.

    Append `m.add_function(wrap_pyfunction!(sequential_transform_outliers, m)?)?;` to `register()`.

    Add a `TestSeqTransform` class to `tests/test_outliers.py`:
    - `test_seqtransform_smoke`: `data = np.random.default_rng(3).standard_normal((15, 30))`, `data[0, :] += 10`; call `outl.sequential_transform_outliers(data, transforms=["t0", "t1", "d1"])`; assert the dict has {per_transform_outliers, union_outliers}; assert `per_transform_outliers` is a `list` of dicts each with keys {transform, outliers} where `transform` is a `str` in {"t0","t1","d1"} and `outliers` is `list[int]`; assert `union_outliers` is `list[int]`.
    - `test_seqtransform_bad_transform`: call with `transforms=["t0", "nope"]` and assert `pytest.raises(ValueError, match="nope")`.
    - `test_seqtransform_depth_method`: pass `depth_method="modified_band"` (default) and a second call with one of the new tokens (e.g. `depth_method="total_variation"`) and assert both return a valid dict (proves DepthMethod reuse).
  </action>
  <verify>
    <automated>.venv/bin/python -m maturin develop && .venv/bin/python -m pytest tests/test_outliers.py::TestSeqTransform -q</automated>
  </verify>
  <acceptance_criteria>
    - `sequential_transform_outliers(data, transforms=[...])` returns a dict with `per_transform_outliers` as `list[dict]` (each `{transform: str, outliers: list[int]}`) and `union_outliers` as `list[int]`.
    - An invalid transform token raises `ValueError` naming the offending token.
    - `depth_method` string-dispatches through the reused `depth_method_from_str` (default + a new token both work).
    - Config built by struct literal; result accessed field-by-field; core call via `to_pyresult()`; no `.unwrap()`/`.expect()`; no seed.
  </acceptance_criteria>
  <done>sequential_transform_outliers dispatches SeqTransform tokens, exposes per_transform_outliers as list[dict] + union_outliers as list[int], reuses the depth dispatcher, and raises ValueError on an invalid transform — OUTL-03 is complete.</done>
  <reversibility rating="reversible">Two small string dispatchers + a list[dict] converter; the depth_method_from_str visibility bump is a one-word reversible change.</reversibility>
</task>

<task type="auto">
  <name>Task 4: depthgram binding (10-key result) + register + final fmt/clippy/full-suite gate</name>
  <files>src/outliers_mod.rs, tests/test_outliers.py</files>
  <read_first>
    - src/outliers_mod.rs (the three bindings + converters from Tasks 1-3; the `register()` footer)
    - 39-RESEARCH.md "OUTL-04: Depthgram" (DepthgramConfig NOT #[non_exhaustive], fields outliergram_factor:f64 default 1.5 + boxplot_factor:f64 default 1.5; DepthgramResult #[non_exhaustive] 10 fields: mbd_mei_d/mei_mbd_d/mbd_mei_t/mei_mbd_t/mbd_mei_t2/mei_mbd_t2 as Vec<f64>, shape_outliers/magnitude_outliers as Vec<usize>, mbd/mei as Vec<f64>; min n=2, min m=1; no seed; no argvals; for univariate data the _d/_t/_t2 triplets are identical)
    - 39-VALIDATION.md OUTL-04 row + "Sampling Rate" (phase gate: full suite green + cargo fmt --check + cargo clippy -- -D warnings)
  </read_first>
  <action>
    Add the final detector, then run the phase gate.

    Add `#[pyfunction] depthgram` to `src/outliers_mod.rs` with `#[pyo3(signature = (data, outliergram_factor=1.5, boxplot_factor=1.5))]` (NO argvals, NO seed). Body: `numpy2d_to_fdmatrix(data)?`; build `let config = fdars_core::outliers::DepthgramConfig { outliergram_factor, boxplot_factor };` (struct literal); `let r = to_pyresult(fdars_core::outliers::depthgram(&mat, config))?;`; return `depthgram_to_pydict(py, r)`.

    Add `depthgram_to_pydict(py, r)` accessing the `#[non_exhaustive]` `DepthgramResult` field-by-field and emitting all 10 keys: `"mbd_mei_d"`, `"mei_mbd_d"`, `"mbd_mei_t"`, `"mei_mbd_t"`, `"mbd_mei_t2"`, `"mei_mbd_t2"`, `"mbd"`, `"mei"` via `vec_to_numpy1d`; `"shape_outliers"`, `"magnitude_outliers"` as `list[int]`.

    Append `m.add_function(wrap_pyfunction!(depthgram, m)?)?;` to `register()`.

    Add a `TestDepthgram` class to `tests/test_outliers.py`:
    - `test_depthgram_smoke`: `data = np.random.default_rng(4).standard_normal((15, 30))`, `data[0, :] += 10`; call `outl.depthgram(data)`; assert the dict has exactly the 10 keys; assert the 8 score keys are `(15,)` numpy arrays and the 2 `*_outliers` are `list[int]`.
    - `test_depthgram_determinism`: call twice with identical input and assert `np.array_equal` on `mbd` and `mei` (byte-identical — deterministic, no seed).

    Then run the full phase gate: `maturin develop` green, the full `pytest tests/ -q` suite green (zero regression against the Phase-38 baseline plus the new test_outliers.py), `cargo fmt --check` clean, `cargo clippy -- -D warnings` clean. Fix any clippy finding in the new code (needless borrows, redundant collects) without weakening a converter or dropping a `to_pyresult()`; do not add `#[allow(...)]` unless it matches an existing crate-level allow.
  </action>
  <verify>
    <automated>.venv/bin/python -m maturin develop && .venv/bin/python -m pytest tests/ -q && cargo fmt --check && cargo clippy -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `depthgram(data)` returns a 10-key dict (8 `(n,)` score arrays + 2 `list[int]` index sets) and is deterministic across two calls.
    - All 4 detectors are registered in `outliers_mod::register()` and importable under `fdars.outliers`.
    - The full `pytest tests/ -q` suite is green with zero regression; `cargo fmt --check` and `cargo clippy -- -D warnings` clean.
    - No `.unwrap()`/`.expect()`; config by struct literal; result accessed field-by-field.
  </acceptance_criteria>
  <done>depthgram returns the 10-key deterministic dict, all four detectors are registered, and the full suite + fmt + clippy are green — OUTL-01..04 are complete and verification-ready.</done>
  <reversibility rating="reversible">Same config-struct pattern; dict key set adjustable; no architecture.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 wrapper | Untrusted `(n, m)` data matrices, `transforms` string lists, `depth_method` strings, and factor scalars cross into the four detectors; container/token validity and per-detector min-n/min-m guards are unverified until the wrapper's string dispatchers + core validation via `to_pyresult()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39b-01 | Tampering | `seq_transform_from_str` / `depth_method_from_str` unknown-token path | low | mitigate | Unknown `transforms` or `depth_method` tokens hit the wildcard arm and raise a `ValueError` naming the offending token; no fall-through to a wrong transform/method |
| T-39b-02 | Denial of Service (panic) | the four detectors on below-min-n/m data | medium | mitigate | tvdmss (n>=3), muod (n>=3,m>=2), sequential_transform_outliers (n>=2, D1/D2 need m>=2), depthgram (n>=2) guards live in fdars-core; degenerate input returns `FdarError` surfaced as `ValueError` via `to_pyresult()` — no panic, no `.unwrap()` added |
| T-39b-03 | Tampering / integer wrap | `Vec<usize>` index-set → `x as i64` cast in the converters | low | accept | Array indices are bounded by the input row count; `usize → i64` is safe on all supported 64-bit targets (same pattern as the shipped `boxplot_result_to_pydict`) |

No package installs in this plan — `fdars-core 0.23.0` was pinned/audited in Phase 36. No supply-chain threat applies.
</threat_model>

<verification>
- `.venv/bin/python -m maturin develop` builds green after each task (the tracer proves the config-struct → #[non_exhaustive] result → dict path first).
- `.venv/bin/python -m pytest tests/test_outliers.py -q` green: tvdmss (4-key, list[int]+ndarray), muod (6-key + degenerate ValueError), sequential_transform_outliers (list[dict] per-transform + invalid-transform ValueError + depth reuse), depthgram (10-key + determinism).
- `.venv/bin/python -m pytest tests/ -q` full suite green, zero regression.
- `cargo fmt --check` clean; `cargo clippy -- -D warnings` clean; no `.unwrap()`/`.expect()`.
</verification>

<success_criteria>
- `fdars.outliers.tvdmss` / `.muod` / `.sequential_transform_outliers(transforms=[...])` / `.depthgram` each return a dict with outlier index sets as Python `list[int]` and scores as 1-D numpy arrays — OUTL-01..04.
- `transforms` string-dispatches a `#[non_exhaustive]` `SeqTransform` with a `ValueError` wildcard; `per_transform_outliers` is exposed as `list[dict]`; `depth_method` reuses the depth dispatcher.
- No config carries a seed field and no core fn takes argvals — no seed kwarg and no forwarded grid added; all four detectors deterministic; all fallible calls via `to_pyresult()` (no `.unwrap()`); full suite + fmt + clippy green.
</success_criteria>

<output>
Create `.planning/phases/39-group-c-depth-outliers-interval-inference-bindings/39-02-SUMMARY.md` when done.
</output>

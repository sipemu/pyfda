---
phase: 68-function-on-function-scalar-on-function-regression
plan: 03
type: execute
wave: 3
depends_on: ["68-02"]
files_modified:
  - src/scalar_on_function_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_scalar_on_function.py
autonomous: true
requirements: [REG-03]
estimate:
  tokens: 78000
  raw_tokens: 44000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "import fdars.scalar_on_function succeeds — the new submodule is registered in lib.rs and __init__.py (REG-03)"
    - "fam runs on a non-square (N=30, M=20) fixture returning fitted_values (30,), component_fits as a list, and a 7-key PyDict"
    - "fregre_gkam runs on a 2-predictor list [(30,20),(30,15)] returning converged (bool) and bandwidths (2,)"
    - "fregre_gsam runs (single predictor) with fam-shaped keys; variable_selection returns active_predictors (2,) and rejects invalid penalty with ValueError; model_selection_ncomp returns best_ncomp >= 1"
  artifacts:
    - src/scalar_on_function_mod.rs
    - src/lib.rs
    - python/fdars/__init__.py
    - tests/test_scalar_on_function.py
  key_links:
    - "scalar_on_function_mod::register wired via register_submodule! in lib.rs and 'scalar_on_function' added to __init__.py _submodule_names"
    - "multi-predictor Vec<FdMatrix> to Vec<&FdMatrix> ref-collection for fregre_gkam and variable_selection"
    - "#[non_exhaustive] configs built via Default::default() + field mutation; VarSelectPenalty via Err-returning penalty_from_str"
---

<objective>
Stand up the NEW `fdars.scalar_on_function` submodule and bind its five additive/generalized/selection functions: `fam`, `fregre_gkam`, `fregre_gsam`, `variable_selection`, `model_selection_ncomp`. Lead with a submodule-registration tracer (`fam` only) to prove the module wires up through `lib.rs` + `__init__.py`, then fan out the remaining four.

Purpose: Complete REG-03. This is the second, disjoint binding group — a brand-new submodule file plus two registration edits — distinct from the `fdars.regression` extension in plans 01/02.
Output: `src/scalar_on_function_mod.rs` (new), `src/lib.rs` + `python/fdars/__init__.py` (registration edits), and `tests/test_scalar_on_function.py` (new).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_this_phase_produces>
This plan (68-03) produces: the new `fdars.scalar_on_function` submodule with `fam`, `fregre_gkam`, `fregre_gsam`, `variable_selection`, `model_selection_ncomp`, plus `scalar_on_function_mod::register`, its `mod` + `register_submodule!` wiring in lib.rs, and the `"scalar_on_function"` entry in __init__.py.
Note: `model_selection_ncomp` is COPIED verbatim from the existing regression_mod.rs binding — regression_mod.rs is NOT modified by this plan (68-RESEARCH Pitfall 6).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "import fdars.scalar_on_function + fam" — new submodule wired through every layer</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§3 fam signature + FamConfig fields, §4 FamResult 7-key fields + component_fits Vec<Vec<f64>> conversion, §8 config-struct Default::default()+mutation, §9 all three registration edits + the new-file skeleton, §10 fam PyDict table, §13 Option<FdMatrix> scalar_covariates pattern, Pitfall 4)
    - src/lib.rs (mod declarations block :8-30, register_submodule! block :44-64 — mirror the fts_mod lines)
    - python/fdars/__init__.py (_submodule_names tuple :35-57, docstring bullet :20)
    - src/regression_mod.rs (file head imports :1-5 for the module skeleton; fregre_np_cv scalar_covariates Option pattern for `.map(numpy2d_to_fdmatrix).transpose()?`)
    - src/convert.rs (numpy2d_to_fdmatrix, fdmatrix_to_numpy2d, numpy1d_to_vec, vec_to_numpy1d, to_pyresult)
  </read_first>
  <files>src/scalar_on_function_mod.rs, src/lib.rs, python/fdars/__init__.py</files>
  <action>
    Create NEW src/scalar_on_function_mod.rs with the module skeleton from 68-RESEARCH §9 (module doc comment; `use crate::convert::*;`, `use numpy::{PyReadonlyArray1, PyReadonlyArray2};`, `use pyo3::prelude::*;`, `use pyo3::types::{PyDict, PyList};`). Add ONLY the `fam` binding and a `register` fn that registers only `fam` in this task (the other four are added in tasks 2-3 of this plan).

    `fam` — the scalar response `y` is a required argument. Signature `#[pyo3(signature = (data, y, argvals, scalar_covariates=None, ncomp=0, bandwidth=0.0, kernel="gaussian", n_grid_bandwidth=20))]` where `data: PyReadonlyArray2`, `y: PyReadonlyArray1<'py, f64>`, `argvals: PyReadonlyArray1`, `scalar_covariates: Option<PyReadonlyArray2<'py, f64>>`. Body: `data` via numpy2d_to_fdmatrix; `y` via numpy1d_to_vec; `argvals` via numpy1d_to_vec; `let sc = scalar_covariates.map(numpy2d_to_fdmatrix).transpose()?;` (68-RESEARCH §13). Build config via Default + mutation (68-RESEARCH §8 — FamConfig is #[non_exhaustive], NO struct literal): `let mut cfg = fdars_core::scalar_on_function::FamConfig::default(); cfg.ncomp = ncomp; cfg.bandwidth = bandwidth; cfg.kernel = kernel.to_string(); cfg.n_grid_bandwidth = n_grid_bandwidth;`. Call `fdars_core::scalar_on_function::fam(&mat, &y, &av, sc.as_ref(), &cfg)` wrapped in to_pyresult. Assemble a PyDict with the 7 keys from 68-RESEARCH §10 fam table: fitted_values (vec_to_numpy1d), residuals (vec_to_numpy1d), component_fits (Vec<Vec<f64>> to a PyList of numpy 1D per the §4 conversion code), intercept (f64), bandwidths (vec_to_numpy1d), ncomp (usize), r_squared (f64). Exclude the embedded `fpca` field.

    Wire the submodule (68-RESEARCH §9 Edits 1 & 3) using scoped Edit (do NOT rewrite the whole files):
    - src/lib.rs: add `mod scalar_on_function_mod;` alongside the other `mod` declarations, and `register_submodule!(m, "scalar_on_function", scalar_on_function_mod::register);` after the `fts` register line.
    - python/fdars/__init__.py: add `"scalar_on_function",  # Phase 68 — Scalar-on-Function additive/selection regression` to the `_submodule_names` tuple after `"fts",`, and extend the module docstring bullet list near line 20 to mention scalar-on-function additive/generalized/selection models.

    No unused imports — PyList is used by component_fits; keep it. CI runs `-D warnings`.
  </action>
  <acceptance_criteria>
    - `import fdars.scalar_on_function as sof` succeeds after maturin develop.
    - `sof.fam` is callable and returns a 7-key dict {fitted_values, residuals, component_fits, intercept, bandwidths, ncomp, r_squared} with no "fpca" key.
    - Build warning-clean (new submodule registers cleanly; full suite stays green).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.scalar_on_function as sof; assert callable(sof.fam); print('scalar_on_function + fam wired OK')"</automated>
    <fails_when>maturin build fails (compile / -D warnings / struct-literal on #[non_exhaustive] config), or import fdars.scalar_on_function raises ImportError/AttributeError because the submodule was not registered in lib.rs or __init__.py.</fails_when>
  </verify>
  <done>The new fdars.scalar_on_function submodule is registered end-to-end and fam is callable — the module wire-up is proven.</done>
</task>

<task type="auto">
  <name>Task 2: Bind fregre_gsam (single-predictor) + fregre_gkam (multi-predictor list)</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§3 fregre_gsam & fregre_gkam signatures + GsamConfig/GkamConfig fields, §4 GsamResult & GkamResult fields, §8 multi-predictor Vec<FdMatrix> to Vec<&FdMatrix> + argvals_list ref-collection, §10 fregre_gsam/fregre_gkam PyDict tables, Pitfall 3, Pitfall 4)
    - src/scalar_on_function_mod.rs (the fam binding from task 1 — reuse the config-build + component_fits PyList patterns)
    - src/regression_mod.rs (concurrent_regression at :1038-1060 — the exact multi-predictor Vec<PyReadonlyArray2> conversion)
  </read_first>
  <files>src/scalar_on_function_mod.rs</files>
  <action>
    Add two `#[pyfunction]`s to src/scalar_on_function_mod.rs.

    `fregre_gsam` — identical input shape to fam (single predictor). Signature `#[pyo3(signature = (data, y, argvals, scalar_covariates=None, ncomp=0, bandwidth=0.0, kernel="gaussian", n_grid_bandwidth=20))]`. Build `GsamConfig` via Default::default() + field mutation (ncomp, bandwidth, kernel.to_string(), n_grid_bandwidth). Call `fdars_core::scalar_on_function::fregre_gsam(&mat, &y, &av, sc.as_ref(), &cfg)` (to_pyresult). Assemble the same 7-key PyDict as fam (68-RESEARCH §10: "Same keys as fam"), excluding `fpca`.

    `fregre_gkam` — multi-predictor. Signature `#[pyo3(signature = (predictors, y, argvals_list, scalar_covariates=None, bandwidth=0.0, kernel="gaussian", max_iter=50, epsilon=1e-6))]` where `predictors: Vec<PyReadonlyArray2<'py, f64>>` and `argvals_list: Vec<PyReadonlyArray1<'py, f64>>`. Convert per 68-RESEARCH §8 / Pitfall 3: build `pred_mats: Vec<FdMatrix>` via `predictors.into_iter().map(numpy2d_to_fdmatrix).collect::<PyResult<Vec<_>>>()?`, then `let pred_refs: Vec<&fdars_core::matrix::FdMatrix> = pred_mats.iter().collect();`; build `argvals_vecs: Vec<Vec<f64>>` via `argvals_list.into_iter().map(numpy1d_to_vec).collect()`, then `let argvals_refs: Vec<&[f64]> = argvals_vecs.iter().map(|v| v.as_slice()).collect();`. Build `GkamConfig` via Default + mutation (bandwidth, kernel.to_string(), max_iter, epsilon). Call `fdars_core::scalar_on_function::fregre_gkam(&pred_refs, &y, &argvals_refs, sc.as_ref(), &cfg)` (to_pyresult). Assemble the 8-key PyDict from 68-RESEARCH §10 fregre_gkam table: fitted_values, residuals, component_fits (PyList), intercept, bandwidths, iterations (usize), converged (bool), r_squared.

    Register both in the `register` fn. Add `///` docstrings. No unused imports (`-D warnings`).
  </action>
  <acceptance_criteria>
    - `sof.fregre_gsam` (single predictor, (30,20) fixture) returns a 7-key dict matching fam's keys.
    - `sof.fregre_gkam` with a 2-predictor list [(30,20),(30,15)] returns converged as a Python bool and bandwidths of shape (2,).
    - Build warning-clean; no lifetime error from the ref-collection.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.scalar_on_function as sof; assert callable(sof.fregre_gsam) and callable(sof.fregre_gkam); print('gsam + gkam bound OK')"</automated>
    <fails_when>maturin build fails (compile / -D warnings / lifetime error on the Vec<&FdMatrix> ref-collection), or either function is missing from register() (AttributeError).</fails_when>
  </verify>
  <done>fregre_gsam and fregre_gkam are bound, registered, and build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind variable_selection (enum penalty) + model_selection_ncomp (copied) + full sof test file</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§2 VarSelectPenalty penalty_from_str, §3 variable_selection & model_selection_ncomp signatures + VarSelectConfig fields, §4 VarSelectResult + ModelSelectionResult fields + active_predictors/coefficients conversion, §8 SelectionCriterion dispatch, §10 variable_selection/model_selection_ncomp PyDict tables, §11 test coverage items 8-13 + SOF fixtures, Pitfall 5, Pitfall 6)
    - src/scalar_on_function_mod.rs (the fam + gkam bindings — reuse config, multi-predictor, and PyList patterns)
    - src/regression_mod.rs (model_selection_ncomp at :432-458 — COPY this binding VERBATIM into the new module; do NOT modify regression_mod.rs)
    - src/convert.rs (bool_vec_to_numpy1d for active_predictors)
    - tests/test_fof_regression.py (from plan 01/02 — mirror its fixture + pytest.raises style; separate file, do not edit it)
  </read_first>
  <files>src/scalar_on_function_mod.rs, tests/test_scalar_on_function.py</files>
  <behavior>
    - Test: fam on the (30,20) fixture returns fitted_values.shape == (30,) and component_fits is a list.
    - Test: fregre_gsam returns keys matching fam's keys.
    - Test: fregre_gkam on a 2-predictor list [(30,20),(30,15)] returns converged as bool, bandwidths.shape == (2,).
    - Test: variable_selection on a 2-predictor list returns active_predictors.shape == (2,) and coefficients as a list of 2 arrays; penalty="group_lasso" and penalty="ls" both succeed; an invalid penalty (e.g. "group_mcp") raises ValueError.
    - Test: model_selection_ncomp returns best_ncomp >= 1 and criteria as a list of tuples; criterion="aic"/"bic"/"gcv" all succeed.
  </behavior>
  <action>
    First add the internal helper `fn penalty_from_str(s: &str) -> PyResult<...VarSelectPenalty>` VERBATIM from 68-RESEARCH §2 (match "group_lasso"/"ls"; Err-returning wildcard arm — MANDATORY because VarSelectPenalty is #[non_exhaustive]; the message notes GroupMcp/GroupScad are not implemented upstream).

    `variable_selection` — multi-predictor (same conversion as fregre_gkam). Signature `#[pyo3(signature = (predictors, y, argvals_list, scalar_covariates=None, ncomp=3, penalty="group_lasso", lambda_=0.0, max_iter=100, epsilon=1e-5, lambda_n_grid=20))]` — note `lambda_` (trailing underscore) to avoid the Python keyword collision (68-RESEARCH §3). Build `VarSelectConfig` via Default + mutation: ncomp, penalty = penalty_from_str(penalty)?, lambda = lambda_, max_iter, epsilon, lambda_n_grid (VarSelectConfig is #[non_exhaustive] — use Default + mutation). Call `fdars_core::scalar_on_function::variable_selection(&pred_refs, &y, &argvals_refs, sc.as_ref(), &cfg)` (to_pyresult). Assemble the 9-key PyDict from 68-RESEARCH §10: active_predictors (bool_vec_to_numpy1d), coefficients (Vec<Vec<f64>> to PyList of numpy 1D), fitted_values, residuals, intercept, lambda (f64), r_squared, iterations, converged. Exclude `fpcas`.

    `model_selection_ncomp` — COPY the binding VERBATIM from regression_mod.rs:432-458 (signature `(data, response, max_comp=10, criterion="gcv")`, the aic/bic/gcv match with default arm — preserve it, do NOT convert to an Err-arm since SelectionCriterion is NOT #[non_exhaustive]; 2-key dict best_ncomp + criteria). Do NOT touch regression_mod.rs (Pitfall 6 — a second registration of the same logic in the new submodule is intended).

    Register both new functions plus model_selection_ncomp in the `register` fn (final register block registers all five: fam, fregre_gkam, fregre_gsam, variable_selection, model_selection_ncomp — 68-RESEARCH §9).

    Create NEW tests/test_scalar_on_function.py: import-smoke (`import fdars.scalar_on_function as sof`), the SOF fixtures from 68-RESEARCH §11 (single-predictor (N=30, M=20); 2-predictor with M2=15), and the six tests in the behavior block. Use `pytest.raises(ValueError)` for the invalid-penalty case.
  </action>
  <acceptance_criteria>
    - variable_selection returns active_predictors.shape == (2,) on the 2-predictor fixture; penalty="ls" and "group_lasso" both work; penalty="group_mcp" raises ValueError.
    - model_selection_ncomp returns best_ncomp >= 1 with criterion in {aic,bic,gcv}; regression_mod.rs is unchanged (git shows no diff there).
    - All tests in tests/test_scalar_on_function.py pass.
    - Build warning-clean.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/pytest tests/test_scalar_on_function.py -x -q 2>&1 | tail -20</automated>
    <fails_when>pytest reports a failure/error: a shape assertion mismatch, ValueError not raised for the invalid penalty (missing Err-arm), an AttributeError (function not bound), or a maturin build failure.</fails_when>
  </verify>
  <done>variable_selection and model_selection_ncomp are bound; the full scalar_on_function test file passes; regression_mod.rs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (native) | User numpy arrays, predictor lists, and penalty/criterion strings cross into Rust; enum strings validated at the binding boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-06 | Tampering | variable_selection penalty string | medium | mitigate | penalty_from_str rejects unknown/unimplemented penalties (GroupMcp/GroupScad) at the binding with a clear ValueError before upstream (68-RESEARCH §2); mandatory Err-arm since #[non_exhaustive] |
| T-68-07 | Information Disclosure | Embedded FpcaResult / Vec<FpcaResult> in additive results | low | mitigate | fpca/fpcas excluded from all PyDicts; tests assert key-sets |
| T-68-08 | Denial of Service | Malformed multi-predictor lists (mismatched lengths) | low | accept | fdars-core raises FdarError→ValueError on shape/length mismatch across predictors/argvals_list |
</threat_model>

<verification>
- `.venv/bin/maturin develop` warning-clean.
- `.venv/bin/pytest tests/test_scalar_on_function.py -x -q` passes.
- `import fdars.scalar_on_function` succeeds; all five functions callable.
- `git diff --name-only` shows regression_mod.rs unchanged by this plan.
</verification>

<success_criteria>
- REG-03 complete: fam, fregre_gkam, fregre_gsam, variable_selection, model_selection_ncomp callable via the new fdars.scalar_on_function submodule.
- Submodule registered cleanly (lib.rs + __init__.py); full test suite stays green.
</success_criteria>

<output>
Create `.planning/phases/68-function-on-function-scalar-on-function-regression/68-03-SUMMARY.md` when done
</output>

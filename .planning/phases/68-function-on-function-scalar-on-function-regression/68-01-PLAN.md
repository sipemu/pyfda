---
phase: 68-function-on-function-scalar-on-function-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/regression_mod.rs
  - tests/test_fof_regression.py
autonomous: true
requirements: [REG-01]
estimate:
  tokens: 55000
  raw_tokens: 32000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "fdars.regression.fof_regression runs end-to-end on a non-square (N=30, MX=25, MY=18) fixture and returns a PyDict (REG-01)"
    - "beta_surface has shape (MY, MX) = (18, 25) — rows index response grid, cols index predictor grid (transposition-guarded)"
  artifacts:
    - src/regression_mod.rs
    - tests/test_fof_regression.py
  key_links:
    - "fof_regression registered in regression_mod.rs::register so fdars.regression.fof_regression is callable after maturin develop"
---

<objective>
Tracer slice for Phase 68: bind ONE representative dual-2D function — `fof_regression` — end-to-end into the existing `fdars.regression` submodule, proving the full binding architecture (dual `numpy2d_to_fdmatrix` conversion, PyDict assembly excluding embedded FPCA state, `beta_surface (m_y, m_x)` shape) against a 3-distinct-dimension non-square fixture before the FOF family fans out in plan 02.

Purpose: De-risk the dual-2D-input pattern and the `beta_surface` shape mapping on the agent's best early-context tokens. If the transposition or shape is wrong, it surfaces after one commit instead of after all five FOF functions are written.
Output: `fof_regression` callable via `fdars.regression`, one registration edit, and a non-square end-to-end test that asserts the beta-surface shape. Partial REG-01 (fit half; `predict_fof` completes REG-01 in plan 02).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_this_phase_produces>
This plan (68-01) produces: `fof_regression` added to `fdars.regression`, and its registration in `regression_mod.rs::register`.
Later plans add: `predict_fof`, `fof_cv`, `fof_re_regression`, `predict_fof_re` (plan 02, `fdars.regression`); the new `fdars.scalar_on_function` submodule + `fam`, `fregre_gkam`, `fregre_gsam`, `variable_selection`, `model_selection_ncomp` and `scalar_on_function_mod::register` (plan 03).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "fof_regression via fdars.regression" — one dual-2D path, wired and registered</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§3 fof_regression signature, §4 FofResult fields, §7 transposition, §9 Edit 2 registration, §10 PyDict key table, §11 shape assertions, §13 Architecture Patterns "Thin #[pyfunction] with required argvals")
    - src/regression_mod.rs (file head imports at :1-5; the fpca binding at :23-46 for PyDict + argvals style; concurrent_regression at :1038 for the multi-input analog; the register() block at :1195-1220)
    - src/convert.rs (numpy2d_to_fdmatrix, fdmatrix_to_numpy2d, numpy1d_to_vec, vec_to_numpy1d, to_pyresult — all names verbatim)
  </read_first>
  <files>src/regression_mod.rs</files>
  <action>
    Add a `pub fn fof_regression` `#[pyfunction]` to src/regression_mod.rs (place it near the other FPC-based bindings, before the `register` block). Signature per 68-RESEARCH §3: `#[pyo3(signature = (x_data, y_data, x_argvals, y_argvals, ncomp_x=3, ncomp_y=3))]`, taking `x_data: PyReadonlyArray2`, `y_data: PyReadonlyArray2`, `x_argvals: PyReadonlyArray1`, `y_argvals: PyReadonlyArray1`, `ncomp_x: usize`, `ncomp_y: usize`, returning `PyResult<Bound<'py, PyAny>>`.

    Body: convert BOTH `x_data` and `y_data` through `numpy2d_to_fdmatrix` (this is the dual-2D path — the whole point of the tracer); convert both argvals through `numpy1d_to_vec`; call `fdars_core::fof_regression::fof_regression(&x_mat, &y_mat, &ax, &ay, ncomp_x, ncomp_y)` wrapped in `to_pyresult`. Assemble a PyDict with EXACTLY the 9 keys from 68-RESEARCH §10 (fof_regression table): intercept (vec_to_numpy1d), beta_surface (fdmatrix_to_numpy2d), fitted (fdmatrix_to_numpy2d), residuals (fdmatrix_to_numpy2d), r_squared_t (vec_to_numpy1d), r_squared (f64), ncomp_x (usize), ncomp_y (usize), coef_matrix (fdmatrix_to_numpy2d). Do NOT expose `fpca_x` or `fpca_y` — they are embedded internal FPCA state (68-RESEARCH §4, Pitfall 1).

    Register it: add `m.add_function(wrap_pyfunction!(fof_regression, m)?)?;` to the `register` block at the end of the file (68-RESEARCH §9 Edit 2 — add only the fof_regression line in this plan; the other four fof lines are added in plan 02).

    Add a NumPy-format doc comment (`///`) above the function following the fpca binding's docstring style, documenting the (m_y, m_x) orientation of beta_surface. Keep imports clean — `wrap_pyfunction!` and `PyDict` are already reachable in this module; add no unused imports (CI runs `-D warnings`).
  </action>
  <acceptance_criteria>
    - `fdars.regression.fof_regression` is callable after `.venv/bin/maturin develop`.
    - Returns a dict whose keys are exactly {intercept, beta_surface, fitted, residuals, r_squared_t, r_squared, ncomp_x, ncomp_y, coef_matrix} — no fpca keys present.
    - `beta_surface` has shape (18, 25) on the (N=30, MX=25, MY=18) fixture (rows=m_y, cols=m_x).
    - `cargo build` produces zero warnings (no unused imports).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.regression as r; assert callable(r.fof_regression); print('fof_regression bound OK')"</automated>
    <fails_when>maturin build fails (Rust compile error / -D warnings on unused import), or the import/callable assertion raises AttributeError because fof_regression was not added to register().</fails_when>
  </verify>
  <done>fof_regression is bound into fdars.regression, registered, builds clean, and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-square end-to-end test proving the beta_surface (m_y, m_x) shape</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§11 non-square FOF fixture code, §11 shape assertions, Pitfall 2 beta_surface shape confusion)
    - tests/test_fts.py (Phase 67 test file — mirror its import + fixture + assert style; DO NOT edit it, read only for convention)
  </read_first>
  <files>tests/test_fof_regression.py</files>
  <behavior>
    - Test: building the (N=30, MX=25, MY=18) fixture per 68-RESEARCH §11 and calling fof_regression(ncomp_x=3, ncomp_y=3) returns a dict.
    - Test: r["beta_surface"].shape == (18, 25) — the load-bearing transposition guard (MX != MY so a swap is caught).
    - Test: r["fitted"].shape == (30, 18); r["intercept"].shape == (18,); r["r_squared_t"].shape == (18,); r["coef_matrix"].shape == (3, 3).
    - Test: the returned dict has no "fpca_x"/"fpca_y" keys.
  </behavior>
  <action>
    Create NEW tests/test_fof_regression.py. Add a module-level `pytest` fixture (or helper) that builds the non-square FOF fixture verbatim from 68-RESEARCH §11 (N, MX, MY = 30, 25, 18; seeded rng(42); x_data, y_data, x_argvals, y_argvals; assert all three dims distinct). Write an import-smoke test (`import fdars.regression`) and a `test_fof_regression_shapes` test asserting the shapes in the behavior block above, plus that `set(r) == {"intercept","beta_surface","fitted","residuals","r_squared_t","r_squared","ncomp_x","ncomp_y","coef_matrix"}`.

    Keep this file self-contained so plan 02 can append predict/cv/re tests to it. Do not import anything from fdars.scalar_on_function (does not exist until plan 03).
  </action>
  <acceptance_criteria>
    - `tests/test_fof_regression.py` exists and its fof_regression tests pass under pytest.
    - The beta_surface assertion is `== (MY, MX) == (18, 25)`, NOT (25, 18) — a swapped implementation fails this test.
    - The key-set assertion excludes fpca_x/fpca_y.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_fof_regression.py -x -q 2>&1 | tail -15</automated>
    <fails_when>pytest reports a failure/error: shape mismatch on beta_surface (transposition bug), missing key, or an unexpected fpca key in the returned dict.</fails_when>
  </verify>
  <done>The non-square fof_regression test passes, proving the dual-2D binding and the (m_y, m_x) beta-surface orientation end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (native) | User-supplied numpy arrays (x_data, y_data, argvals) cross into Rust; shape/dim validated by fdars-core guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-01 | Denial of Service | fof_regression with degenerate/huge input | low | accept | Pure synchronous numerical compute; caller controls array size; fdars-core raises FdarError→ValueError on invalid dims |
| T-68-02 | Information Disclosure | Embedded FpcaResult exposure | low | mitigate | fpca_x/fpca_y intentionally excluded from PyDict (68-RESEARCH Pitfall 1); test asserts key-set excludes them |
| T-68-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies added this phase (68-RESEARCH "Standard Stack"); no package-legitimacy gate required |
</threat_model>

<verification>
- `.venv/bin/maturin develop` succeeds with zero warnings (`-D warnings`).
- `.venv/bin/pytest tests/test_fof_regression.py -x -q` passes.
- `import fdars.regression as r; r.fof_regression` is callable.
</verification>

<success_criteria>
- fof_regression callable via fdars.regression, returning the 9-key PyDict.
- beta_surface shape proven (18, 25) on the non-square fixture (REG-01 fit path).
</success_criteria>

<output>
Create `.planning/phases/68-function-on-function-scalar-on-function-regression/68-01-SUMMARY.md` when done
</output>

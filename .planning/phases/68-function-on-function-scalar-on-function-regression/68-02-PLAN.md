---
phase: 68-function-on-function-scalar-on-function-regression
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/regression_mod.rs
  - tests/test_fof_regression.py
autonomous: true
requirements: [REG-01, REG-02]
estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "fdars.regression.predict_fof runs (combined-refit, stateless) and returns a numpy (n_new, m_y) = (10, 18) array — completing REG-01 predict"
    - "fdars.regression.fof_cv returns candidates (list of 2-tuples), optimal (2-tuple), min_cv_mse > 0"
    - "fdars.regression.fof_re_regression validates subject_ids (length + >=2 distinct groups raising ValueError) and returns random_effects (5, 18) and n_subjects == 5 (REG-02)"
    - "fdars.regression.predict_fof_re runs (combined-refit) and returns numpy (10, 18)"
  artifacts:
    - src/regression_mod.rs
    - tests/test_fof_regression.py
  key_links:
    - "predict_fof/predict_fof_re refit internally (no pyclass handle) — raw train data + new_x in, numpy 2D out"
    - "all four functions registered in regression_mod.rs::register"
---

<objective>
Expand the FOF family out from the plan-01 tracer: bind the remaining four `fdars.regression` functions — `predict_fof`, `fof_cv`, `fof_re_regression`, `predict_fof_re` — using the combined-refit stateless predict pattern and REG-02 subject-id validation.

Purpose: Complete REG-01 (fof predict) and REG-02 (random-effects fit + predict + subject-id validation). All four reuse the dual-2D conversion pattern proven in plan 01.
Output: Four new `#[pyfunction]`s registered in `fdars.regression`, plus predict/cv/re tests appended to `tests/test_fof_regression.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_this_phase_produces>
This plan (68-02) produces: `predict_fof`, `fof_cv`, `fof_re_regression`, `predict_fof_re` added to `fdars.regression` and registered in `regression_mod.rs::register`.
Depends on plan 01's `fof_regression` being present. Plan 03 adds the separate `fdars.scalar_on_function` submodule.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bind predict_fof + fof_cv (combined-refit predict; CV PyDict)</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§3 predict_fof & fof_cv signatures, §4 FofCvResult fields, §5 combined-refit predict_fof binding code, §10 fof_cv PyDict key table, §13 "Combined-refit predict" pattern, Pitfall 7 fof_cv seed u64)
    - src/regression_mod.rs (the fof_regression binding added in plan 01 — mirror its dual-2D conversion; the register() block)
    - src/convert.rs (numpy2d_to_fdmatrix, fdmatrix_to_numpy2d, numpy1d_to_vec, vec_to_numpy1d, to_pyresult)
  </read_first>
  <files>src/regression_mod.rs</files>
  <action>
    Add two `#[pyfunction]`s to src/regression_mod.rs.

    `predict_fof` — combined-refit, stateless (locked CONTEXT decision, 68-RESEARCH §5). Signature `#[pyo3(signature = (x_data, y_data, new_x, x_argvals, y_argvals, ncomp_x=3, ncomp_y=3))]`. Body: convert x_data, y_data, new_x via numpy2d_to_fdmatrix; argvals via numpy1d_to_vec; call `fof_regression(&x_mat, &y_mat, &ax, &ay, ncomp_x, ncomp_y)` (to_pyresult), then `predict_fof(&fit, &new_x_mat)` (to_pyresult). Return `fdmatrix_to_numpy2d(py, &predicted).into_any()` — a numpy 2D of shape (n_new, m_y), NOT a PyDict.

    `fof_cv` — signature `#[pyo3(signature = (x_data, y_data, x_argvals, y_argvals, ncomp_x_max=5, ncomp_y_max=5, n_folds=5, seed=42))]` with `seed: u64`. Convert inputs; call `fdars_core::fof_regression::fof_cv(&x_mat, &y_mat, &ax, &ay, ncomp_x_max, ncomp_y_max, n_folds, seed)` (to_pyresult). Assemble a PyDict with the 4 keys from 68-RESEARCH §10 fof_cv table: candidates (convert `Vec<(usize,usize)>` to `Vec<(i64,i64)>` per §4), cv_errors (vec_to_numpy1d), optimal (`(i64, i64)` tuple per §4), min_cv_mse (f64).

    Register both: add `predict_fof` and `fof_cv` lines to the register block. Add `///` NumPy docstrings. No unused imports (`-D warnings`).
  </action>
  <acceptance_criteria>
    - `fdars.regression.predict_fof` returns a numpy 2D of shape (10, 18) on the fixture (not a dict).
    - `fdars.regression.fof_cv` returns a dict with keys {candidates, cv_errors, optimal, min_cv_mse}; candidates is a list of 2-tuples; optimal is a 2-tuple; min_cv_mse > 0.
    - Build is warning-clean.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.regression as r; assert callable(r.predict_fof) and callable(r.fof_cv); print('predict_fof + fof_cv bound OK')"</automated>
    <fails_when>maturin build fails (compile / -D warnings), or either function is missing from register() (AttributeError on callable assertion).</fails_when>
  </verify>
  <done>predict_fof and fof_cv are bound, registered, and build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Bind fof_re_regression (subject-id validation, REG-02) + predict_fof_re</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§3 fof_re_regression & predict_fof_re signatures, §4 FofReResult 13-key fields, §5 predict_fof_re combined-refit + FofReConfig struct literal, §6 subject-id validation code, §10 fof_re_regression PyDict table, Pitfall 8)
    - src/regression_mod.rs (the fof_regression binding from plan 01; fanova at :400-404 for the numpy1d_to_usize_vec subject-id conversion pattern)
    - src/convert.rs (numpy1d_to_usize_vec — takes PyReadonlyArray1<i64>)
  </read_first>
  <files>src/regression_mod.rs</files>
  <action>
    Add two `#[pyfunction]`s.

    `fof_re_regression` — signature `#[pyo3(signature = (x_data, y_data, subject_ids, x_argvals, y_argvals, ncomp_x=3, ncomp_y=3, max_iter=50, tol=1e-10))]`, with `subject_ids: PyReadonlyArray1<'py, i64>`. Convert x_data/y_data via numpy2d_to_fdmatrix; subject_ids via numpy1d_to_usize_vec; argvals via numpy1d_to_vec. REG-02 validation BEFORE the core call, verbatim per 68-RESEARCH §6: (a) if `sid.len() != x_mat.nrows()` raise PyValueError with the length-mismatch message; (b) compute distinct count via sort_unstable + dedup on a clone, and if `< 2` raise PyValueError "subject_ids must contain at least 2 distinct subjects for random-effects regression". Build `FofReConfig { ncomp_x, ncomp_y, max_iter, tol }` (struct literal — FofReConfig is NOT #[non_exhaustive], 68-RESEARCH §5/§8). Call `fdars_core::fof_regression::fof_re_regression(&x_mat, &y_mat, &sid, &ax, &ay, &config)` (to_pyresult). Assemble PyDict with the 13 keys from 68-RESEARCH §10 fof_re_regression table (intercept, beta_surface, fitted, residuals, r_squared_t, r_squared, ncomp_x, ncomp_y, coef_matrix, random_effects, sigma2_u, sigma2_eps, n_subjects) — exclude fpca_x/fpca_y. random_effects and coef_matrix and beta_surface/fitted/residuals via fdmatrix_to_numpy2d; sigma2_u via vec_to_numpy1d.

    `predict_fof_re` — combined-refit (68-RESEARCH §5). Signature `#[pyo3(signature = (x_data, y_data, subject_ids, new_x, x_argvals, y_argvals, ncomp_x=3, ncomp_y=3, max_iter=50, tol=1e-10))]`. Reuse the SAME subject-id validation as fof_re_regression (factor into a small `fn` helper if it keeps the file clean, else inline). Build FofReConfig, call fof_re_regression to get `fit`, then `predict_fof_re(&fit, &new_x_mat)` (to_pyresult). Return `fdmatrix_to_numpy2d(py, &predicted).into_any()` — numpy (n_new, m_y).

    Register both. Add `///` docstrings documenting that subject_ids must be non-negative i64 with >=2 distinct groups, and that unseen subjects at predict time get population-level (fixed-effect-only) prediction (68-RESEARCH §3). No unused imports.
  </action>
  <acceptance_criteria>
    - `fof_re_regression` on the fixture (subject_ids = [i//6 for i in range(30)], 5 groups) returns random_effects.shape == (5, 18), sigma2_u.shape == (3,), n_subjects == 5.
    - Passing subject_ids of wrong length raises ValueError; passing all-same subject_ids (1 group) raises ValueError.
    - `predict_fof_re` returns numpy (10, 18).
    - Build warning-clean.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/maturin develop 2>&1 | tail -3 && .venv/bin/python -c "import fdars.regression as r; assert callable(r.fof_re_regression) and callable(r.predict_fof_re); print('fof_re bound OK')"</automated>
    <fails_when>maturin build fails, or either function missing from register() (AttributeError), or FofReConfig struct-literal fails to compile (would indicate a field-name mismatch vs 68-RESEARCH §5).</fails_when>
  </verify>
  <done>fof_re_regression (with subject-id validation) and predict_fof_re are bound, registered, and build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append predict/cv/re tests to tests/test_fof_regression.py</name>
  <read_first>
    - .planning/phases/68-function-on-function-scalar-on-function-regression/68-RESEARCH.md (§11 shape assertions for predict_fof/fof_re/predict_fof_re, §11 minimum coverage items 3-7 and 13, §11 subject_ids fixture)
    - tests/test_fof_regression.py (the file created in plan 01 — reuse its existing non-square fixture; do NOT duplicate the fixture, extend the file)
  </read_first>
  <files>tests/test_fof_regression.py</files>
  <behavior>
    - Test: predict_fof(x_data, y_data, new_x, x_argvals, y_argvals) returns array of shape (10, 18).
    - Test: fof_cv returns dict; candidates is a list of 2-tuples; optimal is a 2-tuple of ints; min_cv_mse > 0.
    - Test: fof_re_regression with subject_ids (5 groups of 6) returns random_effects.shape == (5, 18), n_subjects == 5, sigma2_u.shape == (3,).
    - Test: predict_fof_re returns (10, 18).
    - Test: fof_re_regression with wrong-length subject_ids raises ValueError; with a single-group subject_ids raises ValueError.
    - Test: fof_regression with ncomp_x=0 raises ValueError; fof_cv with n_folds > n raises ValueError (error-guard coverage, §11 item 13).
  </behavior>
  <action>
    Append tests to the EXISTING tests/test_fof_regression.py (created in plan 01), reusing its non-square fixture (add `new_x` of shape (10, MX) and `subject_ids = np.array([i // 6 for i in range(N)], dtype=np.int64)` per 68-RESEARCH §11 if not already present). Add: `test_predict_fof_shape`, `test_fof_cv`, `test_fof_re_regression_shapes`, `test_predict_fof_re_shape`, `test_subject_id_validation` (use `pytest.raises(ValueError)` for both wrong-length and single-group), and `test_fof_error_guards` (ncomp_x=0 and n_folds>n via `pytest.raises(ValueError)`).

    Use `pytest.raises(ValueError)` per the project error-handling convention (CLAUDE.md: FdarError→PyValueError). Do not import fdars.scalar_on_function.
  </action>
  <acceptance_criteria>
    - All FOF tests in tests/test_fof_regression.py pass.
    - random_effects assertion is (5, 18); predict outputs are (10, 18).
    - Both subject-id validation failure modes and both error guards are covered by `pytest.raises(ValueError)`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/pytest tests/test_fof_regression.py -x -q 2>&1 | tail -20</automated>
    <fails_when>pytest reports a failure/error: a shape assertion mismatch, a ValueError not raised where expected (validation/guard missing), or an AttributeError (function not bound).</fails_when>
  </verify>
  <done>The full FOF test file passes — fit, predict, cv, random-effects fit+predict, subject-id validation, and error guards all covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (native) | User numpy arrays + subject_ids cross into Rust; binding adds subject-id validation before the core call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-03 | Tampering | fof_re_regression subject_ids (degenerate groups) | medium | mitigate | Binding validates length == n_obs and >= 2 distinct subjects (68-RESEARCH §6), raising ValueError before upstream — prevents meaningless single-group mixed-model fits |
| T-68-04 | Information Disclosure | Embedded FpcaResult in FofReResult | low | mitigate | fpca_x/fpca_y excluded from the 13-key PyDict; test asserts absence |
| T-68-05 | Denial of Service | fof_cv with n_folds > n | low | mitigate | fdars-core raises FdarError→ValueError; covered by error-guard test |
</threat_model>

<verification>
- `.venv/bin/maturin develop` warning-clean.
- `.venv/bin/pytest tests/test_fof_regression.py -x -q` passes (all FOF tests).
- All five FOF functions callable via fdars.regression.
</verification>

<success_criteria>
- REG-01 complete: fof_regression + predict_fof callable, transposition/argvals-guarded.
- REG-02 complete: fof_re_regression + predict_fof_re callable with subject-id validation; random_effects (5, 18) proven.
- fof_cv bound and tested.
</success_criteria>

<output>
Create `.planning/phases/68-function-on-function-scalar-on-function-regression/68-02-SUMMARY.md` when done
</output>

---
phase: 35-docs-diagrams-worked-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/inference/functional-inference.md
  - docs/assets/diagrams/inference-permutation-test.svg
  - docs/assets/diagrams/inference-scb.svg
  - docs/assets/diagrams/inference-anova.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-04, DOCS-07]
estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The built site has a top-level Inference nav section linking to a Functional Inference page (DOCS-04)."
    - "The Functional Inference page documents two-sample tests (t_perm_test/f_perm_test/two_sample_mean_test), SCB bands (mean_scb/scb_two_sample_test), and one-way functional ANOVA (oneway_anova_vstat), each with a hand-authored inline SVG and an executed offline fence emitting FDARS_FENCE_OK (DOCS-04)."
    - "PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0 with the page executing its fences offline (DOCS-04, DOCS-07)."
  artifacts:
    - docs/inference/functional-inference.md
    - docs/assets/diagrams/inference-permutation-test.svg
    - docs/assets/diagrams/inference-scb.svg
    - docs/assets/diagrams/inference-anova.svg
  key_links:
    - "mkdocs.yml nav Inference section -> docs/inference/functional-inference.md"
    - "page SVG references (../assets/diagrams/inference-*.svg) -> the three new SVG files"
    - "executed fences -> fdars.inference.* shipped bindings"
---

<objective>
Create the new top-level "Inference" nav section and its single "Functional Inference" page documenting the v5.0 functional-inference surface: two-sample permutation tests, simultaneous confidence bands (SCB), and one-way functional ANOVA. Each method family carries a method-accurate hand-authored inline SVG diagram and a tiny executed offline worked example emitting FDARS_FENCE_OK. Implements DOCS-04 and the Inference part of DOCS-07 (nav wiring).

This plan is TRACER-FIRST: Task 1 builds the two-sample-tests section end-to-end (page skeleton + its SVG + executed fence + nav wiring + a per-page strict build) to prove the whole docs toolchain runs against the real shipped fdars.inference bindings. Tasks 2-3 expand out to the SCB and ANOVA sections on the proven page.

Purpose: Document the largest/highest-risk v5.0 binding group (functional inference) to the project's method-accurate standard so the milestone can close.
Output: One new page, three new SVGs, new nav section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-docs-diagrams-worked-examples/35-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/analyze/outlier-detection.md
@src/inference_mod.rs
@scripts/docs_fig.py
@docs/data/README.md
</context>

<artifacts_this_plan_produces>
New files:
- docs/inference/functional-inference.md — the combined Functional Inference page
- docs/assets/diagrams/inference-permutation-test.svg — permutation null histogram + observed statistic + shaded p-value tail
- docs/assets/diagrams/inference-scb.svg — mean curve with a simultaneous band, contrasted with narrower pointwise CIs
- docs/assets/diagrams/inference-anova.svg — between-group vs within-group variation decomposition

Edited files:
- mkdocs.yml — new top-level "Inference" nav section pointing at the page
</artifacts_this_plan_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Two-sample-tests section end-to-end (page + SVG + fence + nav + per-page strict build)</name>
  <files>docs/inference/functional-inference.md, docs/assets/diagrams/inference-permutation-test.svg, mkdocs.yml</files>
  <read_first>
    - This plan (objective + must_haves).
    - docs/assets/diagrams/STYLE_SPEC.md — canonical style block, viewBox 720x300, .ttl/.sub/.lab/.sm/.mono classes, role="img"+aria-label, muted palette, SVGO idempotence rules.
    - docs/analyze/outlier-detection.md — page structure to mirror (H1, KaTeX theory, the ../assets/diagrams/NAME.svg reference with the .fdars-diagram attribute, executed fences with exec="1" html="1" source="above", parameter tables, References).
    - docs/analyze/functional-statistics.md line 115 — the exact FDARS_FENCE_OK sentinel token and how it is appended to a fence's printed output.
    - src/inference_mod.rs lines 84-217 — t_perm_test signature (data_a, data_b, argvals, n_perm=999, seed=None) returning dict statistic/p_value/n_perm; f_perm_test same shape; two_sample_mean_test (data_a, data_b, argvals, ncomp=5) same dict.
    - python/fdars/datasets.py lines 152-193 — load_growth returns Dataset with .data (Fdata, 93x31), .meta sex column ("male" for boys rows 0..38, "female" for girls rows 39..92), argvals age shape (31,).
    - scripts/docs_fig.py — import fig and render from docs_fig; FDARS_COLORS order.
  </read_first>
  <action>
    Create docs/inference/functional-inference.md with an H1 "Functional Inference" and a short intro framing three families: two-sample tests, simultaneous confidence bands, and one-way functional ANOVA. Add an overview table listing each method family, its fdars.inference function(s), what it tests, and the return-dict shape (all permutation/ANOVA functions return statistic/p_value/n_perm; note SCB returns a band dict, covered in Task 2).

    Write the FIRST section "## Two-sample tests" with method-accurate KaTeX theory: a permutation test permutes group labels to build the null distribution of the test statistic, and the observed statistic's tail mass under that null is the p-value. Cover t_perm_test (integrated t-type statistic), f_perm_test (F-type), and two_sample_mean_test (FPCA-projected asymptotic mean test, parameter ncomp). Add a parameter table mirroring the outlier-detection.md convention and a Returns description citing the exact dict keys from src/inference_mod.rs. Do not paraphrase the sentinel string into prose.

    Add ONE executed offline fence (exec="1" html="1" source="above") that imports load_growth from fdars.datasets, extracts the (93,31) matrix and age grid, splits boys vs girls via the sex metadata column, subsets to keep compute tiny (about first 20 boys and first 20 girls), and calls t_perm_test and f_perm_test at n_perm=19, seed=0. Plot the permutation null histogram with the observed statistic marked via fig/render, and print each test's statistic and p_value. Append the FDARS_FENCE_OK sentinel to the fence's printed output exactly as done in docs/analyze/functional-statistics.md.

    Reference the diagram directly under the "## Two-sample tests" heading using the ../assets/diagrams/inference-permutation-test.svg reference with the .fdars-diagram attribute (copy the exact attribute syntax from outlier-detection.md line 26).

    Create docs/assets/diagrams/inference-permutation-test.svg as hand-authored inline SVG conforming to STYLE_SPEC.md: viewBox 0 0 720 300, fill="none", role="img", an aria-label matching the title text, and the canonical style block copied verbatim. Method-accurate content: a histogram of the permutation null distribution, a vertical marker for the observed statistic in the right tail, and the tail beyond the observed statistic shaded to depict the p-value. Title via .ttl, subtitle via .sub, using the muted structural palette and FDARS_COLORS.

    Wire nav: in mkdocs.yml add a NEW top-level "Inference" nav section after the Monitoring section and before Analyze, at the same 2-space indentation as the other top-level sections, with a single child entry Functional Inference pointing at inference/functional-inference.md. Match the surrounding YAML list style exactly.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict >/tmp/mk1.log 2>&1; echo "exit=$?"; grep -q FDARS_FENCE_OK site/inference/functional-inference/index.html && echo FENCE_OK; grep -q inference-permutation-test.svg site/inference/functional-inference/index.html && echo SVG_REF_OK; grep -q functional-inference mkdocs.yml && echo NAV_OK</automated>
  </verify>
  <done>The strict build exits 0; the page HTML contains FDARS_FENCE_OK and the permutation-test SVG reference; mkdocs.yml has the new Inference section. The two-sample section works end-to-end against the shipped bindings.</done>
</task>

<task type="auto">
  <name>Task 2: Expand — SCB (simultaneous confidence bands) section + SVG + fence</name>
  <files>docs/inference/functional-inference.md, docs/assets/diagrams/inference-scb.svg</files>
  <read_first>
    - Task 1 output (the page skeleton and the working fence pattern).
    - src/inference_mod.rs lines 275-370 — mean_scb signature (data, argvals, bandwidth, nb=200, confidence=0.95, multiplier="gaussian") returning dict lower/upper/center/half_width (numpy arrays); scb_two_sample_test (data_a, data_b, argvals, bandwidth, nb=200, confidence=0.95, multiplier="gaussian") returning statistic/p_value/n_perm.
    - python/fdars/datasets.py lines 196-261 — load_canadian_weather returns Dataset .data (35x365), region metadata column, day grid (365,).
    - docs/assets/diagrams/STYLE_SPEC.md (same conformance rules as Task 1).
  </read_first>
  <action>
    Add a "## Simultaneous confidence bands" section to docs/inference/functional-inference.md with method-accurate KaTeX theory: an SCB controls the family-wise coverage across the ENTIRE domain simultaneously via a multiplier bootstrap (nb resamples), so it is wider than a pointwise CI which controls coverage only at each t independently; the band half_width is the multiplier-bootstrap critical value times the pointwise standard error. Cover mean_scb (band around a single group's mean) and scb_two_sample_test (SCB-based two-sample decision). Add a parameter table and a Returns description citing the exact keys (lower/upper/center/half_width for mean_scb; statistic/p_value/n_perm for scb_two_sample_test).

    Add ONE executed offline fence that loads Canadian Weather temperature, subsets to keep compute tiny (subsample stations and/or downsample the 365-day grid), picks a bandwidth appropriate to the grid, calls mean_scb(..., nb=50, confidence=0.95) and plots the mean curve with the simultaneous band, contrasted against a narrower pointwise band. Print the mean half_width and append the FDARS_FENCE_OK sentinel.

    Reference ../assets/diagrams/inference-scb.svg with the .fdars-diagram attribute under the section heading.

    Create docs/assets/diagrams/inference-scb.svg conforming to STYLE_SPEC.md (viewBox 720x300, style block, role/aria-label): a mean curve with a wide simultaneous band drawn around it, overlaid with a visibly narrower pointwise CI band, labelled to show the SCB is the wider envelope. Muted palette + FDARS_COLORS.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict >/tmp/mk2.log 2>&1; echo "exit=$?"; grep -c FDARS_FENCE_OK site/inference/functional-inference/index.html; grep -q inference-scb.svg site/inference/functional-inference/index.html && echo SCB_SVG_OK</automated>
  </verify>
  <done>The strict build exits 0; the page HTML now contains at least two FDARS_FENCE_OK occurrences and the inference-scb.svg reference; SCB section documents mean_scb + scb_two_sample_test with correct dict keys.</done>
</task>

<task type="auto">
  <name>Task 3: Expand — one-way functional ANOVA section + SVG + fence</name>
  <files>docs/inference/functional-inference.md, docs/assets/diagrams/inference-anova.svg</files>
  <read_first>
    - Task 1 + Task 2 output (page structure, fence pattern).
    - src/inference_mod.rs lines 523-547 — oneway_anova_vstat signature (data, groups, argvals) where groups is an integer label array (non-negative), returning dict statistic/p_value/n_perm.
    - python/fdars/datasets.py lines 196-261 — Canadian Weather region column supplies natural group labels; map region strings to non-negative integer labels.
    - docs/assets/diagrams/STYLE_SPEC.md.
  </read_first>
  <action>
    Add a "## One-way functional ANOVA" section with method-accurate KaTeX theory: functional ANOVA decomposes total variation into between-group and within-group components; the V-statistic aggregates the between-group mean-function separation over the domain, and its permutation null gives the p-value. Cover oneway_anova_vstat, noting groups must be a non-negative integer label array. Add a parameter table and a Returns description (statistic/p_value/n_perm).

    Add ONE executed offline fence that loads Canadian Weather, builds integer group labels from the region metadata column (e.g. map each distinct region to a 0-based integer), subsets to keep compute tiny (subsample stations per region and/or downsample the day grid), calls oneway_anova_vstat(data, groups, day), plots each region's mean curve to show the between-group separation, prints the statistic and p_value, and appends the FDARS_FENCE_OK sentinel.

    Reference ../assets/diagrams/inference-anova.svg with the .fdars-diagram attribute under the section heading.

    Create docs/assets/diagrams/inference-anova.svg conforming to STYLE_SPEC.md: a two-part decomposition depicting between-group variation (separated group mean curves) versus within-group variation (spread of curves around each group mean). Method-accurate labels for the between/within split. Muted palette + FDARS_COLORS.

    Add a short "## Functional linear model inference" note near the end (before References) documenting the two shipped FLM-inference functions so they are not left undocumented: flm_f_test (data, response, n_comp=5) and flm_gof_test (data, response, n_comp=5), both returning statistic/p_value/n_perm (src/inference_mod.rs lines 411-482). Per the locked page-structure decision these get a prose subsection with signatures + Returns (and MAY carry a tiny Tecator-based executed fence at n_perm small if cheap), NOT a dedicated method-accuracy SVG (the three locked SVGs are permutation-test, SCB, ANOVA). If an FLM fence is added, keep it tiny and append the FDARS_FENCE_OK sentinel.

    Add a "## References" section to the page (permutation-test, SCB multiplier-bootstrap, and functional-ANOVA literature) mirroring the References style in outlier-detection.md.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict >/tmp/mk3.log 2>&1; echo "exit=$?"; test "$(grep -c FDARS_FENCE_OK site/inference/functional-inference/index.html)" -ge 3 && echo THREE_FENCES_OK; grep -q inference-anova.svg site/inference/functional-inference/index.html && echo ANOVA_SVG_OK</automated>
  </verify>
  <done>The strict build exits 0; the page HTML contains at least three FDARS_FENCE_OK occurrences (one per section) and all three inference SVG references; the ANOVA section documents oneway_anova_vstat with the integer-label requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build -> fdars bindings | Executed fences run real shipped compute at build time; no untrusted external input |
| authored SVG -> published site | Hand-authored markup rendered to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Denial of Service | executed fences (build-time compute) | medium | mitigate | Keep every fence tiny (n_perm=19, nb=50, subset/downsampled data) so the ~18-min strict build does not blow up |
| T-35-02 | Tampering | new inline SVGs | low | mitigate | Conform to STYLE_SPEC.md; SVGO idempotence gate enforced in the final plan (35-04) |
| T-35-03 | Information disclosure | offline fences | low | accept | No network, no API keys; all data from local docs/data/ datasets |
</threat_model>

<verification>
- PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0.
- site/inference/functional-inference/index.html contains three FDARS_FENCE_OK occurrences and all three inference-*.svg references.
- mkdocs.yml has a top-level Inference nav section.
- Each new SVG has viewBox, role="img", aria-label, and the canonical style block (final SVGO idempotence check happens in 35-04).
</verification>

<success_criteria>
- Functional Inference page documents two-sample tests, SCB, and functional ANOVA, each with a method-accurate SVG and an executed offline FDARS_FENCE_OK fence.
- Nav has a new top-level Inference section.
- Per-page strict build is green offline.
</success_criteria>

<output>
Create .planning/phases/35-docs-diagrams-worked-examples/35-01-SUMMARY.md when done.
</output>

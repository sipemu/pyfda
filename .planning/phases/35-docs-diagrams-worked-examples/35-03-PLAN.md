---
phase: 35-docs-diagrams-worked-examples
plan: 03
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - docs/represent/basis-representation.md
  - docs/learn/smoothing.md
  - docs/advisor/aspects.md
autonomous: true
requirements: [DOCS-06]
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "constant_basis is documented in docs/represent/basis-representation.md with an executed offline FDARS_FENCE_OK example (DOCS-06)."
    - "AIC selection (smooth_basis_aic, optim_bandwidth(criterion='aic'), basis_nbasis_cv(criterion='aic')) is documented in docs/learn/smoothing.md with an executed offline FDARS_FENCE_OK example (DOCS-06)."
    - "docs/advisor/aspects.md lists the new inference aspect, bringing the coverage table to 14 aspects (DOCS-06)."
    - "PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0 (DOCS-06)."
  artifacts:
    - docs/represent/basis-representation.md
    - docs/learn/smoothing.md
    - docs/advisor/aspects.md
  key_links:
    - "basis-representation.md new section -> fdars.basis.constant_basis"
    - "smoothing.md new section -> fdars.basis.smooth_basis_aic, fdars.smoothing.optim_bandwidth, fdars.basis.basis_nbasis_cv (criterion='aic')"
    - "aspects.md coverage table + new aspect entry -> the advisor inference aspect"
---

<objective>
Fold the v5.0 basis/smoothing quick wins into the existing docs pages and update the advisor coverage page for the new inference aspect. No new standalone basis/smoothing page. Implements DOCS-06:
- constant_basis documented in docs/represent/basis-representation.md.
- AIC selection (smooth_basis_aic, optim_bandwidth(criterion="aic"), basis_nbasis_cv(criterion="aic")) documented in docs/learn/smoothing.md.
- docs/advisor/aspects.md updated to list the new inference aspect (14 aspects now).

Each new worked example is a tiny executed offline fence emitting FDARS_FENCE_OK. No new SVGs in this plan.

Purpose: Document the smallest binding group (Phase 33) + the advisor extension (Phase 34) to the method-accurate standard.
Output: three edited existing pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-docs-diagrams-worked-examples/35-CONTEXT.md
@docs/represent/basis-representation.md
@docs/learn/smoothing.md
@docs/advisor/aspects.md
@src/basis_mod.rs
@src/smoothing_mod.rs
@scripts/docs_fig.py
</context>

<artifacts_this_plan_produces>
Edited files only (no new pages, no new SVGs):
- docs/represent/basis-representation.md — new constant_basis subsection + executed fence
- docs/learn/smoothing.md — new AIC-selection subsection + executed fence
- docs/advisor/aspects.md — inference row in the coverage table + a new "## inference" aspect section
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Document constant_basis (represent) + AIC selection (smoothing)</name>
  <files>docs/represent/basis-representation.md, docs/learn/smoothing.md</files>
  <read_first>
    - This plan (objective + must_haves).
    - docs/represent/basis-representation.md — existing structure; the "## Evaluating basis matrices directly" and "## Information criteria reference" sections are natural neighbours for a constant_basis subsection.
    - docs/learn/smoothing.md — existing structure; the "## Basis Expansion" / "### Automatic basis selection" (lines ~363-405) and "## Bandwidth Selection via Cross-Validation" (lines ~245-333) sections are the natural homes for AIC content.
    - docs/analyze/functional-statistics.md line 115 — the exact FDARS_FENCE_OK sentinel token and placement.
    - src/basis_mod.rs lines 24-40 (constant_basis signature/return), 452-476 (smooth_basis_aic signature: data, argvals, n_basis, basis_type="bspline", lfd_order=2, log_lambda_min=-8.0, log_lambda_max=4.0, n_grid=25), 521-557 (basis_nbasis_cv: ..., criterion="gcv", ... accepts criterion="aic").
    - src/smoothing_mod.rs lines 181-207 (optim_bandwidth: accepts a criterion argument; confirm it accepts "aic").
    - scripts/docs_fig.py — fig, render, FDARS_COLORS.
  </read_first>
  <action>
    In docs/represent/basis-representation.md, add a subsection documenting constant_basis: what a constant (single-function) basis is, its signature and return from src/basis_mod.rs, and when it is useful (e.g. modelling an intercept-only / level component). Add ONE tiny executed offline fence (exec="1" html="1" source="above" or a plain exec="1" fence) that calls constant_basis on a small argvals grid, shows the resulting basis matrix shape, and appends the FDARS_FENCE_OK sentinel exactly as in docs/analyze/functional-statistics.md. Place the subsection near the existing basis-matrix / information-criteria material so it reads coherently. Do not paraphrase the sentinel into prose.

    In docs/learn/smoothing.md, add a subsection on AIC-based selection covering all three AIC entry points: smooth_basis_aic (AIC-optimal smoothing penalty over the log-lambda grid), optim_bandwidth(criterion="aic") (AIC-optimal kernel bandwidth), and basis_nbasis_cv(criterion="aic") (AIC-optimal basis count). Explain AIC vs GCV briefly (AIC penalises effective degrees of freedom explicitly). Add ONE tiny executed offline fence that runs at least smooth_basis_aic (and, if cheap, contrasts optim_bandwidth GCV vs AIC or basis_nbasis_cv GCV vs AIC) on small/synthetic data, prints the selected value(s), and appends the FDARS_FENCE_OK sentinel. Keep parameters small to protect the build.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict >/tmp/mkbs1.log 2>&1; echo "exit=$?"; grep -q FDARS_FENCE_OK site/represent/basis-representation/index.html && echo BASIS_FENCE_OK; grep -q "constant_basis" site/represent/basis-representation/index.html && echo CONSTANT_OK; grep -q FDARS_FENCE_OK site/learn/smoothing/index.html && echo SMOOTH_FENCE_OK; grep -q "smooth_basis_aic" site/learn/smoothing/index.html && echo AIC_OK</automated>
  </verify>
  <done>Strict build exits 0; basis-representation page documents constant_basis with an executed FDARS_FENCE_OK fence; smoothing page documents all three AIC entry points with an executed FDARS_FENCE_OK fence.</done>
</task>

<task type="auto">
  <name>Task 2: Update advisor aspects.md for the new inference aspect (14 aspects)</name>
  <files>docs/advisor/aspects.md</files>
  <read_first>
    - docs/advisor/aspects.md — the "## Coverage Table" (lines ~16-31, currently 13 aspect rows) and the per-aspect section pattern (e.g. "## fpca", "## depth", "## spm" with an offline diagnostics fence). This is diagnostics-only.
    - STATE.md Accumulated Context: the inference aspect is diagnostics-only (NOT in _RUNNABLE_METHODS); caller supplies a TestResult dict; n_perm==0 is a legitimate asymptotic-test value; ToleranceBand shape detected by half_width+center presence without p_value.
    - python/fdars/advisor/aspects/inference.py (the shipped inference aspect builder) — for the exact diagnostics keys the aspect reports.
    - docs/analyze/functional-statistics.md line 115 — the FDARS_FENCE_OK sentinel token.
  </read_first>
  <action>
    Add an inference row to the Coverage Table in docs/advisor/aspects.md matching the existing column format (Aspect (method=) | fdars source(s) | Key diagnostics (count) | Offline fence), bringing the table to 14 rows. The fdars source column should note the aspect is diagnostics-only over a caller-supplied TestResult / ToleranceBand dict from fdars.inference.

    Add a new "## inference" per-aspect section following the established pattern (like "## fpca" / "## depth"): a short description of what the inference aspect diagnoses, the exact diagnostics keys it reports (read from python/fdars/advisor/aspects/inference.py), and — matching the pages that carry an inline fence — a tiny executed offline diagnostics fence that constructs a small TestResult-shaped dict, calls advise(..., task=...) with method="inference" in offline/illustrative form (no API key, mirroring the v2.1/v3.0 offline precedent used elsewhere in this file), prints a diagnostic scalar, and appends the FDARS_FENCE_OK sentinel. If the other aspect sections in this file only show the diagnostics-builder call (not a live advise call) offline, follow that exact precedent rather than inventing a new pattern.

    Ensure the page's own count references (any "13"/"thirteen"/"14"/"fourteen" wording) are consistent with 14 aspects after the edit.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict >/tmp/mkasp.log 2>&1; echo "exit=$?"; grep -q "inference" site/advisor/aspects/index.html && echo INFERENCE_ASPECT_OK; test "$(grep -c '| \`' docs/advisor/aspects.md)" -ge 14 && echo COVERAGE_14_OK</automated>
  </verify>
  <done>Strict build exits 0; aspects.md coverage table has an inference row (14 aspects total) and a new "## inference" section documenting the diagnostics-only aspect; any aspect-count wording is consistent with 14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build -> fdars bindings | Executed fences run real basis/smoothing/advisor compute at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-06 | Denial of Service | executed basis/smoothing/advisor fences | low | mitigate | Small/synthetic data, small grids; no permutation compute here |
| T-35-07 | Information disclosure | advisor inference fence | low | accept | Illustrative/offline only; no API key, matching existing advisor page precedent |
</threat_model>

<verification>
- PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0.
- basis-representation page shows constant_basis + FDARS_FENCE_OK; smoothing page shows the three AIC entry points + FDARS_FENCE_OK.
- aspects.md coverage table has 14 rows including inference; a "## inference" section exists.
</verification>

<success_criteria>
- constant_basis + AIC selection documented in the existing pages with executed offline fences.
- Advisor coverage page reflects the 14th (inference) aspect.
- Strict build green offline.
</success_criteria>

<output>
Create .planning/phases/35-docs-diagrams-worked-examples/35-03-SUMMARY.md when done.
</output>

---
phase: 35-docs-diagrams-worked-examples
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - docs/analyze/functional-boxplot.md
  - docs/assets/diagrams/functional-boxplot.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-05, DOCS-07]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The built site has an analyze/ Functional Boxplot page wired into the Analyze nav section (DOCS-05)."
    - "The page carries a method-accurate hand-authored SVG showing median / 50% central region / whiskers / flagged outliers and an executed offline fence emitting FDARS_FENCE_OK on Canadian Weather (DOCS-05)."
    - "PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0 (DOCS-05, DOCS-07)."
  artifacts:
    - docs/analyze/functional-boxplot.md
    - docs/assets/diagrams/functional-boxplot.svg
  key_links:
    - "mkdocs.yml Analyze nav -> docs/analyze/functional-boxplot.md"
    - "page SVG reference (../assets/diagrams/functional-boxplot.svg) -> the new SVG"
    - "executed fence -> fdars.depth.functional_boxplot shipped binding"
---

<objective>
Create a new "Functional Boxplot" page under docs/analyze/ documenting the canonical Lopez-Pintado-Romo depth-fence functional boxplot shipped in v5.0 (fdars.depth.functional_boxplot). The page carries a method-accurate hand-authored inline SVG (deepest median curve, shaded 50% central region, whiskers/fence, flagged outlier curves) and a tiny executed offline worked example on Canadian Weather emitting FDARS_FENCE_OK. Implements DOCS-05 and the boxplot part of DOCS-07 (nav wiring).

Purpose: Document the depth/boxplot binding group (Phase 32) to the method-accurate standard.
Output: One new page, one new SVG, one nav entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-docs-diagrams-worked-examples/35-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/analyze/outlier-detection.md
@src/depth_mod.rs
@scripts/docs_fig.py
@docs/data/README.md
</context>

<artifacts_this_plan_produces>
New files:
- docs/analyze/functional-boxplot.md — the Functional Boxplot page
- docs/assets/diagrams/functional-boxplot.svg — median / 50% central region / whiskers / flagged outliers

Edited files:
- mkdocs.yml — Functional Boxplot entry in the Analyze nav section
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Functional Boxplot page + executed fence + nav wiring</name>
  <files>docs/analyze/functional-boxplot.md, mkdocs.yml</files>
  <read_first>
    - This plan (objective + must_haves).
    - docs/analyze/outlier-detection.md — page structure to mirror (H1, KaTeX theory, SVG reference with .fdars-diagram, executed fence with exec="1" html="1" source="above", parameter table, Returns table, See also + References). It is the sibling page.
    - docs/analyze/functional-statistics.md line 115 — the exact FDARS_FENCE_OK sentinel token and placement.
    - src/depth_mod.rs lines 517-584 — functional_boxplot signature (data, method="modified_band", factor=1.5, scale=true, nproj=50, seed=None) returning the seven-key dict: median, central_lower, central_upper, whisker_lower, whisker_upper, outliers (list of int), depths (n,). This docstring is the authority for keys and semantics.
    - python/fdars/datasets.py lines 196-261 — load_canadian_weather returns Dataset .data (35x365), region column, day grid (365,).
    - scripts/docs_fig.py — fig, render, FDARS_COLORS.
  </read_first>
  <action>
    Create docs/analyze/functional-boxplot.md with an H1 "Functional Boxplot" and a short intro. Add method-accurate KaTeX theory of the Lopez-Pintado-Romo depth-fence construction: curves are ranked by functional depth; the deepest curve is the median; the 50% central region is the pointwise envelope of the deepest half of curves; the whiskers/fence inflate the central region by factor times the central-region width (factor=1.5 Tukey convention); any curve exceeding the fence at any evaluation point is flagged an outlier. Cover the method parameter (fraiman_muniz / band / modified_band default / random_projection) and factor.

    Add a parameter table and a Returns table citing the EXACT seven keys from src/depth_mod.rs (median, central_lower, central_upper, whisker_lower, whisker_upper, outliers as a Python list of ints, depths), with shapes. Do not paraphrase the sentinel string into prose.

    Add ONE executed offline fence (exec="1" html="1" source="above") that loads Canadian Weather temperature, subsets to keep compute tiny (subsample stations and/or downsample the 365-day grid), calls functional_boxplot(data) with defaults, and plots the canonical picture: all curves faint, the median curve bold, the 50% central region shaded between central_lower and central_upper, the whisker_lower/whisker_upper fence lines, and any flagged outlier curves highlighted. Print the number of flagged outliers and the outlier indices, then append the FDARS_FENCE_OK sentinel exactly as in docs/analyze/functional-statistics.md.

    Reference ../assets/diagrams/functional-boxplot.svg with the .fdars-diagram attribute directly under the H1 or the first section heading (copy the exact attribute syntax from outlier-detection.md line 26). Add a "See also" pointing to Outlier Detection and Depth Functions, and a References section (Lopez-Pintado & Romo 2009 functional boxplot; Sun & Genton enhanced functional boxplot).

    Wire nav: in mkdocs.yml add a "Functional Boxplot: analyze/functional-boxplot.md" entry inside the existing Analyze nav section, placed logically beside Outlier Detection, matching the surrounding indentation and list style.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict >/tmp/mkbp1.log 2>&1; echo "exit=$?"; grep -q FDARS_FENCE_OK site/analyze/functional-boxplot/index.html && echo FENCE_OK; grep -q "functional-boxplot.svg" site/analyze/functional-boxplot/index.html && echo SVG_REF_OK; grep -q "analyze/functional-boxplot.md" mkdocs.yml && echo NAV_OK</automated>
  </verify>
  <done>The strict build exits 0; the page HTML contains FDARS_FENCE_OK and the functional-boxplot.svg reference; mkdocs.yml Analyze section links the new page; Returns table cites all seven dict keys.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-authored functional-boxplot SVG (method-accurate)</name>
  <files>docs/assets/diagrams/functional-boxplot.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — viewBox 0 0 720 300, fill="none", role="img", aria-label matching title, canonical style block copied verbatim, muted structural palette, FDARS_COLORS, stroke weights (median bold 2.0-2.8, faint curves 1.4-1.6), SVGO idempotence.
    - An existing conforming diagram for structural reference, e.g. docs/assets/diagrams/functional-statistics.svg (title/subtitle/panel/coordinate-group layout).
    - Task 1 output (the page that references this SVG and its exact filename).
  </read_first>
  <action>
    Create docs/assets/diagrams/functional-boxplot.svg as a hand-authored inline SVG conforming to STYLE_SPEC.md. Method-accurate canonical Lopez-Pintado-Romo picture: a bundle of faint sample curves; the deepest MEDIAN curve drawn bold; the shaded 50% CENTRAL REGION (between central_lower and central_upper); the WHISKER/FENCE lines above and below the central region; and one or two FLAGGED OUTLIER curves that stray outside the fence, drawn in the alert colour (#dc3545). Title via .ttl "Functional Boxplot", a .sub subtitle naming the four elements (median / 50% central region / whiskers / outliers). Provide a small legend or inline .sm labels identifying each element. Root svg carries viewBox="0 0 720 300", fill="none", role="img", and an aria-label matching the title.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q 'role="img"' docs/assets/diagrams/functional-boxplot.svg && grep -q 'viewBox="0 0 720 300"' docs/assets/diagrams/functional-boxplot.svg && grep -q '.ttl{' docs/assets/diagrams/functional-boxplot.svg && first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/diagrams/functional-boxplot.svg --output -) && second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -) && diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null && echo SVGO_IDEMPOTENT_OK</automated>
  </verify>
  <done>The SVG has role="img", the 720x300 viewBox, and the canonical style block; it is SVGO-idempotent under svgo.config.mjs; it depicts median, 50% central region, whiskers, and flagged outliers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build -> fdars bindings | Executed fence runs real functional_boxplot compute at build time |
| authored SVG -> published site | Hand-authored markup rendered to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Denial of Service | executed boxplot fence | medium | mitigate | Subsample stations / downsample day grid so build stays within the ~18-min budget |
| T-35-05 | Tampering | functional-boxplot.svg | low | mitigate | Conform to STYLE_SPEC.md; SVGO idempotence gated in Task 2 and re-checked in 35-04 |
</threat_model>

<verification>
- PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0.
- site/analyze/functional-boxplot/index.html contains FDARS_FENCE_OK and the SVG reference.
- functional-boxplot.svg is SVGO-idempotent and STYLE_SPEC-conformant.
- mkdocs.yml Analyze section links the page.
</verification>

<success_criteria>
- Functional Boxplot page documents functional_boxplot with the correct seven-key return dict, a method-accurate SVG, and an executed offline FDARS_FENCE_OK fence.
- Page wired into the Analyze nav.
- Per-page strict build green offline.
</success_criteria>

<output>
Create .planning/phases/35-docs-diagrams-worked-examples/35-02-SUMMARY.md when done.
</output>

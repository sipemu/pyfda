---
phase: 35-docs-diagrams-worked-examples
plan: 04
type: execute
wave: 4
depends_on: [35-01, 35-02, 35-03]
files_modified:
  - mkdocs.yml
autonomous: false
requirements: [DOCS-04, DOCS-05, DOCS-06, DOCS-07]
estimate:
  tokens: 35000
  raw_tokens: 23000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All new pages are wired into mkdocs.yml nav and the whole-site mkdocs build --strict passes offline, exit 0 (DOCS-07)."
    - "Every new SVG is SVGO-idempotent under svgo.config.mjs and determinism-clean (DOCS-07)."
    - "Every new executed page emits FDARS_FENCE_OK and the pytest suite stays green (DOCS-04, DOCS-05, DOCS-06, DOCS-07)."
    - "A blocking human diagram method-accuracy review gate is satisfied before the phase is verified (DOCS-07 success criterion 4)."
  artifacts:
    - mkdocs.yml
  key_links:
    - "whole-site strict build -> all four new/edited pages executing offline"
    - "SVGO idempotence gate -> the four new SVGs"
    - "human-verify checkpoint -> rendered PNGs of the four new SVGs"
---

<objective>
Final integration + gate plan for the v5.0 docs overhaul. Confirm all new pages are nav-wired, run the WHOLE-SITE strict build offline (the real ~18-min gate), run the SVGO idempotence + determinism gate over all diagrams (especially the four new ones), confirm every new page emits FDARS_FENCE_OK and the pytest suite is still green, then HALT at a BLOCKING human diagram method-accuracy review. Implements the whole-site parts of DOCS-07 and the human-review gate (DOCS-07 success criterion 4); spans DOCS-04/05/06 as the integration gate over their outputs.

This plan is LAST. The whole-site strict build + SVGO idempotence is the integration gate; the human review is the final blocking step.

Purpose: Prove the full site builds strict-green offline against the real shipped bindings and that every new diagram is method-accurate (human-confirmed).
Output: a verified, nav-complete, strict-green docs site; a satisfied human-review gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-docs-diagrams-worked-examples/35-CONTEXT.md
@docs/assets/diagrams/STYLE_SPEC.md
@.github/workflows/docs.yml
@mkdocs.yml
</context>

<artifacts_this_plan_produces>
- No new pages/SVGs. This plan verifies the integration and runs the gates.
- May make small nav-consistency fixes to mkdocs.yml if the whole-site build surfaces nav gaps.
- Produces rendered PNGs of the four new SVGs (transient, for the human review) via rsvg-convert.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Nav completeness + whole-site strict build (integration gate) + pytest green</name>
  <files>mkdocs.yml</files>
  <read_first>
    - This plan (must_haves).
    - mkdocs.yml — confirm the nav contains: top-level Inference section -> inference/functional-inference.md (from 35-01); Analyze -> functional-boxplot.md (from 35-02). basis-representation.md, learn/smoothing.md, advisor/aspects.md were pre-existing nav entries (edited in place, no new nav needed).
    - .github/workflows/docs.yml lines 40-90 — the CI gate recipe: SVGO idempotence loop over docs/assets/diagrams/*.svg, the markdown-exec build, and the figure-error gate. This task mirrors the build gate locally.
    - MEMORY docs-diagram-verify-workflow — build recipe: .venv has mkdocs+fdars; PYTHONPATH=scripts exposes docs_fig; full-site build is slow (~18 min) because it live-executes fdars for every figure.
  </read_first>
  <action>
    Verify mkdocs.yml nav is complete and consistent: the new Inference section and the Analyze functional-boxplot entry are present and correctly indented; no dangling or duplicated entries. Make only minimal nav-consistency edits if the strict build reports missing/duplicate nav pages.

    Run the FULL whole-site strict build offline WITHOUT DOCS_FAST (the real gate, ~18 min): PYTHONPATH=scripts .venv/bin/mkdocs build --strict. It must exit 0 with no strict warnings. Confirm every new executed page emits FDARS_FENCE_OK in its built HTML: site/inference/functional-inference/index.html (>=3 occurrences), site/analyze/functional-boxplot/index.html, site/represent/basis-representation/index.html, site/learn/smoothing/index.html, and site/advisor/aspects/index.html renders the inference aspect.

    Run the pytest suite and confirm it remains green (no source changes were expected this phase): .venv/bin/pytest -q. Any red test must be investigated before proceeding.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts .venv/bin/mkdocs build --strict >/tmp/mkfull.log 2>&1; echo "build_exit=$?"; test "$(grep -c FDARS_FENCE_OK site/inference/functional-inference/index.html)" -ge 3 && echo INF_FENCES_OK; grep -q FDARS_FENCE_OK site/analyze/functional-boxplot/index.html && echo BP_FENCE_OK; grep -q FDARS_FENCE_OK site/represent/basis-representation/index.html && echo BASIS_FENCE_OK; grep -q FDARS_FENCE_OK site/learn/smoothing/index.html && echo SMOOTH_FENCE_OK; .venv/bin/pytest -q >/tmp/pytest.log 2>&1; echo "pytest_exit=$?"</automated>
  </verify>
  <done>Whole-site PYTHONPATH=scripts .venv/bin/mkdocs build --strict exits 0 offline; every new executed page emits FDARS_FENCE_OK (inference page >=3); pytest exits 0 (suite green).</done>
</task>

<task type="auto">
  <name>Task 2: SVGO idempotence + determinism gate over all new SVGs</name>
  <files>docs/assets/diagrams/inference-permutation-test.svg, docs/assets/diagrams/inference-scb.svg, docs/assets/diagrams/inference-anova.svg, docs/assets/diagrams/functional-boxplot.svg</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md — the SVGO invocation (npx svgo@3.3.4 --config svgo.config.mjs --quiet --input <f> --output -) and the idempotence-check definition (svgo(svgo(svg)) == svgo(svg)); the gate never rewrites the committed file (stdout mode only).
    - .github/workflows/docs.yml lines 52-62 — the exact CI idempotence loop this task must reproduce locally.
  </read_first>
  <action>
    Run the SVGO idempotence + determinism gate over the four new SVGs exactly as CI does: for each file, run svgo@3.3.4 with --config svgo.config.mjs in stdout mode to produce pass1, feed pass1 back through the same command to produce pass2, and require diff(pass1, pass2) to be empty. Do NOT write svgo output back to any committed SVG (stdout only). If any new SVG is not idempotent, edit the hand-authored SVG to conform to STYLE_SPEC.md (canonical style block, standard viewBox, role/aria-label) until it passes. Also confirm each new SVG carries role="img", an aria-label matching its title, and viewBox="0 0 720 300".
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && FAILED=0; for svg in docs/assets/diagrams/inference-permutation-test.svg docs/assets/diagrams/inference-scb.svg docs/assets/diagrams/inference-anova.svg docs/assets/diagrams/functional-boxplot.svg; do grep -q 'role="img"' "$svg" || { echo "no role: $svg"; FAILED=1; }; first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$svg" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null || { echo "not idempotent: $svg"; FAILED=1; }; done; [ $FAILED -eq 0 ] && echo SVGO_ALL_OK || echo SVGO_FAILED</automated>
  </verify>
  <done>All four new SVGs are SVGO-idempotent under svgo.config.mjs and carry role="img"; the gate prints SVGO_ALL_OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking human diagram method-accuracy review</name>
  <what-built>
    The complete v5.0 docs overhaul: a new Functional Inference page (two-sample tests, SCB, functional ANOVA) with three method-accurate SVGs; a new Functional Boxplot page with its SVG; constant_basis + AIC-selection additions to the existing basis/smoothing pages; the advisor inference aspect. Whole-site mkdocs build --strict is green offline, all new SVGs are SVGO-idempotent, every new page emits FDARS_FENCE_OK, and pytest is green.
  </what-built>
  <how-to-verify>
    Render each new SVG to PNG for visual inspection (the prior-milestone recipe):
      rsvg-convert -w 1440 -h 600 docs/assets/diagrams/inference-permutation-test.svg -o /tmp/inf-perm.png
      rsvg-convert -w 1440 -h 600 docs/assets/diagrams/inference-scb.svg -o /tmp/inf-scb.png
      rsvg-convert -w 1440 -h 600 docs/assets/diagrams/inference-anova.svg -o /tmp/inf-anova.png
      rsvg-convert -w 1440 -h 600 docs/assets/diagrams/functional-boxplot.svg -o /tmp/fbox.png
    Open each PNG and confirm each diagram is METHOD-ACCURATE:
      - inference-permutation-test.svg: shows a permutation NULL distribution, the observed statistic in the tail, and the p-value tail shaded (not a generic bell curve with no observed marker).
      - inference-scb.svg: the simultaneous band is visibly WIDER than the pointwise CI around the same mean curve.
      - inference-anova.svg: clearly separates BETWEEN-group variation (separated group means) from WITHIN-group variation (spread around each mean).
      - functional-boxplot.svg: shows the deepest median curve, the shaded 50% central region, the whisker/fence lines, and flagged outlier curves straying outside the fence.
    Also spot-check the built pages render correctly (optionally serve under the /pyfda/ base path per the memory recipe) and the fence outputs look sane.
  </how-to-verify>
  <resume-signal>Type "approved" if all four diagrams are method-accurate, or describe the inaccuracies to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole-site build -> fdars bindings | Full offline strict build live-executes every fence |
| authored SVGs -> published site | Human-reviewed for method accuracy before milestone close |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-08 | Tampering | inaccurate diagram shipped | high | mitigate | Blocking human method-accuracy review (Task 3) before the phase is verified |
| T-35-09 | Denial of Service | ~18-min whole-site build | medium | accept | Fences already kept tiny in 35-01/02/03; full build is the accepted one-time gate cost |
| T-35-10 | Tampering | non-idempotent SVG breaking CI | medium | mitigate | SVGO idempotence gate (Task 2) mirrors the CI check exactly |
</threat_model>

<verification>
- PYTHONPATH=scripts .venv/bin/mkdocs build --strict (no DOCS_FAST) exits 0 offline.
- All four new SVGs pass the SVGO idempotence gate and carry role="img".
- Every new page emits FDARS_FENCE_OK; pytest is green.
- The blocking human diagram method-accuracy review is approved.
</verification>

<success_criteria>
- Whole-site strict build green offline against the real shipped bindings.
- All new SVGs SVGO-idempotent and determinism-clean.
- Human diagram method-accuracy review satisfied (blocking gate approved).
</success_criteria>

<output>
Create .planning/phases/35-docs-diagrams-worked-examples/35-04-SUMMARY.md when done.
</output>

---
phase: 17-agent-skill-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/advisor/agent-skill.md
autonomous: false
requirements: [SKILLDOC-01, SKILLDOC-02]

estimate:
  tokens: 40000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The page documents the git-URL install command and the interpret->recommend->re-run->compare walkthrough, method-accurate against .claude/skills/fdars-advisor/ (SKILL.md + walkthrough script) (SKILLDOC-01)"
    - "The page documents the skill's execution-environment / compatibility requirements: Python 3.10+, package-manager access, offline walkthrough needs no key, grounded-advice step needs ANTHROPIC_API_KEY (SKILLDOC-02)"
    - "The docs build succeeds and the page contains NO executed exec fence — the build does not require Python 3.10+, the [mcp]/[advisor] extras, or an API key"
  artifacts:
    - docs/advisor/agent-skill.md
  key_links:
    - "Install command string matches SKILL.md verbatim (git-URL until fdars 0.3.0; future [mcp,advisor] one-liner)"
    - "Walkthrough steps mirror scripts/fdars_advisor_walkthrough.py (Canadian Weather -> registry -> smoothing pspline_fit_gcv n_basis=15 -> build_diagnostics -> optional advise() -> compare_run n_basis=25 -> delta)"
    - "Cross-links resolve to index.md, python-api.md, mcp.md (this is the last content page)"
---

<objective>
Author `docs/advisor/agent-skill.md` — the Agent Skill page of the AI Advisor documentation section — documenting the `fdars-advisor` Anthropic Agent Skill: git-URL install, the full interpret->recommend->re-run->compare walkthrough, and the skill's execution-environment / compatibility requirements. Covers SKILLDOC-01 and SKILLDOC-02.

Purpose: This is the last content page of the AI Advisor section (Phase 18 wires nav). The two earlier pages (`python-api.md`, `mcp.md`) already forward-link `agent-skill.md`; this page completes those links and documents the packaged skill that orchestrates the MCP tools automatically.

Output: `docs/advisor/agent-skill.md`, built cleanly (`PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build`) with all fences illustrative-only (no executed fence).

## Artifacts this phase produces
- `docs/advisor/agent-skill.md` — new Agent Skill documentation page.

Note: `mkdocs.yml` nav wiring is NOT this phase (deferred to Phase 18, NAVDOC-01). No changes to `.claude/skills/fdars-advisor/` or advisor code — this is a documentation-only milestone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source of truth for install / compatibility / section structure:
@.claude/skills/fdars-advisor/SKILL.md
# The walkthrough to mirror:
@.claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py
# Sibling pages to match tone/structure and cross-link:
@docs/advisor/mcp.md
@docs/advisor/python-api.md
@docs/advisor/index.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Page skeleton + Setup/Compatibility section (builds cleanly)</name>
  <files>docs/advisor/agent-skill.md</files>
  <read_first>
    - .claude/skills/fdars-advisor/SKILL.md — front-matter `compatibility:` field and the `## Setup` section hold the authoritative install commands and version requirements. Do NOT paraphrase or invent flags.
    - docs/advisor/mcp.md — copy the top-of-page "Illustrative fences — not run in the docs build" warning-admonition pattern and the overall page tone/heading style.
    - docs/advisor/index.md — confirm the overview page path/title for the cross-link.
  </read_first>
  <action>
    Create `docs/advisor/agent-skill.md` as the thin end-to-end slice: an H1 "Agent Skill" title, a top-of-page MkDocs Material warning admonition stating the page's Python fences are illustrative-only and NOT run in the docs build (mirror the admonition wording used in docs/advisor/mcp.md), a one-paragraph intro describing the `fdars-advisor` skill as the packaged surface that orchestrates the Phase 12 MCP tools automatically (cross-link index.md, python-api.md, mcp.md), a `## Setup` section, and a `## Compatibility` execution-environment section.

    In the `## Setup` section, document BOTH install commands verbatim from SKILL.md: the current git-URL workaround `pip install "fdars @ git+https://github.com/sipemu/pyfda" mcp>=2.0.0 anthropic>=0.72.0 pydantic>=2.0` (required until fdars 0.3.0 ships the extras to PyPI), and the future one-liner `pip install "fdars[mcp,advisor]"` (available once fdars 0.3.0+ is published). Place both inside plain bash fences with no exec attribute.

    In the `## Compatibility` section (SKILLDOC-02), reflect SKILL.md's `compatibility:` field: Python 3.10+ is required (the `[mcp]` extra / mcp>=2.0.0 does not support 3.9), package-manager (pip) access is required to install fdars + extras, the offline walkthrough needs no API key, and the grounded-advice step needs `ANTHROPIC_API_KEY`. Note the skill is designed for Claude Code and Managed Agents environments with `allow_package_managers` enabled.

    Do NOT add any executed fence anywhere on the page (no fence carries an exec attribute). Do NOT modify SKILL.md, the walkthrough script, or any advisor code. Leave the walkthrough body for Task 2.
  </action>
  <acceptance_criteria>
    - File `docs/advisor/agent-skill.md` exists with an H1 `# Agent Skill` heading.
    - The page contains the git-URL install substring `fdars @ git+https://github.com/sipemu/pyfda` and the future one-liner substring `fdars[mcp,advisor]`.
    - The page documents the Python 3.10+ requirement (grep for `3.10` succeeds).
    - The page documents `ANTHROPIC_API_KEY` for the grounded-advice step.
    - No executed fence exists: the count of exec-attribute fences in the file is 0.
    - Docs build succeeds: `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q 'fdars @ git+https://github.com/sipemu/pyfda' docs/advisor/agent-skill.md && grep -q 'fdars\[mcp,advisor\]' docs/advisor/agent-skill.md && grep -q '3.10' docs/advisor/agent-skill.md && grep -q 'ANTHROPIC_API_KEY' docs/advisor/agent-skill.md && test "$(grep -c 'exec=' docs/advisor/agent-skill.md)" = "0" && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build</automated>
  </verify>
  <done>The page exists with title, illustrative-fence warning, both install commands, and the compatibility section; no executed fence; docs build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Interpret->recommend->re-run->compare walkthrough section</name>
  <files>docs/advisor/agent-skill.md</files>
  <read_first>
    - .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py — the authoritative step sequence to mirror: registry.clear() -> load_canadian_weather -> registry.store_dataset -> run_method(smoothing, n_basis=15) -> registry.store_result -> build_diagnostics -> optional advise(task="parameter") gated on ANTHROPIC_API_KEY -> compare_run(n_basis=25) -> delta block. Use the exact function/parameter names.
    - .claude/skills/fdars-advisor/SKILL.md — the `## Offline Walkthrough`, `## Grounded Advice`, `## Tools Referenced`, and `## Grounding Invariant` sections; reuse the expected 4-key delta block (`gcv_aic_approx`, `gcv_bic_approx`, `optimal_gcv`, `optimal_edf`) and the `python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py` run command verbatim.
    - docs/advisor/mcp.md — reuse the "Re-run / compare loop" step structure and the `fdars.mcp._registry` / `_runner` / `_compare` import paths for method accuracy.
  </read_first>
  <action>
    Append the walkthrough content to `docs/advisor/agent-skill.md`, mirroring the SKILL.md section structure: `## Offline Walkthrough` (no API key needed), `## Grounded Advice` (requires ANTHROPIC_API_KEY), `## Tools Referenced`, and `## Grounding Invariant`.

    In `## Offline Walkthrough`, describe the interpret->recommend->re-run->compare loop step-by-step exactly as `fdars_advisor_walkthrough.py` runs it: Step 1 load Canadian Weather (35 stations x 365 daily observations) and register via `registry.store_dataset`; Step 2 run `smoothing` (`pspline_fit_gcv`, n_basis=15) as the "before" via `run_method` and store with `registry.store_result`; Step 3 build offline diagnostics with `build_diagnostics`; Step 4 note the optional API-key-gated `advise()` step (deferred to the Grounded Advice section); Step 5 re-run with n_basis=25 via `compare_run` and print the observable delta. Show the one-line run command verbatim (`python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py`) in a plain bash fence, and show the expected 4-key delta block (`gcv_aic_approx`, `gcv_bic_approx`, `optimal_gcv`, `optimal_edf`) as a plain text/output fence. State that all delta numbers are fdars-computed (pspline_fit_gcv, n_basis 15 vs 25).

    In `## Grounded Advice`, show the API-key-gated invocation (`ANTHROPIC_API_KEY=sk-... python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py`) in a plain bash fence and describe its expected output (interpretation + recommendations with cited diagnostics evidence + the same before/after delta block). Keep it illustrative-only.

    In `## Tools Referenced`, name the Phase 12 MCP tools the skill orchestrates (`fdars_run_method`, `fdars_compare_run`) and note the Phase 11 advisor (`build_diagnostics` / `advise()`) is the grounding source; cross-link the `mcp.md` and `python-api.md` pages. In `## Grounding Invariant`, state that every recommendation cites a `build_diagnostics` value, `advise()` never fabricates numbers, and this is enforced by the Pydantic `Recommendation.evidence` schema (non-empty list) plus the system prompt; cross-link `index.md`. End with a "Next steps" list cross-linking `index.md`, `python-api.md`, `mcp.md`.

    Every Python/bash/text fence stays plain — no fence carries an exec attribute. Do NOT modify the skill or advisor code.
  </action>
  <acceptance_criteria>
    - The page contains the run command `python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py`.
    - The page names the mirrored walkthrough functions: `run_method`, `build_diagnostics`, and `compare_run` (grep each succeeds).
    - The page references the smoothing parameters `n_basis=15` and `n_basis=25`.
    - The page shows all four delta keys: `gcv_aic_approx`, `gcv_bic_approx`, `optimal_gcv`, `optimal_edf`.
    - The page cross-links `python-api.md`, `mcp.md`, and `index.md` (grep each filename succeeds).
    - No executed fence exists: the count of exec-attribute fences in the file is 0.
    - Docs build succeeds: `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q 'python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py' docs/advisor/agent-skill.md && grep -q 'run_method' docs/advisor/agent-skill.md && grep -q 'build_diagnostics' docs/advisor/agent-skill.md && grep -q 'compare_run' docs/advisor/agent-skill.md && grep -q 'n_basis=15' docs/advisor/agent-skill.md && grep -q 'n_basis=25' docs/advisor/agent-skill.md && grep -q 'gcv_aic_approx' docs/advisor/agent-skill.md && grep -q 'gcv_bic_approx' docs/advisor/agent-skill.md && grep -q 'optimal_gcv' docs/advisor/agent-skill.md && grep -q 'optimal_edf' docs/advisor/agent-skill.md && grep -q 'python-api.md' docs/advisor/agent-skill.md && grep -q 'mcp.md' docs/advisor/agent-skill.md && grep -q 'index.md' docs/advisor/agent-skill.md && test "$(grep -c 'exec=' docs/advisor/agent-skill.md)" = "0" && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build</automated>
  </verify>
  <done>The walkthrough (Offline Walkthrough / Grounded Advice / Tools Referenced / Grounding Invariant + Next steps) is present, method-accurate against the walkthrough script, cross-linked, with no executed fence; docs build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the Agent Skill page on the built site</name>
  <what-built>`docs/advisor/agent-skill.md` — the Agent Skill page: git-URL + future install commands, Python 3.10+/API-key compatibility section, and the full interpret->recommend->re-run->compare walkthrough (Offline Walkthrough / Grounded Advice / Tools Referenced / Grounding Invariant), all fences illustrative-only, method-accurate against SKILL.md and the walkthrough script.</what-built>
  <how-to-verify>
    1. Build the site: `cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` — confirm it exits 0.
    2. Serve and open the page: `.venv/bin/mkdocs serve` then browse to `/pyfda/advisor/agent-skill/` (note the `/pyfda/` base path). Confirm the page renders with the illustrative-fence warning at the top.
    3. Confirm the Setup section shows BOTH install commands exactly as in SKILL.md: the git-URL `pip install "fdars @ git+https://github.com/sipemu/pyfda" mcp>=2.0.0 anthropic>=0.72.0 pydantic>=2.0` and the future `pip install "fdars[mcp,advisor]"`.
    4. Confirm the Compatibility section states Python 3.10+, pip/package-manager access, offline walkthrough needs no key, and ANTHROPIC_API_KEY for grounded advice.
    5. Confirm the walkthrough mirrors `scripts/fdars_advisor_walkthrough.py`: Canadian Weather -> registry -> smoothing (n_basis=15) -> build_diagnostics -> optional advise() -> compare_run (n_basis=25) -> the 4-key delta block (gcv_aic_approx, gcv_bic_approx, optimal_gcv, optimal_edf).
    6. Confirm cross-links to index.md, python-api.md, and mcp.md resolve on the built site.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs source -> published site | Hand-authored markdown becomes a public doc page; inaccurate install/compat text or invented flags would mislead readers |
| Docs fence -> docs build | Illustrative fences must NOT execute; an accidental exec fence would pull the [mcp]/[advisor] extras + Python 3.10+ + API key into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | Install/compatibility text | medium | mitigate | Install commands + compat text copied verbatim from SKILL.md; acceptance criteria grep the exact git-URL and one-liner substrings |
| T-17-02 | Tampering | Walkthrough content | medium | mitigate | Walkthrough steps mirror scripts/fdars_advisor_walkthrough.py; acceptance criteria grep the mirrored function names, n_basis values, and the 4 delta keys |
| T-17-03 | Denial of Service | Docs build | high | mitigate | All fences illustrative (no exec attribute); acceptance criteria assert 0 exec fences and a passing docs build so the build never requires extras/API key |

No package installs are introduced by this phase (documentation-only). No package-legitimacy gate required (T-17-SC not applicable).
</threat_model>

<verification>
- `docs/advisor/agent-skill.md` exists and the full docs build passes: `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` exits 0.
- No executed fence on the page: `grep -c 'exec=' docs/advisor/agent-skill.md` returns 0.
- Install commands present verbatim: git-URL substring `fdars @ git+https://github.com/sipemu/pyfda` and future one-liner `fdars[mcp,advisor]`.
- Compatibility documented: `3.10` and `ANTHROPIC_API_KEY` both present.
- Walkthrough method-accurate: `run_method`, `build_diagnostics`, `compare_run`, `n_basis=15`, `n_basis=25`, and all four delta keys present.
- Cross-links to `index.md`, `python-api.md`, `mcp.md` present.
</verification>

<success_criteria>
1. The page covers git-URL install and the interpret->recommend->re-run->compare walkthrough, accurate against `.claude/skills/fdars-advisor/` (SKILL.md + walkthrough) — SKILLDOC-01.
2. The page documents the skill's execution-environment / compatibility requirements (Python 3.10+, package-manager access, offline vs. API-key-gated boundary) — SKILLDOC-02.
3. The page builds cleanly and contains NO executed fence — the docs build does not require the [mcp]/[advisor] extras, Python 3.10+, or an API key.
</success_criteria>

<output>
Create `.planning/phases/17-agent-skill-page/17-01-SUMMARY.md` when done.
</output>

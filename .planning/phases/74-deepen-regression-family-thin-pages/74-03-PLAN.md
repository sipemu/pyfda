---
phase: 74-deepen-regression-family-thin-pages
plan: 03
type: execute
wave: 3
depends_on: [74-01]
files_modified:
  - docs/regression/additive-sof.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-01]
estimate:
  tokens: 58000
  raw_tokens: 39000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "additive-sof.md has a '## When to use' decision section and a '## See also' block (DEPTH-01)"
    - "fam/fregre_gsam defaults corrected to ncomp=0 (auto) and bandwidth=0.0 (auto), not 3/0.5"
    - "model_selection_ncomp uses max_comp (not ncomp_max); variable_selection documents its full 9-key return dict; fregre_gkam documents iterations + r_squared"
    - "Page carries >=3 admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK offline under .venv (module is fdars.scalar_on_function)"
  artifacts:
    - docs/regression/additive-sof.md
  key_links:
    - "Fences import from fdars.scalar_on_function (NOT fdars.regression) and run against the installed .venv package"
    - "See also links resolve to existing docs/regression/*.md pages"
---

<objective>
Bring `docs/regression/additive-sof.md` to mature-page parity (DEPTH-01), following the pattern proven by the frechet-regression tracer (74-01). Correct the confirmed default/param-name/return-key errors, add the missing decision/interpretation/cross-ref sections, and add runnable sentinel fences including a partial-effect figure.

Purpose: A reader can decide when to use the additive/generalized SoF family, choose ncomp/bandwidth (or use auto), interpret component_fits / active_predictors, and copy runnable examples.
Output: Parity-grade page with every exec fence emitting FDARS_FENCE_OK offline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. Modifies exactly ONE existing tracked doc page: `docs/regression/additive-sof.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Deepen additive-sof.md to parity + fix fam/gsam defaults & return keys</name>
  <files>docs/regression/additive-sof.md</files>
  <read_first>
    - docs/regression/additive-sof.md (preserve the correct additive-model intro, the fam parameter/return tables, the fregre_gsam/fregre_gkam brief descriptions, the group_mcp/group_scad ValueError note, and references)
    - docs/regression/scalar-on-function.md (mature template to mirror)
    - docs/regression/frechet-regression.md (the completed tracer — copy its section shape and fence style)
    - src/scalar_on_function_mod.rs (source of truth: fam, fregre_gsam, fregre_gkam, variable_selection, model_selection_ncomp — VERIFIED lines in RESEARCH 3.3)
    - .planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md (Sections 2.3, 3.3, 4, 5, 6, pitfall 3)
    - scripts/docs_fig.py (fig/render for partial-effect curves)
  </read_first>
  <action>
Restructure to mirror the mature template, preserving correct existing content. All fences import from fdars.scalar_on_function (NOT fdars.regression). Deliver ALL of the following. (DEPTH-01)

CORRECT these confirmed API errors (VERIFIED against src/scalar_on_function_mod.rs per RESEARCH 2.3 / 3.3):
1. fam and fregre_gsam defaults are ncomp=0 (auto via GCV) and bandwidth=0.0 (auto per component via GCV), NOT ncomp=3 / bandwidth=0.5. Fix the API-reference default column; also fix n_grid_bandwidth default to 20. The existing exec fence may still pass explicit ncomp/bandwidth values (that is legal) but the DEFAULTS documented in the table must be 0 / 0.0 (RESEARCH pitfall 3).
2. model_selection_ncomp parameter is max_comp (NOT ncomp_max). Fix the call and table. Its return dict is best_ncomp + criteria (list of (ncomp, aic, bic, gcv) tuples).
3. variable_selection returns a 9-key dict: active_predictors (bool P,), coefficients (list), fitted_values, residuals, intercept, lambda, r_squared, iterations, converged. Replace the 2-key description. Supported penalties are "group_lasso" and "ls" only.
4. fregre_gkam returns 8 keys: fitted_values, residuals, component_fits, intercept, bandwidths, iterations, converged, r_squared. Add the missing iterations and r_squared. Its signature is fregre_gkam(predictors, y, argvals_list, scalar_covariates=None, bandwidth=0.0, kernel="gaussian", max_iter=50, epsilon=1e-6), with predictors a list of (n, m_p) arrays and argvals_list a list of (m_p,) grids.
5. Confirm fam/fregre_gsam return the same 7-key dict: fitted_values, residuals, component_fits (list of 1D arrays, one per FPC), intercept, bandwidths (ncomp,), ncomp, r_squared.

ADD these missing parity sections:
- Keep the intro + concept-diagram image line.
- A "## When to use" decision section: FAM/GSAM (additive functional model, smooth per-component effects) vs linear scalar-on-function vs GKAM (kernel additive, multiple functional predictors). Small table + a "Start with ..." quick rule; link scalar-on-function.md inline.
- Numbered method sections for fam, fregre_gsam, fregre_gkam, variable_selection, and model_selection_ncomp, each with corrected API/return tables and a runnable fence where natural.
- Parameter-selection subsection: ncomp=0/bandwidth=0.0 auto-GCV vs fixed; backfitting convergence guidance for fregre_gkam (max_iter/epsilon; check the converged flag and iterations).
- Result-interpretation subsection: what component_fits are (per-FPC / per-predictor partial effects); what active_predictors means and choosing lambda in variable_selection.
- Keep the group_mcp/group_scad ValueError note (already accurate). Add an accurate "Methods available in the R package but not (yet) in Python" note only if a genuinely-missing R method exists (the group_mcp/group_scad penalties are the accurate not-yet-implemented item per RESEARCH 3.3).
- Keep references; append a "## See also" bullet list linking (RESEARCH Section 4) scalar-on-function.md, robust-regression.md, cross-validation.md, index.md (3-5 links).

FENCES (>=3 runnable, each printing the sentinel; >=1 with html=1; keep n<=30, m<=50; deterministic rng; import from fdars.scalar_on_function):
- A fam fit fence (may pass explicit ncomp/bandwidth for determinism/speed) printing ncomp and r_squared, then the sentinel.
- A variable_selection or fregre_gkam fence printing active_predictors / converged (as applicable), then the sentinel.
- ONE html=1 partial-effect figure fence plotting one or more component_fits curves (from docs_fig import fig, render), then the sentinel.
- If a Tecator real-data fence is used (RESEARCH Assumption A1 / Section 5.3): time it first; fall back to synthetic if slow. Prefer synthetic to keep the Phase-79 build in budget.

ADMONITIONS (>=3, mixing types): e.g. a warning on the auto-default ncomp=0/bandwidth=0.0 vs the old wrong 3/0.5, a tip on GKAM backfitting convergence, an info/note on the scalar_on_function module path. Do NOT paste an acceptance-criterion literal as admonition body text.

Do NOT run mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG.
  </action>
  <acceptance_criteria>
    - grep for the When-to-use heading and the See-also heading each match >= 1
    - occurrences of the sentinel token in the page >= 3
    - lines matching the admonition prefix (note|tip|warning|info) >= 3
    - at least one fence opener declares html=1
    - the page uses max_comp for model_selection_ncomp and no longer contains ncomp_max
    - the page documents active_predictors AND at least one other of the corrected 9 variable_selection keys (e.g. converged or r_squared)
    - the shared fence runner reports ALL EXEC FENCES EMITTED the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/regression/additive-sof.md","utf8");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,uses_max_comp:/max_comp/.test(s),no_ncomp_max:!/ncomp_max/.test(s),varsel_keys:/active_predictors/.test(s)&&/(converged|r_squared|iterations)/.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>Missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; `ncomp_max` still appears (should be `max_comp`); or the corrected `variable_selection` keys are not documented.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/regression/additive-sof.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK`.</fails_when>
  </verify>
  <done>additive-sof.md is at parity with corrected fam/gsam auto-defaults, model_selection_ncomp max_comp param, the full variable_selection 9-key dict, and fregre_gkam iterations/r_squared; has `## When to use` + `## See also`, >=3 admonitions, >=3 sentinel fences (>=1 partial-effect figure), all fences importing from fdars.scalar_on_function and emitting FDARS_FENCE_OK offline.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only — one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv`. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes.
- Fence gate: `scripts/run_page_fences.py docs/regression/additive-sof.md` exits 0.
- Whole-site `mkdocs build --strict` deferred to Phase 79.
</verification>

<success_criteria>
additive-sof.md is at mature-page parity (DEPTH-01) with all confirmed API corrections; every exec fence emits FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/74-deepen-regression-family-thin-pages/74-03-SUMMARY.md` when done.
</output>

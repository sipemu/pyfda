---
phase: 74-deepen-regression-family-thin-pages
plan: 02
type: execute
wave: 2
depends_on: [74-01]
files_modified:
  - docs/regression/function-on-function.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-01]
estimate:
  tokens: 58000
  raw_tokens: 39000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "function-on-function.md has a '## When to use' decision section and a '## See also' block (DEPTH-01)"
    - "predict_fof is documented with new_x as the THIRD positional arg (before argvals); predict_fof_re with new_x FOURTH (after subject_ids)"
    - "fof_cv uses ncomp_x_max/ncomp_y_max (no ncomp_x_range); fof_re_regression shows max_iter/tol and its 13-key return dict"
    - "Page carries >=3 admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK offline under .venv"
  artifacts:
    - docs/regression/function-on-function.md
  key_links:
    - "Fences import from fdars.regression and run against the installed .venv package"
    - "See also links resolve to existing docs/regression/*.md pages"
---

<objective>
Bring `docs/regression/function-on-function.md` to mature-page parity (DEPTH-01), following the pattern proven by the frechet-regression tracer (74-01). Correct the confirmed argument-order and parameter-name errors, add the missing decision/interpretation/cross-ref sections, and add runnable sentinel fences including a beta-surface figure.

Purpose: A reader can decide when to use function-on-function regression, choose ncomp via fof_cv, interpret beta_surface / r_squared_t, and copy runnable examples.
Output: Parity-grade page with every exec fence emitting FDARS_FENCE_OK offline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. Modifies exactly ONE existing tracked doc page: `docs/regression/function-on-function.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Deepen function-on-function.md to parity + fix predict_fof/fof_cv API</name>
  <files>docs/regression/function-on-function.md</files>
  <read_first>
    - docs/regression/function-on-function.md (preserve the correct bivariate/truncated-FPCA intro, the fof_regression API table, the fpca_x/fpca_y excluded-keys note, and the references)
    - docs/regression/scalar-on-function.md (mature template to mirror)
    - docs/regression/frechet-regression.md (the just-completed tracer — copy its section shape, admonition mix, and fence style)
    - src/regression_mod.rs (source of truth: fof_regression, predict_fof, fof_cv, fof_re_regression, predict_fof_re — VERIFIED lines in RESEARCH 3.2)
    - .planning/phases/74-deepen-regression-family-thin-pages/74-RESEARCH.md (Sections 2.2, 3.2, 4, 5, 6, 7 — corrections, cross-ref targets, fence patterns, pitfalls 2)
    - scripts/docs_fig.py (fig/render for the beta-surface heatmap)
  </read_first>
  <action>
Restructure the page to mirror the mature template, preserving correct existing content. Deliver ALL of the following. (DEPTH-01)

CORRECT these confirmed API errors (VERIFIED against src/regression_mod.rs per RESEARCH 2.2 / 3.2):
1. predict_fof signature is predict_fof(x_data, y_data, new_x, x_argvals, y_argvals, ncomp_x=3, ncomp_y=3) — new_x is the THIRD positional argument, before the argvals. Fix the call shown on the page and any parameter table ordering (RESEARCH pitfall 2).
2. fof_cv takes ncomp_x_max=5, ncomp_y_max=5, n_folds=5, seed=42 — there is NO ncomp_x_range list parameter. Rewrite the fof_cv call and describe its return dict: candidates (list of (int,int)), cv_errors, optimal (int,int), min_cv_mse.
3. predict_fof_re signature is predict_fof_re(x_data, y_data, subject_ids, new_x, x_argvals, y_argvals, ncomp_x=3, ncomp_y=3, max_iter=50, tol=1e-10) — new_x is FOURTH (after subject_ids). Fix the random-effects variant table.
4. fof_re_regression has max_iter=50, tol=1e-10 params and a 13-key return dict (the 9 fof_regression keys plus random_effects, sigma2_u, sigma2_eps, n_subjects). Document these.
5. Confirm the fof_regression 9-key return dict is stated accurately: intercept, beta_surface (m_y, m_x), fitted, residuals, r_squared_t (m_y,), r_squared, ncomp_x, ncomp_y, coef_matrix.

ADD these missing parity sections:
- Keep the intro + concept-diagram image line.
- A "## When to use" decision section: FoF (functional predictor -> functional response, global coefficient surface) vs concurrent regression (same-t local effect) vs scalar-on-function (scalar response). Small table + a "Start with ..." quick rule; link concurrent-regression.md and scalar-on-function.md inline.
- Numbered method sections for fof_regression, predict_fof, fof_cv, and the random-effects variant, each with the corrected API/return tables and a runnable fence where natural.
- Parameter-selection subsection: choosing ncomp_x / ncomp_y via fof_cv.
- Result-interpretation subsection: reading beta_surface (rows = response grid m_y, cols = predictor grid m_x) and when the pointwise r_squared_t diverges from the scalar r_squared.
- Accurate "Methods available in the R package but not (yet) in Python" note covering ONLY the two accurate items in RESEARCH 3.2 (basis-expansion FoF; historical covariance inspection tools).
- Keep references; append a "## See also" bullet list linking (RESEARCH Section 4) concurrent-regression.md, function-on-scalar.md, scalar-on-function.md, cross-validation.md, index.md (3-5 links).

FENCES (>=3 runnable, each printing the sentinel; >=1 with html=1; keep n<=25, mx<=20, my<=15; deterministic rng):
- A fof_regression fit fence printing beta_surface shape and r_squared, then the sentinel (RESEARCH Section 7 Fence-1 pattern is a valid starting point).
- A predict_fof fence using the CORRECTED argument order (new_x third) that prints the prediction shape, then the sentinel.
- ONE html=1 beta-surface heatmap figure fence (from docs_fig import fig, render; imshow of beta_surface) then the sentinel (RESEARCH Section 7 Fence-2 pattern).
- (Optional 4th) a fof_cv fence with ncomp_x_max/ncomp_y_max printing optimal, then the sentinel — include only if fast.

Note RESEARCH Assumption A3: if a fof_re_regression fence is added, use enough subjects/observations to converge (do NOT rely on n=20 with 2 groups). Prefer keeping the random-effects section prose-only with a non-exec code block if convergence on small n is unreliable.

ADMONITIONS (>=3, mixing types): e.g. a warning on predict_fof argument order, a tip on ncomp selection via fof_cv, an info/note on beta_surface orientation. Do NOT paste an acceptance-criterion literal (like a bare token) as admonition body text.

Do NOT run mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG.
  </action>
  <acceptance_criteria>
    - grep for the When-to-use heading and the See-also heading each match >= 1
    - occurrences of the sentinel token in the page >= 3
    - lines matching the admonition prefix (note|tip|warning|info) >= 3
    - at least one fence opener declares html=1
    - the page contains a predict_fof call with new_x positioned before x_argvals (new_x third)
    - the page no longer contains the string ncomp_x_range; it uses ncomp_x_max / ncomp_y_max
    - the shared fence runner reports ALL EXEC FENCES EMITTED the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/regression/function-on-function.md","utf8");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,no_ncomp_range:!/ncomp_x_range/.test(s),has_ncomp_max:/ncomp_x_max/.test(s),predict_newx_third:/predict_fof\(\s*[A-Za-z0-9_]+\s*,\s*[A-Za-z0-9_]+\s*,\s*new_x/.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>Missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; the string `ncomp_x_range` still appears; `ncomp_x_max` absent; or no `predict_fof(x, y, new_x, ...)` call with `new_x` in the third position.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/regression/function-on-function.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK`.</fails_when>
  </verify>
  <done>function-on-function.md is at parity with corrected predict_fof / predict_fof_re argument order and fof_cv / fof_re_regression parameters, has `## When to use` + `## See also`, >=3 admonitions, >=3 sentinel fences (>=1 beta-surface figure), and every exec fence emits FDARS_FENCE_OK offline.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only — one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with no external data. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes.
- Fence gate: `scripts/run_page_fences.py docs/regression/function-on-function.md` exits 0.
- Whole-site `mkdocs build --strict` deferred to Phase 79.
</verification>

<success_criteria>
function-on-function.md is at mature-page parity (DEPTH-01) with all confirmed API corrections; every exec fence emits FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/74-deepen-regression-family-thin-pages/74-02-SUMMARY.md` when done.
</output>

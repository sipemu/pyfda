---
plan: 65-01
phase: 65-style-spec-refresh-gate-review
wave: 1
depends_on: []
autonomous: false
requirements: [SPEC-02, GATE-01, GATE-02, GATE-03]
files_modified:
  - docs/assets/diagrams/STYLE_SPEC.md
---

# Plan 65-01 — STYLE_SPEC Refresh, Whole-Site Gate & Human Review

Final gate phase. Executed inline by the orchestrator (session-quota fallback). `autonomous: false` because GATE-03 is a blocking human review.

## Tasks

<task id="1" type="auto">
<name>SPEC-02 — refresh STYLE_SPEC.md</name>
<action>Update stale v1.0-era status/counts ("34 of 43") to current reality (93 concept diagrams; viewBox distribution 64/28/1); finalize the accessibility pattern to the now-universal role+aria-label+long-form title/desc/aria-labelledby; note decorative gallery thumbs (alt="" + aria-hidden); mark the legacy non-conforming viewBoxes RESOLVED; refresh the SVGO gate coverage section.</action>
<verify><automated>grep -q "93 of 93" docs/assets/diagrams/STYLE_SPEC.md && ! grep -q "34 of 43" docs/assets/diagrams/STYLE_SPEC.md</automated><fails_when>the "34 of 43" stale count still appears or the "93 of 93" refreshed status is absent</fails_when></verify>
</task>

<task id="2" type="auto">
<name>GATE-01 — SVGO idempotence + determinism</name>
<action>Run svgo@3.3.4 --config svgo.config.mjs idempotence check (svgo(svgo(x))==svgo(x)) across all 159 SVGs (concept + cards + thumbs).</action>
<verify><automated>npx --yes svgo@3.3.4 --config svgo.config.mjs -q -f docs/assets/diagrams -o /tmp/p1 && npx --yes svgo@3.3.4 --config svgo.config.mjs -q -f /tmp/p1 -o /tmp/p2 && diff -rq /tmp/p1 /tmp/p2</automated><fails_when>diff reports any differing file (non-idempotent diagram)</fails_when></verify>
</task>

<task id="3" type="auto">
<name>GATE-02 — whole-site mkdocs build --strict green offline</name>
<action>Run PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict; must exit 0.</action>
<verify><automated>PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict</automated><fails_when>non-zero exit or "Aborted with N warnings in strict mode"</fails_when></verify>
</task>

<task id="4" type="checkpoint">
<name>GATE-03 — blocking human diagram review</name>
<action>Present the consolidated human-review checklist (PHASE-65-HUMAN-REVIEW-CARRYFORWARD.md) and the built site to the user for visual method-accuracy/design sign-off. This gate cannot be self-approved; the milestone cannot close without it.</action>
<verify><manual>User visually reviews the corrected diagrams (esp. the carried-forward items) on the built site and approves.</manual><fails_when>user reports a method-accuracy or design defect</fails_when></verify>
</task>

## must_haves
- STYLE_SPEC.md reflects current reality (93 diagrams, universal a11y). [SPEC-02]
- SVGO idempotence green across all diagrams. [GATE-01]
- Whole-site --strict build exits 0 offline. [GATE-02]
- Human diagram review approved. [GATE-03]

## Artifacts this phase produces
- Refreshed docs/assets/diagrams/STYLE_SPEC.md
- 65-01-SUMMARY.md, 65-VERIFICATION.md

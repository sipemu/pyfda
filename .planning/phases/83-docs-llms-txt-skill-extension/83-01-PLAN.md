---
phase: 83-docs-llms-txt-skill-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_capability_dataset.py
  - docs/references.md
  - docs/llms.txt
autonomous: true
requirements: [DOCS-01, DOCS-02]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "`python scripts/generate_capability_dataset.py --references` runs with NO fdars import and exits 0"
    - "docs/references.md is regenerated with a Coverage section reading '28 of 437' (derived, not hardcoded)"
    - "docs/llms.txt gains a '## Scientific Provenance & Cross-Language Implementations' section with the same 437 denominator"
    - "Sentinel papers (keys starting `_uncurated`) never appear in either emitted artifact"
    - "The provenance section carries the 'MAY synthesize but MUST flag grounded: false' note"
  artifacts:
    - scripts/generate_capability_dataset.py
    - docs/references.md
    - docs/llms.txt
  key_links:
    - "emit_references() reads _references_map.json + _capability_map.json offline and calls _emit_references_page + _emit_llmstxt(ref_data, cap_data)"
    - "coverage denominator = sum(len(v) for v in cap_data.values()); numerator = callables backed by a curated:true paper (per DOCS-01/DOCS-02, mirrors server.py derivation)"
---

<objective>
Add the `--references` offline emitter to `scripts/generate_capability_dataset.py`. It reads the two committed JSON side-files (`_references_map.json` + `_capability_map.json`) WITHOUT importing `fdars`, and produces two artifacts: a family-grouped `docs/references.md` (DOCS-01) and an extended `docs/llms.txt` carrying a `## Scientific Provenance & Cross-Language Implementations` section (DOCS-02). Both surfaces state an honest DERIVED coverage fraction (`28/437` at authoring time), never a hardcoded literal.

Purpose: This is the phase tracer — it proves end-to-end that the provenance data curated in Phase 81 can be emitted offline into public, consumable docs. Everything downstream (nav wiring in Plan 02, skill in Plan 03) builds on these artifacts.
Output: A new `emit_references()` dispatch path plus the two regenerated doc files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md
@.planning/phases/83-docs-llms-txt-skill-extension/83-CONTEXT.md
</context>

## Artifacts this phase produces (this plan)

- NEW `--references` CLI flag in `scripts/generate_capability_dataset.py`
- NEW `emit_references(repo_root)` function + `_emit_references_page(...)` helper + `_provenance_section_lines(...)` helper
- Extended `_emit_llmstxt(...)` signature accepting optional `ref_data` / `cap_data`
- Regenerated `docs/references.md`
- Regenerated `docs/llms.txt` (now with the provenance section)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end `--references` offline emit — one path, both artifacts</name>
  <files>scripts/generate_capability_dataset.py</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Design 1 §1.1–1.6, Design 2 §2.1–2.3, Pitfalls 1/2/5/6)
    - scripts/generate_capability_dataset.py (lines 237–513: existing `_emit_llmstxt`, `emit_docs`, `__main__` dispatch — the offline pattern to mirror)
    - python/fdars/_references_map.json (top-level keys `papers` + `callable_index`; a curated paper has title/authors/year/doi/url/type/callables/cross_language/curated; `_uncurated*` sentinels have empty title/null year)
    - python/fdars/mcp/server.py:1032-1040 (the exact coverage derivation to mirror)
  </read_first>
  <action>
    In `scripts/generate_capability_dataset.py`, add the offline `--references` path. Do NOT import `fdars` anywhere reachable from this path (Pitfall 2 — the docs-build env has no compiled extension).

    (1) Dispatch: in the `__main__` block (currently `if "--llmstxt" ... else ...`), insert an `elif "--references" in sys.argv: emit_references(repo_root)` branch BEFORE the `else:` live-introspection branch. Per DOCS-01.

    (2) `emit_references(repo_root)`: mirror `emit_docs()` structure. Resolve `ref_path = repo_root/"python"/"fdars"/"_references_map.json"` and `cap_path = repo_root/"python"/"fdars"/"_capability_map.json"`. If either is missing, print an ERROR to stderr and `sys.exit(1)`. `json.load` both into `ref_data` and `cap_data`. Call `_emit_references_page(ref_data, cap_data, repo_root)` then `_emit_llmstxt(cap_data, repo_root, ref_data=ref_data, cap_data=cap_data)`. Print both written paths. Document in the function docstring (and update the module docstring Usage block) that `--references` re-emits BOTH `docs/references.md` AND the full `docs/llms.txt` (with the provenance section) so the two stay in sync.

    (3) Shared coverage helper `_coverage_counts(ref_data, cap_data) -> tuple[int, int]`: `denominator = sum(len(v) for v in cap_data.values())`; `curated_paper_keys = frozenset(pk for pk, p in ref_data.get("papers", {}).items() if p.get("curated", False) is True)`; `numerator = sum(1 for _, pks in ref_data.get("callable_index", {}).items() if any(pk in curated_paper_keys for pk in pks))`. Return `(numerator, denominator)`. This mirrors server.py:1032-1040 exactly (DOCS-01/DOCS-02 honest-denominator rule). NEVER write a `437` or `409` or `28` integer literal in the source — all three numbers are derived.

    (4) `_emit_references_page(ref_data, cap_data, repo_root) -> Path`: write `repo_root/"docs"/"references.md"`. Skip any paper whose key starts with `_uncurated` (`if paper_key.startswith("_uncurated"): continue` — Pitfall 6). Group the remaining papers by primary module = `paper["callables"][0].split(".")[0]`; a paper appears once, under its primary module, listing all of its `callables` (Pitfall 5 — do not split multi-module papers). Sort module groups alphabetically; within a group keep paper keys in sorted order. Emit a top intro blockquote + a `## Coverage` section stating the derived `{numerator} of {denominator}` fraction. For the module heading use `## Fdata Class Methods` when the module key is `_Fdata`, else `## fdars.{module}` with the summary from the existing `_MODULE_SUMMARIES` dict. Per paper, emit an H4 with authors/year/title and the fields (Authors, Year, DOI shown as `doi:{doi}` only when non-empty, URL as a markdown link, Type, Status = "Curated" when `curated:true` else "Pending DOI verification"), the list of implementing callables (each with its purpose looked up via a small `_callable_purpose(key, cap_data)` helper that splits on the first `.` and does `cap_data.get(module, {}).get(fn, {}).get("purpose", "")`), and a cross-language table ONLY when `paper.get("cross_language")` is non-empty (omit the table entirely otherwise — honest gap, not a placeholder). Only use plain Markdown (headings, tables, bullet lists); no `{#anchor}` attr_list syntax and no HTML blocks (Design 3.2 gotchas). The only internal doc link permitted is `[AI Capability Map](ai-capability-map.md)` (that file exists).

    (5) Extend `_emit_llmstxt` signature to `_emit_llmstxt(data, repo_root, ref_data=None, cap_data=None)`. Leave all existing emit logic unchanged. After the existing per-module loop, if BOTH `ref_data` and `cap_data` are not None, `lines.extend(_provenance_section_lines(ref_data, cap_data))` before joining/writing. The existing `emit_docs()` caller passes no ref/cap args, so the `--llmstxt` path is byte-unchanged except that it must still produce identical output (backward compat — Design 2.1).

    (6) `_provenance_section_lines(ref_data, cap_data) -> list[str]`: build the `## Scientific Provenance & Cross-Language Implementations` block (DOCS-02). Header line: derived `Coverage: {numerator} of {denominator} public callables have curated primary-paper entries.` Then the note that a consumer MAY synthesize provenance for uncurated callables but MUST structurally flag each synthesized citation ungrounded, never presenting synthesized provenance as curated. Then per-module H3 subsections; per callable, one line per backing paper in the form `- \`{module.callable}\` — {Authors} ({Year})` followed by `doi:{doi}` ONLY when doi is non-empty, followed by cross-language pointers `; R: {pkg}::{fn} / Python: {pkg}.{fn}` ONLY for languages present in `cross_language` (omit the `;` segment entirely when `cross_language` is empty). Emit ONLY callables whose backing paper is `curated:true` (same predicate as the numerator); never emit `_uncurated*` sentinels. Close the section with the verbatim uncurated-count note `> Uncurated callables ({denominator - numerator} of {denominator}) are absent from this section.` and the `{ "grounded": false }` per-citation instruction.

    Keep all string content in helper-returned line lists; do not place fenced code blocks in emitted markdown that would be mistaken for executable fences.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && { [ -x .venv/bin/python ] && P=.venv/bin/python || P=python; } && $P -c "import ast,sys; src=open('scripts/generate_capability_dataset.py').read(); t=ast.parse(src); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,))}; assert {'emit_references','_emit_references_page','_provenance_section_lines'} <= names, sorted(names); assert '--references' in src; assert '437' not in src and ' 409' not in src, 'hardcoded denominator literal found'; print('STRUCTURE OK')"</automated>
    <fails_when>emit_references / _emit_references_page / _provenance_section_lines are absent, the `--references` flag string is missing, or a hardcoded 437/409 denominator literal appears in the source.</fails_when>
  </verify>
  <done>The three new functions exist, the `--references` dispatch branch is wired, `_emit_llmstxt` accepts the optional ref/cap args, and no hardcoded coverage denominator literal appears in the source.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs/references.md + docs/llms.txt and assert offline + derived coverage</name>
  <files>docs/references.md, docs/llms.txt</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Design 1.5 page layout, Design 2.2 provenance template)
    - scripts/generate_capability_dataset.py (the functions written in Task 1)
  </read_first>
  <action>
    Run the emitter to produce the artifacts: `python scripts/generate_capability_dataset.py --references` (use `.venv/bin/python` if present). This regenerates `docs/references.md` and rewrites `docs/llms.txt` with the provenance section. Commit both regenerated files as part of this plan. Per DOCS-01 and DOCS-02.

    Confirm by inspection that `docs/references.md` has a `## Coverage` section reading `28 of 437` (authoring-time derived value), that `docs/llms.txt` contains the `## Scientific Provenance & Cross-Language Implementations` heading and the same `28 of 437` fraction, and that neither file contains any `_uncurated` sentinel paper (no empty-title/null-year rows).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && { [ -x .venv/bin/python ] && P=.venv/bin/python || P=python; } && PYTHONPATH= $P scripts/generate_capability_dataset.py --references && grep -q "28 of 437" docs/references.md && grep -q "## Scientific Provenance & Cross-Language Implementations" docs/llms.txt && grep -q "28 of 437" docs/llms.txt && ! grep -q "_uncurated" docs/references.md && ! grep -q "_uncurated" docs/llms.txt && echo EMIT_OK</automated>
    <fails_when>the emitter errors (e.g. an accidental fdars import under empty PYTHONPATH), the derived `28 of 437` fraction is absent from either file, the provenance heading is missing from llms.txt, or a `_uncurated` sentinel leaked into either artifact.</fails_when>
  </verify>
  <done>Both docs artifacts regenerate offline (empty PYTHONPATH proves no fdars import), each states the derived `28 of 437` coverage, llms.txt carries the provenance section, and no sentinel paper appears in either.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the emitter is offline and family grouping is honest</name>
  <files>docs/references.md, docs/llms.txt</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Pitfall 2 offline guarantee, Pitfall 5 multi-module paper, §2.2 grounded-false note)
    - docs/references.md, docs/llms.txt (the artifacts from Task 2)
  </read_first>
  <action>
    Add no new production code; this task asserts three honesty properties of the emitted surface required by DOCS-01/DOCS-02:
    (a) OFFLINE — the emit path imports no fdars. Assert by grepping the source: no bare `import fdars` outside `generate_capability_dataset()` / `_build_fdata_entry()` (the two `else:`-branch-only functions), and re-running the emitter with `PYTHONPATH=` succeeds.
    (b) GROUNDED-FALSE NOTE — `docs/llms.txt` provenance section contains the machine-readable `grounded: false` instruction verbatim so a consumer can parse it.
    (c) NO SENTINEL LEAK + curated-only — every `module.callable` line in the provenance section corresponds to a curated:true-backed callable (spot-check that `depth.fraiman_muniz_1d` appears and `clustering.align_cluster_fd` does NOT, since its only backing paper is an uncurated sentinel entry).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && { [ -x .venv/bin/python ] && P=.venv/bin/python || P=python; } && grep -vE "^\s*#" scripts/generate_capability_dataset.py | grep -n "import fdars" | grep -qv "" ; test $? -eq 1 && echo "no-stray-import-check-skipped"; PYTHONPATH= $P scripts/generate_capability_dataset.py --references >/dev/null 2>&1 && grep -q "grounded" docs/llms.txt && grep -q "depth.fraiman_muniz_1d" docs/llms.txt && ! grep -q "clustering.align_cluster_fd" docs/llms.txt && echo HONESTY_OK</automated>
    <fails_when>the emitter fails to run offline (empty PYTHONPATH), the `grounded` flag instruction is missing from llms.txt, the curated hit `depth.fraiman_muniz_1d` is absent, or the uncurated-only callable `clustering.align_cluster_fd` leaked into the provenance section.</fails_when>
  </verify>
  <done>The emit path runs under empty PYTHONPATH, the provenance section carries the `grounded` flag instruction, a known curated callable is present, and a known uncurated-only callable is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON → emitted docs | `_references_map.json` content is rendered verbatim into public docs; a wrong/sentinel entry would surface publicly |
| build env → emitter | docs-build env has no compiled `fdars`; an accidental import breaks CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-01 | Information Disclosure | emit_references sentinel handling | medium | mitigate | Skip `_uncurated*` keys in every emit loop; Task 3 asserts no sentinel leak |
| T-83-02 | Tampering | hardcoded coverage denominator | high | mitigate | Derive numerator/denominator from live JSON; Task 1 verify greps for `437`/`409` literals and fails on hit |
| T-83-03 | Denial of Service | accidental `import fdars` in offline path | high | mitigate | Keep all fdars imports inside the `else:`-only functions; Task 3 runs the emitter under empty PYTHONPATH |
| T-83-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan — docs emit only, no new dependencies |
</threat_model>

<verification>
- `python scripts/generate_capability_dataset.py --references` exits 0 under empty PYTHONPATH.
- `docs/references.md` has a `## Coverage` section with the derived `28 of 437`.
- `docs/llms.txt` has the `## Scientific Provenance & Cross-Language Implementations` section, the same fraction, and the `grounded` flag note.
- No `_uncurated` sentinel appears in either artifact.
- No `437`/`409` denominator literal in `scripts/generate_capability_dataset.py`.
</verification>

<success_criteria>
DOCS-01 and DOCS-02 satisfied: an offline `--references` emitter produces a family-grouped `docs/references.md` and an extended `docs/llms.txt` provenance section, both stating an honest derived coverage fraction, with no sentinel leakage and no fdars import.
</success_criteria>

<output>
Create `.planning/phases/83-docs-llms-txt-skill-extension/83-01-SUMMARY.md` when done. Note the 409→437 denominator correction (requirement text says 409; emitted honest value is 437) for Phase-84 requirement-text reconciliation.
</output>

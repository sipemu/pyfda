---
phase: 83-docs-llms-txt-skill-extension
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/fdars-capabilities/SKILL.md
  - tests/test_references_skill_boundary.py
autonomous: true
requirements: [SKILL-01, SKILL-02]
estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "SKILL.md has a `## Scientific Provenance Protocol` section with the three-path hybrid decision tree (curated hit / curated:false hit / NO_CURATED_ENTRY sentinel)"
    - "the protocol requires a machine-readable `grounded: false` flag per synthesized citation and forbids presenting synthesized provenance as curated"
    - "the walkthrough demonstrates BOTH a curated hit (depth.fraiman_muniz_1d) and an ungrounded fallback (explain.shap_values sentinel and/or clustering.align_cluster_fd all-curated:false)"
    - "skill tests exercise both branches and pass (or skip cleanly on Python 3.9 without mcp)"
    - "the section states its non-duplication boundary with fdars-advisor"
  artifacts:
    - .claude/skills/fdars-capabilities/SKILL.md
    - tests/test_references_skill_boundary.py
  key_links:
    - "the protocol routes on the Phase-82 fdars_method_references return contract: curated:true → verbatim; curated:false hit → candidate + grounded:false; NO_CURATED_ENTRY sentinel → MAY synthesize + grounded:false"
    - "tests mirror tests/test_guard_sync_version_independent.py convention (pytest.importorskip('mcp'))"
---

<objective>
Extend `.claude/skills/fdars-capabilities/SKILL.md` with a `## Scientific Provenance Protocol` section (SKILL-01) encoding the hybrid curated/ungrounded decision tree over the Phase-82 `fdars_method_references` tool, plus a both-path walkthrough and a skill-boundary test file (SKILL-02) that exercises the curated-hit and ungrounded-fallback branches.

Purpose: The provenance surface is only consumable by an agent if the skill tells it exactly how to route curated vs ungrounded citations and NEVER pass synthesized provenance off as curated. This closes the consumer side of the phase.
Output: A new SKILL.md section + `tests/test_references_skill_boundary.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- NEW `## Scientific Provenance Protocol` section in `.claude/skills/fdars-capabilities/SKILL.md`
- NEW `tests/test_references_skill_boundary.py` exercising both routing branches

<tasks>

<task type="auto">
  <name>Task 1: Add the Scientific Provenance Protocol section + both-path walkthrough to SKILL.md</name>
  <files>.claude/skills/fdars-capabilities/SKILL.md</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Design 4.1 insertion point, 4.2 decision tree, 4.3 walkthroughs A/B/C, Pitfall 4)
    - .claude/skills/fdars-capabilities/SKILL.md (existing sections: Capability Map, Module Overview, How to Find a Method, Skill Boundary at ~line 82, Optional Extras at ~line 91 — insert the new section between Skill Boundary and Optional Extras)
    - python/fdars/mcp/server.py:838-1044 (the fdars_method_references return contract: curated:true hit, curated:false hit, NO_CURATED_ENTRY sentinel, AMBIGUOUS_CALLABLE sentinel)
    - python/fdars/_references_map.json (confirm depth.fraiman_muniz_1d → fraiman_muniz_2001 curated:true; clustering.align_cluster_fd → _uncurated (all curated:false); explain.shap_values absent from callable_index → sentinel)
  </read_first>
  <action>
    Insert a `## Scientific Provenance Protocol` section into `.claude/skills/fdars-capabilities/SKILL.md` AFTER `## Skill Boundary` and BEFORE `## Optional Extras`, in the skill's existing prose style. Per SKILL-01 and SKILL-02.

    Content (three-path hybrid decision tree over the Phase-82 tool):
    - Step 1: look up via `fdars_method_references("<module.callable>")` (MCP tool, Python 3.10+), with the offline `_references_map.json` fallback and the published `references.md` / `llms.txt` URLs as alternative surfaces.
    - Step 2 routing:
      - Path A — return has `curated: true` with at least one `curated:true` paper: use the paper data VERBATIM (authors, year, DOI, cross-language); do NOT paraphrase or add citations not in the payload.
      - Path B — a hit where ALL backing papers are `curated: false`: state the candidate paper(s) from the payload WITH the caveat that they are pending Phase-84 human DOI-landing-page verification; attach `grounded: false` to each citation in structured output; do NOT present as confirmed curated provenance.
      - Path C — `NO_CURATED_ENTRY` sentinel (callable absent from callable_index): the consumer MAY synthesize a candidate from training knowledge but MUST structurally flag every synthesized citation `{ "grounded": false, "source": "synthesis", "review_required": true }` and state explicitly it is ungrounded; NEVER omit the flag and NEVER present synthesized provenance as curated (Pitfall 4).
    - A non-duplication note: this protocol covers citation lookup + cross-language pointers only; for parameter tuning, method-selection diagnostics, and before/after comparisons use the `fdars-advisor` skill — do NOT route those here.

    Then add a both-path WALKTHROUGH demonstrating: (A) a curated hit `depth.fraiman_muniz_1d` → Path A verbatim response citing Fraiman & Muniz (2001) with the R/Python cross-language pointers; and at least one ungrounded fallback — (B) `explain.shap_values` → NO_CURATED_ENTRY sentinel → Path C synthesized-and-labelled response carrying the `grounded: false` structure, and/or (C) `clustering.align_cluster_fd` → all-curated:false hit → Path B candidate-with-caveat carrying `grounded: false`. The walkthrough must make the ungrounded citation VISIBLY labelled, never styled as curated.

    Keep the `grounded` flag token spelled exactly `grounded` (the tests grep for it). Do not alter any existing SKILL.md section.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q "## Scientific Provenance Protocol" .claude/skills/fdars-capabilities/SKILL.md && grep -q "NO_CURATED_ENTRY" .claude/skills/fdars-capabilities/SKILL.md && grep -q "grounded" .claude/skills/fdars-capabilities/SKILL.md && grep -q "depth.fraiman_muniz_1d" .claude/skills/fdars-capabilities/SKILL.md && grep -q "fdars-advisor" .claude/skills/fdars-capabilities/SKILL.md && echo SKILL_SECTION_OK</automated>
    <fails_when>the `## Scientific Provenance Protocol` heading is absent, or the section is missing any of: the `NO_CURATED_ENTRY` sentinel path, the `grounded` flag, the curated-hit walkthrough callable `depth.fraiman_muniz_1d`, or the `fdars-advisor` non-duplication note.</fails_when>
  </verify>
  <done>SKILL.md gains the `## Scientific Provenance Protocol` section (between Skill Boundary and Optional Extras) with the three-path decision tree, both-path walkthrough, the machine-readable `grounded: false` flag, and the fdars-advisor boundary note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/test_references_skill_boundary.py exercising both routing branches</name>
  <files>tests/test_references_skill_boundary.py</files>
  <read_first>
    - .planning/phases/83-docs-llms-txt-skill-extension/83-RESEARCH.md (Design 4.4 skill test convention)
    - tests/test_guard_sync_version_independent.py (the `pytest.importorskip("mcp")` guard convention; `test_references_tool_llm_free_boundary` uses depth.fraiman_muniz_1d as the curated hit and explain.shap_values as the sentinel miss)
    - python/fdars/mcp/server.py (fdars_method_references return keys: curated, papers, sentinel)
  </read_first>
  <behavior>
    - Path A (curated hit): fdars_method_references("depth.fraiman_muniz_1d") returns curated is True and at least one paper with curated is True.
    - Path C (sentinel): fdars_method_references("explain.shap_values") returns curated is False and sentinel == "NO_CURATED_ENTRY".
    - Both tests skip cleanly on Python 3.9 (no mcp) via pytest.importorskip("mcp").
  </behavior>
  <action>
    Create `tests/test_references_skill_boundary.py` following the existing MCP-test convention (SKILL-02). Use `pytest.importorskip("mcp")` at the top of each test so it skips on Python 3.9 where mcp is unavailable, then `from fdars.mcp.server import fdars_method_references`.

    Two tests:
    - `test_references_curated_hit_returns_curated_true` (Path A): call `fdars_method_references("depth.fraiman_muniz_1d")`; assert `result["curated"] is True` and at least one paper in `result["papers"]` has `curated` true.
    - `test_references_sentinel_returns_no_curated_entry` (Path C): call `fdars_method_references("explain.shap_values")`; assert `result["curated"] is False` and `result.get("sentinel") == "NO_CURATED_ENTRY"`.

    These directly mirror the hit/miss pair the phase-82 guard already exercises, so they are stable. Do not add a Path-B test unless the return contract for the all-curated:false case is confirmed against server.py during execution (Path B is demonstrated in the SKILL.md walkthrough, which is sufficient for SKILL-02's "both paths" requirement — the required branches for the test are the curated hit and the ungrounded sentinel).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && { [ -x .venv/bin/python ] && P=.venv/bin/python || P=python; } && PYTHONPATH=python $P -m pytest tests/test_references_skill_boundary.py -q</automated>
    <fails_when>either test fails (wrong curated flag, missing sentinel) or errors on collection; a clean skip on a no-mcp interpreter is acceptable and does NOT count as failure.</fails_when>
  </verify>
  <done>`tests/test_references_skill_boundary.py` exists with both branch tests; they pass (or skip cleanly without mcp) under pytest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill guidance → agent output | if the skill permits an unflagged synthesized citation, a consumer treats ungrounded provenance as curated fdars data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-05 | Spoofing | synthesized citation presented as curated | high | mitigate | Protocol Path C mandates the machine-readable `grounded: false` flag and forbids curated styling; SKILL section verify greps for `grounded` (Pitfall 4) |
| T-83-06 | Repudiation | skill/tool boundary drift with fdars-advisor | low | accept | Non-duplication note in the section; boundary is documentation-level, not enforced in code |
| T-83-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — SKILL.md edit + a stdlib/pytest test, no new dependencies |
</threat_model>

<verification>
- SKILL.md has `## Scientific Provenance Protocol` with the three-path tree, `grounded` flag, both-path walkthrough, and the fdars-advisor boundary.
- `pytest tests/test_references_skill_boundary.py -q` passes (or skips cleanly without mcp).
</verification>

<success_criteria>
SKILL-01 and SKILL-02 satisfied: the skill carries the hybrid curated/ungrounded protocol with a machine-readable `grounded: false` flag and non-duplication boundary, its walkthrough demonstrates both the curated hit and the ungrounded fallback, and tests exercise both branches.
</success_criteria>

<output>
Create `.planning/phases/83-docs-llms-txt-skill-extension/83-03-SUMMARY.md` when done.
</output>
---
phase: 53-closed-loop-auto-tuning-capstone
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/advisor/_schema.py
  - python/fdars/advisor/_tuning.py
  - tests/test_advisor_tuning.py
  - tests/test_advisor_schema.py
autonomous: true
requirements: [TUNE-01, TUNE-02, TUNE-05, TUNE-06]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "run_tuning_loop() with a mock always-improve propose_fn terminates at max_steps (stop_reason='budget') offline, no API key (TUNE-01, TUNE-02)"
    - "A no-improvement mock propose_fn converges after K=3 consecutive non-improvements (stop_reason='converged') (TUNE-02)"
    - "An oscillating mock propose_fn that revisits a rounded param value or ping-pongs terminates with stop_reason='oscillation' (TUNE-02)"
    - "A propose_fn raising _UnparseableProposalError exits immediately with stop_reason='parse_failure' and n_steps=0 — the loop never retries the proposer (TUNE-01)"
    - "A clustering mock that drives k to a degenerate cluster (min(cluster_sizes)<2) stops with stop_reason='guard_stop' and non-empty guard_violations while the target was improving — Goodhart protection (TUNE-05)"
    - "Two identical (propose_fn, initial params, mocked diagnostics) calls produce byte-identical TuningTrace — determinism (TUNE-01)"
    - "TuneProposal/TuningStep/TuningTrace/TuneResult are importable and JSON-serialisable both with pydantic and via the ImportError fallback stand-ins (TUNE-06)"
    - "Recommendation.parameter_delta defaults to None; existing Advice/Recommendation construction for interpretation/parameter/method/comparison/pipeline tasks is unchanged (TUNE-06)"
  artifacts:
    - python/fdars/advisor/_tuning.py
    - python/fdars/advisor/_schema.py
    - tests/test_advisor_tuning.py
    - tests/test_advisor_schema.py
  key_links:
    - "run_tuning_loop checks step>=max_steps BEFORE calling propose_fn (budget-before-propose precedence)"
    - "target direction is read from _METRIC_REGISTRY (imported), never a new direction dict"
    - "list-valued targets (cumulative_variance_explained) extracted via _extract_metric_value, never direct index"
    - "float params rounded to 4 significant figures before entering visited_params set"
    - "_check_guards reads min(cluster_sizes) with an isinstance-list guard, never the raw list"
---

<objective>
TRACER + schemas: prove the bounded tuning loop end-to-end, fully offline, with an injectable mock propose_fn — before any LLM, heuristic, or MCP work exists. This wave-1 slice wires the single path: propose (mock) then apply (mocked run_method) then rebuild diagnostics (mocked) then compare (real _METRIC_REGISTRY direction) then guard check then termination decision then TuningTrace record. It also lands the schema types the loop records into (per D: TUNE-06).

Purpose: The loop mechanics are simple; the SAFETY guarantees (bounded termination, oscillation detection, Goodhart guard, determinism, LLM-never-in-numeric-path) are the phase's actual risk. Proving them on a mock in wave 1 catches an architectural dead-end after one commit instead of after LLM+heuristic+MCP layers are built on a broken core.
Output: _tuning.py (loop core + _PARAM_REGISTRY + _UnparseableProposalError), _schema.py extended with TuneProposal/TuningStep/TuningTrace/TuneResult + optional Recommendation.parameter_delta, and an offline test suite exercising all five stop reasons + determinism.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md
@.planning/phases/53-closed-loop-auto-tuning-capstone/53-CONTEXT.md

@python/fdars/advisor/_compare_methods.py
@python/fdars/advisor/_schema.py
</context>

<artifacts>
## Artifacts this phase produces (cumulative — this plan's slice)

- run_tuning_loop(dataset_id, method, initial_params, target_metric, propose_fn, *, max_steps=10, eps=1e-4, no_improve_window=3, guard_thresholds=None, argvals=None, seed=None) -> TuningTrace in python/fdars/advisor/_tuning.py
- _PARAM_REGISTRY (smoothing/basis/fpca/clustering tuneable=True; alignment/depth tuneable=False) in _tuning.py
- _UnparseableProposalError, _check_guards, _round_param, _is_improvement, _extract_target, _is_ping_pong helpers in _tuning.py
- _make_mock_propose_fn test seam (mock proposer)
- Schemas TuneProposal, TuningStep, TuningTrace, TuneResult + optional Recommendation.parameter_delta in _schema.py
- Tests: tests/test_advisor_tuning.py, tests/test_advisor_schema.py
</artifacts>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Schema types + optional parameter_delta (pydantic and fallback)</name>
  <files>python/fdars/advisor/_schema.py, tests/test_advisor_schema.py</files>
  <read_first>
    - python/fdars/advisor/_schema.py (the file being modified — study the try/except ImportError twin-definition pattern: Recommendation/Advice/PipelineReport each defined once under pydantic and once as a plain stand-in)
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md Schema Design section (field lists) and Open Question 4 (fallback requirement)
  </read_first>
  <behavior>
    - TuneProposal fields: param str, new_value float, rationale str
    - TuningStep fields: step int, param_before float, param_after float, target_before float, target_after (float or None), accepted bool, stop_reason (str or None), guard_violations list of str, proposal_source str
    - TuningTrace fields: method, param, target_metric, target_direction, steps list of TuningStep, final_params dict, final_diagnostics dict, converged bool, stop_reason str, n_steps int, steps_used int, budget_remaining int
    - TuneResult fields: trace TuningTrace, improved bool, initial_target_value float, final_target_value float, improvement_pct (float or None)
    - Recommendation gains optional parameter_delta defaulting to None; existing five-field construction still works with no parameter_delta supplied
    - All four new types round-trip through json.dumps of their field dict under pydantic and under the fallback path
  </behavior>
  <action>Extend _schema.py following its existing twin-definition contract. Under the try pydantic branch, add TuneProposal, TuningStep, TuningTrace, TuneResult as _PydanticBaseModel subclasses with the fields in the behavior block, using Optional (import from typing) for nullable fields defaulting to None. Add parameter_delta Optional TuneProposal defaulting None to the existing Recommendation pydantic class as the LAST field so the five prior usages stay valid. Under the except ImportError branch, add plain stand-in classes with the same fields, an __init__ giving parameter_delta on Recommendation and every nullable field a None default, plus __repr__ and __eq__ mirroring the existing stand-ins. TuningStep and TuningTrace stand-ins accept keyword construction. Do NOT change the module no-import invariant (still imports nothing from other fdars.advisor submodules). In tests/test_advisor_schema.py add test_recommendation_parameter_delta_optional (construct Recommendation with the original five fields, assert parameter_delta is None), test_tune_proposal_fields, and test_tune_schema_json_serialisable (build a TuningTrace with one TuningStep and assert json.dumps over the field dict succeeds). If tests/test_advisor_schema.py does not exist create it with these tests; if it exists append.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -m pytest tests/test_advisor_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_advisor_schema.py exits 0
    - Recommendation constructed with the original five fields has parameter_delta None
    - TuneProposal, TuningStep, TuningTrace, TuneResult import from fdars.advisor._schema
    - A TuningTrace containing one TuningStep is JSON-serialisable
  </acceptance_criteria>
  <done>Four tuning schema types plus the optional Recommendation.parameter_delta exist under both the pydantic and the ImportError-fallback branches; schema tests pass; no existing schema test regresses.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Bounded loop core + _PARAM_REGISTRY + termination state machine</name>
  <files>python/fdars/advisor/_tuning.py</files>
  <read_first>
    - python/fdars/advisor/_compare_methods.py (analog — _METRIC_REGISTRY, _extract_metric_value, and the ValueError-on-unknown-metric fail-closed pattern the loop reuses)
    - python/fdars/advisor/_schema.py (the TuningStep/TuningTrace types this loop records into — from Task 1)
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md Tunable-Param Registry, Pattern 2 (convergence+oscillation with exact termination precedence), Pattern 5 (guard rules), Common Pitfalls 3/4/5/7/8
  </read_first>
  <behavior>
    - step>=max_steps checked as the FIRST action each iteration, giving stop_reason budget, before propose_fn is called
    - propose_fn returning a param-key set not equal to the declared param set gives stop_reason parse_failure
    - propose_fn raising _UnparseableProposalError gives stop_reason parse_failure; on the first iteration the loop exits with n_steps 0
    - a rounded new param value already in visited_params gives stop_reason oscillation
    - guard violation from _check_guards gives stop_reason guard_stop, step recorded accepted False
    - improvement measured direction-aware via _METRIC_REGISTRY; accepted step resets no_improve_count and adds rounded param to visited_params; rejected step increments no_improve_count
    - no_improve_count >= no_improve_window gives stop_reason converged
    - last-3 accepted param values alternating direction with targets within eps gives stop_reason oscillation (ping-pong)
    - float params rounded to 4 significant figures for visited_params; ints used exactly
    - seed extracted from initial_params and forwarded to every clustering re-run (held fixed)
  </behavior>
  <action>Create python/fdars/advisor/_tuning.py. Define _PARAM_REGISTRY exactly per RESEARCH Recommended Tunable-Param Registry: smoothing (param n_basis, int, default 15, range 4 to 60, target optimal_gcv lower, guard optimal_edf upper_fraction); basis (lambda_, float, log_scale True, range 1e-6 to 1e4, target optimal_edf lower, guard optimal_gcv relative_degradation_20pct); fpca (n_comp, int, range 1 to 20, target cumulative_variance_explained higher, guard phase_leakage_indicator upper_threshold_0.5); clustering (k, int, range 2 to 15, target mean_amplitude_separation higher, guard cluster_sizes min_cluster_size_ge_2); alignment and depth with tuneable False plus a reason string. Import _METRIC_REGISTRY and _extract_metric_value from ._compare_methods (do NOT redefine direction). Define _UnparseableProposalError as an Exception subclass. Define helpers: _round_param(value, spec) (4 sig figs for float, exact int); _extract_target(diag, metric) delegating to _extract_metric_value; _is_improvement(new, prev, direction); _check_guards(diag, guard_thresholds, initial_diag, n_obs) implementing the four rules (upper_fraction uses n_obs from loop state per Open Question 1; min_cluster_size_ge_2 uses min of the list behind an isinstance-list check per Pitfall 4); _is_ping_pong(recent_param_values, recent_targets, eps). Implement run_tuning_loop with the signature in the artifacts section and the termination precedence exactly in behavior order: budget check FIRST, then propose_fn, then key-set validation, then oscillation-revisit, then run_method plus build_diagnostics (imported lazily inside the loop body, LLM-free), then guard check, then improvement/convergence/ping-pong. Extract seed from initial_params at loop start and forward it on every run_method call. Extract n_obs and argvals at loop start from the dataset registry (lazy import registry) when a real dataset_id is given; the loop must accept mocked run_method and build_diagnostics via monkeypatching. Record a TuningStep every iteration; assemble and return a TuningTrace. Add _make_mock_propose_fn(deltas) (replays a fixed delta list; raises StopIteration when exhausted) as a module-level test seam. Keep the module free of any anthropic or provider import.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -c "from fdars.advisor._tuning import run_tuning_loop, _PARAM_REGISTRY, _UnparseableProposalError, _check_guards; assert set(_PARAM_REGISTRY)=={'smoothing','basis','fpca','clustering','alignment','depth'}; assert _PARAM_REGISTRY['clustering']['tuneable'] and not _PARAM_REGISTRY['depth']['tuneable']; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The import-and-assert one-liner exits 0 (registry has the 6 methods with correct tuneable flags)
    - _tuning.py imports _METRIC_REGISTRY and _extract_metric_value from _compare_methods (grep confirms; no local direction dict)
    - grep -v '^#' python/fdars/advisor/_tuning.py | grep -c "import anthropic" is 0 (LLM-free at module load)
    - run_tuning_loop is callable with an injectable propose_fn and mocked run_method/build_diagnostics
  </acceptance_criteria>
  <done>_tuning.py exists with the six-method _PARAM_REGISTRY, the termination state machine in the researched precedence order, direction reused from _METRIC_REGISTRY, and the guard helpers handling the cluster_sizes-list and n_obs gotchas; the module imports offline with no LLM dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline tracer test suite — all five stop reasons + determinism + guard</name>
  <files>tests/test_advisor_tuning.py</files>
  <read_first>
    - python/fdars/advisor/_tuning.py (the loop being tested — from Task 2)
    - .planning/phases/53-closed-loop-auto-tuning-capstone/53-RESEARCH.md Offline Testability (fixtures + test matrix) and Common Pitfall 4 (cluster_sizes list guard)
    - tests/test_mcp_compare_methods.py (analog — monkeypatch/fixture style used in this repo's advisor tests)
  </read_first>
  <behavior>
    - test_budget_exhaustion: always-improve mock with max_steps 2 gives stop_reason budget and n_steps 2
    - test_convergence: no-improve mock gives stop_reason converged with converged True after K=3
    - test_oscillation_param_revisit: mock revisiting the same int param gives stop_reason oscillation
    - test_oscillation_ping_pong: 3-step alternating mock gives stop_reason oscillation
    - test_parse_failure: mock raising _UnparseableProposalError gives stop_reason parse_failure and n_steps 0, and the proposer is called exactly once (no retry)
    - test_guard_stop_clustering: mock driving k to a degenerate cluster gives stop_reason guard_stop with non-empty guard_violations
    - test_determinism: two identical calls give equal TuningTrace field dicts
    - test_fpca_cumulative_extraction: a list-valued cumulative_variance_explained target is reduced to its last element via _extract_metric_value
    - test_trace_json_serialisable: the returned TuningTrace field dict is json.dumps-able
  </behavior>
  <action>Create tests/test_advisor_tuning.py. All tests run offline with NO API key and NO network — monkeypatch the loop's run_method and build_diagnostics (patch the names as looked up inside fdars.advisor._tuning) to return synthetic in-memory diagnostics dicts so no real fdars call happens. Provide fixtures _always_improve_propose_fn, _no_improve_propose_fn, _oscillating_propose_fn, and _bad_propose_fn per RESEARCH Offline Testability. For test_guard_stop_clustering, have the mocked build_diagnostics return cluster_sizes with a singleton (e.g. one cluster of size 1) once k crosses a threshold while mean_amplitude_separation keeps rising, asserting the guard fires despite target improvement (Goodhart). For test_parse_failure wrap the proposer in a call-counter and assert it is invoked exactly once. For test_determinism call run_tuning_loop twice with identical inputs and a fixed mock and assert the two traces' field dicts compare equal. Cover the exact stop_reason strings budget, converged, oscillation, parse_failure, guard_stop. Do NOT introduce any anthropic import in the test module.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -m pytest tests/test_advisor_tuning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_advisor_tuning.py exits 0
    - loop terminates at max_steps with an always-improve mock propose_fn (budget)
    - oscillating mock triggers the oscillation stop
    - a propose_fn raising _UnparseableProposalError exits without a numeric-path retry (proposer called exactly once, n_steps 0)
    - guard degradation (singleton cluster) stops the loop with guard_stop while the target was improving
    - two identical runs produce equal TuningTrace field dicts
  </acceptance_criteria>
  <done>The offline test suite exercises all five stop reasons plus determinism and the fpca list-extraction, runs green with no API key or network, and proves the bounded mock-propose_fn loop end-to-end before any LLM/heuristic/MCP layer exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| propose_fn -> loop | proposer (mock now, LLM/heuristic later) returns an untrusted param value that must be validated/clamped before entering the numeric path |
| loop -> fdars (run_method) | only the declared scalar param value crosses; the method key is fixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53A-01 | Denial of Service | run_tuning_loop | high | mitigate | budget check (step>=max_steps) is the FIRST action each iteration; the loop is provably bounded even if propose_fn always proposes a novel improving value |
| T-53A-02 | Tampering | propose_fn output | high | mitigate | key-set validation rejects any proposer that changes the param key set (parse_failure); float rounding into visited_params catches near-revisits |
| T-53A-03 | Tampering | _check_guards on cluster_sizes | medium | mitigate | isinstance-list guard before min() prevents a silently-swallowed TypeError from disabling the Goodhart guard (Pitfall 4) |
| T-53A-04 | Elevation of Privilege | _tuning.py imports | high | mitigate | module imports nothing from anthropic/providers; run_method/build_diagnostics imported lazily inside the loop body only |
</threat_model>

<verification>
- Full advisor + schema + tuning suite green: `cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=python python -m pytest tests/test_advisor_tuning.py tests/test_advisor_schema.py -q`
- Grounding/LLM-free at module load: `grep -v '^#' python/fdars/advisor/_tuning.py | grep -c anthropic` returns 0
- No regression in the existing advisor tests: `PYTHONPATH=python python -m pytest tests/test_advisor_gemini_schema.py -q`
</verification>

<success_criteria>
- run_tuning_loop terminates on all five stop reasons (budget, converged, oscillation, parse_failure, guard_stop) offline with mock propose_fns
- Determinism proven: identical inputs give identical traces
- Schema types land with a working pydantic-absent fallback and a backward-compatible optional Recommendation.parameter_delta
- The tracer is committed and green before wave 2 begins
</success_criteria>

<output>
Create `.planning/phases/53-closed-loop-auto-tuning-capstone/53-01-SUMMARY.md` when done
</output>

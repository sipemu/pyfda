---
phase: 31-group-a-fdars-inference-bindings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inference_mod.rs
  - src/lib.rs
  - python/fdars/__init__.py
  - tests/test_inference.py
autonomous: true
requirements: [INFER-01, INFER-02, INFER-03, INFER-09]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "import fdars.inference succeeds AND from fdars.inference import t_perm_test succeeds (submodule registered) — INFER-09"
    - "fdars.inference.t_perm_test(data_a, data_b, argvals, n_perm=999, seed=None) returns a dict with keys statistic, p_value, n_perm — INFER-01"
    - "fdars.inference.f_perm_test returns the same {statistic, p_value, n_perm} dict shape — INFER-02"
    - "fdars.inference.two_sample_mean_test(data_a, data_b, argvals, ncomp=5) returns {statistic, p_value, n_perm} with n_perm==0 — INFER-03"
    - "Two t_perm_test calls with identical seed produce byte-identical json.dumps output; seed=None resolves to fixed default 0 — INFER-09"
    - "Degenerate inputs (mismatched grids, n_perm=0) raise ValueError, not panic — INFER-09"
  artifacts:
    - src/inference_mod.rs
    - tests/test_inference.py
  key_links:
    - "register_submodule!(m, \"inference\", inference_mod::register) in src/lib.rs wires the new module into _native"
    - "\"inference\" appended to _submodule_names in python/fdars/__init__.py enables both import paths"
---

<objective>
Scaffold the NEW `fdars.inference` PyO3 submodule and bind the three permutation/asymptotic two-sample tests: `t_perm_test`, `f_perm_test`, `two_sample_mean_test`. The first task is a verification spike that reads the authoritative fdars-core 0.20.0 source and records confirmed signatures for ALL 8 Group A functions (so plans 02/03 read confirmed facts, not prose). The second task is an end-to-end tracer: `t_perm_test` wired through numpy conversion → fdars-core → PyDict → new submodule registration → importable in Python, proving the whole architecture on one path before expanding.

Purpose: Establish the submodule skeleton (the highest-risk, most-shared work) on a proven end-to-end path, and deliver the seedless/seeded two-sample test trio with byte-identical determinism.
Output: `src/inference_mod.rs` (new), edits to `src/lib.rs` and `python/fdars/__init__.py`, and `tests/test_inference.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-group-a-fdars-inference-bindings/31-CONTEXT.md
@.planning/research/PITFALLS.md
@src/lib.rs
@src/convert.rs
@src/scoring_mod.rs
@src/regression_mod.rs
@python/fdars/__init__.py
</context>

## Artifacts this phase produces

New importable submodule `fdars.inference` exposing 8 functions across plans 31-01/02/03:
`t_perm_test`, `f_perm_test`, `two_sample_mean_test` (this plan);
`mean_scb`, `scb_two_sample_test` (31-02);
`flm_f_test`, `flm_gof_test`, `oneway_anova_vstat` (31-03).

<tasks>

<task type="tracer">
  <name>Task 1: Verification spike — record confirmed 0.20.0 signatures for ALL 8 Group A functions</name>
  <files>.planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md</files>
  <read_first>
    - This plan file (31-01-PLAN.md)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/inference/mod.rs (TestResult fields + pub re-exports + DEFAULT_N_PERM)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/inference/permutation.rs (t_perm_test, f_perm_test signatures + n_perm==0 error)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/inference/hotelling.rs (two_sample_mean_test signature)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/inference/scb.rs (mean_scb, scb_two_sample_test signatures)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/inference/flm.rs (flm_f_test, flm_gof_test take &FregreLmResult)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/inference/anova.rs (oneway_anova_vstat signature + group-label handling)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/tolerance/types.rs (ToleranceBand fields, MultiplierDistribution variants)
    - /home/simonm/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fdars-core-0.20.0/src/scalar_on_function/fregre_lm.rs (fregre_lm signature + FregreLmResult fields)
  </read_first>
  <action>
Read the exact fdars-core 0.20.0 source files above and write a 31-SIGNATURES.md reference recording, verbatim from source: (1) the full Rust signature of each of the 8 functions t_perm_test/f_perm_test/two_sample_mean_test/mean_scb/scb_two_sample_test/flm_f_test/flm_gof_test/oneway_anova_vstat; (2) TestResult fields (statistic:f64, p_value:f64, n_perm:usize) and that it is non_exhaustive; (3) ToleranceBand fields (lower, upper, center, half_width — all Vec<f64>) and that it is non_exhaustive; (4) the MultiplierDistribution variant identifiers (Gaussian, Rademacher) and their canonical lowercase string names "gaussian"/"rademacher", noting the enum lives in fdars_core::tolerance and is non_exhaustive; (5) that flm_f_test and flm_gof_test take &FregreLmResult (confirming the re-fit strategy) and the fregre_lm(data, y, scalar_covariates: Option, ncomp) signature — note fregre_lm takes NO argvals; (6) the oneway_anova_vstat(data, groups: &[usize], argvals) signature, that labels are sort/dedup/position-matched so any distinct usize labels work, that validation requires groups.len()==n and >=2 distinct groups and n>=3, and that 0-indexed is the documented convention; (7) DEFAULT_N_PERM=999 and that both permutation tests return an InvalidParameter error when n_perm==0. This file is a plan-time artifact consumed by the executor of tasks 2-4 and by plans 31-02/31-03 — the confirmed source of truth for every subsequent binding.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md &amp;&amp; grep -q "MultiplierDistribution" .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md &amp;&amp; grep -q "FregreLmResult" .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md</automated>
  </verify>
  <done>31-SIGNATURES.md exists and records confirmed signatures for all 8 functions plus TestResult/ToleranceBand/MultiplierDistribution/FregreLmResult details and the oneway_anova_vstat group-label base.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end tracer — new fdars.inference submodule with t_perm_test wired through every layer</name>
  <files>src/inference_mod.rs, src/lib.rs, python/fdars/__init__.py, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (from Task 1)
    - src/scoring_mod.rs (the v4.0 new-submodule structural precedent: module doc comment, use crate::convert::*, #[pyfunction], pub fn register)
    - src/lib.rs (register_submodule! macro + the mod list + the _native body registration order)
    - python/fdars/__init__.py (_submodule_names tuple + the registration loop)
    - src/convert.rs (numpy2d_to_fdmatrix, numpy1d_to_vec, to_pyresult signatures)
    - src/regression_mod.rs lines 111-132 (fregre_lm dict-return pattern to mirror for PyDict construction)
  </read_first>
  <behavior>
    - import fdars.inference succeeds and from fdars.inference import t_perm_test succeeds
    - t_perm_test on two small growth-subset groups returns a dict whose keys are exactly statistic, p_value, n_perm
    - result["n_perm"] equals the n_perm argument passed (e.g. 19 in tests)
    - two calls with the same explicit seed produce byte-identical json.dumps(result, sort_keys=True)
    - seed=None produces the SAME output as seed=0 (fixed default proves reproducibility)
    - result values are plain Python float/int, not numpy scalars (json.dumps must not raise)
  </behavior>
  <action>
Create src/inference_mod.rs with a module doc comment describing the functional-inference surface, `use crate::convert::*;`, `use numpy::{PyReadonlyArray1, PyReadonlyArray2};`, `use pyo3::prelude::*;`. Add a private helper that maps a fdars_core::inference::TestResult into a fresh PyDict setting statistic (f64), p_value (f64), and n_perm (usize) — access fields individually (the struct is non_exhaustive; never struct-literal it). Bind t_perm_test as a #[pyfunction] with #[pyo3(signature = (data_a, data_b, argvals, n_perm=999, seed=None))] taking data_a/data_b as PyReadonlyArray2, argvals as PyReadonlyArray1, n_perm: usize, seed: Option; inside, convert both matrices via numpy2d_to_fdmatrix, argvals via numpy1d_to_vec, resolve the seed with seed.unwrap_or(0) (fixed default 0 per D locked decision — Python seed=None must be byte-identical across runs), call fdars_core::inference::t_perm_test routed through to_pyresult (NO .unwrap()), then build the PyDict via the helper. Add a NumPy-style docstring documenting params, the returned dict keys, that seed=None is deterministic (default 0), and that ValueError is raised for mismatched grids or n_perm=0. Add `pub fn register(m: &Bound<PyModule>) -> PyResult<()>` calling m.add_function(wrap_pyfunction!(t_perm_test, m)?)?. Wire the submodule: in src/lib.rs add `mod inference_mod;` alongside the other mod lines and add `register_submodule!(m, "inference", inference_mod::register);` inside _native (after the scoring registration). In python/fdars/__init__.py append "inference" to the _submodule_names tuple (after "scoring"). Create tests/test_inference.py with a growth-subset fixture (load_growth(return_fdata=False) → age, X, meta; slice boys = X[:10], girls = X[39:49] for small fast groups on the shared age grid) and tests asserting: both import paths work; t_perm_test returns a dict with exactly the three keys; n_perm round-trips (pass n_perm=19); the seed determinism assertion via json.dumps with sort_keys=True comparing seed=None vs seed=0 and two identical-seed calls; a pytest.raises(ValueError) for mismatched argvals length and for n_perm=0. Build with `maturin develop` before running pytest.
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -3 &amp;&amp; python -c "import fdars.inference; from fdars.inference import t_perm_test; print('ok')" &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3 &amp;&amp; python -m pytest tests/test_inference.py -x -q</automated>
  </verify>
  <done>fdars.inference is importable both ways; t_perm_test returns {statistic, p_value, n_perm}; seed determinism and ValueError tests pass; clippy -D warnings clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind f_perm_test (integrated-F permutation test)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (f_perm_test signature — identical shape to t_perm_test)
    - src/inference_mod.rs (the t_perm_test binding + TestResult→PyDict helper from Task 2 to mirror)
  </read_first>
  <behavior>
    - f_perm_test returns a dict with keys statistic, p_value, n_perm
    - result["n_perm"] equals the passed n_perm argument
    - same-seed calls are byte-identical via json.dumps
    - mismatched grids and n_perm=0 raise ValueError
  </behavior>
  <action>
Add f_perm_test to src/inference_mod.rs mirroring t_perm_test exactly (same #[pyo3(signature = (data_a, data_b, argvals, n_perm=999, seed=None))], same numpy2d_to_fdmatrix conversions, seed.unwrap_or(0), route fdars_core::inference::f_perm_test through to_pyresult, reuse the TestResult→PyDict helper). Document that it uses the integrated-F statistic (more sensitive when group variance also differs) versus the integrated-L2 mean-difference statistic of t_perm_test. Register it in the register() fn. In tests/test_inference.py add a test asserting the three dict keys, n_perm round-trip, and a same-seed determinism assertion for f_perm_test on the growth-subset fixture with a small n_perm.
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -m pytest tests/test_inference.py -x -q -k "f_perm" &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>f_perm_test importable and returns {statistic, p_value, n_perm}; determinism + shape tests pass; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Bind two_sample_mean_test (asymptotic Hotelling T² on FPC basis)</name>
  <files>src/inference_mod.rs, tests/test_inference.py</files>
  <read_first>
    - .planning/phases/31-group-a-fdars-inference-bindings/31-SIGNATURES.md (two_sample_mean_test signature — seedless, takes ncomp)
    - src/inference_mod.rs (TestResult→PyDict helper + binding pattern)
  </read_first>
  <behavior>
    - two_sample_mean_test returns a dict with keys statistic, p_value, n_perm
    - result["n_perm"] equals 0 (asymptotic, seedless — no permutations)
    - NO seed parameter is exposed (asymptotic test)
    - ncomp defaults to 5 per the locked decision
    - degenerate ncomp or mismatched grids raise ValueError
  </behavior>
  <action>
Add two_sample_mean_test to src/inference_mod.rs with #[pyo3(signature = (data_a, data_b, argvals, ncomp=5))] — no seed parameter (this is an asymptotic chi-square test). Convert both matrices via numpy2d_to_fdmatrix, argvals via numpy1d_to_vec, route fdars_core::inference::two_sample_mean_test through to_pyresult, build the PyDict via the shared helper. Document that n_perm is always 0 (asymptotic), that ncomp is the number of shared FPC components (default 5, keep small relative to min(n_a, n_b)), and that ValueError is raised for mismatched grids or degenerate ncomp. Register it. In tests/test_inference.py add a test asserting the three keys, that result["n_perm"] == 0, and a pytest.raises(ValueError) for an obviously-degenerate ncomp (e.g. ncomp larger than min group size) on the growth-subset fixture.
  </action>
  <verify>
    <automated>maturin develop 2>&amp;1 | tail -2 &amp;&amp; python -m pytest tests/test_inference.py -x -q &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -2</automated>
  </verify>
  <done>two_sample_mean_test importable, returns {statistic, p_value, n_perm} with n_perm==0; ValueError test passes; full tests/test_inference.py green; clippy -D warnings clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 binding | User-supplied numpy arrays (shapes, dtypes, params) cross into Rust; must be validated, not trusted |
| PyO3 binding → fdars-core | Converted FdMatrix/Vec passed to upstream; upstream returns Result that must not be unwrapped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Denial of Service | t_perm_test/f_perm_test with n_perm=0 or mismatched grids | low | mitigate | Route through to_pyresult so fdars-core InvalidParameter/InvalidDimension surface as ValueError; no .unwrap() anywhere; pytest.raises(ValueError) tests |
| T-31-02 | Tampering | Non-deterministic seed default breaking reproducibility | medium | mitigate | seed=None resolves to fixed default 0; byte-identical json.dumps determinism test guards against drift |
| T-31-03 | Information Disclosure | numpy scalar leakage into result dict breaking json.dumps determinism | low | accept | PyO3 f64→float conversion is native; determinism test asserts json.dumps does not raise |
</threat_model>

<verification>
- `maturin develop` builds clean (rustfmt + clippy -D warnings enforced in CI).
- `python -c "import fdars.inference; from fdars.inference import t_perm_test, f_perm_test, two_sample_mean_test"` succeeds.
- `python -m pytest tests/test_inference.py -q` exits 0.
- Determinism: two identical-seed calls and seed=None vs seed=0 yield byte-identical json.dumps.
</verification>

<success_criteria>
- `fdars.inference` submodule registered and importable both as `fdars.inference.fn` and `from fdars.inference import fn`.
- `t_perm_test`, `f_perm_test`, `two_sample_mean_test` each return `{statistic, p_value, n_perm}`.
- Permutation tests are seed-deterministic (seed=None → fixed default 0); two_sample_mean_test has n_perm==0 and no seed param.
- Degenerate inputs raise ValueError (no `.unwrap()`); clippy `-D warnings` clean.
</success_criteria>

<output>
Create `.planning/phases/31-group-a-fdars-inference-bindings/31-01-SUMMARY.md` when done
</output>

---
phase: 25-crate-bump-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - tests/test_basic.py
  - tests/test_r_parity.py
  - tests/test_advisor.py
  - tests/test_aspect_provider_matrix.py
autonomous: false
requirements: [DEP-01, DEP-02]
user_setup:
  - service: rust-toolchain-and-venv
    why: "The bump can only be proven by building the native extension and running the live suite; a Rust 1.83+ toolchain plus a venv with maturin + dev/advisor extras installed must exist locally, else the regression gate cannot run and Task 3 becomes a human-verify checkpoint."
    env_vars: []

estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cargo.toml pins fdars-core = 0.17.0 with features = [\"parallel\"] and NOT linalg (DEP-01)"
    - "Cargo.lock resolves fdars-core to 0.17.0 and is committed (DEP-01)"
    - "maturin develop builds the _native extension green against 0.17.0 (DEP-01)"
    - "The full Rust build-gate (cargo test) and Python suite (pytest, ~259 tests) pass against 0.17.0 (DEP-02)"
    - "Only FPCA/SVD-derived exact-equality assertions that actually broke are relaxed, minimally, to absorb the 1e-8·σ₁ faer drift (DEP-02)"
    - "No existing binding signature or public behavior changed — confirmed against the live green suite, not assumed (DEP-02)"
  artifacts:
    - Cargo.toml
    - Cargo.lock
    - "tests/* files that carry a relaxed FPCA tolerance (discovered at execute time)"
  key_links:
    - "Cargo.toml version string -> Cargo.lock resolution -> maturin-built _native -> live pytest suite"
    - "faer FPCA SVD path (0.15+) -> FPCA/SPM/tolerance/advisor-fpca assertions -> relaxed atol"
---

<objective>
Upgrade the pinned `fdars-core` from 0.14.0 to 0.17.0, regenerate and commit `Cargo.lock`, rebuild the `_native` extension via maturin, and prove the entire existing Rust + Python binding + advisor suite green on the new engine — relaxing ONLY the FPCA/SVD-derived exact-equality assertions that actually break under the faer SVD drift (`1e-8·σ₁`). This isolates the sole numeric behavior change before any new binding work (Phases 26–29) begins.

Purpose: Establish a known-green 0.17.0 baseline and empirically pin the faer FPCA drift magnitude, so all downstream binding phases build on proven ground rather than on faith that 0.15→0.17 is non-breaking.
Output: Modified `Cargo.toml` (single version-string change), regenerated `Cargo.lock`, and minimally-scoped tolerance edits in whichever test files actually break. No new symbols, no new bindings, no public API change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/codebase/TESTING.md
@Cargo.toml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Apply the bump end-to-end — edit pin, regenerate lock, build, and run the FULL suite to DISCOVER what breaks</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (line 18 is the single pin to edit: `fdars-core = { version = "0.14.0", features = ["parallel"] }`)
    - .planning/research/STACK.md §1 (exact line, caret semantics: `^0.14.0` locks to <0.15 so the explicit bump is mandatory; keep `parallel`; do NOT add `linalg` — needs Rust 1.84 > MSRV 1.83)
    - .planning/research/PITFALLS.md Pitfall 7 (faer FPCA SVD drift, equivalent within 1e-8·σ₁) and Pitfall 8 (caret pin not covering 0.17.0)
    - .planning/codebase/TESTING.md (suite runs via `pytest tests/`; assertions via numpy.testing; three FPCA-touching areas: test_basic FPCA/SPM/tolerance, test_r_parity FPCA/SPM, test_advisor byte-identical FPCA diagnostics)
  </read_first>
  <action>
This is the tracer: one path through every layer the phase touches — pin edit, lock regeneration, native build, full suite — wired end-to-end, with the live suite as the runnable verify. Do NOT pre-guess which tests break; discover them empirically and record the exact failures, because the observed drift magnitude drives Task 2's tolerance edits.

Steps: (1) In `Cargo.toml`, change the fdars-core version string from `"0.14.0"` to `"0.17.0"` on the pin line, keeping `features = ["parallel"]` exactly as-is and adding no other feature. Use the exact string `"0.17.0"` (the milestone's locked reproducibility precedent per 25-CONTEXT.md — an exact pin, not the caret variant the pitfall discusses). (2) Regenerate the lock: `cargo update -p fdars-core --precise 0.17.0`. (3) Confirm resolution: `cargo tree | grep fdars-core` must show 0.17.0 and `grep -A1 'name = "fdars-core"' Cargo.lock` must show `version = "0.17.0"`. (4) Build the extension into the active venv: `maturin develop` (must exit 0). (5) Run the full suite and CAPTURE every failure verbatim into the SUMMARY: `cargo test` (a build/compile gate — src/ carries zero `#[test]`, so this proves the wrapper crate still compiles and links against 0.17.0), then `pytest tests/ -q` (the real regression gate, ~259 tests). For each pytest failure, record the file::test, the asserted vs actual values, and the numeric delta — this is the empirical drift evidence Task 2 consumes.

If `maturin develop` or the toolchain/venv is unavailable in the execution environment, STOP and surface it as a human-validation checkpoint (see Task 3) — do NOT mark the gate passed on an unrun suite.

Confirm the additive/non-breaking claim concretely: no failure may be a signature/attribute/import error or a changed return shape. If any failure is NOT a pure FPCA/SVD numeric-tolerance drift, treat it as a real regression and halt for investigation rather than relaxing it away.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q 'fdars-core = { version = "0.17.0"' Cargo.toml && grep -A1 'name = "fdars-core"' Cargo.lock | grep -q 'version = "0.17.0"' && grep -vE '^\s*#' Cargo.toml | grep -c 'linalg' | grep -qx 0 && echo PIN_OK</automated>
  </verify>
  <reversibility rating="reversible">The bump is undone by reverting the Cargo.toml version string and Cargo.lock — no data migration, no one-way door.</reversibility>
  <acceptance_criteria>
    - `grep 'fdars-core' Cargo.toml` shows `version = "0.17.0"` with `features = ["parallel"]`.
    - `grep -c 'linalg' Cargo.toml` returns 0 (linalg feature NOT enabled).
    - `cargo tree | grep fdars-core` shows `0.17.0`; `Cargo.lock` contains `version = "0.17.0"` under `name = "fdars-core"`.
    - `maturin develop` exits 0.
    - `cargo test` exits 0 (compile/link gate).
    - `pytest tests/ -q` has been run and every failure captured verbatim (file::test, expected vs actual, delta) in the SUMMARY; every captured failure is a pure FPCA/SVD numeric drift and none is a signature/import/shape error.
  </acceptance_criteria>
  <done>The pin is 0.17.0 (parallel only, no linalg), the lock is regenerated to 0.17.0, the extension builds, and the full suite has been run once with all failures empirically recorded and classified as FPCA drift vs real regression.</done>
</task>

<task type="auto">
  <name>Task 2: Relax ONLY the broken FPCA/SVD exact-equality assertions to absorb the 1e-8·σ₁ drift, then re-run the full suite green</name>
  <files>tests/test_basic.py, tests/test_r_parity.py, tests/test_advisor.py, tests/test_aspect_provider_matrix.py</files>
  <read_first>
    - The Task 1 SUMMARY failure list (the authoritative set of assertions to touch — edit nothing that did not actually break)
    - tests/test_basic.py (FPCA surfaces: `test_fpca` ~L121, `TestSPM::test_spm_phase1` ~L185, `test_fpca_tolerance_band` ~L199)
    - tests/test_r_parity.py (`test_fpca_variant` ~L252 and the SPM tests ~L191-220 that route through the FPCA/SVD path)
    - tests/test_advisor.py (`test_fpca_output_unchanged_after_refactor` ~L282 — asserts byte-identical FPCA diagnostics; a prime break candidate whose fixed FPCA fixture values may need regenerating from the 0.17.0 build)
    - tests/test_aspect_provider_matrix.py (`_FPCA_RESULT` fixture ~L153, only if a hard-coded FPCA value participates in an equality check that broke)
    - .planning/research/PITFALLS.md Pitfall 7 (target tolerance ~atol=1e-6, rtol=1e-5; scoped, not blanket)
  </read_first>
  <action>
For each assertion Task 1 recorded as broken, apply the minimal change that absorbs the documented faer drift and nothing more. Preferred edits, in order: (a) an exact-equality check (`assert_array_equal` / `atol` tighter than the drift) on an FPCA/SVD-derived value becomes `np.testing.assert_allclose(..., atol=1e-6, rtol=1e-5)`; (b) a byte-identical FPCA diagnostics fixture (test_advisor) whose expected numbers shifted gets its expected values regenerated from the current 0.17.0 build and, where the test compares floats, its comparison loosened to the same tolerance rather than left as an exact dict-equality on floats. Scope every edit to the FPCA/SVD-derived quantity only — do NOT loosen unrelated Simpson-integration, centering, or distance-matrix tolerances that already pass, and do NOT touch any assertion that Task 1 did not list as failing.

Annotate each relaxed assertion with a short rationale note tying it to the faer SVD path and the 1e-8·σ₁ equivalence, so a future reader knows the loosening is deliberate and bounded, not incidental. Then re-run `pytest tests/ -q` and `cargo test` and confirm both exit 0 (pytest: 0 failed; skips are acceptable and unchanged from the pre-bump baseline count).

If Task 1 recorded ZERO failures, make no test edits — record in the SUMMARY that the faer drift did not surface at the suite's existing tolerances (DEP-02 satisfied with no relaxation needed).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && pytest tests/ -q 2>&1 | tail -3 | grep -qE '([0-9]+ passed|no tests ran)' && ! pytest tests/ -q 2>&1 | tail -3 | grep -qE '[0-9]+ failed' && echo SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0 with 0 failed (skips unchanged from the pre-bump baseline).
    - `cargo test` still exits 0.
    - Every tolerance edit maps 1:1 to a failure recorded in Task 1; no assertion that already passed was loosened (diff review confirms only FPCA/SVD-derived assertions changed).
    - Each relaxed assertion carries a rationale note referencing the faer SVD path / 1e-8·σ₁ equivalence.
    - If zero failures occurred in Task 1, the diff touches no test file and the SUMMARY states no relaxation was needed.
  </acceptance_criteria>
  <done>The full suite is green on 0.17.0 with FPCA tolerances relaxed minimally and only where the drift actually broke an assertion; no unrelated test was loosened.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the regression gate actually ran green on 0.17.0</name>
  <what-built>The crate bump to fdars-core 0.17.0 (Cargo.toml + regenerated Cargo.lock), a green `maturin develop` build of the `_native` extension, and a full Rust-compile + Python (~259-test) suite pass with minimally-scoped FPCA tolerance relaxations.</what-built>
  <how-to-verify>
Confirm the environment actually ran the gate (not skipped):
1. `cd /home/simonm/projects/rust/pyfda && cargo tree | grep fdars-core` → shows 0.17.0.
2. `maturin develop` → exits 0 (extension rebuilt into your venv).
3. `pytest tests/ -q` → 0 failed (N passed / M skipped; M matches the pre-bump skip count).
4. `git diff --stat Cargo.toml Cargo.lock tests/` → the only test changes are FPCA/SVD tolerance relaxations, each annotated; Cargo.toml is a one-line version change; no `linalg` anywhere.
Review the SUMMARY's captured-failures list and confirm every relaxation traces to a real faer-drift failure and that no failure was a signature/import/shape regression (i.e., the 0.15→0.17 additive/non-breaking claim held on the live suite).

If your environment lacks a Rust toolchain or a maturin-enabled venv, say so — this is the human-validation gate: the phase must NOT be marked green on an unrun suite.
  </how-to-verify>
  <resume-signal>Type "approved" once the suite is confirmed green on 0.17.0 with only scoped FPCA relaxations, or describe the failure/regression to investigate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream crate → build | fdars-core 0.17.0 source/checksum pulled from crates.io into Cargo.lock and compiled into the shipped `_native` extension |
| Rust `_native` → Python | the maturin-built extension is imported and exercised by the live pytest suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | fdars-core 0.17.0 crate pulled into Cargo.lock | medium | mitigate | Cargo verifies the registry checksum on `cargo update`/build; the regenerated `Cargo.lock` records and pins the 0.17.0 checksum, and is committed so CI resolves the identical artifact. fdars-core is the project's own first-party upstream (sipemu/fdars). |
| T-25-02 | Tampering | new/changed transitive dependencies via the bump | low | accept | Research (STACK.md §2/§3) confirms 0.15→0.17 adds no new dependencies with `parallel`-only features; `linalg` (which would add faer/anofox) is explicitly NOT enabled. Any unexpected new lock entry surfaces in the committed `Cargo.lock` diff at review. |
| T-25-03 | Denial of Service | faer FPCA SVD numeric drift silently loosening test rigor | medium | mitigate | Tolerance relaxations are scoped 1:1 to empirically-observed FPCA failures at ~atol=1e-6 (bounded by the documented 1e-8·σ₁ equivalence), each annotated; no blanket loosening. Non-FPCA failures are treated as real regressions and halt, not relaxed. |

_No npm/pip/cargo package-INSTALL tasks are introduced this phase (no new dependencies), so the package-legitimacy gate does not apply; the single crate-version bump is covered by T-25-01/02 above._
</threat_model>

<verification>
- `grep 'fdars-core' Cargo.toml` → `version = "0.17.0"`, `features = ["parallel"]`; `grep -c 'linalg' Cargo.toml` → 0.
- `cargo tree | grep fdars-core` → 0.17.0; `grep -A1 'name = "fdars-core"' Cargo.lock` → `version = "0.17.0"`.
- `maturin develop` exits 0; `cargo test` exits 0.
- `pytest tests/ -q` → 0 failed (skips unchanged from baseline).
- `git diff` shows only: one-line Cargo.toml version change, regenerated Cargo.lock, and annotated FPCA/SVD tolerance relaxations that each trace to a Task 1 failure.
- No signature/import/shape regression anywhere in the suite (additive/non-breaking 0.15→0.17 confirmed live).
</verification>

<success_criteria>
- DEP-01: `fdars-core` pinned to 0.17.0 with `parallel` retained and `linalg` NOT enabled; `Cargo.lock` regenerated to 0.17.0 and committed; `maturin develop` builds green.
- DEP-02: full existing Rust-compile + Python (~259-test) suite passes against 0.17.0, with FPCA-related tolerances relaxed only where the faer 1e-8·σ₁ drift actually broke an exact-equality assertion — minimal and scoped.
- No existing binding signature or public behavior changed, confirmed against the live green suite.
</success_criteria>

<output>
## Artifacts this phase produces

No new public symbols, bindings, or API surface — this is a pure infrastructure/upgrade phase. Modified files only:

- `Cargo.toml` — single version-string change `0.14.0 → 0.17.0` (parallel retained, linalg NOT added).
- `Cargo.lock` — regenerated; `fdars-core` resolves to 0.17.0.
- FPCA/SVD tolerance relaxations in whichever of `tests/test_basic.py`, `tests/test_r_parity.py`, `tests/test_advisor.py`, `tests/test_aspect_provider_matrix.py` actually break (discovered at execute time; may be an empty set if the drift stays under existing tolerances).

Create `.planning/phases/25-crate-bump-regression-gate/25-01-SUMMARY.md` when done, including the captured-failures list (file::test, expected vs actual, delta) and the exact set of tolerance edits applied.
</output>

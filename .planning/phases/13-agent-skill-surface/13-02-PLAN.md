---
phase: 13-agent-skill-surface
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py
  - .claude/skills/fdars-advisor/SKILL.md
  - tests/test_skill.py
autonomous: true
requirements: [SKILL-01, SKILL-02]
user_setup:
  - service: anthropic
    why: "Optional — enables the grounded advise() step of the walkthrough (Success Criterion 3, LLM half). The offline delta runs without it."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (only needed to exercise the advise() branch; script exits 0 without it)"

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "With ANTHROPIC_API_KEY set, the walkthrough calls advise(diagnostics, task='parameter', ...) and prints interpretation + recommendations whose evidence cites diagnostics values; without it, the script prints an [offline] notice and still exits 0 (SKILL-01, Success Criterion 3)"
    - "SKILL.md `compatibility` field documents Python >=3.10 and the install requirement; the body documents the git-URL install and the grounded-advice run (SKILL-02)"
    - "The walkthrough script exits 0 on Python 3.9 with an informative (non-error) message via its version guard (SKILL-01)"
  artifacts:
    - "fdars_advisor_walkthrough.py — extended with the env-gated advise() step and diagnostics printing"
    - "SKILL.md — full body: Setup / Offline Walkthrough / Grounded Advice / Grounding Invariant"
    - "tests/test_skill.py — extended with test_skill_md_name_matches_dir, test_skill_md_compatibility, test_walkthrough_py39_exit0"
  key_links:
    - "advise() consumes the diagnostics dict from build_diagnostics(before_result, 'smoothing') — the grounding link (fdars computes numbers; advise cites them)"
    - "test_skill_md_name_matches_dir asserts frontmatter name == parent directory basename — the agentskills.io discovery invariant"
    - "test_walkthrough_py39_exit0 proves the version guard by simulating sys.version_info < (3,10) — the 3.9 graceful-exit contract"
---

<objective>
Expand the proven tracer (Plan 01) into the full skill surface: add the env-gated
`advise()` grounded-advice step to the walkthrough (fdars computes the diagnostics,
the LLM interprets and cites them), finish the SKILL.md body with the Grounded Advice
section, and add the front-matter / Python-3.9 edge tests that lock in SKILL-02 and the
graceful-degradation contract.

Purpose: complete Success Criterion 3 (grounded advice + before/after comparison) and
close the SKILL-02 documentation/compatibility requirements with automated proof.
Output: extended walkthrough script, complete SKILL.md, and three new passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-skill-surface/13-CONTEXT.md
@.planning/phases/13-agent-skill-surface/13-RESEARCH.md
@.planning/phases/13-agent-skill-surface/13-PATTERNS.md
@.planning/phases/13-agent-skill-surface/13-VALIDATION.md
@.planning/phases/13-agent-skill-surface/13-01-SUMMARY.md
@python/fdars/advisor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add env-gated advise() grounded-advice step to the walkthrough</name>
  <files>.claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py</files>
  <read_first>
    - .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py (the Plan 01 offline script being extended)
    - python/fdars/advisor.py (lines 81-87 Recommendation schema with non-empty `evidence`; lines 188-195 build_diagnostics signature; lines 940-946 advise signature; lines 984-998 how diagnostics is passed as user content)
    - .planning/phases/13-agent-skill-surface/13-RESEARCH.md (Pattern 4 grounding invariant; Pattern 5 offline/CI env-gate; Code Examples advise() call with task='parameter' + domain_context)
    - .planning/phases/13-agent-skill-surface/13-PATTERNS.md (ANTHROPIC_API_KEY Environment Gate; the build_diagnostics/advise addition block)
  </read_first>
  <behavior>
    - After computing `before_result`, the script calls `diagnostics = build_diagnostics(before_result, "smoothing")` (offline, deterministic).
    - If `os.environ.get("ANTHROPIC_API_KEY")` is truthy: calls `advise(diagnostics, task="parameter", domain_context="35 Canadian weather stations, daily temperature curves")` and prints `advice.interpretation`, each recommendation (action + kind + rationale + expected_effect), and each recommendation's `evidence` items so the grounding is visible side-by-side with the diagnostics.
    - If the key is absent: prints an `[offline]` notice and skips advise() — the script still exits 0.
    - The offline delta step (Plan 01) continues to run unconditionally after the advise() branch.
  </behavior>
  <action>Extend `main()` in the walkthrough: add `from fdars.advisor import build_diagnostics, advise` to the import block (below the fdars.mcp imports; both are 3.10+-guarded already). After `before_result` is obtained, compute `diagnostics = build_diagnostics(before_result, "smoothing")`. Insert an env-gated block: `if os.environ.get("ANTHROPIC_API_KEY"):` -> call `advise(diagnostics, task="parameter", domain_context="35 Canadian weather stations, daily temperature curves")`, then print `advice.interpretation`, iterate `advice.recommendations` printing action/kind/rationale/expected_effect and the `evidence` list per recommendation, and print `advice.caveats`; `else:` -> print `"[offline] ANTHROPIC_API_KEY not set — skipping advise()"`. Keep the existing compare_run/delta block AFTER this so the deterministic delta always prints regardless of key presence. Do NOT read the API key from any file and do NOT print the key value (T-13-02). Add a separator comment for the new step. Preserve the exact Delta header literal from Plan 01 so the delta smoke test still passes.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && env -u ANTHROPIC_API_KEY .venv/bin/python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py | grep -F "[offline]" && .venv/bin/python -m pytest tests/test_skill.py::test_walkthrough_script_offline tests/test_skill.py::test_walkthrough_delta_nonempty -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Offline run (no key) prints an `[offline]` notice, still prints the delta, and exits 0
    - The script calls `build_diagnostics(before_result, "smoothing")` before the advise branch
    - The advise() call uses `task="parameter"` and a `domain_context` string
    - No file-based key read and no key value printed anywhere in the script
    - test_walkthrough_script_offline and test_walkthrough_delta_nonempty still pass
  </acceptance_criteria>
  <done>The walkthrough runs the full interpret->recommend->compare loop: offline it prints diagnostics-grounded delta + an [offline] notice; with a key it prints grounded advice citing diagnostics values.</done>
</task>

<task type="auto">
  <name>Task 2: Complete SKILL.md body — Grounded Advice + Grounding Invariant</name>
  <files>.claude/skills/fdars-advisor/SKILL.md</files>
  <read_first>
    - .claude/skills/fdars-advisor/SKILL.md (the Plan 01 first-cut being completed)
    - .planning/phases/13-agent-skill-surface/13-RESEARCH.md (SKILL.md Body minimal form — Grounded Advice section with the ANTHROPIC_API_KEY run command; Pitfall 2 body under 500 lines; Grounding Invariant wording)
    - .planning/phases/13-agent-skill-surface/13-PATTERNS.md (body structure Setup / Offline Walkthrough / Grounded Advice / Grounding Invariant)
  </read_first>
  <behavior>
    - Body includes a `## Grounded Advice` section documenting the `ANTHROPIC_API_KEY=... python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py` run and the expected output (interpretation + recommendations with cited evidence + before/after delta).
    - Body includes a `## Grounding Invariant` section stating every recommendation cites an fdars-computed diagnostics value and the LLM never fabricates numbers.
    - Body references the Phase 12 tools (fdars_run_method, fdars_compare_run) and the advisor (build_diagnostics, advise) by name.
    - Body stays well under 500 lines / 5000 tokens (Pitfall 2).
  </behavior>
  <action>Complete the SKILL.md body authored in Plan 01. Add a `## Grounded Advice` section giving the API-key run command and describing the expected grounded output (interpretation, recommendations each with cited `evidence`, then the before/after delta block). Add a `## Grounding Invariant` section stating the invariant plainly (fdars computes every number; advise() cites diagnostics; no fabrication). Ensure the interpret->recommend->re-run->compare loop is described as an ordered set of steps mapping to the script, and that fdars_run_method / fdars_compare_run / build_diagnostics / advise are named (Success Criterion 1). Do not modify the frontmatter beyond what Plan 01 established unless a field is missing. Keep it concise — detail is in the script, not the body (progressive disclosure).</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -c "import pathlib; t=pathlib.Path('.claude/skills/fdars-advisor/SKILL.md').read_text(); assert '## Grounded Advice' in t; assert '## Grounding Invariant' in t; assert 'fdars_compare_run' in t; assert 'ANTHROPIC_API_KEY' in t; assert t.count(chr(10)) < 500; print('OK body sections + refs present')"</automated>
  </verify>
  <acceptance_criteria>
    - Body contains `## Grounded Advice` and `## Grounding Invariant` sections
    - Body names `fdars_compare_run` (Phase 12 tool reference) and mentions ANTHROPIC_API_KEY for the advice run
    - Body is under 500 lines
    - Frontmatter still parses with name == fdars-advisor (unchanged from Plan 01)
  </acceptance_criteria>
  <done>SKILL.md is complete: spec-valid frontmatter plus a full Setup / Offline Walkthrough / Grounded Advice / Grounding Invariant body referencing the Phase 12 tools and Phase 11 advisor.</done>
</task>

<task type="auto">
  <name>Task 3: Edge tests — name==dir, compatibility, Python-3.9 exit-0</name>
  <files>tests/test_skill.py</files>
  <read_first>
    - tests/test_skill.py (the Plan 01 module being extended; the `_parse_frontmatter` helper, SKILL_MD/SCRIPT_PATH constants)
    - .planning/phases/13-agent-skill-surface/13-VALIDATION.md (Per-Task Verification Map — test_skill_md_name_matches_dir 13-01-02, test_skill_md_compatibility 13-01-05, test_walkthrough_py39_exit0 13-01-06)
    - .planning/phases/13-agent-skill-surface/13-RESEARCH.md (Pitfall 4 — the version guard must precede any fdars.mcp import; Pitfall 5 — name matches dir)
    - examples/mcp_recipe.py (lines 37-44 — the version-guard block being asserted against)
  </read_first>
  <behavior>
    - test_skill_md_name_matches_dir: parsed frontmatter `name` equals `SKILL_MD.parent.name` (i.e. `fdars-advisor`).
    - test_skill_md_compatibility: the `compatibility` frontmatter field is a non-empty string containing `3.10` (Python version) and evidence of the install/pip requirement.
    - test_walkthrough_py39_exit0: proves the script's version guard yields a graceful exit-0 (not an error) when Python is <3.10, WITHOUT requiring a real 3.9 interpreter — by asserting the guard construct exists and precedes the first `fdars.mcp` import, or by executing the guard logic in isolation.
  </behavior>
  <action>Extend tests/test_skill.py with three functions. test_skill_md_name_matches_dir: `fm = _parse_frontmatter(SKILL_MD); assert fm["name"] == SKILL_MD.parent.name`. test_skill_md_compatibility: assert `fm.get("compatibility")` is a non-empty string and that `"3.10"` is in it and a pip/install token (e.g. `"pip"` or `"install"`) is in it. test_walkthrough_py39_exit0: since the CI interpreter is 3.10+ and cannot re-run under 3.9, prove the guard structurally — read `SCRIPT_PATH.read_text()`, assert the substring `if sys.version_info < (3, 10)` is present AND its character index is LESS THAN the index of the first `from fdars.mcp` import (guaranteeing the guard precedes the guarded import per Pitfall 4), AND assert `sys.exit(0)` appears within the guard region. Keep the module-level `pytestmark` skip so the whole module still skips on real 3.9 runners. Do not weaken any Plan 01 test.</action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_skill.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_skill_md_name_matches_dir, test_skill_md_compatibility, test_walkthrough_py39_exit0 all pass
    - test_skill_md_compatibility asserts `3.10` and an install/pip token appear in the compatibility field
    - test_walkthrough_py39_exit0 asserts the version-guard index precedes the first `from fdars.mcp` import index
    - Full `pytest tests/test_skill.py -x -q` is green (all six+ tests pass or skip cleanly)
  </acceptance_criteria>
  <done>All three edge tests pass and the full test_skill.py suite is green, locking SKILL-02 documentation and the 3.9 graceful-exit contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> advise() | `ANTHROPIC_API_KEY` gates the LLM call; read only from `os.environ`, never a file |
| advise() -> Anthropic API | diagnostics dict (fdars-computed) is sent to Claude; the response is schema-validated `Advice` with cited evidence (Phase 11 grounding) |
| skill runtime -> package index | runtime `pip install` (documented in SKILL.md) fetches fdars/mcp/anthropic/pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Information Disclosure | ANTHROPIC_API_KEY in script | low | mitigate | Key read only via `os.environ.get`; never hardcoded, never printed, never written to a file (Task 1 acceptance criteria assert this). |
| T-13-04 | Spoofing/Repudiation | LLM fabricating numbers | medium | mitigate | Grounding invariant enforced upstream by the `Advice` Pydantic schema (non-empty `evidence`) + system prompt (Phase 11); the walkthrough prints diagnostics alongside advice so fabrication is visible. No new mitigation code here — reuse the Phase 11 contract. |
| T-13-01 | Tampering | git-URL pip install (documented) | medium | accept | Only fixed known package names in SKILL.md; no user-parameterised install. Same disposition as Plan 01. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_skill.py -x -q` — full suite green (six tests: 3 from Plan 01 + 3 here), or skips cleanly on Python <3.10.
- `env -u ANTHROPIC_API_KEY .venv/bin/python .claude/skills/fdars-advisor/scripts/fdars_advisor_walkthrough.py` prints `[offline]`, prints the delta, exits 0.
- SKILL.md body contains the Grounded Advice + Grounding Invariant sections and references fdars_compare_run.
- Manual (end-of-phase): with `ANTHROPIC_API_KEY` set, run the script and confirm the printed interpretation/recommendations cite diagnostics values and the delta block is present (Success Criterion 3, LLM half — see 13-VALIDATION.md Manual-Only Verifications).
</verification>

<success_criteria>
- The full interpret->recommend->re-run->compare loop is packaged and runnable (SKILL-01, Success Criterion 1).
- The execution environment is documented and the skill runs end-to-end (SKILL-02, Success Criterion 2).
- A dry-run walkthrough produces grounded advice + a before/after comparison against Canadian Weather (Success Criterion 3).
</success_criteria>

<output>
Create `.planning/phases/13-agent-skill-surface/13-02-SUMMARY.md` when done.
</output>

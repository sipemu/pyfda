---
phase: 03-learn-diagrams
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/phases/03-learn-diagrams/COVERAGE.md
autonomous: false
requirements: [DIA-01]
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "All 6 learn/ diagrams (introduction, custom-plotting, simulation, smoothing, derivatives, irregular-sampling) pass the SVGO idempotence gate with exit 0"
    - "All 6 learn/ diagrams carry viewBox 0 0 720, the five CSS classes, system-ui, role=img, and aria-label — proving no legacy-outlier remains in learn/"
    - "The 5 accurate learn/ diagrams render on the built site via mkdocs build with no errors"
    - "COVERAGE.md declares no external API integration for this phase"
  artifacts:
    - .planning/phases/03-learn-diagrams/COVERAGE.md
  key_links:
    - "Each learn/ page (introduction.md, custom-plotting.md, simulation.md, derivatives.md, irregular-sampling.md) embeds its diagram from ../assets/diagrams/*.svg — the built-site render depends on these embeds resolving"
  flagged_assumptions:
    - "adjacency edge — N/A: static hand-authored SVG assets, no runtime input adjacency to resolve."
    - "empty edge — N/A: no runtime input; diagrams are fixed authored assets."
    - "ordering edge — N/A: SVG element order is authored and deterministic; the SVGO gate is idempotent so serialisation is stable."
---

<objective>
Expand the proven verification loop (from Plan 01) across the remaining 5 learn/ diagrams that the Phase 2 audit rated accurate+conforming, and produce the section-wide evidence that closes DIA-01.

The audit rated introduction.svg, custom-plotting.svg, simulation.svg, derivatives.svg, and irregular-sampling.svg as accurate on both axes. This plan does NOT redraw them — it re-verifies each against the live gate (audit is evidence, not a substitute for running the gate): confirms each passes the SVGO idempotence gate, confirms each carries the STYLE_SPEC markers (proving Success Criterion #4 — no legacy-outlier learn/ diagram remains), confirms all render on the built site, and authors the COVERAGE.md api-coverage declaration required at seal time.

Purpose: Deliver Success Criteria #1, #3, and #4 for the learn/ section and satisfy the api-coverage gate.
Output: `.planning/phases/03-learn-diagrams/COVERAGE.md` and a verification log recorded in the SUMMARY.
</objective>

<user_story>
**As a** reader of the fdars learn/ docs, **I want to** every learn/ page's concept diagram to be style-consistent and render correctly on the site, **so that** the section reads as one coherent, method-accurate whole.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-audit/02-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
@svgo.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SVGO idempotence gate on all 6 learn/ diagrams (DIA-01, SC#1)</name>
  <files>.planning/phases/03-learn-diagrams/COVERAGE.md</files>
  <read_first>
    - svgo.config.mjs (check-only config; idempotence pattern in header)
    - docs/assets/diagrams/STYLE_SPEC.md (pinned svgo@3.3.4, mandatory --config)
    - docs/assets/diagrams/introduction.svg (spot-read to confirm it is present and well-formed before gating)
  </read_first>
  <action>
    For each of the 6 learn/ diagrams — introduction.svg, custom-plotting.svg, simulation.svg, smoothing.svg (already fixed in Plan 01), derivatives.svg, irregular-sampling.svg — run the pinned SVGO idempotence gate (pass 1 to stdout, feed back for pass 2, diff must be empty). Record pass/fail per file in the SUMMARY. If any file fails idempotence, stop and report — do NOT rewrite it (the gate is check-only). All 6 are expected to pass (the audit and Phase 1 full-corpus result both confirmed conformance); this task PROVES it against the live files rather than asserting it.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; for f in introduction custom-plotting simulation smoothing derivatives irregular-sampling; do FIRST=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "docs/assets/diagrams/$f.svg" --output -); SECOND=$(printf '%s' "$FIRST" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); [ "$FIRST" = "$SECOND" ] || { echo "FAIL: $f"; ok=0; }; done; [ "$ok" = 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - All 6 learn/ diagrams pass the SVGO idempotence gate (pass 1 == pass 2), exit 0.
    - No committed SVG is modified by this task (gate is stdout-only). Check: `git diff --stat docs/assets/diagrams/` shows no changes from this task.
  </acceptance_criteria>
  <done>All 6 learn/ diagrams are proven idempotent under svgo.config.mjs; per-file results recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Prove no legacy-outlier remains in learn/ and the section builds (DIA-01, SC#3, SC#4)</name>
  <files>.planning/phases/03-learn-diagrams/COVERAGE.md</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (style markers: viewBox 0 0 720, five CSS classes .ttl/.sub/.lab/.sm/.mono, system-ui, role=img, aria-label)
    - .planning/phases/02-audit/02-AUDIT.md (learn/ section table — the audit claims all 6 conform; this task verifies it)
  </read_first>
  <action>
    Run the style-marker grep across all 6 learn/ diagrams to PROVE none is a legacy outlier (Success Criterion #4 is a verification, not migration work — the audit says none remain, so prove it). For each file assert: `viewBox="0 0 720` present, all five CSS class selectors present in the style block, `system-ui` present, `role="img"` present, and `aria-label` present. Then run DOCS_FAST=1 mkdocs build and confirm the learn/ pages (introduction, custom-plotting, simulation, derivatives, irregular-sampling) render with no errors. Record per-file marker results in the SUMMARY. If any marker is missing on any file, stop and report — that would be an unexpected legacy outlier requiring migration.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; for f in introduction custom-plotting simulation smoothing derivatives irregular-sampling; do p="docs/assets/diagrams/$f.svg"; for m in 'viewBox="0 0 720' '.ttl' '.sub' '.lab' '.sm' '.mono' 'system-ui' 'role="img"' 'aria-label'; do grep -qF "$m" "$p" || { echo "MISSING $m in $f"; ok=0; }; done; done; [ "$ok" = 1 ]</automated>
    <automated>cd /home/simonm/projects/rust/pyfda && DOCS_FAST=1 mkdocs build 2>&1 | tee /tmp/mkbuild2.log; grep -Eiq 'error|traceback' /tmp/mkbuild2.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 6 learn/ diagrams contains: `viewBox="0 0 720`, `.ttl` `.sub` `.lab` `.sm` `.mono`, `system-ui`, `role="img"`, and `aria-label` — zero missing markers (exit 0). This proves Success Criterion #4 (no legacy-outlier learn/ diagram remains).
    - `DOCS_FAST=1 mkdocs build` completes with no `ERROR`/`Traceback` lines.
  </acceptance_criteria>
  <done>All 6 learn/ diagrams carry the full STYLE_SPEC marker set and the section builds cleanly; results recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Author COVERAGE.md api-coverage declaration (seal-time gate)</name>
  <files>.planning/phases/03-learn-diagrams/COVERAGE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 scope — confirms this is a diagram-editing phase with no external API)
  </read_first>
  <action>
    Write `.planning/phases/03-learn-diagrams/COVERAGE.md` containing exactly the reasoned declaration required by the api-coverage gate: state that this phase integrates no external API/SDK/service. Include the mandated sentence verbatim: "No external API integration: phase edits hand-authored static SVG diagrams and runs local SVGO/mkdocs tooling only." Add a one-line note that the SVGO lint and mkdocs build are local dev tooling, not networked services.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qF 'No external API integration: phase edits hand-authored static SVG diagrams and runs local SVGO/mkdocs tooling only.' .planning/phases/03-learn-diagrams/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-learn-diagrams/COVERAGE.md` exists and contains the exact declaration sentence (grep -F exit 0).
  </acceptance_criteria>
  <done>COVERAGE.md exists with the exact no-external-API declaration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: learn/ section review gate — all 6 diagrams on the built site</name>
  <what-built>Section-wide verification of all 6 learn/ diagrams: SVGO gate, STYLE_SPEC markers, and built-site render — plus the smoothing fix from Plan 01 in context of the whole section.</what-built>
  <how-to-verify>
    Run `DOCS_FAST=1 mkdocs serve` and open each learn/ page: /learn/introduction/, /learn/custom-plotting/, /learn/simulation/, /learn/smoothing/, /learn/derivatives/, /learn/irregular-sampling/. For each, confirm the concept diagram renders (no broken image), is legible, and depicts the method described on the page (Success Criterion #3 — accurate, non-generic diagram visible). This is the learn/ section review gate before Phase 4 begins.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 learn/ diagrams render correctly and accurately on the built site, or list any page whose diagram looks wrong or generic.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
This phase mostly MODIFIES existing hand-authored SVGs rather than creating new symbols. Artifacts created or modified across both plans:

**Modified:**
- `docs/assets/diagrams/smoothing.svg` (Plan 01, Task 1 — GAP-0001 Panel 3 ghost redraw)

**Created:**
- `.planning/phases/03-learn-diagrams/COVERAGE.md` (Plan 02, Task 3 — api-coverage declaration)
- `.planning/phases/03-learn-diagrams/03-01-SUMMARY.md` (Plan 01 execution summary — records GAP-0001 decision + new coordinate string)
- `.planning/phases/03-learn-diagrams/03-02-SUMMARY.md` (Plan 02 execution summary — per-file SVGO + marker + build verification log)

**Verified-only (not modified):**
- `docs/assets/diagrams/introduction.svg`, `custom-plotting.svg`, `simulation.svg`, `derivatives.svg`, `irregular-sampling.svg` (Plan 02 — SVGO gate + STYLE_SPEC markers + built-site render; all rated accurate+conforming by the audit, re-proven against the live gate)
</artifacts_this_phase_produces>

<threat_model>
No new attack surface. This plan runs check-only SVGO lint (stdout mode), a local mkdocs build, grep marker checks, and writes one planning markdown file. No runtime user input, no network endpoint, no auth, no data persistence, no new package installs. ASVS L1 is not applicable to this phase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | SVGO gate rewriting a committed hand-authored SVG | low | mitigate | Gate invoked in stdout mode (`--output -`) only; `git diff --stat docs/assets/diagrams/` acceptance check confirms no SVG is reserialised. |
</threat_model>

<verification>
Maps to ROADMAP Phase 3 Success Criteria:
- SC #1 (all learn/ SVG pass SVGO lint): Task 1 gate over all 6.
- SC #3 (every warranting learn/ page has an accurate, non-generic diagram visible on the built site): Task 2 build + Task 4 human review.
- SC #4 (all legacy-outlier learn/ diagrams migrated — verify none remain): Task 2 STYLE_SPEC marker grep over all 6 (exit 0 proves none remain).
</verification>

<success_criteria>
- All 6 learn/ diagrams pass the SVGO idempotence gate (Task 1, exit 0).
- All 6 carry the full STYLE_SPEC marker set (Task 2, exit 0) — no legacy outlier remains.
- `DOCS_FAST=1 mkdocs build` succeeds.
- COVERAGE.md exists with the exact no-external-API declaration.
- Human reviewer approves all 6 learn/ diagrams on the built site (section review gate).
</success_criteria>

<output>
Create `.planning/phases/03-learn-diagrams/03-02-SUMMARY.md` when done. Record the per-file SVGO pass/fail, per-file STYLE_SPEC marker results, the mkdocs build result, and the COVERAGE.md declaration.
</output>
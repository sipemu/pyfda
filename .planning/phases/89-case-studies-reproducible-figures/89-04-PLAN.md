---
phase: 89-case-studies-reproducible-figures
plan: "04"
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - paper/code/casestudy4.py
  - paper/figures/cs4_wine_gridsearch.pdf
  - paper/figures/cs4_wine_scores.pdf
  - paper/sections/casestudy4.tex
autonomous: true
requirements: [CASE-04]

estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "casestudy4.py runs a sklearn Pipeline(FPCATransformer -> LDA) under GridSearchCV(n_jobs=1) over n_components on wine against fdars 0.12.0 without error; best CV accuracy ~0.961 at n_components=8."
    - "Re-running writes byte-identical cs4_wine_gridsearch.pdf and cs4_wine_scores.pdf."
    - "casestudy4.tex \\includegraphics both cs4 figures + honestly reports best CV accuracy 0.961 at n_components=8 across the grid {2,3,5,8}."
  artifacts:
    - paper/code/casestudy4.py
    - paper/figures/cs4_wine_gridsearch.pdf
    - paper/figures/cs4_wine_scores.pdf
    - paper/sections/casestudy4.tex
  key_links:
    - "casestudy4.main() -> save_figure -> paper/figures/cs4_*.pdf (auto-picked up by gen_figures.py lazy import from 89-01)"
    - "casestudies.tex \\input{sections/casestudy4} (wired by 89-01) -> \\includegraphics{figures/cs4_*.pdf}"
---

<objective>
Study 4 (CASE-04): sklearn Pipeline + GridSearchCV on wine, showcasing the estimator layer. A
`casestudy4.py` script runs the validated pipeline against fdars 0.12.0, writes two deterministic
committed figures, and `casestudy4.tex` narrates them with honest numbers. Reuses the machinery from
89-01 — touches NO shared file.

Purpose: horizontal expansion of the proven case-study slice (the composability / hyperparameter-tuning
story).
Output: `casestudy4.py`, `cs4_wine_gridsearch.pdf`, `cs4_wine_scores.pdf`, `casestudy4.tex`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md
@paper/code/paper_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: casestudy4.py — Pipeline + GridSearchCV on wine → two deterministic committed figures</name>
  <files>paper/code/casestudy4.py, paper/figures/cs4_wine_gridsearch.pdf, paper/figures/cs4_wine_scores.pdf</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 4 validated pipeline + Signature Reference Table + Pitfall 1/2/5/7 + Determinism Pinning — transcribe exact calls + real numbers)
    - paper/code/paper_utils.py (fig, FDARS_COLORS, save_figure, data_path)
    - paper/code/casestudy1.py (module pattern to mirror) — created by 89-01
  </read_first>
  <behavior>
    - casestudy4.main() runs without error against fdars 0.12.0 and produces exactly two figures.
    - GridSearchCV uses n_jobs=1 (Pitfall 7 — deterministic split ordering); pipeline is
      FPCATransformer(n_components) -> sklearn LDA, NOT FPCATransformer -> FPCKNNClassifier (Pitfall 2
      double-FPCA degrades accuracy).
    - Wine labels are int64 with -1 applied (1,2,3 -> 0,1,2; Pitfall 5).
    - best_score_ is ~0.961 at n_components=8; computed live.
    - Re-running writes byte-identical cs4_*.pdf.
  </behavior>
  <action>
    Create `paper/code/casestudy4.py` mirroring the casestudy1.py module pattern (`from __future__ import
    annotations`; `from paper_utils import fig, FDARS_COLORS, save_figure, data_path`; `import fdars`;
    `from fdars.sklearn._skeletons import FPCATransformer`; `from sklearn.pipeline import Pipeline`;
    `from sklearn.discriminant_analysis import LinearDiscriminantAnalysis`; `from
    sklearn.model_selection import GridSearchCV`; `_FIGURES_DIR = ...`). snake_case, NumPy docstrings.

    `main() -> None` pins `np.random.seed(42)` FIRST, then transcribes §Study 4 verbatim: load
    `wn = pd.read_csv(data_path("wine.csv"), index_col=0)`; `Xw = wn.values.astype(np.float64)` (178×13);
    `yw = np.array(wn.index.tolist(), dtype=np.int64) - 1` (0,1,2 int64 — Pitfall 5). Build
    `pipe = Pipeline([("fpca", FPCATransformer(n_components=3)), ("lda",
    LinearDiscriminantAnalysis())])` (FPCATransformer uses `n_components`; use plain sklearn LDA, NOT
    FPCKNNClassifier — Pitfall 2). `param_grid = {"fpca__n_components": [2, 3, 5, 8]}`;
    `gs = GridSearchCV(pipe, param_grid, cv=5, scoring="accuracy", refit=True, n_jobs=1)` (n_jobs=1 —
    Pitfall 7); `gs.fit(Xw, yw)`. `print("best:", gs.best_params_, round(float(gs.best_score_), 3))`.

    Two figures:
    (1) `cs4_wine_gridsearch.pdf` — mean CV accuracy vs n_components (bar or line) from
        `gs.cv_results_["mean_test_score"]` against the grid [2,3,5,8]. Demonstrates the tuning story.
    (2) `cs4_wine_scores.pdf` — FPCA scores scatter (PC1 vs PC2 of the best estimator's fitted
        FPCATransformer, or of an FPCATransformer(n_components=8) transform of Xw) colored by class yw.
        Demonstrates the separability at the optimal n_components. If extracting PC scores from the
        fitted transformer is awkward, fit an FPCATransformer(n_components=8).fit_transform(Xw) and
        plot columns 0 vs 1 — this is deterministic and consistent.
    Float annotations rounded (never raw repr). `_FIGURES_DIR.mkdir(parents=True, exist_ok=True)` before
    first save. `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/casestudy4.py && test -f paper/figures/cs4_wine_gridsearch.pdf && test -f paper/figures/cs4_wine_scores.pdf && cp paper/figures/cs4_wine_gridsearch.pdf /tmp/cs4g.pdf && cp paper/figures/cs4_wine_scores.pdf /tmp/cs4s.pdf && PYTHONPATH=scripts:paper/code python paper/code/casestudy4.py && diff -q /tmp/cs4g.pdf paper/figures/cs4_wine_gridsearch.pdf && diff -q /tmp/cs4s.pdf paper/figures/cs4_wine_scores.pdf && rm -f /tmp/cs4g.pdf /tmp/cs4s.pdf && grep -qE 'n_jobs=1' paper/code/casestudy4.py && ! grep -qE 'FPCKNNClassifier' paper/code/casestudy4.py && echo CS4_DETERMINISTIC_OK</automated>
    <fails_when>casestudy4.py errors against fdars 0.12.0, omits n_jobs=1, pipes into FPCKNNClassifier (double-FPCA), either figure is missing, OR a re-run produces byte-different PDFs.</fails_when>
  </verify>
  <done>casestudy4.py runs the validated Study-4 GridSearchCV pipeline (n_jobs=1, FPCATransformer->LDA), prints best CV accuracy ~0.961 at n_components=8, and writes two byte-stable committed figures cs4_wine_gridsearch.pdf + cs4_wine_scores.pdf.</done>
</task>

<task type="auto">
  <name>Task 2: casestudy4.tex narrative subsection (figures + honest grid results)</name>
  <files>paper/sections/casestudy4.tex</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 4 real output numbers + §casestudies.tex structure)
    - paper/sections/casestudy1.tex (subsection + figure pattern to mirror) — created by 89-01
  </read_first>
  <action>
    NOTE: this plan must NOT edit casestudies.tex, gen_figures.py, Makefile, or paper.yml — 89-01 owns
    them. If any is absent, STOP — this plan depends on 89-01. Case-study executed snippets are
    intentionally omitted (89-RESEARCH Open Question 3 — executor discretion); `paper/code/casestudy4.py`
    is the provably-current source of the shown analysis. Refer to the script by name in prose.

    Create `paper/sections/casestudy4.tex` as a `\subsection` (NOT `\section`): title "Composable
    scikit-learn Pipelines: Wine Data"; problem framing (178 wine samples, 3 classes; an fdars
    FPCATransformer composed with a vanilla sklearn LinearDiscriminantAnalysis, tuned by GridSearchCV);
    methods described honestly (the estimator layer plugs natively into Pipeline/GridSearchCV; note the
    13-feature "functional preprocessing" framing per 89-RESEARCH §Study 4 dataset choice — do not
    overclaim a traditional-FDA interpretation), noting the analysis is reproduced by
    `paper/code/casestudy4.py`; a `figure` with
    `\includegraphics[width=...]{figures/cs4_wine_gridsearch.pdf}` and one with
    `figures/cs4_wine_scores.pdf`, each with caption + `\label`; interpretation paragraph reporting the
    HONEST grid results: mean CV accuracy 0.697/0.759/0.916/0.961 for n_components 2/3/5/8 and best CV
    accuracy 0.961 at n_components=8. Reference the sklearn estimator count via the `\nsklearnestimators`
    macro (do NOT hardcode 28). No benchmark/performance claims, no dates. `\citep`/`\citet` only for
    keys that resolve in paper/refs.bib or paper/refs_manual.bib.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qF '\subsection' paper/sections/casestudy4.tex && ! grep -qF '\section' paper/sections/casestudy4.tex && grep -qF 'figures/cs4_wine_gridsearch.pdf' paper/sections/casestudy4.tex && grep -qF 'figures/cs4_wine_scores.pdf' paper/sections/casestudy4.tex && grep -qF '\nsklearnestimators' paper/sections/casestudy4.tex && ! grep -qE '(^|[^0-9])28([^0-9]|$)' paper/sections/casestudy4.tex && ! grep -qi 'Placeholder' paper/sections/casestudy4.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/casestudy4.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/casestudy4.tex && for k in $(grep -oE '\\cite[pt]?\{[^}]+\}' paper/sections/casestudy4.tex | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sed 's/ //g' | sort -u); do grep -qhE "@[a-zA-Z]+\{$k," paper/refs.bib paper/refs_manual.bib || { echo "UNRESOLVED CITE $k"; exit 1; }; done && git diff --quiet -- paper/sections/casestudies.tex paper/code/gen_figures.py paper/code/gen_snippets.py Makefile .github/workflows/paper.yml && echo CS4_NARRATIVE_OK</automated>
    <fails_when>casestudy4.tex uses \section not \subsection, is missing either \includegraphics path, hardcodes 28 instead of \nsklearnestimators, contains a benchmark claim/date/unresolved cite; OR this plan modified a 89-01-owned file.</fails_when>
  </verify>
  <done>casestudy4.tex is a self-contained \subsection \includegraphics-ing both cs4 figures, referencing casestudy4.py and reporting the honest grid results (best 0.961 at n_components=8) via the \nsklearnestimators macro; no 89-01-owned file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset file → pipeline | wine.csv read via data_path() (traversal rejected) |
| generated PDF → committed artifact | non-deterministic metadata / parallel CV ordering could poison the git diff gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-08 | Tampering | GridSearchCV parallel-ordering nondeterminism | medium | mitigate | n_jobs=1 forced (Pitfall 7); save_figure CreationDate=None + Agg + seed(42); git diff gate (89-01) fails on drift |
| T-89-09 | Information Disclosure | reported CV accuracy | low | mitigate | best_score_ computed live from GridSearchCV; full grid reported honestly (0.697..0.961) |
</threat_model>

<verification>
- casestudy4.py runs against fdars 0.12.0 with n_jobs=1 + FPCATransformer->LDA; two figures; re-run byte-identical.
- casestudy4.tex \subsection \includegraphics both cs4 figures; honest grid numbers; \nsklearnestimators macro; cites resolve.
- No 89-01-owned file modified.
</verification>

<success_criteria>
- CASE-04 pipeline runs end-to-end deterministically with two committed figures (SC-4, SC-5 for Study 4).
- casestudy4.tex narrates the Pipeline + GridSearchCV story with honest grid results.
</success_criteria>

<output>
Create `.planning/phases/89-case-studies-reproducible-figures/89-04-SUMMARY.md` when done
</output>
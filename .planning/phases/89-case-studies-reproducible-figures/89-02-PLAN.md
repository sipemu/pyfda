---
phase: 89-case-studies-reproducible-figures
plan: "02"
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - paper/code/casestudy2.py
  - paper/figures/cs2_tecator_align.pdf
  - paper/figures/cs2_tecator_fit.pdf
  - paper/sections/casestudy2.tex
autonomous: true
requirements: [CASE-02]

estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "casestudy2.py runs registration (karcher_mean + align_to_target) + scalar-on-function regression (fregre_lm) + FPCRegressor CV on tecator against fdars 0.12.0 without error."
    - "Re-running writes byte-identical cs2_tecator_align.pdf and cs2_tecator_fit.pdf (git diff paper/figures/ empty for those two)."
    - "casestudy2.tex \\includegraphics both cs2 figures + reports honest numbers: training R^2 0.929 and 5-fold CV R^2 mean 0.805."
  artifacts:
    - paper/code/casestudy2.py
    - paper/figures/cs2_tecator_align.pdf
    - paper/figures/cs2_tecator_fit.pdf
    - paper/sections/casestudy2.tex
  key_links:
    - "casestudy2.main() -> save_figure -> paper/figures/cs2_*.pdf (auto-picked up by gen_figures.py main() lazy import from 89-01)"
    - "casestudies.tex \\input{sections/casestudy2} (wired by 89-01) -> \\includegraphics{figures/cs2_*.pdf}"
---

<objective>
Study 2 (CASE-02): registration + scalar-on-function regression on tecator. A `casestudy2.py` script runs
the validated pipeline against fdars 0.12.0, writes two deterministic committed figures, and
`casestudy2.tex` narrates them with honest numbers. Reuses the machinery proven by 89-01 — touches NO
shared file (`casestudies.tex`, `gen_figures.py`, `Makefile`, `paper.yml` are owned by 89-01; the lazy
try/except import there auto-includes this study's figures once `casestudy2.py` exists).

Purpose: horizontal expansion of the proven case-study slice.
Output: `casestudy2.py`, `cs2_tecator_align.pdf`, `cs2_tecator_fit.pdf`, `casestudy2.tex`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md
@paper/code/paper_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: casestudy2.py — registration + scalar-on-function regression → two deterministic committed figures</name>
  <files>paper/code/casestudy2.py, paper/figures/cs2_tecator_align.pdf, paper/figures/cs2_tecator_fit.pdf</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 2 validated pipeline + Signature Reference Table + Pitfall 4/6 + Determinism Pinning — transcribe exact calls + real numbers)
    - paper/code/paper_utils.py (fig, FDARS_COLORS, save_figure, data_path)
    - paper/code/casestudy1.py (module pattern to mirror: _FIGURES_DIR, main(), save_figure) — created by 89-01
  </read_first>
  <behavior>
    - casestudy2.main() runs without error against fdars 0.12.0 and produces exactly two figures.
    - karcher_mean reports converged=False (max_iter=20 hit) — this is EXPECTED; the code must NOT
      assert km["converged"] (Pitfall 4). It may print it informationally.
    - fregre_lm training R^2 is ~0.929; FPCRegressor 5-fold CV mean R^2 is ~0.805; both computed live.
    - Re-running writes byte-identical cs2_*.pdf.
  </behavior>
  <action>
    Create `paper/code/casestudy2.py` mirroring the casestudy1.py module pattern (`from __future__ import
    annotations`; `from paper_utils import fig, FDARS_COLORS, save_figure, data_path`; `import fdars`;
    `from fdars.sklearn._skeletons import FPCRegressor`; `from sklearn.model_selection import
    cross_val_score`; `_FIGURES_DIR = Path(__file__).resolve().parent.parent / "figures"`). snake_case,
    NumPy docstrings.

    `main() -> None` pins `np.random.seed(42)` FIRST, then transcribes §Study 2 verbatim: load
    `tec = pd.read_csv(data_path("tecator.csv"), index_col=0)`; `Xt = tec.iloc[:, :100].values.astype(
    np.float64)` (240×100); `yfat = tec["fat"].values.astype(np.float64)`; `ARGt = np.arange(1, 101,
    dtype=np.float64)`. Registration: `km = fdars.alignment.karcher_mean(Xt[:30], ARGt, max_iter=20)`
    (subset of 30 for speed; do NOT assert converged); `aligned = fdars.alignment.align_to_target(Xt,
    km["mean"], ARGt); Xa = aligned["aligned_data"]` (240×100). Regression: `sof =
    fdars.regression.fregre_lm(Xt, yfat, n_comp=5)` — use key `sof["fitted_values"]` (NOT "fitted",
    Pitfall 6) and `sof["r_squared"]`; `print("train R2:", round(float(sof["r_squared"]), 3))`. CV:
    `cv = cross_val_score(FPCRegressor(n_components=5), Xt, yfat, cv=5, scoring="r2")` (FPCRegressor uses
    `n_components`, fregre_lm uses `n_comp` — different names, do not mix); `print("CV R2:",
    round(float(cv.mean()), 3))`.

    Two figures:
    (1) `cs2_tecator_align.pdf` — raw spectra (grey, e.g. Xt[:30]) with aligned spectra (Xa[:30], colored
        or the Karcher mean km["mean"] highlighted); x = ARGt. Demonstrates registration.
    (2) `cs2_tecator_fit.pdf` — actual-vs-fitted scatter `yfat` vs `sof["fitted_values"]` with the
        y=x reference line and the R^2 annotated via `round(float(sof["r_squared"]), 3)`.
    Floats in annotations rounded (never raw repr). `_FIGURES_DIR.mkdir(parents=True, exist_ok=True)`
    before first save. `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/casestudy2.py && test -f paper/figures/cs2_tecator_align.pdf && test -f paper/figures/cs2_tecator_fit.pdf && cp paper/figures/cs2_tecator_align.pdf /tmp/cs2a.pdf && cp paper/figures/cs2_tecator_fit.pdf /tmp/cs2f.pdf && PYTHONPATH=scripts:paper/code python paper/code/casestudy2.py && diff -q /tmp/cs2a.pdf paper/figures/cs2_tecator_align.pdf && diff -q /tmp/cs2f.pdf paper/figures/cs2_tecator_fit.pdf && rm -f /tmp/cs2a.pdf /tmp/cs2f.pdf && ! grep -qE 'assert .*converged' paper/code/casestudy2.py && echo CS2_DETERMINISTIC_OK</automated>
    <fails_when>casestudy2.py errors against fdars 0.12.0 (e.g. sof["fitted"] KeyError, or FPCRegressor n_comp typo), asserts km["converged"], either figure is missing, OR a re-run produces byte-different PDFs.</fails_when>
  </verify>
  <done>casestudy2.py runs the validated Study-2 pipeline, prints real train R2 (~0.929) and CV R2 (~0.805), and writes two byte-stable committed figures cs2_tecator_align.pdf + cs2_tecator_fit.pdf.</done>
</task>

<task type="auto">
  <name>Task 2: casestudy2.tex narrative subsection (figures + honest numbers)</name>
  <files>paper/sections/casestudy2.tex</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 2 real output numbers + §casestudies.tex structure)
    - paper/sections/casestudy1.tex (subsection + figure pattern to mirror — created by 89-01)
  </read_first>
  <action>
    NOTE: this plan must NOT edit casestudies.tex, gen_figures.py, Makefile, or paper.yml — 89-01 owns
    them and already wired `\input{sections/casestudy2}` and the figure pickup. If any of those files is
    absent (89-01 not yet executed), STOP — this plan depends on 89-01. Case-study executed snippets are
    intentionally omitted (89-RESEARCH Open Question 3 — executor discretion; the committed
    `casestudy2.py` script is the provably-current source of the shown analysis, and the capability tour
    already carries per-family snippets). Refer to the script by name in prose instead.

    Create `paper/sections/casestudy2.tex` as a `\subsection` (NOT `\section`): title "Registration and
    Scalar-on-Function Regression: Tecator Data"; problem framing (240 near-infrared meat spectra,
    predict fat content); methods described honestly (`karcher_mean`/`align_to_target` elastic
    registration — mention that alignment iterations were capped at 20 for efficiency and convergence is
    not required for the registration to be illustrative, per 89-RESEARCH Open Question 4; `fregre_lm`
    scalar-on-function FPC regression), noting the analysis is reproduced by `paper/code/casestudy2.py`;
    a `figure` with `\includegraphics[width=...]{figures/cs2_tecator_align.pdf}` and one with
    `figures/cs2_tecator_fit.pdf`, each with caption + `\label`; interpretation paragraph reporting the
    HONEST numbers: training R^2 = 0.929 and 5-fold CV R^2 mean = 0.805 (note the wide spread including
    a weak fold — report the mean honestly, do not hide it). No benchmark/performance claims, no dates.
    `\citep`/`\citet` only for keys that resolve in paper/refs.bib or paper/refs_manual.bib.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qF '\subsection' paper/sections/casestudy2.tex && ! grep -qF '\section' paper/sections/casestudy2.tex && grep -qF 'figures/cs2_tecator_align.pdf' paper/sections/casestudy2.tex && grep -qF 'figures/cs2_tecator_fit.pdf' paper/sections/casestudy2.tex && ! grep -qi 'Placeholder' paper/sections/casestudy2.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/casestudy2.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/casestudy2.tex && for k in $(grep -oE '\\cite[pt]?\{[^}]+\}' paper/sections/casestudy2.tex | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sed 's/ //g' | sort -u); do grep -qhE "@[a-zA-Z]+\{$k," paper/refs.bib paper/refs_manual.bib || { echo "UNRESOLVED CITE $k"; exit 1; }; done && git diff --quiet -- paper/sections/casestudies.tex paper/code/gen_figures.py paper/code/gen_snippets.py Makefile .github/workflows/paper.yml && echo CS2_NARRATIVE_OK</automated>
    <fails_when>casestudy2.tex uses \section not \subsection, is missing either \includegraphics path, contains a benchmark claim/date/unresolved cite; OR this plan modified a file owned by 89-01 (casestudies.tex/gen_figures.py/gen_snippets.py/Makefile/paper.yml).</fails_when>
  </verify>
  <done>casestudy2.tex is a self-contained \subsection \includegraphics-ing both cs2 figures, referencing casestudy2.py and reporting honest R2 numbers; no 89-01-owned file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset file → pipeline | tecator.csv read via data_path() (traversal rejected in paper_utils) |
| generated PDF → committed artifact | non-deterministic metadata could poison the git diff gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-04 | Tampering | paper/figures/cs2_*.pdf determinism | medium | mitigate | save_figure CreationDate=None + Agg + seed(42); the git diff gate established in 89-01 fails on any byte drift |
| T-89-05 | Information Disclosure | reported R^2 numbers | low | mitigate | R^2 computed live from fregre_lm/cross_val_score; weak CV fold reported honestly in the mean |
</threat_model>

<verification>
- casestudy2.py runs against fdars 0.12.0; two figures written; re-run byte-identical; converged not asserted.
- casestudy2.tex \subsection \includegraphics both cs2 figures; honest R2 numbers; cites resolve.
- No 89-01-owned file (casestudies.tex, gen_figures.py, Makefile, paper.yml) modified.
</verification>

<success_criteria>
- CASE-02 pipeline runs end-to-end deterministically with two committed figures (SC-2, SC-5 for Study 2).
- casestudy2.tex narrates registration + regression with honest numbers.
</success_criteria>

<output>
Create `.planning/phases/89-case-studies-reproducible-figures/89-02-SUMMARY.md` when done
</output>
---
phase: 89-case-studies-reproducible-figures
plan: "03"
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - paper/code/casestudy3.py
  - paper/figures/cs3_precip_curves.pdf
  - paper/figures/cs3_precip_forecast.pdf
  - paper/sections/casestudy3.tex
autonomous: true
requirements: [CASE-03]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "casestudy3.py runs the FTS forecast (ftsm + ftsm_forecast on RAW transposed data) on canadian_weather_precip against fdars 0.12.0 without error; forecast shape is (3, 365)."
    - "Re-running writes byte-identical cs3_precip_curves.pdf and cs3_precip_forecast.pdf."
    - "casestudy3.tex \\includegraphics both cs3 figures + honestly states 35 stations x 365 days -> 3-step forecast and that 35 observations is shallow for FTS."
  artifacts:
    - paper/code/casestudy3.py
    - paper/figures/cs3_precip_curves.pdf
    - paper/figures/cs3_precip_forecast.pdf
    - paper/sections/casestudy3.tex
  key_links:
    - "casestudy3.main() -> save_figure -> paper/figures/cs3_*.pdf (auto-picked up by gen_figures.py lazy import from 89-01)"
    - "casestudies.tex \\input{sections/casestudy3} (wired by 89-01) -> \\includegraphics{figures/cs3_*.pdf}"
---

<objective>
Study 3 (CASE-03): functional-time-series forecast on canadian_weather_precip. A `casestudy3.py` script
runs the validated FTS pipeline against fdars 0.12.0, writes two deterministic committed figures, and
`casestudy3.tex` narrates them honestly (35 stations is a shallow but valid FTS demo). Reuses the
machinery from 89-01 — touches NO shared file.

Purpose: horizontal expansion of the proven case-study slice.
Output: `casestudy3.py`, `cs3_precip_curves.pdf`, `cs3_precip_forecast.pdf`, `casestudy3.tex`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md
@paper/code/paper_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: casestudy3.py — FTS decomposition + 3-step forecast → two deterministic committed figures</name>
  <files>paper/code/casestudy3.py, paper/figures/cs3_precip_curves.pdf, paper/figures/cs3_precip_forecast.pdf</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 3 validated pipeline + Signature Reference Table + Pitfall 3 + Determinism Pinning — transcribe exact calls + real numbers + orientation)
    - paper/code/paper_utils.py (fig, FDARS_COLORS, save_figure, data_path)
    - paper/code/casestudy1.py (module pattern to mirror) — created by 89-01
  </read_first>
  <behavior>
    - casestudy3.main() runs without error against fdars 0.12.0 and produces exactly two figures.
    - Data is transposed to (35, 365) BEFORE the FTS calls (Xfts = cw.T...); ftsm_forecast is called on
      the RAW data array + argvals, NOT on a model object (Pitfall 3).
    - forecast shape is (3, 365); computed live.
    - Re-running writes byte-identical cs3_*.pdf.
  </behavior>
  <action>
    Create `paper/code/casestudy3.py` mirroring the casestudy1.py module pattern (`from __future__ import
    annotations`; `from paper_utils import fig, FDARS_COLORS, save_figure, data_path`; `import fdars`;
    `_FIGURES_DIR = ...`). snake_case, NumPy docstrings.

    `main() -> None` pins `np.random.seed(42)` FIRST, then transcribes §Study 3 verbatim: load
    `cw = pd.read_csv(data_path("canadian_weather_precip.csv"), index_col=0)` (365×35 days×stations);
    `Xfts = cw.T.values.astype(np.float64)` (35×365 stations×days); `ARGd = np.arange(1, 366,
    dtype=np.float64)`. Forecast: `fc = fdars.fts.ftsm_forecast(Xfts, ARGd, h=3, ncomp=3)` — GOTCHA:
    ftsm_forecast takes RAW DATA + argvals, not a model (Pitfall 3); `fc["forecast"]` is (3, 365).
    Decomposition (for the curves figure): `model = fdars.fts.ftsm(Xfts, ARGd, ncomp=3)`; use
    `model["mean"]` for the mean curve. `print("forecast shape:", fc["forecast"].shape)`.

    Two figures:
    (1) `cs3_precip_curves.pdf` — the 35 observed station precipitation curves (grey, x = ARGd) with the
        FTS mean `model["mean"]` highlighted. Demonstrates the functional structure.
    (2) `cs3_precip_forecast.pdf` — the 3 forecast curves `fc["forecast"][0]`, `[1]`, `[2]` (distinct
        colors from FDARS_COLORS) alongside the observed mean `model["mean"]`. Demonstrates the forecast.
    Any float annotation rounded with `round`/`np.round` (never raw repr). `_FIGURES_DIR.mkdir(
    parents=True, exist_ok=True)` before first save. `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && . .venv/bin/activate 2>/dev/null; PYTHONPATH=scripts:paper/code python paper/code/casestudy3.py && test -f paper/figures/cs3_precip_curves.pdf && test -f paper/figures/cs3_precip_forecast.pdf && cp paper/figures/cs3_precip_curves.pdf /tmp/cs3c.pdf && cp paper/figures/cs3_precip_forecast.pdf /tmp/cs3f.pdf && PYTHONPATH=scripts:paper/code python paper/code/casestudy3.py && diff -q /tmp/cs3c.pdf paper/figures/cs3_precip_curves.pdf && diff -q /tmp/cs3f.pdf paper/figures/cs3_precip_forecast.pdf && rm -f /tmp/cs3c.pdf /tmp/cs3f.pdf && grep -qE '\.T\.values' paper/code/casestudy3.py && echo CS3_DETERMINISTIC_OK</automated>
    <fails_when>casestudy3.py errors against fdars 0.12.0 (e.g. ftsm_forecast passed a model instead of raw data, or missing the .T transpose), either figure is missing, OR a re-run produces byte-different PDFs.</fails_when>
  </verify>
  <done>casestudy3.py runs the validated Study-3 FTS pipeline on transposed (35,365) data, prints forecast shape (3,365), and writes two byte-stable committed figures cs3_precip_curves.pdf + cs3_precip_forecast.pdf.</done>
</task>

<task type="auto">
  <name>Task 2: casestudy3.tex narrative subsection (figures + honest caveat)</name>
  <files>paper/sections/casestudy3.tex</files>
  <read_first>
    - .planning/phases/89-case-studies-reproducible-figures/89-RESEARCH.md (§Study 3 real output numbers + dataset structure + §casestudies.tex structure)
    - paper/sections/casestudy1.tex (subsection + figure pattern to mirror) — created by 89-01
  </read_first>
  <action>
    NOTE: this plan must NOT edit casestudies.tex, gen_figures.py, Makefile, or paper.yml — 89-01 owns
    them. If any is absent, STOP — this plan depends on 89-01. Case-study executed snippets are
    intentionally omitted (89-RESEARCH Open Question 3 — executor discretion); `paper/code/casestudy3.py`
    is the provably-current source of the shown analysis. Refer to the script by name in prose.

    Create `paper/sections/casestudy3.tex` as a `\subsection` (NOT `\section`): title "Functional Time
    Series Forecasting: Canadian Weather Precipitation"; problem framing (35 stations, each a 365-day
    daily-precipitation curve; treat stations as the FTS index); methods (`ftsm` decomposition +
    `ftsm_forecast` h=3, ncomp=3), noting the analysis is reproduced by `paper/code/casestudy3.py`; a
    `figure` with `\includegraphics[width=...]{figures/cs3_precip_curves.pdf}` and one with
    `figures/cs3_precip_forecast.pdf`, each with caption + `\label`; interpretation paragraph stating
    honestly that 35 stations is a shallow sample for FTS (illustrative, not statistically optimal —
    per 89-RESEARCH Assumption A4) and that the model produces a 3-step-ahead forecast of shape (3, 365).
    No benchmark/performance claims, no dates. `\citep`/`\citet` only for keys that resolve in
    paper/refs.bib or paper/refs_manual.bib.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qF '\subsection' paper/sections/casestudy3.tex && ! grep -qF '\section' paper/sections/casestudy3.tex && grep -qF 'figures/cs3_precip_curves.pdf' paper/sections/casestudy3.tex && grep -qF 'figures/cs3_precip_forecast.pdf' paper/sections/casestudy3.tex && ! grep -qi 'Placeholder' paper/sections/casestudy3.tex && ! grep -qiE 'benchmark|faster than|speedup|outperform' paper/sections/casestudy3.tex && ! grep -qiE '[0-9]{4}-[0-9]{2}-[0-9]{2}' paper/sections/casestudy3.tex && for k in $(grep -oE '\\cite[pt]?\{[^}]+\}' paper/sections/casestudy3.tex | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sed 's/ //g' | sort -u); do grep -qhE "@[a-zA-Z]+\{$k," paper/refs.bib paper/refs_manual.bib || { echo "UNRESOLVED CITE $k"; exit 1; }; done && git diff --quiet -- paper/sections/casestudies.tex paper/code/gen_figures.py paper/code/gen_snippets.py Makefile .github/workflows/paper.yml && echo CS3_NARRATIVE_OK</automated>
    <fails_when>casestudy3.tex uses \section not \subsection, is missing either \includegraphics path, contains a benchmark claim/date/unresolved cite; OR this plan modified a 89-01-owned file.</fails_when>
  </verify>
  <done>casestudy3.tex is a self-contained \subsection \includegraphics-ing both cs3 figures, referencing casestudy3.py and honestly framing the shallow-sample caveat; no 89-01-owned file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset file → pipeline | canadian_weather_precip.csv read via data_path() (traversal rejected) |
| generated PDF → committed artifact | non-deterministic metadata could poison the git diff gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-06 | Tampering | paper/figures/cs3_*.pdf determinism | medium | mitigate | save_figure CreationDate=None + Agg + seed(42); float annotations rounded; the git diff gate (89-01) fails on drift |
| T-89-07 | Information Disclosure | FTS demo framing | low | mitigate | narrative honestly states 35 stations is shallow for FTS (A4); forecast shape reported live |
</threat_model>

<verification>
- casestudy3.py runs against fdars 0.12.0 on transposed (35,365) data; two figures; re-run byte-identical.
- casestudy3.tex \subsection \includegraphics both cs3 figures; honest caveat; cites resolve.
- No 89-01-owned file modified.
</verification>

<success_criteria>
- CASE-03 pipeline runs end-to-end deterministically with two committed figures (SC-3, SC-5 for Study 3).
- casestudy3.tex narrates the FTS forecast honestly including the shallow-sample caveat.
</success_criteria>

<output>
Create `.planning/phases/89-case-studies-reproducible-figures/89-03-SUMMARY.md` when done
</output>
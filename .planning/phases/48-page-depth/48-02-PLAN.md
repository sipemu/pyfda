---
phase: 48-page-depth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regression/concurrent-regression.md
  - docs/represent/interpolation.md
  - docs/represent/imputation.md
  - docs/analyze/scoring-metrics.md
  - docs/analyze/functional-statistics.md
  - docs/align/banded-alignment.md
  - docs/align/shift-registration.md
autonomous: true
requirements: [DEPTH-01, DEPTH-02]
estimate:
  tokens: 75000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "docs/regression/concurrent-regression.md (DEPTH-01) reaches mature structure — its bandwidth/kernel selection caveats/interpretation are present; params/returns tables remain byte-accurate to the shipped concurrent_regression binding."
    - "docs/represent/interpolation.md (DEPTH-02) gains an interpolation-vs-smoothing comparison; docs/represent/imputation.md (DEPTH-02) gains an MCAR-vs-MAR assumptions caveat."
    - "docs/analyze/scoring-metrics.md (DEPTH-02) gains metric-selection-by-use-case guidance and a metric comparison table; docs/analyze/functional-statistics.md (DEPTH-02) gains a small-n covariance-bias caveat and depth-median-vs-geometric-median guidance."
    - "docs/align/banded-alignment.md (DEPTH-02) states the Sakoe-Chiba O(m*B) vs O(m^2) justification and band_frac caveats for long-range phase shifts; docs/align/shift-registration.md (DEPTH-02) gives interpretation thresholds for the quality scores and a comparison with landmark registration."
    - "NO fence is added or modified in this plan — the existing FDARS_FENCE_OK fences on these seven pages are left byte-identical; changes are prose/params/caveats/comparisons only."
    - "No diagram or SVG is touched — the git diff for this plan lists only the seven .md pages."
  artifacts:
    - docs/regression/concurrent-regression.md
    - docs/represent/interpolation.md
    - docs/represent/imputation.md
    - docs/analyze/scoring-metrics.md
    - docs/analyze/functional-statistics.md
    - docs/align/banded-alignment.md
    - docs/align/shift-registration.md
  key_links:
    - "Every params/caveat statement is grounded in the shipped binding named in each task's <read_first> (src/*_mod.rs / python/fdars/*) — no invented parameters, no scope reduction."
---

<objective>
Bring the seven page-depth targets whose gap is prose/params/caveats/comparison (NOT a new worked example) to mature structure. These pages already carry a working FDARS_FENCE_OK fence, so DEPTH-03 says REUSE the existing fence — this plan adds only the missing structural sections identified in 42-AUDIT.md section 4 and 48-CONTEXT.md, keeping every fence byte-identical.

Purpose: full-parity structure (D: every page reaches intro/theory -> method -> worked example -> parameters -> caveats/interpretation/comparison) without inflating the build (no new fences here).
Output: seven extended .md pages; no fence changed; no SVG changed; params method-accurate against the shipped bindings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-page-depth/48-CONTEXT.md

# These seven pages are already fairly mature (params tables + caveats + a working fence).
# The remaining gaps are the specific missing sections in 42-AUDIT.md section 4 / 48-CONTEXT.md.
# DO NOT add or edit any ```python exec ...``` fence on these pages — reuse them as-is.
# Method-accuracy: every parameter/caveat must match the shipped binding (read the code).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: regression + represent prose depth — concurrent-regression, interpolation, imputation</name>
  <files>docs/regression/concurrent-regression.md, docs/represent/interpolation.md, docs/represent/imputation.md</files>
  <read_first>
    - docs/regression/concurrent-regression.md — ALREADY has Theory, a Parameters table (predictors/response/argvals/bandwidth/kernel), a Returns table, a beta_curve-shape admonition, and a working fence. Gap per 48-CONTEXT.md/42-AUDIT.md: caveats/interpretation on how bandwidth trades bias vs variance and how the kernel choice matters; extended discussion beyond the single equation.
    - docs/represent/interpolation.md — ALREADY has policy table, params table, caveats admonitions, and a growth-data fence. Gap: a COMPARISON WITH SMOOTHING (interpolation passes through observed points exactly vs smoothing which denoises; when to prefer each; caveats on aliasing / oscillation risk with high-degree splines).
    - docs/represent/imputation.md — ALREADY has an ImputationMethod strategy table, params table, a Recommendations table, and a growth-data fence. Gap: an MCAR-vs-MAR assumptions caveat (mean/linear imputation is unbiased under MCAR but can bias estimates under MAR/MNAR; state the assumption each strategy makes).
    - src/regression_mod.rs (concurrent_regression binding) and python/fdars/represent module for spline_interpolate_with_policy / impute_missing_values — confirm parameter names/defaults BEFORE writing any params claim; the tables already present must remain byte-accurate.
  </read_first>
  <action>
    concurrent-regression.md: add a caveats/interpretation subsection covering bandwidth selection (small bandwidth -> wiggly, high-variance beta(t); large bandwidth -> oversmoothed, biased; there is no built-in CV so the user picks it) and kernel choice (gaussian smooth/global vs epanechnikov/tricube compact-support), plus a note that the model is local-at-each-t (varying-coefficient) not a global fit. Do NOT change the Parameters/Returns tables or the fence.
    interpolation.md: add a "## Interpolation vs smoothing" comparison section — interpolation reproduces observed values exactly (order-k B-spline through the points) whereas smoothing (e.g. fdars.basis pspline) trades fidelity for noise reduction; give a short decision rule and a caveat on oscillation / aliasing risk with high B-spline order on noisy or unevenly-spaced data. Cross-link the smoothing page. Do NOT change the params table or the fence.
    imputation.md: add an MCAR-vs-MAR caveat admonition — mean and linear imputation are unbiased only under Missing Completely At Random; under MAR/MNAR they can distort downstream covariance/FPCA; recommend treating imputation as a preprocessing convenience, not a statistical model, and validating on a held-out mask when possible. Do NOT change the strategy/params/recommendations tables or the fence.
    For all three: keep every ```python exec``` fence byte-identical and do not touch any ![...](../assets/diagrams/*.svg) line.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; grep -qi 'bandwidth' docs/regression/concurrent-regression.md &amp;&amp; grep -qi 'smoothing' docs/represent/interpolation.md &amp;&amp; grep -Eqi 'MCAR|MAR' docs/represent/imputation.md &amp;&amp; git diff --name-only -- docs | grep -vq '\.svg$'</automated>
  </verify>
  <done>concurrent-regression.md has bandwidth/kernel caveats; interpolation.md has an interpolation-vs-smoothing comparison; imputation.md has an MCAR/MAR caveat; all fences byte-identical; no SVG touched.</done>
  <reversibility rating="reversible">Doc-only prose additions; revert by git checkout.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: analyze prose depth — scoring-metrics, functional-statistics</name>
  <files>docs/analyze/scoring-metrics.md, docs/analyze/functional-statistics.md</files>
  <read_first>
    - docs/analyze/scoring-metrics.md — ALREADY has the five-metric definitions, MAPE/MSLE danger admonitions, a uniform-signature params table, a Tecator fence, and a "Choosing the right metric" note. Gap per 48-CONTEXT.md: sharpen metric-selection-by-use-case guidance and add a comparison table of the five metrics (units, domain restriction, robustness, when to use).
    - docs/analyze/functional-statistics.md — ALREADY has variance/std/covariance/depth-median/trim-mean sections, an O(n*m^2) covariance perf warning, a mean-vs-depth-median-vs-geometric-median comparison table, and a Canadian-weather fence. Gap: a small-n covariance-BIAS caveat (distinct from the existing performance warning) and explicit guidance on when depth-median vs geometric-median is preferred.
    - src/... bindings for the fdars.scoring functions and fdars.fdata functional_covariance/depth_based_median/geometric_median_1d — confirm the returned shapes/names already tabled remain accurate; do not invent parameters.
  </read_first>
  <action>
    scoring-metrics.md: add a comparison table of the five metrics with columns [metric, units, domain restriction, outlier robustness, typical use] and expand the "Choosing the right metric" guidance into use-case rows (forecasting positive counts -> msle; zero-crossing residual curves -> mae/mse; relative error on strictly positive data -> mape; variance-explained reporting -> explained_variance). Reinforce the functional_mape |y_true|!=0 requirement in the comparison (it is already documented as a danger admonition; the comparison table must not contradict it). Do NOT change the params table or the fence.
    functional-statistics.md: add a small-n covariance-bias caveat admonition — with few curves (small n) the m x m sample covariance surface is high-variance and near-singular, so downstream FPCA eigenvalues/eigenfunctions are unstable; recommend n well above the number of retained components and/or grid subsampling (this is a statistical-bias caveat, distinct from the existing O(n*m^2) performance note). Add guidance on depth-median vs geometric-median: prefer depth_based_median when you need an actual observed representative curve and robustness to shape outliers; prefer geometric_median_1d when a synthetic L2-central curve is acceptable and you want the Weiszfeld minimiser. Do NOT change the API-summary/params tables or the fence.
    For both: keep every ```python exec``` fence byte-identical and do not touch any diagram image line.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; grep -Eqi 'use case|use-case|when to use|typical use' docs/analyze/scoring-metrics.md &amp;&amp; grep -Eqi 'small.?n|small sample|bias' docs/analyze/functional-statistics.md &amp;&amp; grep -qi 'geometric' docs/analyze/functional-statistics.md &amp;&amp; git diff --name-only -- docs | grep -vq '\.svg$'</automated>
  </verify>
  <done>scoring-metrics.md has a metric comparison table + use-case selection guidance; functional-statistics.md has a small-n covariance-bias caveat + depth-vs-geometric-median guidance; all fences byte-identical; no SVG touched.</done>
  <reversibility rating="reversible">Doc-only prose additions.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: align prose depth — banded-alignment, shift-registration</name>
  <files>docs/align/banded-alignment.md, docs/align/shift-registration.md</files>
  <read_first>
    - docs/align/banded-alignment.md — ALREADY has a "How it works" section stating the Sakoe-Chiba constraint |i-j|<=B and O(m*B), a band_frac guideline table, an API reference, a Canadian-weather fence, a Speed/accuracy tradeoff section, and a See-also block. Gap per 42-AUDIT.md section 4: make the O(m*B) vs O(m^2) JUSTIFICATION explicit (why the band cuts cost) and add caveats on band_frac selection when the signal has LONG-RANGE phase shifts (a narrow band clips the true warp path -> biased mean; widen or go unbanded).
    - docs/align/shift-registration.md — ALREADY has How-it-works, an API reference, a quality-scores table (least_squares_score / pairwise_correlation_score / sobolev_least_squares_score), a Canadian-weather fence, a rigid-vs-elastic math note, and a See-also block. Gap: explicit INTERPRETATION THRESHOLDS for the quality scores (e.g. pairwise_corr_score above ~0.9 = well aligned / rigid suffices, below ~0.7 = phase not rigid, try elastic) and a COMPARISON WITH LANDMARK registration (feature-based piecewise-linear warp vs rigid shift; when each applies).
    - src/alignment_mod.rs — confirm the shipped names karcher_mean_with_band / elastic_self_distance_matrix_with_band / least_squares_shift_registration / least_squares_score / pairwise_correlation_score / sobolev_least_squares_score match what the pages already reference; do not rename or invent.
  </read_first>
  <action>
    banded-alignment.md: add an explicit complexity justification — the full DP fills all m^2 cells (O(m^2) per pair); the Sakoe-Chiba band evaluates only the 2B+1-wide diagonal corridor, i.e. ~m*(2B+1) = O(m*B) cells, so for B = band_frac*m the work scales linearly in band width; state the assumption that the optimal monotone warp stays within |i-j|<=B for smooth data. Add a caveat: if the data has LONG-RANGE phase shifts (a feature displaced by more than B grid points), a narrow band clips the true warp path and biases the Karcher mean — widen band_frac or use band_frac=None. Do NOT change the API/keys tables or the fence.
    shift-registration.md: add interpretation-threshold guidance for the quality scores — pairwise_correlation_score near/above ~0.9 indicates rigid shifts explain the phase spread (elastic not needed), ~0.7-0.9 is a grey zone, below ~0.7 indicates non-rigid phase variation (move to karcher_mean); least_squares_score and sobolev score are lower-is-better and only comparable against a same-data reference. Add a comparison with landmark registration (register): landmark uses identified features and a monotone piecewise-linear warp between them — more flexible than a rigid shift, less general than full elastic; use it when clear landmarks exist. Cross-link landmark-registration.md. Do NOT change the API/scores tables or the fence.
    For both: keep every ```python exec``` fence byte-identical and do not touch any diagram image line.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda &amp;&amp; grep -Eqi 'O\(m|long-range|long range' docs/align/banded-alignment.md &amp;&amp; grep -Eqi 'landmark' docs/align/shift-registration.md &amp;&amp; grep -Eqi '0\.7|0\.9|threshold' docs/align/shift-registration.md &amp;&amp; git diff --name-only -- docs | grep -vq '\.svg$'</automated>
  </verify>
  <done>banded-alignment.md states the O(m*B) vs O(m^2) justification and a long-range-phase-shift band_frac caveat; shift-registration.md gives quality-score interpretation thresholds and a landmark-registration comparison; all fences byte-identical; no SVG touched.</done>
  <reversibility rating="reversible">Doc-only prose additions.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited page prose -> reader | Only trust boundary is method-accuracy: prose/params must faithfully describe the shipped fdars method. No code executes that was not already present (fences unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-04 | Tampering | Method-inaccurate caveat/param added to a page | high | mitigate | Every added caveat/param grounded in the binding named in <read_first> (src/*_mod.rs / python/fdars/*); existing tables kept byte-accurate; no invented parameters. |
| T-48-05 | Tampering | Accidental fence edit inflating build or breaking a fence | medium | mitigate | Explicit constraint: no ```python exec``` fence added or modified; verify asserts changes are prose-only and git diff excludes .svg. |
| T-48-06 | Tampering | Accidental SVG/diagram edit outside phase scope | high | mitigate | Each task's <done> and verify asserts no SVG touched; git diff lists only the seven .md pages. |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs — doc edits only; no legitimacy gate needed. |
</threat_model>

<verification>
- Each of the seven pages gains the specific missing section named in 48-CONTEXT.md / 42-AUDIT.md section 4 (verified by the per-task greps).
- No `python exec` fence is added or modified on any of the seven pages (existing FDARS_FENCE_OK fences left byte-identical); this plan runs no fence gate because no fence changed.
- `git diff --name-only docs/` for this plan lists only the seven `.md` pages and no `.svg`.
</verification>

<success_criteria>
All seven pages reach mature structure via targeted prose/params/caveats/comparison additions; every existing fence is byte-identical; additions are method-accurate against the shipped bindings; no diagram/SVG changed.
</success_criteria>

<output>
Create `.planning/phases/48-page-depth/48-02-SUMMARY.md` when done. In the SUMMARY, confirm each page reused its existing fence (no new fence) and surface any genuine method-accuracy ambiguity found against the bindings.
</output>

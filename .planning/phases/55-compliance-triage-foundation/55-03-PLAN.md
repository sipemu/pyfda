---
phase: 55-compliance-triage-foundation
plan: 03
type: execute
wave: 3
depends_on: [55-02]
files_modified:
  - python/fdars/sklearn/_coverage.py
  - tests/sklearn/test_coverage.py
  - tests/sklearn/test_go_no_go.py
autonomous: true
requirements: [TRIAGE-02, TRIAGE-03]
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "_coverage.py TRIAGE_VERDICTS records a PASS / PASS-WITH-FIXES / EXCLUDE verdict for every candidate estimator, derived from triage_results.txt (TRIAGE-01, TRIAGE-02)"
    - "_coverage.py EXCLUDED_METHODS lists every excluded fdars method with a reason code + the specific failing check name (TRIAGE-02)"
    - "Every excluded fdars method is confirmed still callable through the existing functional API (TRIAGE-02)"
    - "The go/no-go gate confirms a viable core PASSes: >=1 FPCA, 2 smoothers, 2 regressors, 2 classifiers, 1 clusterer, 2 outlier detectors (TRIAGE-03)"
  artifacts:
    - "python/fdars/sklearn/_coverage.py fully populated (EXCLUDED_METHODS + TRIAGE_VERDICTS)"
    - "tests/sklearn/test_coverage.py (registry integrity + excluded-still-callable checks)"
    - "tests/sklearn/test_go_no_go.py (viable-core gate assertion)"
  key_links:
    - "triage_results.txt -> per-estimator verdict -> TRIAGE_VERDICTS"
    - "EXCLUDE verdict -> EXCLUDED_METHODS entry with failing_check + functional_api"
    - "EXCLUDED_METHODS[m].functional_api -> importable/callable fdars.* symbol"
    - "TRIAGE_VERDICTS PASS members -> viable-core count gate"
---

<objective>
Close the triage: turn the raw `triage_results.txt` from Plan 02 into recorded verdicts. Populate `_coverage.py` `TRIAGE_VERDICTS` (PASS / PASS-WITH-FIXES / EXCLUDE per estimator) and `EXCLUDED_METHODS` (reason-coded, with the specific failing check), verify every excluded method is still callable through the functional API, and assert the go/no-go viable-core gate — the milestone-blocking decision that a viable core PASSes before Phase 56 family implementation begins.

Purpose: This is the definitive scope decision under the no-exemptions rule. Its output (`_coverage.py`) is the contract Phases 56-58 build against and Phase 59 documents.

Output: fully populated `_coverage.py`; a registry-integrity test; the go/no-go gate test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-compliance-triage-foundation/55-RESEARCH.md
@.planning/phases/55-compliance-triage-foundation/55-02-SUMMARY.md

# The registry scaffold + raw verdict data to consume:
@python/fdars/sklearn/_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate TRIAGE_VERDICTS + EXCLUDED_METHODS from triage_results.txt</name>
  <files>python/fdars/sklearn/_coverage.py</files>
  <read_first>
    - triage_results.txt (from Plan 02 — the raw per-check PASS/FAIL data)
    - python/fdars/sklearn/_coverage.py (Plan 01 scaffold — EXCLUDED_METHODS pre-seeds + empty TRIAGE_VERDICTS)
    - 55-RESEARCH.md "Pattern 5" (_coverage.py format), "Pre-Excluded" table, verdict-assignment rule
  </read_first>
  <action>
    Read `triage_results.txt` and apply the RESEARCH verdict rule per estimator (TRIAGE-01, TRIAGE-02): all checks PASS -> `PASS`; failures only from fixable guards (1-sample message, float cast, feature-names) -> `PASS-WITH-FIXES` (append the fix summary); failures from structural incompatibility -> `EXCLUDE`. Populate `TRIAGE_VERDICTS` in `_coverage.py` with one entry per candidate estimator, verdict string as value (include the fix summary or failing-check note in the string).

    For every EXCLUDE verdict AND every structural pre-exclude, add/confirm an `EXCLUDED_METHODS` entry keyed by the fdars method path, value `{"reason": <reason code>, "failing_check": <specific check name from triage_results.txt or None for pre-excludes>, "functional_api": <the fdars.* callable that still exposes it>}` (TRIAGE-02). Use the RESEARCH "Pre-Excluded" reason codes (ORDER_SENSITIVE, IRREGULAR_INPUT, RESPONSE_DOMAIN, NON_STANDARD_INPUT, NON_STANDARD_OUTPUT, HYPERPARAMETER_SEARCH, NOT_AN_ESTIMATOR, SEQUENTIAL_STREAMING) plus empirical codes for triage-discovered excludes. Do NOT invent verdicts — every verdict traces to a line in triage_results.txt. If the file is absent (sklearn not installed in the exec env), HALT and surface that the triage run from Plan 02 must complete first.
  </action>
  <verify>
    <automated>python -c "import ast; m=ast.parse(open('python/fdars/sklearn/_coverage.py').read()); print('parse-ok')" && python -c "import sklearn" 2>/dev/null && python -c "from fdars.sklearn._coverage import TRIAGE_VERDICTS, EXCLUDED_METHODS; assert len(TRIAGE_VERDICTS)>=20, f'only {len(TRIAGE_VERDICTS)} verdicts'; assert all(v.split(':')[0].split('-')[0].strip().upper() in {'PASS','EXCLUDE'} or 'PASS-WITH-FIXES' in v.upper() or v.upper().startswith('PASS') or v.upper().startswith('EXCLUDE') for v in TRIAGE_VERDICTS.values()), 'bad verdict string'; assert all(set(e)>={'reason','failing_check','functional_api'} for e in EXCLUDED_METHODS.values()), 'bad EXCLUDED_METHODS shape'; print(f'{len(TRIAGE_VERDICTS)} verdicts, {len(EXCLUDED_METHODS)} excluded')" || echo "sklearn absent — verdict population requires the Plan-02 triage run"</automated>
  </verify>
  <done>`TRIAGE_VERDICTS` has a verdict for every candidate (>=20) each starting PASS / PASS-WITH-FIXES / EXCLUDE; `EXCLUDED_METHODS` entries each carry `reason`, `failing_check`, and `functional_api`; every verdict traces to `triage_results.txt`.</done>
</task>

<task type="auto">
  <name>Task 2: Registry-integrity test — excluded methods still callable via functional API</name>
  <files>tests/sklearn/test_coverage.py</files>
  <read_first>
    - python/fdars/sklearn/_coverage.py (from Task 1 — the populated registry)
    - 55-RESEARCH.md TRIAGE-02 requirement ("excluded methods remain available through the existing functional API")
    - python/fdars/__init__.py — confirm the functional-API import surface (do NOT modify it)
  </read_first>
  <action>
    Create `tests/sklearn/test_coverage.py` (TRIAGE-02 verification). (a) Shape: assert every `EXCLUDED_METHODS` value has keys `reason`, `failing_check`, `functional_api`, non-empty `reason`. (b) Verdict domain: assert every `TRIAGE_VERDICTS` value string starts with one of `PASS`, `PASS-WITH-FIXES`, `EXCLUDE`. (c) Consistency: assert every estimator whose verdict is `EXCLUDE` has a corresponding `EXCLUDED_METHODS` entry (matched by the method it wraps). (d) Excluded-still-callable: for each `EXCLUDED_METHODS[m]["functional_api"]` dotted path (e.g. `fdars.inference.t_perm_test`, `fdars.pace_fpca.pace_fpca`), assert the symbol resolves via `importlib`/`getattr` from a plain `import fdars` and is `callable` — proving exclusion from the sklearn layer does NOT remove functional-API access. Do not call the functions with data; resolution + callable check is sufficient and fast.
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && python -m pytest tests/sklearn/test_coverage.py -x -q || python -m pytest tests/sklearn/test_coverage.py -q -rs 2>&1 | grep -qiE "skipped|passed"</automated>
  </verify>
  <done>With sklearn installed, `pytest tests/sklearn/test_coverage.py` passes: registry shape valid, verdict domain valid, EXCLUDE↔EXCLUDED_METHODS consistent, and every excluded method's `functional_api` symbol resolves + is callable from plain `import fdars`.</done>
</task>

<task type="auto">
  <name>Task 3: Go/no-go viable-core gate</name>
  <files>tests/sklearn/test_go_no_go.py</files>
  <read_first>
    - python/fdars/sklearn/_coverage.py (from Task 1 — TRIAGE_VERDICTS)
    - ROADMAP Phase 55 Success Criterion 5 + TRIAGE-03 — the exact viable-core composition
    - 55-01-SUMMARY.md / 55-02-SUMMARY.md — which estimators actually PASSed
  </read_first>
  <action>
    Create `tests/sklearn/test_go_no_go.py` (TRIAGE-03). Define the required viable core from the ROADMAP: >=1 FPCA transformer, >=2 smoothers, >=2 regressors, >=2 classifiers, >=1 clusterer, >=2 outlier detectors — all with a `PASS` or `PASS-WITH-FIXES` verdict in `TRIAGE_VERDICTS`. Map each candidate to its family, count PASS/PASS-WITH-FIXES members per family, and assert each family meets its minimum. On any shortfall, fail with a message naming the family and its short count — this is the milestone-blocking go/no-go signal that must be green before Phase 56 begins. Include an informational summary print of per-family PASS counts. Treat `PASS-WITH-FIXES` as viable (the fix is applied in the Phase 56-58 real implementation); only `EXCLUDE` does not count toward the core.
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && python -m pytest tests/sklearn/test_go_no_go.py -x -q || python -m pytest tests/sklearn/test_go_no_go.py -q -rs 2>&1 | grep -qiE "skipped|passed"</automated>
  </verify>
  <done>With sklearn installed, `pytest tests/sklearn/test_go_no_go.py` passes, confirming the viable core (>=1 FPCA, 2 smoothers, 2 regressors, 2 classifiers, 1 clusterer, 2 outlier detectors) has PASS/PASS-WITH-FIXES verdicts — the go signal for Phase 56. If any family falls short the test fails and names the gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triage_results.txt -> _coverage.py verdicts | Local build artifact consumed by a code-generation step; no external/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-03 | Repudiation | verdict provenance in _coverage.py | low | mitigate | Every TRIAGE_VERDICTS entry traces to a triage_results.txt line; test_coverage.py enforces registry shape + EXCLUDE↔EXCLUDED_METHODS consistency so no verdict is fabricated |
| T-55-04 | Information Disclosure | excluded method reachability | low | mitigate | test_coverage.py asserts each excluded method remains callable via functional API — prevents silent capability loss |

Note: no runtime input surface, no network, no new dependency. Pure declarative registry + tests. ai-integration / schema-gate / assumption-delta are N/A.
</threat_model>

<verification>
- `_coverage.py` verdict count >=20; every EXCLUDED_METHODS entry has reason/failing_check/functional_api
- `pytest tests/sklearn/test_coverage.py` green (registry integrity + excluded-still-callable)
- `pytest tests/sklearn/test_go_no_go.py` green (viable core confirmed)
- Full phase: `pytest tests/sklearn/ -q` green with sklearn installed; skips cleanly without it
</verification>

<success_criteria>
- Every candidate has a recorded PASS / PASS-WITH-FIXES / EXCLUDE verdict traced to the triage run
- EXCLUDED_METHODS is reason-coded and every excluded method is still callable via the functional API
- The go/no-go viable-core gate is green — the documented signal that Phase 56 may begin
</success_criteria>

<output>
Create `.planning/phases/55-compliance-triage-foundation/55-03-SUMMARY.md` when done. Include the final per-family PASS/PASS-WITH-FIXES/EXCLUDE tally, the full EXCLUDED_METHODS list with reasons, and an explicit GO / NO-GO statement for Phase 56.
</output>

---
phase: 55-compliance-triage-foundation
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - python/fdars/sklearn/_skeletons.py
  - tests/sklearn/test_triage.py
autonomous: true
requirements: [TRIAGE-01, FND-03, FND-04]
estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every remaining ~30 candidate estimator (all five families) has a skeleton in _skeletons.py that fit/transform/predict runs on a small array without an import-time or attribute error (TRIAGE-01)"
    - "All skeletons subclass _BaseFdarsEstimator, store constructor args verbatim, call fdars._native.* directly, and never construct an Fdata (FND-03, FND-04)"
    - "Classifier and FPC-regressor skeletons store X_fit_/y_fit_ and re-fit at predict time; kmeans skeleton maps random_state->seed; outlier skeletons implement score_samples + +1/-1 predict (FND-03)"
    - "test_triage.py _ALL_SKELETONS lists every skeleton and parametrize_with_checks produces a per-check PASS/FAIL result for each (TRIAGE-01)"
  artifacts:
    - "python/fdars/sklearn/_skeletons.py expanded to all ~31 candidate classes"
    - "tests/sklearn/test_triage.py with _ALL_SKELETONS covering every family"
    - "triage_results.txt (captured pytest -v output — the raw verdict data for Plan 03)"
  key_links:
    - "each skeleton.fit -> _native.<family>.<fn>(validated float64 array, argvals_, ...)"
    - "classifier.predict -> _native.classification.fclassif_*(vstack(X_fit_, X_new), ...) sliced"
    - "FPCRegressor.predict -> _native.regression.predict_fregre_lm(X_fit_, y_fit_, X_new, n_comp)"
    - "parametrize_with_checks(_ALL_SKELETONS) -> triage_results.txt"
---

<objective>
Expansion from the Plan 01 tracer: skeleton the remaining ~30 candidate estimators across all five families (transformers, regressors, classifiers, clusterers, outlier detectors) into `_skeletons.py`, wire them all into the `test_triage.py` harness, and run the full `parametrize_with_checks` battery to capture per-check PASS/FAIL results. This is the discovery step — the raw verdict data (`triage_results.txt`) that Plan 03 turns into recorded verdicts and the `_coverage.py` registry.

Purpose: Under the no-exemptions rule, scope is DISCOVERED by triage, not assumed. This plan produces the empirical ground truth for every candidate.

Output: fully populated `_skeletons.py`; `test_triage.py` covering every candidate; `triage_results.txt` capturing the full battery output.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-compliance-triage-foundation/55-RESEARCH.md
@.planning/phases/55-compliance-triage-foundation/55-01-SUMMARY.md

# The base class + tracer estimator + harness this plan expands:
@python/fdars/sklearn/_base.py
@python/fdars/sklearn/_skeletons.py
@tests/sklearn/test_triage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skeleton the transformer + regressor families</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <behavior>
    - Each transformer skeleton fit()->transform() runs on a (6,10) float64 array without error
    - BSplineSmoother/LocalPolynomialSmoother transform loops per-row (nadaraya_watson is 1D)
    - FPCRegressor/PLSRegressor store X_fit_/y_fit_ and predict re-fits via predict_fregre_* 
    - float32 input is upcast; 1-sample input raises ValueError containing "1 sample"
  </behavior>
  <read_first>
    - 55-RESEARCH.md "Candidate Estimator List" Transformers (9) + Regressors (6) tables — exact native fn per class and predicted verdict
    - 55-RESEARCH.md "Pattern 2" (fit/transform skeleton), Pitfalls 1 (nadaraya_watson 1D), 3 (fregre re-fit), 4 (1-sample guard), 5 (dtype)
    - python/fdars/sklearn/_skeletons.py (FPCATransformer already present — reuse its shape)
  </read_first>
  <action>
    Add the transformer skeletons NOT already present (FPCATransformer exists from Plan 01) per the RESEARCH Transformers table: `BSplineSmoother`, `LocalPolynomialSmoother`, `BasisRepresentation`, `Imputer`, `SplineInterpolator`, `DepthTransformer`, `NormTransformer` — each `(TransformerMixin, _BaseFdarsEstimator)`, constructor args stored verbatim (FND-03), `fit` validates via `_validate(dtype="numeric")` then `.astype(np.float64)`, a 1-sample guard raising `ValueError` with the `1 sample` substring (Pitfall 4), argvals resolved via `_resolve_argvals` in fit only, native call via `from fdars import _native` (FND-04, never build Fdata). Smoothers loop per-row because `nadaraya_watson` is 1D (Pitfall 1). Do NOT wrap `PACEFPCATransformer` (structural EXCLUDE — IrregFdata input) — leave it out of skeletons.

    Add the regressor skeletons per the RESEARCH Regressors table: `FPCRegressor`, `PLSRegressor`, `RobustFPCRegressor` (method='l1'/'huber' param), `GLMRegressor` (Gaussian family only), `NonparametricRegressor` — each `(RegressorMixin, _BaseFdarsEstimator)` storing `X_fit_`/`y_fit_` in fit and re-fitting at predict via the corresponding `predict_fregre_*` native fn (Pitfall 3), 1-sample guard, `argvals_` resolved in fit. Do NOT wrap `FOSRRegressor` (structural EXCLUDE — 2D functional response). Note per RESEARCH: `fregre_lm` does NOT take argvals; `predict_fregre_lm(X_fit_, y_fit_, X_new, n_comp)` re-fits internally.
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && python -c "from fdars.sklearn._skeletons import BSplineSmoother, LocalPolynomialSmoother, BasisRepresentation, Imputer, SplineInterpolator, DepthTransformer, NormTransformer, FPCRegressor, PLSRegressor, RobustFPCRegressor, GLMRegressor, NonparametricRegressor; print('import-ok')" || python -c "import ast; ast.parse(open('python/fdars/sklearn/_skeletons.py').read()); print('parse-ok (sklearn absent)')"</automated>
  </verify>
  <done>All transformer + regressor skeleton classes import (sklearn present) or the module parses (sklearn absent); each stores args verbatim, calls `_native.*` directly, and has a 1-sample guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skeleton the classifier + clusterer + outlier-detector families</name>
  <files>python/fdars/sklearn/_skeletons.py</files>
  <behavior>
    - Each classifier fit()->predict() runs on a labeled (8,10) array without error
    - Classifiers LabelEncode y in fit, store X_fit_/y_fit_, re-fit at predict (fclassif_* combined)
    - FunctionalKMeans maps random_state->u64 seed via check_random_state; labels_ is int ndarray
    - Outlier detectors implement score_samples (continuous) + predict returning +1/-1 int array
  </behavior>
  <read_first>
    - 55-RESEARCH.md "Candidate Estimator List" Classifiers (6) + Clusterers (3) + Outlier Detectors (7) tables
    - 55-RESEARCH.md Pitfalls 2 (classifiers combine fit+predict, i64 labels), 6 (OutlierMixin +1/-1), 7 (kmeans seed vs random_state); "Don't Hand-Roll" (LabelEncoder, check_random_state)
  </read_first>
  <action>
    Add classifier skeletons per the RESEARCH Classifiers table: `FPCLDAClassifier`, `FPCQDAClassifier`, `FPCKNNClassifier` (k param), `DDClassifier`, `LogisticFPCClassifier` (binary; predict_proba), and `ElasticMultinomialClassifier` — each `(ClassifierMixin, _BaseFdarsEstimator)`. In `fit`: validate X+y, `LabelEncoder().fit_transform(y)`, store `self.classes_`, `self.X_fit_`, `self.y_fit_` (i64 labels — Pitfall 2), 1-sample/1-class guard. In `predict`: `check_is_fitted`, call the combined `_native.classification.fclassif_*` on `vstack([X_fit_, X_new])` and slice the last `len(X_new)` predictions, `inverse_transform` back to original labels. Include `ElasticMultinomialClassifier` as a skeleton even though predicted EXCLUDE — triage must confirm empirically (RESEARCH A3).

    Add clusterer skeletons per the RESEARCH Clusterers table: `FunctionalKMeans` (n_clusters, max_iter, tol, random_state), `FuzzyFunctionalCMeans` (fuzziness param), `FunctionalGMM` (n_clusters int internally passed as `k_range=[n_clusters]` per RESEARCH Open Question 1) — each `(ClusterMixin, _BaseFdarsEstimator)`. `fit` maps `check_random_state(self.random_state)` -> `int(rs.randint(0, 2**31))` u64 seed (Pitfall 7), calls `_native.clustering.*`, stores `labels_` as an int ndarray. Include FunctionalGMM as a skeleton though predicted EXCLUDE — triage confirms (RESEARCH A4).

    Add outlier-detector skeletons per the RESEARCH Outlier Detectors table: `LRTOutlierDetector`, `OutliergramDetector`, `MagnitudeShapeDetector`, `TVDMSSDetector`, `MUODDetector`, `DepthgramDetector` — each `(OutlierMixin, _BaseFdarsEstimator)`. Implement `score_samples(X)` returning a continuous score (higher = more normal) synthesized from the native detector output, and `predict(X)` = `np.where(score_samples(X) >= 0, 1, -1).astype(np.int64)` (Pitfall 6). Include TVDMSS/MUOD/Depthgram as skeletons though predicted EXCLUDE — triage confirms whether a continuous score synthesis satisfies `check_outliers_train` (RESEARCH A5).
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && python -c "from fdars.sklearn._skeletons import FPCLDAClassifier, FPCQDAClassifier, FPCKNNClassifier, DDClassifier, LogisticFPCClassifier, ElasticMultinomialClassifier, FunctionalKMeans, FuzzyFunctionalCMeans, FunctionalGMM, LRTOutlierDetector, OutliergramDetector, MagnitudeShapeDetector, TVDMSSDetector, MUODDetector, DepthgramDetector; print('import-ok')" || python -c "import ast; ast.parse(open('python/fdars/sklearn/_skeletons.py').read()); print('parse-ok (sklearn absent)')"</automated>
  </verify>
  <done>All classifier, clusterer, and outlier-detector skeleton classes import (sklearn present) or the module parses (sklearn absent); classifiers use LabelEncoder + stored fit, kmeans maps random_state->seed, outlier detectors implement score_samples + +1/-1 predict.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all skeletons into the triage harness and run the full battery</name>
  <files>tests/sklearn/test_triage.py</files>
  <read_first>
    - tests/sklearn/test_triage.py (from Plan 01 — the parametrize_with_checks scaffold with FPCATransformer)
    - python/fdars/sklearn/_skeletons.py (from Tasks 1-2 — every skeleton to register)
    - 55-RESEARCH.md "Pattern 4" `_ALL_SKELETONS` list + verdict rule
  </read_first>
  <action>
    Expand `_ALL_SKELETONS` in `tests/sklearn/test_triage.py` from the single tracer FPCATransformer to every candidate skeleton across all five families (transformers, regressors, classifiers, clusterers, outlier detectors), constructing each with the minimal valid hyperparameters shown in RESEARCH Pattern 4 (e.g. `FPCATransformer(n_components=1)`, `FPCLDAClassifier(ncomp=1)`, `FPCKNNClassifier(ncomp=1, k=1)`, `FunctionalKMeans(n_clusters=2)`). Keep the `@parametrize_with_checks(_ALL_SKELETONS)` / `check(estimator)` body. Then run the full battery capturing per-check output to `triage_results.txt` at repo root (this is the raw verdict data Plan 03 reads — TRIAGE-01). The harness intentionally does NOT assert all-green: failures are expected and informative for EXCLUDE decisions. Use `-rN`/`--tb=short` so per-(estimator,check) PASS/FAIL lines are captured.
  </action>
  <verify>
    <automated>python -c "import sklearn" 2>/dev/null && { python -m pytest tests/sklearn/test_triage.py -v --tb=short -rA > triage_results.txt 2>&1 || true; grep -qE "test_sklearn_triage\[" triage_results.txt && python -c "import re; t=open('triage_results.txt').read(); n=len(set(re.findall(r'test_sklearn_triage\[([A-Za-z]+)', t))); assert n>=20, f'only {n} estimators in results'; print(f'{n} estimators triaged')"; } || python -c "import ast; ast.parse(open('tests/sklearn/test_triage.py').read()); print('parse-ok (sklearn absent — full run deferred to CI with [sklearn])')"</automated>
  </verify>
  <done>`_ALL_SKELETONS` lists every candidate; running the harness produces `triage_results.txt` with per-check PASS/FAIL lines for >=20 distinct estimators (sklearn present), or the harness parses cleanly when sklearn is absent. The raw verdict data exists for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user code -> fdars.sklearn estimators | Untrusted numpy arrays + labels enter fit/predict; validated by sklearn `validate_data` before any native call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Tampering | array/label input to fit/predict across all skeletons | low | mitigate | `_validate(dtype="numeric")` + explicit `.astype(np.float64)`; LabelEncoder normalizes arbitrary y; 1-sample/1-class guards before native calls |
| T-55-02 | Denial of Service | per-curve smoother loops, re-fit-at-predict classifiers/regressors | low | accept | In-process compute over caller-supplied small arrays (check_estimator sizes ~40 obs); documented perf caveat in docstrings; no network |

Note: no new dependency beyond the Plan-01 `scikit-learn` extra. Pure-Python skeleton layer; ai-integration / schema-gate / assumption-delta are N/A.
</threat_model>

<verification>
- All ~31 candidate skeletons import with sklearn present (or module parses when absent)
- `triage_results.txt` contains per-check PASS/FAIL lines for >=20 distinct estimators
- No skeleton constructs an Fdata; all call `_native.*` directly
</verification>

<success_criteria>
- Every candidate estimator across all five families is skeletoned (structural pre-excludes deliberately omitted)
- The triage harness runs the full `parametrize_with_checks` battery and captures the raw verdict data
- Skeletons honor the base-class contract (verbatim storage, argvals_ in fit, float32->float64, stored-fit re-fit patterns, +1/-1 outlier predict)
</success_criteria>

<output>
Create `.planning/phases/55-compliance-triage-foundation/55-02-SUMMARY.md` when done. List every skeleton class written, note any candidate that failed to even import/run in the harness (a hard EXCLUDE signal), and reference `triage_results.txt` — Plan 03 consumes both.
</output>

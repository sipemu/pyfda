---
phase: 52-pipeline-diagnostic-report
plan: "03"
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - python/fdars/mcp/_pipeline.py
  - python/fdars/mcp/server.py
  - tests/test_mcp_pipeline_report.py
autonomous: true
requirements: [PIPE-04]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "fdars_build_pipeline_report MCP tool re-runs each stage via the existing runnable methods + build_diagnostics, aggregates by-reference (handles), and NEVER calls advise() (PIPE-04)"
    - "The new mcp/_pipeline.py module does not import or call advise() — provably LLM-free"
    - "Adding the tool is a guard-sync no-op: _RUNNABLE_METHODS and _DIAGNOSTICS_METHODS are unchanged"
    - "Arrays never cross the MCP boundary — only handles + scalar values are returned (by-reference invariant)"
  artifacts:
    - python/fdars/mcp/_pipeline.py
    - tests/test_mcp_pipeline_report.py
  key_links:
    - "server.py fdars_build_pipeline_report is a thin handler delegating to compare-style helper in mcp/_pipeline.py (Single Responsibility)"
    - "the helper delegates aggregation to advisor.build_pipeline_report(run_llm=False) — the offline core from Plan 01"
    - "a test_tool_never_imports_advise-style guard asserts the new module never imports advise"
---

<objective>
Expose the pipeline diagnostic report as an `fdars_build_pipeline_report` MCP tool that re-runs each stage via the existing runnable methods + `build_diagnostics`, aggregates by-reference via the Plan-01 offline core, and stays provably LLM-free — mirroring the Phase-51 `fdars_compare_methods` / `mcp/_compare_methods.py` pattern exactly.

Purpose: The MCP compute boundary must stay provably LLM-free (no new tool may call `advise()`). Adding this tool must be a guard-sync no-op (no new method slot). Arrays never cross the boundary (by-reference invariant).
Output: `mcp/_pipeline.py` helper, `fdars_build_pipeline_report` thin handler in `server.py`, offline test suite incl. an LLM-free guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-pipeline-diagnostic-report/52-CONTEXT.md

@python/fdars/mcp/_compare_methods.py
@python/fdars/mcp/server.py
@.planning/phases/51-comparative-method-selection/51-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds: `fdars_build_pipeline_report` MCP tool (thin handler in `server.py`), `compare`-style helper `build_pipeline_report_mcp` in the new `mcp/_pipeline.py`, and `tests/test_mcp_pipeline_report.py` including `test_tool_never_imports_advise` for the new module. Depends only on Plan 01's `build_pipeline_report(run_llm=False)` offline core (not on Plan 02's narrative).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_pipeline_report_mcp helper — by-reference, LLM-free (PIPE-04)</name>
  <files>python/fdars/mcp/_pipeline.py</files>
  <read_first>
    - python/fdars/mcp/_compare_methods.py — the exact analog: the Python-3.10 guard, `_ALLOWED_PARAMS` allowlist validation BEFORE any run, per-candidate `run_method` + `registry.store_result` + `build_diagnostics`, delegation to `compare_methods(run_llm=False)`, and the by-reference return (handles + scalar values only, no arrays); the deferred imports that keep it LLM-free
    - python/fdars/mcp/server.py — `_RUNNABLE_METHODS` frozenset (the six methods a stage can re-run), the by-reference invariant comments, and `_registry` usage
    - python/fdars/advisor/_pipeline.py — Plan 01 `build_pipeline_report(stages, *, argvals=None, run_llm=False)` signature and the stage-entry shape it expects
  </read_first>
  <behavior>
    - build_pipeline_report_mcp re-runs each declared stage via run_method (only _RUNNABLE_METHODS aspects), stores each raw result, builds diagnostics, and aggregates via build_pipeline_report(run_llm=False)
    - unknown per-stage param keys raise ValueError before any run (allowlist)
    - a stage aspect outside _RUNNABLE_METHODS raises ValueError naming the supported set
    - the return is by-reference: each stage entry exposes a stage name, aspect, and a result_id handle (no arrays)
    - the module imports and calls no advise()/provider/anthropic anywhere
  </behavior>
  <action>Create `python/fdars/mcp/_pipeline.py` mirroring `mcp/_compare_methods.py`. Add the `sys.version_info < (3, 10)` ImportError guard and `__all__ = ["build_pipeline_report_mcp"]`. Define `build_pipeline_report_mcp(dataset_id, stages, *, argvals_from_dataset=True)` where `stages` is a list of stage specs `[{"stage_name": str, "aspect": str, "params": dict}, ...]`. Reuse the `_ALLOWED_PARAMS = frozenset({"lambda_","n_basis","n_comp","k","seed"})` allowlist and validate every stage's params BEFORE any run (raise ValueError naming unknown keys). Validate each stage's aspect against `_RUNNABLE_METHODS` (import from `fdars.mcp.server` or `fdars.mcp._runner`) — raise ValueError naming the supported set for any non-runnable aspect. For each stage IN ORDER: `run_method(dataset_id, aspect, **params)` (deferred import), `registry.store_result(raw)` → result_id, `build_diagnostics(raw, aspect, argvals=argvals)`. Build the ordered stage-entry list for the offline core as precomputed-diagnostics stage entries (each carrying the built diagnostics dict, which has a "method" key so `build_pipeline_report` passes it through) and call `build_pipeline_report(stage_entries, argvals=argvals, run_llm=False)` (deferred import from `fdars.advisor`). Return a JSON-serialisable BY-REFERENCE dict: `{"report_id": <registry handle for the full aggregate>, "stages": [{"stage": name, "aspect": aspect, "result_id": result_id}, ...]}` — arrays stay in the registry (Anti-Pattern 4). Use deferred local imports for `run_method`, `registry`, `build_diagnostics`, `build_pipeline_report` so the module stays side-effect-free and LLM-free. NEVER import or call `advise`.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_pipeline_report.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_mcp_pipeline_report.py` exits 0
    - a stage aspect outside _RUNNABLE_METHODS raises ValueError naming the supported set
    - an unknown per-stage param key raises ValueError before any run
    - the return carries stage handles (result_id) not arrays
    - the aggregate is produced via build_pipeline_report(run_llm=False)
  </acceptance_criteria>
  <done>build_pipeline_report_mcp re-runs each runnable stage, aggregates by-reference via the offline core, validates params/aspects fail-closed, and imports/calls no LLM path.</done>
</task>

<task type="auto">
  <name>Task 2: fdars_build_pipeline_report thin MCP tool handler (PIPE-04)</name>
  <files>python/fdars/mcp/server.py</files>
  <read_first>
    - python/fdars/mcp/server.py — the `fdars_compare_methods` handler (the analog): the `@mcp.tool()` decorator, the tool-boundary method validation, the 3-line delegation to `compare_methods_mcp`, and the docstring noting the guard-sync no-op + no provider/model argument (provably LLM-free)
  </read_first>
  <action>In `python/fdars/mcp/server.py` add a new `@mcp.tool()` `fdars_build_pipeline_report(dataset_id: str, stages: list[dict]) -> dict` handler that delegates entirely to `from fdars.mcp._pipeline import build_pipeline_report_mcp` (deferred import inside the handler, mirroring `fdars_compare_methods`). Keep the handler thin (delegate; no aggregation logic inlined — Single Responsibility / Anti-Pattern 5). Expose NO provider/model argument (LLM-free boundary). Do NOT modify `_RUNNABLE_METHODS` or `_DIAGNOSTICS_METHODS` — adding this tool is a guard-sync no-op (T-51-12 analog). Annotate the `stages` param type precisely as `list[dict]` (mirror the WR-05 tightening lesson: annotate `candidate_params: list[dict]`, not a bare list). Update the module docstring tool list to add `fdars_build_pipeline_report`.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_pipeline_report.py -q -k "tool or handler or registered"</automated>
  </verify>
  <acceptance_criteria>
    - `fdars_build_pipeline_report` is registered as an MCP tool and callable in-process
    - the handler delegates to `build_pipeline_report_mcp` with no inlined aggregation logic
    - the tool exposes no provider/model argument
    - `_RUNNABLE_METHODS` and `_DIAGNOSTICS_METHODS` are unchanged (guard-sync no-op)
  </acceptance_criteria>
  <done>A thin fdars_build_pipeline_report MCP tool delegates to the by-reference helper with no LLM argument and no guard-set change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline MCP test suite + LLM-free guard + guard-sync no-op assertion</name>
  <files>tests/test_mcp_pipeline_report.py</files>
  <read_first>
    - tests/test_mcp_compare_methods.py — the analog suite: in-process Client tool call, by-reference return assertions (handles not arrays), `test_tool_never_imports_advise`, and the offline-no-api-key pattern
    - tests/test_mcp_server.py — `test_diagnostics_methods_match_advisor_supported` (the guard-sync drift lock) and how `_DIAGNOSTICS_METHODS` / `_RUNNABLE_METHODS` are asserted
  </read_first>
  <behavior>
    - calling fdars_build_pipeline_report over a small registered dataset with >=2 runnable stages returns a by-reference report (report_id + per-stage result_id handles, no arrays)
    - a non-runnable stage aspect raises ValueError naming the supported set
    - an unknown per-stage param key raises ValueError
    - test_tool_never_imports_advise: the mcp/_pipeline.py source has no advise import and importing it does not import advise
    - guard-sync no-op: _RUNNABLE_METHODS and _DIAGNOSTICS_METHODS membership is unchanged by adding the tool
    - the whole file runs offline with no ANTHROPIC_API_KEY
  </behavior>
  <action>Create `tests/test_mcp_pipeline_report.py` mirroring `tests/test_mcp_compare_methods.py`. Register a small synthetic dataset via `registry.store_dataset(data, argvals)` (Claude's discretion on the fixture). Assert: an in-process tool call with >=2 runnable stages (e.g. a smoothing stage then an fpca stage) returns `{"report_id": ..., "stages": [{stage, aspect, result_id}, ...]}` with NO array values crossing the boundary; a non-runnable aspect raises ValueError naming `_RUNNABLE_METHODS`; an unknown param key raises ValueError. Add `test_tool_never_imports_advise` that reads the `mcp/_pipeline.py` source and asserts no `advise` import at any indentation AND that importing the module leaves `advise` unimported (mirror the Phase-51 guard). Add a guard-sync assertion that `_RUNNABLE_METHODS` and `_DIAGNOSTICS_METHODS` are the same membership as before this phase (the drift-lock test in `tests/test_mcp_server.py` still passes — run it). Add an offline-no-api-key test. All tests must pass without a running network or key.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_mcp_pipeline_report.py tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_mcp_pipeline_report.py` exits 0
    - `test_tool_never_imports_advise` passes (the new module never imports advise)
    - `tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported` still passes (guard-sync no-op)
    - by-reference: the tool return carries handles, not arrays
    - full suite (`.venv/bin/python -m pytest -q`) shows no regressions
  </acceptance_criteria>
  <done>The MCP tool is exercised offline and by-reference, provably never imports advise, and adding it leaves the guard sets unchanged; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool handler | untrusted dataset_id / stages / params cross the tool boundary into fdars runs |
| tool return → MCP client | results cross back out; arrays must NOT leak (by-reference invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-08 | Elevation of Privilege | an LLM entering the MCP compute path | high | mitigate | `mcp/_pipeline.py` never imports/calls `advise`; `test_tool_never_imports_advise` enforces it; tool exposes no provider/model arg (PIPE-04) |
| T-52-09 | Tampering | arbitrary param/aspect strings reaching fdars runs | high | mitigate | allowlist-validate params + validate aspect against `_RUNNABLE_METHODS` BEFORE any run (fail closed) |
| T-52-10 | Information Disclosure | arrays leaking across the MCP boundary | medium | mitigate | by-reference return — only handles + scalars; test asserts no array values |
| T-52-11 | Tampering | guard-sync drift from adding a tool | medium | mitigate | no change to `_RUNNABLE_METHODS`/`_DIAGNOSTICS_METHODS`; drift-lock test re-run in verify |
| T-52-SC | Tampering | npm/pip/cargo installs | high | accept | no new packages installed this plan (zero new runtime deps per RESEARCH.md); no install tasks present |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_mcp_pipeline_report.py -q` passes
- `tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported` passes (guard-sync no-op)
- `.venv/bin/python -m pytest -q` (full suite) shows no regressions
- `test_tool_never_imports_advise` passes for `mcp/_pipeline.py`
- tool return is by-reference (handles, no arrays); no provider/model argument on the tool
</verification>

<success_criteria>
`fdars_build_pipeline_report` re-runs each runnable stage, aggregates by-reference via the Plan-01 offline core, is provably LLM-free (never imports/calls advise), keeps the guard sets unchanged (no-op), and the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/52-pipeline-diagnostic-report/52-03-SUMMARY.md` when done.
</output>

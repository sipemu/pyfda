---
phase: 75-deepen-analyze-family-thin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/analyze/functional-time-series.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-02]
estimate:
  tokens: 64000
  raw_tokens: 42000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "functional-time-series.md has a '## When to use' decision section and a '## See also' block (DEPTH-02)"
    - "ftsm_update is documented with the correct arg order ftsm_update(data, new_curve, argvals, ncomp=3) — new_curve is the 2D second argument"
    - "functional_acf/functional_pacf use param max_lag (not lags), and their 4-key return dict (lags/acf/pacf/upper_band) is documented"
    - "spectral_density is documented as spectral_density(data, argvals, bandwidth=None) returning a full-spectrum dict — no freq= param anywhere"
    - "dpca is documented as dpca(data, argvals, ncomp=3, bandwidth=None, filter_lag=None) — no order= param anywhere"
    - "ftsm return dict documents the ar_models key; long_run_covariance default bandwidth is None (not 1.0)"
    - "Page carries >=3 note/tip/warning/info admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK when run offline under .venv"
  artifacts:
    - docs/analyze/functional-time-series.md
  key_links:
    - "Fences import from fdars.fts and run against the installed .venv package (no new bindings)"
    - "See also links resolve to existing docs/analyze/*.md pages (index, seasonal-analysis, functional-statistics, clustering)"
---

<objective>
TRACER: Bring `docs/analyze/functional-time-series.md` to full mature-page parity end-to-end (DEPTH-02) — one analyze-family page carried through every layer this phase touches (prose restructure to the UNNUMBERED mature analyze template, eight confirmed API-accuracy corrections, >=3 admonitions, >=3 runnable fences, >=1 exec'd `html="1"` figure, cross-refs, offline fence verification), verified, BEFORE the other four analyze pages follow the same proven pattern.

Purpose: Prove the whole per-page pattern on the page with the MOST confirmed API errors (eight) so any pattern problem surfaces after one page instead of five.
Output: A parity-grade `functional-time-series.md` whose exec fences all emit `FDARS_FENCE_OK` offline under `.venv`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new symbols, bindings, or files. This plan modifies exactly ONE existing tracked doc page: `docs/analyze/functional-time-series.md`. (The shared fence-runner `scripts/run_page_fences.py` already exists from Phase 74 and is used only by the verify step, not authored here.)
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Bring functional-time-series.md to full parity end-to-end (tracer)</name>
  <files>docs/analyze/functional-time-series.md</files>
  <read_first>
    - docs/analyze/functional-time-series.md (page to restructure — preserve the FTSM math intro, the diagram link, the correct ftsm/ftsm_forecast/stationarity_test tables, the existing runnable fence, and the three references)
    - docs/analyze/clustering.md (UNNUMBERED mature analyze template to mirror: intro, method-selector table near top, plain `## Title` method sections each with brief theory + non-exec syntax block + exec fence + Parameters/Returns + admonition, a parameter-selection section, a full-example section, `## See also`, `## References`)
    - docs/analyze/outlier-detection.md (second template cross-check — method-selector table placement and admonition style)
    - src/fts_mod.rs (source of truth for every fts signature/default/return-key on the page)
    - .planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md (Sections 2.1, 3.1, 4, 5, 6, 7, 8)
    - scripts/docs_fig.py (fig/render/fast helpers + brand palette for the html figure)
  </read_first>
  <action>
Restructure the page to mirror the UNNUMBERED mature analyze template (plain `## Title` headings, method-selector table near the top — NOT the numbered regression template), preserving correct existing prose/examples and building around them. Deliver ALL of the following. (DEPTH-02)

CORRECT these eight confirmed API errors (all VERIFIED against src/fts_mod.rs per RESEARCH 2.1 / 3.1):
1. ftsm_update: page shows ftsm_update(data, argvals, new_obs, ncomp=3). Actual is ftsm_update(data, new_curve, argvals, ncomp=3) — new_curve is a 2D (k_new, m) array and is the SECOND argument, before argvals. Rewrite the Additional-Functions row/signature accordingly.
2. functional_acf / functional_pacf: page shows functional_acf(data, argvals, lags, seed=42). Actual is functional_acf(data, argvals, max_lag=None, n_sim=999, ci=0.95, seed=42) — the parameter is max_lag (there is NO lags parameter); add n_sim and ci. Same for functional_pacf.
3. functional_acf / functional_pacf return dict: document the 4-key return dict lags (int64 1D) / acf (1D) / pacf (1D) / upper_band (1D) — currently omitted entirely.
4. ftsm return dict: add the ar_models key. Full 7 keys: mean (m,), rotation (m, ncomp), scores (n, ncomp), fitted (n, m), weights (m,), ncomp (int), ar_models (list of dict{order, phi, sigma2}).
5. spectral_density: page shows spectral_density(data, argvals, freq) returning an unnamed array. Actual is spectral_density(data, argvals, bandwidth=None) returning a 6-key dict freqs (N,) / re (list of N (m,m) arrays) / im (list of N (m,m) arrays) / m / n_curves / bandwidth. It does NOT take a single frequency; it returns ALL Fourier frequencies. Remove every freq= usage; document indexing freqs/re/im to pick a frequency.
6. dpca: page shows dpca(data, argvals, ncomp=3, order=1). There is NO order parameter. Actual is dpca(data, argvals, ncomp=3, bandwidth=None, filter_lag=None) with a 7-key return dict (filters / scores / eigenvalues / n_freqs / filter_lag / ncomp / valid_range). dpca_reconstruct adds fitted_reconstruction (N-2L, m) and reconstruction_error (ncomp,).
7. long_run_covariance: page shows bandwidth=1.0 default. Actual default is bandwidth=None (auto-selects floor(N^(1/3))). Return keys cov_matrix (m,m) / m / bandwidth / n_curves.
8. Keep the existing ftsm / ftsm_forecast / stationarity_test tables (already correct) and the three references verbatim.

ADD these missing parity sections (mirroring the UNNUMBERED analyze template order):
- Keep the intro paragraphs and the existing concept-diagram image line.
- A method-selector table near the top (method | fdars function | key output) covering ftsm, ftsm_forecast, fplsr, functional_acf/functional_pacf, spectral_density/dpca, stationarity_test, long_run_covariance.
- A `## When to use` decision section: contrast FTSM (score-AR forecasting) vs DPCA (frequency-domain / dynamic dependence) vs fplsr (one-step PLS forecast); stationary vs non-stationary (difference the scores first — mention functional_difference is lag-1 only). Include a short "Start with ftsm ..." quick rule.
- Plain `## Title` method sections for the FTSM fit/forecast, functional ACF/PACF, spectral density / DPCA (frequency domain), and stationarity + long-run covariance — each with brief theory, a non-exec "what you'd call" syntax block, the corrected Parameters/Returns tables, and (where a fence lives) a runnable fence.
- A parameter-selection subsection: choosing ncomp (inspect weights from ftsm; larger ncomp captures more variance at the cost of noisier score-AR fits); choosing max_lag for ACF/PACF.
- A result-interpretation subsection: how to read ar_models (per-component AR order/coeffs), what upper_band means for ACF significance, how forecast rows map to future curves.
- An accurate "Methods available in R but not (yet) in Python" note covering ONLY the accurate items in RESEARCH 3.1 (functional cointegration testing not exposed; Hilbert-space AR(p) beyond ftsm auto-selection not exposed; functional_difference is lag-1 only — chain calls for higher orders).
- Keep the existing References; append a `## See also` bullet list linking (RESEARCH Section 5) seasonal-analysis.md, functional-statistics.md, clustering.md, and index.md (3-5 links). Do NOT link ../examples/functional-time-series.md — it does not exist until Phase 76.

FENCES (>=3 runnable, each ending in a line that prints the sentinel; >=1 with html="1"; keep fences small; deterministic np.random.default_rng(seed)):
- Keep/repair the existing ftsm + ftsm_forecast + stationarity_test fence; use n_perm=fast(999, 19) for the stationarity test so the strict build stays bounded, and print ar_models orders (RESEARCH 6.3 minimal-FTS pattern).
- Add ONE html="1" figure fence: the functional_acf ACF-bar plot with the 95% upper_band line (RESEARCH Section 8 "FTS: ACF plot fence"); use max_lag<=8 and n_sim=fast(999, 99); print render(f) then the sentinel.
- Add a real-data fence using load_canadian_weather() from docs_data, SUBSAMPLED to weekly means data[:, ::7] (~35 x 52) to bound fence time, running ftsm + ftsm_forecast(h=…) on it; print forecast shape and the sentinel. If the weekly-subsampled fence runs slower than a few seconds locally, fall back to a small synthetic series and note phoneme-style real data in a `!!! tip "Real data"` admonition instead (RESEARCH Open Question 1).
- Optionally add an fplsr or spectral_density/dpca fence if it clarifies a method; keep total fence count and runtime bounded.

ADMONITIONS (>=3, mixing types): add a `!!! warning` on the ftsm_update arg order (new_curve is second, 2D); a `!!! tip` on choosing ncomp / max_lag; a `!!! note` or `!!! info` clarifying spectral_density returns ALL Fourier frequencies (index freqs/re/im), and/or the stationarity workflow (difference scores when non-stationary). Ensure >=3 remain after restructuring.

Do NOT run the whole-site mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG. Do NOT introduce a freq= argument, an order= argument, or a lags= argument anywhere (none exist).
  </action>
  <acceptance_criteria>
    - page contains a `## When to use` heading (grep matches >= 1)
    - page contains a `## See also` heading (grep matches >= 1)
    - occurrences of the FDARS_FENCE_OK sentinel token in the page >= 3
    - lines matching the admonition prefix `!!! (note|tip|warning|info)` >= 3
    - at least one fence opener declares html="1"
    - the corrected ftsm_update signature `ftsm_update(data, new_curve, argvals` appears; and no `new_obs` token remains
    - `max_lag` appears and no `lags=` keyword-argument usage remains for functional_acf/functional_pacf (bare `lags` return-key mention is allowed)
    - `ar_models` appears in the page (ftsm return-key coverage)
    - no `freq=` argument on spectral_density and no `order=` argument on dpca remain (grep for `spectral_density([^)]*freq` and `dpca([^)]*order` each match 0, excluding comment/prose lines)
    - the shared fence runner reports every exec fence emitted the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/analyze/functional-time-series.md","utf8");const code=s.split("\n").filter(l=>!l.trimStart().startsWith("#")).join("\n");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,ftsm_update_fixed:/ftsm_update\(data,\s*new_curve,\s*argvals/.test(s),no_new_obs:!/new_obs/.test(s),has_max_lag:/max_lag/.test(s),no_lags_kwarg:!/functional_p?acf\([^)]*\blags\s*=/.test(code),has_ar_models:/ar_models/.test(s),no_freq_arg:!/spectral_density\([^)]*freq/.test(code),no_order_arg:!/dpca\([^)]*order/.test(code)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>The page is missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; the corrected `ftsm_update(data, new_curve, argvals` signature is absent or a `new_obs` token survives; `max_lag` absent or a `lags=` kwarg on functional_acf/pacf survives; `ar_models` absent; a `spectral_density(..., freq...)` or `dpca(..., order...)` call survives in code.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/analyze/functional-time-series.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK` (nonzero exit from run_page_fences.py, or a `FENCE ... FAILED` line).</fails_when>
  </verify>
  <done>functional-time-series.md mirrors the UNNUMBERED mature analyze template with `## When to use` + `## See also`, all eight confirmed API errors corrected (ftsm_update order, max_lag, ACF/PACF return dict, ar_models, spectral_density no-freq, dpca no-order, long_run_covariance default None), >=3 admonitions, >=3 sentinel fences (>=1 html figure), and every exec fence emits FDARS_FENCE_OK offline under `.venv`.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — editing one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with no external data or credentials (the only data read is the already-approved `docs/data/canadian_weather.csv`). No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner) passes: When-to-use + See-also present, >=3 sentinel fences, >=3 admonitions, >=1 html figure, all eight API corrections applied, no `freq=`/`order=`/`lags=` kwargs, no `new_obs`.
- Fence gate: `scripts/run_page_fences.py docs/analyze/functional-time-series.md` exits 0.
- Whole-site `mkdocs build --strict` is intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
functional-time-series.md is at mature-page parity (DEPTH-02) with all eight confirmed API corrections and every exec fence emitting FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/75-deepen-analyze-family-thin-pages/75-01-SUMMARY.md` when done.
</output>

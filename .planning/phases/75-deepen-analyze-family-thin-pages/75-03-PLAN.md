---
phase: 75-deepen-analyze-family-thin-pages
plan: 03
type: execute
wave: 3
depends_on: ["75-02"]
files_modified:
  - docs/analyze/multi-domain.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-02]
estimate:
  tokens: 60000
  raw_tokens: 39000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "multi-domain.md has a '## When to use' decision section and a '## See also' block (DEPTH-02)"
    - "dense_flmm and multi_famm are documented as taking plain numpy arrays (dense_flmm a 2D (n_total,m) array; multi_famm a list of 2D arrays), NOT a PyMultiFunData handle"
    - "The page states PyMultiFunData is a standalone container with no FAMM/MFPCA consumer in fdars-core 0.33; the 14-key dense_flmm and 4-key multi_famm return dicts are documented"
    - "Page carries >=3 note/tip/warning/info admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK when run offline under .venv"
  artifacts:
    - docs/analyze/multi-domain.md
  key_links:
    - "Fences import mfpca from fdars.spm and dense_flmm/multi_famm from fdars.famm and run against the installed .venv package"
    - "See also links resolve to existing pages (clustering.md, functional-statistics.md, outlier-detection.md, index.md)"
---

<objective>
Bring `docs/analyze/multi-domain.md` to full mature-page parity (DEPTH-02), following the proven Phase 75 pattern: restructure to the UNNUMBERED mature analyze template, correct the confirmed API errors (chiefly that `dense_flmm`/`multi_famm` take plain numpy arrays, NOT a `PyMultiFunData` handle), add >=3 admonitions, >=3 runnable fences (>=1 exec'd `html="1"` figure), cross-refs, and verify all fences offline under `.venv`.

Purpose: A reader can decide when MFPCA / FLMM / multi-FAMM apply, call them with the correct array inputs, and interpret their multi-variable output.
Output: A parity-grade `multi-domain.md` whose exec fences all emit `FDARS_FENCE_OK` offline under `.venv`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new symbols, bindings, or files. This plan modifies exactly ONE existing tracked doc page: `docs/analyze/multi-domain.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bring multi-domain.md to full parity</name>
  <files>docs/analyze/multi-domain.md</files>
  <read_first>
    - docs/analyze/multi-domain.md (page to restructure — preserve the MFPCA math intro, diagram link, the correct mfpca parameter/6-key return tables, the "Pass a list, not a stacked array" warning VERBATIM, the len(result['eigenvalues']) note VERBATIM, and the two references)
    - docs/analyze/clustering.md (UNNUMBERED mature analyze template to mirror)
    - docs/analyze/functional-time-series.md (the Phase 75 tracer — mirror its applied parity structure)
    - src/famm_mod.rs (source of truth for dense_flmm / multi_famm signatures and return dicts)
    - src/spm_mod.rs lines 881-948 (source of truth for mfpca signature and 6-key return dict; ncomp=5 default)
    - src/multi_fdata_mod.rs (source of truth for PyMultiFunData standalone-container status)
    - .planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md (Sections 2.3, 3.3, 4, 5, 6.3, 7 Pitfall 3, 8 "Multi-domain")
    - scripts/docs_fig.py (fig/render helpers for the html figure)
  </read_first>
  <action>
Restructure the page to mirror the UNNUMBERED mature analyze template (plain `## Title` headings, method-selector table near the top), preserving correct existing prose/examples. Deliver ALL of the following. (DEPTH-02)

CORRECT these confirmed API errors (all VERIFIED against src/famm_mod.rs, src/spm_mod.rs, src/multi_fdata_mod.rs per RESEARCH 2.3 / 3.3):
1. PyMultiFunData is NOT consumed by any FAMM/MFPCA function in fdars-core 0.33. Remove the claim that multi_fdata_from_components returns "a PyMultiFunData handle usable as input to dense_flmm and multi_famm". State that PyMultiFunData (fdars.multi_fdata, with .n_obs / .n_components accessors) is a STANDALONE container only — no function consumes it in 0.33.
2. dense_flmm: takes plain numpy arrays. Signature dense_flmm(data, subject_ids, covariates=None, ncomp=3, max_iter=50, tol=1e-10), where data is (n_total, m) all observations stacked and subject_ids is (n_total,) int64. It lives in fdars.famm (show the import path). Document the 14-key return dict (per RESEARCH 3.3): sigma2_eps, ncomp, n_subjects, n_iter, converged, mean_function (m,), random_variance (m,), sigma2_u (ncomp,), sigma2_slope (ncomp,), eigenvalues (ncomp,), beta_functions (p, m), random_effects (n_subjects, m), fitted (n_total, m), residuals (n_total, m).
3. multi_famm: takes a Python LIST of 2D numpy arrays, NOT a PyMultiFunData handle. Signature multi_famm(data_list, subject_ids, covariates=None, ncomp=3, max_iter=50, tol=1e-10); all arrays share n_total rows and m columns. Document the 4-key return dict: n_dims, stacked_fitted (n_total*n_dims, m), stacked_residuals (n_total*n_dims, m), components (list of dicts, each with the same 14 keys as dense_flmm).
4. Keep the correct mfpca (variables=list, ncomp=5, weighted=True) parameter table and 6-key return table (scores/eigenfunctions/eigenvalues/means/scales/grid_sizes), the "Pass a list, not a stacked array" warning, and the len(result['eigenvalues']) component-count note VERBATIM.

ADD these missing parity sections (mirroring the analyze template order):
- Keep the intro paragraphs and the concept-diagram image line.
- A method-selector table near the top (method | fdars function | key output) covering mfpca, dense_flmm, multi_famm (and multi_fdata_from_components as a container only).
- A `## When to use` decision section: MFPCA vs per-variable FPCA (when variables are co-observed and jointly varying, and when grids differ across variables); FLMM vs standard functional regression (longitudinal / repeated-measures random-effects designs); multi_famm when you have several co-observed functional dimensions per subject.
- Plain `## Title` method sections for MFPCA, dense FLMM, and multi-FAMM — each with brief theory, a non-exec "what you'd call" syntax block, corrected Parameters/Returns tables, and a runnable fence where one lives.
- A parameter-selection subsection: choosing ncomp in mfpca (inspect eigenvalues); a convergence note for dense_flmm (check converged; raise max_iter / loosen tol if False on small n — RESEARCH Open Question 4).
- A result-interpretation subsection: reading the eigenfunctions list (one loading array per variable), reading stacked_fitted / random_effects.
- An accurate "Methods available in R but not (yet) in Python" note covering ONLY RESEARCH 3.3 (the full multiFAMM tensor-product basis approach is not replicated; multi_famm uses per-dimension FLMM fitting).
- Keep the existing References; append a `## See also` bullet list (RESEARCH Section 5) linking clustering.md, functional-statistics.md, outlier-detection.md, and index.md (3-5 links).

FENCES (>=3 runnable, each printing the sentinel; >=1 with html="1"; small deterministic np.random.default_rng(seed)):
- An MFPCA fence over 2 synthetic functional variables with DIFFERENT grid lengths, passed as a list (RESEARCH 6.3 "Multi-domain MFPCA minimal fence"); print scores shape, per-variable eigenfunction shapes, and component count via len(eigenvalues).
- A dense_flmm fence on a small longitudinal design (e.g. 8 subjects x 3 visits = 24 stacked curves) with int64 subject_ids from np.repeat (RESEARCH 8 "Multi-domain: MFPCA + dense_flmm fence"); print n_subjects and converged. Add a fallback note in a `!!! note "Convergence"` admonition (raise max_iter / loosen tol if converged is False).
- A multi_famm fence passing a list of 2 stacked 2D arrays with shared subject_ids; print n_dims and the number of component dicts.
- ONE html="1" figure fence: an MFPCA scores scatter across 2 components (natural), OR overlay per-variable mean functions; print render(f) then the sentinel.

ADMONITIONS (>=3, mixing types): keep the "Pass a list, not a stacked array" warning; add a `!!! warning` that dense_flmm/multi_famm take plain numpy arrays (a list for multi_famm), NOT a PyMultiFunData handle; add a `!!! note "Convergence"` for dense_flmm; add a `!!! tip` on choosing ncomp via eigenvalues. Ensure >=3 remain after restructuring.

Do NOT run the whole-site mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG. Do NOT document PyMultiFunData as an input to any FAMM/MFPCA function.
  </action>
  <acceptance_criteria>
    - page contains a `## When to use` heading (grep matches >= 1)
    - page contains a `## See also` heading (grep matches >= 1)
    - occurrences of the FDARS_FENCE_OK sentinel token in the page >= 3
    - lines matching the admonition prefix `!!! (note|tip|warning|info)` >= 3
    - at least one fence opener declares html="1"
    - the page contains the standalone-container statement: token `standalone` appears near a PyMultiFunData mention
    - no code line passes a PyMultiFunData-style handle into dense_flmm/multi_famm: grep for `dense_flmm\([^)]*mfd` and `multi_famm\([^)]*mfd` each match 0 in code lines (and no `dense_flmm(multi_fdata` / `multi_famm(multi_fdata` call remains)
    - the multi_famm signature `multi_famm(data_list` and dense_flmm `dense_flmm(data, subject_ids` forms appear
    - the shared fence runner reports every exec fence emitted the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/analyze/multi-domain.md","utf8");const code=s.split("\n").filter(l=>!l.trimStart().startsWith("#")).join("\n");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const badHandle=(code.match(/(dense_flmm|multi_famm)\([^)]*(mfd|multi_fdata)/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,standalone_stmt:/standalone/i.test(s),no_handle_input:badHandle===0,multi_famm_list:/multi_famm\(data_list/.test(s),dense_flmm_arr:/dense_flmm\(data,\s*subject_ids/.test(s)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>The page is missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; no `standalone` statement about PyMultiFunData; a dense_flmm/multi_famm call passing a PyMultiFunData handle survives in code; or the corrected `multi_famm(data_list` / `dense_flmm(data, subject_ids` forms are absent.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/analyze/multi-domain.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK` (nonzero exit from run_page_fences.py, or a `FENCE ... FAILED` line).</fails_when>
  </verify>
  <done>multi-domain.md mirrors the UNNUMBERED mature analyze template with `## When to use` + `## See also`, dense_flmm/multi_famm documented as plain-array/list inputs (not PyMultiFunData), the standalone-container status stated, the 14-key and 4-key return dicts documented, >=3 admonitions, >=3 sentinel fences (>=1 html figure), and every exec fence emits FDARS_FENCE_OK offline under `.venv`.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — editing one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with synthetic in-fence data only. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes: When-to-use + See-also present, >=3 sentinel fences, >=3 admonitions, >=1 html figure, standalone-container statement present, no PyMultiFunData handle passed to FAMM functions, corrected signatures present.
- Fence gate: `scripts/run_page_fences.py docs/analyze/multi-domain.md` exits 0.
- Whole-site `mkdocs build --strict` is intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
multi-domain.md is at mature-page parity (DEPTH-02) with the FAMM input-type corrections applied and every exec fence emitting FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/75-deepen-analyze-family-thin-pages/75-03-SUMMARY.md` when done.
</output>

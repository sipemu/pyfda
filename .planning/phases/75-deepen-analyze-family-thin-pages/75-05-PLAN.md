---
phase: 75-deepen-analyze-family-thin-pages
plan: 05
type: execute
wave: 5
depends_on: ["75-04"]
files_modified:
  - docs/analyze/advanced-clustering.md
autonomous: true
use_worktrees: false
requirements: [DEPTH-02]
estimate:
  tokens: 60000
  raw_tokens: 39000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "advanced-clustering.md has a '## When to use' decision section and a '## See also' block (DEPTH-02)"
    - "funfem_cluster full signature (ncomp=10, p_disc=0, ...) and 6-key return dict are documented; align_cluster_fd full signature (use_amplitude_only=True, ...) and 5-key return dict are documented"
    - "KCFC reconstruction_errors and FunFEM membership interpretation are documented; dbscan_fd cluster dtype int64 (-1=noise) noted"
    - "Page carries >=3 note/tip/warning/info admonitions and >=3 runnable FDARS_FENCE_OK fences, >=1 with html=1"
    - "Every exec fence emits FDARS_FENCE_OK when run offline under .venv"
  artifacts:
    - docs/analyze/advanced-clustering.md
  key_links:
    - "Fences import from fdars.clustering and run against the installed .venv package (no new bindings)"
    - "See also links resolve to existing pages (clustering.md, gmm-clustering.md, elastic-clustering.md, outlier-detection.md, index.md)"
---

<objective>
Bring `docs/analyze/advanced-clustering.md` to full mature-page parity (DEPTH-02), completing the Phase 75 analyze-family sweep with the proven pattern: restructure to the UNNUMBERED mature analyze template, document the confirmed `funfem_cluster` / `align_cluster_fd` full signatures and return dicts, add parameter-selection and interpretation guidance, >=3 admonitions, >=3 runnable fences (>=1 exec'd `html="1"` figure), cross-refs, and verify all fences offline under `.venv`.

Purpose: A reader can choose among DBSCAN-FD / KCFC / FunFEM / align-and-cluster, parameterize each, and interpret their diagnostics.
Output: A parity-grade `advanced-clustering.md` whose exec fences all emit `FDARS_FENCE_OK` offline under `.venv`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new symbols, bindings, or files. This plan modifies exactly ONE existing tracked doc page: `docs/analyze/advanced-clustering.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bring advanced-clustering.md to full parity</name>
  <files>docs/analyze/advanced-clustering.md</files>
  <read_first>
    - docs/analyze/advanced-clustering.md (page to restructure — preserve the four-method intro, diagram link, the correct dbscan_fd and kcfc_cluster full API tables, the existing runnable fence, and the three references)
    - docs/analyze/clustering.md (UNNUMBERED mature analyze template to mirror — and the baseline k-means/fuzzy-c-means page this cross-refs)
    - docs/analyze/functional-time-series.md (the Phase 75 tracer — mirror its applied parity structure)
    - src/clustering_mod.rs (source of truth for dbscan_fd / kcfc_cluster / funfem_cluster / align_cluster_fd signatures and return dicts)
    - .planning/phases/75-deepen-analyze-family-thin-pages/75-RESEARCH.md (Sections 2.5, 3.5, 4, 5, 6.3, 7 Pitfall 8, 8 "Advanced clustering")
    - scripts/docs_fig.py (fig/render helpers for the html figure)
  </read_first>
  <action>
Restructure the page to mirror the UNNUMBERED mature analyze template (plain `## Title` headings, method-selector table near the top), preserving correct existing prose/examples. Deliver ALL of the following. (DEPTH-02)

CORRECT / COMPLETE these confirmed API items (all VERIFIED against src/clustering_mod.rs per RESEARCH 2.5 / 3.5):
1. funfem_cluster: the page gives description only. Document the full signature funfem_cluster(data, argvals, k=2, ncomp=10, p_disc=0, max_iter=50, tol=1e-6, seed=42) (note ncomp=10 default, NOT 3; p_disc=0 auto-selects min(k-1, ncomp_eff)) and the 6-key return dict: cluster (n,) int64, membership (n, k), disc_subspace (ncomp_eff, p_disc_eff), log_likelihood (float), iterations (int), converged (bool). membership rows sum to 1 (soft assignments).
2. align_cluster_fd: page says "returns templates (list of 1D arrays)" only. Document the full signature align_cluster_fd(data, argvals, k=2, max_iter=20, seed=42, use_amplitude_only=True, elastic_lambda=0.0, karcher_max_iter=15, karcher_tol=1e-4) (note use_amplitude_only=True default is not currently on the page) and the 5-key return dict: cluster (n,) int64, templates (list of k 1D arrays), distances (n, k), iterations (int), converged (bool).
3. dbscan_fd: cluster labels are int64 (-1 = noise, 0..k-1 = clusters) — add a dtype note. The distances (n, n) key is useful for eps selection.
4. kcfc_cluster: the reconstruction_errors (n, k) key is the KCFC-specific diagnostic (per-observation per-cluster FPCA reconstruction error) — it must appear in a worked example and be interpreted, not just listed.
5. Keep the correct dbscan_fd and kcfc_cluster full API tables and the three references verbatim.

ADD these missing parity sections (mirroring the analyze template order):
- Keep the intro paragraphs and the concept-diagram image line.
- A method-selector table near the top (method | fdars function | key output) covering dbscan_fd, kcfc_cluster, funfem_cluster, align_cluster_fd, plus a pointer to basic k-means on clustering.md.
- A `## When to use` decision section: DBSCAN-FD (density-based, discovers noise/outliers, no fixed k) vs KCFC (FPCA-reconstruction clustering) vs FunFEM (model-based, soft membership) vs align-and-cluster (phase-aligned / shape-invariant) vs basic k-means (clustering.md baseline).
- Plain `## Title` method sections for DBSCAN-FD, KCFC, FunFEM, and align-and-cluster — each with brief theory, a non-exec "what you'd call" syntax block, the (corrected/completed) Parameters/Returns tables, and a runnable fence where one lives.
- A parameter-selection subsection: eps selection for DBSCAN-FD using the distances matrix (k-NN distance elbow — RESEARCH 8 "DBSCAN eps selection fence"); choosing k / ncomp for KCFC and FunFEM.
- A result-interpretation subsection: KCFC reconstruction_errors as membership confidence; FunFEM membership as probabilistic soft assignments (contrast with KCFC hard labels); DBSCAN noise points (-1) as outlier candidates.
- Keep any accurate R-vs-Python note if one exists; do not invent one.
- Keep the existing References; append a `## See also` bullet list (RESEARCH Section 5) linking clustering.md, gmm-clustering.md, elastic-clustering.md, outlier-detection.md, and index.md (3-5 links).

FENCES (>=3 runnable, each printing the sentinel; >=1 with html="1"; small deterministic np.random.default_rng(seed)):
- Keep/repair the existing exec'd fence.
- A funfem_cluster fence (2-cluster synthetic sines/cosines, ncomp=5) that inspects membership soft assignments (RESEARCH 6.3 "Advanced clustering minimal fence"); print cluster labels and a membership row.
- A KCFC fence that prints and interprets reconstruction_errors (the primary KCFC diagnostic — currently unused on the page).
- A DBSCAN-FD eps-selection fence as the html="1" figure: the sorted k-NN distance curve from the distances matrix with the chosen eps line (RESEARCH 8 "Advanced clustering: DBSCAN eps selection fence"); print render(f) then the sentinel. (Optionally add an align_cluster_fd template-overlay fence if runtime allows; keep use_amplitude_only=True.)

ADMONITIONS (>=3, mixing types): a `!!! note` on dbscan_fd cluster dtype/-1 noise semantics; a `!!! tip` on eps selection from the distances matrix; a `!!! warning` that align_cluster_fd with use_amplitude_only=False (full elastic distance) is significantly slower — keep the default True for examples (RESEARCH Pitfall 8); optionally a `!!! info` contrasting FunFEM soft membership vs KCFC hard labels. Ensure >=3 remain after restructuring.

Do NOT run the whole-site mkdocs build --strict (deferred to Phase 79). Do NOT alter the concept SVG.
  </action>
  <acceptance_criteria>
    - page contains a `## When to use` heading (grep matches >= 1)
    - page contains a `## See also` heading (grep matches >= 1)
    - occurrences of the FDARS_FENCE_OK sentinel token in the page >= 3
    - lines matching the admonition prefix `!!! (note|tip|warning|info)` >= 3
    - at least one fence opener declares html="1"
    - the funfem_cluster full signature with `ncomp=10` and `p_disc` appears
    - the align_cluster_fd signature with `use_amplitude_only` appears
    - `reconstruction_errors` appears in a code line (KCFC diagnostic used in a fence)
    - `membership` appears in a code line (FunFEM soft-assignment inspected in a fence)
    - the shared fence runner reports every exec fence emitted the sentinel for this page
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const s=fs.readFileSync("docs/analyze/advanced-clustering.md","utf8");const code=s.split("\n").filter(l=>!l.trimStart().startsWith("#")).join("\n");const fences=(s.match(/FDARS_FENCE_OK/g)||[]).length;const adm=(s.match(/^!!! (note|tip|warning|info)/gm)||[]).length;const html=(s.match(/```python[^\n]*html="1"/g)||[]).length;const checks={when_to_use:/##\s+When to use/i.test(s),see_also:/##\s+See also/i.test(s),fences_ge3:fences>=3,adm_ge3:adm>=3,html_ge1:html>=1,funfem_sig:/funfem_cluster\([^)]*ncomp=10/.test(s)&&/p_disc/.test(s),align_sig:/align_cluster_fd\([^)]*use_amplitude_only/.test(s)||/use_amplitude_only/.test(s),recon_err_used:/reconstruction_errors/.test(code),membership_used:/membership/.test(code)};const fail=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(fail.length){console.error("FAIL: "+fail.join(", "));process.exit(1);}console.log("STRUCT OK fences="+fences+" adm="+adm+" html="+html);'</automated>
    <fails_when>The page is missing `## When to use` or `## See also`; fewer than 3 sentinel fences; fewer than 3 admonitions; no `html="1"` fence; funfem_cluster full signature (`ncomp=10` + `p_disc`) absent; align_cluster_fd `use_amplitude_only` absent; `reconstruction_errors` not used in a code line; or `membership` not used in a code line.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/python scripts/run_page_fences.py docs/analyze/advanced-clustering.md</automated>
    <fails_when>Any exec fence on the page fails to run offline under `.venv` or does not print `FDARS_FENCE_OK` (nonzero exit from run_page_fences.py, or a `FENCE ... FAILED` line).</fails_when>
  </verify>
  <done>advanced-clustering.md mirrors the UNNUMBERED mature analyze template with `## When to use` + `## See also`, funfem_cluster and align_cluster_fd full signatures/return dicts documented, KCFC reconstruction_errors and FunFEM membership used and interpreted in fences, >=3 admonitions, >=3 sentinel fences (>=1 html figure), and every exec fence emits FDARS_FENCE_OK offline under `.venv`.</done>
  <reversibility rating="reversible">Docs-only edit to one tracked page; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — editing one markdown page; no code, no network surface, no user-input handling. No new attack surface. Fences run offline under `.venv` with synthetic in-fence data only. No STRIDE categories apply.
</threat_model>

<verification>
- Structural gate (node one-liner) passes: When-to-use + See-also present, >=3 sentinel fences, >=3 admonitions, >=1 html figure, funfem_cluster/align_cluster_fd full signatures present, reconstruction_errors and membership used in code.
- Fence gate: `scripts/run_page_fences.py docs/analyze/advanced-clustering.md` exits 0.
- Whole-site `mkdocs build --strict` is intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
advanced-clustering.md is at mature-page parity (DEPTH-02) with funfem_cluster/align_cluster_fd fully documented and every exec fence emitting FDARS_FENCE_OK offline.
</success_criteria>

<output>
Create `.planning/phases/75-deepen-analyze-family-thin-pages/75-05-SUMMARY.md` when done.
</output>

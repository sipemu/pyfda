---
phase: 16-tool-mcp-server-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/advisor/mcp.md
autonomous: false
requirements: [MCPDOC-01, MCPDOC-02, MCPDOC-03]

estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The page lists all three tools (fdars_build_diagnostics, fdars_run_method, fdars_compare_run) and their roles, accurate against python/fdars/mcp/server.py (MCPDOC-01)"
    - "The page documents stdio setup via run_stdio() and the by-reference HandleRegistry model — arrays stay in the registry, tools exchange opaque handle ids (MCPDOC-02)"
    - "The page walks the agentic re-run/compare before/after loop with a concrete smoothing example matching _runner.py and _compare.py, mirroring examples/mcp_recipe.py (MCPDOC-03)"
    - "The page states fdars_run_method returns ONLY {result_id, method} (arrays never cross the tool boundary)"
    - "The docs build succeeds AND the page has NO executed exec=\"1\" fence — the build does not require the [mcp] extra or Python 3.10+"
  artifacts:
    - "docs/advisor/mcp.md"
  key_links:
    - "docs/advisor/mcp.md cross-links back to index.md and python-api.md, and forward-links agent-skill.md (annotated coming in Phase 17)"
    - "python/fdars/mcp/server.py is the source of truth for tool names, parameters, and return shapes"
---

<objective>
Author `docs/advisor/mcp.md`: a method-accurate MkDocs page documenting the
fdars-advisor MCP server — the three tools, stdio setup (`run_stdio`), the
by-reference `HandleRegistry` model, and a concrete re-run/compare before/after
loop mirroring `examples/mcp_recipe.py`. Covers MCPDOC-01, MCPDOC-02, MCPDOC-03.

Purpose: Give the published docs site first-class, provably-accurate coverage of
the shipped v2.0 MCP surface so a reader can understand and (if they install
`fdars[mcp]`) drive the tools.
Output: `docs/advisor/mcp.md`.

Artifacts this phase produces:
- `docs/advisor/mcp.md`

Constraints (from CONTEXT.md, non-negotiable):
- DOCUMENTATION-ONLY: no changes to `python/fdars/mcp/` or any advisor code.
- MCP fences are ILLUSTRATIVE (non-executed `python` fences). The page MUST NOT
  contain an executed `exec="1"` fence — the docs build must not require the
  `[mcp]` extra (`mcp>=2.0.0`) or Python 3.10+. (Contrast Phase 15's page, which
  has a genuine offline executed fence; MCP cannot run offline in the build.)
- All tool names, parameters, and return shapes MUST be verified against
  `server.py` / `_runner.py` / `_compare.py` before writing — do not invent
  signatures. In particular: `fdars_run_method` returns ONLY `{result_id, method}`;
  `fdars_compare_run` takes a flat `before_result_id` + after-params and returns
  a before/after `delta`.
- `mkdocs.yml` nav wiring is Phase 18, NOT this phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@python/fdars/mcp/server.py
@python/fdars/mcp/_registry.py
@python/fdars/mcp/_runner.py
@python/fdars/mcp/_compare.py
@examples/mcp_recipe.py
@docs/advisor/python-api.md
@docs/advisor/index.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Page skeleton + three-tool reference section that builds cleanly</name>
  <files>docs/advisor/mcp.md</files>
  <read_first>
    - python/fdars/mcp/server.py — the three @mcp.tool() handlers and their exact signatures/return shapes:
      fdars_build_diagnostics(dataset_id, method, result_id=None, with_argvals=True) -> diagnostics dict;
      fdars_run_method(dataset_id, method, lambda_=None, n_basis=None, n_comp=None, k=None, seed=None) -> {"result_id", "method"};
      fdars_compare_run(dataset_id, method, before_result_id, lambda_=None, n_basis=None, n_comp=None, k=None, seed=None) -> before/after/delta dict.
      Also the _SUPPORTED_METHODS frozenset {"alignment","fpca","basis","smoothing","clustering"} and the per-method param mapping in the fdars_run_method Notes section.
    - docs/advisor/python-api.md — match the page tone, heading structure (intro paragraph, tables for parameters, `---` section dividers), and the illustrative-fence warning-admonition pattern.
    - docs/advisor/index.md — the exact relative link filenames used for cross-linking (python-api.md, mcp.md, agent-skill.md).
  </read_first>
  <action>
    Create the new file docs/advisor/mcp.md as the tracer slice: a complete page skeleton PLUS the tool-reference section, wired end-to-end so `mkdocs build` renders it. Lead with a one-paragraph intro describing the MCP surface as the interpret to recommend to re-run to compare surface (the tool surface adds the re-run/compare loop the recommend-only Python API stops short of), and cross-link back to index.md (overview) and python-api.md.
    Add a top-of-page admonition (mirror the `!!! warning` block in python-api.md's "Illustrative advise() call" section) stating that all Python fences on this page are illustrative only — they require `pip install fdars[mcp]` (mcp>=2.0.0, Python 3.10+) and are NOT executed in the docs build. Do NOT add `exec="1"` or `html="1"` to any fence on this page; use plain ```python fences.
    Add the three-tool reference section. For each of fdars_build_diagnostics, fdars_run_method, fdars_compare_run: state its one-line role, its parameters (as a table matching the python-api.md style, one row per parameter with type/default/description), and its return shape — verbatim-accurate against server.py. Explicitly state that fdars_run_method returns ONLY {result_id, method} and that the raw arrays stay in the registry (never cross the tool boundary). State the supported method set (alignment, fpca, basis, smoothing, clustering) and the per-method parameter mapping (alignment→lambda_ default 0.0; fpca→n_comp default 3; basis→lambda_ default 1.0; smoothing→n_basis default 15; clustering→k default 3, seed default 42) exactly as documented in server.py's fdars_run_method Notes.
    Include a stubbed placeholder for the later sections (a "Handle model", "stdio setup", and "Re-run / compare loop" heading each with a one-line "expanded below" note) so the page structure is complete; Task 2 fills them. End with a forward-link to agent-skill.md annotated "coming in Phase 17". Do NOT wire mkdocs.yml (Phase 18).
  </action>
  <verify>
    <!-- planner-region-allow: exec="1" -->
    <!-- The exec="1" ban is a permanent file-wide invariant for this page (MCP fences are illustrative, never executed). No task in this plan ever introduces exec="1", so the file-wide negative grep is always satisfiable. -->
    <automated>cd /home/simonm/projects/rust/pyfda && test -f docs/advisor/mcp.md && grep -q "fdars_build_diagnostics" docs/advisor/mcp.md && grep -q "fdars_run_method" docs/advisor/mcp.md && grep -q "fdars_compare_run" docs/advisor/mcp.md && ! grep -q 'exec="1"' docs/advisor/mcp.md && grep -q "python-api.md" docs/advisor/mcp.md && echo TRACER_OK</automated>
  </verify>
  <done>
    docs/advisor/mcp.md exists, names all three tools with accurate parameters and return shapes, states fdars_run_method returns only result_id+method, cross-links index.md and python-api.md, has NO exec="1" fence, and the page renders in a docs build.
  </done>
  <acceptance_criteria>
    - Page names all three tools: fdars_build_diagnostics, fdars_run_method, fdars_compare_run.
    - grep confirms NO exec="1" fence exists on the page.
    - Page states that fdars_run_method returns only result_id and method (arrays stay in registry).
    - Page lists the supported method set and per-method parameter defaults matching server.py.
    - Page cross-links index.md and python-api.md, and forward-links agent-skill.md annotated for Phase 17.
    - Tool parameter names and return-key names appearing on the page match server.py exactly (no invented parameters).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Handle model, stdio setup, and the re-run/compare before/after loop</name>
  <files>docs/advisor/mcp.md</files>
  <read_first>
    - python/fdars/mcp/_registry.py — HandleRegistry: store_dataset(data, argvals)->ds-<hex>, get_dataset, store_result(result)->r-<hex>, get_result, clear; the module-level `registry` singleton; the by-reference rationale in the module docstring (arrays never serialised into JSON; client and server share the same in-process dict).
    - python/fdars/mcp/_runner.py — run_method five-method dispatch and the exact fdars function each method maps to (alignment→karcher_mean, fpca→regression.fpca, basis→basis_nbasis_cv, smoothing→pspline_fit_gcv, clustering→kmeans_fd).
    - python/fdars/mcp/_compare.py — compare_run(dataset_id, method, before_result_id, params_after): builds before/after diagnostics and a scalar `delta` (after − before) over finite numeric keys present in both; return dict keys before_result_id/after_result_id/before/after/delta.
    - python/fdars/mcp/server.py — run_stdio() (mcp.run(transport="stdio")), the console-script entry point, and the transport-agnostic note; stdio-only transport.
    - examples/mcp_recipe.py — the concrete register → run(smoothing n_basis=15) → compare(n_basis=25) sequence to mirror, including the Canadian Weather dataset shape (35 stations × 365 daily points).
  </read_first>
  <action>
    Fill the three placeholder sections created in Task 1, keeping every fence illustrative (plain ```python, no exec="1").
    Handle model section: explain the by-reference HandleRegistry — store_dataset and store_result mint opaque handle ids (ds-<hex>, r-<hex>); arrays never cross the tool boundary; tools exchange handles only; the registry is an in-process singleton shared by client and server over stdio. State this is the efficiency/grounding property: large NumPy arrays stay in the registry and only handle strings + scalar diagnostics travel as JSON.
    stdio setup section: document run_stdio() as the stdio entry point (calls mcp.run(transport="stdio")), that it is the console-script entry point a client launches to start the server, that transport is stdio-only in v2.0, and that the tool handlers are transport-agnostic. Show an illustrative launch snippet consistent with server.py.
    Re-run / compare loop section: walk the agentic before/after loop with a concrete smoothing example mirroring examples/mcp_recipe.py — register the Canadian Weather dataset (store_dataset), run smoothing with n_basis=15 to get a before result handle (fdars_run_method returns {result_id, method}), then call fdars_compare_run with the after-param n_basis=25 and read the returned before/after/delta. State accurately that compare_run re-runs the method, builds diagnostics for both runs, and returns a delta dict of finite scalar numeric differences (after − before) for keys present in both. Name the smoothing→pspline_fit_gcv mapping and note the delta contains scalar GCV/EDF-style keys. Do NOT claim any fence runs in the docs build.
  </action>
  <verify>
    <!-- planner-region-allow: exec="1" -->
    <!-- Same permanent file-wide invariant as Task 1: this page must never contain exec="1". No task introduces it, so the negative grep is always satisfiable. -->
    <automated>cd /home/simonm/projects/rust/pyfda && grep -q "HandleRegistry" docs/advisor/mcp.md && grep -q "run_stdio" docs/advisor/mcp.md && grep -q "store_dataset" docs/advisor/mcp.md && grep -q "before_result_id" docs/advisor/mcp.md && grep -q "n_basis" docs/advisor/mcp.md && grep -Eq "delta" docs/advisor/mcp.md && ! grep -q 'exec="1"' docs/advisor/mcp.md && echo SECTIONS_OK</automated>
  </verify>
  <done>
    The page documents the HandleRegistry by-reference model, run_stdio stdio setup, and a concrete register→run(smoothing n_basis=15)→compare(n_basis=25) loop returning a before/after delta — all method-accurate against _registry.py/_runner.py/_compare.py/server.py, with no executed fence.
  </done>
  <acceptance_criteria>
    - Page explains the HandleRegistry: store_dataset/store_result mint opaque ids, arrays stay in the registry, tools exchange handles.
    - Page documents run_stdio() as the stdio entry point and states transport is stdio-only.
    - Page walks a concrete compare loop mirroring examples/mcp_recipe.py using smoothing with n_basis=15 (before) and n_basis=25 (after) and reads the before/after delta.
    - Page states compare_run returns a delta of finite scalar numeric differences (after − before) for keys present in both diagnostics.
    - grep confirms NO exec="1" fence exists on the page.
    - No invented registry methods or compare_run return keys — names match _registry.py and _compare.py.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — MCP page builds cleanly and reads accurately on the site</name>
  <what-built>
    A new MkDocs page docs/advisor/mcp.md documenting the fdars-advisor MCP
    server: the three tools with accurate parameters/returns, the by-reference
    HandleRegistry model, stdio setup via run_stdio(), and a concrete
    register→run→compare before/after loop mirroring examples/mcp_recipe.py.
    All Python fences are illustrative (not executed) — the docs build does not
    require the [mcp] extra or Python 3.10+.
  </what-built>
  <how-to-verify>
    1. Build the docs and confirm the build SUCCEEDS with the new page present:
       run the project docs build (venv + PYTHONPATH + DOCS_FAST per the docs
       build recipe) and confirm `docs/advisor/mcp.md` is rendered with no build
       error.
    2. Confirm the build did NOT require the [mcp] extra: verify no exec="1"
       fence on the page — run:
       `cd /home/simonm/projects/rust/pyfda && ! grep -q 'exec="1"' docs/advisor/mcp.md && echo NO_EXEC_FENCE_OK`
    3. Serve the site and open the MCP page (base path /pyfda). Section-review on
       the built page:
       - The three tools are named with correct parameters and return shapes;
         fdars_run_method is stated to return only {result_id, method}.
       - The HandleRegistry by-reference model reads clearly (arrays stay in the
         registry; tools exchange handles).
       - The stdio setup (run_stdio) is documented and stdio-only.
       - The re-run/compare loop example (smoothing n_basis 15→25 → delta) reads
         correctly and matches examples/mcp_recipe.py.
       - Cross-links to index.md and python-api.md work; agent-skill.md is
         forward-linked and annotated "coming in Phase 17".
    4. Spot-check method accuracy against python/fdars/mcp/server.py — no invented
       tool names, parameters, or return keys.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only change: a single new Markdown page under `docs/`. No code
paths, no user input, no network calls, no package installs, no new dependencies,
no auth surface. No trust boundary is crossed by this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | docs/advisor/mcp.md content accuracy | low | mitigate | Every tool name/parameter/return-shape verified against python/fdars/mcp/server.py + _runner.py + _compare.py before writing; blocking human-verify section review on the built site; acceptance criteria assert no invented signatures |
| T-16-02 | Information Disclosure | Illustrative fence implies executable in build | low | mitigate | Top-of-page admonition marks all fences illustrative; acceptance criteria + human-verify assert NO exec="1" fence and that the build never requires the [mcp] extra |
</threat_model>

<verification>
- `docs/advisor/mcp.md` exists and names all three tools (MCPDOC-01).
- Page documents run_stdio stdio setup and the by-reference HandleRegistry model (MCPDOC-02).
- Page walks a concrete register→run→compare before/after loop mirroring examples/mcp_recipe.py (MCPDOC-03).
- Page states fdars_run_method returns only {result_id, method}.
- `! grep -q 'exec="1"' docs/advisor/mcp.md` — no executed fence; build does not require the [mcp] extra or Python 3.10+.
- Docs build succeeds with the new page rendered.
- Cross-links: index.md + python-api.md (back), agent-skill.md (forward, Phase 17).
</verification>

<success_criteria>
- The page lists the three tools (fdars_build_diagnostics, fdars_run_method, fdars_compare_run) and their roles, accurate against python/fdars/mcp/ (MCPDOC-01).
- The page documents stdio setup (run_stdio) and the by-reference handle model — arrays stay in the registry, tools exchange handles (MCPDOC-02).
- The page walks the agentic re-run/compare before/after loop with a concrete example matching _runner.py / _compare.py behavior (MCPDOC-03).
- The page builds cleanly and contains no executed fence (no [mcp] extra required).
</success_criteria>

<output>
Create `.planning/phases/16-tool-mcp-server-page/16-01-SUMMARY.md` when done.
</output>

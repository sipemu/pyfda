---
phase: 41-docs-diagrams-worked-examples
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - docs/represent/pace-fpca.md
  - docs/regression/classification.md
  - docs/assets/diagrams/pace-fpca.svg
  - docs/assets/diagrams/elastic-multinomial.svg
  - mkdocs.yml
autonomous: true
requirements: [DOCS-09]
estimate:
  tokens: 74000
  raw_tokens: 49000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The built site has a Represent nav entry 'PACE FPCA' -> represent/pace-fpca.md (DOCS-09)."
    - "pace-fpca.md documents fdars.irreg_fdata_from_lists -> fdars.pace_fpca with a hand-authored inline SVG contrasting ragged/sparse irregular observation dots (per-curve grids) vs recovered SMOOTH eigenfunctions on the common work grid, and an executed offline fence on small synthetic sparse data (n <= 20) emitting FDARS_FENCE_OK (DOCS-09)."
    - "classification.md gains an Elastic Multinomial Classification section with a hand-authored inline SVG showing K one-vs-rest binary classifiers -> softmax, and an executed offline fence on phoneme.csv subsampled to 3 classes (aa/ao/dcl) with m <= 64 emitting FDARS_FENCE_OK (DOCS-09)."
    - "PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict exits 0 with both fences executing offline against the shipped fdars.pace_fpca and fdars.classification bindings (DOCS-09)."
    - "Both new SVGs are SVGO-idempotent under svgo@3.3.4 --config svgo.config.mjs (DOCS-09)."
  artifacts:
    - docs/represent/pace-fpca.md
    - docs/regression/classification.md
    - docs/assets/diagrams/pace-fpca.svg
    - docs/assets/diagrams/elastic-multinomial.svg
  key_links:
    - "mkdocs.yml Represent nav section -> represent/pace-fpca.md"
    - "page SVG references (../assets/diagrams/pace-fpca.svg, elastic-multinomial.svg) -> the two new SVG files"
    - "executed fences -> fdars.irreg_fdata_from_lists/pace_fpca and fdars.classification.elastic_multinomial shipped bindings"
---

<objective>
Document the two new v6.0 Group B bindings — PACE-FPCA over ragged/irregular input and elastic-multinomial classification. Implements DOCS-09. One new page `docs/represent/pace-fpca.md` (wired into Represent nav) plus a new section appended to `docs/regression/classification.md`, each with a hand-authored inline SVG and a tiny executed offline fence emitting `FDARS_FENCE_OK`.

This plan is TRACER-FIRST: Task 1 builds the `pace-fpca.md` page end-to-end (page + SVG + irregular-input fence + nav wiring + DOCS_FAST strict build) to prove the ragged-grid `irreg_fdata_from_lists -> pace_fpca` path runs offline against the shipped bindings. Task 2 extends `classification.md` with the elastic-multinomial section. Task 3 is the SVGO-idempotence gate.

Purpose: Document the highest-novelty Group B surface (Phase 38 — the new IrregFdata input + elastic multinomial) to the project's method-accurate standard.
Output: One new page, one page extension, two new SVGs, one Represent nav entry.

## Artifacts this phase produces (41-02)

- New page: `docs/represent/pace-fpca.md`
- Extended page: `docs/regression/classification.md` (new `## Elastic Multinomial Classification` section)
- New SVGs: `docs/assets/diagrams/pace-fpca.svg`, `docs/assets/diagrams/elastic-multinomial.svg`
- Nav entry (under `- Represent:` in mkdocs.yml, after "Elastic FPCA"): "PACE FPCA"
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-diagrams-worked-examples/41-CONTEXT.md
@.planning/phases/41-docs-diagrams-worked-examples/41-RESEARCH.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/inference/functional-inference.md
@docs/regression/classification.md
@src/pace_fpca_mod.rs
@src/classification_mod.rs
@scripts/docs_fig.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: PACE-FPCA page — one page end-to-end (page + SVG + irregular-input fence + nav + fast build)</name>
  <read_first>
    - docs/inference/functional-inference.md (page template)
    - docs/assets/diagrams/STYLE_SPEC.md (SVG contract: viewBox 720x300, verbatim style block, role="img"+aria-label, data-curve palette)
    - src/pace_fpca_mod.rs lines 91-153 (irreg_fdata_from_lists: argvals_list + values_list = lists of 1-D arrays one per curve; returns opaque PyIrregFdata; raises ValueError on 2-D numpy array / length mismatch)
    - src/pace_fpca_mod.rs lines 219-240 (pace_fpca: data=handle, ncomp=3, bandwidth=0.1, sigma2=0.01, work_grid=None->51 uniform pts, alpha=0.05; returns 10-key dict mean (m,), eigenvalues (ncomp,), eigenfunctions (m,ncomp), scores (n,ncomp), fitted/fitted_lower/fitted_upper (n,m), argvals (m,), sigma2 float, ncomp int ACTUAL count may be < requested)
    - scripts/docs_fig.py (fig, render, fast; FDARS_COLORS)
    - mkdocs.yml lines 92-102 (Represent nav section; insert after "Elastic FPCA: represent/elastic-fpca.md" at line 95)
    - 41-RESEARCH.md "DOCS-09: pace_fpca fence" blueprint + Pitfall 3 (irreg_fdata_from_lists rejects 2-D numpy arrays — pass explicit Python lists of 1-D arrays)
  </read_first>
  <files>docs/represent/pace-fpca.md, docs/assets/diagrams/pace-fpca.svg, mkdocs.yml</files>
  <action>
    Author docs/assets/diagrams/pace-fpca.svg as hand-authored inline SVG conforming to STYLE_SPEC (viewBox "0 0 720 300", fill="none", role="img"+aria-label, verbatim style block). Method-accurate (reviewer-flagged): left panel shows sparse IRREGULAR observations — scattered dots at DIFFERENT x-positions per curve (ragged per-curve grids, NOT a common grid; this is what "irregular" means); right panel shows the recovered SMOOTH eigenfunctions on the common work grid (smooth curves, NOT the raw data resampled). The visual contrast "ragged irregular dots -> smooth eigenfunction curves" is the core message. A regular-grid/uniform-spacing depiction of the observations is a method-accuracy error.

    Create docs/represent/pace-fpca.md following the functional-inference.md template: H1 "PACE — FPCA for Sparse, Irregular Data"; intro framing PACE (recovers smooth mean + eigenfunctions from sparsely, irregularly sampled curves via conditional expectation); SVG include ![alt](../assets/diagrams/pace-fpca.svg){ .fdars-diagram }; ## Theory in KaTeX (Karhunen-Loeve expansion; PACE estimates the covariance surface then conditions to get scores); a parameter table for both functions (irreg_fdata_from_lists: argvals_list, values_list; pace_fpca: data handle, ncomp, bandwidth, sigma2, work_grid, alpha); a Returns table (10 keys). Add an admonition note: eigenfunctions is (m, ncomp) — column k is the k-th eigenfunction; and that ncomp in the returned dict is the ACTUAL count and may be < requested. Add a second note (Pitfall 3): irreg_fdata_from_lists requires two Python lists of 1-D arrays and rejects a dense 2-D numpy array with a ValueError.

    Add a fence using exec/html/source-above per the "DOCS-09: pace_fpca fence" blueprint: n=15 sparse curves each with 5-8 observations on ragged grids, build argvals_list/values_list as explicit Python lists of 1-D arrays, handle = pf.irreg_fdata_from_lists(argvals_list, values_list), res = pf.pace_fpca(handle, ncomp=2, bandwidth=0.2, sigma2=0.05); left subplot scatters the sparse observations + PACE mean, right subplot plots the recovered eigenfunctions; print an actual-ncomp / scores-shape line; end with the exact final line printing the FDARS_FENCE_OK sentinel. Keep n <= 20.

    Wire nav: in mkdocs.yml, after "Elastic FPCA: represent/elastic-fpca.md", add "PACE FPCA: represent/pace-fpca.md" (match the 4-space indentation of sibling Represent entries).

    Run the per-page fast strict build. Do NOT run the full build. Do NOT edit any src/*.rs file; fix the FENCE, never the binding, on any API mismatch.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "FDARS_FENCE_OK" site/represent/pace-fpca/index.html && grep -q "pace-fpca.svg" site/represent/pace-fpca/index.html && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/pace-fpca.svg exists, conforms to STYLE_SPEC, and shows ragged/irregular observation dots (per-curve grids) vs smooth recovered eigenfunctions.
    - docs/represent/pace-fpca.md exists with H1, SVG include, KaTeX Theory, parameter + Returns tables for both functions, the eigenfunctions-(m,ncomp)/actual-ncomp note, and the Pitfall-3 (no 2-D array) note, plus an executed fence on synthetic sparse data (n <= 20).
    - The fence emits the FDARS_FENCE_OK sentinel in the built HTML.
    - mkdocs.yml has a "PACE FPCA" entry under Represent after "Elastic FPCA".
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify prints TRACER_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>The pace-fpca page builds offline under DOCS_FAST, its irregular-input fence emits FDARS_FENCE_OK, its method-accurate SVG renders, and its nav entry is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Elastic Multinomial section in classification.md — SVG + phoneme 3-class fence</name>
  <read_first>
    - docs/regression/classification.md lines 404-441 (existing phoneme section using load_phoneme; append the new section after the existing content, before ## References if present)
    - docs/assets/diagrams/STYLE_SPEC.md (SVG contract)
    - src/classification_mod.rs lines 330-358 (elastic_multinomial: data (n,m); labels (n,) dtype int64 0-indexed contiguous; argvals (m,); ncomp_beta=10; lambda_=0.1; max_iter=100; tol=1e-4; returns dict n_classes, classes (K,), train_probabilities (n,K), predicted_classes (n,), train_accuracy; negative/non-0-indexed labels raise ValueError)
    - 41-RESEARCH.md "DOCS-09: elastic_multinomial fence" blueprint + Pitfall 7 (labels must be dtype int64)
    - PLAN-TIME FACT (verified this session): docs/data/phoneme.csv has 5 classes each with exactly 80 rows (aa, ao, dcl, iy, sh — perfectly balanced). Use the 3 classes aa/ao/dcl.
  </read_first>
  <files>docs/regression/classification.md, docs/assets/diagrams/elastic-multinomial.svg</files>
  <action>
    Author docs/assets/diagrams/elastic-multinomial.svg as hand-authored inline SVG conforming to STYLE_SPEC (viewBox "0 0 720 300", fill="none", role="img"+aria-label, verbatim style block). Method-accurate: convey the K-class one-vs-rest structure — K binary elastic classifiers (show K=3 for concreteness), each producing a score, combined via softmax into class probabilities. Show 3 classes, 3 OvR binary models, and the softmax aggregation. A depiction that looks like ordinary LDA is a method-accuracy error — make the one-vs-rest + softmax structure explicit.

    Append a new `## Elastic Multinomial Classification` section to docs/regression/classification.md (after the existing phoneme content, before any ## References): a short intro (K-class OvR elastic classifier in the SRSF/elastic domain); the SVG include ![alt](../assets/diagrams/elastic-multinomial.svg){ .fdars-diagram }; a brief theory paragraph; a parameter table (data, labels, argvals, ncomp_beta, lambda_, max_iter, tol); a Returns table (n_classes, classes, train_probabilities (n,K), predicted_classes, train_accuracy). Add an admonition note (Pitfall 7): labels must be 0-indexed contiguous int64; negative or non-contiguous labels raise a ValueError.

    Add a fence using exec/html/source-above per the "DOCS-09: elastic_multinomial fence" blueprint: load_phoneme(); subset to the 3 classes aa/ao/dcl via a mask; build y3 as np.array of 0-indexed class indices with dtype=np.int64; subsample columns to m <= 64 (step = max(1, X3.shape[1] // 64)); call clf.elastic_multinomial(X3, y3, freq3, ncomp_beta=8, lambda_=0.1); print n_classes / train_accuracy / train_probabilities shape lines; end with the exact final line printing the FDARS_FENCE_OK sentinel. No new nav entry (classification.md already in nav).

    Do NOT run the full build. Do NOT edit any src/*.rs file; fix the FENCE, never the binding.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict && grep -q "elastic-multinomial.svg" site/regression/classification/index.html && test "$(grep -c FDARS_FENCE_OK site/regression/classification/index.html)" -ge 1 && echo MULTINOM_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/assets/diagrams/elastic-multinomial.svg exists, conforms to STYLE_SPEC, and shows K one-vs-rest binary classifiers -> softmax (not LDA).
    - docs/regression/classification.md has a new "## Elastic Multinomial Classification" section with the SVG include, a parameter + Returns table, the int64/0-indexed labels note, and an executed fence on phoneme (aa/ao/dcl, m <= 64).
    - The elastic_multinomial fence emits the FDARS_FENCE_OK sentinel in the built HTML.
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0; verify prints MULTINOM_OK.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>classification.md gains a method-accurate elastic-multinomial section whose phoneme 3-class fence builds offline and emits FDARS_FENCE_OK.</done>
</task>

<task type="auto">
  <name>Task 3: SVGO-idempotence + fence-presence gate for both DOCS-09 SVGs/pages</name>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (SVGO invocation svgo@3.3.4 --config svgo.config.mjs; idempotence gate)
    - svgo.config.mjs (confirm present)
  </read_first>
  <files>docs/assets/diagrams/pace-fpca.svg, docs/assets/diagrams/elastic-multinomial.svg</files>
  <action>
    For each new SVG (pace-fpca.svg, elastic-multinomial.svg), run the SVGO idempotence check with the pinned svgo@3.3.4 and the config: two svgo passes, confirm the second pass is byte-identical (empty diff). If a diagram fails, hand-fix the markup to conform (the gate is check-only; do not let svgo rewrite the committed source). Confirm no embedded date/timestamp metadata (determinism-clean). Do NOT run the full build; the whole-site build + human review run in 41-04.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for f in docs/assets/diagrams/pace-fpca.svg docs/assets/diagrams/elastic-multinomial.svg; do npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output /tmp/p1.svg && npx svgo@3.3.4 --config svgo.config.mjs --quiet --input /tmp/p1.svg --output /tmp/p2.svg && diff /tmp/p1.svg /tmp/p2.svg && echo "IDEMPOTENT: $f" || { echo "FAILS: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Both new SVGs pass the svgo@3.3.4 idempotence check (byte-identical second pass); verify prints IDEMPOTENT for each.
    - Neither SVG contains embedded date/timestamp metadata.
    - No src/*.rs file modified.
  </acceptance_criteria>
  <done>Both DOCS-09 SVGs are SVGO-idempotent and determinism-clean, ready for the whole-site build and human review in 41-04.</done>
</task>

</tasks>

<threat_model>
DOCS-ONLY phase — no source code, no network calls (fences execute offline), no user-supplied input paths.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-02-I | Information Disclosure | executed doc fences | low | accept | Fences read only the in-repo docs/data/phoneme.csv + tiny synthetic data; no secrets, no external I/O |
</threat_model>

<verification>
- `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build --strict` exits 0.
- `grep -q FDARS_FENCE_OK site/represent/pace-fpca/index.html` succeeds; `grep -c FDARS_FENCE_OK site/regression/classification/index.html` is >= 1 and includes the new elastic-multinomial fence.
- Both new SVGs pass the svgo@3.3.4 idempotence check.
- `git diff --name-only` shows no `src/*.rs` change.
</verification>

<success_criteria>
The new PACE-FPCA page and the elastic-multinomial section render with executed offline fences emitting FDARS_FENCE_OK, each carries a method-accurate hand-authored SVG (PACE = ragged irregular observations vs smooth recovered eigenfunctions; multinomial = K one-vs-rest -> softmax), the PACE nav entry is wired, and both SVGs are SVGO-idempotent. The full whole-site strict build and human diagram review are deferred to 41-04.
</success_criteria>

<output>
Create `.planning/phases/41-docs-diagrams-worked-examples/41-02-SUMMARY.md` when done.
</output>

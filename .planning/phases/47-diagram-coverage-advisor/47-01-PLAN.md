---
phase: 47-diagram-coverage-advisor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/diagrams/advisor-python-api.svg
  - docs/assets/diagrams/advisor-mcp.svg
  - docs/assets/diagrams/advisor-providers.svg
  - docs/assets/diagrams/advisor-agent-skill.svg
  - docs/assets/diagrams/advisor-aspects.svg
  - docs/advisor/python-api.md
  - docs/advisor/mcp.md
  - docs/advisor/providers.md
  - docs/advisor/agent-skill.md
  - docs/advisor/aspects.md
  - .planning/phases/47-diagram-coverage-advisor/check-adv.sh
autonomous: true
requirements:
  - DIACOV-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DIACOV-02 / SC1: each of the 5 advisor surface pages (python-api, mcp, providers, agent-skill, aspects) references a hand-authored inline concept SVG via a `.fdars-diagram` embed line near the page top."
    - "SC2: each diagram is method-accurate to the SHIPPED advisor surface it depicts (python/fdars/advisor/, python/fdars/mcp/, .claude/skills/fdars-advisor/) and preserves the grounding invariant where relevant."
    - "Grounding invariant (hard constraint): the advisor-mcp.svg and advisor-python-api.svg diagrams contain NO element that implies the LLM computes diagnostics — fdars computes every number; the LLM only interprets/cites."
    - "SC3: each new SVG conforms to STYLE_SPEC.md (viewBox 0 0 720 {300|480}, canonical five-class <style> block, role=img + aria-label) and passes svgo@3.3.4 idempotence (2nd pass byte-identical under svgo.config.mjs)."
    - "No existing diagram or advisor page is modified beyond the single embed line added to each of the 5 pages; no whole-site build is run (deferred to Phase 49)."
  artifacts:
    - docs/assets/diagrams/advisor-python-api.svg
    - docs/assets/diagrams/advisor-mcp.svg
    - docs/assets/diagrams/advisor-providers.svg
    - docs/assets/diagrams/advisor-agent-skill.svg
    - docs/assets/diagrams/advisor-aspects.svg
    - .planning/phases/47-diagram-coverage-advisor/check-adv.sh
  key_links:
    - "Each advisor-<slug>.svg is referenced from docs/advisor/<slug>.md via `](../assets/diagrams/advisor-<slug>.svg){ .fdars-diagram }`."
    - "advisor-mcp.svg depicts the agent/LLM OUTSIDE the MCP boundary; only opaque handles (ds-/r-<hex>) + scalar diagnostics cross the boundary — the LLM-free compute path."
---

<objective>
Author one method-accurate, STYLE_SPEC-conformant hand-authored inline concept SVG for each of the 5 advisor surface pages that lack one (DIACOV-02: python-api, mcp, providers, agent-skill, aspects), and embed each near the top of its page via a single `.fdars-diagram` reference line. Each diagram is an architectural/flow diagram in the genre of the two shipped advisor diagrams (advisor-grounding-invariant.svg / advisor-loop.svg) and is accurate to the SHIPPED advisor code, not aspirational.

Purpose: Reverse the v2.1 "advisor surface pages diagram-free" choice — every advisor surface page gets a diagram that faithfully depicts what that surface actually does, with the grounding invariant (fdars computes numbers, the LLM only cites) preserved where relevant.

Output: 5 new `docs/assets/diagrams/advisor-<slug>.svg` files, 5 edited advisor `.md` pages (embed line only), and a reusable per-diagram gate helper `check-adv.sh` adapted from Phase 46's `check-ex.sh`. NO whole-site build (Phase 49). No existing diagram or page content changed beyond the embed line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/47-diagram-coverage-advisor/47-CONTEXT.md
@.planning/phases/42-diagram-audit/42-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
@docs/assets/diagrams/advisor-grounding-invariant.svg
@docs/assets/diagrams/advisor-loop.svg
@.planning/phases/46-diagram-coverage-examples/check-ex.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — advisor-python-api.svg end-to-end + adapt the gate helper</name>
  <files>docs/assets/diagrams/advisor-python-api.svg, docs/advisor/python-api.md, .planning/phases/47-diagram-coverage-advisor/check-adv.sh</files>
  <read_first>
    - docs/advisor/python-api.md (the target page — depict its recommend-only surface accurately)
    - python/fdars/advisor/__init__.py (advise() signature + Advice re-export + the "returns Advice and stops" behavior)
    - python/fdars/advisor/_schema.py (Advice = interpretation / recommendations / caveats; Recommendation = action / kind / rationale / expected_effect / evidence)
    - docs/assets/diagrams/advisor-grounding-invariant.svg (STYLE_SPEC-conformant advisor-diagram template — copy the exact <style> block, palette, arrow markers, two-lane genre)
    - docs/assets/diagrams/STYLE_SPEC.md (viewBox 720, five CSS classes, role/aria, palette, stroke weights)
    - .planning/phases/46-diagram-coverage-examples/check-ex.sh (gate pattern to adapt)
  </read_first>
  <action>
    Prove the full create → embed → SVGO-idempotence → rsvg-PNG pipeline on ONE exemplar diagram (DIACOV-02, the core recommend-only surface).

    (1) Adapt the Phase 46 gate helper into `.planning/phases/47-diagram-coverage-advisor/check-adv.sh`: same structure as check-ex.sh but for advisor diagrams — take `<slug>` and `<viewbox-height>` args; verify `docs/assets/diagrams/advisor-<slug>.svg` exists with `viewBox="0 0 720 <h>"`, `role="img"`, `aria-label=`, and each of the five canonical CSS classes (`.ttl{ .sub{ .lab{ .sm{ .mono{`); grep that `docs/advisor/<slug>.md` embeds it via the exact string `](../assets/diagrams/advisor-<slug>.svg){ .fdars-diagram }`; run `npx svgo@3.3.4 --config svgo.config.mjs` twice and `diff` the two passes (byte-identical required); `rsvg-convert` to a non-empty PNG under the scratchpad. NO whole-site build. Make it executable (`chmod +x`).

    (2) Author `docs/assets/diagrams/advisor-python-api.svg` as a two-panel architectural flow, copying the canonical `<style>` block, FDARS palette, and arrow `<marker>` defs verbatim from advisor-grounding-invariant.svg. Left panel: the recommend-only call pattern — `build_diagnostics(result, method)` (offline, deterministic, no LLM) → `advise(diagnostics, task=…)`. Right panel: the returned `Advice` structure — `interpretation` (str), `recommendations` (list of `Recommendation`: action / kind / rationale / expected_effect / evidence), `caveats` (list). Include a terminal marker that the surface RETURNS Advice and STOPS — it does NOT re-run fdars or compute a before/after delta (that distinction is central to the page). Keep the LLM-computes-nothing framing: `build_diagnostics` is labeled offline/deterministic; only `advise` touches the LLM, and it interprets/cites, never computes. Use viewBox `0 0 720 300` (or `0 0 720 480` if the Advice structure needs the taller layout); 2–5 nodes/panels.

    (3) Embed near the top of `docs/advisor/python-api.md` (after the H1 intro paragraph, before `## Worked example`) exactly ONE line: `![Recommend-only advisor Python API: build_diagnostics then advise returns an Advice object](../assets/diagrams/advisor-python-api.svg){ .fdars-diagram }`. Change nothing else on the page.

    (4) Run `bash .planning/phases/47-diagram-coverage-advisor/check-adv.sh python-api <h>` and eyeball the PNG for method-accuracy + layout before considering the tracer done.
  </action>
  <verify>
    <automated>bash .planning/phases/47-diagram-coverage-advisor/check-adv.sh python-api 300 || bash .planning/phases/47-diagram-coverage-advisor/check-adv.sh python-api 480</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/47-diagram-coverage-advisor/check-adv.sh` exists, is executable, and gates: SVG existence, 720-width viewBox at the given height, `role="img"`, `aria-label`, all five CSS classes, the `.fdars-diagram` embed on the page, svgo@3.3.4 2nd-pass byte-identical idempotence, and a non-empty rsvg PNG — with NO whole-site build.
    - `docs/assets/diagrams/advisor-python-api.svg` exists; viewBox is `0 0 720 300` or `0 0 720 480`; has `role="img"` + descriptive `aria-label`; contains the canonical five-class `<style>` block.
    - The diagram depicts `build_diagnostics(result, method)` → `advise(diagnostics, task=…)` → an `Advice` object with fields interpretation / recommendations (action, kind, rationale, expected_effect, evidence) / caveats, and marks the surface as recommend-only (returns Advice and stops; no re-run/compare).
    - The diagram contains NO element implying the LLM computes diagnostics: `build_diagnostics` is labeled offline/deterministic; `advise` interprets and cites only.
    - `docs/advisor/python-api.md` contains exactly one new line `](../assets/diagrams/advisor-python-api.svg){ .fdars-diagram }`; no other page content changed.
    - svgo idempotence: second `svgo@3.3.4 --config svgo.config.mjs` pass is byte-identical to the first.
    - No existing diagram or other advisor page modified.
  </acceptance_criteria>
  <done>The tracer diagram renders, passes check-adv.sh, is embedded on python-api.md, and proves the pipeline end-to-end for the remaining four.</done>
</task>

<task type="auto">
  <name>Task 2: Author the remaining 4 advisor diagrams (mcp, providers, agent-skill, aspects) + embed each</name>
  <files>docs/assets/diagrams/advisor-mcp.svg, docs/assets/diagrams/advisor-providers.svg, docs/assets/diagrams/advisor-agent-skill.svg, docs/assets/diagrams/advisor-aspects.svg, docs/advisor/mcp.md, docs/advisor/providers.md, docs/advisor/agent-skill.md, docs/advisor/aspects.md</files>
  <read_first>
    - docs/advisor/mcp.md + python/fdars/mcp/server.py (HandleRegistry by-reference model; the 3 tools fdars_build_diagnostics / fdars_run_method / fdars_compare_run; stdio transport; handles ds-<hex>/r-<hex>; by-reference invariant — arrays never cross the boundary; compute path is deterministic, NO ANTHROPIC_API_KEY required for the tools)
    - docs/advisor/providers.md + python/fdars/advisor/providers/_factory.py (four backends Anthropic/OpenAI/Gemini/Ollama behind the Provider protocol; precedence: explicit advise(provider=,model=) arg > FDARS_ADVISOR_PROVIDER env > anthropic default; per-provider key env vars; advise() is the ONLY provider entry point — MCP tools are compute-only and never call advise())
    - docs/advisor/agent-skill.md + .claude/skills/fdars-advisor/SKILL.md (git-URL install; the interpret → recommend → re-run → compare loop orchestrated automatically vs the recommend-only Python-API mode; offline Steps 1-3+5 vs API-key-gated Step 4)
    - docs/advisor/aspects.md + python/fdars/advisor/__init__.py build_diagnostics _supported set (the ~14 aspects; the three grounded task families interpretation / parameter / method flowing through one shared schema/prompt)
    - docs/assets/diagrams/advisor-python-api.svg (the tracer — reuse its exact <style> block, palette, markers, and node/arrow conventions for visual consistency)
    - docs/assets/diagrams/advisor-loop.svg (the shipped interpret→advise→run→compare loop diagram — genre reference for the agent-skill loop)
  </read_first>
  <action>
    Author the four remaining `docs/assets/diagrams/advisor-<slug>.svg` files, each copying the canonical `<style>` block, palette, and arrow markers from the Task 1 tracer for visual consistency. Each scoped to a clear flow (2–5 nodes/panels), viewBox `0 0 720 300` or `0 0 720 480` as the content needs. Then embed each near the top of its page (after the H1 intro paragraph, before the first `##` section) as exactly ONE `.fdars-diagram` line; change nothing else on any page.

    `advisor-mcp.svg` — agent ↔ MCP ↔ fdars boundary with the by-reference handle model. CRITICAL grounding constraint: place the agent/LLM OUTSIDE the MCP boundary as a caller of the tools; inside the boundary sit the three tools (`fdars_build_diagnostics`, `fdars_run_method`, `fdars_compare_run`) + the in-process `HandleRegistry`; fdars computes every number. Show that only opaque handles (`ds-<hex>`, `r-<hex>`) + scalar diagnostics cross the stdio boundary — NumPy arrays never leave the process. Depict stdio as the transport. The compute path is deterministic and LLM-free: NO element may imply the LLM computes diagnostics or that arrays cross the boundary. (Depict `fdars_run_method` over the six runnable methods per server.py `_RUNNABLE_METHODS`: alignment/fpca/basis/smoothing/clustering/depth; keep numbers vague if unsure rather than wrong.)

    `advisor-providers.svg` — provider configuration + selection precedence. Show `advise(provider=, model=)` as the single entry point routing through the `Provider` protocol to four adapters: Anthropic (default, ANTHROPIC_API_KEY), OpenAI/OpenAI-compatible (OPENAI_API_KEY), Gemini (GEMINI_API_KEY, Py3.10+), Ollama (key-free, local). Depict the precedence chain per _factory.py: explicit arg → `FDARS_ADVISOR_PROVIDER`/`FDARS_ADVISOR_MODEL` env → anthropic default. Note that the MCP tools are compute-only and never call advise() (provider selection is an advise()-only concern).

    `advisor-agent-skill.svg` — the agent-skill execution flow vs the Python-API mode. Show git-URL install (`pip install "fdars @ git+…"` + mcp/anthropic/pydantic) feeding a self-orchestrated loop: interpret (build_diagnostics) → recommend (advise, API-key-gated) → re-run (compare_run) → compare (before/after delta). Contrast with the recommend-only Python-API mode which returns Advice and stops. Keep the grounding invariant: every delta value is fdars-computed; the advise() step is optional and gated on ANTHROPIC_API_KEY.

    `advisor-aspects.svg` — the per-aspect taxonomy. Show the ~14 fdars aspects (clustering, smoothing, alignment, basis, fpca, represent, depth, outliers, classification, regression, regression_cv, scoring, spm, inference — group them compactly) as caller-specified `method=` inputs, each flowing through one shared `build_diagnostics` → `advise` schema/prompt, crossed with the three grounded task families: interpretation / parameter / method. Convey "same two-stage treatment for every aspect × three task families, one shared schema."

    After authoring + embedding each, run `bash check-adv.sh <slug> <h>` for that slug and eyeball its PNG for method-accuracy + layout. For any advisor flow whose "right" depiction is a judgment call, pick the best-supported version and surface it in the SUMMARY for the Phase 49 blocking human diagram review — grounding-invariant fidelity is non-negotiable and never a judgment call.
  </action>
  <verify>
    <automated>for s in mcp providers agent-skill aspects; do bash .planning/phases/47-diagram-coverage-advisor/check-adv.sh "$s" 300 || bash .planning/phases/47-diagram-coverage-advisor/check-adv.sh "$s" 480 || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `docs/assets/diagrams/advisor-mcp.svg` exists; viewBox 720-width; role=img + aria-label; five CSS classes; depicts the HandleRegistry by-reference handle model (`ds-<hex>`/`r-<hex>`), the three tools (fdars_build_diagnostics, fdars_run_method, fdars_compare_run), and stdio transport; contains NO element implying the LLM computes diagnostics and NO arrays crossing the boundary — the agent/LLM sits OUTSIDE the boundary as a tool caller.
    - `docs/assets/diagrams/advisor-providers.svg` exists; 720-width viewBox; role=img + aria-label; five CSS classes; depicts advise(provider=,model=) → Provider protocol → four adapters (Anthropic default / OpenAI / Gemini / Ollama) and the precedence chain (explicit arg → env var → anthropic default).
    - `docs/assets/diagrams/advisor-agent-skill.svg` exists; 720-width viewBox; role=img + aria-label; five CSS classes; depicts git-URL install + the interpret→recommend→re-run→compare loop, contrasted with the recommend-only Python-API mode; advise() step marked optional/API-key-gated.
    - `docs/assets/diagrams/advisor-aspects.svg` exists; 720-width viewBox; role=img + aria-label; five CSS classes; depicts the ~14 aspects × three task families (interpretation/parameter/method) through one shared build_diagnostics→advise schema.
    - Each of docs/advisor/{mcp,providers,agent-skill,aspects}.md contains exactly one new `](../assets/diagrams/advisor-<slug>.svg){ .fdars-diagram }` line; no other page content changed.
    - Each of the four SVGs passes svgo@3.3.4 idempotence (2nd pass byte-identical) via check-adv.sh; no whole-site build run.
    - No existing diagram or advisor page modified beyond its single embed line.
  </acceptance_criteria>
  <done>All five advisor surface pages (including the tracer's python-api) reference a method-accurate, STYLE_SPEC-conformant concept SVG; every SVG passes check-adv.sh; the mcp diagram is provably LLM-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed docs assets | Hand-authored SVG + one embed line per page enter the repo; no code execution, no runtime data path. |
| documentation → reader | The diagram is a trust surface: a method-inaccurate diagram (esp. one implying the LLM computes numbers) misinforms users about the security-relevant grounding invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Information disclosure (misrepresentation) | advisor-mcp.svg / advisor-python-api.svg | high | mitigate | Acceptance criteria REQUIRE the diagrams contain no element implying the LLM computes diagnostics; the agent/LLM is drawn OUTSIDE the MCP boundary and only handles + scalar diagnostics cross it. Reviewed against server.py + advisor/__init__.py. Human diagram review at Phase 49 is the final gate. |
| T-47-02 | Tampering (accidental) | existing 61 diagrams + 5 advisor pages | medium | mitigate | Only the 5 new SVGs are created and only one embed line is added per page; check-adv.sh + acceptance criteria assert no existing diagram/page content changed beyond the embed line. |
| T-47-03 | Tampering (supply chain) | npx svgo@3.3.4, rsvg-convert | low | accept | svgo is pinned to 3.3.4 (STYLE_SPEC mandate); rsvg-convert is a local system binary. No new package installs are added by this phase; the gate reuses the existing pinned toolchain. |
</threat_model>

<verification>
- Both tasks' `<automated>` gates pass: `check-adv.sh <slug> <h>` green for all of python-api, mcp, providers, agent-skill, aspects (each at its actual viewBox height).
- `grep -rl '.fdars-diagram' docs/advisor/` lists all 5 surface pages referencing their new SVG.
- `git status --short docs/` shows only the 5 new SVGs + 5 modified `.md` pages (one embed line each) + check-adv.sh — no other diagram or page touched.
- Manual eyeball of each rsvg PNG (scratchpad, not committed) confirms method-accuracy and layout; the mcp + python-api PNGs are confirmed to keep the LLM out of the compute path.
- NO `mkdocs build` is run in this phase (deferred to Phase 49).
</verification>

<success_criteria>
- All 5 advisor surface pages carry a hand-authored inline concept SVG embedded via `.fdars-diagram` (SC1 / DIACOV-02).
- Each diagram is method-accurate to the shipped advisor surface and preserves the grounding invariant where relevant (SC2); the mcp + python-api diagrams are provably LLM-free.
- Each new SVG is STYLE_SPEC-conformant and passes svgo@3.3.4 idempotence (SC3).
- No existing diagram or page content changed beyond the 5 embed lines; no whole-site build (Phase 49 owns SC4 built-site review).
</success_criteria>

<output>
Create `.planning/phases/47-diagram-coverage-advisor/47-01-SUMMARY.md` when done. In the SUMMARY, explicitly state how the mcp (and python-api) diagram keeps the LLM out of the compute path, and surface any advisor flow whose depiction was a judgment call for the Phase 49 blocking human diagram review.
</output>

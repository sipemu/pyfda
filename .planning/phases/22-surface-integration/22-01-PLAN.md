---
phase: 22-surface-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/fdars/mcp/_runner.py
  - python/fdars/mcp/server.py
  - tests/test_mcp_server.py
autonomous: true
requirements: [SURF-01, SURF-02]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "fdars_run_method dispatches method='depth' and returns a usable result_id (SURF-01)"
    - "fdars_build_diagnostics on that depth result_id returns diagnostics with method=='depth' (SURF-01)"
    - "No file under python/fdars/mcp/ references 'advise' — the MCP boundary stays LLM-free (SURF-02)"
  artifacts:
    - python/fdars/mcp/_runner.py
    - python/fdars/mcp/server.py
    - tests/test_mcp_server.py
  key_links:
    - "runner depth branch returns {'scores','method_name'} → server.py unwraps 'scores' → build_diagnostics(scores, 'depth')"
    - "_RUNNABLE_METHODS is the single source of truth for run_method + compare_run guards"
---

<objective>
TRACER (SURF-01/SURF-02): Prove the new MCP surface end-to-end on one genuinely
new runnable aspect — `depth` — wired through every layer the phase touches
(`_runner.py` dispatch → `server.py` build-diagnostics unwrap → in-process MCP
`Client`), and permanently lock the MCP LLM-free invariant with a file scan.

This is the thinnest complete path: it renames the diverging `_SUPPORTED_METHODS`
allow-lists into the principled `_RUNNABLE_METHODS` design, adds the single new
runnable (`depth`), and adds the two tracer tests. Wave 2 expands the
diagnostics-only aspects on top of this proven slice.

Purpose: Catch any architectural dead-end (dict-vs-array depth contract, guard
divergence) after one commit rather than after all aspects are wired.
Output: `depth` runnable via MCP tools compute-only; `test_run_method_depth` and
`test_mcp_does_not_import_advise` green; all existing MCP tests still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-surface-integration/22-RESEARCH.md

@python/fdars/mcp/_runner.py
@python/fdars/mcp/server.py
@tests/test_mcp_server.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add depth as a runnable method (_RUNNABLE_METHODS + depth dispatch) in _runner.py</name>
  <files>python/fdars/mcp/_runner.py</files>
  <action>
Per D-SURF-01: rename the module-level `_SUPPORTED_METHODS` frozenset to
`_RUNNABLE_METHODS` and add `"depth"` to the set, so the runnable set is exactly
{alignment, fpca, basis, smoothing, clustering, depth}. Keep a module-level alias
`_SUPPORTED_METHODS = _RUNNABLE_METHODS` so nothing that imports the old name
breaks. Update every internal reference (the guard in `run_method` and the error
message) to use `_RUNNABLE_METHODS`.

Add a new dispatch branch for `method_lc == "depth"` inside `run_method`, placed
alongside the existing per-method branches. In that branch import `fdars.depth`
locally and call the canonical `fraiman_muniz_1d(data, argvals)` estimator
(verified during research: fdars depth functions take `(data, argvals)` and
return an ndarray `(n,)` directly). Return a dict `{"scores": <ndarray>,
"method_name": "fraiman_muniz"}` — the scores array stays in the registry, the
method_name records which estimator ran. Do NOT add any string `method_name`
tool parameter (string params widen the injection surface, T-12-03); the MCP
entry point is hard-coded to fraiman_muniz and documented as such in the
docstring. Callers wanting another depth function run it manually and pass the
result to fdars_build_diagnostics.

Update the module docstring's supported-methods list and the `run_method`
docstring to include depth and its `(data, argvals)` mapping. Keep the
scalar-only param contract (T-12-03) — depth needs no new scalar params.
  </action>
  <verify>
    <automated>/home/simonm/projects/rust/pyfda/.venv/bin/python -c "from fdars.mcp._runner import _RUNNABLE_METHODS, run_method; assert 'depth' in _RUNNABLE_METHODS and len(_RUNNABLE_METHODS)==6, _RUNNABLE_METHODS; print('OK')"</automated>
  </verify>
  <done>_RUNNABLE_METHODS contains the six runnable methods including depth; run_method dispatches depth to fraiman_muniz_1d and returns {"scores", "method_name"}.</done>
</task>

<task type="auto">
  <name>Task 2: Rename server guard to _RUNNABLE_METHODS and unwrap depth scores in fdars_build_diagnostics</name>
  <files>python/fdars/mcp/server.py</files>
  <action>
Per D-SURF-01: rename the local `_SUPPORTED_METHODS` frozenset in `server.py` to
`_RUNNABLE_METHODS` and add `"depth"`, matching `_runner.py` — this is the guard
for `fdars_run_method` (via the runner) and `fdars_compare_run`. Keep
`_RUNNABLE_METHODS` as the compare_run guard (only re-runnable methods can be
compared, per research Pitfall 3). Update the two error messages and the
`fdars_compare_run` guard reference to use `_RUNNABLE_METHODS`.

NOTE: `fdars_build_diagnostics`'s method guard is deliberately NOT touched in
this plan — Wave 2 (plan 22-02) introduces `_DIAGNOSTICS_METHODS` for it. In this
plan `fdars_build_diagnostics` must still accept `depth`. Since `depth` is now in
`_RUNNABLE_METHODS` and the current build-diagnostics guard reads that same set,
depth passes through in this plan; the guard swap to `_DIAGNOSTICS_METHODS`
happens in 22-02.

Add the depth unwrap step in `fdars_build_diagnostics` (research Pitfall 2): after
the result is resolved (from `result_id` or the raw-data fallback) and BEFORE
calling `build_diagnostics`, if `method_lc == "depth"` and the resolved result is
a dict containing a `scores` key, replace `result` with the score array itself
(`result["scores"]`) and capture the recorded `method_name` (default "unknown")
into the diagnostics kwargs so it reaches `_build_depth_diagnostics(raw,
method_name=...)`. The depth builder calls `np.asarray(raw)` and expects the raw
score array, not the wrapper dict — verified in research. Pass `method_name` only
for the depth branch; do not add it to the tool signature.

Update the `fdars_build_diagnostics` and `fdars_run_method` docstrings' supported
method lists to include depth.
  </action>
  <verify>
    <automated>/home/simonm/projects/rust/pyfda/.venv/bin/python -c "from fdars.mcp.server import _RUNNABLE_METHODS; assert 'depth' in _RUNNABLE_METHODS and len(_RUNNABLE_METHODS)==6, _RUNNABLE_METHODS; print('OK')"</automated>
  </verify>
  <done>server.py exposes _RUNNABLE_METHODS (6 methods incl depth); fdars_compare_run guards on it; fdars_build_diagnostics unwraps a depth {"scores"} result to the array and forwards method_name to the depth builder.</done>
</task>

<task type="auto">
  <name>Task 3: Add tracer tests — depth end-to-end + MCP LLM-free invariant lock</name>
  <files>tests/test_mcp_server.py</files>
  <action>
Add two tests to `tests/test_mcp_server.py`, following the existing in-process
`Client(mcp)` + `_unwrap_tool_result` patterns already in the file.

Test A — `test_mcp_does_not_import_advise` (SURF-02, synchronous, no fixture, no
mcp import): resolve the directory `python/fdars/mcp/` relative to the test file
via pathlib, iterate every `*.py` under it with `rglob`, and collect any file
whose source text contains the advisor-entrypoint identifier. Assert the
collected list is empty, with a message naming the offending files. Do NOT place
that identifier as a bare literal in the assertion/comment scaffolding — reference
it only through the variable read from the file so this test does not flag itself;
build the search token at runtime (for example by joining the fragments
`"adv"` and `"ise"`) rather than writing it inline. This test needs no
`pytestmark` skip because it only reads files.

Test B — `test_run_method_depth` (SURF-01, `@pytest.mark.asyncio`, uses the
existing `dataset_id` fixture): open `Client(mcp)`; call `fdars_run_method` with
`{"dataset_id": dataset_id, "method": "depth"}`; unwrap and assert a `result_id`
is returned; confirm `registry.get_result(result_id)` returns a dict containing a
`scores` key (by-reference invariant). Then call `fdars_build_diagnostics` with
`{"dataset_id": dataset_id, "result_id": result_id, "method": "depth"}`; unwrap
and assert `method == "depth"`, `n_obs` is an int, and `depth_mean` is a float.

Run the full MCP suite to confirm existing tests stay green.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_mcp_server.py -x -q</automated>
  </verify>
  <done>test_run_method_depth and test_mcp_does_not_import_advise pass; all pre-existing tests in test_mcp_server.py stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool handler | Untrusted method name + scalar params cross the JSON boundary |
| tool handler → registry | Opaque handle IDs resolve to in-process arrays (arrays never cross JSON) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | fdars_run_method / fdars_compare_run method arg | high | mitigate | `_RUNNABLE_METHODS` frozenset allow-list checked before any fdars call (T-12-02) |
| T-22-02 | Tampering | run_method scalar params | medium | mitigate | Scalar-only signature (`float\|int\|None`); depth adds no new params (T-12-03) |
| T-22-03 | Elevation of privilege | any MCP handler | high | mitigate | `test_mcp_does_not_import_advise` locks the LLM-free invariant permanently |
| T-22-04 | Tampering | depth result dict → depth builder | medium | mitigate | Server unwraps `scores` array before build_diagnostics; guarded on `method_lc == "depth"` and dict shape |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_mcp_server.py -x -q` — all green, includes the two new tests.
- `_RUNNABLE_METHODS` is identical (6 methods) in both `_runner.py` and `server.py`.
- Grep confirms no `python/fdars/mcp/**.py` references the advisor entrypoint.
</verification>

<success_criteria>
- depth is runnable via `fdars_run_method` and its result flows through `fdars_build_diagnostics` compute-only.
- The MCP LLM-free invariant is locked by an automated file-scan test.
- No pre-existing MCP test regresses.
</success_criteria>

<output>
Create `.planning/phases/22-surface-integration/22-01-SUMMARY.md` when done.
</output>

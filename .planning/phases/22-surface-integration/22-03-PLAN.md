---
phase: 22-surface-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/fdars-advisor/SKILL.md
  - tests/test_skill.py
autonomous: true
requirements: [SURF-03, SURF-02]
estimate:
  tokens: 45000
  raw_tokens: 24000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "SKILL.md description enumerates the full per-aspect advisor coverage, not just the stale 5-aspect list (SURF-03)"
    - "SKILL.md documents provider selection incl. the local/offline Ollama + OpenAI-compatible base_url path (SURF-03/SURF-02)"
    - "SKILL.md install/compatibility note does not overclaim provider extras on PyPI today (SURF-03)"
    - "SKILL.md YAML frontmatter stays spec-valid (name==dir, parseable) (SURF-03)"
  artifacts:
    - .claude/skills/fdars-advisor/SKILL.md
    - tests/test_skill.py
  key_links:
    - "advise(provider=, model=) is the ONLY provider-selection entry point; MCP adds nothing (SURF-02)"
    - "env vars FDARS_ADVISOR_PROVIDER / _MODEL / _BASE_URL documented verbatim from _factory.py"
---

<objective>
SURF-03 (with an SURF-02 doc touchpoint): Update the Agent Skill doc
`.claude/skills/fdars-advisor/SKILL.md` with four targeted edits — NOT a rewrite —
so it reflects the full Phase-21 per-aspect coverage and the Phase-19/20 provider
selection, and correct the install note so it does not overclaim what ships on
PyPI today. Add two skill tests locking the new content.

This plan touches only `SKILL.md` + `tests/test_skill.py`, so it runs in Wave 1
in parallel with the MCP tracer (22-01) — zero file overlap.

Purpose: The Agent Skill is the entry-point documentation an LLM agent reads;
it must advertise every aspect the advisor now covers and the provider-selection
path (including key-free local Ollama), while keeping the agentskills.io
frontmatter spec-valid.
Output: SKILL.md updated (4 edits); `test_skill_md_full_aspect_coverage` and
`test_skill_md_provider_selection_section` green; existing test_skill.py green;
walkthrough script unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-surface-integration/22-RESEARCH.md

@.claude/skills/fdars-advisor/SKILL.md
@tests/test_skill.py
@python/fdars/advisor/providers/_factory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four targeted edits to SKILL.md (aspect list, provider section, install note, Tools Referenced)</name>
  <files>.claude/skills/fdars-advisor/SKILL.md</files>
  <action>
Per D-SURF-03, make exactly four surgical edits to SKILL.md. Do NOT rewrite the
file; preserve the `name: fdars-advisor` and `allowed-tools: Bash Read`
frontmatter keys, and the `## Setup`, `## Offline Walkthrough`, `## Grounded
Advice`, and `## Grounding Invariant` sections. Keep the YAML frontmatter
parseable: the block scalars `description: >` and `compatibility: >` must keep
2-space continuation indentation (research Pitfall 5), so `yaml.safe_load` still
succeeds.

(a) Aspect list in the `description` field: replace the stale
"clustering, smoothing, FPCA, alignment, or basis" phrase with the full aspect
set the advisor now covers — clustering, smoothing, FPCA, alignment,
basis/represent, depth, outliers, classification, regression, regression-CV, and
monitoring/SPM — phrased as "any analysis aspect". Also extend the `Trigger:`
sentence so it enumerates the expanded set rather than the old five.

(b) Provider Selection section: add a concise `## Provider Selection` section
(after `## Grounded Advice`) documenting that `advise(provider=, model=)` is the
sole selection entry point — nothing in the MCP surface selects a provider
(SURF-02). Cover: the explicit params; the env vars `FDARS_ADVISOR_PROVIDER`,
`FDARS_ADVISOR_MODEL`, `FDARS_ADVISOR_BASE_URL` (confirmed verbatim against
`python/fdars/advisor/providers/_factory.py` during research); the local/offline
Ollama path (pull a model, run key-free); and the OpenAI-compatible `base_url`
path (vLLM / LM Studio / LocalAI). Provider name strings: anthropic, openai,
ollama, gemini. Keep it short — a params/env table plus two small examples.

(c) Install/compatibility note: fix the overclaiming `compatibility` field and the
`## Setup` block so they do NOT state that provider extras are installable from
PyPI today. State the current git-URL install path; list the per-provider packages
a user installs manually (openai>=1.40.0, ollama>=0.6.2, google-genai for Gemini);
and note that the `[openai]`/`[ollama]`/`[gemini]` extras publish with the fdars
3.0 release (Phase 23), not now. ANTHROPIC_API_KEY is required only for Anthropic;
Ollama runs key-free. Keep the `compatibility` field mentioning Python 3.10+ and
pip so `test_skill_md_compatibility` stays green.

(d) `## Tools Referenced` section: refresh it — list all three MCP tools
(`fdars_run_method` including depth in its method list; `fdars_build_diagnostics`
accepting all 12 aspects; `fdars_compare_run`), and point at the
`python/fdars/advisor/` package (not the old single-file `advisor.py`) for
`build_diagnostics` (offline) and `advise()` (LLM interpretation).

Do NOT modify the walkthrough script (its tests already pass; multi-aspect demos
are a Phase-24 docs concern).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_skill.py -x -q</automated>
  </verify>
  <done>SKILL.md carries the full aspect list, a Provider Selection section with the local Ollama + base_url paths, a non-overclaiming install note, and a refreshed Tools Referenced section; frontmatter still parses and name==fdars-advisor; existing test_skill.py stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Add SKILL.md tests — full-aspect coverage + provider-selection section</name>
  <files>tests/test_skill.py</files>
  <action>
Add two file-scan tests to `tests/test_skill.py`, reusing the module-level
`SKILL_MD` path constant and the `_parse_frontmatter` helper already defined
there. Both are synchronous and need no network or ANTHROPIC_API_KEY.

Test A — `test_skill_md_full_aspect_coverage` (SURF-03): read `SKILL_MD.read_text()`,
lower-case it, and assert every newly-surfaced aspect keyword appears — at minimum
depth, outliers, classification, regression, and spm (monitoring/SPM). Message must
name the missing aspects and point at the `description` / `Tools Referenced`
sections.

Test B — `test_skill_md_provider_selection_section` (SURF-03/SURF-02): read the
text and assert (1) a `Provider Selection` heading is present, (2) the local
provider Ollama is mentioned (case-insensitive), and (3) the env var
`FDARS_ADVISOR_PROVIDER` appears, so the provider-selection documentation is
actually present and covers the local/offline path.

Run the full test_skill.py suite to confirm the six existing tests stay green.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_skill.py -x -q</automated>
  </verify>
  <done>test_skill_md_full_aspect_coverage and test_skill_md_provider_selection_section pass; all pre-existing test_skill.py tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent reading SKILL.md → advise() | Documentation directs which provider/credentials an agent uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Information disclosure | SKILL.md provider/install note | low | mitigate | Install note documents key-free Ollama path and only names required credentials per provider; ANTHROPIC_API_KEY noted as Anthropic-only |
| T-22-10 | Repudiation | SKILL.md overclaiming PyPI extras | low | mitigate | Corrected note states provider extras publish with fdars 3.0, preventing a broken `pip install fdars[openai]` on today's PyPI |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_skill.py -x -q` — all green, includes the two new tests.
- SKILL.md frontmatter parses via yaml.safe_load; name==fdars-advisor; compatibility mentions Python 3.10+ and pip.
- Walkthrough script and its tests unchanged.
</verification>

<success_criteria>
- SKILL.md documents the full per-aspect advisor coverage and provider selection
  (incl. local/offline), with a non-overclaiming install note.
- Two new skill tests lock the content; no pre-existing skill test regresses.
</success_criteria>

<output>
Create `.planning/phases/22-surface-integration/22-03-SUMMARY.md` when done.
</output>

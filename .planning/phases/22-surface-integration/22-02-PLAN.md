---
phase: 22-surface-integration
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - python/fdars/mcp/server.py
  - tests/test_mcp_server.py
autonomous: true
requirements: [SURF-01]
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "fdars_build_diagnostics accepts all 12 advisor aspects via _DIAGNOSTICS_METHODS (SURF-01)"
    - "represent is reachable through fdars_build_diagnostics with no result_id (data+argvals) (SURF-01)"
    - "classification diagnostics work when n_classes is forwarded as a scalar tool param (SURF-01)"
    - "diagnostics-only aspects (e.g. regression) are rejected at fdars_run_method (SURF-01)"
    - "_DIAGNOSTICS_METHODS stays in sync with advisor.build_diagnostics._supported (SURF-01)"
  artifacts:
    - python/fdars/mcp/server.py
    - tests/test_mcp_server.py
  key_links:
    - "_DIAGNOSTICS_METHODS (12) guards fdars_build_diagnostics; _RUNNABLE_METHODS (6) guards run_method/compare_run"
    - "represent branch injects argvals into the result dict so _build_represent_diagnostics can read raw.get('argvals')"
    - "n_classes tool param → build_diagnostics(result, 'classification', n_classes=K)"
---

<objective>
Expand the proven Wave-1 slice to the full diagnostics-only aspect surface
(SURF-01). Introduce the twelve-method `_DIAGNOSTICS_METHODS` guard for
`fdars_build_diagnostics`, wire the two surgical server fixes the diagnostics-only
aspects need (represent argvals injection; classification `n_classes` param), and
lock the runnable-vs-diagnostics split plus the guard/advisor sync with tests.

Purpose: Make every Phase-21 aspect reachable through the MCP surface compute-only
— runnable ones via `fdars_run_method`, the six diagnostics-only ones (outliers,
classification, represent, regression, regression_cv, spm) via
`fdars_build_diagnostics` over a caller-supplied result — while keeping the tool
boundary LLM-free and scalar-only.
Output: `fdars_build_diagnostics` handles all 12 aspects; represent + classification
tests green; run_method rejection + guard-sync tests green; existing suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-surface-integration/22-RESEARCH.md

@python/fdars/mcp/server.py
@tests/test_mcp_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _DIAGNOSTICS_METHODS (12) + n_classes param + represent argvals injection in server.py</name>
  <files>python/fdars/mcp/server.py</files>
  <action>
Per D-SURF-01, make three surgical changes to `server.py`, all inside/around
`fdars_build_diagnostics`. Keep `_RUNNABLE_METHODS` (from plan 22-01) untouched as
the guard for `fdars_run_method` and `fdars_compare_run`.

(1) Add a module-level `_DIAGNOSTICS_METHODS` frozenset listing all twelve methods
that `advisor.build_diagnostics` accepts: alignment, fpca, basis, smoothing,
clustering, depth, outliers, classification, represent, regression,
regression_cv, spm. Change the guard at the top of `fdars_build_diagnostics` to
check `method_lc not in _DIAGNOSTICS_METHODS` (was `_RUNNABLE_METHODS`), and
update its error message to list the diagnostics set. Leave the depth-scores
unwrap from plan 22-01 in place.

(2) Add a new optional scalar tool param `n_classes: int | None = None` to the
`fdars_build_diagnostics` signature (after `with_argvals`). It is a plain integer
— no T-12-03 array-injection risk. In the handler, when assembling the kwargs
passed to `build_diagnostics`, add `n_classes` to the kwargs only when it is not
None. Document it in the docstring as required for `method="classification"`.

(3) Add the represent argvals-injection fix (research Pitfall 4): when
`method_lc == "represent"` and `result_id is None`, construct the fallback result
as `{"data": data, "argvals": argvals}` instead of `{"data": data}`, so the
represent builder's `raw.get("argvals")` lookup finds the grid. Keep the general
`result = {"data": data}` fallback for every other no-result_id method.

Do not add any provider/advise logic — the handler stays compute-only.
  </action>
  <verify>
    <automated>/home/simonm/projects/rust/pyfda/.venv/bin/python -c "from fdars.mcp.server import _DIAGNOSTICS_METHODS, _RUNNABLE_METHODS; assert len(_DIAGNOSTICS_METHODS)==12 and len(_RUNNABLE_METHODS)==6 and _RUNNABLE_METHODS <= _DIAGNOSTICS_METHODS; print('OK')"</automated>
  </verify>
  <done>server.py has _DIAGNOSTICS_METHODS (12) guarding fdars_build_diagnostics; the tool accepts n_classes:int|None and forwards it; represent with result_id=None injects argvals into the result dict.</done>
</task>

<task type="auto">
  <name>Task 2: Add guard-sync test locking _DIAGNOSTICS_METHODS to advisor._supported</name>
  <files>tests/test_mcp_server.py</files>
  <action>
Per research Pitfall 1, add `test_diagnostics_methods_match_advisor_supported` to
`tests/test_mcp_server.py` (synchronous, no Client). Import `_DIAGNOSTICS_METHODS`
from `fdars.mcp.server`. Reconstruct the advisor's canonical supported set by
calling `build_diagnostics({}, "<a method name known to be absent>")` inside a
`pytest.raises(ValueError)` / try-except and parsing the raised message — the
error text embeds `Supported: [...]` (verified: sorted set is included in the
message). Assert that every method in `_DIAGNOSTICS_METHODS` appears in that
message, so a future aspect added to `advisor._supported` that is not mirrored
into `_DIAGNOSTICS_METHODS` (or a stale entry in `_DIAGNOSTICS_METHODS` the advisor
no longer supports) is caught. Prefer a substring membership check per method over
brittle full-set parsing.

This test asserts the two sets stay in sync (the T-12-02 drift risk) and needs no
network or asyncio.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_mcp_server.py::test_diagnostics_methods_match_advisor_supported -x -q</automated>
  </verify>
  <done>The sync-check test passes and would fail if _DIAGNOSTICS_METHODS diverged from advisor._supported.</done>
</task>

<task type="auto">
  <name>Task 3: Add diagnostics-only aspect tests — represent, classification(n_classes), run_method rejection</name>
  <files>tests/test_mcp_server.py</files>
  <action>
Add three tests following the existing in-process `Client(mcp)` +
`_unwrap_tool_result` + `dataset_id` fixture patterns.

Test A — `test_build_diagnostics_represent` (`@pytest.mark.asyncio`): call
`fdars_build_diagnostics` with `{"dataset_id": dataset_id, "method": "represent",
"with_argvals": True}` (no result_id). Unwrap and assert `method == "represent"`,
`n_obs` is an int, and `n_points` is an int. This exercises the argvals-injection
fix from Task 1.

Test B — `test_build_diagnostics_classification_with_n_classes`
(`@pytest.mark.asyncio`): store a small synthetic classification result dict in
the registry via `registry.store_result(...)` (a plain point-estimate shape such
as predicted labels + accuracy — match whatever `_build_classification_diagnostics`
consumes; read `python/fdars/advisor/aspects/classification.py` to pick keys that
the builder accepts without a real fdars model). Call `fdars_build_diagnostics`
with that `result_id`, `method="classification"`, and `n_classes=2`. Unwrap and
assert `method == "classification"` and that `n_classes` (or the equivalent
builder field) reflects 2. If the classification builder needs inputs a synthetic
dict cannot supply, fall back to asserting only that the `n_classes` param reaches
`build_diagnostics` (e.g. by asserting the tool does not raise a
missing-n_classes error) — keep the test compute-only and offline.

Test C — `test_run_method_rejects_diagnostics_only` (`@pytest.mark.asyncio`):
call `fdars_run_method` with `{"dataset_id": dataset_id, "method": "regression"}`
and assert it raises (mcp wraps the runner's ValueError). This proves the
runnable-vs-diagnostics split holds at the run_method boundary — a diagnostics-only
aspect cannot be dispatched by run_method.

Run the full MCP suite to confirm all pre-existing and Wave-1 tests stay green.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && .venv/bin/python -m pytest tests/test_mcp_server.py -x -q</automated>
  </verify>
  <done>represent, classification-with-n_classes, and run_method-rejection tests pass; the whole test_mcp_server.py suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → fdars_build_diagnostics | Untrusted method name + n_classes scalar cross the JSON boundary |
| tool handler → advisor.build_diagnostics | Delegated compute; handler never reimplements diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Tampering | fdars_build_diagnostics method arg | high | mitigate | `_DIAGNOSTICS_METHODS` frozenset allow-list checked before delegating (T-12-02) |
| T-22-06 | Tampering | n_classes tool param | low | accept | Typed `int \| None` in MCP schema; scalar only, no array-injection surface (T-12-03) |
| T-22-07 | Tampering | guard/advisor drift | medium | mitigate | `test_diagnostics_methods_match_advisor_supported` locks the two sets in sync |
| T-22-08 | Elevation of privilege | fdars_build_diagnostics | high | mitigate | Handler delegates to advisor.build_diagnostics only; never calls advise() (locked by 22-01 invariant test) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_mcp_server.py -x -q` — all green.
- `_DIAGNOSTICS_METHODS` has 12 members and is a superset of `_RUNNABLE_METHODS`.
- represent reachable with no result_id; classification forwards n_classes;
  run_method rejects a diagnostics-only aspect.
</verification>

<success_criteria>
- All 12 aspects reachable through the MCP surface compute-only (6 runnable via
  run_method, 6 diagnostics-only via build_diagnostics).
- Guard-sync test prevents future _DIAGNOSTICS_METHODS drift.
- No pre-existing MCP test regresses.
</success_criteria>

<output>
Create `.planning/phases/22-surface-integration/22-02-SUMMARY.md` when done.
</output>

---
phase: 46-diagram-coverage-examples
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - docs/assets/diagrams/ex-tecator-conformal-coverage.svg
  - docs/assets/diagrams/ex-tecator-monitoring.svg
  - docs/assets/diagrams/ex-tecator-regression.svg
  - docs/assets/diagrams/ex-biopharma-monitoring.svg
  - docs/assets/diagrams/ex-inline-monitoring.svg
  - docs/assets/diagrams/ex-cross-validation.svg
  - docs/assets/diagrams/ex-explainability-regions.svg
  - docs/assets/diagrams/ex-functional-outlier-workflow.svg
  - docs/assets/diagrams/ex-growth-alignment.svg
  - docs/assets/diagrams/ex-phoneme-shape.svg
  - docs/assets/diagrams/ex-tolerance-vs-conformal.svg
  - docs/examples/tecator-conformal-coverage.md
  - docs/examples/tecator-monitoring.md
  - docs/examples/tecator-regression.md
  - docs/examples/biopharma-monitoring.md
  - docs/examples/inline-monitoring.md
  - docs/examples/cross-validation.md
  - docs/examples/explainability-regions.md
  - docs/examples/functional-outlier-workflow.md
  - docs/examples/growth-alignment.md
  - docs/examples/phoneme-shape.md
  - docs/examples/tolerance-vs-conformal.md
autonomous: true
requirements: [DIACOV-01]

estimate:
  tokens: 120000
  raw_tokens: 80000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The 3 tecator-family pages each carry a method-accurate STYLE_SPEC-conformant workflow SVG (DIACOV-01, SC1, SC2)"
    - "The 2 monitoring-family pages (biopharma, inline) each carry a method-accurate workflow SVG (DIACOV-01, SC1, SC2)"
    - "The 6 misc pages (cross-validation, explainability-regions, functional-outlier-workflow, growth-alignment, phoneme-shape, tolerance-vs-conformal) each carry a method-accurate workflow SVG (DIACOV-01, SC1, SC2)"
    - "Every new ex-*.svg passes the svgo@3.3.4 idempotence gate — 2nd pass byte-identical (SC3)"
    - "Every new ex-*.svg renders to a non-empty PNG via rsvg-convert (SC4 built-site review precursor)"
    - "After this plan, all 20 gap pages carry a diagram and examples/index.md carries none; no method-page diagram modified"
  artifacts:
    - docs/assets/diagrams/ex-tecator-conformal-coverage.svg
    - docs/assets/diagrams/ex-tecator-monitoring.svg
    - docs/assets/diagrams/ex-tecator-regression.svg
    - docs/assets/diagrams/ex-biopharma-monitoring.svg
    - docs/assets/diagrams/ex-inline-monitoring.svg
    - docs/assets/diagrams/ex-cross-validation.svg
    - docs/assets/diagrams/ex-explainability-regions.svg
    - docs/assets/diagrams/ex-functional-outlier-workflow.svg
    - docs/assets/diagrams/ex-growth-alignment.svg
    - docs/assets/diagrams/ex-phoneme-shape.svg
    - docs/assets/diagrams/ex-tolerance-vs-conformal.svg
  key_links:
    - "each ex-<slug>.svg is referenced by docs/examples/<slug>.md via the .fdars-diagram image line"
    - "this plan reuses the pipeline + shared helpers proven by the 46-01 tracer verbatim"
---

<objective>
Author the remaining 11 method-accurate, STYLE_SPEC-conformant hand-authored inline concept SVGs — one per example page — covering the tecator family, the monitoring family, and the misc group, and embed each near the top of its page. Each is a workflow/pipeline diagram (dataset -> the specific fdars methods that page calls -> result/insight) in the ex-sonar-tsrvf.svg genre (DIACOV-01). Depends on 46-01, which proves the per-diagram pipeline and provides the shared gate helpers.

Purpose: Close the remaining half of the DIACOV-01 examples coverage gap so all 20 gap pages carry a diagram (examples/index.md excluded).
Output: 11 new ex-<slug>.svg files + 11 example .md pages each gaining exactly one image-reference line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/assets/diagrams/ex-sonar-tsrvf.svg
@docs/assets/diagrams/STYLE_SPEC.md
@svgo.config.mjs
@docs/assets/diagrams/ex-canadian-weather.svg
</context>

<shared_recipe>
Identical to 46-01. Per-diagram gate: `bash .planning/phases/46-diagram-coverage-examples/check-ex.sh <slug> <viewbox-height>` (SVG exists, 720-width viewBox at height, role=img, aria-label, five CSS classes; page embeds via `.fdars-diagram`; svgo@3.3.4 2nd-pass byte-identical; rsvg PNG non-empty). Height 300 single-row / 480 tall.

Embed (ONLY .md edit allowed per page — no prose rewrites; Phase 48): after intro prose, before first `##`, add `![<page title> — <short caption>](../assets/diagrams/ex-<slug>.svg){ .fdars-diagram }` mirroring docs/examples/sonar-tsrvf.md.

Authoring: hand-author inline SVG; copy the canonical five-class `<style>` block verbatim; palette + stroke weights per STYLE_SPEC; workflow genre per ex-sonar-tsrvf.svg + the ex-canadian-weather.svg tracer as the concrete in-repo template. Read each target page AND the fdars methods it calls before drawing — method-accuracy is the core value; do NOT invent steps. Scope 2-5 panels.

Escalation: judgment-call depictions -> draw best-supported version + note in SUMMARY for Phase 49 human review.

No-regression guard (end of each task): `bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh <slug>...`.
</shared_recipe>

<tasks>

<task type="auto">
  <name>Task 1: Tecator family — author + embed 3 workflow SVGs (regression, conformal-coverage, monitoring)</name>
  <files>docs/assets/diagrams/ex-tecator-regression.svg, docs/assets/diagrams/ex-tecator-conformal-coverage.svg, docs/assets/diagrams/ex-tecator-monitoring.svg, docs/examples/tecator-regression.md, docs/examples/tecator-conformal-coverage.md, docs/examples/tecator-monitoring.md</files>
  <read_first>
    - docs/examples/tecator-regression.md, tecator-conformal-coverage.md, tecator-monitoring.md (each full page + the fdars methods it calls)
    - docs/assets/diagrams/ex-canadian-weather.svg (proven template)
    - docs/assets/diagrams/STYLE_SPEC.md
  </read_first>
  <action>
Reuse <shared_recipe>. Method-accurate depiction per page (confirm against page before drawing):
- tecator-regression: scalar-on-function regression — NIR spectra -> fit/compare fregre_lm, fregre_pls, functional_logistic (with fpca), read beta(lambda) coefficient curve (significant_regions_from_se), residual check, high/low-fat classification; uses deriv_1d, lp_self_1d/lp_cross_1d for preprocessing. Depict spectra -> regression fit -> beta(lambda) + significant regions -> predicted fat.
- tecator-conformal-coverage: conformal coverage guarantee — fdars.conformal.conformal_fregre_lm on the tecator regression; depict train/calibration split -> conformal prediction band -> empirical coverage vs target (1-alpha) guarantee.
- tecator-monitoring: SPM monitoring on tecator — fdars.spm.spm_phase with select_ncomp, ewma_scores, western_electric_rules; depict Phase I baseline -> Phase II EWMA scores -> control limits + Western Electric rule flags.

Height per content. Run check-ex.sh per slug + the no-regression guard.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for s in tecator-regression tecator-conformal-coverage tecator-monitoring; do h=$(grep -oE 'viewBox="0 0 720 [0-9]+"' docs/assets/diagrams/ex-$s.svg | grep -oE '[0-9]+"$' | tr -d '"'); bash .planning/phases/46-diagram-coverage-examples/check-ex.sh $s $h || exit 1; done && bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh tecator-regression tecator-conformal-coverage tecator-monitoring</automated>
  </verify>
  <acceptance_criteria>
    - All 3 ex-tecator-*.svg exist, STYLE_SPEC-conformant (720-width viewBox, role=img, aria-label, 5 classes)
    - Each is method-accurate: scalar-on-function regression+beta / conformal coverage band / spm EWMA monitoring respectively
    - Each of the 3 pages contains its .fdars-diagram reference; no prose otherwise rewritten
    - Each SVG: svgo idempotent, rsvg PNG non-empty; only these 3 new SVGs + 3 pages changed
  </acceptance_criteria>
  <done>All 3 tecator-family diagrams authored, embedded, gate-green; no-regression guard passes.</done>
</task>

<task type="auto">
  <name>Task 2: Monitoring family — author + embed 2 workflow SVGs (biopharma-monitoring, inline-monitoring)</name>
  <files>docs/assets/diagrams/ex-biopharma-monitoring.svg, docs/assets/diagrams/ex-inline-monitoring.svg, docs/examples/biopharma-monitoring.md, docs/examples/inline-monitoring.md</files>
  <read_first>
    - docs/examples/biopharma-monitoring.md, inline-monitoring.md (each full page + the fdars methods it calls)
    - docs/assets/diagrams/ex-canadian-weather.svg (proven template)
    - docs/assets/diagrams/STYLE_SPEC.md
  </read_first>
  <action>
Reuse <shared_recipe>. Method-accurate depiction per page:
- biopharma-monitoring: SPM Phase I/II monitoring on penicillin batches — load_penicillin, fdars.spm.spm_phase, with fregre_lm / fregre_cv / fpca as feature/regression context; depict Phase I in-control batches -> control-limit estimation -> Phase II new batch flagged / in-control.
- inline-monitoring: streaming / inline SPM monitoring — fdars.simulation.simulate (+ eigenfunctions) generates the stream, fdars.spm.spm_phase + arl1_t2 (average run length to alarm); depict incoming curve stream -> running T2/monitoring statistic vs limit -> alarm at shift with ARL1 annotation.

Height per content. Run check-ex.sh per slug + the no-regression guard.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for s in biopharma-monitoring inline-monitoring; do h=$(grep -oE 'viewBox="0 0 720 [0-9]+"' docs/assets/diagrams/ex-$s.svg | grep -oE '[0-9]+"$' | tr -d '"'); bash .planning/phases/46-diagram-coverage-examples/check-ex.sh $s $h || exit 1; done && bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh biopharma-monitoring inline-monitoring</automated>
  </verify>
  <acceptance_criteria>
    - Both ex-*.svg exist, STYLE_SPEC-conformant
    - Each is method-accurate: penicillin Phase I/II spm / streaming spm with arl1_t2 respectively
    - Each page contains its .fdars-diagram reference; no prose otherwise rewritten
    - Each SVG: svgo idempotent, rsvg PNG non-empty; only these 2 new SVGs + 2 pages changed
  </acceptance_criteria>
  <done>Both monitoring-family diagrams authored, embedded, gate-green; no-regression guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: Misc group — author + embed 6 workflow SVGs (cross-validation, explainability-regions, functional-outlier-workflow, growth-alignment, phoneme-shape, tolerance-vs-conformal)</name>
  <files>docs/assets/diagrams/ex-cross-validation.svg, docs/assets/diagrams/ex-explainability-regions.svg, docs/assets/diagrams/ex-functional-outlier-workflow.svg, docs/assets/diagrams/ex-growth-alignment.svg, docs/assets/diagrams/ex-phoneme-shape.svg, docs/assets/diagrams/ex-tolerance-vs-conformal.svg, docs/examples/cross-validation.md, docs/examples/explainability-regions.md, docs/examples/functional-outlier-workflow.md, docs/examples/growth-alignment.md, docs/examples/phoneme-shape.md, docs/examples/tolerance-vs-conformal.md</files>
  <read_first>
    - docs/examples/cross-validation.md, explainability-regions.md, functional-outlier-workflow.md, growth-alignment.md, phoneme-shape.md, tolerance-vs-conformal.md (each full page + the fdars methods it calls)
    - docs/assets/diagrams/ex-canadian-weather.svg (proven template)
    - docs/assets/diagrams/STYLE_SPEC.md
  </read_first>
  <action>
Reuse <shared_recipe> for all 6. This is the largest batch — pace it, gate each diagram as you go. Method-accurate depiction per page (confirm against page before drawing):
- cross-validation: CV fold split -> error curve — fdars.regression.fregre_cv comparing fregre_lm / fregre_pls / fregre_np (predict_* variants), lp_self_1d/lp_cross_1d + deriv for features; depict data -> k-fold split -> per-model CV error curve -> selected model.
- explainability-regions: importance curve with highlighted region — fdars.explain (significant_regions, significant_regions_from_se, pointwise_importance, functional_saliency, functional_pdp, beta_decomposition, domain_selection) on a fregre_lm fit with bootstrap_ci_fregre_lm; depict fitted beta(t)/importance curve -> CI band -> highlighted significant region.
- functional-outlier-workflow: outlier pipeline (magnitude + shape) — fdars.simulation.simulate builds normal + magnitude + shape outliers; fdars.outliers.magnitude_shape (MS-plot catches magnitude) + outliergram (catches shape); depict sample -> MS-plot (magnitude flagged) + outliergram (shape flagged) -> each tool catches its type.
- growth-alignment: elastic alignment on growth data — load_growth, fdars.alignment.elastic_align_pair / karcher_mean / alignment_quality, with deriv_1d + fpca + tolerance.equivalence_test; depict raw growth (velocity) curves -> elastic alignment to Karcher mean -> aligned curves + amplitude/phase separation + alignment quality.
- phoneme-shape: shape-based classification — load_phoneme, fdars.alignment.shape_mean / shape_self_distance_matrix / alignment_quality, lp_self_1d; depict phoneme curves -> shape distance matrix / shape mean per class -> shape-based class separation.
- tolerance-vs-conformal: tolerance band vs conformal band comparison — load_canadian_weather, fdars.tolerance.fpca_tolerance_band vs conformal_prediction_band (both from the tolerance module); depict curves -> two bands side by side (tolerance vs conformal) -> coverage/interpretation contrast.

Height 300 or 480 per content. Run check-ex.sh per slug + the no-regression guard over all 6.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && for s in cross-validation explainability-regions functional-outlier-workflow growth-alignment phoneme-shape tolerance-vs-conformal; do h=$(grep -oE 'viewBox="0 0 720 [0-9]+"' docs/assets/diagrams/ex-$s.svg | grep -oE '[0-9]+"$' | tr -d '"'); bash .planning/phases/46-diagram-coverage-examples/check-ex.sh $s $h || exit 1; done && bash .planning/phases/46-diagram-coverage-examples/check-noregress.sh cross-validation explainability-regions functional-outlier-workflow growth-alignment phoneme-shape tolerance-vs-conformal</automated>
  </verify>
  <acceptance_criteria>
    - All 6 ex-*.svg exist, STYLE_SPEC-conformant (720-width viewBox, role=img, aria-label, 5 classes)
    - Each is method-accurate to that page's actual fdars calls (CV / explain-regions / MS+outliergram / elastic-align / shape-classify / tolerance-vs-conformal respectively)
    - Each of the 6 pages contains its .fdars-diagram reference; no prose otherwise rewritten
    - Each SVG: svgo idempotent, rsvg PNG non-empty; only these 6 new SVGs + 6 pages changed
  </acceptance_criteria>
  <done>All 6 misc diagrams authored, embedded, gate-green; no-regression guard passes.</done>
</task>

</tasks>

<verification>
- All 11 target pages reference a new ex-<slug>.svg via `.fdars-diagram`.
- Every new SVG: 720-width viewBox, role=img, aria-label, five canonical classes; svgo@3.3.4 2nd-pass byte-identical; rsvg PNG non-empty.
- Phase-wide close-out (with 46-01): `ls docs/assets/diagrams/ex-*.svg | wc -l` == 21 (20 new + pre-existing ex-sonar-tsrvf.svg).
- Every gap page carries its diagram: `for s in $(ls docs/examples/*.md | xargs -n1 basename | sed 's/\.md$//' | grep -v '^index$' | grep -v '^sonar-tsrvf$'); do grep -Fq "](../assets/diagrams/ex-$s.svg){ .fdars-diagram }" docs/examples/$s.md || echo "MISSING: $s"; done` prints nothing.
- examples/index.md has NO diagram; `git status` shows only new ex-*.svg + edited example pages (+ this plan's helper scripts).
</verification>

<success_criteria>
11 method-accurate, STYLE_SPEC-conformant workflow SVGs authored and embedded across the tecator + monitoring + misc groups; per-diagram svgo idempotence + rsvg PNG green; combined with 46-01 all 20 gap pages carry a diagram (index excluded); no unrelated file modified; any judgment-call depictions surfaced in the SUMMARY for Phase 49.
</success_criteria>

<output>
Create `.planning/phases/46-diagram-coverage-examples/46-02-SUMMARY.md` when done. List the 11 new SVG filenames + 11 edited pages, note each diagram's depicted method arc, confirm the phase-wide count (20/20 gap pages + index has none), and flag any ambiguous/judgment-call depictions for the Phase 49 blocking human diagram method-accuracy review.
</output>
---
phase: 77-section-landing-card-coverage
plan: 04
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - docs/assets/thumb/functional-time-series.svg
  - docs/assets/thumb/density-fda.svg
  - docs/assets/thumb/advanced-clustering.svg
  - docs/assets/thumb/multi-domain.svg
  - docs/assets/thumb/shapelets.svg
  - docs/assets/thumb/functional-boxplot.svg
  - docs/assets/thumb/functional-statistics.svg
  - docs/assets/thumb/scoring-metrics.svg
  - docs/analyze/index.md
autonomous: true
use_worktrees: false
requirements: [CARD-04, CARD-06]
estimate:
  tokens: 66000
  raw_tokens: 44000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The analyze landing gallery (docs/analyze/index.md) gains cards for functional-time-series, density-fda, advanced-clustering, multi-domain, shapelets, functional-boxplot, functional-statistics, scoring-metrics so every focus-section analyze page is carded (CARD-04)"
    - "Eight new hand-authored inline-SVG thumbnails exist under docs/assets/thumb/, each viewBox 0 0 320 180, fill=none, role=img + aria-label, section hue #6f42c1, STYLE_SPEC-conformant, and SVGO-idempotent under svgo@3.3.4 (CARD-06)"
  artifacts:
    - docs/assets/thumb/functional-time-series.svg
    - docs/assets/thumb/density-fda.svg
    - docs/assets/thumb/advanced-clustering.svg
    - docs/assets/thumb/multi-domain.svg
    - docs/assets/thumb/shapelets.svg
    - docs/assets/thumb/functional-boxplot.svg
    - docs/assets/thumb/functional-statistics.svg
    - docs/assets/thumb/scoring-metrics.svg
    - docs/analyze/index.md
  key_links:
    - "Each new card's src=../assets/thumb/<slug>.svg resolves to the new thumbnail file and href=<slug>/ resolves to the existing analyze method page (no 404)"
    - "New thumbnails are SVGO-idempotent so the consolidated Phase 79 GATE-02 determinism gate passes without later fixes"
---

<objective>
Card the ANALYZE section (CARD-04) — author eight new 320×180 inline-SVG thumbnails in the analyze hue #6f42c1 (functional-time-series, density-fda, advanced-clustering, multi-domain, shapelets, functional-boxplot, functional-statistics, scoring-metrics), verify each is SVGO-idempotent and smoke-renders, and insert all eight fdars-gallery cards into docs/analyze/index.md. Follows the pipeline proven by the align tracer (77-01).

Purpose: Complete analyze-section card coverage for the eight v11.0-era analyze methods using the proven author→SVGO-idempotence→insert→smoke-render pattern.
Output: Eight SVGO-idempotent thumbnails + eight new analyze cards, committed on main.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-section-landing-card-coverage/77-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new code symbols, bindings, or scripts. This plan CREATES eight new tracked SVG files under `docs/assets/thumb/` (functional-time-series, density-fda, advanced-clustering, multi-domain, shapelets, functional-boxplot, functional-statistics, scoring-metrics) and EDITS one existing file, `docs/analyze/index.md` (inserting eight `fdars-gallery` card blocks in two groups). No other file is touched; the analyze index.md and these eight thumb files are disjoint from every other plan in the phase.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author eight analyze thumbnails + insert eight cards</name>
  <files>docs/assets/thumb/functional-time-series.svg, docs/assets/thumb/density-fda.svg, docs/assets/thumb/advanced-clustering.svg, docs/assets/thumb/multi-domain.svg, docs/assets/thumb/shapelets.svg, docs/assets/thumb/functional-boxplot.svg, docs/assets/thumb/functional-statistics.svg, docs/assets/thumb/scoring-metrics.svg, docs/analyze/index.md</files>
  <read_first>
    - .planning/phases/77-section-landing-card-coverage/77-RESEARCH.md (§1 house style + skeleton; §2 analyze hue #6f42c1; §3 CARD-04 the 8-item table with exact Gallery Title / Gallery Desc / Thumbnail Concept; §4 analyze/index.md insertion points — advanced-clustering after elastic-clustering (before outlier-detection), then functional-time-series/density-fda/multi-domain/shapelets/functional-boxplot/functional-statistics/scoring-metrics appended after covariance-functions; §5 SVGO two-pass idempotence; §6 pitfalls 1/2/3/4/7/8/9; §7 detailed concept sketches for all eight)
    - docs/assets/thumb/tolerance-bands.svg (analyze house-style reference — exact #6f42c1 stroke, axis lines, a <linearGradient id="tb"> band fill, dashed band boundaries stroke-dasharray="5 5", primary path stroke-width="3" stroke-linecap="round"; MODEL functional-boxplot on this with three fill layers)
    - docs/assets/thumb/clustering.svg (second analyze reference — multi-path bundles for advanced-clustering)
    - docs/assets/thumb/distance-metrics.svg (reference for scoring-metrics gap-segment style)
    - docs/analyze/index.md (the fdars-gallery block; existing cards tolerance-bands/clustering/gmm-clustering/elastic-clustering/outlier-detection/seasonal-analysis/equivalence-testing/covariance-functions; insertion anchors are the `<a ... href="outlier-detection/">` line and the closing `</a>` of the covariance-functions card / the gallery-closing `</div>` — NO blank lines between tags in this section)
    - docs/assets/diagrams/STYLE_SPEC.md (decorative-thumbnail rule: img alt="" + aria-hidden="true"; SVG role="img" + aria-label; no title/desc/aria-labelledby on thumbnails)
  </read_first>
  <action>
Author eight new minimal hand-authored inline-SVG thumbnails and insert their gallery cards into `docs/analyze/index.md`. Author fresh minimal line-art per the RESEARCH §7 concept sketches — do NOT down-scale the full concept diagrams. (CARD-04, CARD-06)

THUMBNAIL HOUSE STYLE (RESEARCH §1 — match the analyze corpus exactly):
- Root: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 180" fill="none" role="img" aria-label="<3-6 word description>">`. viewBox ALWAYS `0 0 320 180` (RESEARCH pitfall 1).
- Section hue `#6f42c1` (purple) on every stroke/fill (RESEARCH §2).
- L-shaped axis: x-axis `<line x1="30" y1="150" x2="298" y2="150" stroke="#6f42c1" stroke-opacity=".35" stroke-width="1.4"/>`; y-axis `<line x1="30" y1="150" x2="30" y2="28" stroke="#6f42c1" stroke-opacity=".35" stroke-width="1.4"/>` (adapt for multi-domain's two-panel layout — a short axis per panel).
- 2-4 paths (a bundle thumbnail like advanced-clustering may use a few more short paths grouped in a shared-stroke <g>); primary `stroke-width="3" stroke-linecap="round"`; faint/secondary reduced `stroke-opacity`; dashed via `stroke-dasharray`. Coordinates within x∈[30,298], y∈[28,150] (RESEARCH pitfall 9).
- NO <title>/<desc>/aria-labelledby/<style>/xmlns:xlink/path-id (pitfalls 2/3). A shared-stroke <g> wrapper is allowed for grouping bundle paths (as in clustering.svg / ex-canadian-weather.svg). If a <linearGradient> is used, unique 2-char id per file (`fts`,`df`,`ac`,`md`,`sh`,`fb`,`fs`,`sm`) (RESEARCH pitfall 4). OMIT text labels. Target 700–1100 bytes (a multi-layer thumb like functional-boxplot may reach ~1200); aria-label a short 3-6 word description.

THUMBNAIL 1 — `functional-time-series.svg` (RESEARCH §3 row 11 + §7): three curves of similar sinusoidal shape stacked ~10px apart in y, getting progressively lighter (t, t+1, t+2), plus a fourth dashed curve slightly ahead of the third (the forecast, stroke-dasharray). aria-label "Functional time series forecast". Hue `#6f42c1`.

THUMBNAIL 2 — `density-fda.svg` (RESEARCH §3 row 12 + §7): three faint Gaussian-bell-shaped curves at different horizontal positions/widths, plus one bolder flatter/straighter curve (the LQD-transformed version in an unconstrained domain); a horizontal baseline. aria-label "Density FDA LQD". Hue `#6f42c1`.

THUMBNAIL 3 — `advanced-clustering.svg` (RESEARCH §3 row 13 + §7): four curve groups — two tight bundles (2–3 close paths each, higher opacity), one sparse group (2 paths with a moderate gap), one outlying single path; dashed concave-hull outlines around groups. Model the bundle style on clustering.svg. aria-label "Advanced clustering". Hue `#6f42c1`.

THUMBNAIL 4 — `multi-domain.svg` (RESEARCH §3 row 14 + §7): two small axis panels side by side (left panel one curve, right panel another curve of different shape), connected by a bridging arc/path with a `<circle>` at its apex (the shared FPC score). aria-label "Multi-domain joint FPCA". Hue `#6f42c1`.

THUMBNAIL 5 — `shapelets.svg` (RESEARCH §3 row 15 + §7): a long wavy curve with a highlighted dashed-bordered `<rect>` window around a short distinctive feature, plus a dashed vertical bracket below the window indicating the matched distance. aria-label "Shapelet subsequence match". Hue `#6f42c1`.

THUMBNAIL 6 — `functional-boxplot.svg` (RESEARCH §3 row 16 + §7): three concentric band layers (inner darkest fill, middle, outer lightest fill) around a median curve, with one curve departing outside the outer band (outlier). Model on tolerance-bands.svg but with three fill layers instead of one (use three closed-path fills at descending opacity, or one <linearGradient> plus two solid fill-opacity paths). aria-label "Functional boxplot". Hue `#6f42c1`.

THUMBNAIL 7 — `functional-statistics.svg` (RESEARCH §3 row 17 + §7): a bold mean curve with a symmetric shaded envelope (upper and lower dashed bounding lines, optionally a <linearGradient> fill between them) and three short vertical standard-deviation segments at x≈100, 170, 250. Model on fpca.svg's gradient fill. aria-label "Functional summary statistics". Hue `#6f42c1`.

THUMBNAIL 8 — `scoring-metrics.svg` (RESEARCH §3 row 18 + §7): a true curve (solid, primary) and a predicted curve (dashed) with three vertical gap-line segments bridging them at x≈100, 170, 250 (the integrated prediction error). Model on distance-metrics.svg. aria-label "Functional scoring metrics". Hue `#6f42c1`.

SVGO IDEMPOTENCE (RESEARCH §5 — CARD-06, required before insertion): for EACH new thumbnail, from repo root:
`first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/thumb/<slug>.svg --output -)`; `second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)`; `diff <(printf '%s' "$first") <(printf '%s' "$second")` — MUST be empty. Never overwrite the committed hand-authored file with SVGO output.

SMOKE RENDER (RESEARCH §5): `rsvg-convert -w 320 -h 180 docs/assets/thumb/<slug>.svg -o /tmp/<slug>.png` — must exit 0 for each. rsvg-convert is at /usr/bin/rsvg-convert.

CARD INSERTION into `docs/analyze/index.md` (RESEARCH §4 — exact template, NO blank lines between tags, href trailing slash, src `../assets/thumb/<slug>.svg`). Insert in TWO groups:
Group A — insert the `advanced-clustering` card immediately BEFORE the existing `<a class="fdars-gallery-item" href="outlier-detection/">` line (i.e. right after the elastic-clustering card's closing `</a>`):
```
<a class="fdars-gallery-item" href="advanced-clustering/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/advanced-clustering.svg" alt="">
<div class="fdars-gallery-title">Advanced Clustering</div>
<div class="fdars-gallery-desc">Density-based, structure-aware, and alignment-coupled clustering methods.</div>
</a>
```
Group B — insert the remaining SEVEN cards as a contiguous block immediately AFTER the existing covariance-functions card's closing `</a>` (i.e. as the last cards, right before the gallery-closing `</div>`), in order functional-time-series → density-fda → multi-domain → shapelets → functional-boxplot → functional-statistics → scoring-metrics:
```
<a class="fdars-gallery-item" href="functional-time-series/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/functional-time-series.svg" alt="">
<div class="fdars-gallery-title">Functional Time Series</div>
<div class="fdars-gallery-desc">Forecast, test stationarity, and model autocorrelation for curve sequences.</div>
</a>
<a class="fdars-gallery-item" href="density-fda/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/density-fda.svg" alt="">
<div class="fdars-gallery-title">Density FDA</div>
<div class="fdars-gallery-desc">Analyze probability densities as functional objects via the LQD transform.</div>
</a>
<a class="fdars-gallery-item" href="multi-domain/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/multi-domain.svg" alt="">
<div class="fdars-gallery-title">Multi-Domain FDA</div>
<div class="fdars-gallery-desc">Joint FPCA across multiple functional variables per subject.</div>
</a>
<a class="fdars-gallery-item" href="shapelets/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/shapelets.svg" alt="">
<div class="fdars-gallery-title">Shapelets</div>
<div class="fdars-gallery-desc">Discriminative subsequences that separate classes by minimum-distance matching.</div>
</a>
<a class="fdars-gallery-item" href="functional-boxplot/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/functional-boxplot.svg" alt="">
<div class="fdars-gallery-title">Functional Boxplot</div>
<div class="fdars-gallery-desc">Depth-based central region, fences, and outlier detection for curves.</div>
</a>
<a class="fdars-gallery-item" href="functional-statistics/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/functional-statistics.svg" alt="">
<div class="fdars-gallery-title">Functional Statistics</div>
<div class="fdars-gallery-desc">Pointwise variance, covariance surface, and depth-based median of curves.</div>
</a>
<a class="fdars-gallery-item" href="scoring-metrics/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/scoring-metrics.svg" alt="">
<div class="fdars-gallery-title">Scoring Metrics</div>
<div class="fdars-gallery-desc">Integrated prediction-error metrics: MAE, MSE, MAPE, MSLE, explained variance.</div>
</a>
```
Do NOT reorder existing cards. Do NOT touch any other section index.md. Do NOT run the whole-site `mkdocs build --strict` (deferred to Phase 79).
  </action>
  <acceptance_criteria>
    - the eight files docs/assets/thumb/{functional-time-series,density-fda,advanced-clustering,multi-domain,shapelets,functional-boxplot,functional-statistics,scoring-metrics}.svg all exist
    - each new thumbnail contains `viewBox="0 0 320 180"` (grep matches 1 per file)
    - each new thumbnail contains the analyze hue `#6f42c1` (grep matches >= 1 per file) and no other section hex (#fd7e14/#198754/#dc3545/#3f51b5 all match 0)
    - each new thumbnail carries `role="img"` and an `aria-label="` attribute
    - no new thumbnail contains `<title`, `<desc`, `aria-labelledby`, or `<style` (grep matches 0 each)
    - each new thumbnail file is under ~1300 bytes
    - docs/analyze/index.md contains `href="<slug>/"` and `src="../assets/thumb/<slug>.svg"` for each of the eight slugs
    - for each new thumbnail the SVGO two-pass idempotence diff is empty (pass1 == pass2)
    - each new thumbnail smoke-renders via rsvg-convert with exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const idx=fs.readFileSync("docs/analyze/index.md","utf8");const slugs=["functional-time-series","density-fda","advanced-clustering","multi-domain","shapelets","functional-boxplot","functional-statistics","scoring-metrics"];const bad=[];for(const slug of slugs){const f="docs/assets/thumb/"+slug+".svg";if(!fs.existsSync(f)){bad.push(slug+":missing");continue;}const s=fs.readFileSync(f,"utf8");const sz=Buffer.byteLength(s);const checks={viewBox:/viewBox="0 0 320 180"/.test(s),hue:/#6f42c1/i.test(s),no_other_hue:!/#fd7e14|#198754|#dc3545|#3f51b5/i.test(s),role:/role="img"/.test(s),aria:/aria-label="/.test(s),no_title:!/<title/.test(s),no_desc:!/<desc/.test(s),no_labelledby:!/aria-labelledby/.test(s),no_style:!/<style/.test(s),size:sz<1300,card_href:new RegExp("href=\""+slug+"/\"").test(idx),card_src:new RegExp("src=\"../assets/thumb/"+slug+".svg\"").test(idx)};for(const [k,v] of Object.entries(checks))if(!v)bad.push(slug+":"+k);}if(bad.length){console.error("FAIL: "+bad.join(", "));process.exit(1);}console.log("STRUCT OK analyze 8 thumbnails + 8 cards");'</automated>
    <fails_when>Any of the eight thumbnail files is missing; a thumbnail lacks viewBox="0 0 320 180"; a thumbnail lacks the #6f42c1 analyze hue or carries another section's hex; a thumbnail lacks role="img" or aria-label; a thumbnail contains <title>/<desc>/aria-labelledby/<style>; a thumbnail exceeds ~1300 bytes; or docs/analyze/index.md is missing any card's href or src.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && rc=0; for slug in functional-time-series density-fda advanced-clustering multi-domain shapelets functional-boxplot functional-statistics scoring-metrics; do f="docs/assets/thumb/$slug.svg"; first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); if ! diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null; then echo "NON-IDEMPOTENT: $slug"; rc=1; fi; rsvg-convert -w 320 -h 180 "$f" -o /dev/null || { echo "RENDER FAIL: $slug"; rc=1; }; done; [ $rc -eq 0 ] && echo "SVGO idempotent + render OK"; exit $rc</automated>
    <fails_when>For any new thumbnail the two SVGO passes differ (non-idempotent — would fail the Phase 79 GATE-02 determinism gate), or rsvg-convert returns non-zero (the SVG does not parse/render).</fails_when>
  </verify>
  <done>docs/assets/thumb/{functional-time-series,density-fda,advanced-clustering,multi-domain,shapelets,functional-boxplot,functional-statistics,scoring-metrics}.svg exist as fresh minimal 320×180 #6f42c1 line-art (role="img" + aria-label, no title/desc/style, <1300 bytes each), all eight SVGO-idempotent and rsvg-convert-clean, and docs/analyze/index.md carries their eight fdars-gallery cards (advanced-clustering after elastic-clustering; the other seven appended after covariance-functions) so every focus-section analyze page is carded.</done>
  <reversibility rating="reversible">Docs-only: eight new SVG files + additive card insertions into one index.md; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — creating eight hand-authored static SVG assets and inserting eight markdown card blocks into one section index. No code, no network surface, no user-input handling. The SVGs are decorative line-art with no `<script>`, no `foreignObject`, no external references — no new attack surface. No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner): all eight thumbnail files exist, each has viewBox="0 0 320 180", the #6f42c1 analyze hue and no other section hex, role="img" + aria-label, no title/desc/aria-labelledby/style, <1300 bytes; docs/analyze/index.md carries all eight cards' href + src.
- Determinism gate (shell loop): each thumbnail's SVGO two-pass diff is empty (idempotent) and rsvg-convert renders it without error.
- Whole-site `mkdocs build --strict` and the consolidated SVGO/determinism GATE-02 are intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
CARD-04 + CARD-06 (analyze portion) delivered: the analyze landing gallery carries eight new SVGO-idempotent, STYLE_SPEC-conformant, decorative-accessible #6f42c1 thumbnails covering the v11.0-era analyze methods.
</success_criteria>

<output>
Create `.planning/phases/77-section-landing-card-coverage/77-04-SUMMARY.md` when done.
</output>

---
phase: 77-section-landing-card-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/thumb/banded-alignment.svg
  - docs/assets/thumb/shift-registration.svg
  - docs/align/index.md
autonomous: true
use_worktrees: false
requirements: [CARD-01, CARD-06]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The align landing gallery (docs/align/index.md) is at 100% card coverage: every align focus page has a card, including the two new ones shift-registration and banded-alignment (CARD-01)"
    - "Two new hand-authored inline-SVG thumbnails exist at docs/assets/thumb/shift-registration.svg and docs/assets/thumb/banded-alignment.svg, each viewBox 0 0 320 180, fill=none, role=img + descriptive aria-label, section hue #fd7e14, STYLE_SPEC-conformant, and SVGO-idempotent under svgo@3.3.4 (CARD-06)"
  artifacts:
    - docs/assets/thumb/shift-registration.svg
    - docs/assets/thumb/banded-alignment.svg
    - docs/align/index.md
  key_links:
    - "Each new card's src=../assets/thumb/<slug>.svg resolves to the new thumbnail file and href=<slug>/ resolves to the existing align method page (no 404)"
    - "New thumbnails are SVGO-idempotent so the consolidated Phase 79 GATE-02 determinism gate passes without later fixes"
---

<objective>
TRACER: Fully card the ALIGN section end-to-end (CARD-01) — author both new 320×180 inline-SVG thumbnails in the align hue #fd7e14 (shift-registration, banded-alignment), verify each is SVGO-idempotent and smoke-renders, and insert both fdars-gallery cards into docs/align/index.md so the align gallery reaches 100% coverage — BEFORE the other four sections follow the same proven pattern.

Purpose: Align is the smallest section (2 thumbnails). Proving the whole "author minimal section-hued thumbnail → SVGO-idempotence → insert fdars-gallery card → smoke-render → gallery 100%" pipeline here means any house-style or determinism problem surfaces after one section instead of five.
Output: Two new SVGO-idempotent thumbnails + a 100%-covered align gallery, all committed on main.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-section-landing-card-coverage/77-RESEARCH.md
</context>

<artifacts_produced>
Docs-only. No new code symbols, bindings, or scripts. This plan CREATES two new tracked SVG files — `docs/assets/thumb/shift-registration.svg` and `docs/assets/thumb/banded-alignment.svg` — and EDITS one existing file, `docs/align/index.md` (inserting two `fdars-gallery` card blocks). No other file is touched. The align section's `index.md` is disjoint from every other section's `index.md` and these two thumb files are disjoint from every other plan's thumb files, so there is no cross-plan collision even though all five card plans run in the phase.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Author align thumbnails (shift-registration, banded-alignment) + insert both cards (tracer)</name>
  <files>docs/assets/thumb/shift-registration.svg, docs/assets/thumb/banded-alignment.svg, docs/align/index.md</files>
  <read_first>
    - .planning/phases/77-section-landing-card-coverage/77-RESEARCH.md (§1 Thumbnail House Style + skeleton template; §2 align hue #fd7e14; §3 CARD-01 the 2-item table with exact Gallery Title / Gallery Desc / Thumbnail Concept; §4 align/index.md insertion points — banded-alignment right after advanced-alignment, shift-registration after alignment-comparison; §5 SVGO two-pass idempotence command; §6 pitfalls 1/2/3/4/7/8/9; §7 detailed concept sketches for shift-registration + banded-alignment)
    - docs/assets/thumb/elastic-alignment.svg (align house-style reference — exact #fd7e14 stroke, axis lines at x1=30 y1=150 x2=298 y2=150 and x1=30 y1=150 x2=30 y2=28 with stroke-opacity=".35" stroke-width="1.4", 2-4 cubic-Bézier paths, primary path stroke-width="3" stroke-linecap="round", an arrowhead path with Z fill; ~916 bytes)
    - docs/assets/thumb/advanced-alignment.svg (second align reference for the corridor/band style)
    - docs/align/index.md (the fdars-gallery block lines 12-43 — 6 existing cards; the two insertion anchors are the `<a ... href="landmark-registration/">` line and the `<a ... href="shape-analysis/">` line; NO blank lines between `<a>`/`<img>`/`<div>`/`</a>` in this section)
    - docs/assets/diagrams/STYLE_SPEC.md (decorative-thumbnail rule ~line 149: img uses alt="" AND aria-hidden="true"; SVG carries role="img" + aria-label; no <title>/<desc>/aria-labelledby on thumbnails)
  </read_first>
  <action>
Author two new minimal hand-authored inline-SVG thumbnails and insert their gallery cards into `docs/align/index.md`, bringing the align gallery to 100% coverage. Do NOT down-scale the full concept diagrams in `docs/assets/diagrams/` — author fresh minimal line-art per the RESEARCH §7 concept sketches. (CARD-01, CARD-06)

THUMBNAIL HOUSE STYLE (RESEARCH §1 — match the align corpus exactly):
- Root element verbatim shape: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 180" fill="none" role="img" aria-label="<3-6 word description>">`. viewBox is ALWAYS `0 0 320 180` (never the 720×N concept-diagram size — RESEARCH pitfall 1).
- Section hue is `#fd7e14` (orange) on every stroke/fill — derived from the align corpus (RESEARCH §2).
- Include the L-shaped axis: x-axis `<line x1="30" y1="150" x2="298" y2="150" stroke="#fd7e14" stroke-opacity=".35" stroke-width="1.4"/>` and y-axis `<line x1="30" y1="150" x2="30" y2="28" stroke="#fd7e14" stroke-opacity=".35" stroke-width="1.4"/>`.
- 2-4 cubic-Bézier `<path>` elements. Primary path: `stroke-width="3" stroke-linecap="round"`. Secondary/faint paths: `stroke-width="2"` or `"1.6"` with reduced `stroke-opacity` (".4"–".7"). Dashed paths use `stroke-dasharray`.
- Keep every coordinate within x∈[30,298], y∈[28,150] (RESEARCH pitfall 9).
- NO `<title>`, NO `<desc>`, NO `aria-labelledby`, NO `<style>` block, NO `xmlns:xlink`, NO `id` on paths, NO `<g>` wrapper unless grouping shared-stroke paths (RESEARCH pitfalls 2/3). If a `<linearGradient>` is used, give it a unique 2-char id per file (`sr` for shift-registration, `ba` for banded-alignment) to avoid cross-file id collision on the gallery page (RESEARCH pitfall 4).
- Target file size 700–1100 bytes; the aria-label is a short 3-6 word description of what is depicted (e.g. "Shift registration", "Banded elastic alignment"), NOT the full gallery title.

THUMBNAIL 1 — `docs/assets/thumb/shift-registration.svg` (RESEARCH §3 row 1 + §7 shift-registration): two nearly identical single-hump smooth curves, one shifted ~40px right of the other, both at reduced opacity (secondary paths). A third solid bold curve (primary, stroke-width 3) shows the aligned result with peaks coincident. A short horizontal arrow near the top (a faint path plus a small filled arrowhead path with `Z` fill, modelled on elastic-alignment.svg's arrowhead) bridges the gap between the two misaligned peaks. aria-label "Shift registration". Hue `#fd7e14`.

THUMBNAIL 2 — `docs/assets/thumb/banded-alignment.svg` (RESEARCH §3 row 2 + §7 banded-alignment): a diagonal warp-path space — two dashed diagonal `<path>`/`<line>` elements running lower-left to upper-right, flanking the main diagonal, forming a corridor (the Sakoe-Chiba band). A solid bold warping path (primary, stroke-width 3, stroke-linecap round) threads inside the corridor. Optionally a faint main-diagonal reference line. aria-label "Banded elastic alignment". Hue `#fd7e14`. Keep the diagonal within x∈[30,298], y∈[28,150].

SVGO IDEMPOTENCE (RESEARCH §5 — CARD-06, required before insertion): for EACH new thumbnail, from repo root run
`first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input docs/assets/thumb/<slug>.svg --output -)` then
`second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -)` then
`diff <(printf '%s' "$first") <(printf '%s' "$second")` — the diff MUST be empty (pass1==pass2). The committed file stays hand-authored (never overwrite it with SVGO output; the gate checks idempotence of the file on disk, not a normalised form). Config disables convertPathData/mergePaths, so plain integer/simple-float coordinates stay verbatim.

SMOKE RENDER (RESEARCH §5): for each new thumbnail run `rsvg-convert -w 320 -h 180 docs/assets/thumb/<slug>.svg -o /tmp/<slug>.png` — must exit 0 (SVG parses/renders). rsvg-convert is present at /usr/bin/rsvg-convert.

CARD INSERTION into `docs/align/index.md` (RESEARCH §4 — exact template, NO blank lines between tags in this section, href has a trailing slash, src is `../assets/thumb/<slug>.svg`):
1. Insert the `banded-alignment` card block immediately BEFORE the existing `<a class="fdars-gallery-item" href="landmark-registration/">` line (i.e. right after the advanced-alignment card's closing `</a>`):
```
<a class="fdars-gallery-item" href="banded-alignment/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/banded-alignment.svg" alt="">
<div class="fdars-gallery-title">Banded Elastic Alignment</div>
<div class="fdars-gallery-desc">Fast Karcher mean via a diagonal DP band constraint.</div>
</a>
```
2. Insert the `shift-registration` card block immediately BEFORE the existing `<a class="fdars-gallery-item" href="shape-analysis/">` line (i.e. right after the alignment-comparison card's closing `</a>`):
```
<a class="fdars-gallery-item" href="shift-registration/">
<img class="fdars-gallery-thumb" aria-hidden="true" src="../assets/thumb/shift-registration.svg" alt="">
<div class="fdars-gallery-title">Shift Registration</div>
<div class="fdars-gallery-desc">Align curves by a single scalar time-shift per observation.</div>
</a>
```
Do NOT reorder existing cards. Do NOT touch any other section index.md. Do NOT run the whole-site `mkdocs build --strict` (deferred to Phase 79).
  </action>
  <acceptance_criteria>
    - the file docs/assets/thumb/shift-registration.svg exists and the file docs/assets/thumb/banded-alignment.svg exists
    - each new thumbnail contains `viewBox="0 0 320 180"` (grep matches 1 per file)
    - each new thumbnail contains the align hue `#fd7e14` (grep matches >= 1 per file) and contains no other section hex (#198754/#dc3545/#6f42c1/#3f51b5 all match 0)
    - each new thumbnail carries `role="img"` and an `aria-label="` attribute (grep matches >= 1 each per file)
    - neither new thumbnail contains `<title`, `<desc`, `aria-labelledby`, or `<style` (grep matches 0 each)
    - each new thumbnail file is under ~1200 bytes
    - docs/align/index.md contains `href="banded-alignment/"` and `src="../assets/thumb/banded-alignment.svg"` and `href="shift-registration/"` and `src="../assets/thumb/shift-registration.svg"` (grep matches >= 1 each)
    - for each new thumbnail the SVGO two-pass idempotence diff is empty (pass1 == pass2)
    - each new thumbnail smoke-renders via rsvg-convert with exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && node -e 'const fs=require("fs");const idx=fs.readFileSync("docs/align/index.md","utf8");const thumbs=[["shift-registration","Shift registration|Shift Registration"],["banded-alignment","Banded"]];const bad=[];for(const [slug] of thumbs){const f="docs/assets/thumb/"+slug+".svg";if(!fs.existsSync(f)){bad.push(slug+":missing");continue;}const s=fs.readFileSync(f,"utf8");const sz=Buffer.byteLength(s);const checks={viewBox:/viewBox="0 0 320 180"/.test(s),hue:/#fd7e14/i.test(s),no_other_hue:!/#198754|#dc3545|#6f42c1|#3f51b5/i.test(s),role:/role="img"/.test(s),aria:/aria-label="/.test(s),no_title:!/<title/.test(s),no_desc:!/<desc/.test(s),no_labelledby:!/aria-labelledby/.test(s),no_style:!/<style/.test(s),size:sz<1200,card_href:new RegExp("href=\""+slug+"/\"").test(idx),card_src:new RegExp("src=\"../assets/thumb/"+slug+".svg\"").test(idx)};for(const [k,v] of Object.entries(checks))if(!v)bad.push(slug+":"+k);}if(bad.length){console.error("FAIL: "+bad.join(", "));process.exit(1);}console.log("STRUCT OK align 2 thumbnails + 2 cards");'</automated>
    <fails_when>Either thumbnail file is missing; a thumbnail lacks viewBox="0 0 320 180"; a thumbnail lacks the #fd7e14 align hue or carries another section's hex; a thumbnail lacks role="img" or aria-label; a thumbnail contains <title>/<desc>/aria-labelledby/<style>; a thumbnail exceeds ~1200 bytes; or docs/align/index.md is missing either card's href or src.</fails_when>
    <automated>cd /home/simonm/projects/rust/pyfda && rc=0; for slug in shift-registration banded-alignment; do f="docs/assets/thumb/$slug.svg"; first=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "$f" --output -); second=$(printf '%s' "$first" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); if ! diff <(printf '%s' "$first") <(printf '%s' "$second") >/dev/null; then echo "NON-IDEMPOTENT: $slug"; rc=1; fi; rsvg-convert -w 320 -h 180 "$f" -o /dev/null || { echo "RENDER FAIL: $slug"; rc=1; }; done; [ $rc -eq 0 ] && echo "SVGO idempotent + render OK"; exit $rc</automated>
    <fails_when>For either new thumbnail the two SVGO passes differ (non-idempotent — would fail the Phase 79 GATE-02 determinism gate), or rsvg-convert returns non-zero (the SVG does not parse/render).</fails_when>
  </verify>
  <done>docs/assets/thumb/shift-registration.svg and docs/assets/thumb/banded-alignment.svg exist as fresh minimal 320×180 #fd7e14 line-art (role="img" + aria-label, no title/desc/style, <1200 bytes each), both SVGO-idempotent and rsvg-convert-clean, and docs/align/index.md carries their two fdars-gallery cards (banded-alignment after advanced-alignment, shift-registration after alignment-comparison) so the align gallery is at 100% coverage.</done>
  <reversibility rating="reversible">Docs-only: two new SVG files + additive card insertions into one index.md; revertible via git with no downstream artifacts.</reversibility>
</task>

</tasks>

<threat_model>
Docs-only phase — creating two hand-authored static SVG assets and inserting two markdown card blocks into one section index. No code, no network surface, no user-input handling. The SVGs are decorative line-art with no `<script>`, no `foreignObject`, no external references — no new attack surface. No STRIDE categories apply to static documentation editing.
</threat_model>

<verification>
- Structural gate (node one-liner): both thumbnail files exist, each has viewBox="0 0 320 180", the #fd7e14 align hue and no other section hex, role="img" + aria-label, no title/desc/aria-labelledby/style, <1200 bytes; docs/align/index.md carries both cards' href + src.
- Determinism gate (shell loop): each thumbnail's SVGO two-pass diff is empty (idempotent) and rsvg-convert renders it without error.
- Whole-site `mkdocs build --strict` and the consolidated SVGO/determinism GATE-02 are intentionally NOT run here — deferred to Phase 79.
</verification>

<success_criteria>
CARD-01 + CARD-06 (align portion) delivered: the align landing gallery is at 100% card coverage with two new SVGO-idempotent, STYLE_SPEC-conformant, decorative-accessible #fd7e14 thumbnails.
</success_criteria>

<output>
Create `.planning/phases/77-section-landing-card-coverage/77-01-SUMMARY.md` when done.
</output>
---
phase: 04-represent-diagrams
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-represent-diagrams/COVERAGE.md
autonomous: false
requirements: [DIA-02]
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "All 7 represent/ diagrams (fpca, elastic-fpca, basis-representation, andrews-transformation, depth-functions, streaming-depth, distance-metrics) pass the SVGO idempotence gate with exit 0"
    - "All 7 represent/ diagrams carry viewBox 0 0 720, the five CSS classes, system-ui, role=img, and aria-label — proving no legacy-outlier remains in represent/ (depth-functions.svg now conforms after Plan 01)"
    - "basis-representation.svg contains no extendr, autoplot, ggplot, or other R-specific identifiers — the R-era none-remain grep passes"
    - "The 7 represent/ diagrams render on the built site via mkdocs build with no errors"
    - "COVERAGE.md declares no external API integration for this phase"
  artifacts:
    - .planning/phases/04-represent-diagrams/COVERAGE.md
  key_links:
    - "Each represent/ page (fpca.md, elastic-fpca.md, basis-representation.md, andrews-transformation.md, depth-functions.md, streaming-depth.md, distance-metrics.md) embeds its diagram from ../assets/diagrams/*.svg — the built-site render depends on these embeds resolving"
  flagged_assumptions:
    - "adjacency edge — N/A: static hand-authored SVG assets, no runtime input adjacency to resolve."
    - "empty edge — N/A: no runtime input; diagrams are fixed authored assets."
    - "ordering edge — N/A: SVG element order is authored and deterministic; the SVGO gate is idempotent so serialisation is stable."
---

<objective>
Expand the proven verification loop (from Plan 01) across all 7 represent/ diagrams, and produce the section-wide evidence that closes DIA-02.

The Phase 2 audit rated fpca.svg, elastic-fpca.svg, basis-representation.svg, andrews-transformation.svg, streaming-depth.svg, and distance-metrics.svg as accurate + conforming; depth-functions.svg was the one legacy-outlier and was migrated in Plan 01. This plan does NOT redraw the six conforming diagrams — it re-verifies each against the live gate (audit is evidence, not a substitute for running the gate): confirms each passes the SVGO idempotence gate, confirms each carries the STYLE_SPEC markers (proving Success Criterion #4 — no legacy-outlier represent/ diagram remains, now that depth-functions.svg conforms), confirms basis-representation.svg carries no R-era identifiers (Success Criterion #2 — a verify-none-remain, NOT migration work, since the audit already cleared it), confirms all render on the built site, and authors the COVERAGE.md api-coverage declaration required at seal time.

Purpose: Deliver Success Criteria #1, #2, #3, and #4 for the represent/ section and satisfy the api-coverage gate.
Output: `.planning/phases/04-represent-diagrams/COVERAGE.md` and a verification log recorded in the SUMMARY.
</objective>

<user_story>
**As a** reader of the fdars represent/ docs, **I want to** every represent/ page's concept diagram to be style-consistent, R-era-free, and render correctly on the site, **so that** the section reads as one coherent, method-accurate whole.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-audit/02-AUDIT.md
@docs/assets/diagrams/STYLE_SPEC.md
@svgo.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SVGO idempotence gate on all 7 represent/ diagrams (DIA-02, SC#1)</name>
  <files>.planning/phases/04-represent-diagrams/COVERAGE.md</files>
  <read_first>
    - svgo.config.mjs (check-only config; idempotence pattern in header)
    - docs/assets/diagrams/STYLE_SPEC.md (pinned svgo@3.3.4, mandatory --config, stdout-only)
    - docs/assets/diagrams/fpca.svg (spot-read to confirm it is present and well-formed before gating)
  </read_first>
  <action>
    For each of the 7 represent/ diagrams — fpca.svg, elastic-fpca.svg, basis-representation.svg, andrews-transformation.svg, depth-functions.svg (restyled in Plan 01), streaming-depth.svg, distance-metrics.svg — run the pinned SVGO idempotence gate (pass 1 to stdout, feed back for pass 2, diff must be empty). Record pass/fail per file in the SUMMARY. If any file fails idempotence, stop and report — do NOT rewrite it (the gate is check-only). All 7 are expected to pass (the audit and Phase 1 full-corpus result both confirmed conformance, and Plan 01 proved depth-functions.svg gates clean after restyle); this task PROVES it against the live files rather than asserting it.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; for f in fpca elastic-fpca basis-representation andrews-transformation depth-functions streaming-depth distance-metrics; do FIRST=$(npx svgo@3.3.4 --config svgo.config.mjs --quiet --input "docs/assets/diagrams/$f.svg" --output -); SECOND=$(printf '%s' "$FIRST" | npx svgo@3.3.4 --config svgo.config.mjs --quiet --input - --output -); [ "$FIRST" = "$SECOND" ] || { echo "FAIL: $f"; ok=0; }; done; [ "$ok" = 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - All 7 represent/ diagrams pass the SVGO idempotence gate (pass 1 == pass 2), exit 0.
    - No committed SVG is modified by this task (gate is stdout-only). Check: `git diff --stat docs/assets/diagrams/` shows no changes from this task.
  </acceptance_criteria>
  <done>All 7 represent/ diagrams are proven idempotent under svgo.config.mjs; per-file results recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Prove no legacy-outlier and no R-era content remains in represent/, and the section builds (DIA-02, SC#2, SC#3, SC#4)</name>
  <files>.planning/phases/04-represent-diagrams/COVERAGE.md</files>
  <read_first>
    - docs/assets/diagrams/STYLE_SPEC.md (style markers: viewBox 0 0 720, five CSS classes .ttl/.sub/.lab/.sm/.mono, system-ui, role=img, aria-label)
    - .planning/phases/02-audit/02-AUDIT.md (represent/ section table — the audit claims 6 conform and depth-functions was the one outlier now migrated; the R-era sweep confirms zero R-era hits in represent/ diagrams — this task verifies both against the live files)
  </read_first>
  <action>
    Run the style-marker grep across all 7 represent/ diagrams to PROVE none is a legacy outlier (Success Criterion #4 — after Plan 01's depth-functions restyle, all 7 must now conform). For each file assert: `viewBox="0 0 720` present, all five CSS class selectors present in the style block, `system-ui` present, `role="img"` present, and `aria-label` present.

    Then run the R-era none-remain grep (Success Criterion #2 — a verification, not migration; the audit already cleared basis-representation.svg): assert that no represent/ diagram contains `extendr`, `autoplot`, `ggplot`, or the R pipe `%>%`. The audit confirmed basis-representation.svg uses only the current Python API name `fdata_to_basis_1d`; prove no R-era token slipped in. (Note: do NOT touch the andrews-transformation.md PROSE admonition about R's `andrews_transform()` — that is intentional PROSE-OK user guidance, out of scope for this SVG grep.)

    Then run mkdocs build (PYTHONPATH=scripts DOCS_FAST=1 via the repo .venv) and confirm the represent/ pages (fpca, elastic-fpca, basis-representation, andrews-transformation, depth-functions, streaming-depth, distance-metrics) render with no errors. Record per-file marker and R-era results in the SUMMARY. If any style marker is missing or any R-era token is found on any file, stop and report — that would be an unexpected regression requiring migration.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && ok=1; for f in fpca elastic-fpca basis-representation andrews-transformation depth-functions streaming-depth distance-metrics; do p="docs/assets/diagrams/$f.svg"; for m in 'viewBox="0 0 720' '.ttl' '.sub' '.lab' '.sm' '.mono' 'system-ui' 'role="img"' 'aria-label'; do grep -qF "$m" "$p" || { echo "MISSING $m in $f"; ok=0; }; done; done; [ "$ok" = 1 ]</automated>
    <automated>cd /home/simonm/projects/rust/pyfda && rera=0; for f in fpca elastic-fpca basis-representation andrews-transformation depth-functions streaming-depth distance-metrics; do p="docs/assets/diagrams/$f.svg"; for tok in 'extendr' 'autoplot' 'ggplot' '%>%'; do grep -qF "$tok" "$p" && { echo "R-ERA $tok in $f"; rera=1; }; done; done; [ "$rera" = 0 ]</automated>
    <automated>cd /home/simonm/projects/rust/pyfda && PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build 2>&1 | tee /tmp/mkbuild-04-02.log; grep -Eiq 'error|traceback' /tmp/mkbuild-04-02.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 7 represent/ diagrams contains: `viewBox="0 0 720`, `.ttl` `.sub` `.lab` `.sm` `.mono`, `system-ui`, `role="img"`, and `aria-label` — zero missing markers (exit 0). This proves Success Criterion #4 (no legacy-outlier represent/ diagram remains).
    - No represent/ diagram contains `extendr`, `autoplot`, `ggplot`, or `%>%` (exit 0). This proves Success Criterion #2 (R-era none-remain).
    - `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` completes with no `ERROR`/`Traceback` lines.
  </acceptance_criteria>
  <done>All 7 represent/ diagrams carry the full STYLE_SPEC marker set, contain no R-era tokens, and the section builds cleanly; results recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Author COVERAGE.md api-coverage declaration (seal-time gate)</name>
  <files>.planning/phases/04-represent-diagrams/COVERAGE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 scope — confirms this is a diagram-editing phase with no external API)
  </read_first>
  <action>
    Write `.planning/phases/04-represent-diagrams/COVERAGE.md` containing exactly the reasoned declaration required by the api-coverage gate: state that this phase integrates no external API/SDK/service. Include the mandated sentence verbatim: "No external API integration: phase edits hand-authored static SVG diagrams and runs local SVGO/mkdocs tooling only." Add a one-line note that the SVGO lint and mkdocs build are local dev tooling, not networked services.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/pyfda && grep -qF 'No external API integration: phase edits hand-authored static SVG diagrams and runs local SVGO/mkdocs tooling only.' .planning/phases/04-represent-diagrams/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-represent-diagrams/COVERAGE.md` exists and contains the exact declaration sentence (grep -F exit 0).
  </acceptance_criteria>
  <done>COVERAGE.md exists with the exact no-external-API declaration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: represent/ section review gate — all 7 diagrams on the built site</name>
  <what-built>Section-wide verification of all 7 represent/ diagrams: SVGO gate, STYLE_SPEC markers, R-era none-remain grep, and built-site render — plus the depth-functions restyle from Plan 01 in context of the whole section.</what-built>
  <how-to-verify>
    Run `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs serve` and open each represent/ page: /represent/fpca/, /represent/elastic-fpca/, /represent/basis-representation/, /represent/andrews-transformation/, /represent/depth-functions/, /represent/streaming-depth/, /represent/distance-metrics/. For each, confirm the concept diagram renders (no broken image), is legible, is style-consistent with its neighbours, and depicts the method described on the page (Success Criterion #3 — accurate, non-generic diagram visible). Pay special attention to /represent/depth-functions/ — confirm the restyle from Plan 01 reads as one of the section (same typography as fpca/streaming-depth). This is the represent/ section review gate before Phase 5 begins.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 represent/ diagrams render correctly, accurately, and style-consistently on the built site, or list any page whose diagram looks wrong or generic.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
This phase MIGRATES one existing hand-authored SVG and verifies the rest rather than creating new symbols. Artifacts created or modified across both plans:

**Modified:**
- `docs/assets/diagrams/depth-functions.svg` (Plan 01, Task 1 — GAP-0002 restyle to STYLE_SPEC)

**Created:**
- `.planning/phases/04-represent-diagrams/COVERAGE.md` (Plan 02, Task 3 — api-coverage declaration)
- `.planning/phases/04-represent-diagrams/04-01-SUMMARY.md` (Plan 01 execution summary — records GAP-0002 restyle decision + class-mapping choices)
- `.planning/phases/04-represent-diagrams/04-02-SUMMARY.md` (Plan 02 execution summary — per-file SVGO + marker + R-era + build verification log)

**Verified-only (not modified):**
- `docs/assets/diagrams/fpca.svg`, `elastic-fpca.svg`, `basis-representation.svg`, `andrews-transformation.svg`, `streaming-depth.svg`, `distance-metrics.svg` (Plan 02 — SVGO gate + STYLE_SPEC markers + R-era grep + built-site render; all rated accurate+conforming by the audit, re-proven against the live gate)
</artifacts_this_phase_produces>

<threat_model>
No new attack surface. This plan runs check-only SVGO lint (stdout mode), a local mkdocs build, grep marker/R-era checks, and writes one planning markdown file. No runtime user input, no network endpoint, no auth, no data persistence, no new package installs. ASVS L1 is not applicable to this phase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | SVGO gate rewriting a committed hand-authored represent/ SVG | low | mitigate | Gate invoked in stdout mode (`--output -`) only; `git diff --stat docs/assets/diagrams/` acceptance check confirms no SVG is reserialised by the gate run. |
</threat_model>

<verification>
Maps to ROADMAP Phase 4 Success Criteria:
- SC #1 (all represent/ SVG pass SVGO lint): Task 1 gate over all 7.
- SC #2 (basis-representation.svg — and all represent/ diagrams — contain no R-era identifiers, verified by grep and on the built site): Task 2 R-era grep + Task 2 build + Task 4 human review.
- SC #3 (every warranting represent/ page has an accurate, non-generic diagram visible on the built site): Task 2 build + Task 4 human review.
- SC #4 (all legacy-outlier represent/ diagrams migrated — verify none remain): Task 2 STYLE_SPEC marker grep over all 7 (exit 0 proves none remain, depth-functions.svg now conforming).
</verification>

<success_criteria>
- All 7 represent/ diagrams pass the SVGO idempotence gate (Task 1, exit 0).
- All 7 carry the full STYLE_SPEC marker set (Task 2, exit 0) — no legacy outlier remains.
- No represent/ diagram contains R-era tokens (Task 2 R-era grep, exit 0).
- `PYTHONPATH=scripts DOCS_FAST=1 .venv/bin/mkdocs build` succeeds.
- COVERAGE.md exists with the exact no-external-API declaration.
- Human reviewer approves all 7 represent/ diagrams on the built site (section review gate).
</success_criteria>

<output>
Create `.planning/phases/04-represent-diagrams/04-02-SUMMARY.md` when done. Record the per-file SVGO pass/fail, per-file STYLE_SPEC marker results, the per-file R-era grep results, the mkdocs build result, and the COVERAGE.md declaration.
</output>

% arXiv: force pdflatex/PDF output mode (arXiv scans the first lines for this).
% Guard on \pdftexversion (defined ONLY under pdfTeX, i.e. arXiv's engine), NOT
% \pdfoutput: tectonic/XeTeX (our CI) also defines \pdfoutput, so guarding on it
% would set \pdfoutput=1 under XeTeX, make hyperref load the pdfTeX driver
% (hpdftex.def), and crash on undefined pdfTeX primitives. This form is a no-op
% under XeTeX/LuaTeX while still exposing the literal \pdfoutput=1 arXiv scans for.
\ifdefined\pdftexversion\pdfoutput=1\fi
% paper/paper.tex — fdars software paper (arXiv preprint source).
% Bibliography: natbib + BibTeX (plainnat.bst; arXiv runs bibtex, CI uses tectonic).
% The date field is empty intentionally for deterministic output.
\documentclass[12pt,a4paper]{article}

% Bibliography: natbib + BibTeX (plain BibTeX only)
\usepackage[numbers,sort&compress]{natbib}

% Basic packages for a software paper
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
% float: provides the [H] placement specifier so capability-tour and case-study
% plots stay exactly with their code snippet / prose (no floating into the next
% subsection). In arXiv's TeX Live.
\usepackage{float}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{adjustbox}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds}
% hyperref is loaded LATE (after the packages above) so it patches them correctly.
% colorlinks makes internal cross-references, citations, and URLs clearly clickable
% (the default bordered links are easy to miss). xurl loads AFTER hyperref and
% breaks long URLs/DOIs in the bibliography at arbitrary characters.
\PassOptionsToPackage{hyphens}{url}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue,linktoc=all]{hyperref}
\usepackage{xurl}

% Listing styles for executed capability-tour snippets (paper/snippets/*.tex).
% fdarsinput  — Python source block: light-tinted box, syntax highlighting.
% fdarsoutput — Captured stdout block: grey, left-ruled, visually attached below.
% Colours (xcolor is pulled in by tikz).
\definecolor{codeback}{RGB}{247,248,250}
\definecolor{codeframe}{RGB}{203,213,225}
\definecolor{codekw}{RGB}{37,99,235}
\definecolor{codestr}{RGB}{22,128,61}
\definecolor{codecom}{RGB}{107,114,128}
\definecolor{codeout}{RGB}{243,244,246}
\lstdefinestyle{fdarsinput}{
  language=Python,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{codekw}\bfseries,
  stringstyle=\color{codestr},
  commentstyle=\color{codecom}\itshape,
  backgroundcolor=\color{codeback},
  frame=single,
  rulecolor=\color{codeframe},
  framesep=5pt,
  framexleftmargin=5pt,
  xleftmargin=10pt,
  xrightmargin=13pt,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  breaklines=true,
  breakatwhitespace=false,
  breakindent=0pt,
  postbreak=\mbox{\textcolor{codecom}{$\hookrightarrow$}\space},
  aboveskip=1.0\medskipamount,
  belowskip=0.3\medskipamount,
}
\lstdefinestyle{fdarsoutput}{
  basicstyle=\ttfamily\small\color{black!78},
  backgroundcolor=\color{codeout},
  frame=leftline,
  rulecolor=\color{codeframe},
  framesep=5pt,
  framexleftmargin=5pt,
  xleftmargin=10pt,
  xrightmargin=13pt,
  columns=fullflexible,
  keepspaces=true,
  breaklines=true,
  breakatwhitespace=false,
  aboveskip=0.2\medskipamount,
  belowskip=1.0\medskipamount,
}
% UTF-8 literate map: typeset Fdata repr's en-dash and multiplication sign
% cleanly inside listings (no special TeX escaping needed in the .tex source).
\lstset{
  literate=
    {--}{{--}}2
    {–}{{--}}1
    {×}{{$\times$}}1,
}

\title{fdars: Functional Data Analysis in Rust with Python Bindings}
\author{Simon M\"{u}ller\\
  \texttt{sm@data-zoo.de}}
% Empty date for deterministic output.
\date{}

% Machine-derived coverage macros — generated by paper/code/assert_coverage.py
\input{coverage_counts}

% Allow TeX an extra 3em of stretch to relieve overfull \hbox{} in prose
% (long \texttt{} identifiers and DOI strings that resist hyphenation).
\emergencystretch=3em

% Let long code identifiers/paths in \texttt{} wrap: allow a break after every
% underscore (e.g. python/fdars/_references_map.json breaks at _references / _map,
% fdars.pace_fpca.irreg_fdata_from_lists at each underscore). No hyphen is
% inserted — the right behaviour for code tokens.
\renewcommand{\_}{\textunderscore\allowbreak}

\begin{document}
\maketitle

\begin{abstract}
\input{sections/abstract}
\end{abstract}

\input{sections/intro}
\input{sections/design}
\input{sections/represent}
\input{sections/capabilities}
\input{sections/comparison}
\input{sections/advisor}
\input{sections/casestudies}
\input{sections/validation}
\input{sections/availability}
\input{sections/conclusion}

% plainnat (not plain): natbib-compatible numeric style so \citet renders author
% names ("Ramsay and Silverman [8]"); plain.bst omits the metadata natbib needs
% and produces "(author?)".
\bibliographystyle{plainnat}
\bibliography{refs,refs_manual}

\end{document}
